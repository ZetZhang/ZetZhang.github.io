<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>1 基础议题</title>
    <url>/2020/06/14/1-%E5%9F%BA%E7%A1%80%E8%AE%AE%E9%A2%98/</url>
    <content><![CDATA[<h4 id="条款1：指针与引用的区别"><a href="#条款1：指针与引用的区别" class="headerlink" title="条款1：指针与引用的区别"></a>条款1：指针与引用的区别</h4><ol>
<li>当你知道你必须指向一个对象并且不想改变其指向时，或者在重载符并为防止不必要的语义误解时，你不应该使用指针。除此之外的情况下，则使用指针。</li>
</ol>
<h4 id="条款2：尽量使用C-风格的类型转换"><a href="#条款2：尽量使用C-风格的类型转换" class="headerlink" title="条款2：尽量使用C++风格的类型转换"></a>条款2：尽量使用C++风格的类型转换</h4><ol>
<li>将一个pointer-to-const-object转型为一个pointer-to-non-const-object（改变对象常量性，和将一个pointer-to-base-class-object转型为一个pointer-to-derived-class-object（改变对象类型）有很大差异。而传统C转型动作并无区分，不过旧式的C转型动作并非唯一选择，C式转型式为C设计的，不是为了C++。C++导入4个新的<font color="red">转型操作符</font>（<font color="green">cast operators</font>）：<code>static_cast</code>、<code>const_cast</code>、<code>dynamic_cast</code>和<code>reinterpret_cast</code>。</li>
<li><font color="red">static_cast</font>具有与C旧式转型相同的威力与意义，以及相同的限制。static_cast不能移除表达式的常量性（constness），因为有const_cast。</li>
<li>其他新式C++转型操作符适用于更集种（范围更狭窄）的目的。<font color="red">const_cast用来改变表达式中的常量性（constness）或变易性（volatileness）。常见的用途在于将某个对象的常量性去掉。</font></li>
<li><font color="red">dynamic_cast</font>用来执行继承体系中“<strong>安全的向下转型或跨转型动作</strong>”。它不能应用在缺乏虚函数的类型身上。</li>
<li><font color="red">reinterpret_cast</font>这个于编译平台有关，基本不具移植性。它的常用用途是转换“函数指针”类型： <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*FuncPtr)</span><span class="params">()</span></span>;</span><br><span class="line">FuncPtr funcPtrArray[<span class="number">10</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">doSomething</span><span class="params">()</span></span>;</span><br><span class="line">funcPtrArray[<span class="number">0</span>] = &amp;doSomething; <span class="comment">// error</span></span><br><span class="line">funcPtrArray[<span class="number">0</span>] = <span class="keyword">reinterpret_cast</span>&lt;FuncPtr&gt;(&amp;doSomething);  <span class="comment">//ok</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款3：绝对不要以多态（polymorphically）方式处理数组"><a href="#条款3：绝对不要以多态（polymorphically）方式处理数组" class="headerlink" title="条款3：绝对不要以多态（polymorphically）方式处理数组"></a>条款3：绝对不要以多态（polymorphically）方式处理数组</h4><ol>
<li><p><font color="red">继承</font>（<font color="green">inheritance</font>）的最重要性质之一是可以通过指向base class objects的pinters或references来操作derived class objects。我们说它行为是<font color="red">多态的</font>（<font color="green">polymorphically</font>）。它也允许以上形成的数组。但这不值得沾沾自喜，因为它几乎不会如预期般运作。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BST</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BalancedBST</span> :</span> <span class="keyword">public</span> BST &#123; ... &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printBSTArray</span><span class="params">(<span class="built_in">std</span>::ostream &amp;s, <span class="keyword">const</span> BST <span class="built_in">array</span>[], <span class="keyword">int</span> numElements)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; numElements; ++i)</span><br><span class="line">        s &lt;&lt; <span class="built_in">array</span>[i];      <span class="comment">// 假设BST objects有个operator&lt;&lt;可用</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">BST BSTArray[<span class="number">10</span>];</span><br><span class="line">printBSTArray(<span class="built_in">std</span>::<span class="built_in">cout</span>, BSTArray, <span class="number">10</span>); <span class="comment">// ok</span></span><br><span class="line"><span class="comment">// 编译器可能会被误导，它假设数组元素大小是BST的大小，通常derived classes比base classes大。</span></span><br><span class="line"><span class="comment">// 那么会发生不可预期的事情？？</span></span><br><span class="line">BalancedBST bBSTArray[<span class="number">10</span>];</span><br><span class="line">printBSTArray(<span class="built_in">std</span>::<span class="built_in">cout</span>, bBSTArray, <span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">deleteArray</span><span class="params">(<span class="built_in">std</span>::ostream &amp;logStream, BST <span class="built_in">array</span>[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    logStream &lt;&lt; <span class="string">"Deleting array at address "</span></span><br><span class="line">              &lt;&lt; stati_cast&lt;<span class="keyword">void</span>*&gt;(<span class="built_in">array</span>) &lt;&lt; <span class="string">'\n'</span>;</span><br><span class="line">    <span class="keyword">delete</span> []<span class="built_in">array</span>;</span><br><span class="line">&#125;</span><br><span class="line">BalancedBST &amp;balTreeArray = <span class="keyword">new</span> BalancedBST[<span class="number">50</span>];</span><br><span class="line">deleteArray(<span class="built_in">std</span>::<span class="built_in">cout</span>, balTreeArray);</span><br><span class="line"><span class="comment">// delete []array会产生这样的代码</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">for (int i = the number of elements in the array -1; i &gt;= 0; --i)</span></span><br><span class="line"><span class="comment">    array[i].BST::~BST();   // 调用array[i]的destructor</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<p> 在C++语言规范中说，<strong>通过base class指针删除一个由derived classes objects构成的数组，其结果未定义</strong>。</p>
</li>
</ol>
<h4 id="条款4：非必要不提供default-constructor"><a href="#条款4：非必要不提供default-constructor" class="headerlink" title="条款4：非必要不提供default constructor"></a>条款4：非必要不提供default constructor</h4><ol>
<li><p>所谓default constructor是C++一种无中生有的方式。constructors用来将对象初始化，所以default constructors的意思是在没有任何外来信息的情况将对象初始化。然而有些对象如果没有外来信息，是无法初始化的，可能产生的对象没有意义。</p>
</li>
<li><p>如果class缺乏一个default constructor，使用class时会有限制，第一个问题在产生<font color="red">数组</font>的时候。例如：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EquipmentPiece</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    EquipmentPiece(<span class="keyword">int</span> IDNumber);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">EquipmentPiece bestPieces[<span class="number">10</span>];  <span class="comment">// error</span></span><br><span class="line">EquipmentPiece *bestPieces = <span class="keyword">new</span> EquipmentPiece[<span class="number">10</span>]; <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 在产生数组的时候，一般而言没有任何方法可以为数组中的对象指定constructor自变量。有三个方法可以解决这个束缚：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个方式是使用non-heap数组</span></span><br><span class="line"><span class="comment">// 不幸的是此法无法延伸至heap数组</span></span><br><span class="line"><span class="keyword">int</span> ID1, ID2, ID3, ..., ID10;  </span><br><span class="line">...</span><br><span class="line">EquipmentPiece bestPieces[] = </span><br><span class="line">&#123;</span><br><span class="line">    EquipmentPiece(ID1)，</span><br><span class="line">    EquipmentPiece(ID2)，</span><br><span class="line">    EquipmentPiece(ID3)，</span><br><span class="line">    ...,</span><br><span class="line">    EquipmentPiece(ID10)</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 更一般化的做法是使用“指针数组”而非“对象数组</span></span><br><span class="line"><span class="comment">// 此法有两个缺点。</span></span><br><span class="line"><span class="comment">// 第一，必须记得此数组所指的所有对象删除，否则会出现resource leak（资源泄露）</span></span><br><span class="line"><span class="comment">// 第二，需要足够大的内存总量，需要一些空间来放置指针和一些空间来放置EqupmentPiece objects</span></span><br><span class="line"><span class="keyword">typedef</span> EquipmentPiece *PEP;</span><br><span class="line">PEP bestPieces[<span class="number">10</span>]; <span class="comment">// ok，不需要调用ctor</span></span><br><span class="line">PEP *bestPieces = <span class="keyword">new</span> PEP[<span class="number">10</span>];  <span class="comment">// ok</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i)</span><br><span class="line">    bestPieces[i] = <span class="keyword">new</span> EquipmentPiece(ID number);</span><br></pre></td></tr></table></figure>

<p> 过度使用内存这个问题可以避免：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 分配足够的raw memory</span></span><br><span class="line"><span class="keyword">void</span> *rawMemory = <span class="keyword">operator</span> <span class="keyword">new</span>[](<span class="number">10</span> * <span class="keyword">sizeof</span>(EquipmentPiece));</span><br><span class="line"><span class="comment">// 让bestPieces指向此块内存，将这块内存视为一个EquipmentPiece数组</span></span><br><span class="line">EquipmentPiece *bestPiece = <span class="keyword">static_cast</span>&lt;EquipmentPiece*&gt;(rawwMemory);</span><br><span class="line"><span class="comment">// 利用“placement new”构造这块内存中的objects</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i)</span><br><span class="line">    <span class="keyword">new</span>(&amp;bestPieces[i]) EquipmentPiece(ID number);</span><br></pre></td></tr></table></figure>

<p> placement new的缺点是，维护起来比较困难。此外，在数组内的对象结束生命时，以手动方式调用其destructors，最后还得调用operator delete[]的方式释放raw memory：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 相反顺序析构掉</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">    bestPieces[i].~EquipmentPiece();</span><br><span class="line"><span class="comment">// free raw memroy</span></span><br><span class="line"><span class="keyword">operator</span> <span class="keyword">delete</span>[](rwaMemory);</span><br></pre></td></tr></table></figure>


</li>
</ol>
<ol start="3">
<li><p>classes缺乏default constructors带来的第二个缺点是：它们不适用于许多<font color="red">template-based container classes</font>。对template而言，<font color="red">被实例化</font>（<font color="green">instantiated</font>）的目标类型必须有一个deault constructors。这是一个普遍的共同需求，那些templates内几乎总是会产生一个以“template类型参数”作为类型而架构起来的数组，例如：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">Array</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Array(<span class="keyword">int</span> size);</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T *data;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">Array</span>&lt;T&gt;:</span>:Array(<span class="keyword">int</span> size)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 数组中的每个元素都调用T::T()</span></span><br><span class="line">    data = <span class="keyword">new</span> T[size];</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 如果谨慎设计，可以消除对default constructor的需求。标准库中vector template（产生可扩展数组的各种classes）就不要求default constructor。</p>
</li>
<li><p>最后一个考虑点是<font colro="red">virtual base classes</font>。因为virtual base class constructors的自变量由最深层次的派生类的class提供。于是，一个缺乏default constructor的virtual base class，要求所有derived classes都必须知道、了解其意义，并且提供自变量。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>2 构造、析构、赋值运算</title>
    <url>/2020/06/14/2-%E6%9E%84%E9%80%A0%E3%80%81%E6%9E%90%E6%9E%84%E3%80%81%E8%B5%8B%E5%80%BC%E8%BF%90%E7%AE%97/</url>
    <content><![CDATA[<h4 id="条款05：了解C-默默编写并调用哪些函数"><a href="#条款05：了解C-默默编写并调用哪些函数" class="headerlink" title="条款05：了解C++默默编写并调用哪些函数"></a>条款05：了解C++默默编写并调用哪些函数</h4><ol>
<li>如果自己没声明，编译器会自动声明（编译器版本）的<font color="red">copy构造函数</font>、<font color="red">copy assignment操作符</font>和<font color="red">析构函数</font>。如果没有声明构造函数，编译器也会声明一个<font color="red">default构造函数</font>。</li>
<li>如果某个base classes将copy assignment操作符声明为private，编译器将拒绝为其derived classes生成一个copy assignment操作符。</li>
<li>如果一个类中有reference或pointer类型的数据。那么对于copy assignment操作符需要自己定义，而不是使用编译器生成版本。</li>
</ol>
<h4 id="条款06：若不想使用编译器自动生成的函数，就该明确拒绝"><a href="#条款06：若不想使用编译器自动生成的函数，就该明确拒绝" class="headerlink" title="条款06：若不想使用编译器自动生成的函数，就该明确拒绝"></a>条款06：若不想使用编译器自动生成的函数，就该明确拒绝</h4><ol>
<li>为了组织copying动作可以设计一个base class并且不予以实现：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Uncopyable</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Uncopyable() &#123;&#125;</span><br><span class="line">    ~Uncopyable() &#123;&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Uncopyable(<span class="keyword">const</span> Uncopyable&amp;);</span><br><span class="line">    Uncopyable&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Uncopyable&amp;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HomeForSale</span> :</span> <span class="keyword">private</span> Uncopyable</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// class不再声明copy构造函数或copy assignment操作符</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;这行得通，甚至是member函数或friend函数——尝试拷贝HomeForSale对象，编译器便试图生成一个copy构造函数和一个copy assignment操作符，生成版会尝试调用base class的对应函数，而调用被拒绝，因为拷贝函数是private。</p>
<h4 id="条款07：为多态基类声明virtual析构函数"><a href="#条款07：为多态基类声明virtual析构函数" class="headerlink" title="条款07：为多态基类声明virtual析构函数"></a>条款07：为多态基类声明virtual析构函数</h4><ol>
<li>给base class一个virtual析构函数，此后删除derived class对象就会销毁整个对象。如果class不含virtual函数，通常表示意图不被做一个base class。</li>
<li>想实现出virtual函数，对象必须携带某些信息，主要用来在运行期决定哪一个virtual函数该被调用。它由一个<font color="red">vptr</font>（<font color="green">virtual table pointer</font>）指向一个由函数指针构成的数组，称为<font color="red">vtbl</font>（<font color="green">virtual table</font>）；每个带有virtual函数的class都有一个相应的vtbl。当对象调用virtual函数时，编译器在其中寻找适当的函数指针。给base classes一个virtual析构函数这个规则只适用于<font color="green">polymorphic</font>（<font color="red">带多态性质的</font>）base classes身上。</li>
</ol>
<h4 id="条款08：别让异常逃离析构函数"><a href="#条款08：别让异常逃离析构函数" class="headerlink" title="条款08：别让异常逃离析构函数"></a>条款08：别让异常逃离析构函数</h4><ol>
<li>对于析构函数中可能抛出的异常，由两种办法可以避免。一是如果抛出异常就结束程序，通常通过在析构函数调用<code>abort</code>抢先制不明确行为于死地；二是吞下它。</li>
<li>如果用户需要对某个操作函数运行期间的异常做出反应，应该提供一个普通函数来操作。然后在结束时析构函数来判断二重保险。</li>
</ol>
<h4 id="条款09：绝不在构造和析构过程中调用virtual函数"><a href="#条款09：绝不在构造和析构过程中调用virtual函数" class="headerlink" title="条款09：绝不在构造和析构过程中调用virtual函数"></a>条款09：绝不在构造和析构过程中调用virtual函数</h4><ol>
<li>由于base class构造函数的执行更早于derived class构造函数，当base class构造函数执行时derived class的成员变量尚未初始化。如果此期间调用的virtual函数下降至derived classes阶层，那些成员变量尚未初始化，这将产生未定义行为。</li>
<li>在构造和析构期间不要调用virtual函数，因为这类调用从不下降至derived class。</li>
</ol>
<h4 id="条款10：令operator-返回一个reference-to-this"><a href="#条款10：令operator-返回一个reference-to-this" class="headerlink" title="条款10：令operator=返回一个reference to *this"></a>条款10：令operator=返回一个reference to *this</h4><h4 id="条款11：在operator-中处理“自我赋值”"><a href="#条款11：在operator-中处理“自我赋值”" class="headerlink" title="条款11：在operator=中处理“自我赋值”"></a>条款11：在operator=中处理“自我赋值”</h4><ol>
<li>如果operator=函数内的*this和赋值的来源是同一个对象，潜在的delete操作可能就摧毁了全部对象。因此需要注意在复制之前要么数据最好别删除，要么通过测试是否自我赋值。</li>
<li>在operator=函数内的替换方案最好是使用所谓的copy and swap技术。<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Widget&amp; Widget::<span class="keyword">operator</span>=(Widget rhs)</span><br><span class="line">&#123;</span><br><span class="line">    swap(rhs);</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款12：赋值对象时勿忘其每一个成分"><a href="#条款12：赋值对象时勿忘其每一个成分" class="headerlink" title="条款12：赋值对象时勿忘其每一个成分"></a>条款12：赋值对象时勿忘其每一个成分</h4><ol>
<li>当你编写一个copying函数，要确保复制所有local成员变量，且调用所有base calsses内的适当的copying函数。</li>
<li>对于为了消除复制代码的重复性，不该用copy assignment操作符调用copy构造函数，反之亦然。消除重复代码的做法是，建立一个新的成员函数给两者调用。这样的函数往往是private而且被命名为init。</li>
</ol>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>1 让自己习惯C++</title>
    <url>/2020/06/14/1-%E8%AE%A9%E8%87%AA%E5%B7%B1%E4%B9%A0%E6%83%AFC/</url>
    <content><![CDATA[<h4 id="条款01-视C-为一个语言联邦"><a href="#条款01-视C-为一个语言联邦" class="headerlink" title="条款01: 视C++为一个语言联邦"></a>条款01: 视C++为一个语言联邦</h4><ol>
<li>C++是一个<font color="red">多重范式编程语言</font>（<font color="green">multiparadigm programming language</font>），它是一个支持<font color="red">过程形式</font>（<font color="green">procedural</font>）、<font color="red">面向对象形式</font>（<font color="green">object-oriented</font>）、<font color="red">函数形式</font>（<font color="green">functional</font>）、<font color="red">泛型形式</font>（<font color="green">generic</font>）、<font color="red">元编程形式</font>（<font color="green">metaprogramming</font>）的语言。</li>
<li>为了理解C++可以从四个方面入手：<ul>
<li>C。区块（blocks）、语句（statements）、预处理器（preprocessor）、内置数据类型（built-in data types）、数组（arrays）、指针（pointers）。</li>
<li>Object-Oriented C++。classes（构造函数和析构函数），封装（encapsulation）、继承（inheritance）、多态（polymorphism）、virtual函数（动态绑定）。</li>
<li>Template C++。</li>
<li>STL。容器（containers）、迭代器（iterators）、算法（algorithms）以及函数对象（function objects）。</li>
</ul>
</li>
</ol>
<h4 id="条款02：-尽量以const，enum，inline替换-define"><a href="#条款02：-尽量以const，enum，inline替换-define" class="headerlink" title="条款02： 尽量以const，enum，inline替换#define"></a>条款02： 尽量以const，enum，inline替换#define</h4><ol>
<li><code>#define</code>记号也许在编译器开始处理源码前就被预处理器移走了。于是可能记号没有进入<font color="red">记号表</font>（<font color="green">symbol table</font>）内。</li>
<li>当我们以常量替换<code>#define</code>，如果要在头文件内定义一个常量<code>char*</code>-based字符串，必须写const两次（顶层const和底层const）：<code>const char* const authorName = &quot;Scott Meyers&quot;;</code>但通常用string对象更合宜；如果是class专属常量，为了确保常量至多只有一份，必须称为一个static成员。  </li>
<li>如果不想别人获得一个pointer或reference指向整数常量，enum可以帮助实现这个约束。</li>
<li>用<code>template&lt;typename T&gt;inline void callWithMax(cons T &amp;a, const T &amp;b);</code>来代替<code>#define</code>定义的仿函数，这种<font color="red">template inline函数</font>不仅获得宏带来的效率还有一般函数的所有可预料行为和类型安全性（type safety）。</li>
</ol>
<h4 id="条款03：尽可能使用const"><a href="#条款03：尽可能使用const" class="headerlink" title="条款03：尽可能使用const"></a>条款03：尽可能使用const</h4><ol>
<li>const多才多艺，可以在classes外部修饰global或namespace中的常量，或修饰文件、函数、或区块作用域（block scope）中被声明为static的对象。也可以修饰calsses内部的static和non-static成员变量。面对指针，可以指出指针自身、指针所指物，或两者都不是。</li>
<li>STL迭代器系是以指针塑膜出来，作用跟T*指针一样。声明迭代器为const表示迭代器不指向不同的东西（T *const指针）。但是希望迭代器所指的东西不可改动（模拟const T*指针），需要的是<font color="red"><code>const_iterator</code></font>。</li>
<li><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// operator[] for const 对象</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="built_in">std</span>::<span class="keyword">size_t</span> position) <span class="keyword">const</span>  </span><br><span class="line">    &#123; <span class="keyword">return</span> text[position]; &#125;</span><br><span class="line">    <span class="comment">// operator[] for non-const对象</span></span><br><span class="line">    <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="built_in">std</span>::<span class="keyword">size_t</span> position)  </span><br><span class="line">    &#123; <span class="keyword">return</span> text[position]; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> text;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;TextBlock的operator[]可这么使用：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">TextBlock <span class="title">tb</span><span class="params">(<span class="string">"Hello"</span>)</span></span>;</span><br><span class="line"><span class="comment">// 调用non-const TextBlock::operator[]</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; tb[<span class="number">0</span>]; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">const</span> TextBlock <span class="title">ctb</span><span class="params">(<span class="string">"World"</span>)</span></span>;</span><br><span class="line"><span class="comment">// 调用const TextBlock::operator[]</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; ctb[<span class="number">0</span>];</span><br></pre></td></tr></table></figure>

<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;注意：non-const operator[]的返回类型是reference to cahr，不是char。因为如果返回类型是个内置类型，那么改动返回值就不合法，即使合法，C++以by value返回对象意味着返回值是副本，而不是本身。<br>4. <font color="red"><code>mutable</code>成员变量</font>总是会被更改，即使在const成员函数内。<br>5. 当const和non-const成员函数有着实质等价的实现时，令non-const版本调用const版本可避免代码重复。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="built_in">std</span>::<span class="keyword">size_t</span> position) <span class="keyword">const</span></span><br><span class="line">    &#123;</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">return</span> text[position];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="built_in">std</span>::<span class="keyword">size_t</span> position)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 将op[]返回值的const转除；为*this加上const；调用const op[]</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">const_cast</span>&lt;<span class="keyword">char</span>&amp;&gt;(</span><br><span class="line">            <span class="keyword">static_cast</span>&lt;<span class="keyword">const</span> TextBlock&amp;&gt;(*<span class="keyword">this</span>)</span><br><span class="line">                [position]</span><br><span class="line">            );</span><br><span class="line">    &#125;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="条款04：确定对象被使用前已先被初始化"><a href="#条款04：确定对象被使用前已先被初始化" class="headerlink" title="条款04：确定对象被使用前已先被初始化"></a>条款04：确定对象被使用前已先被初始化</h4><ol>
<li>永远在使用对象之前将它初始化。对于无任何成员的内置类型，必须手工完成，因为C++不保证初始化它们。</li>
<li>构造函数最好使用<font color="red">成员初值列</font>（<font color="green">member initialization list</font>）</li>
<li><font color="red">non-local static</font>对象被<font color="red">local static</font>对象替换。以“函数调用”替换“直接访问non-local static对象“。<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FileSYstem</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="function">FileSystem&amp; <span class="title">tfs</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> FileSystem fs;</span><br><span class="line">    <span class="keyword">return</span> fs;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Directory</span> &#123;</span> ... &#125;;</span><br><span class="line">Directory::Directory(params)</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="built_in">std</span>::<span class="keyword">size_t</span> disks = tfs().numDisks();</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">Directory&amp; <span class="title">tempDir</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Directory td;</span><br><span class="line">    <span class="keyword">return</span> td;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;这些<font color="red">reference-returning</font>函数往往十分单纯，这些函数”内涵static对象“的事实使它们在多线程系统中带有不确定性。此时的做法是：在程序的单线程启动阶段（single-threaded startup portion）手工调用所有reference-returning函数，可消除与初始化有关的”&lt;/font color=red&gt;竞速形势（race conditions）“。</p>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>2 操作符</title>
    <url>/2020/06/14/2-%E6%93%8D%E4%BD%9C%E7%AC%A6/</url>
    <content><![CDATA[<h4 id="条款5：对定制的“类型转换函数”保持警觉"><a href="#条款5：对定制的“类型转换函数”保持警觉" class="headerlink" title="条款5：对定制的“类型转换函数”保持警觉"></a>条款5：对定制的“类型转换函数”保持警觉</h4><ol>
<li><p>C++允许编译器在不同类型之间执行<font color="red">隐式转换</font>（<font color="green">implicit conversions</font>）。它继承C的传统，允许将char转换为int，将short转换为double。然而还有令人害怕的转型，包括将int转换为short，将double转换为char，这些类型转换会遗失信息。所以我们可以使用自己的类型。两种函数允许编译器执行这样的转换：<strong>单自变量constructors</strong>和<strong>隐式类型转换操作符</strong>。所谓的单自变量constructor是指能够以单一自变量成功调用的constructors：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Name</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// string to Name</span></span><br><span class="line">    Name(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;s);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// Rational to double</span></span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">double</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> ，而隐式类型转换操作符，是一个拥有奇怪名称的member function：关键词operator后面加上一个类型名称，你不能为此函数指定返回值类型，因为其返回值类型已经表现在函数名称上了：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">double</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function">Rational <span class="title">r</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>)</span></span>;   <span class="comment">// r == 1/2</span></span><br><span class="line"><span class="keyword">double</span> d = <span class="number">0.5</span> * r; <span class="comment">// 将r转为double，然后执行乘法运算</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>假设有一个class用来表现分数（rational numbers）。你希望像内建类型一样地输出Rational objects内容。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Rational <span class="title">r</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>)</span></span>;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; r;</span><br></pre></td></tr></table></figure>

<p> 假设忘了给Rational写一个operator&lt;&lt;，上述打印不但不会出错，编译器在面对上述动作，它会想尽各种办法（包括找出一系列可接受的隐式类型转换）让函数调用动作成功。而之间的代码发现，只要调用了Rational::oeprator double，将r转换为double，调用动作就能成功，却又有隐式类型转换操作符的缺点：它的出现可能到导致错误（非预期）的函数被调用。于是有了解决办法：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">double</span> <span class="title">asDouble</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; r;             <span class="comment">// error</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; r.asDouble();  <span class="comment">// ok, 以double形式输出r</span></span><br></pre></td></tr></table></figure>

<p> 必须明白调用类型转换函数虽然有点不便，却可以避免默默调用了不想调用的函数。这也就是为什么标准程序库的string类型并未含有从string object到C-style char*的隐式转换函数，而是提供了一个显式的member function <code>c_str</code>。<br> 在单自变量constructors，这些函数造成的隐式类型转换的情况更难对付：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">Array</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Array(<span class="keyword">int</span> lowBound, <span class="keyword">int</span> highBound);</span><br><span class="line">    Array(<span class="keyword">int</span> size);</span><br><span class="line">    T&amp; <span class="keyword">operator</span>[](<span class="keyword">int</span> index);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">bool</span> <span class="keyword">operator</span>==(<span class="keyword">const</span> Array&lt;<span class="keyword">int</span>&gt; &amp;lhs, <span class="keyword">const</span> Array&lt;<span class="keyword">int</span>&gt; &amp;rhs);</span><br><span class="line">Array&lt;<span class="keyword">int</span>&gt; a(<span class="number">10</span>);</span><br><span class="line">Array&lt;<span class="keyword">int</span>&gt; b(<span class="number">10</span>);</span><br><span class="line">...</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i)</span><br><span class="line">    <span class="keyword">if</span> (a == b[i])      <span class="comment">// 如果打算写a[i] == b[i]</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">do</span> something <span class="keyword">for</span> when a[i] <span class="keyword">and</span> b[i] are equal;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">        <span class="keyword">do</span> something <span class="keyword">for</span> when they're <span class="keyword">not</span>;</span><br></pre></td></tr></table></figure>

<p> 这样的错误编译器并没有提醒，它一声不吭，编译器注意到它，发现只要调用Array&lt;int&gt; constructor（需要一个int自变量）就可以将int转为Array&lt;int&gt; object。于是它放手去做。<br> 有两种办法阻止编译器这么做：一个是简易法，另一个可在编译器不支持简易法的情况下使用。<br> 简易法是使用C++特征：关键词<font color="red"><code>explicit</code></font>。只要将constructors声明为explicit，编译器就能因隐式转换而调用它，不过显式类型转换是允许的（没发现）。有个解决方案：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">Array</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">ArraySize</span></span></span><br><span class="line"><span class="class">    &#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        ArraySize(<span class="keyword">int</span> numElements) :  theSize(numElements) &#123;&#125;</span><br><span class="line">        <span class="function"><span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> theSize; &#125;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="keyword">int</span> theSize;</span><br><span class="line">    &#125;;</span><br><span class="line">    Array(<span class="keyword">int</span> lowBound, <span class="keyword">int</span> highBound);</span><br><span class="line">    Array(ArraySize size);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">bool</span> <span class="keyword">operator</span>==(<span class="keyword">const</span> Array&lt;<span class="keyword">int</span>&gt; &amp;lhs, <span class="keyword">const</span> Array&lt;<span class="keyword">int</span>&gt; &amp;rhs);</span><br><span class="line">Array&lt;<span class="keyword">int</span>&gt; a(<span class="number">10</span>);</span><br><span class="line">Array&lt;<span class="keyword">int</span>&gt; b(<span class="number">10</span>);</span><br><span class="line">...</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i)</span><br><span class="line">    <span class="keyword">if</span> (a == b[i])  <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 编译器需要一个Array&lt;int&gt;的对象在==右边，但此时没有这样隐式转换的constructor。更不能将int转换一个<strong>临时性</strong>的ArraySize对象，再根据这个临时对象产生必要的Array&lt;int&gt;对象，所以报错。类似ArraySize这样的classes，往往被称为proxy classes，因为它的每一个对象都是为了其他对象而存在的，好像其他对象的<font cololr="red">代理人</font>（<font color="green">proxy</font>）一样。这是一个很值得学习的技术。</p>
</li>
</ol>
<h4 id="条款6：区别increment-decrement操作符的前置（prefix）和后置（postfix）形式"><a href="#条款6：区别increment-decrement操作符的前置（prefix）和后置（postfix）形式" class="headerlink" title="条款6：区别increment/decrement操作符的前置（prefix）和后置（postfix）形式"></a>条款6：区别increment/decrement操作符的前置（prefix）和后置（postfix）形式</h4><ol>
<li><p>重载函数是以其参数类型来区分彼此的，然而不论increment或decrement操作符的前置式或后置式，都没有参数，为了填平这个语言学上的漏洞，只好让后置式有一个int自变量，并且让它被调用时，编译器默默地为该int指定一个<font color="red"><strong>0值</strong></font>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UPInt</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    UPInt&amp; <span class="keyword">operator</span>++();            <span class="comment">// 前置式++</span></span><br><span class="line">    <span class="keyword">const</span> UPInt <span class="keyword">operator</span>++(<span class="keyword">int</span>);    <span class="comment">// 后置式++</span></span><br><span class="line">    UPInt&amp; <span class="keyword">operator</span>--();            <span class="comment">// 前置式--</span></span><br><span class="line">    <span class="keyword">const</span> UPInt <span class="keyword">operator</span>--(<span class="keyword">int</span>);    <span class="comment">// 后置式--</span></span><br><span class="line">    UPInt&amp; <span class="keyword">operator</span>+=(<span class="keyword">int</span>);         <span class="comment">// +=操作符</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">UPInt i;</span><br><span class="line">++i;    <span class="comment">// i.operator++();</span></span><br><span class="line">i++;    <span class="comment">// i.operator++(0);</span></span><br><span class="line">--i;    <span class="comment">// i.operator--();</span></span><br><span class="line">i--;    <span class="comment">// i.operator--(0);</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>所谓的increment操作符的前置式意义“increment and fetch“（积累然后取出），后置式意义”fetch and increment“（取出然后累加）：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">UPInt&amp; UPInt::<span class="keyword">operator</span>++()</span><br><span class="line">&#123;</span><br><span class="line">    *<span class="keyword">this</span> += <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> UPInt UPInt::<span class="keyword">operator</span>++(<span class="keyword">int</span>)</span><br><span class="line">&#123;</span><br><span class="line">    UPInt oldValue = *<span class="keyword">this</span>;</span><br><span class="line">    ++(*<span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">return</span> oldValue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 为什么后置increment操作符返回的对象是const呢？以下：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">UPInt i;</span><br><span class="line">i++++;  <span class="comment">//  后置increment操作符两次</span></span><br><span class="line">i.<span class="keyword">operator</span>(<span class="number">0</span>).<span class="keyword">operator</span>++(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款7：千万不要重载-amp-amp-，-和，操作符"><a href="#条款7：千万不要重载-amp-amp-，-和，操作符" class="headerlink" title="条款7：千万不要重载&amp;&amp;，||和，操作符"></a>条款7：千万不要重载&amp;&amp;，||和，操作符</h4><ol>
<li><p>操作符重载也不是任何都能用来重，它也是有底线的，你不能重载以下操作符：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">.       .*      ::      ?:      <span class="keyword">new</span> </span><br><span class="line"><span class="keyword">delete</span>  <span class="keyword">sizeof</span>  <span class="keyword">typeid</span>  <span class="keyword">static_cast</span></span><br><span class="line"><span class="keyword">dynamic_cast</span>    <span class="keyword">const_cast</span>  <span class="keyword">reinterpret_cast</span></span><br></pre></td></tr></table></figure>

<p> 可以重载的有：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">operator</span> <span class="keyword">new</span>        <span class="keyword">operator</span> <span class="keyword">delete</span></span><br><span class="line"><span class="keyword">operator</span> <span class="keyword">new</span>[]      <span class="keyword">operator</span> <span class="keyword">delete</span>[]</span><br><span class="line">+   -   *   /   %   ^   &amp;   |   ~   </span><br><span class="line">!   =   &lt;   &gt;   +=  -=  *=  /=  %=</span><br><span class="line">^=  &amp;=  |=  &lt;&lt;  &gt;&gt;  &gt;&gt;= &lt;&lt;= ==  !=</span><br><span class="line">&lt;=  &gt;=  &amp;&amp;  ||  ++  --  ,   -&gt;* -&gt;</span><br><span class="line">()  []</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h4 id="条款8：了解各种不同意义的new和delete"><a href="#条款8：了解各种不同意义的new和delete" class="headerlink" title="条款8：了解各种不同意义的new和delete"></a>条款8：了解各种不同意义的new和delete</h4><ol>
<li><p>new operator和operator new之间是有差别的。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span> *ps = <span class="keyword">new</span> <span class="built_in">string</span>(<span class="string">"Memory Management"</span>);</span><br></pre></td></tr></table></figure>

<p> 所使用的new是所谓的<font color="red">new operator</font>。这个操作符是语言内建的，跟sizeof一样不能改变意义。它的分为两方面：<strong>分配足够的内存用来存放某类型的对象</strong>；<strong>而后调用一个constructor为刚才分配的内存中的那个对象设定初值</strong>。<br> 我们能够重写改函数，改变其行为。这个函数名称叫<font color="red">operator new</font>。它的函数原型是：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> size)</span></span>;</span><br></pre></td></tr></table></figure>

<p> size_t参数表示需要分配多少内存。然后返回一块原始的内存。和malloc一样，operator new唯一任务是分配内存。但它<strong>不知道</strong>什么是constructors。当你想要调用一个constructor，有个特殊版本的operator new，称为<font color="red">placement new</font>，允许你这么做：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Widget(<span class="keyword">int</span> widgetSize);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function">Widget* <span class="title">constructorWidgetInBuffer</span><span class="params">(<span class="keyword">void</span> *buffer, <span class="keyword">int</span> widgetSize)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span>(buffer) Widget(widgetSize);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这是new operator的用法之一，指定一个额外自变量（buffer）作为new operator隐式调用operator new时所用。于是，被调用的operator new除了几首size_t变量外，还接受了一个void*参数，这就是所谓的placement new，看起来像这样：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>, <span class="keyword">void</span> *location)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> location;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 如果你希望将对象产生于heap，请使用new operator，它不但分配内存而且为对象调用一个constructor。如果只是打算分配内存，调用operator new，那没有任何constructor被调用。如果打算在heap objects产生时决定内存分配方式，写一个自己的operator new。</p>
</li>
<li><p>为了避免<font color="red">resource leaks</font>（<strong>资源泄露</strong>），每一个动态分配行为都必须匹配一个相应但相反的释放动作。内存释放动作是由operator delete执行：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span> *ps;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">delete</span> ps;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *memoryToBeDeallocated)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 因此，<code>delete</code>动作会产生类似代码：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ps-&gt;~<span class="built_in">string</span>();</span><br><span class="line"><span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(ps)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 这里呈现的暗示是，如果只打算处理原始的、未设初值的内存，应该回避new operator和delete operators。如果使用了placement new产生对象，避免对该内存使用delete operator。因为delete operator会调用operator delete来释放内存，但该内存并非由operator new分配得来的。placement new只是返回它接受的指针而已，所以未了抵消该对象的constructor的影响，应该直接调用该对象的destructor。</p>
</li>
<li><p>面对数组情况，也有所不同：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span> *ps = <span class="keyword">new</span> <span class="built_in">string</span>[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>

<p> 上述的new仍然是new operator，但由于诞生的是数组，所以new operator的行为不再以operator new分配，而是由一个名为operator new[]负责分配（通常称为array new）。同样道理，当delete operator被用于数组，它会针对数组中的每个元素调用其destructor，然后调用operator delete[]释放内存。<br> 总结以下，new operator和delete operator都是内建操作符，无法为你所控制，但是它们所调用的内存分配/释放函数则不然。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>4 设计与声明</title>
    <url>/2020/06/14/4-%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%A3%B0%E6%98%8E/</url>
    <content><![CDATA[<h4 id="条款18：让接口容易被正确使用，不易被误用"><a href="#条款18：让接口容易被正确使用，不易被误用" class="headerlink" title="条款18：让接口容易被正确使用，不易被误用"></a>条款18：让接口容易被正确使用，不易被误用</h4><ol>
<li>除非有好理由，否则应该尽量令你的types的行为与内置types一致。“促进正确使用”的办法包括接口的一致性，以及与内置类型的行为兼容。</li>
<li>“阻止误用”的办法包括建立新类型、限制类型上的操作，束缚对象值，以及消除用户的资源管理责任。</li>
<li>std::shared_ptr支持定制型删除其（custom deleter）。这可防范DLL问题（在这个dll创建对象，在另一边dll删除），可被用来自动解除互斥锁等等。</li>
</ol>
<h4 id="条款19：设计class犹如设计type"><a href="#条款19：设计class犹如设计type" class="headerlink" title="条款19：设计class犹如设计type"></a>条款19：设计class犹如设计type</h4><ol>
<li>C++程序员许多时间主要用来扩张类型系统（type system）。这意味着他不仅是class设计者，还是type设计者。包括<em>重载（overloading）函数和操作符、控制内存的分配和归还、定义对象的初始化和终结</em>……所以应该带着和“语言设计者当初设计语言内置类型时”一样的谨慎来研讨class设计。</li>
<li>设计高效的classes需要考虑：<ol>
<li><strong>新type的对象应该如何被创建和销毁？</strong>（operator new，operator new[]，operator delete和operator delete[]的设计）；</li>
<li><strong>对象的初始化和对象的复制有什么样的差别？</strong>（构造函数和复制（assigment）操作符的行为；</li>
<li><strong>新type的对象如果被passed by value（以值传递），意味着什么？</strong></li>
<li><strong>什么是新type的“合法值“？</strong> class必须维护的约束条件（invariants）（构造函数、赋值操作符和”setter“函数需要的错误检查工作。它影响函数的异常抛出、以及函数异常明细列（exception specifications）。</li>
<li><strong>新type需要配合某个继承图系（inheritance graph）吗？</strong> （必然受到那些classes设计的束缚）</li>
<li><strong>新type需要什么样的转换？</strong> （当需要隐式转换，必须写一个类型转换函数或者写一个non-explicit-one-argument（可被单一实参调用）构造函数）</li>
<li><strong>什么样的操作符和函数对此新type而言是合理的？</strong> </li>
<li><strong>什么样的标准函数应该驳回？</strong> （private）</li>
<li><strong>谁该取用新type的成员？</strong> （public、protected、private、friends）</li>
<li><strong>什么是新type的”未声明接口“（undeclared interface）？</strong> </li>
<li><strong>新type有多么一般化？</strong> 如果是定义一整个types家族，就需要定义一个class template）</li>
<li><strong>真的需要一个新type吗？</strong></li>
</ol>
</li>
</ol>
<h4 id="条款20：宁以pass-by-reference-to-const替换pass-by-value"><a href="#条款20：宁以pass-by-reference-to-const替换pass-by-value" class="headerlink" title="条款20：宁以pass-by-reference-to-const替换pass-by-value"></a>条款20：宁以pass-by-reference-to-const替换pass-by-value</h4><ol>
<li>当一个函数以pass by value方式接受对象，它的成本是很高的，很短的声明周期使它调用构造函数和析构函数。以by reference方式传递参数可以避免<font color="red">slicing</font>（对象切割）问题（derived class对于base class来说的特化信息丢失）。</li>
<li>当使用内置类型有机会选择采用pass-by-value或pass-by-reference-to-const时，by value方式可能会效率高些，因为内置类型都相当小。一般可以合理假设”pass-by-value并不昂贵“的唯一对象就是内置类型和STL的迭代器和函数对象。</li>
</ol>
<h4 id="条款21：必须返回对象时，别妄想返回其reference"><a href="#条款21：必须返回对象时，别妄想返回其reference" class="headerlink" title="条款21：必须返回对象时，别妄想返回其reference"></a>条款21：必须返回对象时，别妄想返回其reference</h4><ol>
<li>绝不要返回pointer或reference指向一个local stack对象，或返回reference指向一个eap-allocated对象，或返回pointer或reference指向一个local sattic对象而有可能同时需要多个这样的对象。</li>
</ol>
<h4 id="条款22：将成员变量声明未private"><a href="#条款22：将成员变量声明未private" class="headerlink" title="条款22：将成员变量声明未private"></a>条款22：将成员变量声明未private</h4><ol>
<li>成员变量的封装性与”成员变量的内容改变时所破坏的代码数量“成反比。取消一个public成员变量，所有使用它的客户码都会被破坏，；取消一个protected成员变量，所有使用它的derived classes都会被破坏。从封装的角度来看，其实只有两种访问权限：private（提供封装）和其他（不提供封装）。</li>
<li>将成员变量声明为private。可赋予客户访问数据的一致性、可细微划分访问控制、允诺约束条件获得保证，并提供class作者以充分的实现弹性。</li>
<li>protected并不比public更具封装性。</li>
</ol>
<h4 id="条款23：宁以non-member、non-friend替换member函数"><a href="#条款23：宁以non-member、non-friend替换member函数" class="headerlink" title="条款23：宁以non-member、non-friend替换member函数"></a>条款23：宁以non-member、non-friend替换member函数</h4><ol>
<li>如果某些东西被封装，越多东西被封装，就越多的弹性去改变它。</li>
<li>如果在一个member函数和一个non-member，non-friend函数之间做抉择，两者提供相同机能，那么较大封装性的是non-member non-friend函数，因为它不增加访问private成分的函数数量。</li>
<li>标准程序库并不是拥有单一、整体、庞大的&lt;C++StandardLibrary&gt;头文件并在其中包含std命名空间内的每一样东西，而是有数十个头文件，每个头文件声明std的某些机能。</li>
<li>将所有便利函数放在多个头文件但隶属同一个ing名空间，意味着客户可以轻松扩展这一组便利函数。它需要做的就是添加更多non-member non-friend函数到此命名空间内。</li>
</ol>
<h4 id="条款24：若所有参数皆需类型转换，请为此采用non-member函数"><a href="#条款24：若所有参数皆需类型转换，请为此采用non-member函数" class="headerlink" title="条款24：若所有参数皆需类型转换，请为此采用non-member函数"></a>条款24：若所有参数皆需类型转换，请为此采用non-member函数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Rationalint numerator = <span class="number">0</span>, <span class="keyword">int</span> denominator = <span class="number">1</span>);</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">numerator</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">denominator</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="keyword">const</span> Rational oeprator*(<span class="keyword">const</span> Rational &amp;rhs) <span class="keyword">const</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>当尝试混合式算式，只有一般行的通；</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Rational <span class="title">oneEighth</span><span class="params">(<span class="number">1</span>, <span class="number">8</span>)</span></span>;</span><br><span class="line"><span class="function">Rational <span class="title">oneHalf</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>)</span></span>;</span><br><span class="line">Rational result = oneHalf * oneEighth;  <span class="comment">// ok</span></span><br><span class="line">result = oneHalf * <span class="number">2</span>;                   <span class="comment">// ok</span></span><br><span class="line">result = <span class="number">2</span> * oneHalf;                   <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p><code>oneHalf</code>是一个内含<code>operator *</code>函数的class对象，而整数2并没有相应的class，也就没有<code>operator*</code>成员函数。但为什么第二个参数是2时可被接受？这里发生了<font color="red">隐式类型转换</font>（<font color="green">implicit type conversion</font>）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">const</span> Rational <span class="title">temp</span><span class="params">(<span class="number">2</span>)</span></span>;</span><br><span class="line">result = oneHalf * temp;</span><br></pre></td></tr></table></figure>

<p>而实际想要支持混合式算数运算，就让<code>operator*</code>称为一个non-member函数，允许编译器在每一个实参身上执行隐式类型转换：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Rational <span class="keyword">operator</span>*(<span class="keyword">const</span> Rational &amp;lhs, <span class="keyword">const</span> Rational &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> Rational(lhs.numberator() * rhs.numerator(), lhs.denominator() * rhs.denominator());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因此结论：如果你需要为某个函数的所有参数（包括被this指针所指的那个隐喻参数）进行类型转换，那么这个函数必须是non-member。</p>
<h4 id="条款25：考虑写出一个不抛出异常的swap函数"><a href="#条款25：考虑写出一个不抛出异常的swap函数" class="headerlink" title="条款25：考虑写出一个不抛出异常的swap函数"></a>条款25：考虑写出一个不抛出异常的swap函数</h4><ol>
<li>一旦要置换两个类对象值，唯一需要做的就是置换其pImpl指针，但default swap算法不知道这点。确切实践思路的一个做法是将std::swap针对该class对象<font color="red">特化</font>（<font color="green">total template specialization</font>）：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widge</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(Widget &amp;other)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">using</span> <span class="built_in">std</span>::swap;</span><br><span class="line">        swap(pImpl, other.pImpl);   <span class="comment">// 置换指针</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="built_in">std</span></span><br><span class="line">&#123;</span><br><span class="line">temlate&lt;&gt;</span><br><span class="line"><span class="keyword">void</span> swap&lt;Widget&gt;(Widget &amp;a, Widget &amp;b)</span><br><span class="line">&#123;</span><br><span class="line">    a.swap(b);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>假设<code>Widget</code>和<code>WidgetImpl</code>都是class templates而非classes，可以尝试将它们的数据类型参数化：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">WidgetImpl</span> &#123;</span> ... &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span> ... &#125;;</span><br></pre></td></tr></table></figure>

<p>在类内放个swap成员函数很简单，却在特化std::swap时遇上乱流：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> <span class="built_in">std</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="comment">// error</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">void</span> swap&lt; Widget&lt;T&gt; &gt;(Widget&lt;T&gt; &amp;a, Widget&lt;T&gt; &amp;b) </span><br><span class="line">&#123; a.swap(b); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>看起来合理但不合法，这是企图<font color="red">偏特化</font>（<font color="green">partially specialize</font>）一个function template，但C++只允许对class template偏特化。有时候std的内容是标准委员会决定，如果希望软件有预期行为，最好不加新东西到std里头。为此，还是声明non-member swap让它调用member swap，但不再将non-member swap声明为std::swap特别版本或重载版本。<br>顺带一提，任何地点的任何代码打算置换两个Widget对象，因而调用swap，C++的名称查找法则（name lookup rules，或argument-dependent lookup和koenig lookup法则）会找到专属版本，那正是我们需要的：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doSomething</span><span class="params">(T &amp;obj1, T &amp;obj2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="built_in">std</span>::swap;    <span class="comment">// 令std::swap在此函数可用</span></span><br><span class="line">    ...</span><br><span class="line">    swap(obj1, obj2);   <span class="comment">// 寻找最佳swap版本woc~</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>查找法则首先寻找global作用域或T所在命名空间内的T专属swap，找不到才使用std内的swap，这得感谢using声明式在函数内曝光。<br>2. 成员版swap绝不可抛出异常，因为swap的一个最好的应用是帮助clasees或class templates提供异常安全性保障。</p>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>4 效率</title>
    <url>/2020/06/14/4-%E6%95%88%E7%8E%87/</url>
    <content><![CDATA[<h4 id="条例16：谨记80-20原则"><a href="#条例16：谨记80-20原则" class="headerlink" title="条例16：谨记80-20原则"></a>条例16：谨记80-20原则</h4><ol>
<li>80-20原则说：一个程序80%的资源用于20%的代码身上。因此根据观察或实验来识别出造成你心痛那20%代码，辨别之道就是使用某个程序分析器（program profiler）。</li>
</ol>
<h4 id="条款17：考虑使用lazy-evaluation（缓式评估）"><a href="#条款17：考虑使用lazy-evaluation（缓式评估）" class="headerlink" title="条款17：考虑使用lazy evaluation（缓式评估）"></a>条款17：考虑使用lazy evaluation（缓式评估）</h4><ol>
<li><font color="green">lazy evaluation</font>（<font color="red">缓式评估</font>）就是以某种方式撰写你的classes，使它们延缓运算，直到那些运算结果刻不容缓地迫切需要为止。其运算结果一直不需要，运算也就一直不执行。lazy evaluation有以下4种常见用途：<ul>
<li><strong>Reference Counting（引用计数）</strong>。可避免非必要的对象赋值。lazy做法可以省下许多工作，string s1和string s2是两个字符串对象，我们让s2以s1为初值，而当s2的值需要修改时，才求取其值。那么我们可以在s2在需要被修改前，使用s1和s2的共享数据，直到需要被修改时才创建s2副本。</li>
<li><strong>区分读和写</strong>。对于一个类的operator[]调用动作，可能为了写或读，而运用lazy evaluatino和proxy classes，可以延缓决定究竟是读是写。</li>
<li><strong>lazy fetching（缓式取出）</strong>。可避免非必要的数据库读取动作。产生一个对象时，只产生相应的“外壳”，不从磁盘读取任何数据，直至被需要时，才求取。这个可以使用smart pointer或者mutable修饰在const member functions里头判断并决定是否求取赋值。</li>
<li><strong>lazy expression evaluation（表达式缓评估）</strong>。可避免非必要的数值计算动作。</li>
</ul>
</li>
</ol>
<h4 id="条款18：分期摊还预期的计算成本"><a href="#条款18：分期摊还预期的计算成本" class="headerlink" title="条款18：分期摊还预期的计算成本"></a>条款18：分期摊还预期的计算成本</h4><ol>
<li><p>与lazy evaluation不同的是，<font color="red">超急评估</font>（<font color="green">over-eager evaluation</font>）超前进度地做“要求以外”的更多工作。举个例子，有个class template用来表现大型数据收集中心：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">NumbercalType</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">DataCollection</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">NumercalType <span class="title">min</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function">NumercalType <span class="title">max</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function">NumercalType <span class="title">avg</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 这个函数的实现方法有三种：</p>
<ul>
<li><strong>eager evaluation</strong>：被调用时返回结果。</li>
<li><strong>lazy evaluation</strong>：函数的返回值需要被派上用场时，决定适当值。</li>
<li><strong>over-eager evaluation</strong>：随实记录程序执行过程中数据集的最小值、最大值和平均值。一旦被调用，立刻返回正确值无需再计算。这种方式通过频繁的调用<strong>摊还成本</strong>。  </li>
</ul>
<p> <strong>Caching</strong>时“分期摊还预期计算成本”的一种做法。<strong>Prefetching（预先取出）</strong>则是另一个种做法。可以想象它是大批购买物品后的折扣。因为磁盘控制器从磁盘读取数据时，读的是整个数据块或sectors。经验显示，<em>如果某处数据被需要，通常其邻近的数据也被需要</em>。这就是有名的<font color="red">locality of reference现象</font>。系统设计者以此现象设计出<font color="red">磁盘缓存</font>（<font color="green">disk caches</font>）、<font color="red">指令与数据的内存缓存</font>（<font color="green">memory caches</font>）以及<font color="red">指令预先取出</font>（<font color="green">instruction prefetches</font>）。</p>
</li>
<li><p>加入要实现一个动态数组的template：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">DynArray</span> &#123;</span> ... &#125;;</span><br><span class="line">DynArray&lt;<span class="keyword">double</span>&gt; a;</span><br><span class="line">a[<span class="number">22</span>] = <span class="number">3.5</span>;</span><br><span class="line">a[<span class="number">32</span>] = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p> 它怎么扩张自己？做内存动态分配行为：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;clas T&gt;</span><br><span class="line">T&amp; DynArray&lt;T&gt;::<span class="keyword">operator</span>[](<span class="keyword">int</span> index)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> an exception;</span><br><span class="line">    <span class="keyword">if</span> (index &gt; the current maximum index value) &#123;</span><br><span class="line">        <span class="keyword">int</span> diff = index - the current maximum index value;</span><br><span class="line">        </span><br><span class="line">        call <span class="keyword">new</span> to allocate enough additional memory so that index+diff is valid;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> the indexth element of the <span class="built_in">array</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 增加数组大小会调用new，但new会调用operator new，而operator new通常代价昂贵，因为它们会调用底层操作系统，而系统调用比进程内的函数调用速度慢，所以尽量不要系统调用。这时就可以使用over-eager evaluation（超急评估）策略，通过增加额外的数组大小来降低不久的下次可能再扩张时的成本。</p>
</li>
</ol>
<h4 id="条款19：了解临时对象的来源"><a href="#条款19：了解临时对象的来源" class="headerlink" title="条款19：了解临时对象的来源"></a>条款19：了解临时对象的来源</h4><ol>
<li><p>程序员交谈的时候往往把一个短暂需要的变量称为临时变量，但在C++中它并不是临时对象，只是函数中的一个局部对象。C++真正的临时对象是不可见的。只要产生一个<strong>non-heap object</strong>而没有为它命名，就是临时对象。这种匿名对象发生于两种情况：一是<strong>隐式类型转换（implicit type conversions）</strong>；二是<strong>函数返回对象</strong>。</p>
<ul>
<li><p>第一种考虑为了让函数调用成功而产生的临时对象，发生在传递某对象给一个函数，而其类与它即将绑定上去的参数类型不同：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回ch在str中出现的个数</span></span><br><span class="line"><span class="keyword">size_t</span> countChar(<span class="keyword">const</span> <span class="built_in">string</span> &amp;str, <span class="keyword">char</span> ch);</span><br><span class="line"><span class="keyword">char</span> buffer[MAX_STRING_LEN];</span><br><span class="line"><span class="keyword">char</span> c;</span><br><span class="line"><span class="comment">// 读入一个char和一个string，利用setw避免读入string时缓冲区满溢的情况</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cin</span> &gt;&gt; c &gt;&gt; setw(MAX_STRING_LEN) &gt;&gt; buffer;</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; <span class="string">"There are "</span> &lt;&lt; countChar(buffer, c)</span><br><span class="line">    &lt;&lt; <span class="string">" occurrences of the character "</span> &lt;&lt; c</span><br><span class="line">    &lt;&lt; <span class="string">" in "</span> &lt;&lt; buffer &lt;&lt; <span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure>

<p>编译器擦除了类型不吻合的状态：以buffer作为自变量，调用string constructor，countChar的str参数绑定于此string临时对象上。当countChar返回，此临时对象自动销毁。只有当对象以by value或当对象传给一个reference-to-const参数时转换才会发生。</p>
</li>
<li><p>第二种会产生临时对象的情况是当函数返回一个对象时。例如operator+的返回对象。</p>
</li>
</ul>
</li>
</ol>
<h4 id="条款20：协助完成“返回值优化（RVO）“"><a href="#条款20：协助完成“返回值优化（RVO）“" class="headerlink" title="条款20：协助完成“返回值优化（RVO）“"></a>条款20：协助完成“返回值优化（RVO）“</h4><ol>
<li><p>函数如果返回对象，对于效率狂来说是个严重的溃败，因为以by-value方式返回对象，背后隐藏的constructor和destructor都将无法消除。而如果以by-pointer方式返回对象，对于其删除指针的操作通常会遗忘而导致资源泄露（resource leaks）。甚至以by-reference返回对象，那也是一个错误的行为，它返回的reference指向一个不再存活的对象。</p>
</li>
<li><p>我们可以用某种特殊写法来撰写函数， 使它在返回对象时能够让编译器消除临时变量的成本。这个伎俩就是：返回所谓的<font color="red"><strong>constructor arguments</strong></font>以取代对象：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回对象：最有效率的做法。</span></span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">const</span> Rational <span class="keyword">operator</span>*(<span class="keyword">const</span> Rational &amp;lhs, <span class="keyword">const</span> Rational &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> Rational(lhs.numerator() * rhs.numerator(), lhs.denominator() * rhs.denominator());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 它返回了一个表达式，该表达式调用了一个Rational constructor，这是一个临时对象，而函数复制此临时对象。<br> C++允许编译器将临时对下个你优化。编译器消除operator*内的临时对象及被operaotr*返回的临时对象。它可以让return表达式所定义的对象构造于所绑定的命名对象的内存内。如果编译器这么做，调用operator*的临时对象的总成本为0。甚至还可以声明为inline，以消除调用operator*所花费的额外开销。这种特殊的优化行为甚至有个专属名称：<font color="red">return value optimization</font>。</p>
</li>
</ol>
<h4 id="条款21：利用重载技术（overload）避免隐式类型转换（implicit-type-conversions）"><a href="#条款21：利用重载技术（overload）避免隐式类型转换（implicit-type-conversions）" class="headerlink" title="条款21：利用重载技术（overload）避免隐式类型转换（implicit type conversions）"></a>条款21：利用重载技术（overload）避免隐式类型转换（implicit type conversions）</h4><ol>
<li>对于解决可能操作符运算隐式的内置类型转换为用户定制类型产生的临时变量，有个办法是为该操作符重载一个内置参数的版本。</li>
</ol>
<h4 id="条款22：考虑以操作符符合形式（op-）取代其独身形式（op）"><a href="#条款22：考虑以操作符符合形式（op-）取代其独身形式（op）" class="headerlink" title="条款22：考虑以操作符符合形式（op==）取代其独身形式（op）"></a>条款22：考虑以操作符符合形式（op==）取代其独身形式（op）</h4><ol>
<li><p>大部分程序员希望，如果他们能<code>x = x + y;</code>这么写，那么他们也能够<code>x += y</code>这么写。如果x和y属于定制类型，就不保证如此，到目前为止C++并不考虑在operator+，operator=和operator+=之间设立任何互动关系。除非自己实现。确保操作符的<font color="red">复合形式</font>（operator+=）和<font color="red">独身形式</font>（operator+）之间的自然关系能够存在，一个好办法就是<strong>以前者为基础实现后者</strong>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">const</span> <span class="title">T</span> <span class="title">operator</span>+(<span class="title">const</span> <span class="title">T</span> &amp;<span class="title">lhs</span>, <span class="title">const</span> <span class="title">T</span> &amp;<span class="title">rhs</span>)</span></span><br><span class="line"><span class="class">&#123;</span> <span class="keyword">return</span> T(lhs) += rhs; &#125;</span><br><span class="line"></span><br><span class="line">Rational a, b, c, d, result;</span><br><span class="line"><span class="comment">// 会用到3个临时变量</span></span><br><span class="line">result = a + b + c + d;</span><br><span class="line"><span class="comment">// 不需要临时变量，效率比上面的好</span></span><br><span class="line"><span class="comment">// 如果同时供应两种选择，便于客户较易理解的操作符独身版之余，同时保留了更有效率的符合版本</span></span><br><span class="line">result = a;</span><br><span class="line">result += b;</span><br><span class="line">result += c;</span><br><span class="line">result += d;</span><br></pre></td></tr></table></figure>

<p> T(lhs)是个调用动作，调用T的copy constructor，它会产生一个临时变量，然后临时变量被用来调用operator+=：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">const</span> <span class="title">T</span> <span class="title">operator</span>+(<span class="title">const</span> <span class="title">T</span> &amp;<span class="title">lhs</span>, <span class="title">const</span> <span class="title">T</span> &amp;<span class="title">rhs</span>)</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="function">T <span class="title">result</span><span class="params">(lhs)</span></span>;          <span class="comment">// 将lhs复制发给result</span></span><br><span class="line">    <span class="keyword">return</span> result += rhs;   <span class="comment">// 将rhs加到result上然后返回</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这个template几乎等同于上一个，但是有个重要差异。第二个template内含了一个命名对象，这意味着<font color="red">返回值优化</font>（<font color="green">return value optimization</font>）无法施展于此operator+。</p>
</li>
</ol>
<h4 id="条款23：考虑使用其他程序库"><a href="#条款23：考虑使用其他程序库" class="headerlink" title="条款23：考虑使用其他程序库"></a>条款23：考虑使用其他程序库</h4><ol>
<li><p>理想的程序库应该小、快速、威力强大、富弹性、有扩展性、直观、可广泛使用、有良好支持，使用时没有束缚，而且没有臭虫。然而这时不存在的。如果针对大小和速度做优化，通常不具移植性，如果有丰富性能，就不容易直观，没有臭虫的程序库只能在乌托邦中寻找。</p>
</li>
<li><p>检验一个极为简单的性能评估软件，它只测试基本的IO机能。这个程序从标准输入读取30,000个浮点数，然后以固定格式将它们写到标准输出设备。程序用iostream还是stdio取决于预处理器符号STDIO：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> STDIO</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span> </span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iomanip&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> VALUES = <span class="number">30000</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">double</span> d;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">1</span>; n &lt;= VALUES; ++n)</span><br><span class="line">    &#123;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> STDIO</span></span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">"%lf"</span>, &amp;d);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%10.5f"</span>, d);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cin</span> &gt;&gt; d;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::setw(<span class="number">10</span>)</span><br><span class="line">                  &lt;&lt; <span class="built_in">std</span>::setprecision(<span class="number">5</span>)</span><br><span class="line">                  &lt;&lt; <span class="built_in">std</span>::setiosflags(<span class="built_in">std</span>::ios::showpoint)</span><br><span class="line">                  &lt;&lt; <span class="built_in">std</span>::setiosflags(<span class="built_in">std</span>::ios::fixed)</span><br><span class="line">                  &lt;&lt; d;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">        <span class="keyword">if</span> (n % <span class="number">5</span> == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> STDIO</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span> </span></span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">'\n'</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> iostreams也可以产生固定格式的I/O，虽然不易写。但是operator&lt;&lt;不但类型安全而且可扩充，而printf两者皆否。stdio版有时候比iostream只是块一点点（20%），有时候很多（200%）。</p>
</li>
</ol>
<h4 id="条款24：了解virtual-functions、multiple-inheritance、virtual-base-classes、runtime-type-identification的成本"><a href="#条款24：了解virtual-functions、multiple-inheritance、virtual-base-classes、runtime-type-identification的成本" class="headerlink" title="条款24：了解virtual functions、multiple inheritance、virtual base classes、runtime type identification的成本"></a>条款24：了解virtual functions、multiple inheritance、virtual base classes、runtime type identification的成本</h4><ol>
<li><p>了解编译器以什么样的方法来实现它们是件重要的事情。其中最重要的就是虚函数。当一个虚函数被调用，执行的代码对应于调用者对象的类型。对象的pointer或reference，其类型是无形的，编译器是用<font color="red">virtual tables</font>和<font color="red">virtual table pointers</font>，通常被简写为vtbls和vptrs。</p>
<ul>
<li><p><font color="red">vtbl</font>通常是由函数指针架构而成的数组（有些是用链表）。<strong>程序中每个class凡声明（或继承）虚函数者，都有自己的一个vtbl，而其中的<font color="red">条目</font>（<font color="green">entires</font>）就是该class的各个虚函数实现体的指针</strong>。这就是虚函数的<font color="red"><strong>第一个成本</strong></font>：<strong>必须为每个拥有虚函数的class耗费一个vtbl空间，空间大小视虚函数的个数（包括继承而来的）而定</strong>。</p>
</li>
<li><p>凡声明由虚函数的class，其对象都含有一个隐藏的data member，用来指向该class的vtbl。这个隐藏的data member——所谓的<font color="red">vptr</font>——被编译器加入对象内某个只有编译器才知道的位置。这是虚函数的<font color="red"><strong>第二个成本</strong></font>：<strong>必须在每个拥有虚函数的对象内付出一个额外指针的代价</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C1</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    C1();</span><br><span class="line">    <span class="keyword">virtual</span> ~C1();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f1</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">int</span> <span class="title">f2</span><span class="params">(<span class="keyword">char</span> c)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f3</span><span class="params">(<span class="keyword">const</span> <span class="built_in">string</span> &amp;s)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">f4</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C2</span> :</span> <span class="keyword">public</span> C1</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    C2();</span><br><span class="line">    <span class="keyword">virtual</span> ~C2();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f1</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f5</span><span class="params">(<span class="keyword">char</span> *str)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">makeACall</span><span class="params">(C1 *pC1)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    pC1-&gt;f1();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译器必须产生代码，完成以下动作：</p>
</li>
</ul>
<ol>
<li><p><strong>根据对象的vptr找出其vbl</strong>。因为编译器知道去哪里找vptr。成本只有<strong>一个<font color="red">偏移调整</font>（<font color="green">offset adjustment</font>，以便获得vptr）</strong>和<strong>一个指针间接动作（以便获得vtbl）</strong>。</p>
</li>
<li><p><strong>找出被调用函数（f1）在vtbl内的对应指针</strong>。成本只是一个<strong>差移（offset）</strong>。</p>
</li>
<li><p><strong>调用步骤2所得指针所指向的函数</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 想象一下每个对象都有隐藏的data member称为vtpr，而函数发</span></span><br><span class="line">的vtbl索引是i</span><br><span class="line"><span class="comment">// 那么先前语句</span></span><br><span class="line">pC1-&gt;f1();</span><br><span class="line"><span class="comment">// 产生的代码将是：</span></span><br><span class="line">(*pC1-&gt;vptr[i])(pC1);</span><br><span class="line"><span class="comment">// 调用pC1-&gt;vptr所指的vtbl中的第i条目所指函数。</span></span><br><span class="line"><span class="comment">// 然后pC1被传给该函数的this指针所用</span></span><br></pre></td></tr></table></figure>

<p>这几乎和一个非虚函数的效率相当。它调用虚函数的成本基本和通过一个函数指针来调用函数相同。虚函数本身不构成性能上的瓶颈。<br>虚函数真正的运行期成本发生在和inlining互动的时候。虚函数不应该inlined。因为<strong>inline意味着在编译期，将调用端的调用动作被调用函数的函数本体所取代，而virtual则意味着等待，直到运行期间才知道哪个函数被调用</strong>。这就是虚函数的<font color="red"><strong>第三个成本</strong></font>：<strong>事实上等同于放弃了inlining。</strong>（如果虚函数通过对象被调用，是可以inlined的，但大部分虚函数调用动作是通过对象的指针或references完成的，这行为无法被inlined，所以虚函数等于无法被inlined。）<br>在多重继承的情况下，情况变得有些复杂，一个对象可能包含多个vptr（每个base class各对应一个），这使得空间负担更大，运行期的调用成本也有增长。<br>多重继承往往导致virtual base classes（虚拟基类）的需求。virtual base classes可导致另一种成本，当利用指针或references指向某个virtual base class成分时，对象内可能出现多个这样的指针：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">virtual</span> <span class="keyword">public</span> A &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> :</span> <span class="keyword">virtual</span> <span class="keyword">public</span> A &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">D</span> :</span> <span class="keyword">public</span> B, <span class="keyword">public</span> C &#123; ... &#125;;</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p><font color="red">运行时期类型辨识</font>（<font color="green">runtime type identification</font>, <font color="red">RTTI</font>）让我们在运行期获得objects和classes的相关信息，它们被存放在类型为type_info的对象内。可以利用<code>typeid</code>操作符取得某个class相应的type_info对象。只有当某种类型拥有至少一个虚函数，才保证我们能检验该类型对象的动态类型。它是个根据class的vtbl来实现的。RTTI的空间成本就只需在每个class vtbl内增加一个条目，再加上每个class所需的一份type_info对象空间。</p>
</li>
</ol>
<table>
<thead>
<tr>
<th>性质</th>
<th>对象大小增加</th>
<th>Class数据量增加</th>
<th>Inlining几率降低</th>
</tr>
</thead>
<tbody><tr>
<td>虚函数<br>Virtual Functions</td>
<td>是</td>
<td>是</td>
<td>是</td>
</tr>
<tr>
<td>多重继承<br>Multiple Inheritance</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr>
<td>虚拟基类<br>Virtual Base Classes</td>
<td>往往如此</td>
<td>有时候</td>
<td>否</td>
</tr>
<tr>
<td>运行时期类型标识<br>RTTI</td>
<td>否</td>
<td>是</td>
<td>否</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>3 资源管理</title>
    <url>/2020/06/14/3-%E8%B5%84%E6%BA%90%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h4 id="条款13：以对象管理资源"><a href="#条款13：以对象管理资源" class="headerlink" title="条款13：以对象管理资源"></a>条款13：以对象管理资源</h4><ol>
<li>以对象管理资源的观念常被称为“<font color="red">资源取得时机便是初始化实际</font>”（<font color="green">Resource Acquisition Is Initialization; </font><font color="red">RAII</font>）。在获得的同时立刻被放进管理对象中。<code>auto_ptrs</code>有个不寻常的性质：若通过copy构造函数或copy assignment操作符复制，它们就会变成null，而复制所得的指针将取得资源的唯一拥有权，并在析构函数中释放资源。</li>
<li><code>auto_ptr</code>的替代方案是“<font color="red">引用计数型智能指针</font>”（<font color="green">reference-counting smart pointer；</font><font color="red">RCSP</font>）。它提供的行为类似<font color="red">垃圾回收</font>（<font color="green">garbage collection</font>）。</li>
</ol>
<h4 id="条款14：在资源管理类中小心copying行为？？？"><a href="#条款14：在资源管理类中小心copying行为？？？" class="headerlink" title="条款14：在资源管理类中小心copying行为？？？"></a>条款14：在资源管理类中小心copying行为？？？</h4><ol>
<li>复制RAII对象必须一并复制它所管理的资源，所以资源的copying行为决定RAII对象的copying行为。</li>
<li>普遍而常见的RAII class copying行为是：抑制copying、施行引用计数法（reference counting）。</li>
</ol>
<h4 id="条款15：在资源管理类中提供对原始资源的访问"><a href="#条款15：在资源管理类中提供对原始资源的访问" class="headerlink" title="条款15：在资源管理类中提供对原始资源的访问"></a>条款15：在资源管理类中提供对原始资源的访问</h4><ol>
<li>除非发誓永不录用APIs，否则只得绕过资源管理对象（resource-managing objects）直接访问<font color="red">原始资源</font>（<font color="green">raw resources</font>）。要将RAII class对象转换为其所包含的原始资源。有两个做法：显式转换和隐式转换。前者通过get成员函数可获得指针；后者可行是因为RAII重载了<font color="red">指针取值</font>（<font color="green">pointer dereferencing</font>）操作符（operator-&gt;和operator*），它们允许隐式转化至底部原始指针。</li>
</ol>
<h4 id="条款16：成对使用new和delete时要采取相同形式"><a href="#条款16：成对使用new和delete时要采取相同形式" class="headerlink" title="条款16：成对使用new和delete时要采取相同形式"></a>条款16：成对使用new和delete时要采取相同形式</h4><ol>
<li>当你使用new，有两件事发生。第一，内存被分配出来（operator new）；第二，针对此内存会有一个（或更多）构造函数被调用。当你使用delete，也有两件事发生；针对此内存会有一个（或更多）析构函数被调用，然后内存被释放（operator delete）。</li>
<li>new时使用[]，必须在相应调用delete时也使用[]。对于喜欢对数组形式做typedef动作的人，最好不要这样做，而C++ STL标准库中的容器templates，可将数组需求降至最低。</li>
</ol>
<h4 id="条款17：以独立语句将newed对象置入智能指针"><a href="#条款17：以独立语句将newed对象置入智能指针" class="headerlink" title="条款17：以独立语句将newed对象置入智能指针"></a>条款17：以独立语句将newed对象置入智能指针</h4><p>对于<code>processWidget(std::shared_ptr&lt;Widget&gt;(new Widget), priority());</code>这行代码可以通过编译，然而上述调用可能泄露资源。因为它的执行顺序可能时</p>
<ol>
<li>执行“new Widget”</li>
<li>调用priority删除</li>
<li>调用std::shared_ptr构造函数  </li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;万一对priority的调用导致异常，“new Widget”返回的指针将会丢失，因为尚未置入std::shared_ptr内。解决方法是使用独立语句将创建Widget和置入指针两个步骤分割开来：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw(<span class="keyword">new</span> Widget);</span><br><span class="line">processWidget(pw, priority());</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>5 实现</title>
    <url>/2020/06/14/5-%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p>大多数情况下，适当提出classes或class templates定义以及functions和function<br>templates声明，是最花费心力的两件事。<strong><em>太快定义变量可能造成效率上的拖延；过度使用转型可能导致代码变慢又难维护，又找来微妙的理解错误；返回对象内部数据的handle可能会破坏封装并留给客户dangling<br>handles；未考虑异常带来的冲击则可能导致资源泄露和数据败坏；过度热心地inlining可能引起代码膨胀；过度耦合（coupling）则可能导致让人不满意的冗长build<br>times。</em></strong></p>
<h4 id="条款26：尽可能延后变量定义式的出现时间"><a href="#条款26：尽可能延后变量定义式的出现时间" class="headerlink" title="条款26：尽可能延后变量定义式的出现时间"></a>条款26：尽可能延后变量定义式的出现时间</h4><ol>
<li>当定义了一个类型带有构造函数或析构函数，就得付出构造成本和析构成本。如果函数可能抛出异常，最好将未使用的变量尽量延后声明（为何不声明定义分割是因为将函数构造出来再赋值比直接构造时指定初值效率低）。</li>
<li>对于在循环结构中定义在里头还是外头需要权衡：如果赋值成本比“构造+析构”成本高，那么倒不如在循环结构中定义。</li>
</ol>
<h4 id="条款27：尽量少做转型动作"><a href="#条款27：尽量少做转型动作" class="headerlink" title="条款27：尽量少做转型动作"></a>条款27：尽量少做转型动作</h4><ol>
<li>回顾转型语法：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// old-style casts</span></span><br><span class="line">(T)expression</span><br><span class="line">T(expression)</span><br><span class="line"></span><br><span class="line"><span class="comment">// C++-style casts</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    const_cast通常用来将对象常量行转除（cast away he constness）</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">const_cast</span>&lt;T&gt;(expression)</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    dynamic_cast主要用来执行安全向下转型（safe downcasting），用来决定某对象是否归属继承体系中的某个类型，它是唯一无法由旧式语法执行的动作，也很耗费重大运行成本的转型动作</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">dynamic_cast</span>&lt;T&gt;(expression)</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    reinterpret_cast意图执行低级转型，它取决于编译器，不可移植。它可以这么用，将pointer to int转型为一个int</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">reinterpret_cast</span>&lt;T&gt;(expression)</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    static_cast用来强迫隐式转换（implicit conversions），它基本什么都能做，就是无法将const转为non-const，这只有const_cast办得到。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">satic_cast&lt;T&gt;(expression)</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>有人觉得转型起始什么都没做，其实这是错误的观念：底层转型表述几乎会产生一些代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> :</span> <span class="keyword">public</span> Base &#123; ... &#125;;</span><br><span class="line">Derived d;</span><br><span class="line">Base *pb = &amp;d;</span><br></pre></td></tr></table></figure>

<p>这里不过是建立一个base class指针指向一个derived class对象，但有时候两个指针值并不相同。这种情况下会有个<font color="red">偏移量</font>（<font color="green">offset</font>）在<strong>运行期</strong>被施行于Derived*指针身上，才取得正确Base*指针值。<br>我们很容易写出似是而非的代码（在其他语言可能是真的）。例如derived classes内的virtual函数代码调用base class对应函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">    这段程序将*this转型为Window，它调用的不是当前对象上的函数，</span></span><br><span class="line"><span class="comment">    而是*this对象的baseclass部分的一个临时副本。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Window</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">onResize</span><span class="params">()</span> </span>&#123; ... &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpecialWindow</span>:</span> <span class="keyword">public</span> Window</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">onResize</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">static_cast</span>&lt;Winddow&gt;(*<span class="keyword">this</span>).onResize();</span><br><span class="line">        <span class="comment">// 正确做法是：</span></span><br><span class="line">        Window::onResize();</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="条款28：避免返回handles指向对象内部成分"><a href="#条款28：避免返回handles指向对象内部成分" class="headerlink" title="条款28：避免返回handles指向对象内部成分"></a>条款28：避免返回handles指向对象内部成分</h4><ol>
<li>一个成员函数返回<font color="red">references</font>、<font color="red">pointers</font>和<font color="red">itertors</font>，这些统统都是所谓的<font color="red"><strong>handles</strong></font>（号码牌，用来取得某个对象）。而一个返回“代表对象内部数据”的handle，随之而来的是降低对象封装性的风险。</li>
</ol>
<h4 id="条款29：为”异常安全“努力是值得的"><a href="#条款29：为”异常安全“努力是值得的" class="headerlink" title="条款29：为”异常安全“努力是值得的"></a>条款29：为”异常安全“努力是值得的</h4><ol>
<li>当异常被抛出时，带有<font oclor="red">异常安全性</font>（<font color="green">Exception safety</font>）的函数一般不泄露任何资源并不允许数据败坏。这样的函数可分为三种保证：基本型、强烈型和不抛出异常型。</li>
<li>强烈保证以copy-and-swap实现出来，保证一个动作要么全部完成，否则不进行任何变动。</li>
</ol>
<h4 id="条款30：透彻了解inlining的里里外外"><a href="#条款30：透彻了解inlining的里里外外" class="headerlink" title="条款30：透彻了解inlining的里里外外"></a>条款30：透彻了解inlining的里里外外</h4><ol>
<li>用<font color="red">inline</font>内嵌函数可以免除函数的调用成本，编译器最优化机制通常被设计来浓缩不含函数调用的代码。然而过多的inline可能导致增加<font color="red">目标码</font>（<font color="green">object coed</font>）大小。在内存有限 的机器上，过度热衷inlining会使程序体积太大，即使拥有虚内存，造成的代码膨胀会导致额外的<font color="red">换页行为</font>（<font color="green">paging</font>），降低指令<font color="red">高速缓存的命中率</font>（<font color="green">instruction cache hit rate</font>），以及伴随这些问题而来的效率损失。</li>
<li>Inline函数通常被置于头文件内，因为多数build environments在编译过程中进行inlining，在链接期完成inlining。inlining在多数C++程序中是编译期行为。</li>
<li>inline是一个向编译器发出的申请，而不是强制命令。大部分编译器拒绝过于复杂（如有循环和递归）的函数inlining。对于virtual函数也会，因为virtual意味着”wait，知道runtime才确定调用哪个函数“。</li>
</ol>
<h4 id="条款31：将文件间的编译依存关系降至最低"><a href="#条款31：将文件间的编译依存关系降至最低" class="headerlink" title="条款31：将文件间的编译依存关系降至最低"></a>条款31：将文件间的编译依存关系降至最低</h4><ol>
<li><code>#include</code>指示符提供的定义式和其含入文件之间形成了一种<font color="red">编译依存关系</font>（<font color="green">compilation dependency</font>）。如果这些头文件中有任何一个被改变，或者这些头文件所依赖的其他头文件有任何改变，那么每个含有class的文件就得重新编译。解决方案是通过<strong>前置声明</strong>配合指针或引用类型声明来减少编译依赖。</li>
</ol>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>3 异常</title>
    <url>/2020/06/14/3-%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<p>C++增加了exceptions性质后，可能令人不舒服的改变了很多事情。原始指针的使用如今称为高风险行为，资源泄露的机会大增，撰写符合期望的constructors和destructors的难度也大增。程序员特别小心，放置程序在执行时突然中止。可执行文件和程序库变得更大、速度更慢。<br>为什么使用exceptions？答案很简单：exceptions无法被忽视。如果一个函数利用<strong>设定状态变量</strong>的方式或是利用<strong>返回错误码</strong>的方式发出一个异常，无法保证此函数的调用者会检查那个变量或检验那个错误码。于是程序不断执行，远离错误发生地点。但如果函数以抛出exception的方式发出异常信号，而该exception未被捕捉，程序的执行立刻中止。<br>C只有<font color="red"><code>setjmp</code></font>和<font color="red"><code>longjmp</code></font>才能近似这样的行为。但longjmp在C++中有缺陷：当它调整栈（stack）的时候，无法调用局部（local）对象的destructors。C++很依赖destructors被调用。</p>
<h4 id="条款9：利用destuctors避免资源泄露"><a href="#条款9：利用destuctors避免资源泄露" class="headerlink" title="条款9：利用destuctors避免资源泄露"></a>条款9：利用destuctors避免资源泄露</h4><ol>
<li><p>有个例子，加入你现在要写一个软件。收养中心每天会产生一个文件，其中有它所安排的当天收养个案，你的工作是写程序读文件，然后为每个个案做适当处理：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ALA: abstract base class</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ALA</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">processAdoption</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Puppy</span> :</span> <span class="keyword">public</span> ALA</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">processAdoption</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Kitten</span> :</span> <span class="keyword">public</span> ALA</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">processAdoption</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function">ALA* <span class="title">readALA</span><span class="params">(istream &amp;s)</span></span>;</span><br><span class="line"><span class="comment">// 程序核心大致</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">processAdoptions</span><span class="params">(istream &amp;dataSource)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 如果还有数据</span></span><br><span class="line">    <span class="keyword">while</span> (dataSource)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 取出下一只动物</span></span><br><span class="line">        ALA *pa = readALA(dataSource);</span><br><span class="line">        <span class="comment">// 处理收养事宜</span></span><br><span class="line">        pa-&gt;processAdoption();</span><br><span class="line">        <span class="comment">// 删除readALA返回的对象</span></span><br><span class="line">        <span class="keyword">delete</span> pa;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 如果pa-&gt;processAdoption抛出一个exception怎么办，很简单：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">processAdoptions</span><span class="params">(istream &amp;dataSource)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (dataSource)</span><br><span class="line">    &#123;</span><br><span class="line">        ALA *pa = readALA(dataSource);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            pa-&gt;processAdoption();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (...) &#123; <span class="comment">// 捕捉</span></span><br><span class="line">            <span class="keyword">delete</span> pa;  <span class="comment">// delete，避免泄露</span></span><br><span class="line">            <span class="keyword">throw</span>;      <span class="comment">// 传播给调用端</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">delete</span> pa;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这样程序被try语句块和catch语句块搞得乱七八糟。解决方法是以一个类似指针的对象取代指针pa，这样当这个类指针对象被（自动）销毁，可以令其destructor调用delete。这就是<font color="red">smart pointers</font>。C++标准库有，它看起来是这样子的（此书有点旧）：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">auto_ptr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">auto_ptr</span>(T *p = <span class="number">0</span>) : ptr(p) &#123;&#125;</span><br><span class="line">    ~<span class="built_in">auto_ptr</span>() &#123; <span class="keyword">delete</span> ptr; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T *ptr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> auto_ptr不适合delete数组对象的指针。如果你希望有这样的auto_ptr用在数组上，要么自己写一个，要么最好使用一些容器，比如vector来取代。用auto_ptr对象取代原始指针后，processAdoptions看起来这样子：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">processAdoptions</span><span class="params">(istream &amp;dataSource)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (dataSource)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">auto_ptr</span>&lt;ALA&gt; pa(readALA(dataSource));</span><br><span class="line">        pa-&gt;processAdoption();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 它和原版本的差异只有两处。一是pa被声明为一个auto_ptr&lt;ALA&gt;对象；二是循环最后不再有delete语句。它是隐藏在auto_ptr背后的观念——以一个对象存放必须自动释放的资源，并依赖该对象的destructor释放。</p>
</li>
</ol>
<h4 id="条款10：在constructors内阻止资源泄露（resource-leak）"><a href="#条款10：在constructors内阻止资源泄露（resource-leak）" class="headerlink" title="条款10：在constructors内阻止资源泄露（resource leak）"></a>条款10：在constructors内阻止资源泄露（resource leak）</h4><ol>
<li><p>对于尚未完全构造好的对象，C++拒绝调用其destructor。由于C++不自动清理构造期间抛出exceptions的对象，所以设法让constructor在那种情况下也能自我清理。这通常将可能的exceptions捕捉起来，执行清理工作，然后重新抛出exception。让它继续传播：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Image</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Image(<span class="keyword">const</span> <span class="built_in">string</span> &amp;imageDataFileName);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AudioClip</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    AudioClip(<span class="keyword">const</span> <span class="built_in">string</span> &amp;audioDataFileName);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BookEntry</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    ~BookEntry();</span><br><span class="line">    BookEntry(<span class="keyword">const</span> <span class="built_in">string</span>&amp;, <span class="keyword">const</span> <span class="built_in">string</span>&amp;, <span class="keyword">const</span> <span class="built_in">string</span>&amp;, <span class="keyword">const</span> <span class="built_in">string</span>&amp;);</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">addPhoneNumber</span><span class="params">(<span class="keyword">const</span> PhoneNumber &amp;number)</span></span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">cleanup</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="built_in">string</span> theName;</span><br><span class="line">    <span class="built_in">string</span> theAddress;</span><br><span class="line">    <span class="built_in">list</span>&lt;PhoneNumber&gt; thePhones;</span><br><span class="line">    Iimage * <span class="keyword">const</span> theImage;</span><br><span class="line">    AudioClip * <span class="keyword">const</span> theAudioClip;</span><br><span class="line">    <span class="function">Image* <span class="title">initImage</span><span class="params">(<span class="keyword">const</span> <span class="built_in">string</span> &amp;imageFileName)</span></span>;</span><br><span class="line">    <span class="function">AudioClip* <span class="title">initAudioClip</span><span class="params">(<span class="keyword">const</span> <span class="built_in">string</span> &amp;audioClipFileName)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> BookEntry::cleanup()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> theImage;</span><br><span class="line">    <span class="keyword">delete</span> the AudioClip;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">BookEntry::BookEntry(const string &amp;name,</span></span><br><span class="line"><span class="comment">                    const string &amp;address,</span></span><br><span class="line"><span class="comment">                    const string &amp;imageFileName,</span></span><br><span class="line"><span class="comment">                    const string &amp;audioClipFileName) </span></span><br><span class="line"><span class="comment">: theName(name, theAddress(address),</span></span><br><span class="line"><span class="comment">    theImage(0), theAudioClip(0)</span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">    try &#123;</span></span><br><span class="line"><span class="comment">        if (imageFileName != "") </span></span><br><span class="line"><span class="comment">            theImage = new Image(imageFileName);</span></span><br><span class="line"><span class="comment">        if (audioClipFileName != "")</span></span><br><span class="line"><span class="comment">            theAudioClip = new AudioClip(audioClipFileName);</span></span><br><span class="line"><span class="comment">    &#125; catch (...) &#123;</span></span><br><span class="line"><span class="comment">        cleanup();</span></span><br><span class="line"><span class="comment">        throw;</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 想要在exceptions传播到constructor外之前，无法将try和catch放到member initialization list中，就放到某些private member functions中。</span></span><br><span class="line">BookEntry::BookEntry(<span class="keyword">const</span> <span class="built_in">string</span> &amp;name,</span><br><span class="line">                    <span class="keyword">const</span> <span class="built_in">string</span> &amp;address,</span><br><span class="line">                    <span class="keyword">const</span> <span class="built_in">string</span> &amp;imageFileName,</span><br><span class="line">                    <span class="keyword">const</span> <span class="built_in">string</span> &amp;audioClipFileName) </span><br><span class="line">: theName(name, theAddress(address),</span><br><span class="line">    theImage(initImage(imageFileName)), theAudioClip(initAudioClip(audioClipFileName))</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line">Image* BookEntry::initImage(<span class="keyword">const</span> <span class="built_in">string</span> &amp;imageFileName)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (imageFileName != <span class="string">""</span>) </span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Image(imageFileName);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">AudioClip* BookEntry::initAudioClip(<span class="keyword">const</span> <span class="built_in">string</span> &amp;audioClipFileName)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (audioClipFileName != <span class="string">""</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> AudioClip(audioClipFIleName);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (...) &#123;</span><br><span class="line">        <span class="keyword">delete</span> theImage;</span><br><span class="line">        <span class="keyword">throw</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">BookEntry::~BookEntry()</span><br><span class="line">&#123;</span><br><span class="line">    cleanup();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 更好的做法是如条款9一样，使用smart pointer来取代pointer class members，免除了exception出现时发生资源泄露。</p>
</li>
</ol>
<h4 id="条款11：禁止异常（exceptions）流出destructors之外"><a href="#条款11：禁止异常（exceptions）流出destructors之外" class="headerlink" title="条款11：禁止异常（exceptions）流出destructors之外"></a>条款11：禁止异常（exceptions）流出destructors之外</h4><ol>
<li>两种情况下destructor会被调用。第一种情况是<strong>当对象在正常状态下被销毁，也就是离开生存空间（scope）或是被明确删除时</strong>；第二种情况是<strong>当对象被exception处理机制——也就是exception传播过程中的stack-unwinding（栈展开）机制——销毁</strong>。</li>
<li>当destructor被调用，可能有一个exception正在作用中，可惜的是无法在destructor区分这些状态（有了）。这样就要保守撰写destructors了。如果控制权基于exception的因素离开destructor，而此时另一个exception处于作用状态，C++会调用terminate函数，将程序结束掉。</li>
<li>有两个理由支持阻止exceptions传出destructor之外。第一，<strong>它可以避免terminate函数在exception传播过程的<font color="red">栈展开</font>（<font color="green">stack-unwinding</font>）机制被调用</strong>；第二，<strong>它可以协助确保destructors完成其应该完成的所有事情</strong>。</li>
</ol>
<h4 id="条款12：了解”抛出一个exception“与”传递一个参数“或”调用一个虚函数“之间的差异"><a href="#条款12：了解”抛出一个exception“与”传递一个参数“或”调用一个虚函数“之间的差异" class="headerlink" title="条款12：了解”抛出一个exception“与”传递一个参数“或”调用一个虚函数“之间的差异"></a>条款12：了解”抛出一个exception“与”传递一个参数“或”调用一个虚函数“之间的差异</h4><ol>
<li><p>从抛出端传递一个exception到catch字句，看起来和从函数调用端传递一个自变量到函数参数是一样的。但是有重大的不同。函数参数和exception的传递参数有3中：by value，by reference，by pointer。然而，当你调用一个函数，控制权最终会回到调用端（除非函数失败），但是当你抛出一个exception，控制权不再回到抛出端。而且被捕捉的exception是某个对象的副本，即使以by reference方式传参。</p>
</li>
<li><p>当对象被赋值当作一个exception，复制行为是由对象的copy constructor执行的。这个copy constructor相当于该对象的”静态类型“而非”动态类型”。例如：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpecialWidget</span> :</span> <span class="keyword">public</span> Widget &#123; ... &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">passAndThrowWidget</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    SpecialWidget localSpecialWidget;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// rw表示一个SpecialWidget</span></span><br><span class="line">    Widget&amp; rw = localSpecialWidget;</span><br><span class="line">    <span class="comment">// 抛出一个类型为Widget的exception</span></span><br><span class="line">    <span class="keyword">throw</span> rw;   <span class="comment">// （条款25有返回动态类型的技术）</span></span><br><span class="line">    <span class="comment">// 抛出一个类型为SpecialWidget的exception；这是因为它并没有发生复制行为，只是再抛出。</span></span><br><span class="line">    <span class="keyword">throw</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">catch</span> (Widget w) ...    <span class="comment">// by value</span></span><br><span class="line"><span class="keyword">catch</span> (Widget &amp;w) ...   <span class="comment">// by reference</span></span><br><span class="line"><span class="keyword">catch</span> (<span class="keyword">const</span> Widget &amp;w) ... <span class="comment">// by reference-to-const</span></span><br></pre></td></tr></table></figure>

<p> 以上“参数传递”和“exception传播“之间的另一个区别。exception的复制动作，其结果是个临时对象。一个被抛出的对象可以简单的用by reference的方式捕捉，不需要以<font color="red">by reference-to-const</font>的方式捕捉。调用过程将一个临时对象传递给<font color="red">non-const reference</font>参数是不允许的，但对exceptions则是合法。<br> 值得一提，by value方式传递exception，得符出”被抛出物“<strong>两个副本</strong>的构造代价，其中一个构造动作用于产生exception临时对象，另一个构造动作作用于将临时对象赋值到w；而by reference得付出<strong>单一副本</strong>的构造代价，这个副本就是临时副本，by reference方式传递函数参数不会发生复制行为。<br> 还有，exceptions与catch子句匹配过程中，不会发生隐式转换的行为。它仅有两种转换可以发生。第一种是<font color="red">继承架构中的类转换</font>（<font color="green">inheritance-based conversions</font>）。</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">logic_error--&gt;exception</span><br><span class="line">runtime_error--&gt;exception</span><br><span class="line">domain_error--&gt;logic_error</span><br><span class="line">invalid_argument--&gt;logic_error</span><br><span class="line">length_error--&gt;logic_error</span><br><span class="line">out_of_range--&gt;logic_error</span><br><span class="line">range_error--&gt;runtime_error</span><br><span class="line">underflow_error--&gt;runtime_error</span><br><span class="line">overflow_error--&gt;runtime_error</span><br></pre></td></tr></table></figure>

 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">catch</span> (runtime_error) ...</span><br><span class="line"><span class="keyword">catch</span> (runtime_error&amp;) ...</span><br><span class="line"><span class="keyword">catch</span> (<span class="keyword">const</span> runtime_error&amp;) ...</span><br><span class="line"><span class="keyword">catch</span> (runtime_error*) ...</span><br><span class="line"><span class="keyword">catch</span> (<span class="keyword">const</span> runtime_error*) ...</span><br><span class="line"><span class="keyword">catch</span> (<span class="keyword">const</span> <span class="keyword">void</span>*) ...</span><br></pre></td></tr></table></figure>

<p> ”传递参数“和”传播exception“的最后不同是，catch子句总是依出现顺序做匹配尝试。将此行为和调用虚函数的行为比对。当调用虚函数时，被调用的函数是“调用者的动态类型”中的函数。在这方面，虚函数采用<font color="red"><strong>”best fit“（最佳吻合）策略</strong></font>，<font color="red"><strong>exception遵循”first fit”（最先吻合）策略</strong></font>。所以针对derived class而设计的catch子句必须放到针对base class设计的catch子句之前。</p>
</li>
</ol>
<h4 id="条款13：以by-reference方式捕捉exceptions"><a href="#条款13：以by-reference方式捕捉exceptions" class="headerlink" title="条款13：以by reference方式捕捉exceptions"></a>条款13：以by reference方式捕捉exceptions</h4><ol>
<li><font color="red">throw by pointer</font>应该是最有效率的一种做法,因为它不复制对象。但它并没有想象中的好，因为了让exception objects在控制权离开抛出指针的函数之后依然存在，就需要声明global或static对象，而程序很容易忘记。而且如果抛出的指针指向一个新的heap-based，负责catch的人不能区分是global即static还是heap中的exception object，于是可能产生内存泄露或者未定义的行为等潜在问题。<br> 此外，catch-by-pointer和语言本身建立起来的惯例有矛盾。4个标准的exception——<strong>bad-alloc、bad_cast、bad_typeid和bad_exception</strong>——统统都是对象，不是对象指针。说明只能使用by value或by reference。然而<font color="red">catch-by-value</font>每当exception objects被抛出，就得复制两次。而且他会引起<font color="red">切割</font>（<font color="green">slicing</font>）问题，因为derived class exception objects被捕捉并被视为base class exceptions：被切割过的对象其实就是缺少derived class data members。<br> 所以接下来就是<font color="red">catch-by-reference</font>了。它不像catch-by-pointer发生对象删除问题，也不存在catch-by-value的切割问题，而且exception objects只会被复制一次。</li>
</ol>
<h4 id="条款14：明智运用exception-specifications"><a href="#条款14：明智运用exception-specifications" class="headerlink" title="条款14：明智运用exception specifications"></a>条款14：明智运用exception specifications</h4><ol>
<li><p><font color="red">exception specifications</font>让代码更容易被理解，因为它明确指出一个函数可以抛出什么样的exceptions。但它不只是一个注释而已。编译器有时候能够在编译期间侦测到与exception specifications不一致的行为。如果函数抛出了一个并未列入于其exception speicification的exception，这个错误会在运行期被检验出来。于是特殊函数<font color="red">unexpected</font>会被自动调用。unexpected的默认行为是调用terminate，而terminate的默认行为是调用abort，所以程序如果违反exception speicification，默认结果就是程序被中止。</p>
</li>
<li><p>编译器只会对exception specifications做局部经检验，调用某个函数而该函数可能违反调用端函数本身的exception specification。这么做可能会导致程序被迫终止，为将这种不一致性降到最低，一个方法是<strong>避免将exception specification放在需要类型自变量的templates身上</strong>。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不良的temmplates design，它带有exception specification</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">bool</span> <span class="title">operator</span>==(<span class="title">const</span> <span class="title">T</span> &amp;<span class="title">lhs</span>, <span class="title">const</span> <span class="title">T</span> &amp;<span class="title">rhs</span>) <span class="title">throw</span>()</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">return</span> &amp;lhs == &amp;rhs;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 它指明template产生出来的函数不会抛出任何exception，然而它调用的operator &amp;可能会抛出一个异常。更一般化的问题是，没有任何方法可以知道一个template的类型参数可能抛出什么。<br> 避免踏上unexpected之路的第二个方法是：<strong>如果A函数调用的B函数没有exception specifications，那么A函数本身也不要设定exception specifications</strong>。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 标准委员会宣称typedef内不可出现exception specification， 可能需要该写宏</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*CallBackPtr)</span><span class="params">(<span class="keyword">int</span> eventXLocation, <span class="keyword">int</span> eventYLocation, <span class="keyword">void</span> *dataToPassBack)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CallBack</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    CallBack(CallBackPtr fPtr, <span class="keyword">void</span> *dataToPassBack) : func(fPtr), data(dataToPassBack) &#123;&#125; </span><br><span class="line">    <span class="function">vvoid <span class="title">makeCallBack</span><span class="params">(<span class="keyword">int</span> eventXLocation, <span class="keyword">int</span> eentYLocaltion)</span> <span class="keyword">const</span> <span class="title">throw</span><span class="params">()</span></span>;s</span><br><span class="line"><span class="keyword">private</span></span><br><span class="line">    CallBackPtr *func;</span><br><span class="line">    <span class="keyword">void</span> *fdata;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> CallBack::makeCallBack(<span class="keyword">int</span> eventXLocation, <span class="keyword">int</span> eventYLocatino) <span class="function"><span class="keyword">const</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fnuc(eventXLocation, eventYLocation, data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 第三个技术是：<strong>处理“系统”可能抛出的exceptions</strong>。直接处理非预期的exceptions比事先预防来的简单。但如果你写的软件大量运用exception specifications，但调用程序库提供的函数没有使用exception specifications，想要阻止之变得不切实际。这时候可以利用一个事实：以不同类型的exceptions取代非预期的exceptions。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 所有非预期的exception objects都被取代为此类的objects</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UnexpectedException</span> &#123;</span>&#125;;</span><br><span class="line"><span class="comment">// 如果有非预期的exception被抛出，调用此函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">convertUnexpected</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">throw</span> UnexpectedException();</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 以convertUnexpected取代默认的unexpected</span></span><br><span class="line"><span class="built_in">std</span>::set_unexpected(convertUnexpected);</span><br></pre></td></tr></table></figure>

<p> 一旦完成这些部署，非预期的exception会导致该函数被调用。那么只要违反的exception specification内含有Unexpected Exception，exception的传播就会继续下去，而如果未含有，terminate会被调用，犹如未取代unexpected一样。<br> 将非预期的exceptions转换为一个已知类型的另一个做法是，依赖以下事实：如果非预期函数的替代者重新抛出当前（current）exception，该xception会被保准类型bad_exception取代而之。</p>
</li>
</ol>
<h4 id="条款15：了解异常处理（exception-handing）的成本"><a href="#条款15：了解异常处理（exception-handing）的成本" class="headerlink" title="条款15：了解异常处理（exception handing）的成本"></a>条款15：了解异常处理（exception handing）的成本</h4><ol>
<li>为了能够在运行期处理exceptions，程序必须做大量簿记工作。在每个执行点，它们必须能够确认“如果发生exception，哪些对象需要析构”，它们必须在每个try语句块的进入点和离开点做记号，针对每个try语句块它们必须记录对应的catch子句及能够处理的exceptions类型。这些簿记工作必须符出代价，运行时期的比对工作（以确保符合exception specifications）不是免费的。</li>
</ol>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>6 继承与面向对象设计</title>
    <url>/2020/06/14/6-%E7%BB%A7%E6%89%BF%E4%B8%8E%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h4 id="条款32：确定你的public继承塑模出is-a关系"><a href="#条款32：确定你的public继承塑模出is-a关系" class="headerlink" title="条款32：确定你的public继承塑模出is-a关系"></a>条款32：确定你的public继承塑模出is-a关系</h4><ol>
<li><strong>public inheritance（公开继承）意味”<font color="red">is-a</font>“（是一种）的关系。</strong></li>
<li>令class D（”Derived”）以public形式继承class B（“Base”），意味着每个类型为D的对象同时也是一个类型为B的对象，反之不成立。B比D概念上更一般化，D比B更特殊化。</li>
<li>对于现实世界，我们可能对已有事物进行概念上的内涵和外延来抽象不同的状态和动作。企鹅是一种鸟，然而它不会飞，对于鸟会飞的先入为主的观念，可能会使实际偏颇。“企鹅不会飞”这一限制可由编译期强制实施，但若违反“企鹅尝试飞行，是一种错误”这一条规则，只有运行期才能检测出来。</li>
<li>is-a并非是唯一存在于classes之间的关系。另两个常见的关系是<font color="red">has-a</font>（有一个）和<font color="red">is-implemented-in-terms-of</font>（根据某物实现出）。</li>
</ol>
<h4 id="条款33：避免遮掩继承而来的名称"><a href="#条款33：避免遮掩继承而来的名称" class="headerlink" title="条款33：避免遮掩继承而来的名称"></a>条款33：避免遮掩继承而来的名称</h4><ol>
<li>derived classes内的名称会遮掩base classes内的名称。在public继承下从来没有人希望如此。</li>
<li>为了让被遮掩的名称再见天日，可使用using声明式或<font color="red">转交函数</font>（<font color="green">orwarding functions</font>）。</li>
</ol>
<h4 id="条款34：区分接口继承和实现继承"><a href="#条款34：区分接口继承和实现继承" class="headerlink" title="条款34：区分接口继承和实现继承"></a>条款34：区分接口继承和实现继承</h4><ol>
<li>public继承概念由两部分组成：<font color="red">函数接口</font>（<font color="green">function interfaces</font>）继承和<font color="red">函数实现</font>（<font color="green">function implementations</font>）继承。</li>
<li><strong>声明一个<font color="red">pure virtual</font>函数的目的式为了让derived classes只继承函数接口。声明纯朴的<font color="red">impure virtual</font>函数的目的，是让derived classes函数继承改函数的接口和缺省实现。声明<font color="red">non-virtual</font>函数的目的是为了令derived classes继承函数的接口及一份强制性实现。</strong></li>
</ol>
<h4 id="条款35：考虑virtual函数以外的其他选择"><a href="#条款35：考虑virtual函数以外的其他选择" class="headerlink" title="条款35：考虑virtual函数以外的其他选择"></a>条款35：考虑virtual函数以外的其他选择</h4><ol>
<li>令客户通过public non-virtual成员函数间接调用private virtual函数，称为<font color="red">non-virtual interface（NVI）</font>手法。它是Template Method设计模式的独特表现形式。</li>
<li>Strategy设计模式简单应用：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// NVI写法</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">healthValue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">int</span> retVal = doHealthValue();</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">return</span> retVal;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">int</span> <span class="title">doHealthValue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Function Pointers写法</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span>;</span>    <span class="comment">// forward declaration</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">defaultHealthCalc</span><span class="params">(<span class="keyword">const</span> GameCharacter &amp;gc)</span></span>;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">typedef</span> <span class="title">int</span> <span class="params">(*HealthCalcFunc)</span><span class="params">(<span class="keyword">const</span> GameCharacter&amp;)</span></span>;</span><br><span class="line">    explicit GameCharacter(HealthCalcFunc hcf = defaultHealthCalc) : healthFunc(hcf) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">healthValue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> healthFunc(*<span class="keyword">this</span>); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    HealthCalcFunc healthFunc;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// std::fucntion写法</span></span><br><span class="line"><span class="comment">// 与FP写法不同的是，std::function对象相当于一个指向函数的泛化指针。</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span>;</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">defaultHealthCalc</span><span class="params">(<span class="keyword">const</span> GameCharacter &amp;gc)</span></span>;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> <span class="built_in">std</span>::function&lt;<span class="keyword">int</span> (<span class="keyword">const</span> Characater&amp;)&gt; HealthCalcFunc;</span><br><span class="line">    explicit GameCharacter(HealthCalcFunc hcf = defaultHealthCalc) : healthFunc(hcf) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">healthvalue</span><span class="params">()</span> <span class="keyword">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> healthFunc(*<span class="keyword">this</span>); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    HealthClacFunc healthFunc;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 古典设计模式写法</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HealthCalcFunc</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">int</span> <span class="title">calc</span><span class="params">(<span class="keyword">const</span> GameCharacter &amp;gc)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123; ... &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">HealthCalcFunc defaultHealthCalc;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameCharacter</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    explicit GameCharacter(HealthCalcFunc *phcf = &amp;defaultHealthCalc) : pHealthCalc(phcf) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">healthvalue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> pHealthcalc-&gt;calc(*<span class="keyword">this</span>); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    HealthCalcFunc *pHealthcalc;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<pre><code class="C++"><span class="comment">// 有着更惊人的弹性</span>

<span class="comment">// 健康计算函数</span>
<span class="function"><span class="keyword">short</span> <span class="title">calchealth</span><span class="params">(<span class="keyword">const</span> GameCharacter&amp;)</span></span>;
<span class="comment">// 为计算健康而设计的函数对象</span>
<span class="class"><span class="keyword">struct</span> <span class="title">HealthCalclator</span></span>
<span class="class">{</span>
    <span class="function"><span class="keyword">int</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="keyword">const</span> GameCharacter&amp;)</span> <span class="keyword">const</span></span>
<span class="function">    </span>{ ... }</code></pre>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>6 杂项讨论</title>
    <url>/2020/06/14/6-%E6%9D%82%E9%A1%B9%E8%AE%A8%E8%AE%BA/</url>
    <content><![CDATA[<h4 id="条款32：在未来时态下发展程序"><a href="#条款32：在未来时态下发展程序" class="headerlink" title="条款32：在未来时态下发展程序"></a>条款32：在未来时态下发展程序</h4><ol>
<li><strong>请避免”demand-apged“式的虚函数，那会使你习惯于”不让任何函数成为virtual，除非有人需要“</strong>。应该有自己的判断，决定函数的意义，而不要只为了图某人的方便就改变其定义。请确定所做的改变对于整个class的上下关系乃至它所表现的抽象性是合理的。</li>
<li><strong>请为每个class处理assignment和copy construction动作，即使没有人使用那个动作</strong>。如果不易完成，请声明为private，那就不会有人因为不经意调用编译器自动产生却行为错误的版本。</li>
<li>不要做出令人大吃一惊的怪异行为：努力让classes的操作符和函数拥有自然的语法和直观的语义。<strong>请和内置类型的行为保持一致</strong>。请让你的classes容易被正确地使用，不容易被误用。</li>
<li><strong>请努力写出可移植代码</strong>。</li>
<li><strong>设计你的代码，使系统改变所带来的冲击得以局部化</strong>。尽量采用封装性质，尽可能让实现细目成为private。如果可用，尽量用匿名namespaces或文件内的static对象和static函数。尽量避免设计出virtual base classes，因为这种casses必须被其每个derived classes初始化。请避免以RTTI作为设计基础并因而导致一层层的if-then-else语句，因为灭当class继承体系有改变，每一组这样的语句都得更新，而忘了其中某一项，编译器并不会给出警告。</li>
</ol>
<h4 id="条款33：将非尾端类（non-leaf-classes）设计为抽象类-（abstract-classes）"><a href="#条款33：将非尾端类（non-leaf-classes）设计为抽象类-（abstract-classes）" class="headerlink" title="条款33：将非尾端类（non-leaf classes）设计为抽象类 （abstract classes）"></a>条款33：将非尾端类（non-leaf classes）设计为抽象类 （abstract classes）</h4><ol>
<li><p>有个类继承体系：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Lizard --&gt; A(Animal)</span><br><span class="line">Chicken --&gt; A</span><br></pre></td></tr></table></figure>

<p> Animal负责具体化所有东吴的共同特征,Lizard和Chicken是需要特殊对待的两种动物,它们的简化定义像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Animal&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Animal&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lizard</span>:</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Lizard&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Chicken</span>:</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Chicken&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Chicken&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 考虑一段代码：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Lizard liz1;</span><br><span class="line">Lizard liz2;</span><br><span class="line">Animal *pAnimal1 = &amp;liz1;</span><br><span class="line">Animal *pAnimal2 = &amp;liz2;</span><br><span class="line">...</span><br><span class="line">*pAnimal1 = *pAnimal2;</span><br></pre></td></tr></table></figure>

<p> 其中有两个问题。</p>
<ul>
<li><p><strong>最后一行调用Animal class的assignment操作符，即使对象类型是Lizard</strong>。而只有例子<br>的Animal成分被修改，这就是所谓的<font color="red">部分复制</font>（<font color="green">partial assignments</font>）。在这个动作之后，liz1的Animal members于liz2相同，但是lizard members则没变化。</p>
</li>
<li><p>很多人用指针这样写。但复制动作很容易错用。</p>
<p>解决方法是让assignment操作符为<font color="red">virtual</font>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> Animal&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Animal&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lizard</span>:</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Animal&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Chicken</span>:</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> Chicken&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Animal&amp; rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>assignment操作符为了返回一个正确的reference类型，代表class，使得在class中为虚函数声明完全相同的参数类型（Animal）。意味着任何种类的Animal对象都可以出现在assignment操作符的右边：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">lizard liz;</span><br><span class="line">Chicken chick;</span><br><span class="line">Animal *pAnimal1 = &amp;liz;</span><br><span class="line">Animal *pAnimal2 = &amp;chick;</span><br><span class="line">...</span><br><span class="line">*pAnimal1 = *pAnimal2;  <span class="comment">// 将鸡赋值给蜥蜴</span></span><br></pre></td></tr></table></figure>

<p> 这就是<font color="red">异型赋值</font>。语言的强烈类型检验（strong typing）通常会将它们视为不合法。<br> 这使我们为难。一方面希望通过指针同型赋值，但又禁止通过那样的指针进行异型赋值。要区分这些情况，得在运行期才有办法，<font color="red"><strong>dynamic_cast可以协助完成上述愿望</strong></font>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Lizard&amp; Lizard::<span class="keyword">operator</span>=(<span class="keyword">const</span> Animal&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 确定rhs真的是一只蜥蜴</span></span><br><span class="line">    <span class="comment">//将rhs转为const Lizard&amp;,失败则抛出bad_cast exception</span></span><br><span class="line">    <span class="keyword">const</span> Lizard&amp; rhs_liz = <span class="keyword">dynamic_cast</span>&lt;<span class="keyword">const</span> Lizard&amp;&gt;(rhs);</span><br><span class="line">    <span class="comment">// proceed with a normal assignment of rhs_liz to *this;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 同型类型不需要dynamic_cast，成本太高，通过别的实现方式：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lizard</span> :</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Aniaml &amp;rhs);</span><br><span class="line">    Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Lizard &amp;rhs);   <span class="comment">// 加上这行</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Lizard&amp; Lizard::oeprator=(<span class="keyword">const</span> Animal &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 转型成功，调用正常的assignmen操作符，否则抛出bad_cast exception</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">operator</span>=(<span class="keyword">dynamic_cast</span>&lt;<span class="keyword">const</span> Lizard&amp;&gt;(rhs));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 运行时期的所有类型检验动作，以及对dynamic_casts的各种运用，都有其固有的缺点：某些编译器不支持dynamic_cast不说，它竟然要求每一次动作都需要<font color="red"><strong>待命捕捉bad_cast exceptions</strong></font>。为此，将clients可能在一开始就出现问题的赋值动作扼杀在编译器。阻止这种赋值动作的最简单方法就是，让operator=成为Animal的private函数：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Animal&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> ANimal &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lizard</span> :</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Lizard &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Chicken</span> :</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Chicken&amp; operaotr=(<span class="keyword">const</span> Chicken &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Lizard liz1, liz2;</span><br><span class="line">...</span><br><span class="line">liz1 = liz2;    <span class="comment">// good</span></span><br><span class="line">Chicken chick1, chick2;</span><br><span class="line">...</span><br><span class="line">chick1 = chick2;<span class="comment">// good</span></span><br><span class="line"></span><br><span class="line">Animal *pAnimal1 = &amp;liz1;</span><br><span class="line">Animal *pAnimal2 = &amp;chick1;</span><br><span class="line">...</span><br><span class="line">*pAnimal = *pAnimal2;   <span class="comment">// error！企图调用private Animal::operaor=</span></span><br></pre></td></tr></table></figure>

<p> 为了完成Animal对象间的相互赋值，可把private声明为protected，然而，前一个问题又出现了。因此，最简单的办法就是消除Animal对象之间相互赋值的需要，以下是新的继承体系：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Lzard --&gt; A(AbstractAnimal)</span><br><span class="line">Animal --&gt; A</span><br><span class="line">Chicken --&gt; A</span><br></pre></td></tr></table></figure>

 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AbstractAnimal</span> &#123;</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    AbstractAnimal&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> AbstractAnimal &amp;rhs);</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~AbstractAniaml() = <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Aniaml</span> :</span> <span class="keyword">public</span> AbstractAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Animal&amp; operaotr=(<span class="keyword">const</span> Animal &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lizard</span> :</span> <span class="keyword">public</span> AbstractAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Lizard&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Lizard &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Chicken</span> :</span> <span class="keyword">public</span> AbstractAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Chicken&amp; operaotr=(<span class="keyword">const</span> Chicken &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 剩余分析过程实际上体现了这样一种思想:<font color="red"><strong>当具体类被当做基类使用时,应该将具体类转变为抽象基类</strong></font>。<br> 然而有时候需要使用第三方库,并继承其中一个具体类,由于无法修改该库,也就无法将该具体类转为抽象基类,这是就需要采取其他选择：</p>
<ul>
<li>将具体类<strong>继承自既存的（程序库）具体类</strong>，注意assignment相关问题，和条款3的数组陷阱</li>
<li>试着在继承体系中<strong>找一个更高层的抽象类,然后继承它</strong></li>
<li><strong>以”所希望继承的那么程序库类”来实现新类</strong>.例如使用复合或private继承并提供相应接口.此策略不具灵活性</li>
<li>为”所希望继承的那么程序库类”<strong>定义一些non-member</strong>,不再定义新类<h4 id="条款34：如何在同一个程序中结合C-和C"><a href="#条款34：如何在同一个程序中结合C-和C" class="headerlink" title="条款34：如何在同一个程序中结合C++和C"></a>条款34：如何在同一个程序中结合C++和C</h4></li>
</ul>
</li>
<li><p>C++和C混合使用的前提之一就是编译器产生兼容的目标文件(.lib和.dll等).所谓”兼容”,指的是编译器在”预编译器相依的特性上”一致,如int和double大小,参数压栈机制等,只有在这个基础上才能讨论结合使用C++和C模块的问题</p>
</li>
<li><p>有四个事情需要考虑：<font color="red">name mangling（名字重整）</font>、<font color="red">statics（静态对象）初始化</font>、<font color="red">动态内存分配</font>、<font color="red">数据结构的兼容性</font>。</p>
<h5 id="Name-Mangling（名字重整）"><a href="#Name-Mangling（名字重整）" class="headerlink" title="Name Mangling（名字重整）"></a>Name Mangling（名字重整）</h5></li>
<li><p>Name mangling是C++用于支持函数重载的机制，它对函数名称进行一定的修正，使得每个函数有独一无二的名称，但是C不支持函数重载。当C++企图调用C中的某个函数时，会因为找不到重整的函数名而报错。<font color="red"><strong>因此要压抑name mangling，必须使用C++的extern “C”指令。</strong></font>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">drawLine</span><span class="params">(<span class="keyword">int</span> x1, <span class="keyword">int</span> y1, <span class="keyword">int</span> x2, in y2)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 技术上说，extern “C”意味着这个函数有C linkage。如果你写了供给其他语言的程序，那么也可以使用extern “C”，只要压抑了C++函数名称的name mangling程序，就可以了。<br> extern “C”也可以施行于整一组函数身上：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 预处理器符号__cplusplus只针对C++才有定义，可以选择来进行C/C++语言间的判断</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> &#123; </span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">drawLine</span><span class="params">(<span class="keyword">int</span> x1, <span class="keyword">int</span> y1, <span class="keyword">int</span> x2, <span class="keyword">int</span> y2)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">twiddleBits</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">char</span> bits)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">simulate</span><span class="params">(<span class="keyword">int</span> iterations)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h5 id="Statics的初始化"><a href="#Statics的初始化" class="headerlink" title="Statics的初始化"></a>Statics的初始化</h5><ol start="4">
<li>程序的入口实际上并不是main，而是编译器提供的特殊函数(如 mainCRTStartup(void)等，在mainCRTStartup主要任务之一就是全局对象(包括static class对象，全局对象,namespace作用域内的对象以及文件作用域内的对象)的初始化工作，由于C++支持动态初始化(如全局变量int b=a;)而C仅支持静态初始化，因此全局对象的动态初始化涉及到动态初始化就应该在C++中撰写main，而将C main重命名为realMain，然后让C++ main调用realMain，像这样: <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> <span class="comment">// implement this</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">realMain</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span>; <span class="comment">// C程序库的函数,用于完成主函数功能</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> <span class="comment">// C++代码</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span> realMain(argc, argv);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h5 id="动态内存分配"><a href="#动态内存分配" class="headerlink" title="动态内存分配"></a>动态内存分配</h5><ol start="5">
<li><p>动态内存分配规则很简单：C++部分使用new和delete，C部分使用malloc（及其变种）和free。new分配就delete删除，malloc分配就free释放。</p>
<h5 id="数据结构的兼容性"><a href="#数据结构的兼容性" class="headerlink" title="数据结构的兼容性"></a>数据结构的兼容性</h5></li>
<li><p>C和C++对于struct和内置类型变量是兼容的，因为C/C++对于struct的内存布局相同，但如果C++为struct加上非虚成员函数，由于struct内存布局不改变，其对象仍兼容于C，但如果加上虚函数，由于vtbl和vptr的存在，struct内存布局便发生改变，也就不再兼容于C。</p>
<p> 也就是说，在C和C++之间对数据结构做双向交流是安全的——前提是结构的定义式在C和C++中都可编译；如果为C++ struct加上非虚函数，虽然不再兼容于C，但可能不影响兼容性；其他改变如虚函数和继承等则会产生影响。</p>
</li>
</ol>
<h4 id="条款35：让自己习惯于标准C-语言"><a href="#条款35：让自己习惯于标准C-语言" class="headerlink" title="条款35：让自己习惯于标准C++语言"></a>条款35：让自己习惯于标准C++语言</h4><ol>
<li>在从1990年发行之后，<font color="green">The Annotated C++ Reference Manual</font>（<font color="red">ARM</font>）成了程序员的参考凭借。直到ISO/ANSI委员会对这个语言的标准化工作，最后这个参考的标准，ARM不再适合。</li>
<li>C++ ISO/ANSI标准规格，是编译器厂商的咨询对象，也是人们遇到问题与纠纷时的手边依据和最后仲裁。ARM出版后的这些年里，C++最重要几项改变如下：<ul>
<li><strong>增加了一些新的语言特性</strong>：RTTI、namespaces、bool、关键字mutable和explicit、enums作为重载函数参数所引发的类型晋升转换，以及在class定义区内直接为整数型const static class members设定初值的能力。</li>
<li><strong>扩充了Templates的弹性</strong>：允许member tempates存在、接纳”明白指示template当场实例化“的标准语法、允许function templates接受非类型自变量（non-type arguments）、可用class templates作为其他template的自变量。</li>
<li><strong>强化了异常处理机制（Exception handing）</strong>：编译期更严密检验exception specifications、允许unexpected函数抛出bad-exception对象。</li>
<li><strong>修改了内存分配例程</strong>：加入operator new[]和operator delete[]，内存未能分配成功时由operators new/new[]抛出一个exception，提供一个operators new/new[]新版本，在内存分配失败时返回0.</li>
<li><strong>增加了新的转型形式</strong>：static_cast，dynamic_cast，const_cast和reinterpret_cast。</li>
<li><strong>语言规则更为优雅精炼</strong>：重新定义虚函数时返回类型不再一定得与原定义完全符合，临时对象的寿命也有了明确规范。</li>
</ul>
</li>
<li>描述STL之前，有两个C++标准程序库特质需要知道：一是标准程序库中的每样东西几乎都是template；二是所有成分都位于namespace std内。<h5 id="Standard-Template-Library（STL）"><a href="#Standard-Template-Library（STL）" class="headerlink" title="Standard Template Library（STL）"></a>Standard Template Library（STL）</h5></li>
<li>STL不难理解，它以3个基本概念为基础：<font color="red">containers</font>，<font color="red">iterators</font>和<font color="red">algorithms</font>。Containers持有一系列对象。Iterators是一种类似指针的对象，用以遍历STL containers，Algorithms可用于STL containers身上的函数，以iterators来协助工作。</li>
<li>STL是可扩充的，只要遵循STL的标准，可以将自己的容器，迭代器，算法等结合STL使用。(要使自定义的迭代器适用于STL的泛型算法，需要了解C++的<font color="red">traits技法</font>，见Effective C++ 条款47)</li>
</ol>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>9 杂项讨论</title>
    <url>/2020/06/14/9-%E6%9D%82%E9%A1%B9%E8%AE%A8%E8%AE%BA/</url>
    <content><![CDATA[<p>这里有3个条款。第一个条款<strong>强调不可轻忽编译器警告信息</strong>。第二个条件<strong>总览C++<br>Standard，主要是TR1</strong>。最后条款<strong>总览Boost</strong>。很多机能都被在C++之后新特性中不断引入把。</p>
<h4 id="条款53：不要轻忽编译器的警告"><a href="#条款53：不要轻忽编译器的警告" class="headerlink" title="条款53：不要轻忽编译器的警告"></a>条款53：不要轻忽编译器的警告</h4><ol>
<li>严肃对待编译器发出的警告信息。</li>
<li>不要过度依赖编译器的报警能力，不同的编译器对待事情的态度并不相同。一旦移植到另一个编译器上，原本依赖的警告信息有可能消失。</li>
</ol>
<h4 id="条款54：让自己熟悉包括TR1在内的标准程序库"><a href="#条款54：让自己熟悉包括TR1在内的标准程序库" class="headerlink" title="条款54：让自己熟悉包括TR1在内的标准程序库"></a>条款54：让自己熟悉包括TR1在内的标准程序库</h4><ol>
<li>C++98列入C++标准程序库有以下成分：<ul>
<li><font color="red">STL</font>（<font color="green">Standard Template Library</font>，<font color="red">标准模板库</font>）。覆盖容器（containers如vector，string，map）、迭代器（iterators）、算法（algorithms如find，sort，transform）、函数对象（function objects如less，greater）、各种容器适配器（container adapters如stack，priority_queue）和函数对象适配器（function object adapters如mem_fun,not1).</li>
<li>Iostreams，覆盖用户自定缓冲功能、国际化I/O，以及预先定义好的对象cin，cout，cerr和clog。</li>
<li>国际化支持，多区域（multiple active locales）能力。像wchar_t（通常是16 bits/char）和wstring（由wchar_ts组成的stirngs）等类型促进Unicode。</li>
<li>数值处理，包括复数模板（complex）和纯数值数组（valaray）。</li>
<li>异常阶层体系（exception hierarchy），包括base class exception及其derived classes logic_error和runtime_error，以及更深继承的各个classes。</li>
<li>C89标准程序库。</li>
</ul>
</li>
<li>C++11相对于C++98/03有哪些显著的增强呢？<ul>
<li>通过内存模型、线程、原子操作等支持本地并行编程（Native Concurrency）</li>
<li>通过统一初始化表达式、auto、decltype、移动语义等来统一对泛型编程的支持</li>
<li>通过constexpr、POD等来更好地支持系统编程</li>
<li>通过内联命名空间、继承构造函数和右值引用等来更好地支持库的构建</li>
</ul>
</li>
<li>C++11 知识点<blockquote>
<p>auto关键字、for-each循环、右值及移动构造函数 + std::forward + std::move + stl容器新增的emplace_back()方法、std::thread库、std::chrono库、智能指针系列（std::shared_ptr/std::unique_ptr/std::weak_ptr）(智能指针的实现原理一定要知道，最好是自己实现过)、线程库std::thread+线程同步技术库std::mutex/std::condition_variable/std::lock_guard等、lamda表达式（JAVA中现在也常常考察lamda表达式的作用）、std::bind/std::function库、其他的就是一些关键字的用法(override、final、delete)，还有就是一些细节如可以像JAVA一样在类成员变量定义处给出初始化值。</p>
</blockquote>
</li>
</ol>
<h4 id="条款55：让自己熟悉Boost"><a href="#条款55：让自己熟悉Boost" class="headerlink" title="条款55：让自己熟悉Boost"></a>条款55：让自己熟悉Boost</h4><ol>
<li>Boost是C++开发者集结的社群。它和C++标准委员会之间有独一无二的密切关系，Boost由委员会成员创设，因此Boost成员和委员会成员由很大的重叠。Boost的目标是作为一个可被加入标准C++之各种功能的测试场。</li>
<li>Boost程序库对付的主题非常繁多，包括：<ul>
<li><strong>字符串与文本处理</strong>，覆盖具备类型安全（type-safe）的pointer-like格式化动作、正则表达式，以及语汇单元切割（tokenizing）和解析（parsing）。</li>
<li><strong>容器</strong>，覆盖数组、大小可变的bitsets以及多维数组。</li>
<li><strong>函数对象和高级编程</strong>，覆盖若干用来作为TR1机能基础的程序库。其中一个程序库是Lambda。</li>
<li>泛型编程（Generic programming），覆盖traits classes。</li>
<li>模板元编程（Template metaprogramming，TMP）覆盖一个针对编译器assertions而写的程序库。</li>
<li>数学和数值（Math and numerics），包括有理数、八元数和四元数（octonions and quaternions）、常见的公约数（divisor）和少见的多重运算、随机数。</li>
<li>正确性与测试（Correctness and testing），覆盖隐式模板接口（implicit template interfaces）。</li>
<li>数据结构，覆盖类型安全（type-safe）的unions以及tuple程序库。</li>
<li>语言间的支持（Inter-language support），包括C++和Python之间的无缝互操作性（seamless interoperability）。</li>
<li>内存，覆盖Pool程序库，用来做出高效率而区块大小固定的分配器，以及多变化的智能指针（smart pointers）。</li>
<li>杂项，包括CRC检验、日期和时间的处理、在文件系统上来回移动等等。</li>
</ul>
</li>
</ol>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>CMake语法主要框架</title>
    <url>/2020/06/14/CMake%E8%AF%AD%E6%B3%95%E4%B8%BB%E8%A6%81%E6%A1%86%E6%9E%B6/</url>
    <content><![CDATA[<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ---------- 主要使用方式 ----------</span></span><br><span class="line"><span class="keyword">command</span>(arg1 arg2 ...) <span class="comment"># 运行命令</span></span><br><span class="line"><span class="keyword">set</span>(var_name var_value) <span class="comment"># 定义变量，或者赋值</span></span><br><span class="line"><span class="keyword">command</span>(arg1 <span class="variable">$&#123;var_name&#125;</span>) <span class="comment"># 使用变量</span></span><br><span class="line"><span class="comment"># ---------- 工程配置部分 ----------</span></span><br><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION num) <span class="comment"># CMake最低版本号要求</span></span><br><span class="line"><span class="keyword">project</span>(cur_project_name) <span class="comment"># 项目信息</span></span><br><span class="line"><span class="keyword">set</span>(CMAKE_CXX_FLAGS <span class="string">"XXX"</span>) <span class="comment"># 设定编译器版本，如-std=c++11</span></span><br><span class="line"><span class="keyword">set</span>(CMAKE_BUILD_TYPE <span class="string">"XXX"</span>) <span class="comment"># 设定编译模式，如Debug/Release</span></span><br><span class="line"><span class="comment"># ---------- 依赖执行部分 ----------</span></span><br><span class="line"><span class="keyword">find_package</span>(std_lib_name VERSION REQUIRED) <span class="comment"># 引入外部依赖</span></span><br><span class="line"><span class="keyword">add_library</span>(&lt;name&gt; [STATIC|SHARED|MODULE] source1) <span class="comment"># 生成库烈性（动态，静态）</span></span><br><span class="line"><span class="keyword">include_directories</span>(<span class="variable">$&#123;std_lib_name_INCLUDE_DIRS&#125;</span>) <span class="comment"># 指定头文件include路径，相当于gcc的-l</span></span><br><span class="line"><span class="keyword">link_directories</span>(<span class="variable">$&#123;std_lib_name_INCLUDE_DIRS&#125;</span>) <span class="comment"># 指定动态链接库或静态链接库的路径，相当于gcc的-L</span></span><br><span class="line"><span class="keyword">add_executable</span>(cur_project_name XXX.cpp) <span class="comment"># 指定生成目标</span></span><br><span class="line"><span class="keyword">target_link_libraries</span>(<span class="variable">$&#123;std_lib_name_LIBRARIES&#125;</span>) <span class="comment"># 指定libraries路径，放在add_executable</span></span><br><span class="line"><span class="comment"># ---------- 其他辅助部分 ----------</span></span><br><span class="line"><span class="keyword">function</span>(function_name arg) <span class="comment"># 定义一个函数</span></span><br><span class="line"><span class="keyword">add_subdirectory</span>(dir) <span class="comment"># 添加一个子目录</span></span><br><span class="line">AUS_SOURCE_DIRECTORY(. SRC_LIST) <span class="comment"># 查找当前目录所有文件，保存到SRC_LIST变量中</span></span><br><span class="line"><span class="keyword">FOREACH</span>(one_dir <span class="variable">$&#123;SRC_LIST&#125;</span>)</span><br><span class="line">    <span class="keyword">MESSAGE</span>(<span class="variable">$&#123;one_dir&#125;</span>) <span class="comment"># 使用message进行打印</span></span><br><span class="line"><span class="keyword">ENDFOREACH</span>(onedir)</span><br><span class="line"><span class="comment"># ---------- 判断控制部分 ----------</span></span><br><span class="line"><span class="keyword">if</span>(expression)</span><br><span class="line">    COMMAND1(ARGS)</span><br><span class="line"><span class="keyword">ELSE</span>(expression)</span><br><span class="line">    COMMAND2(ARGS)</span><br><span class="line"><span class="keyword">ENDIF</span>(expression)</span><br><span class="line"><span class="comment"># expression</span></span><br><span class="line"><span class="keyword">IF</span>(var)</span><br><span class="line"><span class="keyword">IF</span>(<span class="keyword">NOT</span> var)</span><br><span class="line"><span class="keyword">IF</span>(var1 <span class="keyword">AND</span> var2)</span><br><span class="line"><span class="keyword">IF</span>(var1 <span class="keyword">OR</span> var2)</span><br><span class="line"><span class="keyword">IF</span>(<span class="keyword">COMMAND</span> cmd)</span><br><span class="line"><span class="keyword">IF</span>(<span class="keyword">EXISTS</span> dir)</span><br><span class="line"><span class="keyword">IF</span>(<span class="keyword">EXISTS</span> <span class="keyword">file</span>)</span><br><span class="line"><span class="keyword">IF</span>(<span class="keyword">IS_DIRECTORY</span> dirname)</span><br><span class="line"><span class="keyword">IF</span>(file1 <span class="keyword">IS_NEWER_THAN</span> file2)</span><br><span class="line"><span class="keyword">IF</span>(variable <span class="keyword">MATCHES</span> regex)</span><br><span class="line"><span class="comment"># range</span></span><br><span class="line"><span class="keyword">WHILE</span>(condition)</span><br><span class="line">    COMMAND1(ARGS)</span><br><span class="line">    // ...</span><br><span class="line"><span class="keyword">ENDWHILE</span>(condition)</span><br><span class="line"><span class="comment"># ---------- CMake基本常用指令 ----------</span></span><br><span class="line"><span class="keyword">ADD_DEFINITIONS</span> <span class="comment"># 为源文件的编译添加由-D引入的宏定义，如add_definitions(-DWin32)</span></span><br><span class="line"><span class="keyword">OPTION</span> <span class="comment"># 提供可以选择的选项，格式为：option(variable "description" initial_value)</span></span><br><span class="line"><span class="keyword">ADD_CUSTOM_COMMAND</span>/<span class="keyword">TARGET</span></span><br><span class="line"><span class="comment"># [COMMAND]：为工程添加自定义的构建规则</span></span><br><span class="line"><span class="comment"># [TARGET]：给指定名称的目标指定执行的命令，目标没有输出文件，并始终被构建</span></span><br><span class="line"><span class="keyword">ADD_DEPENDENCIES</span> <span class="comment"># 添加依赖</span></span><br><span class="line"><span class="keyword">INSTALL</span> <span class="comment"># 用于定义安装规则，安装内容包括目标二进制、动态库、静态库以及文件、目录、脚本</span></span><br><span class="line"><span class="keyword">TARGET_INCLUDE_DIRECTORIES</span> <span class="comment"># target_include_directories(&lt;target&gt;[SYSTEM][BEFORE]&lt;INTERFACE|PUBLIC|PRIVATE&gt;[items])</span></span><br><span class="line"><span class="keyword">SET_TARGET_PROPERTIES</span> <span class="comment"># 设置目标oad属性来改变它们构建的方式</span></span><br><span class="line"><span class="keyword">ENABLE_TESTING</span>/<span class="keyword">ADD_TEST</span></span><br><span class="line"><span class="comment"># [enable_testing]：控制Makefile是否构建test目标</span></span><br><span class="line"><span class="comment"># [add_test]：一般和enable_testing配合使用</span></span><br><span class="line"><span class="comment"># 格式ADD_TEST(testname Exename arg1 arg2 ...)，生成后make test测试</span></span><br><span class="line"><span class="comment"># ---------- CMake基本常用变量 ----------</span></span><br><span class="line">CMAKE_INSTALL_PREFIX <span class="comment"># 构建install的路径</span></span><br><span class="line">$ENV&#123;HOME&#125; <span class="comment"># HOME环境下的目录路径</span></span><br><span class="line">PROJECT_NAME <span class="comment"># 工程名变量</span></span><br><span class="line">&lt;PKG&gt;_INCUDE_DIR <span class="comment"># 导入包头文件全路径</span></span><br><span class="line">&lt;PKG&gt;_LIBRARIES <span class="comment"># 导入库文件的权路径</span></span><br><span class="line">PROJECT_SOURCE_DIR <span class="comment"># 构建工程的全路径</span></span><br><span class="line">CMAKE_VERSION <span class="comment"># Cmake版本号</span></span><br><span class="line">CMAKE_SOURCE_DIR <span class="comment"># 源码树顶层路径</span></span><br></pre></td></tr></table></figure>

<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">message</span>([SEND_ERROR|STATUS|FATAL_ERROR] <span class="string">"message"</span>)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Makefile</tag>
        <tag>CMake</tag>
      </tags>
  </entry>
  <entry>
    <title>8 定制new和delete</title>
    <url>/2020/06/14/8-%E5%AE%9A%E5%88%B6new%E5%92%8Cdelete/</url>
    <content><![CDATA[<p>以下operator new和operator delete的规则同样适用于operator new[]和operator<br>delete[]</p>
<h4 id="条款49：了解new-handler的行为"><a href="#条款49：了解new-handler的行为" class="headerlink" title="条款49：了解new-handler的行为"></a>条款49：了解new-handler的行为</h4><ol>
<li><p>当operator new无法满足某一内存分配需求时，就会抛出异常。在它抛出异常前，会先调用一个用户指定的错误处理函数，一个所谓的<code>new-handler</code>，为了指定这个函数，用户需要调用<code>set_new_handler</code>，那是声明于<new>的一个标准程序库函数：</new></p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> <span class="built_in">std</span> &#123;</span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*new_handler)</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">new_handler <span class="title">set_new_handler</span><span class="params">(new_handler p)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用时</span></span><br><span class="line"><span class="built_in">std</span>::set_new_handler(outOfMem);</span><br></pre></td></tr></table></figure>

<p> 一个设计良好的new-handler函数必须做以下事情：</p>
<ul>
<li><strong>让更多内存可被使用</strong>。让operator new的下一次内存分配动作可能成功，就让程序一开始执行分配一大块内存，当new-handler第一次被调用，将它们释放给程序使用。</li>
<li><strong>安装另一个new-handler</strong>。如果目前这个new-handler无法取得个更多可用内存，或许其他new-handler有。那么这个new-handler则set_new_handler来替换自己。下次当operator new调用new-handler时，调用的将是最新安装的那个。</li>
<li><strong>卸除new-handler</strong>。一旦没有安装任何new-handler，operator new会在内存分配不成功时抛出异常，这时候应该将null指针给set_new_handler。</li>
<li><strong>抛出bad_alloc（或派生自bad_alloc）的异常</strong>。这样异常不会被operator new捕捉，会被传播到内存索求处。</li>
<li><strong>不返回</strong>。调用abort或exit。</li>
</ul>
</li>
<li><p>使用class内自定义的operator new的set_new_handler可以替换global new-handler。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NewHandlerHolder</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 取得当前的的new-handler</span></span><br><span class="line">    explicit NewHandlerHolder(std::new_handler nh) : handler(nh) &#123;&#125;</span><br><span class="line">    <span class="comment">// 释放它</span></span><br><span class="line">    ~NewHandlerHolder() &#123;       </span><br><span class="line">        <span class="built_in">std</span>::set_new_handler(handler); </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">std</span>::new_handler handler;</span><br><span class="line">    <span class="comment">// 禁用copying函数</span></span><br><span class="line">    NewHandlerHolder(<span class="keyword">const</span> NewHandlerHolder&amp;);</span><br><span class="line">    NewHandlerHolder&amp; oeprator=(<span class="keyword">const</span> NewHandlerHolder&amp;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::<span class="function">new_handler <span class="title">set_new_handler</span><span class="params">(<span class="built_in">std</span>::new_handler p)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::new_handler currentHandler = <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">std</span>::new_handler Widget::set_new_handler(<span class="built_in">std</span>::new_handler p) <span class="keyword">throw</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">std</span>::new_handler oldHandler = currentHandler;</span><br><span class="line">    currentHandler = p;</span><br><span class="line">    <span class="keyword">return</span> oldHandler;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span>* Widget::<span class="function"><span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 安装Widget的new-handler</span></span><br><span class="line">    <span class="function">NewHandlerHolder <span class="title">h</span><span class="params">(<span class="built_in">std</span>::set_new_handler(currentHandler))</span></span>;</span><br><span class="line">    <span class="comment">// 恢复global new-handler</span></span><br><span class="line">    <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(size);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数声明，将在Widget对象分配失败时被调用</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">outOfMem</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 设定outOfMem为Widget的new-hander函数</span></span><br><span class="line">Widget::set_new_handler(outOfMem);</span><br><span class="line"><span class="comment">// 分配失败调用outOfMem</span></span><br><span class="line">Widget *pw1 = <span class="keyword">new</span> Widget;</span><br><span class="line"><span class="comment">// 分配失败调用global new-handler函数</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> *ps = <span class="keyword">new</span> <span class="built_in">std</span>::<span class="built_in">string</span>;</span><br><span class="line"><span class="comment">// 设定Widget专属的new-handing函数为null</span></span><br><span class="line">Widget::set_new_handler(<span class="number">0</span>);</span><br><span class="line"><span class="comment">// 如果分配失败，立刻抛出异常</span></span><br><span class="line">Widget *pw2 = <span class="keyword">new</span> Widget;</span><br></pre></td></tr></table></figure>
</li>
<li><p>现在设计一个可以被任何有需要的class使用的template：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NewHandlerSupport</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::<span class="function">new_handler <span class="title">set_new_handler</span><span class="params">(<span class="built_in">std</span>::new_handler p)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::new_handler currentHandler;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="built_in">std</span>::new_handler NewHandlerSupport&lt;T&gt;::set_new_handler(<span class="built_in">std</span>::new_handler p) <span class="keyword">throw</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">std</span>::new_handler oldHandler = currentHandler;</span><br><span class="line">    currentHandler = p;</span><br><span class="line">    <span class="keyword">return</span> oldHandler;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">void</span>* NewHandlerSupport&lt;T&gt;::<span class="function"><span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">NewHandlerHolder <span class="title">h</span><span class="params">(<span class="built_in">std</span>::set_new_handler(currentHandler))</span></span>;</span><br><span class="line">    <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(size);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="built_in">std</span>::new_handler NewHandlerSupport&lt;T&gt;::currenHandler = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对于显式声明nothrow，只能保证operator new不抛出异常，构造函数可能又new一些内存，这就无法强迫了</span></span><br><span class="line"><span class="comment">// Widget *pw = new (std::nothrow)Widget;</span></span><br></pre></td></tr></table></figure>

<p> <font color="red">怪异的循环模板模式</font>（<font color="green">curiously recurring template pattern</font>;<font color="red">CRTP</font>）。</p>
</li>
</ol>
<h4 id="条款50：了解new和delete的合理替换时机"><a href="#条款50：了解new和delete的合理替换时机" class="headerlink" title="条款50：了解new和delete的合理替换时机"></a>条款50：了解new和delete的合理替换时机</h4><ol>
<li><p>为什么要替换编译期提供的operator new或operator delete呢？由三个理由：</p>
<ul>
<li><strong>用来检测运用上的错误</strong>。如果new出的内存delete失败，会导致<font color="red">内存泄露</font>（<font color="green">memory leaks</font>）。new上多次delete会导致不确定行为。如果我们自行定义operator news，超额分配内存，在额外内存上放置byte patterns（签名，signatures）。这样delete时就可以发现分配区中有哪些点发生了overrun（写入点在分配区尾端之后）或underruns（写入点在分配区起点之前）。这时候就可以log那个事实了。</li>
<li><strong>为了强化效能</strong>。对于一些需求，包括大块内存、小块内存、大小混合型内存。它们接纳各种分配形态，从程序存活期间的少量区块动态分配，到大量短命对象的持续分配和归还。必须考虑<font color="red">破碎问题</font>（<font color="green">fragmentation</font>）。这会导致程序有总量足够但分散为很多小区块的自由内存，却无法分配大区块内存。</li>
<li><strong>为了收集使用上的统计数据</strong>。制定news和delete之前，先收集软件如何使用动态内存。分配区块的大小分配？寿命分布？FIFO次序还是LIFO次序或随机分配？不同执行阶段有不同分配/归还形态？任何时刻使用最大动态分配量是多少？</li>
</ul>
</li>
<li> <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> signature = <span class="number">0xDEADBEEF</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">char</span> Byte;</span><br><span class="line"><span class="comment">// 还有若干错误</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line">    <span class="comment">// 增加大小，使之能够塞入两个signatures</span></span><br><span class="line">    <span class="keyword">size_t</span> realSize = size + <span class="number">2</span> * <span class="keyword">sizeof</span>(<span class="keyword">int</span>);</span><br><span class="line">    <span class="keyword">void</span>* pMem = <span class="built_in">malloc</span>(realSize);</span><br><span class="line">    <span class="keyword">if</span> (!pMem) <span class="keyword">throw</span> bad_alloc();</span><br><span class="line">    <span class="comment">// 将signature写入内存的前段落和后段落</span></span><br><span class="line">    *(<span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>*&gt;(pMem)) = signature;</span><br><span class="line">    *(reinterrpret_cast&lt;<span class="keyword">int</span>*&gt;(<span class="keyword">static_cast</span>&lt;Byte*&gt;(pMem) + realSize - <span class="keyword">sizeof</span>(<span class="keyword">int</span>))) = signature;</span><br><span class="line">    <span class="comment">// 返回指针，指向第一个signature之后的内存位置</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;Byte*&gt;(pMem) + <span class="keyword">sizeof</span>(<span class="keyword">int</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> <font color="red">计算机体系结构</font>（<font color="green">computer architectures</font>）要求特定的类型必须放在特定内存地址上，例如指针的地址必须是<font color="red">4倍数</font>（<font color="green">for-byte aligned</font>）或doubles地址是<font oclor="red">8倍数</font>（<font color="green">eight-byte aligned</font>）。<font color="red">齐位</font>（<font color="green">alignment</font>）意义重大，C++要求operator news返回的指针都有适当的内存对齐。malloc就是在这样做的，所以operator new返回一个malloc指针是安全的。然而上面代码中返回一个malloc且偏移一个int大小的指针，没人保证它的安全！</p>
</li>
<li><p>摘要：</p>
<ul>
<li>为了检测运用错误。</li>
<li>为了收集动态分配内存之使用统计信息</li>
<li>为了增加分配和归还速度</li>
<li>为了降低缺省内存管理器带来的空间额外开销。</li>
<li>为了弥补缺省分配器中的非最佳齐位（suboptimal alignment）。</li>
<li>为了将相关对象成簇集中。</li>
<li>为了获得非传统的行为。</li>
</ul>
</li>
</ol>
<h4 id="条款51：编写new和delete时需固守常规"><a href="#条款51：编写new和delete时需固守常规" class="headerlink" title="条款51：编写new和delete时需固守常规"></a>条款51：编写new和delete时需固守常规</h4><ol>
<li>operator new应该内含一个无穷循环，并在其中尝试分配内存，如果它无法满足内存需求，应该调用new-handler。它也应该有能力处理0 bytes、null指针申请。class专属版本还应该处理”比正确大小更大的（错误）申请“。</li>
</ol>
<h4 id="条款52：写了placement-new也要写placement-delete"><a href="#条款52：写了placement-new也要写placement-delete" class="headerlink" title="条款52：写了placement new也要写placement delete"></a>条款52：写了placement new也要写placement delete</h4><ol>
<li> <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 正常的operator new</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span>)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line"><span class="comment">// global作用域中的正常签名式</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *rawMemory)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// class作用域中典型的签名式</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *rawMemory, <span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p> 如果operator new接受的参数除了一定会有个size_t之外还有其他，这就是所谓的<font color="red">placement new</font>。众多placement new版本中特别有用的一个是”接受一个指针指向对象该被构造之处“：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span>, <span class="keyword">void</span> *pMemory)</span> <span class="title">throw</span><span class="params">()</span></span>;  <span class="comment">// placement new</span></span><br></pre></td></tr></table></figure>

<p> 这个版本在C++ STL中，只要#include &lt;new&gt;就可以用它。<br> 举个例子，现在写一个class专属的operator new，要求接受一个ostream，用来log相关分配信息，同时写一个正常形式的class专属operator delete：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 非正常形式的new</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::size size, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    <span class="comment">// 正常class专属delete</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory, <span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这里有微妙的内存泄漏</span></span><br><span class="line"><span class="comment">// 调用operator new并传递cerr为其ostream实参，这个动作会在构造函数抛出异常时泄露内存</span></span><br><span class="line">Widget *pw = <span class="keyword">new</span> (<span class="built_in">std</span>::<span class="built_in">cerr</span>) Widget;</span><br></pre></td></tr></table></figure>

<p> 对于类似的new placement版本，operator delete如果接受额外参数，称为placement deletes。带有额外参数的operator new应该调用带相同额外参数的对应版本operator delete。  </p>
</li>
<li><p>为了消弭代码中的内存泄露，Widget有必要声明一个placement delete，对应于有志记功能（logging）的placement new：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 对于placement delete自动调用期间构造函数可能会抛出异常，代码会自动调用placement delete；如果没有抛出异常，则只会调用普通operator delete。<br> 顺带一提，成员函数的名称会掩盖外围作用域中的相同名称，假设有一个base class，其中声明唯一的placement operator new，用户会无法使用正常的new：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 会掩盖正常版本</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> :</span> <span class="keyword">public</span> Base</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">Base *pb = <span class="keyword">new</span> Base;                <span class="comment">// error</span></span><br><span class="line">Base *pb = <span class="keyword">new</span> (<span class="built_in">std</span>::<span class="built_in">cerr</span>) Base;    <span class="comment">// ok</span></span><br><span class="line">Derived *pb = <span class="keyword">new</span> (<span class="built_in">std</span>::<span class="built_in">clog</span>) Derived;<span class="comment">// error</span></span><br><span class="line">Derived *pb = <span class="keyword">new</span> Derived;          <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> 需要记住的时，C++在global作用域中提供了以下operator new：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// normal new</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span>)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line"><span class="comment">// placement new</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span>, <span class="keyword">void</span> *)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// nothrow new</span></span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span>, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="keyword">nothrow_t</span> &amp;)</span> <span class="title">throw</span><span class="params">()</span></span>;  <span class="comment">// 49</span></span><br></pre></td></tr></table></figure>

<p> 除非你要阻止class用户使用这些形式，否则确保它们在你生成的定制型operator new之外还可用。对于每个operator new也有对应的operator delete。如果希望这些函数有平常的行为，令你class专属版本调用global版本：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">StandardNewDeleteForms</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// normal new/delete</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(size); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory)</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123; ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(pMemory)</span></span>; &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// placement new/delete</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size, <span class="keyword">void</span> *ptr)</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(size, ptr); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory, <span class="keyword">void</span> *ptr)</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> ::opeator <span class="keyword">delete</span>(pMemory, ptr); &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// nothrow new/delete</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="keyword">nothrow_t</span> &amp;nt)</span> <span class="keyword">throw</span></span></span><br><span class="line"><span class="function">    </span>&#123; <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(size, nt); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="keyword">nothrow_t</span> &amp;)</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123; ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(pMemory)</span></span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 供想自定义扩充标准形式的用户，利用继承机制或using声明式（33）取得标准形式</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> :</span> <span class="keyword">public</span> StandardNewDeleteForms</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">using</span> StandardNewDeleteForms::<span class="keyword">operator</span> <span class="keyword">new</span>;</span><br><span class="line">    <span class="keyword">using</span> StandardNewDeleteForms::<span class="keyword">operator</span> <span class="keyword">delete</span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="built_in">std</span>::<span class="keyword">size_t</span> size, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">(<span class="built_in">std</span>::bad_alloc)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> oeprator <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *pMemory, <span class="built_in">std</span>::ostream &amp;logStream)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ol>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker数据卷</title>
    <url>/2020/06/12/Docker%E6%95%B0%E6%8D%AE%E5%8D%B7/</url>
    <content><![CDATA[<h2 id="数据卷"><a href="#数据卷" class="headerlink" title="数据卷"></a>数据卷</h2><blockquote>
<p>使用Docker过程中，需要对数据进行持久化，或在多个容器间共享数据，需要容器的数据管理操作</p>
</blockquote>
<ul>
<li>数据管理主要有两种方式：<ul>
<li>数据卷（Data Volumes）：容器数据直接映射到本地主机环境</li>
<li>数据卷容器（Data Volume Containers）：使用特定容器维护数据卷</li>
</ul>
</li>
</ul>
<h4 id="数据卷-1"><a href="#数据卷-1" class="headerlink" title="数据卷"></a>数据卷</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># e.g.: training/webapp镜像创建web容器，并创建数据卷挂载到容器的/webapp目录</span></span><br><span class="line"><span class="comment"># 创建数据卷，-P是映射本地临时端口</span></span><br><span class="line"><span class="comment"># 挂在主机目录为数据卷: from/path:to/path</span></span><br><span class="line"><span class="comment"># 指定数据卷读写权限: ro/rw</span></span><br><span class="line">$ docker run -d -P --name web -v /src/webapp:/opt/webapp:ro training/webapp python app.py</span><br></pre></td></tr></table></figure>

<h4 id="数据卷容器"><a href="#数据卷容器" class="headerlink" title="数据卷容器"></a>数据卷容器</h4><blockquote>
<p>创建数据卷容器dbdata，创建数据卷挂载到容器dbdata</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it -v /dbdata --name dbdata ubuntu</span><br></pre></td></tr></table></figure>

<blockquote>
<p>其他容器对其进行挂载，之后三方任何一方修改各方都可见</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it --volumes-from dbdata --name db1 ubuntu</span><br><span class="line">$ docker run -it --volumes-from dbdata --name db2 ubuntu</span><br></pre></td></tr></table></figure>

<blockquote>
<p>删除最后挂载容器需要使用<code>docker rm -v</code>同时删除关联容器</p>
</blockquote>
<hr>
<h3 id="使用数据卷容器迁移数据"><a href="#使用数据卷容器迁移数据" class="headerlink" title="使用数据卷容器迁移数据"></a>使用数据卷容器迁移数据</h3><h4 id="备份"><a href="#备份" class="headerlink" title="备份"></a>备份</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from dbdata -v $(<span class="built_in">pwd</span>):/backup --name worker ubuntu tar cvf /backup/backup.tar /dbdata</span><br></pre></td></tr></table></figure>

<h4 id="恢复"><a href="#恢复" class="headerlink" title="恢复"></a>恢复</h4><blockquote>
<p>首先创建容器dbdata2</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -v /dbupdate --name dbdata2 ubuntu ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<blockquote>
<p>新容器挂载dbdata2，然后untar解压备份文件到所挂载的容器卷中</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from dbdata2 -v $(<span class="built_in">pwd</span>):/backup busybox tar xvf /backup/backup.tar</span><br></pre></td></tr></table></figure>

<h2 id="端口映射和容器互联"><a href="#端口映射和容器互联" class="headerlink" title="端口映射和容器互联"></a>端口映射和容器互联</h2><h3 id="端口映射"><a href="#端口映射" class="headerlink" title="端口映射"></a>端口映射</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 可以-p映射多端口-p po -p po</span></span><br><span class="line"><span class="comment"># 可以指定ip:po:po</span></span><br><span class="line"><span class="comment"># 还可以本地随机端口ip::po</span></span><br><span class="line"><span class="comment"># 还可以指定udp端口ip:po:po/udp</span></span><br></pre></td></tr></table></figure>

<h3 id="容器互联"><a href="#容器互联" class="headerlink" title="容器互联"></a>容器互联</h3><blockquote>
<p>创建新容器</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -d --name db training/postgres</span><br></pre></td></tr></table></figure>

<blockquote>
<p>创建新容器连接到db容器</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --link　name:alias</span></span><br><span class="line">$ docker run -d -P --name web --link db:db traning/webapp</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker的dockerfile编写</title>
    <url>/2020/06/12/Docker%E7%9A%84dockerfile%E7%BC%96%E5%86%99/</url>
    <content><![CDATA[<h2 id="使用Dockerfile创建镜像"><a href="#使用Dockerfile创建镜像" class="headerlink" title="使用Dockerfile创建镜像"></a>使用Dockerfile创建镜像</h2><ul>
<li>Dockerfile分为四部分<ul>
<li>基础镜像信息</li>
<li>维护者信息</li>
<li>镜像操作指令</li>
<li>容器启动时执行指令<h3 id="detail"><a href="#detail" class="headerlink" title="detail"></a>detail</h3><table>
<thead>
<tr>
<th align="left">指令</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>FROM</code></td>
<td align="left">指定基础镜像</td>
</tr>
<tr>
<td align="left"><code>MAINTAINER</code></td>
<td align="left">维护者信息</td>
</tr>
<tr>
<td align="left"><code>RUN</code></td>
<td align="left">运行命令</td>
</tr>
<tr>
<td align="left"><code>CMD</code></td>
<td align="left">启动容器时默认执行命令，最多只能执行一个命令</td>
</tr>
<tr>
<td align="left"><code>LABEL</code></td>
<td align="left">元数据标签信息</td>
</tr>
<tr>
<td align="left"><code>EXPOSE</code></td>
<td align="left">监听端口</td>
</tr>
<tr>
<td align="left"><code>ENV</code></td>
<td align="left">环境变量</td>
</tr>
<tr>
<td align="left"><code>ADD</code></td>
<td align="left">复制&lt;src&gt;到容器&lt;dest&gt;，如果是tar自动解压，src可以是URL</td>
</tr>
<tr>
<td align="left"><code>COPY</code></td>
<td align="left">复制本地&lt;src&gt;到容器&lt;dest&gt;</td>
</tr>
<tr>
<td align="left"><code>ENTRYPOINT</code></td>
<td align="left">指定镜像默认入口，最多只有一个，可以被<code>docker run --entrypoint</code>覆盖掉</td>
</tr>
<tr>
<td align="left"><code>VOLUME</code></td>
<td align="left">创建数据卷挂载点</td>
</tr>
<tr>
<td align="left"><code>USER</code></td>
<td align="left">指定运行容器时的USER或UID</td>
</tr>
<tr>
<td align="left"><code>WORKDIR</code></td>
<td align="left">为<code>RUN</code>、<code>CMD</code>和<code>ENTRYPOINT</code>指令配置工作目录</td>
</tr>
<tr>
<td align="left"><code>ARG</code></td>
<td align="left">指定一些镜像内使用的参数</td>
</tr>
<tr>
<td align="left"><code>ONBUILD</code></td>
<td align="left">当创建的镜像为其他镜像的基础镜像时执行的操作命令</td>
</tr>
<tr>
<td align="left"><code>STOPSIGNAL</code></td>
<td align="left">指定创建镜像启动的容器接收退出的信号值</td>
</tr>
<tr>
<td align="left"><code>HEALTHCHECK</code></td>
<td align="left">配置启动容器如何进行健康检查</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li>使用<code>.dockerignore</code>文件可以让Docker忽略匹配路径下的目录和文件<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># comment</span><br><span class="line">*/temp*</span><br><span class="line">*/*/temp*</span><br><span class="line">tmp?</span><br><span class="line">~*</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="desc"><a href="#desc" class="headerlink" title="desc"></a>desc</h3><table>
<thead>
<tr>
<th align="left">指令</th>
<th align="left">格式</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>FROM</code></td>
<td align="left"><code>&lt;image&gt;:&lt;tag&gt;</code> || <code>&lt;image&gt;@&lt;digest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>MAINTAINER</code></td>
<td align="left"><code>&lt;name&gt;</code></td>
</tr>
<tr>
<td align="left"><code>RUN</code></td>
<td align="left">shell:<code>&lt;command&gt;</code> || exec:<code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>CMD</code></td>
<td align="left"><code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code> || <code>command param1 param2</code> || [“param1”, “param2”]</td>
</tr>
<tr>
<td align="left"><code>LABEL</code></td>
<td align="left"><code>&lt;key&gt;=&lt;value&gt;[&lt;key&gt;=&lt;value&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>EXPOSE</code></td>
<td align="left"><code>&lt;port&gt;[&lt;port&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>ENV</code></td>
<td align="left"><code>&lt;key&gt; &lt;value&gt;</code> || <code>&lt;key&gt;=&lt;value&gt;[&lt;key&gt;=&lt;value&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>ADD</code></td>
<td align="left"><code>&lt;src&gt; &lt;dest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>COPY</code></td>
<td align="left"><code>&lt;src&gt; &lt;dest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>ENTRYPOINT</code></td>
<td align="left">shell:<code>command param1 param2</code> || exec: <code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>VOLUME</code></td>
<td align="left"><code>[&quot;/data&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>USER</code></td>
<td align="left"><code>daemon</code></td>
</tr>
<tr>
<td align="left"><code>WORKDIR</code></td>
<td align="left"><code>/path/to/workdir</code>，如果是相对路径，路径是基于前个<code>WORKDIR</code>的</td>
</tr>
<tr>
<td align="left">### exampe</td>
<td align="left"></td>
</tr>
<tr>
<td align="left"><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">FROM debian:jessie</span><br><span class="line">MAINTAINER NGINX Docker Maintainers &quot;docker-maint@nginx.com&quot;</span><br><span class="line">ENV NGINX_VERSION 1.10.1-1-jessie</span><br><span class="line">RUN apt-key adv --keyserver hkp://pgp.mit.edu:80 --recv-keys ....</span><br><span class="line">RUN ln -sf /dev/stdout /var/log/nginx/access.log \</span><br><span class="line">        $$ ln -sf /dev/stderr /var/log/nginx/error.log</span><br><span class="line">EXPOSE 80 443</span><br><span class="line">CMD [&quot;nginx&quot;, &quot;-g&quot;, &quot;daemon off;&quot;]</span><br></pre></td></tr></table></figure></td>
<td align="left"></td>
</tr>
</tbody></table>
<h3 id="sys"><a href="#sys" class="headerlink" title="sys"></a>sys</h3><h4 id="如果使用alpine镜像"><a href="#如果使用alpine镜像" class="headerlink" title="如果使用alpine镜像"></a>如果使用alpine镜像</h4><blockquote>
<p>安装软件包需要用apk包管理器替换apt工具</p>
</blockquote>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ apk add --no-cache &lt;package&gt;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>可以从<a href="https://pkgs.alpinelinux.org/packages" target="_blank" rel="noopener">alpine</a>搜索并确定安装包名称<br>如果需要指定社区的安装包，则：</p>
</blockquote>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">echo</span> <span class="string">"http://dl-4.alpinelinux.org/alpine/edge/teseting"</span> &gt;&gt; /etc/apk/repositories</span><br><span class="line">$ apk --update add --no-cache &lt;package&gt;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>7 模板与泛型编程</title>
    <url>/2020/06/14/7-%E6%A8%A1%E6%9D%BF%E4%B8%8E%E6%B3%9B%E5%9E%8B%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>C++<br>templates的最初发展动机很直接：让我们得以建立type-sfae的容器（vector、list、map等）。后来发现templates有能力完成愈多可能的变化。<font color="red">泛型编程</font>（<font color="green">generic<br>programming</font>）——<em>写出的代码和其所处理的对象类型彼此独立</em>（for_each、find、merge等）。最终人们发现C++<br>template机制自身是完整的<font color="red">图灵机</font>（<font color="green">Turing-complete</font>）。于是导出<font color="red">模板元编程</font>（<font color="green">template<br>metaprogramming</font>），创造出在C++编译器内执行并于编译完成时停止执行的程序。</p>
<h4 id="条款41：了解隐式接口和编译期多态"><a href="#条款41：了解隐式接口和编译期多态" class="headerlink" title="条款41：了解隐式接口和编译期多态"></a>条款41：了解隐式接口和编译期多态</h4><ol>
<li><p>面向对象编程世界总是以<font color="red">显示接口</font>（<font color="green">explicit interfaces</font>）和<font color="red">运行期多态</font>（<font color="green">runtime polymorphism</font>）解决问题。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Widget();</span><br><span class="line">    <span class="keyword">virtual</span> ~Widget();</span><br><span class="line">    virutal <span class="built_in">std</span>::<span class="keyword">size_t</span> size() <span class="keyword">const</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">normalize</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(Widget &amp;other)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doProcessing</span><span class="params">(Widget &amp;w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (w.size() &gt; <span class="number">10</span> &amp;&amp; w != someNastyWidget)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">Widget <span class="title">temp</span><span class="params">(w)</span></span>;</span><br><span class="line">        temp.normalize();</span><br><span class="line">        temp.swap(w);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> template及泛型编程的世界，显式接口和运行期多态仍然存在，但重要性降低。反倒是<font color="red">隐式接口</font>（<font color="green">implicit interfaces</font>）和<font color="red">编译期多态</font>（<font color="green">compile-time polymorphism</font>）移到前头。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doProcessing</span><span class="params">(T &amp;w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (w.size() &gt; <span class="number">10</span> &amp;&amp; w != someNastyWidget)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">T <span class="title">temp</span><span class="params">(w)</span></span>;</span><br><span class="line">        temp.normalize();</span><br><span class="line">        temp.swap(w);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这组表达式便是T必须支持的一组隐式接口（implicit interface）。凡涉及w相关的函数调用，如operator&gt;和operator!=，有可能造成template<font color="red">具体化</font>（<font color="green">instantiated</font>），使这些调用成功，这样的具现行为发生在编译期，这就是所谓的编译期多态（compile-time polymorphism）。</p>
</li>
<li><p>对classes而言接口是显式的（explicit），以函数签名为中心。多态则是通过virtual函数发生于运行期。对template参数而言，接口是隐式的（implicit），奠基于有效表达式。多态则是通过template具现化和函数重载解析（function overloading resolution）发生于编译期。</p>
</li>
</ol>
<h4 id="条款42：了解typename的双重意义"><a href="#条款42：了解typename的双重意义" class="headerlink" title="条款42：了解typename的双重意义"></a>条款42：了解typename的双重意义</h4><ol>
<li>template内出现的名称如果相依于某个template参数，称之为<font color="red">从属名称</font>（<font color="green">dependent names</font>）。如果从属名称在class内呈嵌套状，称之为<font color="red">嵌套从属名称</font>（<font color="green">nested dependent name</font>），也就是嵌套从属名称并且指涉某类型；否则一个并不依赖任何template参数的名称。这样的名称是<font color="red">非从属名称</font>（<font color="green">non-dependent names</font>）。</li>
<li>任何当你想要在template中指涉一个嵌套嵌套类型名称，必须在它的前一个位置放上关键字typename。这一规则的例外是，typename不可以出现在base classes list内的嵌套从属类型名称之前，也不可在member initialization list中作为base class修饰符。例如： <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// base class list中不允许typename</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> :</span> <span class="keyword">public</span> Base&lt;T&gt;::Nested </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// mem init list中不允许typename</span></span><br><span class="line">    expliit Derived(int x) : Base&lt;T&gt;::Nested(x) //</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 既不在base class list中也不在mem init list中，需要加上typename</span></span><br><span class="line">        <span class="keyword">typename</span> Base&lt;T&gt;::Nested temp;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">workWithIterator</span><span class="params">(IterT iter)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// traits class相当于类型为IterT对象所指对象的类型，如果IterT是list&lt;string&gt;::iterator, temp的类型就是string</span></span><br><span class="line">    <span class="keyword">typename</span> <span class="built_in">std</span>::iterator_traits&lt;IterT&gt;::<span class="function">value_type <span class="title">temp</span><span class="params">(*iter)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款43：学习处理模板化基类内的名称"><a href="#条款43：学习处理模板化基类内的名称" class="headerlink" title="条款43：学习处理模板化基类内的名称"></a>条款43：学习处理模板化基类内的名称</h4><ol>
<li><p>以下代码编译时出错：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">base</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">printBase</span><span class="params">()</span> </span>&#123; <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"base"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">derived</span> :</span> <span class="keyword">public</span> base&lt;T&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">printDerived</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"derived"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        printBase();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 问题在于当编译器遭遇class template derived定义式时，并不知道它继承什么样的class，它不知道T是个template参数，不到后来具现化无法确切知道它是什么。而不知道T是什么，也就不知道class base看起来像什么。</p>
</li>
<li><p>针对某个类产生一个特化版，需要在class定义式最前头加上“template&lt;&gt;”。这就是所谓的<font color="red">模板全特化</font>（<font color="green">total template specialization</font>）。对于某个被特化的base class templates，那个特化版本可能不提供和一般性template相同的接口，因此它往往拒绝在<font color="green">templatized base classes</font>（<font color="red">模板化基类</font>）内<strong>寻找</strong>继承而来的名称。就某种意义上来说，从Object Oriented C++跨进Template C++就不是畅通无阻了。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// example</span></span><br><span class="line"><span class="keyword">template</span>&lt;&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span>&lt;int&gt;</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    printBase() </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"A::int special"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>为了进入templatized base classes，有三个办法：</p>
<ul>
<li><p>第一是在调用base class函数动作之前加上“this-&gt;”</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">this</span>-&gt;printBase();</span><br></pre></td></tr></table></figure>
</li>
<li><p>第二是使用using声明式。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> A&lt;T&gt;::printBase();</span><br></pre></td></tr></table></figure>
</li>
<li><p>第三种是明白指出调用的函数位于base class内。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">A&lt;T&gt;::printBase();</span><br></pre></td></tr></table></figure>

<p>这是最不让人满意的揭发，如果被调用的是virtual函数，上述的<font color="red">明确资格修饰</font>（<font color="green">explicit qualification</font>）会关闭“virtual绑定行为”。</p>
</li>
</ul>
</li>
</ol>
<h4 id="条款44：将与参数无关的代码抽离templates"><a href="#条款44：将与参数无关的代码抽离templates" class="headerlink" title="条款44：将与参数无关的代码抽离templates"></a>条款44：将与参数无关的代码抽离templates</h4><ol>
<li><p>举个例子，用固定尺寸的正方矩阵编写一个template：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// n * n矩阵</span></span><br><span class="line">tepmlate&lt;<span class="keyword">typename</span> T, <span class="built_in">std</span>::<span class="keyword">size_t</span> n&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SquareMatrix</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 求逆矩阵</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">invert</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line">SquareMatrix&lt;<span class="keyword">double</span>, <span class="number">5</span>&gt; sm1;</span><br><span class="line">sm1.invert();</span><br><span class="line">SquareMatrix&lt;<span class="keyword">double</span>, <span class="number">10</span>&gt; sm2;</span><br><span class="line">sm2.invert();</span><br></pre></td></tr></table></figure>

<p> 这个template接受一个类型参数T，还接受一个size_t的参数，这是<font color="red">非类型参数</font>（<font color="green">non-type parameter</font>）。这些函数并非完全相同，除了常量5和10的区别外完全相同，这就是template引出<strong>代码膨胀</strong>的经典例子。改进方式：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SquareMatrixBase</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    SquareMatrixBase(<span class="built_in">std</span>::<span class="keyword">size_t</span> n, T *pMem) : size(n), pData(pMem) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">setDataPtr</span><span class="params">(T *ptr)</span> </span>&#123; pData = ptr; &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">std</span>::<span class="keyword">size_t</span> size;</span><br><span class="line">    T *pData;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="built_in">std</span>::<span class="keyword">size_t</span> n&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SquareMatrix</span> :</span> <span class="keyword">private</span> SquareMatrixBase&lt;T&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    SquareMatrix() : SquareMatrixBase&lt;T&gt;(n, data) &#123;&#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T data[n * n];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 动态分配办法</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="built_in">std</span>::<span class="keyword">size_t</span> n&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SquareMatrix</span> :</span> <span class="keyword">private</span> SquareMatrixBase&lt;T&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    SquareMatrix() : SquareMatrixBase&lt;T&gt;(n, <span class="number">0</span>), pData(<span class="keyword">new</span> T[n * n])</span><br><span class="line">    &#123; <span class="keyword">this</span>-&gt;setDataPtr(pData.get()); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    boost::scoped_array&lt;T&gt; pData; <span class="comment">// std::array??</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>这个条款讨论的是<font color="green">non-type template parameters</font>（非类型模板参数）带来的膨胀，其实<font color="red">type parameters</font>（类型参数）也会导致膨胀。因此凡templates持有指针者应该对每个成员函数使用唯一一份底层实现。这意味着实现某些成员函数操作<font color="red">强型指针</font>（<font color="green">strongly typed pointers</font>，即T*），令它们调用<font color="red">无类型指针</font>（<font color="green">untyped pointers</font>，即void*）的函数。</p>
</li>
<li><p>因非类型模板参数而造成的代码膨胀，往往可消除，做法是以函数参数或class成员变量替换template参数。因类型参数而造成的代码膨胀，往往可降低，做法是让带有完全相同<font color="red">二进制表述</font>（<font color="green">binary representations</font>）的<font color="red">具体类型</font>（<font color="green">instantiation types</font>）共享实现码。</p>
</li>
</ol>
<h4 id="条款45：运用成员函数模板接受所有兼容类型"><a href="#条款45：运用成员函数模板接受所有兼容类型" class="headerlink" title="条款45：运用成员函数模板接受所有兼容类型"></a>条款45：运用成员函数模板接受所有兼容类型</h4><ol>
<li><p>智能指针（Smart pointers）是“行为像指针“的对象，提供指针没有的机能。真实的指针支持隐式转换（inplicit conversions）。而用户自定义的智能指针模拟转换有点麻烦：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Top</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Middle</span> :</span> <span class="keyword">public</span> Top &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bottom</span> :</span> <span class="keyword">public</span> Middle &#123; ... &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmartPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 以other的heldPtr初始化this的heldPtr</span></span><br><span class="line">    tempalte&lt;<span class="keyword">typename</span> U&gt;</span><br><span class="line">    SmartPtr(<span class="keyword">const</span> SmartPtr&lt;U&gt; &amp;oher) : heldPtr(other.get()) &#123; ... &#125;</span><br><span class="line">    <span class="function">T* <span class="title">get</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> heldPtr; &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T* heldPtr;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 根据对象u创建对象t，根据SmartPtr&lt;U&gt;创建SmartPtr<t>，同一个template不同具现体称为泛化（generalized）copy构造函数。以上使用member initialization list来初始化SmartPtr&lt;t&gt;内类型为T<em>的成员变量，并以类型为U</em>的指针为初值。</t></p>
</li>
<li><p>成员函数模板作用不限于构造函数。TR1的shared_ptr支持所有兼容它的内置指针、shared_ptrs、auto_ptrs、week_ptrs的构造行为以及赋值作用：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">shared_ptr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// copy构造函数</span></span><br><span class="line">    <span class="built_in">shared_ptr</span>(<span class="built_in">shared_ptr</span> <span class="keyword">const</span> &amp;r);</span><br><span class="line">    <span class="comment">// copy assignment</span></span><br><span class="line">    <span class="built_in">shared_ptr</span>&amp; <span class="keyword">operator</span>=(<span class="built_in">shared_ptr</span> <span class="keyword">const</span> &amp;r);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 任何兼容的内置指针</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Y</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">explicit</span> <span class="title">shared_ptr</span>(<span class="title">Y</span> *<span class="title">p</span>);</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// shared_ptr</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Y</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">shared_ptr</span>(<span class="title">shared_ptr</span>&lt;Y&gt; <span class="title">const</span> &amp;<span class="title">r</span>);</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// week_ptr</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Y</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">explicit</span> <span class="title">shared_ptr</span>(<span class="title">week_ptr</span>&lt;Y&gt; <span class="title">const</span> &amp;<span class="title">r</span>);</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// auto_ptr</span></span><br><span class="line">    <span class="keyword">template</span>&lt;classs Y&gt;</span><br><span class="line">    <span class="function">explcit <span class="title">shared_ptr</span><span class="params">(<span class="built_in">auto_ptr</span>&lt;Y&gt; &amp;r)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 赋值</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Y</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">shared_ptr</span>&amp; <span class="title">operator</span>=(<span class="title">shared_ptr</span>&lt;Y&gt; <span class="title">const</span> &amp;<span class="title">r</span>);</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Y</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">shared_ptr</span>&amp; <span class="title">operator</span>=(<span class="title">auto_ptr</span>&lt;Y&gt; &amp;<span class="title">r</span>);</span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 在class内声明泛型copy构造函数（member template）并不会阻止编译期生成它们自己的copy构造函数（一个non-template）。</p>
</li>
</ol>
<h4 id="条款46：需要类型转换时请为模板定义非成员函数"><a href="#条款46：需要类型转换时请为模板定义非成员函数" class="headerlink" title="条款46：需要类型转换时请为模板定义非成员函数"></a>条款46：需要类型转换时请为模板定义非成员函数</h4><ol>
<li><p>参考条款24：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Rational(<span class="keyword">const</span> T &amp;numerator = <span class="number">0</span>, <span class="keyword">const</span> T &amp;denominator = <span class="number">1</span>);</span><br><span class="line">    <span class="function"><span class="keyword">const</span> T <span class="title">numerator</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">const</span> T <span class="title">denominator</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">const</span> Rational&lt;T&gt; <span class="keyword">operator</span>*(<span class="keyword">const</span> Rational&lt;T&gt; &amp;lhs, <span class="keyword">const</span> Rational&lt;T&gt; &amp;rhs)</span><br><span class="line">&#123; ... &#125;</span><br><span class="line"></span><br><span class="line">Rational&lt;<span class="keyword">int</span>&gt; oneHalf(<span class="number">1</span>, <span class="number">2</span>);        <span class="comment">// 这个例子来自条款24</span></span><br><span class="line">Rational&lt;<span class="keyword">int</span>&gt; result = oneHalf * <span class="number">2</span>; <span class="comment">// 错误，无法通过编译。</span></span><br></pre></td></tr></table></figure>

<p> 它给我们的启示是，模板化的Rational内的某些东西似乎和其non-template版本不同。编译期不知道它要调用哪个函数。operator*的第一个参数被声明为Rational&lt;T&gt;，而传递给operator*的的第一个实参的类型是Rational&lt;int&gt;，所以T是int。operator*的第二参数被声明为Rational<t>，但传递给operator*的第二实参的类型是int。你期望编译器使用Rational&lt;int&gt;的non-explicit构造函数将2转换为Rational&lt;int&gt;，但它不这么做。template实参推导过程中并不考虑采纳”通过构造函数而发生的“隐式类型转换。</t></p>
</li>
<li><p>只要利用一个事实，可以缓和编译器在template实参推导方面受到的挑战：<strong>template class内的friend声明式可以指涉某个特定函数</strong>。class templates并不依赖template实参推导（后者只施行于function templates身上），所以编译器总是能够在class Rational<t>具现化时得知T。令Rational<t> class声明适当的operator*为其friend函数，可简化整个问题：</t></t></p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rational</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 为了让这个函数自动具现化，在class内声明non-member函数的唯一方法是让它称为一个friend</span></span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">const</span> Rational <span class="keyword">operator</span>*(<span class="keyword">const</span> Rational &amp;lhs, <span class="keyword">const</span> Rational &amp;rhs)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> Rational(lhs.numerator() * rhs.numerator(), lhs.denominator() * rhs.denominaotr());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为了让类型转换可能发生于所有实参身上，其实这个函数可以当作friend函数的辅助函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">const</span> Rational&lt;T&gt; <span class="keyword">operator</span>*(<span class="keyword">const</span> Rational&lt;T&gt; &amp;lhs, <span class="keyword">const</span> Rational&lt;T&gt; &amp;rhs)</span><br><span class="line">&#123; ... &#125;</span><br></pre></td></tr></table></figure>

<p> 现在对operator*的混合式调用可以通过编译了，因为当对象oneHalf被声明为一个Rational&lt;int&gt;，class Rationao&lt;int&gt;就被具化出来，friend函数也就被自动声明出来（这里的Rational&lt;T&gt;可以省略）。后者身为一个函数而非函数模板，因而编译器在调用它时使用隐式转换函数。</p>
</li>
</ol>
<h4 id="条款47：请使用traits-classes表现类型信息"><a href="#条款47：请使用traits-classes表现类型信息" class="headerlink" title="条款47：请使用traits classes表现类型信息"></a>条款47：请使用traits classes表现类型信息</h4><ol>
<li><p>STL迭代器有5种分类，每类迭代器之间都是继承关系：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// input迭代器只能向前移动，一次一步，只可读取，只能读一次。如istream_iterator</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">input_iterator_tag</span> &#123;</span>&#125;;</span><br><span class="line"><span class="comment">// output迭代器一切只为输出，它向前移动，一次一步，只能涂写一次。如ostream_iterator</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">output_iterator_tag</span> &#123;</span>&#125;;</span><br><span class="line"><span class="comment">// 这两类只能向前移动，而且只能读或写一些，它们只适合”一次性操作算法“（one-pass algorithms）。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// forward迭代器可以读或写多次，可施行于多次行操作算法（multi-pass algorithms）。</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">forward_iterator_tag</span> :</span> <span class="keyword">public</span> input _iterator_tag &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// bidirectional迭代器除了可以向前移动，还可向后移动（set、multiset、map和multimap</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">bidirectional_iterator_tag</span> :</span> <span class="keyword">public</span> forward_iterator_tag &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// random access迭代器除了可以向前向后，还可随机访问</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">random_access_iterator_tag</span> :</span> <span class="keyword">public</span> bdirectional_iterator_tag &#123;&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>STL中有个工具性template，叫advance，用来将某个迭代器移动若干给定距离：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 所有工作都运行期，这这个typeid-based解法比traits解法效率低</span></span><br><span class="line"><span class="comment">// 48条款</span></span><br><span class="line">templaet&lt;<span class="keyword">typename</span> IterT, <span class="keyword">typename</span> DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">advance</span><span class="params">(IterT &amp;iter, DistT d)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (iter i a random access iterator) &#123;</span><br><span class="line">        iter += d;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (d &gt;= <span class="number">0</span>) &#123; <span class="keyword">while</span> (d--) ++iter; &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123; <span class="keyword">while</span> (d++) --iter; &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 为了处理迭代器分类的相关信息，我们需要traits来在编译期间取得某些类型信息，而traits技术必须对<font oclor="red">内置</font>（<font color="green">built-in</font>）类型和<font color="red">用户自定义</font>（<font color="green">user-defined</font>）类型一样有效运行。这样的template在STL中有若干个，其中针对迭代器的有<code>iterator_traits</code>。<br> 它的运作方式是，它首先要求每个用户自定义的迭代器类型嵌套一个typedef，名为iterator_category，用来确认适当的<font color="red">卷标结构</font>（<font color="green">tag struct</font>）。<br> 例如，针对deque和双向list：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt; ... &gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">deque</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">iterator</span></span></span><br><span class="line"><span class="class">    &#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="keyword">typedef</span> random_access_iterator_tag iterator_category;</span><br><span class="line">        ...</span><br><span class="line">    &#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt; ... &gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">list</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">iterator</span></span></span><br><span class="line"><span class="class">    &#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="keyword">typedef</span> bidirectional_iterator_tag iterator_category;</span><br><span class="line">        ...</span><br><span class="line">    &#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// iterator_traits只是鹦鹉学舌</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT&gt;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">iterator_traits</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">typename</span> IterT::iterator_category iterator_category;</span><br><span class="line">    ..</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 这对用户自定义类型行得通，对指针（也是一种迭代器）行不通。为了支持指针迭代器，iterator_traits针对指针类型提供了一个<font color="red">偏特化版本</font>（<font color="green">partial template specialization</font>）：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// iterator_traits的偏特化版本</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT&gt;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">iterator_traits</span>&lt;IterT*&gt;</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">typedef</span> random_access_iterator_tag iterator_category;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 有了std::iterator_traits后，可以对advance践行之前的伪码了，而为了接受不同类型的iterator_category对象，则需要<font color="red">重载</font>（<font color="green">overloading</font>）：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// random access迭代器</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT, typenmae DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doAdvance</span><span class="params">(IterT &amp;iter, DistT d, <span class="built_in">std</span>::random_access_iterator_tag)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    iter += d;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// bidirectional迭代器</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT, <span class="keyword">typename</span> DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doAdvance</span><span class="params">(IterT &amp;iter, DistT d, <span class="built_in">std</span>::bidirectional_iterator_tag)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (d &gt;= <span class="number">0</span>) &#123; <span class="keyword">while</span> (d--) ++ iter; &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123; <span class="keyword">while</span> (d++) --iter; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// input迭代器</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT, <span class="keyword">typename</span> DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doAdvance</span><span class="params">(IterT &amp;iter, DistT d, <span class="built_in">std</span>::input_iterator_tag)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (d &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">std</span>::out_of_range(<span class="string">"Negative distance"</span>);</span><br><span class="line">    <span class="keyword">while</span> (d--) ++iter;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 有了doAdvance重载版本，advance做的只是调用它们并传递对象</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT, <span class="keyword">typename</span> DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span>  <span class="title">advance</span><span class="params">(IterT &amp;iter, DistT d)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    if (typeid(typename std::iterator_traits&lt;IterT&gt;::iterator_category) == typeid(std::random_access_iterator_tag))</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    doAdvance(iter, d, <span class="keyword">typename</span> <span class="built_in">std</span>::iterator_traits&lt;IterT&gt;::iterator_category());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>STL中除了Iterator_traits供应iterator_category还供应另外四种迭代器：<code>value_type</code>、<code>char_traits</code>、<code>numeric_limits</code>等。STL中还有很多其他新的traits classes用以提供类型信息，如<code>is_fundamental&lt;T&gt;</code>（判断T是否为内置类型），<code>is_array&lt;T&gt;</code>（判断T是否为数组类型）以及<code>is_base_of&lt;T1, T2&gt;</code>（T1和T2相同，或T1是T2的base class）。</p>
</li>
</ol>
<h4 id="条款48：认适template元编程"><a href="#条款48：认适template元编程" class="headerlink" title="条款48：认适template元编程"></a>条款48：认适template元编程</h4><ol>
<li><p><font color="green">Template metaprogramming</font>(<font color="red">TMP</font>，<font color="red">模板元编程</font>)是编写template-based C++程序并执行于编译器的过程。</p>
</li>
<li><p>47条款中的伪码部分：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 面对某些自定义类型，会编译失败</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> IterT, <span class="keyword">typename</span> DistT&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">advance</span><span class="params">(IterT &amp;iter, DistT d)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeid</span>(<span class="keyword">typename</span> <span class="built_in">std</span>::iterator_traits&lt;IterT&gt;::iterator_category) == <span class="keyword">typeid</span>(<span class="built_in">std</span>::random_access_iterator_tag)) &#123;</span><br><span class="line">        iter += d;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (d &gt;= <span class="number">0</span>) &#123; <span class="keyword">while</span> (d--) ++iter; &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123; <span class="keyword">while</span> (d++) --iter; &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>TMP已被证明是”图灵完全“（Turing-complete）机器，针对TMP而涉及的程序库提供了更高级的语法。TMP并没有真正的循环结构，所有的循环效果都是由<font color="red">递归</font>（<font color="green">recursion</font>）完成。TMP主要是个函数时语言（functional language），而递归在这类语言是无法分割的，TMP循环并不涉及递归函数调用，而是涉及”<font color="red">递归模板具现化</font>“（<font color="green">recursive template instantiation</font>）。以下是示例源码：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 一般情况下</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">unsigned</span> n&gt;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Factorial</span> &#123;</span></span><br><span class="line">    <span class="keyword">enum</span> &#123; value = n * Factorial&lt;n - <span class="number">1</span>&gt;::value &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 特殊情况下，当到0时</span></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Factorial</span>&lt;0&gt; &#123;</span></span><br><span class="line">    <span class="keyword">enum</span> &#123; value = <span class="number">1</span> &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>TMP能够达到什么目标呢：</p>
<ol>
<li>确保度量单位正确。在科学和工程应用程序中，可以用TMP确保在编译期时确保所有度量单位组合正确，防范于未然。</li>
<li>优化矩阵运算。</li>
<li>生成用户定制之设计模式（custom design pattern）实现品。这项技术已被用来让若干templates实现出智能指针的行为策略（behavioral policies），用来在编译期间生成数以百计不同的智能指针类型。这项技术超越编程工艺如设计模式和智能指针，更广义成为<font color="green">generative programming</font>（<font color="red">殖生式编程</font>）的一个基础。</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>2019.01</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Muduo技要2</title>
    <url>/2020/06/14/Muduo%E6%8A%80%E8%A6%812/</url>
    <content><![CDATA[<h3 id="性能指标"><a href="#性能指标" class="headerlink" title="性能指标"></a>性能指标</h3><ul>
<li>带宽MB/s，关心消息量，不管消息数</li>
<li>吞吐量QPS，TPS</li>
<li>延迟</li>
<li>资源使用率</li>
</ul>
<h3 id="test"><a href="#test" class="headerlink" title="test"></a>test</h3><ul>
<li><p><code>sysctl -A | grep tcp.*mem</code> # 查看网络缓冲区</p>
</li>
<li><p><code>sysctl -A | grep range</code> # 查看端口范围</p>
</li>
<li><p><code>ntptime</code> # 查看ntp时间</p>
</li>
<li><p><code>ntpq -pn</code> # ntp同步情况，service ntp start启动</p>
</li>
<li><p><code>nc</code> # netcat $ nc ip port | [pipe]</p>
</li>
<li><p><code>tcpdump</code>和<code>Wiretshark</code>来性能分析</p>
</li>
<li><p><code>tcpcopy</code>可以用来进行压力测试</p>
</li>
<li><p><code>strace</code></p>
</li>
<li><p><code>prove</code></p>
</li>
<li><p><code>perf</code> # 查看程序的性能热点 e.g. perf record bin/memcached footprint ptmalloc 1000000 &amp;&amp; perf report</p>
</li>
<li><p><code>netstat -tpna | grep
:prot</code>列出某服务客户端地址，<code>netstat</code>或<code>lsof</code>找出进程发起的连接</p>
</li>
<li><p>nm，查看name mangilng</p>
</li>
</ul>
<h3 id="正确使用TCP"><a href="#正确使用TCP" class="headerlink" title="正确使用TCP"></a>正确使用TCP</h3><ul>
<li><code>SO_REUSERADDR</code></li>
<li>ignore <code>SIGPIPE</code></li>
<li><code>TCP_NODELAY</code></li>
</ul>
<h3 id="read"><a href="#read" class="headerlink" title="read"></a>read</h3><ul>
<li><code>netcat</code></li>
</ul>
<h3 id="消息格式"><a href="#消息格式" class="headerlink" title="消息格式"></a>消息格式</h3><ul>
<li><code>Google Protocol Buffers</code></li>
</ul>
<h3 id="网络神话"><a href="#网络神话" class="headerlink" title="网络神话"></a>网络神话</h3><h4 id="Reactor和Proactor"><a href="#Reactor和Proactor" class="headerlink" title="Reactor和Proactor"></a>Reactor和Proactor</h4><ul>
<li>Reactor模式指“non-blocking IO + IO multiplexing”，Proactor模式是另一个。</li>
<li>Recator的基本结构是事件循环（event loop）和事件驱动（event-driven）和事件回调实现业务逻辑，提高并发度和吞吐量，适合IO密集型横须</li>
<li>CPU密集型程序应该使用线程池，即blocking_queue实现的任务队列<h4 id="线程分配"><a href="#线程分配" class="headerlink" title="线程分配"></a>线程分配</h4></li>
<li>当线程很廉价时，创造多于CPU数目的线程，一线程处理一个TCP连接，通常使用阻塞Blocking IO</li>
<li>当线程很宝贵时，创造跟CPU数目一样多的线程，一个线程处理多个TCP连接上的IO，即”non-blocking IO + IO multiplexing”</li>
<li>进程指fork的产物，线程指pthread_create的宝贵的原生NPTL Pthread线程，每个线程由clone产生，对应内核的task_struct</li>
<li>两种场合必须使用单线程：程序fork和限制程序的CPU占用率。此外，只有单线程能使用fork<h4 id="多线程的性能优势"><a href="#多线程的性能优势" class="headerlink" title="多线程的性能优势"></a>多线程的性能优势</h4></li>
<li>如果很少CPU负载让IO跑满，或很少IO负载就让CPU跑满，那么多线程没有性能优势</li>
<li>多线程适用场景：<ul>
<li>多个CPU可用。</li>
<li>线程间共享数据，或者也可进程间共享</li>
<li>可修改的共享数据，或者也可进程间shared memory</li>
<li>提供非均质服务，如处理优先级事件</li>
<li>lantency和throughput一样重要，即IO和CPU同样重要</li>
<li>异步操作</li>
<li>scale up，能享受到多线程的优势</li>
<li>可预测性能。超过临界点会性能下降，线程数量不随负载变化</li>
<li>多线程划分责任和功能</li>
</ul>
</li>
</ul>
<h3 id="C-编译链接模型精要"><a href="#C-编译链接模型精要" class="headerlink" title="C++编译链接模型精要"></a>C++编译链接模型精要</h3><h4 id="一些记录"><a href="#一些记录" class="headerlink" title="一些记录"></a>一些记录</h4><ul>
<li><p>C++三大约束：C兼容、零开销（与Rust的零成本抽象区别？）原则和值语义</p>
</li>
<li><p>笼统地表示编译过程可分为：preprocessor/compiler/assembler/linker四个步骤，C++没有import或using（C++20可能出现），include头文件将导致当需要使用一个函数时预处理阶段必不可少的导入了一堆不相关的函数</p>
</li>
<li><p>一些编译选项：-Wall、-Wextra、-Werror、-Wconversion、-Wshadow</p>
</li>
<li><p>使用前向生命可以减少include，CCS规定不能重载&amp;&amp;、||、，三个操作符，Google规定不能重载一元<code>operator&amp;()</code>，这样class就不能前向生命了</p>
</li>
<li><p>判断一个C++可执行文件是release还是debug模式可以通过看class template的短函数有没有被inline展开，在nm命令中查看可以发现，开启-O2编译的程序是看不到inline函数的任何信息的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span>;</span>  <span class="comment">// 前向生命</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(Foo &amp;foo)</span> </span>&#123;</span><br><span class="line">    Foo *p = &amp;foo;  <span class="comment">// 取foo地址，重载了意思就变了</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>C++的链接模型相比C多了一些内容，name mangling和vague linkage，即一个符号多份互不冲突。C中不允许重复定义</p>
<h4 id="一些名词"><a href="#一些名词" class="headerlink" title="一些名词"></a>一些名词</h4></li>
<li><p>一次定义原则（ODR），C是的，C++不是的</p>
</li>
</ul>
<h4 id="C-设计"><a href="#C-设计" class="headerlink" title="C++设计"></a>C++设计</h4><ul>
<li>POD和非POD</li>
<li>三/五原则：POD类型需要拷贝赋值非平凡类型时，就需要析构函数，而需要析构函数的类也需要拷贝和赋值操作（需要拷贝操作的类也需要赋值操作），否则使用编译器合成的将导致未定义行为或多重析构的危险</li>
</ul>
<h4 id><a href="#" class="headerlink" title></a></h4><ul>
<li>程序迁移工具：Porting Advisor，从X86（CISC）到arm kunpeng（RISC）</li>
<li>数据库测试工具：BenchmarkSQL安装ant -&gt; 创建配置文件 -&gt; 配置数据库连接 -&gt; 场景配置 -&gt; 数据准备 -&gt; 执行测试<ul>
<li>数据库连接：db、driver、conn、user/password</li>
<li>场景配置：warehouse仓库数量、loadWorkers数据并发数、Terminals并发用户数、runMins测试时间、runTxnsPerTerminal、limitTxns。。。</li>
</ul>
</li>
<li>大数据测试工具套件：HiBench：评估框架速度、吞吐量和系统资源利用率<ul>
<li>测试类别：micro、ml、sql、websearch、graph、streaming</li>
</ul>
</li>
</ul>
<h3 id="C-并发相关错误类型"><a href="#C-并发相关错误类型" class="headerlink" title="C++并发相关错误类型"></a>C++并发相关错误类型</h3><h4 id="不必要的阻塞"><a href="#不必要的阻塞" class="headerlink" title="不必要的阻塞"></a>不必要的阻塞</h4><ul>
<li>死锁：指第一个线程等待第二个线程执行后才能继续，而第二个线程又在等待第一个线程，构成一个线程等待循环状态。</li>
<li>活锁：当第一个线程等待第二个线程，第二个线程又在等待第一个线程时，活锁类似死锁。活锁死锁区别在于等待过程不是阻塞状态而是不断循环检测状态，如spinlock。</li>
<li>在I/O或外部输入上的阻塞：当线程阻塞是因为等待某外部输入而无法继续执行，可能外部输入永远都不到来<h4 id="竞争条件"><a href="#竞争条件" class="headerlink" title="竞争条件"></a>竞争条件</h4></li>
<li>数据竞争：指没有同步好对某个共享内存的并发访问</li>
<li>破坏不变量：表现为悬挂指针（线程删除了被访问的数据）、随机存储损坏（线程为局部更新造成读取数据不一致）或者双闲状态（重复释放？）</li>
<li>生存期问题：线程超时访问的某些数据已经被删除、销毁或访问的内存已经被另一个对象重用<h4 id="需要思考的问题"><a href="#需要思考的问题" class="headerlink" title="需要思考的问题"></a>需要思考的问题</h4></li>
<li>哪些数据需要保护，防止并行访问？</li>
<li>如何保证数据是被保护的？</li>
<li>此时其他线程执行到代码的何处？</li>
<li>该线程用了哪些信号量？其他线程用了哪些信号量？</li>
<li>该线程各操作间有先后顺序要求吗？</li>
<li>线程载入的数据是否有效，是否被其他线程修改？<h4 id="假如测试一个队列需要考虑的不同应用场景"><a href="#假如测试一个队列需要考虑的不同应用场景" class="headerlink" title="假如测试一个队列需要考虑的不同应用场景"></a>假如测试一个队列需要考虑的不同应用场景</h4></li>
<li>一个线程在自身队列调用push()或pop()验证</li>
<li>空队列上一个线程调用push()另一个线程pop()</li>
<li>空队列和满队列上分别多个线程push()</li>
<li>空队列和慢队列上分别多线程pop()</li>
<li>空队列和满队列上分别多线程push()和一个线程pop()</li>
<li>空队列和满队列分别多线程push()和pop()</li>
</ul>
]]></content>
      <categories>
        <category>2020.04</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Linux</tag>
        <tag>Muduo</tag>
      </tags>
  </entry>
  <entry>
    <title>Muduo技要</title>
    <url>/2020/06/14/Muduo%E6%8A%80%E8%A6%81/</url>
    <content><![CDATA[<h3 id="性能指标"><a href="#性能指标" class="headerlink" title="性能指标"></a>性能指标</h3><ul>
<li>带宽MB/s，关心消息量，不管消息数</li>
<li>吞吐量QPS，TPS</li>
<li>延迟</li>
<li>资源使用率</li>
</ul>
<h3 id="test"><a href="#test" class="headerlink" title="test"></a>test</h3><ul>
<li><p><code>sysctl -A | grep tcp.*mem</code> # 查看网络缓冲区</p>
</li>
<li><p><code>sysctl -A | grep range</code> # 查看端口范围</p>
</li>
<li><p><code>ntptime</code> # 查看ntp时间</p>
</li>
<li><p><code>ntpq -pn</code> # ntp同步情况，service ntp start启动</p>
</li>
<li><p><code>nc</code> # netcat $ nc ip port | [pipe]</p>
</li>
<li><p><code>tcpdump</code>和<code>Wiretshark</code>来性能分析</p>
</li>
<li><p><code>tcpcopy</code>可以用来进行压力测试</p>
</li>
<li><p><code>strace</code></p>
</li>
<li><p><code>prove</code></p>
</li>
<li><p><code>perf</code> # 查看程序的性能热点 e.g. perf record bin/memcached footprint ptmalloc 1000000 &amp;&amp; perf report</p>
</li>
<li><p><code>netstat -tpna | grep
:prot</code>列出某服务客户端地址，<code>netstat</code>或<code>lsof</code>找出进程发起的连接</p>
</li>
<li><p>nm，查看name mangilng</p>
</li>
</ul>
<h3 id="正确使用TCP"><a href="#正确使用TCP" class="headerlink" title="正确使用TCP"></a>正确使用TCP</h3><ul>
<li><code>SO_REUSERADDR</code></li>
<li>ignore <code>SIGPIPE</code></li>
<li><code>TCP_NODELAY</code></li>
</ul>
<h3 id="read"><a href="#read" class="headerlink" title="read"></a>read</h3><ul>
<li><code>netcat</code></li>
</ul>
<h3 id="消息格式"><a href="#消息格式" class="headerlink" title="消息格式"></a>消息格式</h3><ul>
<li><code>Google Protocol Buffers</code></li>
</ul>
<h3 id="网络神话"><a href="#网络神话" class="headerlink" title="网络神话"></a>网络神话</h3><h4 id="Reactor和Proactor"><a href="#Reactor和Proactor" class="headerlink" title="Reactor和Proactor"></a>Reactor和Proactor</h4><ul>
<li>Reactor模式指“non-blocking IO + IO multiplexing”，Proactor模式是另一个。</li>
<li>Recator的基本结构是事件循环（event loop）和事件驱动（event-driven）和事件回调实现业务逻辑，提高并发度和吞吐量，适合IO密集型横须</li>
<li>CPU密集型程序应该使用线程池，即blocking_queue实现的任务队列<h4 id="线程分配"><a href="#线程分配" class="headerlink" title="线程分配"></a>线程分配</h4></li>
<li>当线程很廉价时，创造多于CPU数目的线程，一线程处理一个TCP连接，通常使用阻塞Blocking IO</li>
<li>当线程很宝贵时，创造跟CPU数目一样多的线程，一个线程处理多个TCP连接上的IO，即”non-blocking IO + IO multiplexing”</li>
<li>进程指fork的产物，线程指pthread_create的宝贵的原生NPTL Pthread线程，每个线程由clone产生，对应内核的task_struct</li>
<li>两种场合必须使用单线程：程序fork和限制程序的CPU占用率。此外，只有单线程能使用fork<h4 id="多线程的性能优势"><a href="#多线程的性能优势" class="headerlink" title="多线程的性能优势"></a>多线程的性能优势</h4></li>
<li>如果很少CPU负载让IO跑满，或很少IO负载就让CPU跑满，那么多线程没有性能优势</li>
<li>多线程适用场景：<ul>
<li>多个CPU可用。</li>
<li>线程间共享数据，或者也可进程间共享</li>
<li>可修改的共享数据，或者也可进程间shared memory</li>
<li>提供非均质服务，如处理优先级事件</li>
<li>lantency和throughput一样重要，即IO和CPU同样重要</li>
<li>异步操作</li>
<li>scale up，能享受到多线程的优势</li>
<li>可预测性能。超过临界点会性能下降，线程数量不随负载变化</li>
<li>多线程划分责任和功能</li>
</ul>
</li>
</ul>
<h3 id="C-编译链接模型精要"><a href="#C-编译链接模型精要" class="headerlink" title="C++编译链接模型精要"></a>C++编译链接模型精要</h3><h4 id="一些记录"><a href="#一些记录" class="headerlink" title="一些记录"></a>一些记录</h4><ul>
<li><p>C++三大约束：C兼容、零开销（与Rust的零成本抽象区别？）原则和值语义</p>
</li>
<li><p>笼统地表示编译过程可分为：preprocessor/compiler/assembler/linker四个步骤，C++没有import或using（C++20可能出现），include头文件将导致当需要使用一个函数时预处理阶段必不可少的导入了一堆不相关的函数</p>
</li>
<li><p>一些编译选项：-Wall、-Wextra、-Werror、-Wconversion、-Wshadow</p>
</li>
<li><p>使用前向生命可以减少include，CCS规定不能重载&amp;&amp;、||、，三个操作符，Google规定不能重载一元<code>operator&amp;()</code>，这样class就不能前向生命了</p>
</li>
<li><p>判断一个C++可执行文件是release还是debug模式可以通过看class template的短函数有没有被inline展开，在nm命令中查看可以发现，开启-O2编译的程序是看不到inline函数的任何信息的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span>;</span>  <span class="comment">// 前向生命</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(Foo &amp;foo)</span> </span>&#123;</span><br><span class="line">    Foo *p = &amp;foo;  <span class="comment">// 取foo地址，重载了意思就变了</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>C++的链接模型相比C多了一些内容，name mangling和vague linkage，即一个符号多份互不冲突。C中不允许重复定义</p>
<h4 id="一些名词"><a href="#一些名词" class="headerlink" title="一些名词"></a>一些名词</h4></li>
<li><p>一次定义原则（ODR），C是的，C++不是的</p>
</li>
</ul>
<h4 id="C-设计"><a href="#C-设计" class="headerlink" title="C++设计"></a>C++设计</h4><ul>
<li>POD和非POD</li>
<li>三/五原则：POD类型需要拷贝赋值非平凡类型时，就需要析构函数，而需要析构函数的类也需要拷贝和赋值操作（需要拷贝操作的类也需要赋值操作），否则使用编译器合成的将导致未定义行为或多重析构的危险</li>
</ul>
<h4 id><a href="#" class="headerlink" title></a></h4><ul>
<li>程序迁移工具：Porting Advisor，从X86（CISC）到arm kunpeng（RISC）</li>
<li>数据库测试工具：BenchmarkSQL安装ant -&gt; 创建配置文件 -&gt; 配置数据库连接 -&gt; 场景配置 -&gt; 数据准备 -&gt; 执行测试<ul>
<li>数据库连接：db、driver、conn、user/password</li>
<li>场景配置：warehouse仓库数量、loadWorkers数据并发数、Terminals并发用户数、runMins测试时间、runTxnsPerTerminal、limitTxns。。。</li>
</ul>
</li>
<li>大数据测试工具套件：HiBench：评估框架速度、吞吐量和系统资源利用率<ul>
<li>测试类别：micro、ml、sql、websearch、graph、streaming</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>2020.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Linux</tag>
        <tag>Muduo</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker基本使用</title>
    <url>/2020/06/12/Docker%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h2 id="修改镜像源"><a href="#修改镜像源" class="headerlink" title="修改镜像源"></a>修改镜像源</h2><blockquote>
<p>（manjaro）只需要添加配置文件/etc/docker/daemon.json</p>
</blockquote>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"registry-mirrors"</span>: [<span class="string">"https://docker.mirrors.ustc.edu.cn"</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="或者使用–registry-mirror选项"><a href="#或者使用–registry-mirror选项" class="headerlink" title="或者使用–registry-mirror选项"></a>或者使用–registry-mirror选项</h3><ul>
<li>修改:/etc/default/docker</li>
<li>添加:DOCKER_OPTS=”–registry-mirror=<a href="https://docker.mirrors.ustc.edu.cn&quot;" target="_blank" rel="noopener">https://docker.mirrors.ustc.edu.cn&quot;</a></li>
<li><code>systemctl daemon-reload</code></li>
<li><code>systemctl restart docker</code></li>
</ul>
<hr>
<h2 id="一些Docker基础命令"><a href="#一些Docker基础命令" class="headerlink" title="一些Docker基础命令"></a>一些Docker基础命令</h2><h2 id="容器操作"><a href="#容器操作" class="headerlink" title="容器操作"></a><em>容器操作</em></h2><h3 id="run-通过运行镜像输出hello"><a href="#run-通过运行镜像输出hello" class="headerlink" title="run: 通过运行镜像输出hello"></a><font color="red">run</font>: 通过运行镜像输出hello</h3><blockquote>
<p><code>docker run CONTAINER [COMMAND] [ARGS]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 相当于先create再start</span></span><br><span class="line"><span class="comment"># e.g. docker run CONTAINER echo 'hello'</span></span><br></pre></td></tr></table></figure>

<h3 id="启动交互式容器"><a href="#启动交互式容器" class="headerlink" title="启动交互式容器"></a>启动交互式容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># e.g.:指定ip:host进行后台运行</span></span><br><span class="line">$ docker run -it -d -p 127.0.0.1:12306:12306 --name postgres ichheitimg:9.1-deb /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="带名字的运行镜像"><a href="#带名字的运行镜像" class="headerlink" title="带名字的运行镜像"></a>带名字的运行镜像</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --name=<span class="string">'name'</span> CONTAINER <span class="built_in">echo</span> <span class="string">'hello'</span></span><br></pre></td></tr></table></figure>

<h3 id="守护形式运行容器"><a href="#守护形式运行容器" class="headerlink" title="守护形式运行容器"></a>守护形式运行容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># C^P C^Q退出，可docker ps查看守护容器</span></span><br></pre></td></tr></table></figure>

<h4 id="可直接启动后台"><a href="#可直接启动后台" class="headerlink" title="可直接启动后台"></a>可直接启动后台</h4><p><a href="https://docs.docker.com/reference/commandline/cli" target="_blank" rel="noopener">配置选项</a></p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 运行相关选项</span></span><br><span class="line"> <span class="comment"># -D, --debug=false</span></span><br><span class="line"> <span class="comment"># -e, --exec-driver="native"</span></span><br><span class="line"> <span class="comment"># -g, --graph="/var/lib/docker"</span></span><br><span class="line"> <span class="comment"># --icc=true</span></span><br><span class="line"> <span class="comment"># -l, --log-level="info"</span></span><br><span class="line"> <span class="comment"># --label=[]</span></span><br><span class="line"> <span class="comment"># -p, --pidfile="/var/run/docker.pid"</span></span><br><span class="line"> <span class="comment"># </span></span><br><span class="line">$ docker run -d CONTAINER [COMMAND] [ARG...]</span><br><span class="line"><span class="comment"># 服务器相关选项</span></span><br><span class="line"> <span class="comment"># -G, --group="docker"</span></span><br><span class="line"> <span class="comment"># -H, --host=[]</span></span><br><span class="line"> <span class="comment"># --tls=false</span></span><br><span class="line"> <span class="comment"># --tlscacert="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlscert="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlskey="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlsverify=false</span></span><br><span class="line"><span class="comment"># Remote API存储相关</span></span><br><span class="line"> <span class="comment"># -s, --storage-driver=""</span></span><br><span class="line"> <span class="comment"># --selinux-enabled=false</span></span><br><span class="line"> <span class="comment"># --storage-opt=[]</span></span><br><span class="line"><span class="comment"># Registry相关</span></span><br><span class="line"> <span class="comment"># --insecure-registry=[]</span></span><br><span class="line"> <span class="comment"># --registry-mirror=p[]</span></span><br><span class="line"><span class="comment"># 网络设置相关</span></span><br><span class="line"> <span class="comment"># -b, --bridge=""</span></span><br><span class="line"> <span class="comment"># --bip=""</span></span><br><span class="line"> <span class="comment"># --fixed-cidr=""</span></span><br><span class="line"> <span class="comment"># --fixed-cidr-v6=""</span></span><br><span class="line"> <span class="comment"># --dns=[]</span></span><br><span class="line"> <span class="comment"># --dns-search=[]</span></span><br><span class="line"> <span class="comment"># --ip=0</span></span><br><span class="line"> <span class="comment"># --ip-forward=true</span></span><br><span class="line"> <span class="comment"># --ip-masq=true</span></span><br><span class="line"> <span class="comment"># --iptables=true</span></span><br><span class="line"> <span class="comment"># --ipv6=false</span></span><br><span class="line"> <span class="comment"># --mtu=0</span></span><br></pre></td></tr></table></figure>

<h3 id="设置容器的端口映射"><a href="#设置容器的端口映射" class="headerlink" title="设置容器的端口映射"></a>设置容器的端口映射</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -p, --publish</span></span><br><span class="line"><span class="comment"># containerPort</span></span><br><span class="line">$ docker run -p 80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># hostPort:containerPort</span></span><br><span class="line">$ docker run -p 8080:80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># ip::containerPort</span></span><br><span class="line">$ docker run -p 0.0.0.0:80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># ip:hostPort:containerPort</span></span><br><span class="line">$ docker run -p 0.0.0.0:8080:80 -it CONTAINER /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="create-创建容器"><a href="#create-创建容器" class="headerlink" title="create: 创建容器"></a><font color="red">create</font>: 创建容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 新建新容器，如果没有指定镜像，会自动pull镜像</span></span><br><span class="line">$ docker create -it ubuntu:latest</span><br></pre></td></tr></table></figure>

<h3 id="start-启动容器"><a href="#start-启动容器" class="headerlink" title="start: 启动容器"></a><font color="red">start</font>: 启动容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 启动创建好的容器</span></span><br><span class="line">$ docker start XXXXXXX</span><br></pre></td></tr></table></figure>

<h3 id="ps-查看容器"><a href="#ps-查看容器" class="headerlink" title="ps: 查看容器"></a><font color="red">ps</font>: 查看容器</h3><blockquote>
<p><code>docker ps [OPT]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a 全部记录</span></span><br><span class="line"><span class="comment"># -l 最新记录</span></span><br></pre></td></tr></table></figure>

<h3 id="rm-删除容器"><a href="#rm-删除容器" class="headerlink" title="rm: 删除容器"></a><font color="red">rm</font>: 删除容器</h3><blockquote>
<p><code>docker rm &#39;{IMAGE}&#39;</code></p>
</blockquote>
<h3 id="attach-继续运行后台守护容器"><a href="#attach-继续运行后台守护容器" class="headerlink" title="attach: 继续运行后台守护容器"></a><font color="red">attach</font>: 继续运行后台守护容器</h3><blockquote>
<p><code>docker attach &#39;{IMAGE}&#39;</code><br><code>dokcer attach [--detach-keys[=[]]] [--no-stdin][--sig-proxy[=true]] 
CONTAINER</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">```</span><br><span class="line"></span><br><span class="line"><span class="comment">### &lt;font color="red"&gt;logs&lt;/font&gt;: 查看容器日志</span></span><br><span class="line">&gt; `docker logs [OPT] <span class="string">'&#123;IMAGE&#125;'</span>`</span><br><span class="line">```zsh</span><br><span class="line"> <span class="comment"># -f --follows=true|false 默认false</span></span><br><span class="line"> <span class="comment"># -t --timestamps=true|false 默认false</span></span><br><span class="line"> <span class="comment"># --tail="all"</span></span><br></pre></td></tr></table></figure>

<h3 id="exec-在已运行的容器中启动新进程"><a href="#exec-在已运行的容器中启动新进程" class="headerlink" title="exec: 在已运行的容器中启动新进程"></a><font color="red">exec</font>: 在已运行的容器中启动新进程</h3><blockquote>
<p><code>docker exec [OPT] CONTAINER [COMMAND] {ARGS}</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -d</span></span><br><span class="line"><span class="comment"># -i </span></span><br><span class="line"><span class="comment"># -t</span></span><br></pre></td></tr></table></figure>

<h3 id="stop-kill-停止容器"><a href="#stop-kill-停止容器" class="headerlink" title="stop/kill: 停止容器"></a><font color="red">stop/kill</font>: 停止容器</h3><blockquote>
<p><code>docker stop [-t | --time[=10]] CONTAINER</code><br><code>docker kill CONTAINER</code></p>
</blockquote>
<hr>
<h2 id="镜像操作"><a href="#镜像操作" class="headerlink" title="镜像操作"></a><em>镜像操作</em></h2><h3 id="images-inspect-查看镜像"><a href="#images-inspect-查看镜像" class="headerlink" title="images/inspect: 查看镜像"></a><font color="red">images/inspect</font>: 查看镜像</h3><blockquote>
<p><code>docker iamges [OPT] [REPOSITORY]</code><br><code>docker inspect [OPT] CONTAINER|IMAGE [...]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a, all=false</span></span><br><span class="line"><span class="comment"># -f, --filter=[]</span></span><br><span class="line"><span class="comment"># --no-trunc=false</span></span><br><span class="line"><span class="comment"># -q, --quiet=false</span></span><br></pre></td></tr></table></figure>

<h3 id="search-查找镜像"><a href="#search-查找镜像" class="headerlink" title="search: 查找镜像"></a><a href="https://registry.hub.docker.com" target="_blank" rel="noopener"><font color="red">search</font>: 查找镜像</a></h3><blockquote>
<p><code>docker search [OPT] TERM</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --automated=false</span></span><br><span class="line"><span class="comment"># --no-trunc=false</span></span><br><span class="line"><span class="comment"># -s, --stars=0</span></span><br><span class="line"><span class="comment"># --filter=stars=3</span></span><br></pre></td></tr></table></figure>

<h3 id="pull-拉取镜像"><a href="#pull-拉取镜像" class="headerlink" title="pull: 拉取镜像"></a><font color="red">pull</font>: 拉取镜像</h3><blockquote>
<p><code>docker pull [OPT] NAME [:TAG]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a, --all-ags=false</span></span><br></pre></td></tr></table></figure>

<h3 id="构建镜像"><a href="#构建镜像" class="headerlink" title="构建镜像"></a>构建镜像</h3><h4 id="commit-通过容器"><a href="#commit-通过容器" class="headerlink" title="commit: 通过容器"></a><font color="red">commit</font>: 通过容器</h4><blockquote>
<p><code>docker commit [OPT] CONTAINER [REPOSITORY[:TAG]]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -a,--author=""</span></span><br><span class="line"> <span class="comment"># -m,--message=""</span></span><br><span class="line"> <span class="comment"># -p,--pause=true, 提交时暂停容器运行</span></span><br><span class="line"><span class="comment"># e.g.:提交本地deb容器</span></span><br><span class="line">$ docker commit deb ichheitimg:9.1-deb</span><br></pre></td></tr></table></figure>

<h4 id="import-通过本地模板导入"><a href="#import-通过本地模板导入" class="headerlink" title="import: 通过本地模板导入"></a><font color="red">import</font>: 通过本地模板导入</h4><blockquote>
<p><code>docker import [OPT] file |URL|[REPOSITORY[:TAG]]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.:导入</span></span><br><span class="line">$ cat ubuntu-14.04-x86_64-minimal.tar.gz | docker import - ubuntu:14.04</span><br></pre></td></tr></table></figure>

<h4 id="build-通过Dockerfile文件"><a href="#build-通过Dockerfile文件" class="headerlink" title="build: 通过Dockerfile文件"></a><font color="red">build</font>: 通过Dockerfile文件</h4><blockquote>
<p><code>docker build</code></p>
</blockquote>
<h4 id="通过Dockerfile"><a href="#通过Dockerfile" class="headerlink" title="通过Dockerfile"></a>通过Dockerfile</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --force-rm=false</span></span><br><span class="line"> <span class="comment"># --no-cache=false</span></span><br><span class="line"> <span class="comment"># --pull=false</span></span><br><span class="line"> <span class="comment"># -q, --quiet=false</span></span><br><span class="line"> <span class="comment"># --rm=true</span></span><br><span class="line"> <span class="comment"># -t, --tag=""</span></span><br><span class="line">$ docker build [OPT] PATH | URL | -</span><br><span class="line"><span class="comment"># e.g. 在创建的目录中编写Dockerfile文件，然后使用$ docker build -t='name' .</span></span><br></pre></td></tr></table></figure>

<h5 id="Dockerfile格式："><a href="#Dockerfile格式：" class="headerlink" title="Dockerfile格式："></a>Dockerfile格式：</h5><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># First Dockerfile</span></span><br><span class="line">   FROM [CONTAINER]</span><br><span class="line">   MAINTAINER [MESSAGE]</span><br><span class="line">   [COMMAND...]</span><br><span class="line">   EXPOSE 80</span><br><span class="line">   CMD [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>] (<span class="built_in">exec</span> mode)</span><br><span class="line">   CMD <span class="built_in">command</span> param1 param2 (shell mode)</span><br><span class="line">   CMD [<span class="string">"param1"</span>, <span class="string">"param2"</span>] (ENTRYPOINT指令搭配下的参数)</span><br><span class="line">   ENTRYPOINT [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>] (<span class="built_in">exec</span> mode)</span><br><span class="line">   ENTRYPOINT <span class="built_in">command</span> param1 param2 (shell mode)</span><br><span class="line">   <span class="comment"># CMD与ENTRYPOINT的区别是后者命令不会被构建时命令所覆盖，也可以使用--entrypoint覆盖</span></span><br><span class="line">   ADD &lt;src&gt; .. &lt;dest&gt;</span><br><span class="line">   COPY &lt;src&gt; ... &lt;dest&gt;</span><br><span class="line">   VOLUMN [<span class="string">"/data"</span>]</span><br><span class="line">   (共享数据等作用，使用时在build构建前应使用--volumes-from选项挂在数据卷容器)</span><br><span class="line">   WORKDIR /path/to/workdir (指定工作目录)</span><br><span class="line">   ENV &lt;key&gt; &lt;value&gt;</span><br><span class="line">   ENV &lt;key&gt;=&lt;value&gt; ...</span><br><span class="line">   USER user (e.g. | USER user | USER user:group | USER user:gid |)</span><br><span class="line">   ONBUILD [INSTRUCTION] (添加触发器)</span><br></pre></td></tr></table></figure>

<h5 id="Dockerfile构建过程"><a href="#Dockerfile构建过程" class="headerlink" title="Dockerfile构建过程"></a>Dockerfile构建过程</h5><ul>
<li>从基础镜像运行容器</li>
<li>执行指令，对容器做修改</li>
<li>执行类docker commit操作，提交新镜像层</li>
<li>基于新镜像运行新容器</li>
<li>执行Dockerfile的下一指令直到所有指令执行完毕</li>
</ul>
<h3 id="save-存入镜像"><a href="#save-存入镜像" class="headerlink" title="save: 存入镜像"></a><font color="red">save</font>: 存入镜像</h3><blockquote>
<p><code>docker save</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 导出本地的ubuntu:14.04为文件ubuntu_14.014.tar</span></span><br><span class="line">$ docker save -o ubuntu_14.04.tar ubuntu:14:04</span><br></pre></td></tr></table></figure>

<h3 id="load-载入镜像"><a href="#load-载入镜像" class="headerlink" title="load: 载入镜像"></a><font color="red">load</font>: 载入镜像</h3><blockquote>
<p><code>docker load</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 导出tar文件到本地镜像库</span></span><br><span class="line">$ docker load --input ubuntu_14.04.tar</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">$ docker load &lt; ubuntu_14.04.tar</span><br></pre></td></tr></table></figure>

<h3 id="push-上传镜像"><a href="#push-上传镜像" class="headerlink" title="push: 上传镜像"></a><font color="red">push</font>: 上传镜像</h3><blockquote>
<p><code>docker push NAME[: TAG] |REGISTRY_HOST[: REGISTRY_PORT]/NAME[: TAG]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 用户user上传test:latest镜像到user/test:latest</span></span><br><span class="line">$ docker tag <span class="built_in">test</span>:latest user/<span class="built_in">test</span>:latest</span><br><span class="line">$ docker push user/<span class="built_in">test</span>:latest</span><br></pre></td></tr></table></figure>

<h3 id="rmi-删除镜像"><a href="#rmi-删除镜像" class="headerlink" title="rmi: 删除镜像"></a><font color="red">rmi</font>: 删除镜像</h3><blockquote>
<p><code>docker rmi [OPT] IMAGE [...]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -f, --force=false</span></span><br><span class="line"><span class="comment"># --no-prune=false</span></span><br></pre></td></tr></table></figure>

<h3 id="Docker的C-S模式：Remote-API"><a href="#Docker的C-S模式：Remote-API" class="headerlink" title="Docker的C/S模式：Remote API"></a>Docker的C/S模式：<a href="https://docs.docker.com/reference/api/docker_remote_api/" target="_blank" rel="noopener">Remote API</a></h3><h4 id="连接方式"><a href="#连接方式" class="headerlink" title="连接方式"></a>连接方式</h4><ul>
<li><code>unix:///var/run/docker.dock</code></li>
<li><code>tcp://host:port</code></li>
<li><code>fd://socketfd</code><h4 id="Docker的远程访问"><a href="#Docker的远程访问" class="headerlink" title="Docker的远程访问"></a>Docker的远程访问</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 默认配置-H unix:///var/run/docker.sock</span></span><br><span class="line"><span class="comment"># e.g.: docker -H tcp://ip:port COMMAND</span></span><br><span class="line"><span class="comment"># 可选择的环境变量DOCKER_HOST</span></span><br><span class="line"><span class="comment"># e.g.: export DOCKER_HOST="tcp://ip:port"</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="Docker容器互联"><a href="#Docker容器互联" class="headerlink" title="Docker容器互联"></a>Docker容器互联</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --icc=true，允许容器间互联</span></span><br><span class="line"><span class="comment"># 每次镜像重启都可能导致IP的变更，因此使用以下方式</span></span><br><span class="line"> <span class="comment"># --link</span></span><br><span class="line">$ docker run --link=[CONTAINER]:[ALIAS] [IMAGE][COMMAND]</span><br><span class="line"><span class="comment"># e.g. docker run -it --name test --link=other:alias img:test</span></span><br><span class="line"><span class="comment"># 只允许特定容器间连接</span></span><br><span class="line">$ --icc=<span class="literal">false</span> --iptables=<span class="literal">true</span></span><br><span class="line">$ --link</span><br></pre></td></tr></table></figure>

<h3 id="Docker容器的数据卷"><a href="#Docker容器的数据卷" class="headerlink" title="Docker容器的数据卷"></a>Docker容器的数据卷</h3><h4 id="为容器添加数据卷"><a href="#为容器添加数据卷" class="headerlink" title="为容器添加数据卷"></a>为容器添加数据卷</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -v local_catalogue:docker_catalogue:[ro]</span></span><br><span class="line"><span class="comment"># e.g.</span></span><br><span class="line">$ sudo dcoker run -v ~/container_data:/data -it ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<h4 id="在Dockerfile中使用COLUMN下指定数据卷容器"><a href="#在Dockerfile中使用COLUMN下指定数据卷容器" class="headerlink" title="在Dockerfile中使用COLUMN下指定数据卷容器"></a>在Dockerfile中使用COLUMN下指定数据卷容器</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.:</span></span><br><span class="line"><span class="comment"># 编写Dockefile，指定VOLUME目录</span></span><br><span class="line">$ docker build -t dorman/dvt .</span><br><span class="line">$ docker run -it --name <span class="built_in">test</span> dorman/dvt</span><br><span class="line"><span class="comment"># 以下--volumes-from将共享Dockfile中的目录</span></span><br><span class="line">$ docker run -it --name test1 --volumes-from <span class="built_in">test</span> ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="Docker数据卷的备份和还原"><a href="#Docker数据卷的备份和还原" class="headerlink" title="Docker数据卷的备份和还原"></a>Docker数据卷的备份和还原</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from [container name] -v local_catalogue:docker_catalogue ubuntu</span><br><span class="line">$ tar cvf /backup/backup.tar [container data volume]</span><br></pre></td></tr></table></figure>

<h3 id="Docker容器的跨主机连接"><a href="#Docker容器的跨主机连接" class="headerlink" title="Docker容器的跨主机连接"></a>Docker容器的跨主机连接</h3><h4 id="网桥实现"><a href="#网桥实现" class="headerlink" title="网桥实现"></a>网桥实现</h4><ul>
<li>通过在主机和虚拟机之间使用同一个网段来跨主机连接</li>
<li>需要网络配置并且配置Docker网桥，并通过–fixed-cidr指定网络范围</li>
<li>(/etc/network/interfaces)</li>
</ul>
<h4 id="Open-vSwitch实现"><a href="#Open-vSwitch实现" class="headerlink" title="Open vSwitch实现"></a>Open vSwitch实现</h4><h5 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h5><ul>
<li>双网卡，Host-Only &amp; NAT</li>
<li>安装Open vSwitch和网桥管理工具bridge-utils</li>
<li>同一网段的IP地址<h5 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h5></li>
<li>建立ovs网桥</li>
<li>添加gre连接</li>
<li>配置docker容器虚拟网桥</li>
<li>为虚拟网桥添加ovs接口</li>
<li>添加不同DOcker容器网段路由</li>
</ul>
<h4 id="weave实现"><a href="#weave实现" class="headerlink" title="weave实现"></a>weave实现</h4>]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt信号和槽</title>
    <url>/2019/07/07/Qt%E4%BF%A1%E5%8F%B7%E5%92%8C%E6%A7%BD/</url>
    <content><![CDATA[<p>信号和槽是Qt的一个核心特点，也是区别于其他框架的重要特性。因为有了信号和槽的编程机制，Qt中处理界面各个组件的交互操作时变得更加直观和简单。信号与槽是<font color="blue">对象间通信</font>的机制，由Qt的元对象系统的支持才能实现。<br><font color="red">信号(Signal)</font>会在某种特定情况或动作下被触发。就是特殊情况下被发射的事件，而GUI程序设计的主要内容就是对界面上各组件信号的响应。<br><font color="red">槽(Slot)</font>则等同于接收并处理信号的函数。它跟一般的C++函数是一样的，可以直接被调用。但槽函数与普通函数不同的是，槽函数与一个信号关联，当信号被发射时，关联的槽函数会自动执行。<br>对于这种对象间通信，Qt隐藏了复杂的底层实现。Qt的信号与槽机制类似于“事件——响应”，但更灵活。  </p>
<hr>
<h2 id="1-connect-函数的不同参数形式"><a href="#1-connect-函数的不同参数形式" class="headerlink" title="1.connect()函数的不同参数形式"></a>1.connect()函数的不同参数形式</h2><ul>
<li><code>QObject::connect()</code>函数多重函数形式，一种参数原型是：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">/×</span><br><span class="line">sender是发送信号的对象名称</span><br><span class="line">signal()是信号名称，根据SIGNAL宏展开成相应字符串</span><br><span class="line">receiver是接受信号的对象名称</span><br><span class="line">slot()是槽函数名称，根据SLOT宏展开成字符串</span><br><span class="line">×/</span><br><span class="line">QMetaObject::Connection QObject::connect(<span class="keyword">const</span> QObject *sender, <span class="keyword">const</span> <span class="keyword">char</span> *signal, <span class="keyword">const</span> QObject *receiver, <span class="keyword">const</span> <span class="keyword">char</span> *method, Qt::ConnectionType type = Qt::AutoConnection)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>一般语法如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// connect()是QObject类中的一个静态函数，QObject又是所有Qt类的基类，实际调用可以忽略前面的限定符</span></span><br><span class="line"><span class="comment">// 宏SIGNAL()指信号，SLOT()指槽函数</span></span><br><span class="line">QObject::connection(sender, SIGNAL(signal()), receiver, SLOT(slot()));</span><br><span class="line"><span class="comment">// 如果带有参数，标明参数类型</span></span><br><span class="line">QObject::connection(spinNum, SIGNAL(valueChanged(<span class="keyword">int</span>)), <span class="keyword">this</span>, SLOT(updateStatus(<span class="keyword">int</span>));</span><br></pre></td></tr></table></figure>

<hr>
<ul>
<li>另一种参数形式的connect()函数原型是：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QMetaObject::Connection QObject::connect(<span class="keyword">const</span> QObject *sender, <span class="keyword">const</span> QMetaMethod &amp;signal, <span class="keyword">const</span> QObject *receiver, <span class="keyword">const</span> QMetaMethod, &amp;method, Qt::ConnectionType type = Qt::Auto Connection)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>这种具有默认参数，信号名称是唯一的，可以使用这种函数指针形式进行关联，语法如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QLineEdit只有一个textChanged(QString)信号，通过在窗体类widget里定义一个槽函数可以将它们关联，语法如下：</span></span><br><span class="line">QObject::connection(lineEdit, &amp;QLineEdit::textChanged, <span class="keyword">this</span>, &amp;widget::on_textChanged);</span><br></pre></td></tr></table></figure>

<p>但如果说类中定义有不同参数的同名信号就不能用函数指针的方式来关联信号和槽了：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QSpinBox有两个valueChanged()信号</span></span><br><span class="line"><span class="keyword">void</span> QSpinBox::valueChanged(<span class="keyword">int</span> i);</span><br><span class="line"><span class="keyword">void</span> QSpinBox::valueChanged(<span class="keyword">const</span> QString &amp;text);</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在窗体中定义一个槽函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">onValueChanged</span><span class="params">(<span class="keyword">int</span> i)</span></span>;</span><br></pre></td></tr></table></figure>

<p>Ctrl+B编译时将会出错：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QObject::connect(spinNum, &amp;QSpinBox::valueChanged, <span class="keyword">this</span>, &amp;widget::onValueChanged); <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<hr>
<p><code>connect()</code>函数最后都有一个<code>Qt::ConnectionType
type</code>，缺省参数是<code>Qt::AutoConnection</code>。<code>Qt::ConnectionType</code>是枚举类型，表示信号与槽的关联方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 缺省值，在信号触发时自动确定关联方式。如果发射者和接受者在同一线程，使用Qt::DirectConnection；否则使用Qt::QueuedConnection</span></span><br><span class="line">Qt::AutoConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号被发射时槽函数立刻执行</span></span><br><span class="line">Qt::DirectConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件循环回到接受者线程后执行槽函数</span></span><br><span class="line">Qt::QueuedConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 与Qt::QueuedConnection相似，但信号线程会阻塞到槽函数执行完毕。</span></span><br><span class="line"><span class="comment">// 注意在同一线程时不用这种方式，会死锁</span></span><br><span class="line">Qt::BlockingQueuedConnection type;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-使用sender-获得信号发送者"><a href="#2-使用sender-获得信号发送者" class="headerlink" title="2.使用sender()获得信号发送者"></a>2.使用sender()获得信号发送者</h2><p>在槽函数里，使用QObject::sender()可以获取信号发送者的指针，知道发射者的类型就可以将它类型转换成确定的类型。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QSpinBox的valueChanged(int)信号里可以通过sender()和qobject_cast去的发射者指针然后对发射者进行操作：</span></span><br><span class="line">QSpinBox *spinBox = qobject_cast&lt;QSpinBox *&gt;(sender());</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过sender返回并打印Button的text值</span></span><br><span class="line">QObject::connect(button1, &amp;QPushButton::clicked, [<span class="keyword">this</span>]&#123;</span><br><span class="line">    QPushButton *button = qobject_cast&lt;QPushButton *&gt;(sender());</span><br><span class="line">    qDebug() &lt;&lt; button-&gt;text();</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-自定义信号及其使用"><a href="#3-自定义信号及其使用" class="headerlink" title="3.自定义信号及其使用"></a>3.自定义信号及其使用</h2><p>通过在自己设计的类中自定义信号，这个信号就是声明的函数，但这个函数不用实现，只需要发射（emit）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在类中定义一个 ageChanged(int)</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">QPerson</span> :</span> <span class="keyword">public</span> QObject</span><br><span class="line">&#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> m_age = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">incAge</span><span class="params">()</span></span>;</span><br><span class="line">signals:</span><br><span class="line">    <span class="comment">// void返回类型，可以多参数，但函数无需实现</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">ageChanged</span><span class="params">(<span class="keyword">int</span> value)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在incAge()函数中发射信号,当m_age改变时，就emit信号。至于有无信号关联的槽函数，发射者不管</span></span><br><span class="line"><span class="keyword">void</span> QPerson::incAge()</span><br><span class="line">&#123;</span><br><span class="line">    m_age++;</span><br><span class="line">    <span class="function">emit <span class="title">ageChanged</span><span class="params">(m_age)</span></span>; <span class="comment">// emit signal</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-关于信号和槽的使用上的规则"><a href="#4-关于信号和槽的使用上的规则" class="headerlink" title="4.关于信号和槽的使用上的规则"></a>4.关于信号和槽的使用上的规则</h2><ol>
<li>一个信号可以连接多个槽</li>
<li>多个信号可以连接同一个槽</li>
<li>一个信号可以连接另一个信号</li>
<li>信号与槽的个数和类型需要一致，至少信号的参数不能少于槽的参数</li>
<li>使用信号和槽的类中，需要在类定义中加入宏Q_OBJECT</li>
<li>当信号被发射时，关联的槽函数会被立即执行，直到执行完毕才会执行余下的程序流程。</li>
</ol>
]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt元对象系统</title>
    <url>/2019/07/06/Qt%E5%85%83%E5%AF%B9%E8%B1%A1%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h3 id="元对象系统"><a href="#元对象系统" class="headerlink" title="元对象系统"></a>元对象系统</h3><p>Qt的<font color="green">元对象编译器</font>(<font color="red">Meta-Object Compiler, MOC</font>)是一个预处理器，它用于将Qt特性的程序转换为标准C++兼容的形式，再由标准C++编译器进行编译。例如，在使用信号与槽机制的类里，通过添加一个<code>Q_OBJECT</code>宏，MOC才对类中的信号与槽的代码进行预处理。<br>Qt的<font color="green">元对象系统</font>(<font color="red">Meta-Object System</font>)提供了对象之间通信的<strong>信号与槽机制</strong>、<strong>运行时类型信息</strong>和<strong>动态属性系统</strong>。<br>除了信号与槽机制，元对象还提供如下一些功能：</p>
<ul>
<li><p><code>QObject::metaObject()</code>：返回类关联的元对象，元对象类<code>QMetaObject</code>包含了访问元对象的接口函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g. 运行时返回类的名称字符串</span></span><br><span class="line">QObject *obj = <span class="keyword">new</span> QPushButton;</span><br><span class="line">obj-&gt;metaObject()-&gt;classNmae;   <span class="comment">// 返回"QPushButton"</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>QMetaObject::newInstance()</code>：创建类的一个新的实例</p>
</li>
<li><p><code>QObject::inherits(const char *className)</code>：判断对象是否是名为className的类，或者QObject的子类的实例</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g.</span></span><br><span class="line">QTimer *timer = <span class="keyword">new</span> QTimer;</span><br><span class="line">timer-&gt;inherits(<span class="string">"Qtimer"</span>);          <span class="comment">// return true</span></span><br><span class="line">timer-&gt;inherits(<span class="string">"QObject"</span>);         <span class="comment">// return true</span></span><br><span class="line">timer-&gt;inherits(<span class="string">"QABstraactButton"</span>);<span class="comment">// return false</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>qobject_cast()</code>：dynamic cast功能</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g. 假设QMyWidget是QWidget的子类并且类中声明了Q_OBJECT宏</span></span><br><span class="line">QObject *obj = <span class="keyword">new</span> QMyWidget;</span><br><span class="line">QWidget *widget = qobject_cast&lt;QWidget *&gt;(obj);         <span class="comment">// 向上转型</span></span><br><span class="line">QMyWidget *myWidget = qobject_cast&lt;QMyWidget *&gt;(obj);   <span class="comment">// 向下转型</span></span><br><span class="line">QLabel *label = qobject_cast&lt;QLabel *&gt;(obj);            <span class="comment">// error</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>QObject::tr()</code>和<code>QObject::trUtf8()</code>用于翻译字符串，用于多语言界面设计</p>
</li>
<li><p><code>QObject::setProperty()</code>和<code>QObject::property()</code>函数用于通过属性名称动态设置和获取属性值</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 基本使用</title>
    <url>/2020/06/14/Git-%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">// 安装git版本控制工具</span><br><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get install git    // apt get</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get install git-core   // outdate</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> sudo yum install git        // yum</span></span><br><span class="line"></span><br><span class="line">// 指定用户和Email地址</span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --blobal user.name <span class="string">"Your Name"</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email <span class="string">"email@example.com"</span></span></span><br><span class="line"></span><br><span class="line">// Git显示颜色</span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global color.ui <span class="literal">true</span></span></span><br><span class="line"></span><br><span class="line">// 配置命令别名</span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global <span class="built_in">alias</span>.&lt;order_name&gt; &lt;previous_name&gt;</span></span><br><span class="line">// $ git config --global alias.st status</span><br><span class="line">// $ git config --global alias.last 'log -l'</span><br><span class="line">// git config --global alias.lg "log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)&lt;%an&gt;%Creset' --abbrev-commit"</span><br><span class="line"></span><br><span class="line">// 查看配置信息</span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --list</span></span><br><span class="line"></span><br><span class="line">// 初始化管理仓库</span><br><span class="line"><span class="meta">$</span><span class="bash"> git init /Users/michael/learngit/.git/</span></span><br><span class="line"></span><br><span class="line">// 将文件添加到暂缓区</span><br><span class="line"><span class="meta">$</span><span class="bash"> git add &lt;file&gt;</span></span><br><span class="line"></span><br><span class="line">// 将文件从暂缓区提交到git仓库</span><br><span class="line"><span class="meta">$</span><span class="bash"> git commit -m &lt;message&gt;</span></span><br><span class="line"></span><br><span class="line">// 查看当前git状态</span><br><span class="line"><span class="meta">$</span><span class="bash"> git status</span></span><br><span class="line">// e.g. $ git status -s</span><br><span class="line"></span><br><span class="line">// 查看文件是否有新改动</span><br><span class="line"><span class="meta">$</span><span class="bash"> git diff &lt;file&gt;</span></span><br><span class="line">// e.g. $ git diff --staged</span><br><span class="line">// e.g. $ git diff --cached</span><br><span class="line"></span><br><span class="line">// 查看修改日志</span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span></span></span><br><span class="line">// e.g. git log --pretty=oneline --abbrev-commit</span><br><span class="line">// e.g. git log -p -2 // -p查看修改差异</span><br><span class="line">// e.g. 还可以定制输出格式</span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span> --pretty=format:<span class="string">"%h - %an, %ar : %s"</span></span></span><br><span class="line">/*</span><br><span class="line"><span class="meta">%</span><span class="bash">H:     提交对象（commit）的完整哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">h:     提交对象的简短哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">T:     树对象（tree）的完整哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">t:     树对象的简短哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">P:     父对象（parent）的完整哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">p:     父对象的简短哈希字串</span></span><br><span class="line"><span class="meta">%</span><span class="bash">an:    作者（author）的名字</span></span><br><span class="line"><span class="meta">%</span><span class="bash">ae:    作者的电子邮件地址</span></span><br><span class="line"><span class="meta">%</span><span class="bash">ad:    作者修订日期（可以用 --date= 选项定制格式）</span></span><br><span class="line"><span class="meta">%</span><span class="bash">ar:    作者修订日期，按多久以前的方式显示</span></span><br><span class="line"><span class="meta">%</span><span class="bash">cn:    提交者（committer）的名字</span></span><br><span class="line"><span class="meta">%</span><span class="bash">ce:    提交者的电子邮件地址</span></span><br><span class="line"><span class="meta">%</span><span class="bash"><span class="built_in">cd</span>:    提交日期</span></span><br><span class="line"><span class="meta">%</span><span class="bash">cr:    提交日期，按多久以前的方式显示</span></span><br><span class="line"><span class="meta">%</span><span class="bash">s:     提交说明</span></span><br><span class="line">*/</span><br><span class="line"></span><br><span class="line">// 返回到上一个版本</span><br><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard HEAD^</span></span><br><span class="line">// e.g. HEAD^^表示上上个版本,HEAD~100返回上100个版本</span><br><span class="line"></span><br><span class="line">// 通过指定SHA-1编码恢复版本</span><br><span class="line"><span class="meta">$</span><span class="bash"> git reset --hard &lt;code&gt;</span></span><br><span class="line">// e.g. git reset --hard 1094a</span><br><span class="line"></span><br><span class="line">// 查看每一次命令的记录</span><br><span class="line"><span class="meta">$</span><span class="bash"> git reflog</span></span><br><span class="line"></span><br><span class="line">// 查看工作区和版本库里最新版本的区别</span><br><span class="line"><span class="meta">$</span><span class="bash"> git diff HEAD -- &lt;file&gt;</span></span><br><span class="line">// e.g. git diff HEAD -- what.cpp</span><br><span class="line"></span><br><span class="line">// 将暂存区的修改撤销掉,放回工作区</span><br><span class="line"><span class="meta">$</span><span class="bash"> git reset HEAD &lt;file&gt;</span></span><br><span class="line"></span><br><span class="line">// 丢弃工作区的修改</span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout -- &lt;file&gt;</span></span><br><span class="line"></span><br><span class="line">// Git中对文件改名</span><br><span class="line"><span class="meta">$</span><span class="bash"> git mv &lt;pre_name&gt; &lt;cur_name&gt;</span></span><br><span class="line">/* 相当于</span><br><span class="line"><span class="meta">$</span><span class="bash"> mv a.left a.right</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git rm a.left</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git add a.right</span></span><br><span class="line">*/</span><br><span class="line"></span><br><span class="line">// 删除文件</span><br><span class="line"><span class="meta">$</span><span class="bash"> git rm &lt;file&gt;</span></span><br><span class="line">// e.g. 放到暂存区的要用 git rm -f &lt;file&gt;</span><br><span class="line"></span><br><span class="line">// 克隆文件</span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> git@github.com:&lt;user&gt;/&lt;Repository_name&gt;.git</span></span><br><span class="line"></span><br><span class="line">// 创建分支,然后切换到该分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout -b &lt;branch_name&gt;</span></span><br><span class="line">// 相当于以下两条指令</span><br><span class="line"><span class="meta">$</span><span class="bash"> git branch &lt;branch_name&gt;  // 创建分支</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout &lt;branch_name&gt;// 选择该分支</span></span><br><span class="line"></span><br><span class="line">// 查看分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git branch</span></span><br><span class="line">// e.g. git branch -v // 查看每个分支最后一次提交</span><br><span class="line"></span><br><span class="line">// 删除分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git branch -d &lt;branch_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 将分支的结果合并到master分支上</span><br><span class="line"><span class="meta">$</span><span class="bash"> git merge &lt;branch_name&gt;</span></span><br><span class="line">// e.g. 合并分支请注意--no-ff参数,表示禁用Fast forward;以普通模式合并,合并后的历史有分支,能看出来曾经做过合并,而fast forward合并就看不出来了</span><br><span class="line">// e.g. $ git merge --no-ff -m "merge with no-ff" dev</span><br><span class="line"></span><br><span class="line">// 查看分支的合并情况</span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span> --graph --pretty=oneline --abbrev-commit</span></span><br><span class="line"></span><br><span class="line">// 如果用户主目录没有.ssh目录、id\_rsa和id\_rsa.pub就创建SSH Key:</span><br><span class="line"><span class="meta">$</span><span class="bash"> ssh-keygen -t rsa -C <span class="string">"youremail@example.com"</span></span></span><br><span class="line"></span><br><span class="line">// 远程分支它以(remote)/(branch)形式命名</span><br><span class="line"></span><br><span class="line">// 关联origin远程库</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote add &lt;remote&gt; git@github.com:&lt;user&gt;/&lt;Repository_name&gt;.git</span></span><br><span class="line"> github仓库</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote add &lt;name&gt; &lt;user&gt;@&lt;Ip_address&gt;:/warehouse/&lt;Repository_name&gt;.git    // 个人服务器示例(SSH)</span></span><br><span class="line">// e.g. origin也可以改成别的,不是固定</span><br><span class="line">// e.g. $ git remote add origin git@github.com:example/test.git</span><br><span class="line"></span><br><span class="line">// 远程仓库重命名</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote rename &lt;pre_remote&gt; &lt;cur_remote&gt;</span></span><br><span class="line"></span><br><span class="line">// 远程仓库的移除</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote rm &lt;remote&gt;</span></span><br><span class="line"></span><br><span class="line">// 删除远程分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git push origin --delete &lt;remote-branch&gt;</span></span><br><span class="line"></span><br><span class="line">// 查看远程库的信息</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote -v</span></span><br><span class="line">// e.g. master dev bug feature</span><br><span class="line"></span><br><span class="line">// 查看远程仓库信息</span><br><span class="line"><span class="meta">$</span><span class="bash"> git remote show &lt;remote&gt;</span></span><br><span class="line"></span><br><span class="line">// 获得远程引用的完整列表</span><br><span class="line"><span class="meta">$</span><span class="bash"> git ls-remote &lt;remote&gt;</span></span><br><span class="line"></span><br><span class="line">// 将本地库的内容推送到远程库上</span><br><span class="line"><span class="meta">$</span><span class="bash"> git push -u origin master</span></span><br><span class="line">// e.g. master是指其中一个branch.可以通过serverfix:awesomebranch方式来将本地的serverfix分支推送到远程仓库上的awesomebranch分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git push origin master:serverfix</span></span><br><span class="line"></span><br><span class="line">// 从远程仓库抓取分支(分离状态的)</span><br><span class="line"><span class="meta">$</span><span class="bash"> git fetch &lt;remote&gt; &lt;branch&gt;</span></span><br><span class="line">// e.g. 本地不会生成可编辑的副本.它不会有serverfix分支,只有一个不可修改的origin/serverfix指针</span><br><span class="line"></span><br><span class="line">// 从远程仓库获取并建立&lt;branch&gt;分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git fetch &lt;remote&gt; &lt;remote-branch&gt;:&lt;<span class="built_in">local</span>-branch&gt;</span></span><br><span class="line">// e.g. git fetch origin master:test</span><br><span class="line">// e.g. git diff tmp // 对比分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout -b serverfix origin/serverfix</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout --track origin/serverfix</span></span><br><span class="line"></span><br><span class="line">// 从远程库获取最新版本并merge到本地</span><br><span class="line"><span class="meta">$</span><span class="bash"> git pull &lt;remote&gt; &lt;branch&gt;</span></span><br><span class="line">// e.g. 需要指定本地分支与远程origin/分支的连接.以下以dev分支为例</span><br><span class="line">// $ git branch --set-upstream-to=origin/dev dev</span><br><span class="line"></span><br><span class="line">// 比较本地master分支和origin/master分支差别</span><br><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">log</span> -p master ..origin/master</span></span><br><span class="line"></span><br><span class="line">// 将stash储藏起来,等以后恢复stash(dirty)</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash</span></span><br><span class="line"></span><br><span class="line">// 查看stash列表</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash list</span></span><br><span class="line"></span><br><span class="line">// 恢复stash</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash apply </span></span><br><span class="line"></span><br><span class="line">// 恢复指定stash</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash apply stash@&#123;0&#125;</span></span><br><span class="line"></span><br><span class="line">// 删除stash</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash drop</span></span><br><span class="line"></span><br><span class="line">// 恢复stash的同时删除stash内容</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash pop</span></span><br><span class="line"></span><br><span class="line">// 尝试重新应用暂存的修改</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash apply --index</span></span><br><span class="line"></span><br><span class="line">// 不要储藏任何已经通过git add暂存的东西</span><br><span class="line"><span class="meta">$</span><span class="bash"> git status -s</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git status --keep-index</span></span><br><span class="line"></span><br><span class="line">// 储藏任何创建的未跟踪的文件</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash --include-untracked </span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash -u</span></span><br><span class="line"></span><br><span class="line">// 交互式提示哪些改动想要储存,哪些改动想要保存工作目录中</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash --patch</span></span><br><span class="line"></span><br><span class="line">// 从储藏创建一个分支</span><br><span class="line"><span class="meta">$</span><span class="bash"> git stash branch &lt;branch&gt;</span></span><br><span class="line"></span><br><span class="line">// 变基</span><br><span class="line"><span class="meta">$</span><span class="bash"> git rebase</span></span><br><span class="line">// 跳过server分支,将client分支与master分支合并</span><br><span class="line"><span class="meta">$</span><span class="bash"> git rabase -onto master server client</span></span><br><span class="line">// e.g. 以上命令意思是:"取出client分支,找出处于client分支和server分支的共同祖先之后的修改,然后把他们放在master分支上重放一遍."</span><br><span class="line"><span class="meta">$</span><span class="bash"> git rebase master server</span></span><br><span class="line">// 以上命令可以直接将特性分支变基到目标分支上,即是说,把server分支续到master分支之后,省去得先切到server分支</span><br><span class="line"></span><br><span class="line">// 打标签</span><br><span class="line"><span class="meta">$</span><span class="bash"> git tag &lt;tag_name&gt; [commit_id]</span></span><br><span class="line">// e.g.  git tag v1.0</span><br><span class="line">// e.g. 再加一个参数是对应的commit id打上标签</span><br><span class="line">// e.g. git tag v1.0 f52c633</span><br><span class="line">// -m 指定说明文字 -a指定标签名</span><br><span class="line"></span><br><span class="line">// 查看所有标签</span><br><span class="line"><span class="meta">$</span><span class="bash"> git tag</span></span><br><span class="line"></span><br><span class="line">// 查看标签信息</span><br><span class="line"><span class="meta">$</span><span class="bash"> git show &lt;tag_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 查看标签所指文件版本</span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout &lt;tag_name&gt;</span></span><br><span class="line">// e.g. 这会使你的仓库处于分离头指针(detached HEAD)状态.也就是说任何修改标签都不会变化,不属于任何分支,也无法访问.</span><br><span class="line">// e.g. 因此要么在初始阶段创建新分支关联,要么在切走分支时,和提交校验码git branch关联.否则将会丢失它</span><br><span class="line"><span class="meta">$</span><span class="bash"> git checkout -b version2 v2.0.0</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git branch version2 XXXXXXX</span></span><br><span class="line"></span><br><span class="line">// 删除标签</span><br><span class="line"><span class="meta">$</span><span class="bash"> git tag -d &lt;tag_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 推送某个标签到远程</span><br><span class="line"><span class="meta">$</span><span class="bash"> git push origin &lt;tag_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 一次性推送全部尚未推送到远程的本地标签</span><br><span class="line"><span class="meta">$</span><span class="bash"> git push origin --tags</span></span><br><span class="line"></span><br><span class="line">// 删除远程标签</span><br><span class="line"><span class="meta">$</span><span class="bash"> git tag -d &lt;tag_name&gt;</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git push origin :refs/tags/&lt;tag_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 添加被.gitignore忽略的文件:</span><br><span class="line"><span class="meta">$</span><span class="bash"> git add -f &lt;file_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 检查.gitignore文件规则</span><br><span class="line"><span class="meta">$</span><span class="bash"> git check-ignore -v &lt;file_name&gt;</span></span><br><span class="line"></span><br><span class="line">// 删除工作目录</span><br><span class="line"><span class="meta">$</span><span class="bash"> git clean </span></span><br><span class="line">-n // 提示将要删除什么文件</span><br><span class="line">-d // 移除文件</span><br><span class="line">-x // 不忽略.gitignore文件规则删除所有</span><br><span class="line">-i // 交互式提醒</span><br></pre></td></tr></table></figure>

<h6 id="创建一个运行git的服务器"><a href="#创建一个运行git的服务器" class="headerlink" title="创建一个运行git的服务器"></a>创建一个运行git的服务器</h6><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">// 在远程服务器安装git</span><br><span class="line"><span class="meta">$</span><span class="bash"> apt-get -y install git</span></span><br><span class="line"></span><br><span class="line">// 创建git用户并输入密码,用来运行git服务</span><br><span class="line"><span class="meta">$</span><span class="bash"> adduser git &amp;&amp; passwd git</span></span><br><span class="line"></span><br><span class="line">// 导入公钥到authorized_keys文件里</span><br><span class="line"></span><br><span class="line">// 选定一个目录作为Git仓库,在目录下创建裸(bare)仓库</span><br><span class="line"><span class="meta">$</span><span class="bash"> git init --bare example.git</span></span><br><span class="line"></span><br><span class="line">// 把.git文件的owner改为git:</span><br><span class="line"><span class="meta">$</span><span class="bash"> chown -R git:git example.git</span></span><br><span class="line"></span><br><span class="line">// 禁用git用户登录shell</span><br><span class="line"><span class="meta">$</span><span class="bash"> vim /etc/passwd</span></span><br><span class="line">// e.g. 将git条目后的/bin/bash改成/usr/bin/git-shell,这样git用户可以通过ssh使用git但是无法登录shell</span><br></pre></td></tr></table></figure>

<h6 id="忽略特殊文件"><a href="#忽略特殊文件" class="headerlink" title="忽略特殊文件"></a>忽略特殊文件</h6><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过在Git工作区根目录下创建特殊文件.gitignore文件,可以把要忽略的文件名填进去,Git会自动忽略这些文件</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">忽略文件原则是:系统自动生成文件</span></span><br><span class="line"><span class="comment">               编译生成的中间文件</span></span><br><span class="line"><span class="comment">               可执行文件</span></span><br><span class="line"><span class="comment">               带有敏感信息的配置文件</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/github/gitignore" target="_blank" rel="noopener">gitignore文件</a></p>
<h6 id="变基"><a href="#变基" class="headerlink" title="变基"></a>变基</h6><p><a href="https://git-scm.com/book/zh/v2/Git-分支-变基" target="_blank" rel="noopener">rebase</a></p>
<blockquote>
<p>rebase和merge的区别就在于:</p>
</blockquote>
<blockquote>
<p>merge会把两个分支的最新快照分别于它们的共同祖先比对,将拼接出合并的结果,然后生成一个新的快照并提交.</p>
</blockquote>
<blockquote>
<p>rebase先找到两个分支的最新快照的共同祖先对比其中某一分支,找出经修改的地方存储为临时文件,并将它指向另一分支(基底),并依偎基底将修改应用一次.完成合并.这就是”重放”??</p>
</blockquote>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL安装.md</title>
    <url>/2020/06/12/PostgreSQL%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<p><a href="https://www.postgresql.org/" target="_blank" rel="noopener">PostgreSQL官网</a></p>
<h3 id="安装依赖："><a href="#安装依赖：" class="headerlink" title="安装依赖："></a>安装依赖：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># centos</span></span><br><span class="line">$ yum -y install readline-devel zlib-devel gcc gcc-c++ zlib readline</span><br><span class="line"><span class="comment"># debian/ubuntu</span></span><br><span class="line">$ apt-get install -y gcc make</span><br><span class="line">$ apt-get install -y libreadline-dev zlib1g zlib1g.dev</span><br></pre></td></tr></table></figure>

<h3 id="安装PostgreSQL："><a href="#安装PostgreSQL：" class="headerlink" title="安装PostgreSQL："></a>安装PostgreSQL：</h3><p><a href="https://sandbox-experiment-resource-north-4.obs.cn-north-4.myhuaweicloud.com/postgresql-arm/postgresql-10.3.tar.gz" target="_blank" rel="noopener">postgresql-10.3(maybe too<br>old)</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ wget https://sandbox-experiment-resource-north-4.obs.cn-north-4.myhuaweicloud.com/postgresql-arm/postgresql-10.3.tar.gz</span><br><span class="line">$ tar -zxvf postgresql-10.3.tar.gz -C /opt/</span><br><span class="line">$ <span class="built_in">cd</span> /opt/postgresql-*/</span><br><span class="line">$ ./configure --build=arm-linux --host=arm-linux --prefix=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line"><span class="comment"># canuse：./configure --prefix=/usr/local/pgsql --with-blocksize=8 \</span></span><br><span class="line"><span class="comment"># --enable-dtrace --enable-debug --enable-thread-safety</span></span><br><span class="line"><span class="comment"># --with-libxml --with-python --with-openssl</span></span><br><span class="line"><span class="comment"># $ make world</span></span><br><span class="line"><span class="comment"># $ make check-world (by normal user)</span></span><br><span class="line"><span class="comment"># $ make install-world</span></span><br><span class="line">$ make &amp;&amp; make install &amp;&amp; make clean</span><br></pre></td></tr></table></figure>

<h3 id="授权用户："><a href="#授权用户：" class="headerlink" title="授权用户："></a>授权用户：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ adduser postgres</span><br><span class="line">$ chown -R postgres:postgres /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ ls -ld /usr/<span class="built_in">local</span>/pgsql</span><br></pre></td></tr></table></figure>

<h3 id="配置环境变量："><a href="#配置环境变量：" class="headerlink" title="配置环境变量："></a>配置环境变量：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ su postgres</span><br><span class="line">$ vim ~/.bash_profile</span><br></pre></td></tr></table></figure>

<blockquote>
<p>配置文件中加入：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">export</span> PGHOME=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ <span class="built_in">export</span> PGDATA=/usr/<span class="built_in">local</span>/pgsql/data</span><br><span class="line">$ <span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:<span class="variable">$PGHOME</span>/bin</span><br></pre></td></tr></table></figure>

<blockquote>
<p>执行修改：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span>  ~/.bash_profile &amp;&amp; psql -V</span><br></pre></td></tr></table></figure>

<h3 id="初始化数据库："><a href="#初始化数据库：" class="headerlink" title="初始化数据库："></a>初始化数据库：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ initdb -D <span class="variable">$PGDATA</span> -E UTF8 --locale=C -U postgres -W</span><br></pre></td></tr></table></figure>

<h3 id="启动日志服务："><a href="#启动日志服务：" class="headerlink" title="启动日志服务："></a>启动日志服务：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mkdir -p /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br><span class="line">$ chmod 755 /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br></pre></td></tr></table></figure>

<h3 id="启动数据库："><a href="#启动数据库：" class="headerlink" title="启动数据库："></a>启动数据库：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span>  -l  <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br></pre></td></tr></table></figure>

<h3 id="检查结果："><a href="#检查结果：" class="headerlink" title="检查结果："></a>检查结果：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ps -ef | grep <span class="string">"postgres"</span></span><br><span class="line">$ netstat -nlp | grep <span class="string">"postgres"</span></span><br></pre></td></tr></table></figure>

<h3 id="修改数据库密码："><a href="#修改数据库密码：" class="headerlink" title="修改数据库密码："></a>修改数据库密码：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ psql中`\password postgres`可修改</span><br></pre></td></tr></table></figure>

<h3 id="配置为远程访问："><a href="#配置为远程访问：" class="headerlink" title="配置为远程访问："></a>配置为远程访问：</h3><blockquote>
<p>将”listen_addresses=’localhost’”取消注释并修改为任意地址’*’</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim /usr/<span class="built_in">local</span>/pgsql/data/postgresql.conf</span><br></pre></td></tr></table></figure>

<blockquote>
<p>修改访问地址控制设置：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim /usr/<span class="built_in">local</span>/pgsql/data/pg_hba.conf</span><br></pre></td></tr></table></figure>

<blockquote>
<p>在”IPv4 local connections”下构建规则：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">host    all             all             0.0.0.0/0               md5</span><br></pre></td></tr></table></figure>

<h3 id="重启生效："><a href="#重启生效：" class="headerlink" title="重启生效："></a>重启生效：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl restart</span><br></pre></td></tr></table></figure>

<h2 id="在debian中"><a href="#在debian中" class="headerlink" title="在debian中"></a>在debian中</h2><h3 id="基本安装"><a href="#基本安装" class="headerlink" title="基本安装"></a>基本安装</h3><blockquote>
<p>创建文件<code>/etc/apt/sources.list.d/pgdg.list</code>，里面添加：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 其中stretch-pgdg具体可以在https://www.postgresql.org/download/linux/debian/中选择具体版本得到</span></span><br><span class="line">deb http://apt.postgresql.org/pub/repos/apt/ stretch-pgdg main</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Import the repository signing key, and update the package lists:</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ apt-get install -y gnupg gnupg1 gnupg2</span><br><span class="line">$ wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -</span><br><span class="line">$ apt-get update</span><br><span class="line">$ apt-get install -y postgresql-10</span><br><span class="line"></span><br><span class="line">$ useradd -m -s /bin/bash postgres</span><br><span class="line">$ mkdir /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ chown postgres:postgres /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ sudo -u postgres /usr/lib/postgressql</span><br></pre></td></tr></table></figure>

<blockquote>
<p>数据库初始化：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ /usr/lib/postgresql/11/bin/initdb -D <span class="variable">$PGDATA</span> -E UTF8 --locale=C -U postgres -W</span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>vim ~/.bash_profile</code>配置：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PGHOME=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line"><span class="built_in">export</span> PGDATA=/usr/<span class="built_in">local</span>/pgsql/data</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/usr/lib/postgresql/11/bin</span><br><span class="line"><span class="built_in">export</span> LANG=en_US.utf8</span><br></pre></td></tr></table></figure>

<blockquote>
<p>数据库日志：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 大可不必</span></span><br><span class="line">$ mkdir -p /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br><span class="line">$ chmod 755 /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br></pre></td></tr></table></figure>

<ul>
<li>can start data server <figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 一般启动</span></span><br><span class="line">$ /usr/lib/postgresql/11/bin/pg_ctl -D /usr/<span class="built_in">local</span>/pgsql/data -l logfile start</span><br><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span> -l <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="通过配置文件"><a href="#通过配置文件" class="headerlink" title="通过配置文件"></a>通过配置文件</h4><blockquote>
<p>启动，vim /etc/sysctl.conf中配置内核相关参数：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">kernel.shmmni = 4096</span><br><span class="line">kernel.sem = 50100 64128000 50100 1280</span><br><span class="line">fs.file-max = 7672460</span><br><span class="line">net.ipv4.ip_local_port_range = 9000 65000</span><br><span class="line">net.core.rmem_default = 1048576</span><br><span class="line">net.core.rmem_max = 4194304</span><br><span class="line">net.core.wmem_default = 262144</span><br><span class="line">net.core.wmen_max = 1028576</span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>vim /etc/sysconfig/iptables</code>进行防火墙设置：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-A RH-Firewall-1-INPUT -i lo -j ACCEPT</span><br><span class="line"># 允许源IP</span><br><span class="line">-A RH-Firewall-1-INPUT -s 192.168.0.0/16 -j ACCEPT</span><br><span class="line"># 允许源IP访问目标端口</span><br><span class="line">-A RH-Firewall-1-INPUT -s 192.168.1.0/24 -m state --state </span><br><span class="line">NEW -m tcp -p tcp --dport [PORT] -j ACCEPT</span><br><span class="line"># 允许任意IP访问目标端口</span><br><span class="line">-A RH-Firewall-1-INPUT -p tcp -m state --state NEW -m</span><br><span class="line">tcp --dport [PORT] -j ACCEPT</span><br><span class="line"># 不是很懂</span><br><span class="line"># 使用iptables -L -v -n查看出入规则</span><br></pre></td></tr></table></figure>

<ul>
<li>在/etc/security/limits.conf配置。。。<blockquote>
<p><code>vim /usr/local/pgsql/data/pg_hba.conf</code>中的IPv4 local<br>connections修改访问地址控制规则设置：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">host    all             all             0.0.0.0/0               md5</span><br></pre></td></tr></table></figure>

</li>
</ul>
<blockquote>
<p><code>vim /usr/local/pgsql/data/postgresql.conf</code>中</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">将listen_addresses=<span class="string">'localhost'</span>取消注释并修改为任意地址<span class="string">'*0.0.0.0'</span></span><br><span class="line">superuser_reserved_connections超级用户最多连接改为13</span><br><span class="line">unix_socket_directories连接改为<span class="string">'.'</span>根目录下</span><br><span class="line">unix_socket_permissions权限为0700</span><br><span class="line"><span class="comment"># 心跳检测</span></span><br><span class="line">tcp_keepalives_idle = 60</span><br><span class="line">tcp_keepalives_interval = 10</span><br><span class="line">tcp_keepalives_count = 10</span><br><span class="line"><span class="comment"># 延迟</span></span><br><span class="line">vacuum_cost_delay = 10</span><br><span class="line">bgwritter_delay = 10ms</span><br><span class="line">wal_writer_delay = 200ms</span><br><span class="line"><span class="comment"># 归档</span></span><br><span class="line">hot_standby = on</span><br><span class="line"><span class="comment"># 日志</span></span><br><span class="line">log_destination = <span class="string">"csvlog"</span></span><br><span class="line">logging_collector = on</span><br><span class="line">log_directory = <span class="string">'pg_log'</span></span><br><span class="line">log_file_mode = 0600</span><br><span class="line">log_truncate_on_rotation = on等</span><br><span class="line"><span class="comment"># 还有checkpoint</span></span><br><span class="line">log_checkpoints = on</span><br><span class="line">log_connection = on</span><br><span class="line">log_disconnections = on</span><br><span class="line">log_error_verbosity = verbose</span><br></pre></td></tr></table></figure>

<blockquote>
<p>启动psql</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span> -l <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br><span class="line">$ psql -h localhost -U postgres</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>PostgreSQL</tag>
        <tag>Config</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust型变</title>
    <url>/2020/06/14/Rust%E5%9E%8B%E5%8F%98/</url>
    <content><![CDATA[<h2 id="型变"><a href="#型变" class="headerlink" title="型变"></a>型变</h2><ul>
<li>型变(subtype)指根据原始类型子类型关系确定复杂类型子类型关系的规则。型变分为三种形式：<ul>
<li>协变(covariant)。保持子类型关系</li>
<li>逆变(contravariant)。逆转子类型关系</li>
<li>不变(invariant)。不保持也不逆转类型关系，即无关</li>
</ul>
</li>
<li>Rust中只有生命周期有子类型关系，长类型是短类型的子类型</li>
<li><code>PhantomData&lt;T&gt;</code>是一个零大小类型的标记结构体，也叫幻影类型，它扮演三个角色：<ul>
<li>型变</li>
<li>标记拥有关系(drop)</li>
<li>自动trait实现(Send和Sync)<h2 id="几个重要的型变列表"><a href="#几个重要的型变列表" class="headerlink" title="几个重要的型变列表"></a>几个重要的型变列表</h2></li>
</ul>
</li>
<li>&amp;’a T在<code>&#39;a</code>和<code>T</code>上是协变的，对应*<code>const T</code>也是协变</li>
<li>&amp;’a mut T在<code>&#39;a</code>上是协变，但在<code>T</code>上是不变的</li>
<li><code>Fn(T)-&gt;U</code>在<code>T</code>上是不变，在<code>U</code>上是协变</li>
<li><code>Box&lt;T&gt;</code>、<code>Vec&lt;T&gt;</code>以及其他集合对于它们包含的类型来说是协变</li>
<li><code>UnsafeCell&lt;T&gt;</code>、<code>Cell&lt;T&gt;</code>、<code>RefCell&lt;T&gt;</code>、<code>Mutex&lt;T&gt;</code>以及其他内部可变类型在<code>T</code>上都是不变，对应<code>* mut T</code>也是不变</li>
<li>对于结构退来说，如果包含的字段都是协变，则结构体是协变，否则为不变。对<code>Phantomdata&lt;T&gt;</code>类型来说，有以下规则：<ul>
<li><code>PhantomData&lt;T&gt;</code>，在<code>T</code>上是协变</li>
<li><code>PhantomData&lt;&amp;&#39;a T&gt;</code>，在<code>&#39;a</code>和<code>T</code>上是协变</li>
<li><code>PhantomData&lt;&amp;&#39;a mut T&gt;</code>，在<code>&#39;a</code>上是协变，在<code>T</code>上是不变</li>
<li><code>PhantomData&lt;*const T&gt;</code>，在<code>T</code>上是协变</li>
<li><code>PhantomData&lt;*mut T&gt;</code>，在<code>T</code>上是不变</li>
<li><code>PhantomData&lt;fn(T)&gt;</code>，在<code>T</code>上是逆变，如果以后修改语法，会成为不变</li>
<li><code>PhantomData&lt;fn()-&gt;T&gt;</code>，在<code>T</code>上是协变</li>
<li><code>PhantomData&lt;fn(T)-&gt;T&gt;</code>，在<code>T</code>上是不变</li>
<li><code>PhantomData&lt;Cell&lt;&amp;&#39;a()&gt;&gt;</code>，在<code>&#39;a</code>上是不变</li>
</ul>
</li>
<li><strong>当协变不会引起未定义行为的时候，可以用协变，否则就保证该类型为不变或逆变</strong></li>
</ul>
]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust配置</title>
    <url>/2020/06/14/Rust%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h3 id="安装Rust"><a href="#安装Rust" class="headerlink" title="安装Rust"></a>安装Rust</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ curl https://sh.rustup.rs -sSf | sh</span><br><span class="line"><span class="comment"># $ curl https://sh.rustup.rs -sSf | sh -s -- --default-toolchain nightly -y</span></span><br></pre></td></tr></table></figure>

<h3 id="修改国内源"><a href="#修改国内源" class="headerlink" title="修改国内源"></a>修改国内源</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置环境变量</span></span><br><span class="line"><span class="built_in">export</span> RUSTUP_DIST_SERVER=http://mirrors.ustc.edu.cn/rust-static</span><br><span class="line"><span class="built_in">export</span> RUSTUP_UPDATE_ROOT=http://mirrors.ustc.edu.cn/rust-static/rustup</span><br></pre></td></tr></table></figure>

<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ~/.cargo/config</span></span><br><span class="line"><span class="section">[source.crates-io]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">"https://github.com/rust-lang/crates.io-index"</span></span><br><span class="line"><span class="attr">replace-with</span> = <span class="string">'ustc'</span></span><br><span class="line"><span class="section">[source.ustc]</span></span><br><span class="line"><span class="attr">registry</span> = <span class="string">"http://mirrors.ustc.edu.cn/crates.io-index"</span></span><br></pre></td></tr></table></figure>

<h3 id="Docker中使用"><a href="#Docker中使用" class="headerlink" title="Docker中使用"></a>Docker中使用</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Dockerfile</span></span><br><span class="line">FROM phusion/baseimage</span><br><span class="line">ENV RUSTUP_HOME=/rust</span><br><span class="line">ENV CARGO_HOME=/cargo</span><br><span class="line">ENV PATH=/cargo/bin:/rust/bin:<span class="variable">$PATH</span></span><br><span class="line">RUN curl https://sh.rustup.rs -sSf | sh -s -- -- default-toolchain nightly -y</span><br></pre></td></tr></table></figure>

<h3 id="Racer代码补全"><a href="#Racer代码补全" class="headerlink" title="Racer代码补全"></a>Racer代码补全</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cargo install racer</span><br><span class="line">$ rustup component add rust-src</span><br><span class="line"><span class="built_in">export</span> RUST_SRC_PATH=<span class="string">"<span class="variable">$(rustc --print sysroot)</span>/lib/rustlib/src/rust/src"</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Config</tag>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt属性系统</title>
    <url>/2019/07/10/Qt%E5%B1%9E%E6%80%A7%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="1-属性定义"><a href="#1-属性定义" class="headerlink" title="1.属性定义"></a>1.属性定义</h2><p>Qt提供了<code>Q_PROPERTY()</code>宏来定义属性，它是基于<a href="https://zetzhang.github.io/2019/07/06/Qt%E5%85%83%E5%AF%B9%E8%B1%A1%E7%B3%BB%E7%BB%9F/" target="_blank" rel="noopener">元对象系统</a>来实现的。作为平台与编译器无关的库，Qt C++程序可以被任何标注你的C++编译器编译。<br>在QObject的子类中，用宏<code>Q_PROPERTY()</code>定义属性，格式是：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Q_PROPERTY(type name</span><br><span class="line">           (READ getFunction [WRITE setFunction] |</span><br><span class="line">            MEMBER memberName [(READ getFunction | WRITE setFunction)])</span><br><span class="line">           [RESET resetFunction]</span><br><span class="line">           [NOTIFY notifySignal]</span><br><span class="line">           [REVISION <span class="keyword">int</span>]</span><br><span class="line">           [DESIGNABLE <span class="keyword">bool</span>]</span><br><span class="line">           [SCRIPTABLE <span class="keyword">bool</span>]</span><br><span class="line">           [STORED <span class="keyword">bool</span>]</span><br><span class="line">           [USER <span class="keyword">bool</span>]</span><br><span class="line">           [CONSTANT]</span><br><span class="line">           [FINAL])</span><br></pre></td></tr></table></figure>

<p><code>Q_PROPERTY</code>宏定义一个返回值类型为<code>type</code>，名称为<code>name</code>的属性，用<code>READ</code>、<code>WRITE</code>来作为属性的读/写操作，还有其他的一些关键字定义属性的一些操作特性。属性的类型可以是<code>QVariant</code>支持的任何类型，也可以用户自定义类型。<br><code>Q_PROPERTY</code>宏定义属性的一些主要关键字的意义如下：</p>
<ul>
<li><code>READ</code>指定一个读取属性值的函数，没有<code>MEMBER</code>关键字时必须设置<code>READ</code>。</li>
<li><code>WRITE</code>指定一个设定属性值的函数，只读属性没有<code>WRITE</code>设置。</li>
<li><code>MEMBER</code>指定一个成员变量与属性关联，成为可读可写的属性，无需再设置<code>READ</code>和<code>WRITE</code>。</li>
<li><code>RESET</code>是可选的，用于指定一个设置属性缺省值的函数。</li>
<li><code>NOTIFY</code>是可选的，用于设置一个信号，当属性值变化时发射此信号。</li>
<li><code>DESIGNABLE</code>表示属性是否在Qt Designer里可见，缺省为<code>true</code>。</li>
<li><code>CONSTANT</code>表示属性值是一个常数，对于一个对象实例，<code>READ</code>指定的函数返回值是常数，但是每个实例的返回值可以不一样。具有<code>CONSTANT</code>关键字的属性不能有<code>WRITE</code>和<code>NOTIFY</code>关键字。</li>
<li><code>FINAL</code>表示所定义的属性不能被子类重载。</li>
</ul>
<p>QWidget 类定义属性的一些例子如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Q_PROPERTY(<span class="keyword">bool</span> focus READ hasFocus)</span><br><span class="line">Q_PROPERTY(<span class="keyword">bool</span> enabled READ isEnabled WRITE setEnabled)</span><br><span class="line">Q_PROPERTY(QCursor cursor READ cursor WRITE setCursor RESET unsetCursor)</span><br><span class="line">Q_PROPERTY(QString windowTitle READ windowTitle WRITE setWindowTitle NOTIFY windowTitleChanged DESIGNABLE isWindow)</span><br></pre></td></tr></table></figure>

<p>上面最后这个声明包含了<code>NOTIFY windowTitleChanged</code>，表明当<code>windowTitle</code>属性发生变化时，会触发<code>windowTitleChanged</code>信号，相应的信号声明在QWidget中。(<code>this-&gt;setProperty(&quot;windowTitle&quot;, &quot;test&quot;)</code>)  </p>
<hr>
<p>关于<a href="http://blog.sina.com.cn/s/blog_c3c7614d0102wdm2.html" target="_blank" rel="noopener">如何使用NTOIFY和RESET——Qt爱好者</a></p>
<hr>
<h2 id="2-动态属性"><a href="#2-动态属性" class="headerlink" title="2.动态属性"></a>2.动态属性</h2><p>其中，不管有没有用<code>READ</code>和<code>WRITE</code>定义接口函数，只要知道属性名称，就可以通过<code>QObject::property()</code>和<code>QObject::setProperty()</code>读取和设置属性值。e.x.：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QPushButton *button = <span class="keyword">new</span> QPushButton;</span><br><span class="line">QObject *object = button;</span><br><span class="line">object-&gt;setProperty(<span class="string">"flat"</span>, <span class="literal">true</span>);</span><br><span class="line"><span class="keyword">bool</span> isFlat = object-&gt;property(<span class="string">"flat"</span>);</span><br></pre></td></tr></table></figure>

<p>这就是<font color="red">动态属性</font>，运行时通过<code>setProperty()</code>定义新属性，并用<code>property()</code>查询，它是针对类实例定义的。<br>在数据表编辑界面上，为了高亮显示一些必填字段，可以在初始化界面为这些字段关联组件定义新的required属性：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">editName-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line">comboSex-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line">checkAgree-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line"><span class="comment">// 应用样式定义将必填字段背景颜色设置为亮绿色</span></span><br><span class="line">*[required=<span class="string">"true"</span>] &#123;background-color: lime&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-类的附加属性"><a href="#3-类的附加属性" class="headerlink" title="3.类的附加属性"></a>3.类的附加属性</h2><p>属性系统还有一个宏<code>Q_CLASSINFO()</code>，可以为类的元对象定义“名称——值”信息，如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">QMyClass</span>:</span><span class="keyword">public</span> QObject </span><br><span class="line">&#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line">    Q_CLASSINFO(<span class="string">"author"</span>, <span class="string">"Wang"</span>)</span><br><span class="line">    Q_CLASSINFO (<span class="string">"company"</span>, <span class="string">"UPC"</span>)</span><br><span class="line">    Q_CLASSINFO(<span class="string">"version "</span>, <span class="string">"3.0.1"</span>)</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>用<code>Q_CLASSINFO()</code>宏定义附加类信息后，可以通过元对象的一些函数获取类的附加信息，如<code>classlnfo(int)</code>获取某个加信息，函数原型定义如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QMetaClassInfo QMetaObject::classInfo(<span class="keyword">int</span> index) <span class="keyword">const</span></span><br></pre></td></tr></table></figure>

<p>返回值是<code>QMetaClassInfo</code>类型，有<code>name()</code>和<code>value()</code>两个函数，可获得类附加信息的名称和值。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置附加值</span></span><br><span class="line">Q_CLASSINFO(<span class="string">"First"</span>, <span class="string">"Second"</span>)</span><br><span class="line"><span class="comment">// 输出键值</span></span><br><span class="line">qDebug() &lt;&lt; <span class="keyword">this</span>-&gt;metaObject()-&gt;classInfo(<span class="number">0</span>).value() &lt;&lt; <span class="string">": "</span> &lt;&lt; <span class="keyword">this</span>-&gt;metaObject()-&gt;classInfo(<span class="number">0</span>).name();</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust错误处理</title>
    <url>/2020/06/14/Rust%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h2 id="消除失败"><a href="#消除失败" class="headerlink" title="消除失败"></a>消除失败</h2><ul>
<li>常用六个Assert<ul>
<li>assert!：断言布尔表达式运行时一定返回true</li>
<li>assert_eq!：断言两个表达式是否相等（使用PartialEq）</li>
<li>assert_ne!：断言两个表达式是否不相等</li>
<li>debug_assert!：等价于assert!，用于调试模式</li>
<li>debug_assert_eq!</li>
<li>debug_assert_ne!<h2 id="分层处理错误"><a href="#分层处理错误" class="headerlink" title="分层处理错误"></a>分层处理错误</h2></li>
</ul>
</li>
<li>Option<t>：处理有和无的情况，包含Some(T)和None两个选体。不存在的对应值应返回None，开发者需要对None进行处理，而不是直接引发恐慌</t></li>
<li>Result&lt;T, E&gt;：用于处理合理解决的问题，如文件没找到、权限被拒绝、字符串解析出错等</li>
<li>线程恐慌（Panic）：用于处理无法合理解决的问题</li>
<li>程序中止（Abort）：使用abort函数将进程正常中止<h3 id="unwrap系列方法"><a href="#unwrap系列方法" class="headerlink" title="unwrap系列方法"></a>unwrap系列方法</h3></li>
<li>unwrap方法可以取出包含于Some内部的值，但遇到None会引发线程恐慌</li>
<li>unwrap_or方法是对match匹配包装的语法糖，该方法可以指定处理None时返回的值</li>
<li>unwrap_or_else方法和unwrap_or类似，只不过参数是FnOnce()-&gt;T闭包<h3 id="map系列方法：把Option中值通过unwrap取出来再参与计算，会多出很多校验代码，显得冗余，map系列方法可以改善这种情况"><a href="#map系列方法：把Option中值通过unwrap取出来再参与计算，会多出很多校验代码，显得冗余，map系列方法可以改善这种情况" class="headerlink" title="map系列方法：把Option中值通过unwrap取出来再参与计算，会多出很多校验代码，显得冗余，map系列方法可以改善这种情况"></a>map系列方法：把Option<t>中值通过unwrap取出来再参与计算，会多出很多校验代码，显得冗余，map系列方法可以改善这种情况</t></h3></li>
<li>map方法是泛型方法，内部是match匹配，对于Some和None分别做了相应处理，参数为FnOnce(T)-&gt;U闭包，在无需取Option<t>的情况下在Option<t>内部计算，这种map叫组合子</t></t></li>
<li>map_or方法</li>
<li>map_orelse方法</li>
<li>and_then组合子，直接返回值内容，而不经过Some包装<h3 id="错误处理Result-lt-T-E-gt"><a href="#错误处理Result-lt-T-E-gt" class="headerlink" title="错误处理Result&lt;T, E&gt;"></a>错误处理Result&lt;T, E&gt;</h3></li>
<li>从语义上来说，Option<t>可以看做是忽略了错误类型的Result&lt;T, ()&gt;，Result&lt;T, E&gt;枚举体包含两个变体：Ok(T)和Err(E)。使用#[must_use]用来警告应该进行错误处理<h2 id="恐慌（Panic）"><a href="#恐慌（Panic）" class="headerlink" title="恐慌（Panic）"></a>恐慌（Panic）</h2></t></li>
<li>Rust中，使用恐慌安全（Panic Safety）来代替异常安全的说法。Rust中有一些代码会引发恐慌，如None进行unwrap操作、除以0等。Rust提供一个叫UnwindSafe的标记trait，专门标记恐慌安全你的类型，catch_unwind方法可以捕获恐慌，恢复当前线程，除了使用abort引发的进程中止无法捕获</li>
<li>可以使用std::panic::set_hook方法来自定义消息</li>
</ul>
<h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><ul>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI1MjAzNDI1MA==&mid=2648210671&idx=1&sn=b9b6c5e16944889a82f760d2a65a1f3a&chksm=f1c5314ac6b2b85cbf8e42351a40d84c713d1a66d50b8528fcdd88f21b484dbbac90da9e822a&mpshare=1&scene=1&srcid=0614whr6Ck3XNfLcPK2VOgVF&sharer_sharetime=1592119296188&sharer_shareid=d75e104079b232035471512d1d57b844#rd" target="_blank" rel="noopener">细说Rust错误处理</a></li>
</ul>
]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>UNIX格言</title>
    <url>/2020/06/14/UNIX%E6%A0%BC%E8%A8%80/</url>
    <content><![CDATA[<ol>
<li><p>让每个程序就做好一件事。如果有新任务，就重新开始，不要往原程序中加入新功能而搞得复杂。</p>
</li>
<li><p>假定每个程序的输出都会成为另一个程序的输入，哪怕那个程序还是未知的。输出中不要有无关的信息干扰。避免使用严格的分栏格式和二进制格式输入。不要坚持 使用交互式输入。</p>
</li>
<li><p>尽可能早地将设计和编译的软件投入试用, 哪怕是操作系统也不例外，理想情况下, 应该是在几星期内。对拙劣的代码别犹豫，扔掉重写。</p>
</li>
<li><p>优先使用工具而不是拙劣的帮助来减轻编程任务的负担。工欲善其事，必先利其器。</p>
</li>
</ol>
<p>原 则1：你无法断定程序会在什么地方耗费运行时间。瓶颈经常出现在想不到的地方，所以别急于胡乱找个地方改代码，除非你已经证实那儿就是瓶颈所在。</p>
<p>原 则2：估量。在你没对代码进行估量，特别是没找到最耗时的那部分之前，别去优化速度。</p>
<p>原则3：花哨的算法在n很小时通常很慢，而n通常很小。花哨 算法的常数复杂度很大。除非你确定n总是很大，否则不要用花哨算法（即使n很大，也优先考虑原则2）。</p>
<p>原则4：花哨的算法比简单算法更容易出 bug、更难实现。尽量使用简单的算法配合简单的数据结构。</p>
<p>原则5：数据压倒一切。如果已经选择了正确的数据结构并且把一切都组织得井井有条，正 确的算法也就不言自明。编程的核心是数据结构，而不是算法[7]。</p>
<p>原则6：没有原则6。</p>
<blockquote>
<p>《The Art of Unix Programming》</p>
</blockquote>
<ol>
<li>模块原则：使用简洁的接口拼合简单的部件。</li>
<li>清晰原则：清晰胜于机巧。</li>
<li>组合原则：设计时考虑拼接组合。</li>
<li>分离原则：策略同机制分离，接口同引擎分离。</li>
<li>简洁原则：设计要简洁，复杂度能低则低。</li>
<li>吝啬原则：除非确无它法，不要编写庞大的程序。</li>
<li>透明性原则：设计要可见，以便审查和调试。</li>
<li>健壮原则：健壮源于透明与简洁。</li>
<li>表示原则：把知识叠入数据以求逻辑质朴而健壮。</li>
<li>通俗原则：接口设计避免标新立异。</li>
<li>缄默原则：如果一个程序没什么好说的，就沉默。</li>
<li>补救原则：出现异常时，马上退出并给出足够错误信息。</li>
<li>经济原则：宁花机器一分，不花程序员一秒。</li>
<li>生成原则：避免手工hack，尽量编写程序去生成程序。</li>
<li>优化原则：雕琢前先要有原型，跑之前先学会走。</li>
<li>多样原则：决不相信所谓“不二法门”的断言。</li>
<li>扩展原则：设计着眼未来，未来总比预想来得快。</li>
</ol>
]]></content>
      <categories>
        <category>2020.02</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Linux</tag>
        <tag>UNIX</tag>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust的一些概念</title>
    <url>/2020/06/14/Rust%E7%9A%84%E4%B8%80%E4%BA%9B%E6%A6%82%E5%BF%B5/</url>
    <content><![CDATA[<h2 id="类型系统与多态性"><a href="#类型系统与多态性" class="headerlink" title="类型系统与多态性"></a>类型系统与多态性</h2><ul>
<li>Rust一切皆表达式，表达式皆有值，值皆有类型，Rust中一切皆类型</li>
<li>现代编程语言包含三种多态形式：参数化多态、Ad-hoc多态和子类型多态：参数化多态和Ad-hoc是静多态，子类型多态是动多态</li>
<li>参数化多态指泛型，Ad-hoc多态叫特定多态：Haskell中是Typeclass，Rust中是trait；子类型多态出自面向对象语言</li>
<li>Rust没有GC，内存由编译器分配，Rust代码被编译为LLVM IR，其中携带内存分配信息</li>
<li>Rust也有少量的动态大小类型（DST），比如str，因为编译器不可能实现直到字符串，因此字符串切片&amp;str是引用类型，它携带指针和长度信息</li>
<li>Rust还支持零大小类型（ZST），比如单元类型和单元结构体，大小是0</li>
<li>如果ZST类型是空，那么底类型表示无，它是!，被称为Bang Type。</li>
<li>::&lt;&gt;是turbofish操作符。is_positive()可以判断正负</li>
<li>Rust的泛型是静多态，编译器不管是泛型枚举、泛型函数还是泛型结构体，都会被单态化（Monomorphization）（静态分发）</li>
<li>trait有4种用法：接口抽象、泛型约束、抽象类型和标签trait</li>
<li>孤儿原则（Orphan Rule）：如果要实现某个trait，该trait和要实现该trait的类型至少有一个在当前crate中定义。</li>
<li>Rust的trait限定与Java的泛型限定、Ruby和Python的Duck Typeing、Golang的Structural Typeing类似。trait是类型，是集合impl&lt;T: A + B&gt; C for T意思是为所有T属于A交B实现Trait C</li>
<li>泛型中使用trait限定，可以将类型的范围根据类型行为限定精确可控的范围。而OOP那套，可以使用trait object，它将在运行期根据虚表指针从虚表（析构函数、大小、对齐和方法等信息）找出正确指针，然后动态调用，这就是动态分发，区别于泛型静态分发</li>
<li>只有当trait的Self类型参数不能被限定为Sized且trait中所有方法都是对象安全时才能作为trait对象使用</li>
<li>Sized、Unsize和?Sized的关系是，?Sized包含前者，Sized在编译期确定大小，而UnSize是不确定的</li>
<li>对象安全的方法必须满足三点之一<ol>
<li>方法受Self: Sized约束</li>
<li>方法签名同时满足三点<ul>
<li>不包含任何泛型参数。（否则trait对象在Vtable中将不确定调用哪个方法）</li>
<li>第一参数必须为Self类型或可以解引用为Self的类型。（如self、&amp;self、&amp;mut self和self: Box<self></self></li>
<li>Self不能出现在除第一参数外的地方。</li>
</ul>
</li>
<li>trait中不包含关联常量，不过可以增加默认的关联常量，只需要使用const关键字</li>
</ol>
</li>
<li>Rust提供了5个重要标签trait，定义在std::marker，分别是Sized trait、Unsize trait、Copy trait、Send trait和Sync trait</li>
<li>动态大小类型不能随意使用，需遵循三条限制规则：<ul>
<li>只允许通过fat pointer来操作Unsize，类型，如&amp;[T]或&amp;Trait</li>
<li>变量、参数和枚举变量不能使用DST</li>
<li>结构体最后一个字段可以使用DST，其他字段不行</li>
</ul>
</li>
<li>Copy trait继承自Clone trait，因此实现Copy功能同时需要实现Clone trait。Copy行为是隐式行为，开发者不能重载，它是简单的位复制，发生执行在变量绑定、函数参数传递和函数返回等场景中</li>
<li>任何类型都能实现Clone trait，但不是所有类型都能实现Copy trait，前提是所有成员都实现了Copy trait</li>
<li>Rust可以通过实现Deref trait来自定义解引用操作，如果类型T实现了Deref&lt;Target=U&gt;，则该类型T引用或指针在引用时自动转换为U</li>
<li>Rust的解引用有std::ops::Deref的.deref()、.as_ref()、std:borrow::Borrow的.borrow()、&amp;*x和&amp;x[..]</li>
<li>From和Into是定义于std::convert模块中的两个trait，定义了from和into两个方法互为&amp;str和String类型的反操作。标准库中还包含TryFrom和TryInto两种trait，它们是错误处理版本</li>
<li>Rust标准库中包含了AsRef和AsMut两种trait，将值分别转换为不可变引用和可变引用</li>
<li>Box<t>（<code>pub struct Box&lt;T: ?Sized&gt;(Unique&lt;T&gt;);</code>）和Fn、FnMut、FnOnce、Sized等都加上#[fundamental]属性，代表享有特权，不必遵守孤儿规则</t></li>
<li>除了Rust，Rust还有重叠规则（Overlap），规则规定不能为重叠的类型实现同一个trait</li>
<li>迭代元素时，只能按值迭代，有时必须重新分配数据，不能通过引用来复用原始数据，为了支持引用类型，可以重用内部缓存区不需要重新分配内存，需要更高级的类型多态性，即泛型关联类型（Generic Associated Type， GAT）</li>
</ul>
]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust一些结构体需要实现的trait特质</title>
    <url>/2020/06/14/Rust%E4%B8%80%E4%BA%9B%E7%BB%93%E6%9E%84%E4%BD%93%E9%9C%80%E8%A6%81%E5%AE%9E%E7%8E%B0%E7%9A%84trait%E7%89%B9%E8%B4%A8/</url>
    <content><![CDATA[<h3 id="deriving限制一些特定trait"><a href="#deriving限制一些特定trait" class="headerlink" title="deriving限制一些特定trait"></a>deriving限制一些特定trait</h3><ul>
<li><code>Clone</code></li>
<li><code>Copy</code></li>
<li><code>Debug</code></li>
<li><code>Default</code></li>
<li><code>Eq</code></li>
<li><code>Hash</code></li>
<li><code>Ord</code></li>
<li><code>PartialEq</code></li>
<li><code>PartialOrd</code><h3 id="其他一些实现trait"><a href="#其他一些实现trait" class="headerlink" title="其他一些实现trait"></a>其他一些实现trait</h3></li>
<li>检查相等问题需要实现<code>core::cmp::PartialEq</code>的trait</li>
<li>构造和析构可以实现<code>new</code>方法和<code>Drop trait</code>的<code>drop</code>方法</li>
<li>调用函数可以重载trait系统，有三个，分别是<code>Fn</code>、<code>FnMut</code>和<code>FnOnce</code></li>
<li>如果需要实现打印输出功能，需要实现<code>fmt::Display trait</code>(<code>std::fmt::Display</code>)，错误输出则需要返回fmt::Result</li>
<li>实现错误类型需要实现<code>error::Error trait</code>(<code>std::error</code>)的description方法和cause方法</li>
<li>如果需要重载<code>+</code>运算符可以通过重载Add特性实现 <a href="https://doc.rust-lang.org/stable/std/ops/" target="_blank" rel="noopener">std::ops</a></li>
<li>想要自定义指针类型，可以通过重载<code>Deref</code>和<code>DereMut</code>(<code>std::ops::Deref</code>)特性的<code>deref</code>方法来实现解引用运算符</li>
<li>实现自定义派生属性，则需要在函数前加上<code>#[proc_macro_derive(attr_name)]</code>，并且<code>input</code>参数和返回值为<code>TokenStream</code>，实现函数名为<code>derive</code><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Add +.</span><br><span class="line">AddAssign +=.</span><br><span class="line">BitAnd &amp;.</span><br><span class="line">BitAndAssign &amp;=.</span><br><span class="line">BitOr |.</span><br><span class="line">BitOrAssign |=.</span><br><span class="line">BitXor ^.</span><br><span class="line">BitXorAssign ^=.</span><br><span class="line">Deref *v.</span><br><span class="line">DerefMut *v = 1;.</span><br><span class="line">Div /.</span><br><span class="line">DivAssign /=.</span><br><span class="line">Drop &apos;destructor&apos;.</span><br><span class="line">Fn an immutable receiver.</span><br><span class="line">FnMut a mutable receiver.</span><br><span class="line">FnOnce a by-value receiver.</span><br><span class="line">Index (container[index]).</span><br><span class="line">IndexMut (container[index]).</span><br><span class="line">Mul *.</span><br><span class="line">MulAssign *=.</span><br><span class="line">Neg -.</span><br><span class="line">Not !.</span><br><span class="line">RangeBounds RangeBounds is implemented by Rust&apos;s built-in range types, produced by range syntax like .., a.., ..b, ..=c, d..e, or f..=g.</span><br><span class="line">Rem %.</span><br><span class="line">RemAssign %=.</span><br><span class="line">Shl &lt;&lt;</span><br><span class="line">ShlAssign &lt;&lt;=.</span><br><span class="line">Shr &gt;&gt; </span><br><span class="line">ShrAssign &gt;&gt;=.</span><br><span class="line">Sub -.</span><br><span class="line">SubAssign -=.</span><br><span class="line">CoerceUnsized	Experimental Trait that indicates that this is a pointer or a wrapper for one, where unsizing can be performed on the pointee.</span><br><span class="line">DispatchFromDyn	Experimental This is used for object safety, to check that a method&apos;s receiver type can be dispatched on.</span><br><span class="line">Generator	Experimental The trait implemented by builtin generator types.</span><br><span class="line">Try	Experimental A trait for customizing the behavior of the ? operator.</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="macro-rules片段分类符句法含义："><a href="#macro-rules片段分类符句法含义：" class="headerlink" title="macro_rules片段分类符句法含义："></a>macro_rules片段分类符句法含义：</h3><ul>
<li><code>ident</code>：一个标识符。如：x，foo</li>
<li><code>path</code>：一个受限的名字，指代路径。如：T::SpecialA、foo、std::iter等</li>
<li><code>expr</code>：一个表达式。如2+2；f(42)</li>
<li><code>ty</code>：一个类型。如i32;Vec&lt;(char, String)&gt;；&amp;T</li>
<li><code>pat</code>：一个模式。如：Some(t)；_</li>
<li><code>stmt</code>：一个单独语句。如：let x = 3</li>
<li><code>block</code>：一个大括号界定的语句序列</li>
<li><code>item</code>：一个项。如fn foo() {}， struct Bar，模块、声明、函数定义、类型定义、结构体定义、impl实现等都是</li>
<li><code>meta</code>：一个元数据项，可以从属性中找到。如：cfg(target_os = “windows”)，包含在#[…]或![…]属性内信息</li>
<li><code>tt</code>：TokenTree缩写，指代词条树</li>
<li><code>vis</code>：指代可见性，如pub</li>
<li><code>lifetime</code>：指代生命周期<h4 id="对于原变量后面的记号有额外的规则："><a href="#对于原变量后面的记号有额外的规则：" class="headerlink" title="对于原变量后面的记号有额外的规则："></a>对于原变量后面的记号有额外的规则：</h4></li>
<li>expr和stmt变量必须后跟任意一个：=&gt; , ;</li>
<li>ty和path变量必须后跟任意一个：=&gt; , = | ; : &gt; [ { as where</li>
<li>pat变量必须后跟任意一个：=&gt; , = | if in</li>
<li>其他变量可以跟任何记号<h4 id="宏系统完全不处理解析模糊。通过在某些记号前面加上记号可以解决问题，如：-I-i-ident-E-e-expr"><a href="#宏系统完全不处理解析模糊。通过在某些记号前面加上记号可以解决问题，如：-I-i-ident-E-e-expr" class="headerlink" title="宏系统完全不处理解析模糊。通过在某些记号前面加上记号可以解决问题，如：$(I $i:ident)* E $e:expr"></a>宏系统完全不处理解析模糊。通过在某些记号前面加上记号可以解决问题，如：$(I $i:ident)* E $e:expr</h4><h4 id="macro-use-、-macro-export-、-no-link"><a href="#macro-use-、-macro-export-、-no-link" class="headerlink" title="#[macro_use]、#[macro_export]、#[no_link]"></a>#[macro_use]、#[macro_export]、#[no_link]</h4></li>
</ul>
]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>VIM下AsyncRun插件的环境变量</title>
    <url>/2020/06/14/VIM%E4%B8%8BAsyncRun%E6%8F%92%E4%BB%B6%E7%9A%84%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$(VIM_FILEPATH)      ＃具有完整路径的当前缓冲区的文件名。</span><br><span class="line">$(VIM_FILENAME)      ＃当前缓冲区的文件名，不带路径。</span><br><span class="line">$(VIM_FILEDIR)       ＃当前缓冲区的完整路径，没有文件名。</span><br><span class="line">$(VIM_FILEEXT)       ＃当前缓冲区的文件扩展名。</span><br><span class="line">$(VIM_FILETYPE)      ＃文件类型（在vim中为＆ft的值）</span><br><span class="line">$(VIM_FILENOEXT)     ＃当前缓冲区的文件名，不带路径和扩展名。</span><br><span class="line">$(VIM_PATHNOEXT)     ＃当前文件名，具有完整路径，但没有扩展名。</span><br><span class="line">$(VIM_CWD)           ＃当前目录（由：pwd返回）。</span><br><span class="line">$(VIM_RELDIR)        ＃文件路径相对于当前目录。</span><br><span class="line">$(VIM_RELNAME)       ＃文件名相对于当前目录。</span><br><span class="line">$(VIM_ROOT)          ＃项目根目录。</span><br><span class="line">$(VIM_CWORD)         ＃光标下的单词。</span><br><span class="line">$(VIM_CFILE)         ＃光标下的文件名。</span><br><span class="line">$(VIM_CLINE)         ＃当前缓冲区</span><br><span class="line">$(VIM_GUI)           ＃有（&apos;gui_runnin&apos;）吗？</span><br><span class="line">$(VIM_VERSION)       ＃诉价值：版本。</span><br><span class="line">$(VIM_COLUMNS)       ＃当前的屏幕宽度。</span><br><span class="line">$(VIM_LINES)         ＃当前的屏幕高度。</span><br><span class="line">$(VIM_SVRNAME)       ＃诉值：服务器。</span><br><span class="line">$(VIM_PRONAME)       ＃当前项目根目录的名称</span><br><span class="line">$(VIM_DIRNAME)       ＃当前目录的名称</span><br><span class="line">$(VIM_INIFILE)       ＃当前ini（.tasks）文件的完整路径名。</span><br><span class="line">$(VIM_INIHOME)       ＃当INI文件所在。</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.05</category>
      </categories>
      <tags>
        <tag>VIM</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust跨语言</title>
    <url>/2020/06/14/Rust%E8%B7%A8%E8%AF%AD%E8%A8%80/</url>
    <content><![CDATA[<h2 id="语言交互"><a href="#语言交互" class="headerlink" title="语言交互"></a>语言交互</h2><ul>
<li><p>外部函数接口（Foreign Function Interface，FFI）用于规范语言间调用的语法特征。其他有java的JNI（Java Native Interface），一样的意思</p>
</li>
<li><p>应用程序二进制接口（ABI）涵盖内容：</p>
<ul>
<li>调用约定。参数、函数、返回值</li>
<li>内存布局。大小和对齐方式</li>
<li>处理器指令集。不同平台实现</li>
<li>目标文件和库的二进制形式。</li>
</ul>
</li>
<li><p>Rust中使用FFI需要通过extern和extern块对FFI接口进行标注，编译时会由LLVM默认生成C-ABI</p>
<h3 id="链接与Crate-Type"><a href="#链接与Crate-Type" class="headerlink" title="链接与Crate Type"></a>链接与Crate Type</h3></li>
<li><p>链接产生于程序开发的模块化。C和C++每个文件都是一个编译单元，Rust是以包（crate）为编译单元</p>
</li>
<li><p>链接过程就是编译器经过符号解析、存储空间分配和重定位的过程，最终生成可执行文件或库。</p>
</li>
<li><p>库分为静态库和动态库。静态库是普通目标文件集合的简单拼接，使用简单，但浪费空间；动态库则是把链接过程延迟到运行时，如运行时重定位，这比较省空间</p>
</li>
<li><p>Rust支持四种库：Rust的dylib、rlib，和其他语言的cdylib、staticlib</p>
</li>
<li><p>Rust库类型的命令行参数或crate_type属性：</p>
<ul>
<li><code>--crate-type=bin</code>或<code>#[crate_type = &quot;bin&quot;]</code>，表示是可执行文件，程序必须包含main函数</li>
<li><code>--crate-type=lib</code>或<code>#[crate_type = &quot;lib&quot;]</code>，表示生成Rust库，什么库编译器决定，一般是rlib静态库</li>
<li><code>--crate-type=rlib</code>或<code>#[crate_type = &quot;rlib&quot;]</code>，生成静态Rust库</li>
<li><code>--crate-type=dylib</code>或<code>#[crate_type = &quot;dylib&quot;]</code>，生成动态Rust库</li>
<li><code>--crate-type=staitclib</code>或<code>#[crate_type = &quot;staticlib&quot;]</code>，将生成静态系统库，Rust编译器永远不会链接它。用于和C语言链接</li>
<li><code>--crate-type=cdylib</code>或<code>#[crate_type = &quot;cdylib&quot;]</code>，生成动态系统库<h3 id="交叉编译"><a href="#交叉编译" class="headerlink" title="交叉编译"></a>交叉编译</h3></li>
</ul>
</li>
<li><p>本地平台上编译出需要在其他平台运行的程序叫做交叉编译。</p>
</li>
<li><p>可以使用rustc进行交叉编译，只需要传递target参数即可：如<code>$ rustc --target=arm-unknown-linux-gnueabihf hello.rs</code></p>
</li>
<li><p>也可以使用Cargo交叉编译，方法类似，在cargo<br>build命令传递–target参数即可。Rust默认使用cc作为交叉编译的链接器，修改配置文件可以指定链接器：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># $ car ~/.cargo/config</span></span><br><span class="line"><span class="section">[target.arm-unknown-linux-gnueabihf]</span></span><br><span class="line"><span class="attr">linker</span> = <span class="string">"arm-linux-gnueabihf-gcc-4.8"</span>  <span class="comment"># 指定链接器</span></span><br><span class="line"><span class="comment"># 使用cargo交叉编译</span></span><br><span class="line"><span class="comment"># $ cargo build --target=arm-unknown-linux-gnueabihf</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>target triple格式是<code>{arch}-{vendor}-{sys}-{abi}</code>，分别代表编译程序的主机系统、供应商、操作系统、ABI接口，最后abi有时候可以省略</p>
<h3 id="extern语法"><a href="#extern语法" class="headerlink" title="extern语法"></a>extern语法</h3></li>
<li><p>有extern关键字和extern块。有三个extern API字符串是跨平台的：<code>extern &quot;Rust&quot;</code>、<code>extern &quot;C&quot;</code>、<code>extern &quot;system&quot;</code></p>
</li>
<li><p>另外有三个Rust编译器专用的ABI字符串：</p>
<ul>
<li><code>extern &quot;rust-intrinsic&quot;</code>，代表Rust编译器内部函数的ABI</li>
<li><code>extern &quot;rust-call&quot;</code>，Fn::call的ABI</li>
<li><code>extern &quot;platform-intrinsic&quot;</code>，特定平台内在函数的ABI<h2 id="与C-C-交互"><a href="#与C-C-交互" class="headerlink" title="与C/C++交互"></a>与C/C++交互</h2><h3 id="Rust调用C函数"><a href="#Rust调用C函数" class="headerlink" title="Rust调用C函数"></a>Rust调用C函数</h3></li>
</ul>
</li>
<li><p>首先新建C/C++源代码文件夹，编写好代码后，使用<code>extern &quot;C&quot; {}</code>外暴露接口</p>
</li>
<li><p>在caogo.toml配置文件中，<code>[build-dependencies]</code>中使用cc库，然后在<code>[package]</code>中将某个build.rs文件指定为自动化编译配置文件</p>
</li>
<li><p>在build.rs中引进包<code>extern crate cc</code>，使用<code>cc.Build::new()</code>等方法初始化，在Rust源文件中正常使用时，用<code>extern { ... }</code>声明指定的外部接口</p>
<h3 id="C调用Rust库"><a href="#C调用Rust库" class="headerlink" title="C调用Rust库"></a>C调用Rust库</h3></li>
<li><p>首先在cargo.toml配置文件中<code>[lib]</code>设置Rust链接库名称，如：<code>name = &quot;callrust&quot;</code>，将在Rust同级目录下写C头文件用于暴露Rust接口，并设置<code>crate-type  = [&quot;staticlib&quot;, &quot;cdylib&quot;]</code>用于生成什么类型的链接库，库将产生在target/Debug下</p>
</li>
<li><p>编写Rust程序，将对应接口写在callrust.c中，并编写markdown文件：cargo用于编译Rust代码，gcc用于链接库并生成对应二进制文件</p>
</li>
<li><p>在另起的C源代码文件夹中直接<code>#include &quot;callrust&quot;</code>，然后调用接口函数即可</p>
<h3 id="第三方工具"><a href="#第三方工具" class="headerlink" title="第三方工具"></a>第三方工具</h3></li>
<li><p>rust-bindgen：根据头文件自动生成Rust FFI的C绑定，支持部分C++功能</p>
</li>
<li><p>cbindgen：根据Rust代码自动生成头文件</p>
</li>
<li><p>ctest：为Rust FFI的C绑定自动生成测试文件</p>
</li>
<li><p>移动平台下也有两个库：</p>
<ul>
<li>cargo-lipo：提供<code>cargo lipo</code>命令，自动生成用于iOS的通用库</li>
<li>jni：提供Rust的JNI绑定，用于和Android平台交互</li>
</ul>
</li>
</ul>
<h2 id="使用Rust提升动态语言性能"><a href="#使用Rust提升动态语言性能" class="headerlink" title="使用Rust提升动态语言性能"></a>使用Rust提升动态语言性能</h2><ul>
<li>使用Rust为动态库编写扩展，能保证性能，还能提升内存安全</li>
<li>动态语言有自己的虚拟机，调用Rust代码不能像C/C++_直接链接Rust的链接库获取函数调用信息。因此动态语言提供FFI 基本都是基于libffi库来实现动态调用C函数的能力，兼容C-ABI的链接库都可以直接被动态调用。libffi库是动态语言虚拟机和二进制的一道桥梁</li>
</ul>
<h2 id="Rust与WebAssembly"><a href="#Rust与WebAssembly" class="headerlink" title="Rust与WebAssembly"></a>Rust与WebAssembly</h2><p>WebAssembly是兴起的新的字节码格式，缩写是“WASM”，它是一个面向Web通用二进制和文本格式项目，它是为了作为C/C++/Rust语言的一种编译目标，在客户端提供一种接近本地运行速度的多语言编写代码的方式。某种意义上说，WebAssembly是一种中间语言（<br>IR），就像汇编（Assembly）语言那样是所有语言转换成机器码的通用底层语言，WebAssembly就是面向Web的汇编</p>
<ul>
<li><p>WebAssembly比JavaScript快的原因：</p>
<ul>
<li>WebAssembly体积更小，下载和解析更快。</li>
<li>WebAssembly不受JavaScript约束，可以利用更多CPU特性。如64位整数、内存读写偏移量、非内存对齐读写和多种CPU指令等。</li>
<li>生成WebAssembly编译器工具链的优化和改进。如在Rust中，可以用wasm-gc工具优化生成的wasm文件大小。</li>
<li>WebAssembly不需要垃圾回收。内存操作是手动控制，但也没必要担心内存泄漏，它的内存空间是JavaScript分配的对象，最终Javascript的GC管理</li>
<li>WebAssembly在Web只是一个应用环境，WebAssembly还可以应用其他领域：桌面图形化程序、区块链智能合约和编写操作系统微内核<h2 id="WebAssembly要点"><a href="#WebAssembly要点" class="headerlink" title="WebAssembly要点"></a>WebAssembly要点</h2></li>
</ul>
</li>
<li><p>模块。一个.wasm文件局势一个基本编译单位</p>
</li>
<li><p>线性内存。用于和JavaScript通信，是个可变大小的ArrayBuffer，由JavaScript分配</p>
</li>
<li><p>表格。用于存放函数引用，支持动态调用函数</p>
</li>
<li><p>实例。同一个模块的多个实例共享相同的内存和表格</p>
</li>
<li><p>栈式机器。WebAssembly指令的运行基于栈式机器定义，每种指令都是在站上进行出栈和入站操作</p>
<h2 id="使用Rust开发WebAssembly"><a href="#使用Rust开发WebAssembly" class="headerlink" title="使用Rust开发WebAssembly"></a>使用Rust开发WebAssembly</h2></li>
<li><p>可以手写wat文本格式开发wasm模块，但效率不高，WebAssmebly设计之初是为了作为编译目标存在的，因此可以作为很多变成语言的编译目标：</p>
<ul>
<li><p>C/C++，可以通过EmScripten工具来编译到wasm。EmScripten是个LLVM后端工具，可以将LLVM中间码编译到asm.js。所以C/C++的编译流程可以通过任何一个LLVM前端工具生成LLVM IR，然后通过EmScripten生成asm.js，最后通过WebAssembly编译工具链Binaryen将asm.js生成wasm二进制格式。一些不支持wasm的浏览器也可以使用asm.js来代替</p>
</li>
<li><p>Rust，支持wasm的两种编译目标：</p>
<ul>
<li>wasm32-unknown-unknown，使用LLVM WebAssembly Backend和lld链接器</li>
<li>wasm32-unknown-emscripten，继续使用EmScripten，和C/C++类似</li>
</ul>
</li>
<li><p>搭建wasm开发环境：</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ rustup toolchain install nightly</span><br><span class="line">$ rustup target add wasm32-unknwon-unknown --toolchain nightly</span><br></pre></td></tr></table></figure>
</li>
<li><p>生成wasm例子：</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cargo install wasm-gc</span><br><span class="line">$ cargo build --target wasm32-unknown-unknown</span><br><span class="line">$ cp target/wasm32-unknown-unknown-unknown/debug/hello_wasm.wasm output</span><br><span class="line">$ wasm-gc output/hello_wasm.wasm output/small_hello.wasm</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h2 id="WebAssembly开发生态"><a href="#WebAssembly开发生态" class="headerlink" title="WebAssembly开发生态"></a>WebAssembly开发生态</h2><ul>
<li><p>Rust开发效率还是很低，WebAssembly标准值定义了四种类型：i32、i64、f32和f64。因此，Rust官方打造了一系列工具：</p>
<ul>
<li><p>wasm-bindgen：它的核心是促进JavaScript和Rust之间使用wasm进行通信，允许开发者直接使用Rust的结构体、JavaScript的类、字符串等类型</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cargo +nightly install wasm-bindgen-cli</span><br></pre></td></tr></table></figure>
</li>
<li><p>wasm-pack：一站式构建、发布Rust编译的wasm到npm平台。不需要安装npm、node.js等JavaScript环境，wasm-pack会编译并优化生成JavaScript绑定，然后发布到npm中</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cargo install wasm-pack</span><br><span class="line"><span class="comment"># 在项目的根目录下执行命令，自动生成JavaScript相关文件，方便打包wasm到npm平台</span></span><br><span class="line">$ wasm-pack build</span><br></pre></td></tr></table></figure>
</li>
<li><p>cargo-generate：直接生成wasm-bindgen和wasm-pack项目模板，方便开发</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cargo install cargo-generate</span><br><span class="line"><span class="comment"># 命令生成模板会在Cargo.toml文件配置好wasm-bindgen，需要先安装wasm-bindgen-cli工具</span></span><br><span class="line">$ cargo-generate --git https://github.com/your-responsbility</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>除此之外，社区的一些有代表性的框架：</p>
<ul>
<li>stdweb：Rust和WebAssembly实现的Web客户端标准库，可能会被web-sys替代</li>
<li>cargo-web：方便编写Web客户端的Cargo子命令库</li>
<li>yew：构建客户端Web应用的Rust框架，基于stdweb库</li>
<li>percy：实现虚拟Dom，根据服务端HTML字符串渲染到浏览器的Dom</li>
<li>ruukh：实验性Rust Web前端框架</li>
</ul>
</li>
</ul>
<hr>
<h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><ul>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI1MjAzNDI1MA==&mid=2648210830&idx=1&sn=0cc558bddbdb4a2fc46e88899355f392&chksm=f1c5302bc6b2b93d18475908366cee2ab6578d0e372a30682ae9f27a146ed65df06da5011add&mpshare=1&scene=1&srcid=0614QlAVxJwnv78DfLGPcVvR&sharer_sharetime=1592119955890&sharer_shareid=d75e104079b232035471512d1d57b844#rd" target="_blank" rel="noopener">Rust FFI 开发系列教程 - 序言</a></li>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI1MjAzNDI1MA==&mid=2648210039&idx=2&sn=473d5fb6a022e2f8b84bdf7b72cd07e0&chksm=f1c537d2c6b2bec475e01dbb7a700a0f88796b4c7c65804f50ed1303a6643c61c16cad0077d0&mpshare=1&scene=1&srcid=0614K1t3l4DLRTRGxmQagcFc&sharer_sharetime=1592119988371&sharer_shareid=d75e104079b232035471512d1d57b844#rd" target="_blank" rel="noopener">设计优雅的 Rust 库 API</a></li>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI1MjAzNDI1MA==&mid=2648208895&idx=2&sn=c20ddf5904df7a03ae870466c395fa8c&chksm=f1c5385ac6b2b14cf05979110e8d99e98765244e44579bd2e2b02f2e5bb9e3a7c2311a5f6ca7&mpshare=1&scene=1&srcid=0614bEtcOeEC5soG4lvYNsc2&sharer_sharetime=1592120071267&sharer_shareid=d75e104079b232035471512d1d57b844#rd" target="_blank" rel="noopener">Rust FFI 实践</a></li>
</ul>
]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Rust</tag>
        <tag>FFI</tag>
        <tag>ABI</tag>
      </tags>
  </entry>
  <entry>
    <title>VIM配置</title>
    <url>/2020/06/14/VIM%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<blockquote>
<p>抛却<code>vim-spf13</code>后，偷窃了一些配置，拼凑为自己的<code>VIM</code>配置</p>
</blockquote>
<h3 id="通过以下命令，安装VIM插件，仅限Manjaro操作系统"><a href="#通过以下命令，安装VIM插件，仅限Manjaro操作系统" class="headerlink" title="通过以下命令，安装VIM插件，仅限Manjaro操作系统"></a>通过以下命令，安装VIM插件，<strong>仅限Manjaro操作系统</strong></h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> https://github.com/ZetZhang/vim-congiration-installer.git</span><br><span class="line">$ <span class="built_in">cd</span> vim-congiration-installer</span><br><span class="line">$ ./install.sh</span><br><span class="line"></span><br><span class="line"><span class="comment">### 效果图</span></span><br></pre></td></tr></table></figure>

<p><a href="https://imgchr.com/i/tzado9" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2020/06/14/tzado9.png" alt="tzado9.png"></a></p>
]]></content>
      <categories>
        <category>2020.04</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Config</tag>
        <tag>VIM</tag>
        <tag>Manjaro</tag>
      </tags>
  </entry>
  <entry>
    <title>yum提示rpmdb_open_failed解决方案</title>
    <url>/2020/06/14/yum%E6%8F%90%E7%A4%BArpmdb-open-failed%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd /var/lib/rpm # rpmdb所在目录</span><br><span class="line">rm -f __db.*    # 清除原rpmdb文件</span><br><span class="line">rpm --rebuilddb # 重建rpm数据库</span><br><span class="line">yum clean all   # 清除所有yum的缓存</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.01</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>CentOS</tag>
      </tags>
  </entry>
  <entry>
    <title>个人Blog</title>
    <url>/2019/06/30/%E5%85%B3%E4%BA%8E%E4%B8%AA%E4%BA%BABlog%E7%9A%84%E5%9F%BA%E7%A1%80%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h2 id="nvm：node-js版本管理工具安装"><a href="#nvm：node-js版本管理工具安装" class="headerlink" title="nvm：node.js版本管理工具安装"></a><font color="red">nvm</font>：node.js版本管理工具安装</h2><p>nvm是可以管理不同版本的node和npm，所以我们通过nvm来安装Node.js<br>nvm的Github地址[nvm][<a href="https://github.com/nvm-sh/nvm]" target="_blank" rel="noopener">https://github.com/nvm-sh/nvm]</a></p>
<h3 id="安装nvm"><a href="#安装nvm" class="headerlink" title="安装nvm"></a>安装nvm</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># cURL</span></span><br><span class="line">$ curl https://raw.github.com/creationix/nvm/v0.33.11/install.sh | sh</span><br><span class="line"><span class="comment"># Wget</span></span><br><span class="line">$ wget -qO- https://raw.github.com/creationix/nvm/v0.33.11/install.sh | sh</span><br></pre></td></tr></table></figure>

<h3 id="进入～-nvm目录下配置环境"><a href="#进入～-nvm目录下配置环境" class="headerlink" title="进入～/.nvm目录下配置环境"></a>进入～/.nvm目录下配置环境</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> ~/.nvm/ &amp;&amp; touch .bash_profile</span><br></pre></td></tr></table></figure>

<h3 id="向-bash-profile中写入如下内容"><a href="#向-bash-profile中写入如下内容" class="headerlink" title="向.bash_profile中写入如下内容"></a>向.bash_profile中写入如下内容</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> NVM_DIR=<span class="string">"<span class="variable">$HOME</span>/.nvm"</span></span><br><span class="line">[ -s <span class="string">"<span class="variable">$NVM_DIR</span>/nvm.sh"</span> ] &amp;&amp; \. <span class="string">"<span class="variable">$NVM_DIR</span>/nvm.sh"</span> <span class="comment"># This loads nvm</span></span><br></pre></td></tr></table></figure>

<h3 id="执行文件配置"><a href="#执行文件配置" class="headerlink" title="执行文件配置"></a>执行文件配置</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span> .bash_profile</span><br></pre></td></tr></table></figure>

<h3 id="这时候就安装成功了"><a href="#这时候就安装成功了" class="headerlink" title="这时候就安装成功了"></a>这时候就安装成功了</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">nvm --version</span><br></pre></td></tr></table></figure>

<h3 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ nvm install stable</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h2><p><font color="red">npm</font>是世界上最大的软件注册表，每个星期有30亿次下载量</p>
<h3 id="npm设置为淘宝的镜像源"><a href="#npm设置为淘宝的镜像源" class="headerlink" title="npm设置为淘宝的镜像源"></a>npm设置为淘宝的镜像源</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ npm install -g cnpm --registry=https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure>

<h3 id="安装hexo-1"><a href="#安装hexo-1" class="headerlink" title="安装hexo"></a>安装hexo</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cnpm install -g hexo-cli</span><br></pre></td></tr></table></figure>

<h3 id="在想要创建的文件夹下初始化hexo目录"><a href="#在想要创建的文件夹下初始化hexo目录" class="headerlink" title="在想要创建的文件夹下初始化hexo目录"></a>在想要创建的文件夹下初始化hexo目录</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo init</span><br></pre></td></tr></table></figure>

<h3 id="使用hexo"><a href="#使用hexo" class="headerlink" title="使用hexo"></a>使用hexo</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo s        <span class="comment"># 启动hexo</span></span><br><span class="line">$ hexo clean    <span class="comment"># 删除数据库</span></span><br><span class="line">$ hexo g        <span class="comment"># update generate</span></span><br></pre></td></tr></table></figure>

<p>然后就可以在浏览器地址栏中输入localhost:4000访问到博客了</p>
<hr>
<h2 id="部署到Github"><a href="#部署到Github" class="headerlink" title="部署到Github"></a>部署到Github</h2><p>通过部署到远端服务器来公开使用</p>
<h3 id="在github上创建名为-你的ID-github-io-的项目"><a href="#在github上创建名为-你的ID-github-io-的项目" class="headerlink" title="在github上创建名为[你的ID.github.io]的项目"></a>在github上创建名为[你的ID.github.io]的项目</h3><h3 id="安装部署插件"><a href="#安装部署插件" class="headerlink" title="安装部署插件"></a>安装部署插件</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cnpm install --save hexo-deployer-git</span><br></pre></td></tr></table></figure>

<h3 id="设置-config-yml"><a href="#设置-config-yml" class="headerlink" title="设置_config.yml"></a>设置_config.yml</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Deployment下配置</span></span><br><span class="line">    <span class="built_in">type</span>: git</span><br><span class="line">    repo: [your github repository url]</span><br><span class="line">    branch: master</span><br></pre></td></tr></table></figure>

<h3 id="部署到远端"><a href="#部署到远端" class="headerlink" title="部署到远端"></a>部署到远端</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>

<p><a href="https://hexo.io/zh-cn/docs/" target="_blank" rel="noopener">Hoex</a>文档说明</p>
<p>Reference from <a href="https://www.codesheep.cn/" target="_blank" rel="noopener">codesheep</a></p>
<h3 id="hexo-console-rename插件安装"><a href="#hexo-console-rename插件安装" class="headerlink" title="hexo-console-rename插件安装"></a>hexo-console-rename插件安装</h3><p><a href="https://github.com/timnew/hexo-console-rename" target="_blank" rel="noopener">hexo-console-rename</a> install using npm</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-console-rename@3 --save</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2019.09</category>
      </categories>
      <tags>
        <tag>Github</tag>
        <tag>hexo</tag>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>如何使共享库可执行</title>
    <url>/2020/06/14/%E5%A6%82%E4%BD%95%E4%BD%BF%E5%85%B1%E4%BA%AB%E5%BA%93%E5%8F%AF%E6%89%A7%E8%A1%8C/</url>
    <content><![CDATA[<blockquote>
<p>以<code>shared_hello.c</code>为例子，里面有函数和main入口</p>
</blockquote>
<h3 id="让可执行文件可共享"><a href="#让可执行文件可共享" class="headerlink" title="让可执行文件可共享"></a>让可执行文件可共享</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ gcc -m32 -pie -fpie -rdynamic -o libshared_hello.so shared_hello.c</span><br><span class="line">$ gcc -m32 -o shared_hello.noc -L./ -lshared_hello</span><br><span class="line">$ LD_LIBRARY_PATH=<span class="variable">$LD_LIBRARY_PATH</span>:./ ./shared_hello.noc</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="让共享库可执行"><a href="#让共享库可执行" class="headerlink" title="让共享库可执行"></a>让共享库可执行</h3><h4 id="这种生成方式的程序没有入口"><a href="#这种生成方式的程序没有入口" class="headerlink" title="这种生成方式的程序没有入口"></a>这种生成方式的程序没有入口</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ gcc -m32 -shared -fpic -o libshared_hello.so shared_hello.c</span><br><span class="line">$ readelf -h libshared_hello.so | grep <span class="string">"Entry point"</span></span><br><span class="line">$ objdump -d libshared_hello.so | grep 1050 | head -2</span><br></pre></td></tr></table></figure>

<h4 id="解决入口问题也是如此"><a href="#解决入口问题也是如此" class="headerlink" title="解决入口问题也是如此"></a>解决入口问题也是如此</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ gcc -m32 -shared -fpic -o libshared_hello.so shared_hello.c -Wl,-emain</span><br></pre></td></tr></table></figure>

<h4 id="gdb的core-dump解析"><a href="#gdb的core-dump解析" class="headerlink" title="gdb的core dump解析"></a><code>gdb</code>的core dump解析</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">ulimit</span> -c unlimited</span><br></pre></td></tr></table></figure>

<h4 id="通过主动链接器跑一下"><a href="#通过主动链接器跑一下" class="headerlink" title="通过主动链接器跑一下"></a>通过主动链接器跑一下</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ /lib/ld-linux.so.2 ./libshared_hello.so</span><br></pre></td></tr></table></figure>

<h4 id="通过程序中加入标准退出函数-exit-0-，就能正常执行了"><a href="#通过程序中加入标准退出函数-exit-0-，就能正常执行了" class="headerlink" title="通过程序中加入标准退出函数_exit(0)，就能正常执行了"></a>通过程序中加入标准退出函数<code>_exit(0)</code>，就能正常执行了</h4><hr>
<h4 id="实际入口函数并非main-，而是-start，再改造，编译时连入口都不用指定了"><a href="#实际入口函数并非main-，而是-start，再改造，编译时连入口都不用指定了" class="headerlink" title="实际入口函数并非main()，而是_start，再改造，编译时连入口都不用指定了"></a>实际入口函数并非<code>main()</code>，而是<code>_start</code>，再改造，编译时连入口都不用指定了</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ gcc -m32 -g -shared -fpic -o libshared_hello.so shared_hello.c</span><br><span class="line">$ /lib/ld-linux.so.2 ./libshared_hello.so</span><br></pre></td></tr></table></figure>

<h4 id="也可以当共享库使用了"><a href="#也可以当共享库使用了" class="headerlink" title="也可以当共享库使用了"></a>也可以当共享库使用了</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ gcc -m32 -o shared_hello.noc -L./ -lshared_hello</span><br><span class="line">$ LD_LIBRARY_PATH=<span class="variable">$LD_LIBRARY_PATH</span>:./ ./shared_hello.noc</span><br></pre></td></tr></table></figure>

<h4 id="然后那个asm内嵌汇编解决主动链接问题的跑不起来"><a href="#然后那个asm内嵌汇编解决主动链接问题的跑不起来" class="headerlink" title="然后那个asm内嵌汇编解决主动链接问题的跑不起来"></a>然后那个asm内嵌汇编解决主动链接问题的跑不起来</h4>]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>Shell</tag>
      </tags>
  </entry>
  <entry>
    <title>5 技术</title>
    <url>/2020/06/14/5-%E6%8A%80%E6%9C%AF/</url>
    <content><![CDATA[<p>设计C++软件时，有一些问题会不断重复出现。例如，如何让constructors及nonmember<br>functions像虚函数一样地作用？如何限制class实体个数？如何阻止对象产生于heap内？如何保证对象产生于heap内？如何能够产生某种对象，使它再某些class的member<br>functions被调用时，自动执行某些动作？如何令不同的对象共享同一份数据结构，却让用户错以为每个对象各自有一份数据？如何区分operator[]的读写用途？如何产生一个虚函数，使其行为视多个对象的动态类型而定？  </p>
<h4 id="条款25：将constructor和non-member-functions虚化"><a href="#条款25：将constructor和non-member-functions虚化" class="headerlink" title="条款25：将constructor和non-member functions虚化"></a>条款25：将constructor和non-member functions虚化</h4><ol>
<li><p>当你手上有一个对象的pointer或reference，而你不知道该对象的真正类型是什么的时候，你会调用<font color="red">virtual function</font>（<font color="green">虚函数</font>）以完成因类型而异的行为。当你尚未获得对象，但已经知道需要什么类型的时候，你会调用constructor以构造对象。那么virtual constructors是什么？假如设计一个软件，用来处理时事新闻，内容由文字和图形构成。组织如下：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 抽象基类，时事消息的组件（components），至少一个纯虚函数</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NLComponent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> NLComponent* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> TextBlock* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> TextBlock(*<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Graphic</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Graphic* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Graphic(*<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NewsLetter</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    NewsLetter(istream &amp;str);</span><br><span class="line">    NewsLetter(<span class="keyword">const</span> NewsLetter &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">list</span>&lt;NLComponent*&gt; components;</span><br><span class="line">    <span class="comment">// 从str读取下一个NLComponent的数据，产生组件并返回一个指向它的指针</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> NLComponent* <span class="title">readComponent</span><span class="params">(istream &amp;str)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">NewsLetter::NewsLetter(istream &amp;str)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span> (str)</span><br><span class="line">    &#123;</span><br><span class="line">        components.push_back(readComponent(str));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">NewsLetter::NewsLetter(<span class="keyword">const</span> NewsLetter &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 迭代遍历rhs的list，运用每个元素的virtual copy constructor，将元素复制</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="built_in">list</span>&lt;NLComponent*&gt;::const_iteraotr it = rhs.components.begin();</span><br><span class="line">        it != rhs.components.end();</span><br><span class="line">        ++it)</span><br><span class="line">        <span class="comment">// 调用it当前指向rhs.components的元素，然后调用该元素的clone取得副本加到本对象的list尾端</span></span><br><span class="line">        components.push_back((*it)-&gt;clone());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> <code>readComponent</code>产生了一个崭新的对象，是TextBlock是Graphic视读入的数据而定。由于它产生了新对象，行为仿若constructor，但它能产生不同类型的对象，所以它是一个<font color="red">virtual constructor</font>。<strong>所谓virtual constructor是某种函数，它获得输入，可产生不同类型的对象</strong>。  </p>
</li>
<li><p>有一种特别的virtual constructor——所谓<font color="red">virtual copy constructor</font>。它返回一个指针，指向其调用者的一个新副本。virtual copy constructors通常以copySelf或cloneSelf命名：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NLComponent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> NLComponent* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> :</span> <span class="keyword">public</span> NLCommponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// virtual copy constructor</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> TextBlock* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> TextBlock(*<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Graphic</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// virtual copy constructor</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Graphic* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Graphic(*<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  class的virtual copy constructor只是调用真正的copy constructor而已。真正的copy constructor执行的是<font color="red">浅拷贝</font>（<font color="green">shallow copy</font>），virtual copy constructor一样，如果真正的copy constructor执行的是<font color="red">深复制</font>（<font color="green">deep copy</font>），virtual copy constructor亦然。<br>  上述实现手法是利用“虚函数之返回类型”规则中的一个宽松点，它是晚些才被接纳的规则。<font color="red"><strong>当derived class重新定义其base class的虚函数时，不再需要得声明与其原本相同的返回类型</strong></font>。</p>
<h5 id="将Non-Member-Functions的行为虚化"><a href="#将Non-Member-Functions的行为虚化" class="headerlink" title="将Non-Member Functions的行为虚化"></a>将Non-Member Functions的行为虚化</h5></li>
<li><p>像constructors无法真正被虚化一样，non-member functions也是。让output操作符虚化（operator&lt;&lt;），获得一个ostream&amp;作为其左端自变量，因此它不可能成为member function（其实可以，但会发生）：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NLComponent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream &amp;str) <span class="keyword">const</span> = <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream &amp;str) <span class="keyword">const</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Graphic</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream &amp;str) <span class="keyword">const</span>;</span><br><span class="line">&#125;;</span><br><span class="line">TextBlock t;</span><br><span class="line">Graphic g;</span><br><span class="line">...</span><br><span class="line"><span class="comment">// 此些语法与传统不符，Clients必须把stream对象放在&lt;&lt;左侧</span></span><br><span class="line">t &lt;&lt; <span class="built_in">cout</span>;</span><br><span class="line">g &lt;&lt; <span class="built_in">cout</span>;</span><br></pre></td></tr></table></figure>

<p> 如果我们用虚函数（如print）来作为打印，就跟其他类型对象语法不一致了，很不自然。我们需要的是名为<strong>operator&lt;&lt;的non-member function</strong>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NLComponent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> ostream&amp; <span class="title">print</span><span class="params">(ostream &amp;s)</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TextBlock</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> ostream&amp; <span class="title">print</span><span class="params">(ostream &amp;s)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Graphic</span> :</span> <span class="keyword">public</span> NLComponent</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> ostream&amp; <span class="title">print</span><span class="params">(ostream &amp;s)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">inline</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream &amp;s, <span class="keyword">const</span> NLComponent &amp;s)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> c.print(s);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h4 id="条款26：限制某个class所能产生的对象数量"><a href="#条款26：限制某个class所能产生的对象数量" class="headerlink" title="条款26：限制某个class所能产生的对象数量"></a>条款26：限制某个class所能产生的对象数量</h4><h5 id="允许零个或一个对象"><a href="#允许零个或一个对象" class="headerlink" title="允许零个或一个对象"></a>允许零个或一个对象</h5><ol>
<li><p>没当产生一个对象，会有constructor被调用。阻止某个对象被产出的方法是将constructor声明为private：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PrintJob</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Printer</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">submitJob</span><span class="params">(<span class="keyword">const</span> PrintJob &amp;job)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">reset</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">performSelfTest</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">friend</span> Printer&amp; <span class="title">thePrinter</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Printer();</span><br><span class="line">    Printer(<span class="keyword">const</span> Printer &amp;rhs);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Printer&amp; <span class="title">thePrinter</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Printer p;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PrintJob</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    PrintJob(<span class="keyword">const</span> <span class="built_in">string</span> &amp;whatToPrint);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">string</span> buffer;</span><br><span class="line">..</span><br><span class="line">thePrinter().reset();</span><br><span class="line">thePrinter().submitJob(buffer);</span><br></pre></td></tr></table></figure>

<p> 这个设计由三个成分。第一，Printer class的constructor的属性为private，可以压制对象诞生；第二，全局函数thePrinter被声明为class的一个friend，使thePrinter不受private constructors的约束；第三，thePrinter内含一个static Printer对象，意指只有一个Printer对象被产出。<br> 在此thePrinter的实现代码中，有两个精细的地方值得探讨。<font color="red">第一个细微点</font>是，形成唯一的Printer对象，是<font color="red"><strong>函数中的static对象而非class中的staic对象</strong></font>。“class拥有一个static对象意思是：即使从未被用过，它也会被构造（及析构）。相反”函数拥有一个static对象“指的是，此对象在函数第一次被调用才产生。让打印机成为<font color="red">class static</font>而非一个<font color="red">function static</font>有个缺点，那就是不知道它的初始化时机，而function static的初始化实际是确切知道的。<br> <font color="red">第二个细微点</font>是<font color="red"><strong>函数的”static对象与inlining的互动“</strong></font>。这个函数未被声明为inline。因为声明static意味着只需要唯一一个对象，但对于inline non-member function其中内含local static对象，意味着你这个函数有内部连接（internal linkage）。你的程序可能会拥有多份该static对象的副本。（新版编译期这个问题已经消除）</p>
</li>
</ol>
<h5 id="不同的对象构造状态"><a href="#不同的对象构造状态" class="headerlink" title="不同的对象构造状态"></a>不同的对象构造状态</h5><ol start="2">
<li>或许有人认为使用计数器来限制对象数量更简单。甚至是更一般化，可以使对象的最大数量可以设定为1以外的值。但这策略有问题。当其他对象继承Printer对象或者包含Printer时，就会抛出TooManyObjects exception。除非避免具体类继承其他具体类。问题出在Printer对象可在3种不同状态下生存：（1）<strong>它自己</strong>，（2）<strong>派生物的base class成分</strong>，（3）<strong>内嵌于较大对象之中</strong>。通常你只对上述（1）感兴趣。如果采用原先的策略，很容易达成。因为constructor是private的，如果没有声明任何friend的话，是不能被用来当作base classes的，也不能内嵌于其他对象内。</li>
</ol>
<h5 id="允许对象生生灭灭"><a href="#允许对象生生灭灭" class="headerlink" title="允许对象生生灭灭"></a>允许对象生生灭灭</h5><ol start="3">
<li><p>知道了对象的constructor可于3种情况下被调用，知道了令constructors成为private可以混淆的对象计数。虽然用thePrinter函数封装起来，虽然限制了Printer对象的个数为1，但也限制了每次执行程序只能有唯一一个Printer对象（？？？），因此不能写出一下代码：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">create Printer object p1;</span><br><span class="line">use p1;</span><br><span class="line">destroy p1;</span><br><span class="line">create Printer object p2;</span><br><span class="line">use p2;</span><br><span class="line">destroy p2;</span><br></pre></td></tr></table></figure>

<p> 唯一的做法是将稍早的对象计数（object-counting）码和先前所见的伪构造函数(pseudo-constructors）结合起来：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Printer</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">TooManyObjects</span> &#123;</span>&#125;;</span><br><span class="line">    <span class="comment">// pseudo-constructor</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> Printer* <span class="title">makePrinter</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> Printer* <span class="title">makePrinter</span><span class="params">(<span class="keyword">const</span> Printer &amp;rhs)</span></span>;</span><br><span class="line">    ~Printer();</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">submitJob</span><span class="params">(<span class="keyword">const</span> PrintJob &amp;job)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">reset</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">performSelfTest</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">size_t</span> numObjects;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">size_t</span> Printer::maxObjects = <span class="number">10</span>; <span class="comment">//</span></span><br><span class="line">    Printer();</span><br><span class="line">    <span class="comment">// 不允许拷贝 E27</span></span><br><span class="line">    Printer(<span class="keyword">const</span> Printer &amp;rhs);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// class static</span></span><br><span class="line"><span class="keyword">size_t</span> Printer::numObjects = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">size_t</span> Printer::maxObjects;</span><br><span class="line"></span><br><span class="line">Printer::Printer()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (numObjects &gt;= maxObjects)</span><br><span class="line">        <span class="keyword">throw</span> TooManyObject();</span><br><span class="line">    proceed with normal object construction here;</span><br><span class="line">    ++numObjects;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">PrinterPrinter(<span class="keyword">const</span> Printer &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (numObjects &gt;= maxObejcts)</span><br><span class="line">        <span class="keyword">throw</span> TooManyObjects();</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Printer* Printer::makePrinter()</span><br><span class="line">&#123; <span class="keyword">return</span> <span class="keyword">new</span> Printer; &#125;</span><br><span class="line"></span><br><span class="line">Printer* Printer::makePrinter(<span class="keyword">const</span> Printer &amp;rhs)</span><br><span class="line">&#123; <span class="keyword">return</span> <span class="keyword">new</span> Printer(rhs); &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// old</span></span><br><span class="line">Printer p1; <span class="comment">// error, default ctor is private</span></span><br><span class="line">Printer *p2 = Printer::makePrinter();   <span class="comment">// ok</span></span><br><span class="line">Printer p3 = *p2;   <span class="comment">// error</span></span><br><span class="line">p2-&gt;performSelfTest();  <span class="comment">//</span></span><br><span class="line">p2-&gt;reset();</span><br><span class="line">...</span><br><span class="line"><span class="keyword">delete</span> p2;  <span class="comment">// 如果p2是smart pointer就不用</span></span><br></pre></td></tr></table></figure>

<p> 这就被泛化伪任意个数（不限一个）的对象，它将原本的常量1改为class专属的一个数值。</p>
<h5 id="一个用来计算对象个数的Base-Class"><a href="#一个用来计算对象个数的Base-Class" class="headerlink" title="一个用来计算对象个数的Base Class"></a>一个用来计算对象个数的Base Class</h5></li>
<li><p>我们可以轻易完成一个base class，用来当作对象计数来使用，并让Printer之类的classes继承它。确保计算对象的每个class都有各自的计数器。设计一个class template：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">BeingCounted</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">Counted</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">TooManyObject</span> &#123;</span>&#125;; <span class="comment">// 可能抛出exceptions</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">objectCount</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> numObjects;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Counted();</span><br><span class="line">    Counted(<span class="keyword">const</span> Counted &amp;rhs);</span><br><span class="line">    ~Counted() &#123; --numObjects; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> numObjects;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">size_t</span> maxObjects;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>;    <span class="comment">// 避免ctor重复出现</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">BeingCounted</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">Counted</span>&lt;BeingCounted&gt;:</span>:Counted()</span><br><span class="line">&#123; init(); &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">BeingCounted</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">Counted</span>&lt;BeingCounted&gt;:</span>:Counted(<span class="keyword">const</span> Counted&lt;BeingCounted&gt;&amp;)</span><br><span class="line">&#123; init(); &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">BeingCounted</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">Counted</span>&lt;BeingCounted&gt;:</span>:init()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (numObjects &gt;= maxObjects) <span class="keyword">throw</span> TooManyObjects();</span><br><span class="line">    ++numObjects;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 修改Printer class，让它运用Counted template：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Printer</span> :</span> <span class="keyword">private</span> Counted&lt;Printer&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// pseudo-constructors</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> Printer* <span class="title">makePrinter</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> Printer* <span class="title">makePrinter</span><span class="params">(<span class="keyword">const</span> Printer &amp;rhs)</span></span>;</span><br><span class="line">    ~Printer();</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">submitJob</span><span class="params">(<span class="keyword">const</span> PrintJob &amp;job)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">reset</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">performSelfTest</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">using</span> Counted&lt;Printer&gt;::objectCount;</span><br><span class="line">    <span class="keyword">using</span> Counted&lt;Printer&gt;::TooManyObjects;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Printer();</span><br><span class="line">    Printer(<span class="keyword">const</span> Printer &amp;rhs);ss</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> Counted的大部分作为都隐藏起来不让Printer的用户知道，但用户可能希望知道有多少个Printer对象存在。Counted template提供了objectCount函数，提供信息。然而该函数在Printer中变成了private访问层级，因为上面用的是private inheritance，<strong>为了恢复public访问层级，采用了using表达式</strong>。关于Counted内的statics义务性定义，只要将：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">BeingCounted</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">int</span> <span class="title">Counted</span>&lt;BeingCounted&gt;:</span>:numObjects;  <span class="comment">// 定义并自动初始化为0</span></span><br></pre></td></tr></table></figure>

<p> 放进某个Counted的某个实现文件就可以了。而maxObjects则放在Printer作者的某个实现文件中就可以了：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">size_t</span> Counted&lt;Printer&gt;::maxObjects = <span class="number">10</span>;</span><br></pre></td></tr></table></figure>


</li>
</ol>
<h4 id="条款27：要求（或禁止）对象产生于heap之中"><a href="#条款27：要求（或禁止）对象产生于heap之中" class="headerlink" title="条款27：要求（或禁止）对象产生于heap之中"></a>条款27：要求（或禁止）对象产生于heap之中</h4><h5 id="要求对象产生于heap之中-Heap-Based-Objects"><a href="#要求对象产生于heap之中-Heap-Based-Objects" class="headerlink" title="要求对象产生于heap之中(Heap-Based Objects)"></a>要求对象产生于heap之中(Heap-Based Objects)</h5><ol>
<li><p>阻止clients不得使用new以外的方法产生对象,这很容易办到,只要让那些被隐式调用的构造动作和析构动作不合法就可以了。把constructors和destructor声明为private就太过了，那么只<strong>将destructors成为private</strong>就可以了。而用一个pseudo destructor函数来调用真正的destructor：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UPNumber</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    UPNumber();</span><br><span class="line">    UPNumber(<span class="keyword">int</span> initValue);</span><br><span class="line">    UPNumber(<span class="keyword">double</span> initValue);</span><br><span class="line">    UPNumber(<span class="keyword">const</span> UPNumber &amp;rhs);</span><br><span class="line">    <span class="comment">// pseudo destructor</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">delete</span> <span class="keyword">this</span>; &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    ~UPNumber();</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">UPNumber n; <span class="comment">// error</span></span><br><span class="line">UPNumber *p = <span class="keyword">new</span> UPNumber; <span class="comment">// ok</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">delete</span> p;   <span class="comment">// error</span></span><br><span class="line">p-&gt;destroy();   <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> 把constructors声明为private的缺点是，必须记住每一个constructor都声明为private。只要限制destructor或constructors的运用，就可以阻止non-heap objects的诞生，但是，它妨碍了<strong>继承（inheritance）</strong>和<strong>内含（containment）</strong>。这个困难可以克服，只需要将UPNumber的destructor成为protected就可以解决：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UPNumber</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NonNegativeUPNumber</span> :</span> <span class="keyword">public</span> UPNumber &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Asset</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Asset(<span class="keyword">int</span> initValue);</span><br><span class="line">    ~Asset();</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    UPNumber *value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Asset::Asset(<span class="keyword">int</span> initValue) : value(<span class="keyword">new</span> UPNumber(initValue)) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line">Asset::~Asset()</span><br><span class="line">&#123; value-&gt;destroy(); &#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h5 id="判断某个对象是否位于heap内"><a href="#判断某个对象是否位于heap内" class="headerlink" title="判断某个对象是否位于heap内"></a>判断某个对象是否位于heap内</h5><ol start="2">
<li><p>没有办法侦测出一个construcotr是不是在heap内。或许可以通过设置标志位，但对于operator new[]来说，设置标志位却只有一次机会设立标志信息，而第二次就会exception。或者是利用系统的一个特点：<strong>stack（栈）高地址往低地址成长，heap（堆）由低地址往高地址成长</strong>。但是这是不可移植的。这个函数的观念很有趣。一个临时stack是个局部变量，它会被放在stack的顶端，而stack向低地址成长，所以这个stack的地址一定比任何一个位于stack中的变量（或对象）更低。因此，如果一个位于heap的对象，其地址一定比临时stack的地址更低。但这个想法不完善，很多系统把static对象放置在heap的底下。<br> 如果真实目的只是为了想知道对象调用delete是否安全，那就跟判断对象是否在heap是两回事了：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">void</span> *p = getMemory(size);</span><br><span class="line">    <span class="comment">// add p to the collection of allocated addresses;</span></span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    releaseMemory(ptr); <span class="comment">// free store</span></span><br><span class="line">    <span class="comment">// remove ptr from the collection of allocated addresses;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isSafeToDelete</span><span class="params">(<span class="keyword">const</span> <span class="keyword">void</span> *address)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// return whether address is in collection of allocated addresses</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> operator new负责把一些条目（entires）加到一个由“动态分配而得的地址”所形成的集合中，operator delete负责把这些条目移除；isSafeToDelete负责查找集合，看看地址是否在其中。这些operator new和operator delete函数都在全局范围内，这应该堆所有类型都管用（甚至内置类型）。但这不是我们想要的——当对象存在多重继承或虚拟继承时，可能拥有多地址，这就是<font color="red">非自然多态</font>（<font color="green">unnatural polymorphism</font>）。<br> 我们需要的是一个提供函数机能，但不符带全局命名空间的污染问题、额外的义务性负担，以及正确性的疑惑。那么<font color="green">abstract mixin base class</font>（<font color="red">抽象混合式基类</font>）可以满足需求。抽象基类不能被实例化，因为它至少有一个纯虚函数。mix in class则提供了一组定义好的能力，能与derived class兼容：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HeapTracked</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">class <span class="title">MissingAddress</span><span class="params">()</span></span>; <span class="comment">// exception class</span></span><br><span class="line">    <span class="keyword">virtual</span> ~HeapTracked() = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> size)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isOnHeap</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">const</span> <span class="keyword">void</span> *RawAddress;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">list</span>&lt;RawAddress&gt; addresses;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// static class member的义务性定义</span></span><br><span class="line"><span class="built_in">list</span>&lt;RawAddress&gt; HeapTracked::addresses;</span><br><span class="line"></span><br><span class="line">HeapTracked::~HeapTracked() &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span>* HeapTracked::<span class="function"><span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">void</span> *memPtr = ::<span class="keyword">operator</span> <span class="keyword">new</span>(size);    <span class="comment">// 取得内存</span></span><br><span class="line">    addresses.push_front(memPtr); <span class="comment">// 将地址放到list头部</span></span><br><span class="line">    <span class="keyword">return</span> memPtr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> HeapTracked::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span>;</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">list</span>&lt;RawAddress&gt;::iterator it = find(address.begin(), address.end(), ptr);</span><br><span class="line">    <span class="keyword">if</span> (it != address.end()) &#123;</span><br><span class="line">        addresses.erase(it);</span><br><span class="line">        ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(ptr)</span></span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">throw</span> MissingAddress();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">bool</span> HeapTracked::isOnHeap() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">void</span> *rawAddress = <span class="keyword">dynamic_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">void</span>*&gt;(<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">list</span>&lt;RawAddress&gt;::iterator it = find(address.begin(), address.end(), rawAddress);</span><br><span class="line">    <span class="keyword">return</span> it != addresses.end();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Asset</span> :</span> <span class="keyword">public</span> HeapTracked</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    UPNumber value;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">inventoryAsset</span><span class="params">(<span class="keyword">const</span> Asset *ap)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (ap-&gt;isOnHeap())</span><br><span class="line">        ap is a heap-based asset -- inventory it as such;</span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">        ssap is a non-heap-based asset -- record it that way;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 凡涉及“多重或虚拟基类”的对象，会拥有多个地址，如果写在全局函数就会很复杂，但是isOnHeap只施行于HeapTracked对象身上，所以只要简单的将指针<strong>dynamic_cast</strong>为<strong>void*</strong>（或<strong>const void*</strong>或<strong>volatile void*</strong>或<strong>const volatile void*</strong>），就会获得一个指针，指向原指针所指对象的内存起始处。不过dynamic_cast只适用于有至少一个虚函数的指针身上。之所以前面的isSafeToDelete撰写很复杂，就是因为它可以对任何类型起作用，因此dynamic_cast无法帮助它。isOnHeap有所选择（只针对HeapTracked对象的指针）。这个技术有移植性，只要编译器支持dynamic_cast。</p>
<h5 id="禁止对象产生于heap之中"><a href="#禁止对象产生于heap之中" class="headerlink" title="禁止对象产生于heap之中"></a>禁止对象产生于heap之中</h5></li>
<li><p>一般有3种情况：</p>
<ul>
<li>对象被直接实例化</li>
<li>对象被实例化为derived class objects内的base class成分</li>
<li>对象被内嵌于其他对象之中。<br>阻止clients直接将对象实例化于heap之中，很容易，只需要将operator new和operator delete声明为private就可以了，如果想禁止对象所组成的数组，可以将operator new[]和operator delete声明为private。</li>
</ul>
</li>
</ol>
<h4 id="条款28：Smart-Pointers（智能指针）"><a href="#条款28：Smart-Pointers（智能指针）" class="headerlink" title="条款28：Smart Pointers（智能指针）"></a>条款28：Smart Pointers（智能指针）</h4><ol>
<li><p>Smart poinnters是一种像内建指针，却提供了更多机能的对象。当以smart pointers取代C++的内建指针（<strong>dumb pointer</strong>），你会获得各种指针行为的控制权：</p>
<ul>
<li><font color="red">构造和析构</font>（<font color="green">Construction and Destruction</font>）。你可以决定smart pointer被产生以及被构造时发生什么事。通常给smart pointers一个默认值0，以避免指针未获初始化。</li>
<li><font color="red">复制和复制</font>（<font color="green">Copying and Assignment</font>）。当一个smart pointer被复制或涉及复制动作时，可以控制发生什么事。</li>
<li><font color="red">解引</font>（<font color="green">Dereferencing</font>）。当client解引（取用）smart pointer所指的对象时，有权决定发生什么事情。<br>Smart pointers由templates产生出来。由于像内建指针一样，所以它必须由强烈的<font color="red">类型性</font>（<font color="green">strongly teyped</font>）。<h5 id="Smart-Pointers的构造、复制、析构"><a href="#Smart-Pointers的构造、复制、析构" class="headerlink" title="Smart Pointers的构造、复制、析构"></a>Smart Pointers的构造、复制、析构</h5></li>
</ul>
</li>
<li><p>auto_ptr template可能实现如下：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">auto_ptr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    auto_tpr(T *ptr = <span class="number">0</span>) : pointee(ptr) &#123;&#125;</span><br><span class="line">    ~<span class="built_in">auto_ptr</span>() &#123; <span class="keyword">delete</span> pointee; &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T *pointee;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 在同一个对象只可被一个auto_ptr拥有的前提下，上述做法有效运转。但一旦auto_ptr被复制或被赋值，会发生什么？会导致两个auto_ptrs指向同一对象。这当在销毁对象时，可能会删除两次，往往会导致未定义。<br> 另一个做法是<strong>以new操作符为所指对象产生一个新副本</strong>。而auto_ptr<t>不得指向一个类型为T的对象，可以指向一个T派生类型的对象。auto_ptr采用了一个富弹性的解法：当auto_ptr被复制或被赋值，其对象拥有权会转移：</t></p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">auto_ptr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="built_in">auto_ptr</span>(<span class="built_in">auto_ptr</span>&lt;T&gt; &amp;rhs); <span class="comment">// copy constructor</span></span><br><span class="line">    <span class="built_in">auto_ptr</span>&lt;T&gt;&amp; <span class="keyword">operator</span>=(<span class="built_in">auto_ptr</span>&lt;T&gt; &amp;rhs);  <span class="comment">// assignment operator</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">auto_ptr</span>&lt;T&gt;:</span>:<span class="built_in">auto_ptr</span>(<span class="built_in">auto_ptr</span>&lt;T&gt; &amp;rhs);</span><br><span class="line">&#123;</span><br><span class="line">    pointee = rhs.pointee;</span><br><span class="line">    rhs.pointee = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">auto_ptr</span>&lt;T&gt;&amp; <span class="title">auto_ptr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>=(<span class="built_in">auto_ptr</span>&lt;T&gt; &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span> == &amp;rhs);</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    <span class="keyword">delete</span> pointee; <span class="comment">// 删除原有的对象</span></span><br><span class="line">    pointee = rhs.pointee;</span><br><span class="line">    rhs.pointee = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 由于auto_ptr的copy constructor被调用时，对象拥有权转移了，所以以by value方式转递auto_ptrs往往是个糟糕的主意：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printTreeNode</span><span class="params">(ostream &amp;s, <span class="built_in">auto_ptr</span>&lt;TreeNode&gt; p)</span></span></span><br><span class="line"><span class="function"></span>&#123; s &lt;&lt; *p; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">auto_ptr</span>&lt;TreeNode&gt; ptn(ew TreeNode);</span><br><span class="line">    ...</span><br><span class="line">    printTreeNode(<span class="built_in">cout</span>, ptn);   <span class="comment">// 以by value传递</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 当printTreeNode的参数被p初始化（通过auto_ptr的copy constructor），ptn所指的对象拥有权被转移至p。当printTreeNode结束，p离开生存空间，destructor被调用。然而ptn不再指向任何东西，这将产生未定义行为。所以Pass-by-reference-to-const才是适当的途径：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printTreeNode</span><span class="params">(ostream &amp;s, <span class="keyword">const</span> <span class="built_in">auto_ptr</span>&lt;TreeNode&gt; &amp;p)</span></span></span><br><span class="line"><span class="function"></span>&#123; s &lt;&lt; *p; &#125;</span><br></pre></td></tr></table></figure>

<p> 在此函数中，p是个referene而不是对象。所以不会由constructor被用来为p设定初值，ptn将保留拥有权。</p>
<h5 id="实现Dereferencing-Operators（解引操作符）"><a href="#实现Dereferencing-Operators（解引操作符）" class="headerlink" title="实现Dereferencing Operators（解引操作符）"></a>实现Dereferencing Operators（解引操作符）</h5></li>
<li><p>现在注意力放在smart pointers的核心：operator*和operator-&gt;函数身上：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">T</span>&amp; <span class="title">SmartPtr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>*() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    perform <span class="string">"smart pointer"</span> processing;</span><br><span class="line">    <span class="keyword">return</span> *pointee;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这个函数首先做任何必要的初始化动作或是让pointee获得有效值的任何动作。<br> 检验operator-&gt;前，看一下此函数的不寻常意义：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 产生smart ptrs，用来指向分布式数据库（DB）内的对象</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">DBPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    DBPtr(T *realPtr = <span class="number">0</span>);</span><br><span class="line">    DBPtr(DataBaseID id);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用来表现数据库中的一笔数据（tuples）</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tuple</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">displayEditDialog</span><span class="params">()</span></span>;   <span class="comment">// 呈现图形式对话框，供输入tuple</span></span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isValid</span><span class="params">()</span> <span class="keyword">const</span></span>;   <span class="comment">// 检验*this是否有效</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// class template，用来在T对象被修改时，完成运转记录（log entires）。</span></span><br><span class="line"><span class="keyword">template</span>&lt;classs T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LogEntry</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    LogEntry(<span class="keyword">const</span> T &amp;objectToBeModified);</span><br><span class="line">    ~LogEntry();</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">editTuple</span><span class="params">(DBPtr&lt;Tuple &amp;pt)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LogEntry&lt;Tuple&gt; entry(*pt);</span><br><span class="line">    <span class="comment">// 反复显示编辑对话框，知道获得有效值为止</span></span><br><span class="line">    <span class="keyword">do</span> pt-&gt;displayEditDialog();</span><br><span class="line">    <span class="keyword">while</span> (pt-&gt;isValid() == <span class="literal">false</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 其中<code>pt-&gt;displayEditDialog();</code>会被编译器解释为：<code>(pt.operator-&gt;())-&gt;displayEditDialog();</code>这个意味着，<strong>不论operator-&gt;返回什么，在该回传值身上施行-&gt;操作符都是合法的</strong>。因此operator-&gt;只能返回两个东西：一个dumb pointer或是一个smart pointer：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">T</span>* <span class="title">SmartPtr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>-&gt;() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    perform <span class="string">"smart pointer"</span> processing;</span><br><span class="line">    <span class="keyword">return</span> pointee;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h5 id="测试Smart-Pointers是否为NULL"><a href="#测试Smart-Pointers是否为NULL" class="headerlink" title="测试Smart Pointers是否为NULL"></a>测试Smart Pointers是否为NULL</h5><ol start="4">
<li><p>我们可以产生、销毁、赋值、复制、解引smart pointers。但有一件事没办法做，就是判断是否为NULL：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">SmartPtr&lt;TreeNode&gt; ptn;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (ptn == <span class="number">0</span>) ...   <span class="comment">// error</span></span><br><span class="line"><span class="keyword">if</span> (ptn) ...        <span class="comment">// error</span></span><br><span class="line"><span class="keyword">if</span> (!ptn) ...       <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 为我们smart pointer class添加一个isNULL很容易，但不自然。另一个做法是，提供一个隐式转换操作符，允许上述测试动作通过编译。<font color="red">这个转换的传统目标是void*</font>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">SmartPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">operator</span> <span class="keyword">void</span>*();   <span class="comment">// 如果dumb ptr是null，返回0，否则是非0值</span></span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">SmartPtr&lt;TreeNode&gt; ptn;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (ptn == <span class="number">0</span>) ...   <span class="comment">// ok</span></span><br><span class="line"><span class="keyword">if</span> (ptn) ...        <span class="comment">// ok</span></span><br><span class="line"><span class="keyword">if</span> (!ptn) ...       <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> <strong>这个做法的缺点是，它竟然可以用来对完全不同类型的对象比较</strong>（我好像没出现这样的情况……）！这个问题无法解决，但有个差强人意的做法，允许提供测试nullness的合理语法，并能够将意外引起不同类型之smart pointers相互比较的机会降到最低，那就是将!操作符重载，并在其调用者是null的情况下，返回true：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">SmartPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">bool</span> <span class="keyword">operator</span>!() <span class="keyword">const</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">SmartPtr&lt;TreeNode&gt; ptn;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (!ptn) ...   <span class="comment">// ok ptn is null</span></span><br><span class="line"><span class="keyword">else</span> ...        <span class="comment">// ptn is not null</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (ptn == <span class="number">0</span>) ...   <span class="comment">// error</span></span><br><span class="line"><span class="keyword">if</span> (ptn) ...        <span class="comment">// error</span></span><br><span class="line"></span><br><span class="line">SmartPtr&lt;Apple&gt; pa;</span><br><span class="line">SmartPtr&lt;Orange&gt; po;    </span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (!pa == !po) ... <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> 这个有风险的做法的原因是很少有程序员这么个写法。在C++标准程序库中，隐式转换为void*已被隐式转换为bool取代，而operator bool总是返回operator!的反相。</p>
<h5 id="将Smart-Pointers转换为Dumb-Pointers"><a href="#将Smart-Pointers转换为Dumb-Pointers" class="headerlink" title="将Smart Pointers转换为Dumb Pointers"></a>将Smart Pointers转换为Dumb Pointers</h5></li>
<li><p>有时候你希望将smart pointers加入已使用的dumb pointers应用软件中，</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tuple</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">normalize</span><span class="params">(Tuple *pt)</span></span>;  <span class="comment">// by dumb pointer</span></span><br><span class="line"></span><br><span class="line">DBPtr&lt;Tuple&gt; pt;</span><br><span class="line">...</span><br><span class="line">normaize(pt);   <span class="comment">// error</span></span><br><span class="line">normalize(&amp;*pt);<span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> 那个转换动作很难看，如果为smart pointer-to-T template加上隐式类型转换操作符，使之转换为<font color="red">dumb pointer-to-T</font>，先前的调用就能成功。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">DBPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">operator</span> T*() &#123; <span class="keyword">return</span> pointee; &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">DBPtr&lt;Tuple&gt; pt;</span><br><span class="line">...</span><br><span class="line">normalize(pt);  <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

<p> 上述函数一加上，nullness测试问题也一并解决了。不过这样的转换也有阴暗面，它式clients得以轻易地直接对dumb pointers做动作，因而回避了smart pointer的最初目的。允许直接适用dumb pointers有灾难，它导致class的计数簿记工作方面的错误。造成引用计数所用的数据结构崩溃。<br> 即使提供了一个隐式转换操作符，smart pointer还是无法完全取代dumb pointer。因为从smart pointer转换为dumb pointer是一种用户定制的转换行为，而<font color="red"><strong>编译器禁止一次施行一次以上这类转换</strong></font>。举个例子，有个class，考虑一个函数用来整合两个TupleAccessors对象的信息：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TupleAccessors</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    TupleAccessors(<span class="keyword">const</span> Tuple *pt);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">TupleAccessors <span class="title">merge</span><span class="params">(<span class="keyword">const</span> TupleAccessors &amp;ta1, <span class="keyword">const</span> TupleAccessors &amp;ta2)</span></span>;</span><br><span class="line"></span><br><span class="line">Tuple *pt1, *pt2;</span><br><span class="line">merge(pt1, pt2);    <span class="comment">// ok</span></span><br><span class="line"></span><br><span class="line">DBPtr&lt;Tuple&gt; pt1, pt2;</span><br><span class="line">merge(pt1, pt2);    <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 这是因为从DBPtr<tuple>转换为TupleAccessors需要两个用户定制转换，而着C++不允许，所以第一个将DBPtr<tuple>转换为Tuple*，而第二个将需要将Tuple*转环为TupleAccessors）。所以不要提供对dumb pointers的隐式转换操作符，除非不得已。</tuple></tuple></p>
<h5 id="Smart-Pointers和与继承有关的类型转换"><a href="#Smart-Pointers和与继承有关的类型转换" class="headerlink" title="Smart Pointers和与继承有关的类型转换"></a>Smart Pointers和与继承有关的类型转换</h5></li>
<li><p>假设有一个public inheritance继承体系，构建出消费性音乐产品：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MusicProduct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    MusicProduct(<span class="keyword">const</span> <span class="built_in">string</span> &amp;title);</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">play</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">displayTitle</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Cassette</span> :</span> <span class="keyword">public</span> MusicProduct</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Cassette(<span class="keyword">const</span> <span class="built_in">string</span> &amp;title);</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">play</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">displayTitle</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CD</span> :</span> <span class="keyword">public</span> MusicProduct</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    CD(<span class="keyword">const</span> <span class="built_in">string</span> &amp;title);</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">play</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">displalyTitle</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">displayAndPlay</span><span class="params">(<span class="keyword">const</span> MusicProduct *pmp, <span class="keyword">int</span> numTimes)</span></span>;</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= numTimes; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        pmp-&gt;displayTitle();</span><br><span class="line">        pmp-&gt;play();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// dumb pointers的方式没问题，smart pointer取代之则另一种情况了</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">displayAndPlay</span><span class="params">(<span class="keyword">const</span> SmartPtr&lt;MusicProduct&gt; &amp;pmp, <span class="keyword">int</span> numTimes)</span></span>;</span><br><span class="line">SmartPtr&lt;Cassette&gt; funMusic(<span class="keyword">new</span> Cassette(<span class="string">"Alapalooza"</span>));</span><br><span class="line">SmartPtr&lt;CD&gt; nightmareMusic(<span class="keyword">new</span> CD(<span class="string">"Disco Hits of the 70s"</span>));</span><br><span class="line">displayAndPlay(funMusic, <span class="number">10</span>);       <span class="comment">// error</span></span><br><span class="line">displayAndPlay(nightmareMusic, <span class="number">0</span>);  <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 之所以无法通过编译，是因为编译器所看见的是3个互不相干的classs。有个办法可以绕弯解除这一束缚：<strong>令每个smart pointer class有个隐式类型转换操作符，用来转换至另一个smart pointer class</strong>：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmartPtr</span>&lt;Cassette&gt;</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">operator</span> SmartPtr&lt;MusicProduct&gt;()</span><br><span class="line">    &#123; <span class="keyword">return</span> SmartPtr&lt;MusicProduct&gt;(pointee); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Cassette *pointee;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmartPtr</span>&lt;CD&gt;</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">operator</span> SmartPtr&lt;MusicProduct&gt;()</span><br><span class="line">    &#123; <span class="keyword">return</span> SmartPtr&lt;MusicProduct&gt;(pointee); &#125;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    CD *pointee;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这个做法有两个缺点。第一，必须为每一个“SmartPtr class实例”加入上述例子；第二，需要加上很多这样的转换操作符，因为所指的对象位于继承体系的底层，必须为对象直接继承或间接继承的每个base class提供一个转换函数。由于编译器禁止一次执行一个以上的用户定制类型转换函数，所以无法将<font color="red">smart pointer-to-T</font>转换为一个<font color="red">smart pointer-to-indirect-base-class-of-T</font>。刚好有个语言扩充性质，它可以将nonvirtual member function声明为template，可以用它来产生smart pointer的转换函数：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// template class，用于smart pointer-to-T对象</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">SmartPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    SmartPtr(T *realPtr = <span class="number">0</span>);</span><br><span class="line">    T* <span class="keyword">operator</span>-&gt;() <span class="keyword">const</span>;</span><br><span class="line">    T&amp; <span class="keyword">operator</span>*() <span class="keyword">const</span>;</span><br><span class="line">    <span class="comment">// template function，用于隐式转换操作符</span></span><br><span class="line">    <span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">newType</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">operator</span> <span class="title">SmartPtr</span>&lt;newType&gt;()</span></span><br><span class="line"><span class="class">    &#123;</span> <span class="keyword">return</span> SmartPtr&lt;newType&gt;(pointee); &#125;</span><br><span class="line">    ,,,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> funMusic对象属于SmartPtr<cassette>类型，而displayAndPlay函数期望得到一个SmartPtr<musicproduct>对象。编译器发现类型不吻合，于是企图将funMusic转换其期望的对象。编译器在SmartPtr<musicproduct> class内企图找一个”单一变量之construcotr“，其自变量类型为SmartPtr<cassette>，但没有找到；于是企图在SmartPtr<cassette> class内找一个隐式类型转换操作符，希望产出已给SmartPtr<musicproduct> class，但也失败了；接下来再试图寻找一个”可实例化以导出合宜转换函数“的member function template。它在SmartPtr<cassette>找到一个东西，当它被实例化并令newType绑定MusicProduct时，编译器将之实例化，可得：</cassette></musicproduct></cassette></cassette></musicproduct></musicproduct></cassette></p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">SmartPtr&lt;Cassette&gt;::<span class="keyword">operator</span> SmartPtr&lt;MusicProduct&gt;() </span><br><span class="line">&#123; <span class="keyword">return</span> SmartPTr&lt;MusicProduct&gt;(pointee); &#125;</span><br></pre></td></tr></table></figure>

<p> 这其中涵盖的技术不简单，它包括：（1）函数调用的自变量匹配规则、（2）隐式类型转换函数、（3）template functions的暗自实例化、（4）member function templates等技术。</p>
<h6 id="Smart-Pointers与const"><a href="#Smart-Pointers与const" class="headerlink" title="Smart Pointers与const"></a>Smart Pointers与const</h6></li>
<li><p>const可以修饰被指的东西</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">CD <span class="title">goodCD</span><span class="params">(<span class="string">"Flood"</span>)</span></span>;</span><br><span class="line"><span class="keyword">const</span> CD *p;    <span class="comment">// p时non-const指针，指向const CD object</span></span><br><span class="line">CD *<span class="keyword">const</span> p = &amp;goodCD;  <span class="comment">// p是哟个const指针，指向non-const CD object，必须有初值</span></span><br><span class="line"><span class="keyword">const</span> CD *<span class="keyword">const</span> p = &amp;goodCD;    <span class="comment">//p是一个const指针，指向一个const CD object</span></span><br></pre></td></tr></table></figure>

<p> 很自然，我们想要smart pointers也有同样的弹性。但Smart pointer只能有一个地方放置const，只能施行与指针身上，不能及于所指对象，不过我们可以对const以及non-const的对象及指针，产生4种组合：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">SmartPtr&lt;CD&gt; p;</span><br><span class="line">Smart&lt;<span class="keyword">const</span> CD&gt; p;</span><br><span class="line"><span class="keyword">const</span> SmartPtr&lt;CD&gt; p = &amp;goodCD;</span><br><span class="line"><span class="keyword">const</span> SmartPtr&lt;<span class="keyword">const</span> CD&gt; p = &amp;goodCD;</span><br></pre></td></tr></table></figure>

<p> 如果适用dumb pionters，可以non-const指针作为const指针的初值，也可以指向non-const独享的指针作为指向const对象指针的初始值，赋值规则类似：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">CD *pCD = <span class="keyword">new</span> CD(<span class="string">"Famous Movie Themes"</span>);</span><br><span class="line"><span class="keyword">const</span> CD *pConstCD = pCD;   <span class="comment">// ok</span></span><br><span class="line">SmartPtr&lt;CD&gt; pCD = <span class="keyword">new</span> CD(<span class="string">"Famous Movie Themes"</span>);</span><br><span class="line">SmartPtr&lt;<span class="keyword">const</span> CD&gt; pConstCD = pCD;  <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p> 类型转换如果涉及const，便是一条单行道：从non-const到const是安全的，从const到non-const是不安全的。解决上面的方法是，将derived class object转换为base class object，令smart pointer-to-T class公开继承一个对应的smart pointer-to-const-T class：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">SmartPtrToConst</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ...</span><br><span class="line">    functions</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">union</span>(</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">const</span> T *constPointee;</span><br><span class="line">        T *pointee;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;class T&gt;</span><br><span class="line">class SmartPtr : <span class="keyword">public</span> SmartPtrToConst&lt;T&gt;</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 运用这个设计，我们获得了自己希望的行为：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">SmartPtr&lt;CD&gt; pCD = <span class="keyword">new</span> CD(<span class="string">"Famous Movie Themes"</span>);</span><br><span class="line">SmartPtrToConst&lt;CD&gt; pConstCD = pCD; <span class="comment">// ok</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款29：Reference-counting（引用计数）"><a href="#条款29：Reference-counting（引用计数）" class="headerlink" title="条款29：Reference counting（引用计数）"></a>条款29：Reference counting（引用计数）</h4><ol>
<li><p>Reference counting这项技术，允许多个等值对象共享同一个实值。Reference counting可以消除记录对象拥有权的负荷，因为当对象用reference counting技术，它拥有它自己，一旦没人使用它，便自动销毁自己。因此，reference counting建构出<font color="red">垃圾回收机制</font>（<font color="green">garbage colection</font>）的一个简单形式。</p>
<h5 id="Reference-Counting（引用计数）的实现"><a href="#Reference-Counting（引用计数）的实现" class="headerlink" title="Reference Counting（引用计数）的实现"></a>Reference Counting（引用计数）的实现</h5></li>
<li><p>基本设计像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">StringValue</span> &#123;</span> ... &#125;; <span class="comment">// 包含引用计数和字符串值</span></span><br><span class="line">    StringValue *value; <span class="comment">// value of this String</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 内嵌的结构体StringValue主要用于存储引用计数和字符串值,并使得引用计数和字符串值相关联.StringValue的实现像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">StringValue</span> &#123;</span></span><br><span class="line">        <span class="keyword">int</span> refCount;</span><br><span class="line">        <span class="keyword">char</span> *data;</span><br><span class="line">        StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue);</span><br><span class="line">        ~StringValue();</span><br><span class="line">    &#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue) : refCount(<span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    data = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="built_in">strlen</span>(initValue) + <span class="number">1</span>];</span><br><span class="line">    <span class="built_in">strcpy</span>(data, initValue);</span><br><span class="line">&#125;</span><br><span class="line">String::StringValue::~StringValue()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> [] data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> StringValue只对String类可见,而对客户不可见,接口由String定义并提供给客户。String的构造函数:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String(<span class="keyword">const</span> <span class="keyword">char</span> *initValue = <span class="string">""</span>);</span><br><span class="line">String(<span class="keyword">const</span> String&amp; rhs);</span><br></pre></td></tr></table></figure>

<p>  第一个构造函数的实现较简单,根据传入的char数组构造StringValue对象,然后使String中的指针指向这个String即可:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String::String(<span class="keyword">const</span> <span class="keyword">char</span> *initValue): value(<span class="keyword">new</span> StringValue(initValue))&#123;&#125;</span><br></pre></td></tr></table></figure>

<p> 但这样的实现导致”分开构造,但拥有相同初值的String对象,并不共享同一个数据结构”,因此像这样的代码:   </p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">String <span class="title">s1</span><span class="params">(<span class="string">"More Effective C++"</span>)</span></span>;</span><br><span class="line"><span class="function">String <span class="title">s2</span><span class="params">(<span class="string">"More Effective C++"</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 尽管是s1和s2的值相同,但它们却并不共享同一个块内存,而是各自拥有独立内存。<br> 拷贝构造函数可以使用引用计数,并共享内存,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String::String(<span class="keyword">const</span> String&amp; rhs): value(rhs.value)&#123; ++value-&gt;refCount; &#125;</span><br></pre></td></tr></table></figure>

<p> 析构函数负责在引用计数为0的时候撤销内存:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String::~String()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (--value-&gt;refCount == <span class="number">0</span>) </span><br><span class="line">        <span class="keyword">delete</span> value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 赋值操作符要注意自身赋值的情况:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String&amp; String::<span class="keyword">operator</span>=(<span class="keyword">const</span> String&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (value == rhs.value) &#123; <span class="comment">//处理自身赋值</span></span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>; </span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">if</span> (--value-&gt;refCount == <span class="number">0</span>) &#123; </span><br><span class="line">        <span class="keyword">delete</span> value; </span><br><span class="line">    &#125;</span><br><span class="line">    value = rhs.value; </span><br><span class="line">    ++value-&gt;refCount;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h5 id="Copy-on-Write-写时才复制"><a href="#Copy-on-Write-写时才复制" class="headerlink" title="Copy-on-Write(写时才复制)"></a>Copy-on-Write(写时才复制)</h5><ol start="3">
<li><p>对operator[]的重载比较复杂:const版本是只读动作,因而只返回指定字符即可,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>&amp; String::<span class="keyword">operator</span>[](<span class="keyword">int</span> index) <span class="keyword">const</span>&#123; <span class="keyword">return</span> value-&gt;data[index]; &#125;</span><br></pre></td></tr></table></figure>

<p> 但non-const版本面临着被写入新的值的可能,由于对当前String的修改不应影响到共享内存的其他String对象,因此需要先为当前String分配独立内存并将原值进行拷贝,像这样:</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">char&amp; String::operator[](int index)</span><br><span class="line">&#123;</span><br><span class="line">    if (value-&gt;refCount &gt; 1) &#123;</span><br><span class="line">        --value-&gt;refCount; </span><br><span class="line">        value =new StringValue(value-&gt;data); </span><br><span class="line">    &#125;</span><br><span class="line">    return value-&gt;data[index];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 不仅是operator[],其他可能改变String对象的操作也应该采取和non-cons版本operator[]相同的动作.这其实是lazy evaluation的一种应用.</p>
<h5 id="Pointers-References-以及Copy-on-Write"><a href="#Pointers-References-以及Copy-on-Write" class="headerlink" title="Pointers,References,以及Copy-on-Write"></a>Pointers,References,以及Copy-on-Write</h5></li>
<li><p>3中对operator[]的重载解释并解决了可能的写操作篡改共享内存的问题,但是却无法阻止外部指针或引用对共享内存的篡改,像这样:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String s1 = <span class="string">"Hello"</span>;</span><br><span class="line"><span class="keyword">char</span> *p = &amp;s1[<span class="number">1</span>];</span><br><span class="line">String s2 = s1;</span><br></pre></td></tr></table></figure>

<p>对p所指向的内存的任何写操作都会同时更改s1和s2的值,但是s1却对此一无所知,因为p和s1没有内在联系.解决办法并不难:为每一个StringValue对象加一个标志(flag)变量,用表示是否可以被共享,开始时先将flag设为true(可以共享),一旦non-const operator[]被调用就将该flag设为false,并可能永远不许再更改(除非重新为StringValue分配更大内存而导致指针失效),StringValue的修改版像这样:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">StringValue</span> &#123;</span></span><br><span class="line">        <span class="keyword">int</span> refCount;</span><br><span class="line">        <span class="keyword">bool</span> shareable; </span><br><span class="line">        <span class="keyword">char</span> *data;</span><br><span class="line">        StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue);</span><br><span class="line">        ~StringValue();</span><br><span class="line">    &#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue): refCount(<span class="number">1</span>),shareable(<span class="literal">true</span>) </span><br><span class="line">&#123;</span><br><span class="line">    data = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="built_in">strlen</span>(initValue) + <span class="number">1</span>];</span><br><span class="line">    <span class="built_in">strcpy</span>(data, initValue);</span><br><span class="line">&#125;</span><br><span class="line">String::StringValue::~StringValue()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> [] data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>String的member function在企图使用共享内存前,就必须测试内存是否允许被共享:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String::String(<span class="keyword">const</span> String&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (rhs.value-&gt;shareable) &#123;</span><br><span class="line">        value = rhs.value;</span><br><span class="line">        ++value-&gt;refCount;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        value = <span class="keyword">new</span> StringValue(rhs.value-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其他返回引用的member function(对于String只有operator[])都涉及到对flag的修改,而其他可能需要共享内存的member function都涉及到对flag的检测。  </p>
<p>条款30的proxy class技术可以将operator[]的读和写用途加以区分,从而降低”需被标记为不可共享”之StringValue对象的个数。</p>
<h5 id="一个Reference-Counting-引用计数-基类"><a href="#一个Reference-Counting-引用计数-基类" class="headerlink" title="一个Reference-Counting(引用计数)基类"></a>一个Reference-Counting(引用计数)基类</h5></li>
<li><p>任何要支持内存共享的class都可以使用reference-counting,因此可以考虑把它抽象为一个类,任何需要reference-counting功能的class只要使用这个类即可。  </p>
<p> 第一步就是产生一个base class RCObject,执行引用计数的功能并标记对象是否可被共享,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RCObject</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    RCObject();</span><br><span class="line">    RCObject(<span class="keyword">const</span> RCObject&amp; rhs);</span><br><span class="line">    RCObject&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> RCObject&amp; rhs);</span><br><span class="line">    <span class="keyword">virtual</span> ~RCObject() = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">addReference</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">removeReference</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">markUnshareable</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isShareable</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isShared</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> refCount;</span><br><span class="line">    <span class="keyword">bool</span> shareable;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 由于RCObject的作用只是实现引用计数的辅助功能,然后让StringValue继承它,因此StringValue被设为一个抽象基类——通过将析构函数设为纯虚函数,但仍需要为析构函数提供定义.RCObject的实现像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">RCObject::RCObject(): refCount(<span class="number">0</span>), shareable(<span class="literal">true</span>) &#123;&#125;</span><br><span class="line">RCObject::RCObject(<span class="keyword">const</span> RCObject&amp;):refCount(<span class="number">0</span>),shareable(<span class="literal">true</span>) &#123;&#125;</span><br><span class="line">RCObject&amp; RCObject::<span class="keyword">operator</span>=(<span class="keyword">const</span> RCObject&amp;)</span><br><span class="line">&#123; <span class="keyword">return</span> *<span class="keyword">this</span>; &#125;</span><br><span class="line">RCObject::~RCObject() &#123;&#125; <span class="comment">// virtual dtors must always</span></span><br><span class="line"><span class="keyword">void</span> RCObject::addReference() &#123; ++refCount; &#125;</span><br><span class="line"><span class="keyword">void</span> RCObject::removeReference()</span><br><span class="line">&#123; <span class="keyword">if</span> (--refCount == <span class="number">0</span>) <span class="keyword">delete</span> <span class="keyword">this</span>; &#125;</span><br><span class="line"><span class="keyword">void</span> RCObject::markUnshareable()</span><br><span class="line">&#123; shareable = <span class="literal">false</span>; &#125;</span><br><span class="line"><span class="keyword">bool</span> RCObject::isShareable() <span class="keyword">const</span></span><br><span class="line">&#123; <span class="keyword">return</span> shareable; &#125;</span><br><span class="line"><span class="keyword">bool</span> RCObject::isShared() <span class="keyword">const</span></span><br><span class="line">&#123; <span class="keyword">return</span> refCount &gt; <span class="number">1</span>; &#125;</span><br></pre></td></tr></table></figure>

<p> RCObject的实现非常简单,但是其拷贝构造函数和赋值操作符有些特殊——它们的参数没有名字,也就是说参数没有作用,其拷贝构造函数和赋值操作符都只是形式上的:  </p>
<p> RCObjetc拷贝构造函数与RCObject的作用相对应——RCObject一旦被构造,就说明一个新的对象被产生出来,那么RCObject对象本身的初始值和默认构造函数相同,至于refCount设为0而不是1,这要求对象创建者自行将refCount设为1.  </p>
<p> RCObject的赋值操作符什么也不做,仅仅返回*this,因为它不应该被调用,正如之前的StringValue,如果对String对象赋值,那么或者StringValue被共享,或者拷贝构造一个新的StringValue,实际上StringValue的赋值操作永远不会被调用.即使要对StringValue做赋值操作,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">sv1=sv2;<span class="comment">//sv1和sv2是StringValue型对象</span></span><br></pre></td></tr></table></figure>

<p> 指向sv1和sv2的对象数目实际上并未改变,因此sv1的基类部分RCObject什么也不做仍然是正确的.<br> removeReference的责任不仅在于将refCount减1,实际上还承担了析构函数的作用——在refCount=1的时候delete销毁对象,从这里可以看出RCObject必须被产生于heap中.<br> StringValue要直接使用RCObject,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">StringValue</span>:</span> <span class="keyword">public</span> RCObject &#123;</span><br><span class="line">        <span class="keyword">char</span> *data;</span><br><span class="line">        StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue);</span><br><span class="line">        ~StringValue();</span><br><span class="line">    &#125;;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue)</span><br><span class="line">&#123;</span><br><span class="line">    data = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="built_in">strlen</span>(initValue) + <span class="number">1</span>];</span><br><span class="line">    <span class="built_in">strcpy</span>(data, initValue);</span><br><span class="line">&#125;</span><br><span class="line">String::StringValue::~StringValue()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> [] data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> StringValue类public继承自RCObject,因此它继承了RCObject的接口并供String使用,StringValue也必须构造在heap中.</p>
<h5 id="自动操作Reference-Count-引用计数"><a href="#自动操作Reference-Count-引用计数" class="headerlink" title="自动操作Reference Count(引用计数)"></a>自动操作Reference Count(引用计数)</h5></li>
<li><p>RCObject提供了一定程度的代码复用功能,但还远远不够——String类仍然需要手动调用RCObject的成员函数来对引用计数进行更改.解决方法就是”计算机科学领域中大部分问题得以解决的原理”——在中间加一层,也就是在String和StringValue中间加一层智能指针类对引用计数进行管理,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//管理引用计数的智能指针类</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">RCPtr</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    RCPtr(T* realPtr = <span class="number">0</span>);</span><br><span class="line">    RCPtr(<span class="keyword">const</span> RCPtr&amp; rhs);</span><br><span class="line">    ~RCPtr();</span><br><span class="line">    RCPtr&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> RCPtr&amp; rhs);</span><br><span class="line">    T* <span class="keyword">operator</span>-&gt;() <span class="keyword">const</span>; <span class="comment">// see Item 28</span></span><br><span class="line">    T&amp; <span class="keyword">operator</span>*() <span class="keyword">const</span>; <span class="comment">// see Item 28</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T *pointee; </span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>; <span class="comment">//将构造函数中的重复操作提取成一个函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 之前RCPtr是一个类模板,String之前有一个StringValue*成员,现在只要将它替换为RCPtr<stringvalue>即可.<br> RCPtr的构造函数像这样:</stringvalue></p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">RCPtr</span>&lt;T&gt;:</span>:RCPtr(T* realPtr): pointee(realPtr)</span><br><span class="line">&#123;</span><br><span class="line">    init();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">RCPtr</span>&lt;T&gt;:</span>:RCPtr(<span class="keyword">const</span> RCPtr&amp; rhs): pointee(rhs.pointee)</span><br><span class="line">&#123;</span><br><span class="line">    init();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">RCPtr</span>&lt;T&gt;:</span>:init()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (pointee == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (pointee-&gt;isShareable() == <span class="literal">false</span>) &#123; </span><br><span class="line">        pointee = <span class="keyword">new</span> T(*pointee); </span><br><span class="line">    &#125; </span><br><span class="line">    pointee-&gt;addReference();<span class="comment">//引用计数的更改负担转移到这里</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> init中使用了new关键字,它调用T的拷贝构造函数,为防止编译器为StringValue合成的拷贝构造函数执行浅复制,需要为StringValue定义执行深度复制的拷贝构造函数,像这样:</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> StringValue&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    data = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="built_in">strlen</span>(rhs.data) + <span class="number">1</span>];</span><br><span class="line">    <span class="built_in">strcpy</span>(data, rhs.data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 此外,由于多态性的存在,尽管pointee是T*类型,但它实际可能指向T类型的派生类,在此情况下new调用的却是T的拷贝构造函数,要防止这种现象,可以使用virtual copy constructor(见条款25),这里不再讨论</p>
<h5 id="把所有努力放到这里"><a href="#把所有努力放到这里" class="headerlink" title="把所有努力放到这里"></a>把所有努力放到这里</h5> <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">RCPtr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    RCPtr(T *realPtr = <span class="number">0</span>);</span><br><span class="line">    RCPtr(<span class="keyword">const</span> RCPtr &amp;rhs);</span><br><span class="line">    ~RCPtr();</span><br><span class="line">    RCPtr&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> RCPtr &amp;rhs);</span><br><span class="line">    T* <span class="keyword">operator</span>-&gt;() <span class="keyword">const</span>;</span><br><span class="line">    T&amp; <span class="keyword">operator</span>*() <span class="keyword">const</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T *pointee;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">void</span> <span class="title">RCPtr</span>&lt;T&gt;:</span>:init()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (pointee == <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span> (pointee-&gt;isShareable() == <span class="literal">false</span>)</span><br><span class="line">        pointee = <span class="keyword">new</span> T(*pointee);</span><br><span class="line">    pointee-&gt;addReference();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">RCPtr</span>&lt;T&gt;:</span>:RCPtr(T *realPtr) : pointee(realPtr) &#123; init(); &#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">RCPtr</span>&lt;T&gt;:</span>:RCPtr(<span class="keyword">const</span> RCPtr &amp;rhs) : pointee(rhs.pointee) &#123; init(); &#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">RCPtr</span>&lt;T&gt;:</span>:~RCPtr() &#123; <span class="keyword">if</span> (pointee) pointee-&gt;removeReference(); &#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">T</span>* <span class="title">RCPtr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>-&gt;() <span class="keyword">const</span> &#123; <span class="keyword">return</span> pointee; &#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">T</span>&amp; <span class="title">RCPtr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>*() <span class="keyword">const</span> &#123; <span class="keyword">return</span> *pointee; &#125;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">RCPtr</span>&lt;T&gt;&amp; <span class="title">RCPtr</span>&lt;T&gt;:</span>:<span class="keyword">operator</span>=(<span class="keyword">const</span> RCPtr &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (pointee != rhs.pointee)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (pointee) pointee-&gt;removeReference();</span><br><span class="line">        pointee = rhs.pointee;</span><br><span class="line">        init();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RCObject</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">addReference</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">removeReference</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">markUnshareable</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isShareable</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isShared</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    RCObject();</span><br><span class="line">    RCObject(<span class="keyword">const</span> RCObject &amp;rhs);</span><br><span class="line">    RCObject&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> RCObject &amp;rhs);</span><br><span class="line">    <span class="keyword">virtual</span> ~RCObject() = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> refCount;</span><br><span class="line">    <span class="keyword">bool</span> shareable;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">RCObject::RCObject() : refCount(<span class="number">0</span>), shareable(<span class="literal">true</span>) &#123;&#125;</span><br><span class="line">RCObject::RCObject(<span class="keyword">const</span> RCObject&amp;) : refCount(<span class="number">0</span>), shareable(<span class="literal">true</span>) &#123;&#125;</span><br><span class="line">RCObject&amp; RCObject::<span class="keyword">operator</span>=(<span class="keyword">const</span> RCObject&amp;) &#123; <span class="keyword">return</span> *<span class="keyword">this</span>; &#125;</span><br><span class="line">RCObject::~RCObject() &#123;&#125;</span><br><span class="line"><span class="keyword">void</span> RCObject::addReference() &#123; ++refCount; &#125;</span><br><span class="line"><span class="keyword">void</span> RCObject::removeReference() &#123; <span class="keyword">if</span> (--refCount == <span class="number">0</span>) <span class="keyword">delete</span> <span class="keyword">this</span>; &#125;</span><br><span class="line"><span class="keyword">void</span> RCObject::markUnshareable() &#123; shareable == <span class="literal">false</span>; &#125;</span><br><span class="line"><span class="keyword">bool</span> RCObject::isShareable() <span class="keyword">const</span> &#123; <span class="keyword">return</span> shareable; &#125;</span><br><span class="line"><span class="keyword">bool</span> RCObject::isShared() <span class="keyword">const</span> &#123; <span class="keyword">return</span> refCount &gt; <span class="number">1</span>; &#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    String(<span class="keyword">const</span> <span class="keyword">char</span> *value = <span class="string">""</span>);</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="keyword">int</span> index) <span class="keyword">const</span>;</span><br><span class="line">    <span class="keyword">char</span>&amp; <span class="keyword">operator</span>[](<span class="keyword">int</span> index);</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">StringValue</span> :</span> <span class="keyword">public</span> RCObject</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">char</span> *data;</span><br><span class="line">        StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue);</span><br><span class="line">        StringValue(<span class="keyword">const</span> StringValue &amp;rhs);</span><br><span class="line">        <span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *initValue)</span></span>;</span><br><span class="line">        ~StringValue();</span><br><span class="line">    &#125;;</span><br><span class="line">    RCPtr&lt;StringValue&gt; value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> String::StringValue::init(<span class="keyword">const</span> <span class="keyword">char</span> *initValue)</span><br><span class="line">&#123;</span><br><span class="line">    data = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="built_in">strlen</span>(initValue) + <span class="number">1</span>];</span><br><span class="line">    <span class="built_in">strcpy</span>(data, initValue);</span><br><span class="line">&#125;</span><br><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> <span class="keyword">char</span> *initValue) &#123; init(initValue); &#125;</span><br><span class="line">String::StringValue::StringValue(<span class="keyword">const</span> StringValue&amp; rhs) &#123; init(rhs.data); &#125;</span><br><span class="line">String::StringValue::~StringValue() &#123; <span class="keyword">delete</span> []data; &#125;</span><br><span class="line"></span><br><span class="line">String::String(<span class="keyword">const</span> <span class="keyword">char</span> *initValue) : value(<span class="keyword">new</span> StringValue(initValue)) &#123;&#125;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>&amp; String::<span class="keyword">operator</span>[](<span class="keyword">int</span> index) <span class="keyword">const</span> &#123; <span class="keyword">return</span> value-&gt;data[index]; &#125;</span><br><span class="line"><span class="keyword">char</span>&amp; String::<span class="keyword">operator</span>[](<span class="keyword">int</span> index)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (value-&gt;isShared())</span><br><span class="line">        value = <span class="keyword">new</span> StringValue(value-&gt;data);</span><br><span class="line">    value-&gt;markUnshareable();</span><br><span class="line">    <span class="keyword">return</span> value-&gt;data[index];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h4 id="条款30：Proxy-classes（替身类、代理类）"><a href="#条款30：Proxy-classes（替身类、代理类）" class="headerlink" title="条款30：Proxy classes（替身类、代理类）"></a>条款30：Proxy classes（替身类、代理类）</h4><ol>
<li>通过一个类对象来象征一个其他对象，常被称为<font color="green">proxy objects</font>（<font color="red">替身对象</font>）。它允许我们完成某些十分困难或几乎不可能完成的行为。多维数组是其中之一，左值/右值的区分是其中之二，压抑隐式转换是其中之三。</li>
<li><a href="https://www.cnblogs.com/reasno/p/4858490.html" target="_blank" rel="noopener">https://www.cnblogs.com/reasno/p/4858490.html</a></li>
</ol>
<h4 id="条款31：让函数根据一个以上的对象类型来决定如何虚化"><a href="#条款31：让函数根据一个以上的对象类型来决定如何虚化" class="headerlink" title="条款31：让函数根据一个以上的对象类型来决定如何虚化"></a>条款31：让函数根据一个以上的对象类型来决定如何虚化</h4><ol>
<li><p>假设设计一个游戏，根据宇宙飞船、太空站和小行星三者的共同特征，设计为以下情况：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">A[SpaceShip] --&gt; S(GameObject) </span><br><span class="line">B[SpaceStation] --&gt; S</span><br><span class="line">C[Asteroid] --&gt; S</span><br></pre></td></tr></table></figure>

<p> 整个继承体系：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameObject</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceShip</span> :</span> <span class="keyword">public</span> GameObject &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceStation</span> :</span> <span class="keyword">public</span> GameObject &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Asteroid</span> :</span> <span class="keyword">public</span> GameObject &#123; ... &#125;;</span><br></pre></td></tr></table></figure>

<p> 不同对象相撞要有不同的规则，处理碰撞的函数声明像这样：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">checkForCollision</span><span class="params">(GameObject &amp;object1, GameObject &amp;object2)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 现在挑战出来了，处理他们之间的碰撞，首先得知道它们的动态类型。人们常把面向对象里虚函数的调用动作称为<font color="green">&gt;message dispatch</font>（<font color="red">消息分派</font>）。然而C++并不直接支持multiple dispatch。</p>
<h5 id="虚函数-RTTI（运行时期类型识别）"><a href="#虚函数-RTTI（运行时期类型识别）" class="headerlink" title="虚函数 + RTTI（运行时期类型识别）"></a>虚函数 + RTTI（运行时期类型识别）</h5></li>
<li><p>最一般化的double-dispatching实现方法是用<font color="red"><strong>if-then-elses</strong></font>来仿真虚函数：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameObject</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObject &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">calss SpaceShip : <span class="keyword">public</span> GameObject &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObject &amp;otherObject)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果和一个未知类型对象相撞，就抛出以下exception</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CollisionWithUnknownObject</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    CollisionWithUnknownObject(GameObject &amp;whatWeHit);</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> SpaceShip::collide(GameObject &amp;otherObject) &#123;</span><br><span class="line">    <span class="keyword">const</span> type_info &amp;objectType = <span class="keyword">typeid</span>(otherObject);</span><br><span class="line">    <span class="keyword">if</span> (objectType == <span class="keyword">typeid</span>(SpaceShip)) &#123;</span><br><span class="line">        SpaceShip &amp;ss = <span class="keyword">static_cast</span>&lt;SpaceShip&amp;&gt;(otherObject);</span><br><span class="line">        <span class="comment">// process a SpaceShip-SpaceShip collision;</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (objectType == <span class="keyword">typeid</span>(SpaceStation)) &#123;</span><br><span class="line">        SpaceStation &amp;ss = <span class="keyword">static_cast</span>&lt;SpaceStation&amp;&gt;(otherObject);</span><br><span class="line">        <span class="comment">// process a SpaceShip-SpaceStation collision;</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (objectType == <span class="keyword">typeid</span>(Asteroid)) &#123;</span><br><span class="line">        Asteroid &amp;a = <span class="keyword">static_cast</span>&lt;Asteroid&amp;&gt;(otherObject);</span><br><span class="line">        <span class="comment">// sprocess a SpaceShip-Asteroid collision;</span></span><br><span class="line">    &#125; <span class="keyword">else</span> </span><br><span class="line">        <span class="keyword">throw</span> CollisionWithUnknownObject(otherObejct);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 我们只需要决定碰撞双方的其中一个类型，因为另一个对象是*this，它的类型被虚函数机制决定下来了，是SpaceShip对象。<br> 这种以类型为行事基准的方法有个缺点：它会造成程序难以维护。这就是虚函数当初被发明出来的原因：把生产维护以类型为行事基准的函数的负担，从程序员转到编译器。但现在如果使用RTTI来实现double-dispatching，等于回到老而糟糕的年代。</p>
<h5 id="只使用虚函数"><a href="#只使用虚函数" class="headerlink" title="只使用虚函数"></a>只使用虚函数</h5></li>
<li><p>看看如何只以虚函数来解决问题：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceShip</span>;</span>    <span class="comment">// forward declarations</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceStation</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Asteroid</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameObject</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObjet &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(SpaceShip &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(SpaceStation &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(Asteroid &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceShip</span> :</span> <span class="keyword">public</span> GameObject &#123;</span><br><span class="line"><span class="keyword">public</span> :</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObjet &amp;otherObject)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(SpaceShip &amp;otherObject)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(SpaceStation &amp;otherObject)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(Asteroid &amp;otherObject)</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 这种方法将double-dispatching以两个分离的虚函数调用实现出来的。这并不是递归调用。第一个虚函数调用动作针对的是接收GameObject&amp;参数的collide函数，有点简单：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> SpaceShip::collide(GameObject &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    otherObject.collide(*<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> SpaceShip::collide(SpaceShip &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// process a SpaceShip-SpaceShip collision;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> SpaceShip::collide(SpaceStation &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// processa SpaceShip-SpaceStation collision;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> SpaceShip:collide(Asteroid &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// process SpaceShip-Asteroid collision;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 但这种做法成本太高，每次修改需要在每个class中增添（E34）。但它也有好处，那就是不再因为为止类型而不得不抛出异常。</p>
<h5 id="自行仿真虚函数表格（Virtual-Function-Tables）"><a href="#自行仿真虚函数表格（Virtual-Function-Tables）" class="headerlink" title="自行仿真虚函数表格（Virtual Function Tables）"></a>自行仿真虚函数表格（Virtual Function Tables）</h5></li>
<li><p>回忆条款24，编译器通过vtbl直接索引取得函数指针，而不必条条框框if-then-else运算。这样一来效率也高，也可以当使用RTTI的时候隔离至一个点：vtbl的初始化处。<br> 先从GameObject继承体系内的函数开始：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GameObject</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObject &amp;otherObject)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpaceShip</span> :</span> <span class="keyword">public</span> GameObject &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">collide</span><span class="params">(GameObject &amp;otherObject)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">hitSpaceShip</span><span class="params">(GameObject &amp;spaceShip)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">hitSpaceStation</span><span class="params">(GameObject &amp;spaceStation)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">hitAsteroid</span><span class="params">(GameObject &amp;asteroid)</span></span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function">typeef <span class="title">void</span> <span class="params">(SpaceShip::*HitFunctionPtr)</span><span class="params">(GameObject&amp;)</span></span>;</span><br><span class="line">    <span class="keyword">typedef</span> <span class="built_in">map</span>&lt;<span class="built_in">string</span>, HitFunctionPtr&gt; HitMap;</span><br><span class="line">    <span class="comment">// 产生中介函数lookup</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> hitFunctionPtr <span class="title">lookup</span><span class="params">(<span class="keyword">const</span> GameObject &amp;whaWeHit)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> HitMap* <span class="title">initializeCollisionMap</span><span class="params">()</span></span>;      </span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> SpaceShip::hitSpaceShip(SpaceShip &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    SpaceShip &amp;otherShip = <span class="keyword">dynamic_cast</span>&lt;SpaceShip&amp;&gt;(spaceShip);</span><br><span class="line">    <span class="comment">// process a SpaceShip-SpaceShip collision;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> SpaceShip::hitSpaceStation(SpaceStation &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    SpaceStation &amp;station = <span class="keyword">dynamic_cast</span>&lt;SpaceStation&amp;&gt;(spaceStation);</span><br><span class="line">    <span class="comment">// process a SpaceShip-SpaceStation collision;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> SpaceShip::hitAsteroid(Asteroid &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    Asteroid &amp;theAsteroid = <span class="keyword">dynamic_cast</span>&lt;Asteroid&amp;&gt;(asteroid);</span><br><span class="line">    <span class="comment">// process a SpaceShip-Asteroid collision;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> SpaceShip::collide(GameObject &amp;otherObject)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 找出调用的函数</span></span><br><span class="line">    HitFunctionPtr hfp = lookup(otherObject);</span><br><span class="line">    <span class="keyword">if</span> (hfp) (<span class="keyword">this</span>-&gt;*hfp)(otherObject);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">throw</span> CollisionWithUnknownObject(otherObject);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SpaceShip::HitMap* SpaceShip::initializeCollisionMap()</span><br><span class="line">&#123;</span><br><span class="line">    HitMap *phm = <span class="keyword">new</span> HitMap;</span><br><span class="line">    (*phm)[<span class="string">"SpaceShip"</span>] = &amp;hitSapceShip;</span><br><span class="line">    (*phm)[<span class="string">"SpaceStation"</span>] = &amp;hitSpaceStation;</span><br><span class="line">    (*phm)[<span class="string">"Asteroid"</span>] = &amp;hitAsteroid;</span><br><span class="line">    <span class="keyword">return</span> phm;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SpaceShip::HitFunctionPtr SpaceShip::lookup(<span class="keyword">const</span> GameObject &amp;whatWeHit)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">auto_ptr</span>&lt;HitMap&gt; collisionMap(initializeCollisionMap());</span><br><span class="line">    HitMap::iterator mapEntry = collisionMap.find(<span class="keyword">typeid</span>(whatWeHit).name());</span><br><span class="line">    <span class="keyword">if</span> (mapEntry == collisionMap.end()) </span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> (*mapEntry).second;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 跟一开始说的RTTI解法一样，GameObject class只含有一个碰撞处理函数，这个函数执行两个必要的<strong>single-dispatches</strong>中的第一个。而其他互动函数不再使用同一个collide名称，放弃了重载。<br> 这里我们需要交付给一个中介函数lookup一个GameObject，它会返回一个指向”当和GameObject相撞时“必须调用的函数的指针（<font color="red">函数指针</font>）。为了能够动态映射某个member function指针，一个简单的方法是产生一个关系型（associative）数组，只要获得class名字，导出member function指针（key-value？)。</p>
<h5 id="将自行仿真的虚函数表格（Virtual-Function-Tables）初始化"><a href="#将自行仿真的虚函数表格（Virtual-Function-Tables）初始化" class="headerlink" title="将自行仿真的虚函数表格（Virtual Function Tables）初始化"></a>将自行仿真的虚函数表格（Virtual Function Tables）初始化</h5></li>
<li><p>对于collisionMap的初始化问题，只需要写一个private static member function，名为initializeCollisionMap，用来初始化，然后返回值作为初值就可以了。然而返回值Map按值传递意味着构造和析构成本，如果返回指针，又要苦恼map对象的delete时宜，那么用smart pointer吧(见上面完整实现)。</p>
<h5 id="使用”非成员（Non-Member）函数“的碰撞处理函数"><a href="#使用”非成员（Non-Member）函数“的碰撞处理函数" class="headerlink" title="使用”非成员（Non-Member）函数“的碰撞处理函数"></a>使用”非成员（Non-Member）函数“的碰撞处理函数</h5></li>
<li><p>当有新的class加入时,继承体系的每个类都需要添加处理新型碰撞的代码.这是因为此前的策略都是将处理碰撞的任务交由碰撞的某一方来执行,仿真虚函数表策略也不例外——每个class内含一个仿真的虚函数表,内含的指针也都指向成员函数。将碰撞处理函数设为non-member,就可以使得class定义式不包含碰撞处理函数,当需要添加碰撞处理函数时也就不需要修改class定义。将碰撞处理函数移出class外，成为中立的第三者处理，则构筑processCollision函数：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"SpaceShip.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"SpaceStation.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"Asteroid.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 匿名namespace具有文件内部static的功效</span></span><br><span class="line"><span class="keyword">namespace</span> &#123; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 主要的碰撞处理函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">shipAsteroid</span><span class="params">(GameObject &amp;spaceShip, GameObject &amp;asteroid)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">shipStation</span><span class="params">(GameObject &amp;spaceShip, GameObject &amp;spaceStation)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">asteroidStation</span><span class="params">(GameObject &amp;asteroid, GameObject &amp;spaceStation)</span></span>;</span><br><span class="line">...</span><br><span class="line"><span class="comment">// 次要的碰撞处理函数，只是为了实现对称性</span></span><br><span class="line"><span class="comment">// 对调参数位置，然后调用主要的碰撞处理函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">asteroidShip</span><span class="params">(GameObject &amp;asteroid, GameObject &amp;spaceShip)</span></span></span><br><span class="line"><span class="function"></span>&#123; shipAsteroid(spaceShip, asteroid); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">stationShip</span><span class="params">(GameObject &amp;spaceStation, GameObject &amp;spaceShip)</span> </span></span><br><span class="line"><span class="function"></span>&#123; shipStation(spaceShip, spaceStation); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">stationAsteroid</span><span class="params">(GameObject &amp;spaceStation, GameObject &amp;asetroid)</span></span></span><br><span class="line"><span class="function"></span>&#123; asteroidStation(asteroid, spaceSation); &#125;</span><br><span class="line">... </span><br><span class="line"><span class="comment">// types/functions</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*HitFunctionPtr)</span><span class="params">(GameObject&amp;, GameObject&amp;)</span></span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="built_in">map</span>&lt; pair&lt;<span class="built_in">string</span>, <span class="built_in">string</span>&gt;, HitFunctionPtr &gt; HitMap;</span><br><span class="line"><span class="comment">// 以两个char*字面常量产生一个pair&lt;string, string&gt;对象</span></span><br><span class="line">pair&lt;<span class="built_in">string</span>, <span class="built_in">string</span>&gt; makeStringPair(<span class="keyword">const</span> <span class="keyword">char</span> *s1, <span class="keyword">const</span> <span class="keyword">char</span> *s2)</span><br><span class="line">&#123; <span class="keyword">return</span> pair&lt;<span class="built_in">string</span>, <span class="built_in">string</span>&gt;(s1, s2); &#125;</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="function">HitMap* <span class="title">initializeCollisionMap</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    HitMap *phm = <span class="keyword">new</span> HitMap;</span><br><span class="line">    (*phm)[makeStringPair(<span class="string">"SpaceShip"</span>, <span class="string">"Asteroid"</span>)] = &amp;shipAsteroid;</span><br><span class="line">    (*phm)[makeStringPair(<span class="string">"SpaceShip"</span>, <span class="string">"SpaceStation"</span>)] = &amp;shipStation;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> phm;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 必须修改，以便接纳pair&lt;string, string&gt;对象</span></span><br><span class="line"><span class="function">HitFunctionPtr <span class="title">lookup</span><span class="params">(<span class="keyword">const</span> <span class="built_in">string</span> class1, <span class="keyword">const</span> <span class="built_in">string</span> &amp;class2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">auto_ptr</span>&lt;HitMap&gt; collisionMap(initializeCollisionMap());</span><br><span class="line">    HitMap::iterator mapEntry = collisionMap-&gt;find(make_pair(class1, class2));</span><br><span class="line">    <span class="keyword">if</span> (mapEntry == collisionMap-&gt;end()) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> (*mapEntry).second;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">processCollision</span><span class="params">(GameObject &amp;object1, GameObject &amp;object2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    HitFunctinoPtr phf = lookup(<span class="keyword">typeid</span>(object1).name(), <span class="keyword">typeid</span>(object2).name());</span><br><span class="line">    <span class="keyword">if</span> (phf) phf(object1, object2);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">throw</span> UnknownCollision(object1, object2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 这份实现和先前的member functions版相同，但略有差异：</p>
<ul>
<li><p><font color="red">HitFunctionPtr</font>如今是一个指向non-member function的指针。</p>
</li>
<li><p><font color="red">exception class CollsionWithUnknownObject</font>已经被重新命名为UnknownCollision并改为取得两个对象。</p>
</li>
<li><p><font color="red">lookup</font>需要接收两个类型名称，并执行double-dispatch的完整两半。</p>
<p>这意味着collision map需要持有三份信息：<strong>两个类型名称和一个HitFunctionPtr</strong>。<br>由于makeStringPair，initializationCollisionMap，lookup都声明于匿名namespace内，因此它们必须实现于相同的namesapce中，使得<strong>链接器能够正确的将定义和声明关联起来</strong>。  </p>
</li>
</ul>
<p> <strong>通过将碰撞处理函数从类中分离,实现了即使新的GameObject被添加,原有的class也不需要重新编译,只需要在initializeCollisionMap中增加对应的键-值对,并在processCollision所在的匿名命名空间中申明一个新的碰撞处理函数即可</strong>。</p>
<h5 id="”继承“-”自行仿真的虚函数表格“"><a href="#”继承“-”自行仿真的虚函数表格“" class="headerlink" title="”继承“ + ”自行仿真的虚函数表格“"></a>”继承“ + ”自行仿真的虚函数表格“</h5></li>
<li><p>目前所做的每一件事都可以有效运作——只要在调用碰撞处理函数时不发生<font color="red"><strong>inheritance-based类型转换</strong></font>。</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">A[SpaceStation] --&gt; G</span><br><span class="line">S(SpaceShip) --&gt; G(GameObject)</span><br><span class="line">B[Asteroid] --&gt; G</span><br><span class="line">C[CommercialShip] --&gt; S</span><br><span class="line">D[MilitaryShip] --&gt; S</span><br></pre></td></tr></table></figure>

<p> 如果MilitaryShip和一个Asteroid碰撞，希望调用的时：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">shipAsteroid</span><span class="params">(GameObject &amp;spaceShip, GameObject &amp;asteroid)</span></span>;</span><br></pre></td></tr></table></figure>

<p> 然而事非如此，而是抛出一个<font color="red">UnknownCollision exception</font>。虽然MilitaryShip对象可视为一个SpaceShip对象，但lookup并不知道。<br> 如果想要实现double-dispatching而且需要支持inheritance-based参数转换，那么唯一可用的资源是<strong>”双虚函数调用“机制</strong>。</p>
<h5 id="将自行仿真的虚函数表格初始化（再度讨论）"><a href="#将自行仿真的虚函数表格初始化（再度讨论）" class="headerlink" title="将自行仿真的虚函数表格初始化（再度讨论）"></a>将自行仿真的虚函数表格初始化（再度讨论）</h5></li>
<li></li>
</ol>
]]></content>
      <categories>
        <category>2019.03</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>More Effecive C++</tag>
      </tags>
  </entry>
  <entry>
    <title>一些调试工具命令</title>
    <url>/2020/06/13/%E4%B8%80%E4%BA%9B%E8%B0%83%E8%AF%95%E5%B7%A5%E5%85%B7%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="测试工具"><a href="#测试工具" class="headerlink" title="测试工具"></a>测试工具</h2><blockquote>
<ul>
<li>三剑客<a href="#grep">grep</a>、<a href="#sed">sed</a>、<a href="#awk">awk</a>的使用</li>
<li>网络分析工具<a href="#tcpdump">tcpdump</a>的使用</li>
<li>内存分析：<a href="#valgrind">valgrind</a></li>
<li>其他：netstat、lsof、nc、tcpcopy、prerf、sysctl、<a href="#strace">strace</a>、prove等</li>
<li>性能检测工具：<a href="#perf">perf</a>、ftrace、vmstat</li>
</ul>
</blockquote>
<hr>
<h3 id="Linux三剑客"><a href="#Linux三剑客" class="headerlink" title="Linux三剑客"></a>Linux三剑客</h3><hr>
<h4 id="grep"><a href="#grep" class="headerlink" title="grep"></a><span id="grep">grep</span></h4><h5 id="用法：grep-OPTION-PATTERN-FILE"><a href="#用法：grep-OPTION-PATTERN-FILE" class="headerlink" title="用法：grep [OPTION...] PATTERN [FILE...]"></a>用法：<code>grep [OPTION...] PATTERN [FILE...]</code></h5><h5 id="选项："><a href="#选项：" class="headerlink" title="选项："></a>选项：</h5><ul>
<li>-b：每一行打印字符偏移量</li>
<li>-c：统计符合要求的行数</li>
<li>-E：使用egrep</li>
<li>-f：从文件中取匹配模板</li>
<li>-F：不使用正则快速搜索</li>
<li>-h：不显示文件名</li>
<li>-H：显式</li>
<li>-i：忽略大小写</li>
<li>-l：仅列出符合要求的文件，不列出具体行</li>
<li>-L：与-l相反</li>
<li>-n：显示符合要求的行及行号</li>
<li>-p：silence模式</li>
<li>-v：取反</li>
<li>-r：遍历子目录</li>
<li>-s：no-message</li>
<li>-w：匹配完整的词</li>
<li>-x：匹配完整的行</li>
<li>-An：</li>
<li>-Bn：</li>
<li>-Cn：</li>
<li>正则规则：<ul>
<li><code>[]</code>：表示集合中的内容，如<code>[abc]</code>匹配<code>a</code>或<code>b</code>或<code>c</code>。<ul>
<li><code>[^]</code>：取反</li>
<li><code>[a-z]</code>：小写字母</li>
<li><code>[A-Z]</code>：大写字母</li>
<li><code>[0-9]</code>：数字</li>
</ul>
</li>
<li><code>^</code>和<code>$</code>：表示行首和行位，<code>^$</code>表示空行</li>
<li><code>*</code>：通配符，表示多个重复</li>
<li><code>.</code>：单个。<code>.*</code>表示0个或多个</li>
<li><code>{n, m}</code>：个数范围，n个到m个之间，需要用<code>/</code>转义，egrep下不需要转义</li>
<li><code>egrep</code>：<ul>
<li><code>+</code>：类似<code>.</code>，1个或多个</li>
<li><code>?</code>：表示0个或多个</li>
<li><code>|</code>：表示关系，如<code>&#39;ab|cd|def&#39;</code>表示<code>ab</code>、<code>cd</code>或<code>def</code>的串</li>
<li><code>()</code>：将部分内容合成单元</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h4 id="sed"><a href="#sed" class="headerlink" title="sed"></a><span id="sed">sed</span></h4><h5 id="用法：sed-OPTION-SCRIPTorCOMMAND-FILE"><a href="#用法：sed-OPTION-SCRIPTorCOMMAND-FILE" class="headerlink" title="用法：sed [OPTION]... [SCRIPTorCOMMAND] [FILE]..."></a>用法：<code>sed [OPTION]... [SCRIPTorCOMMAND] [FILE]...</code></h5><h5 id="选项：-1"><a href="#选项：-1" class="headerlink" title="选项："></a>选项：</h5><ul>
<li>-e：执行多条命令</li>
<li>-f：引导sed脚本</li>
<li>-i：直接修改文件</li>
<li>-n：silent，搭配s替换p可以打印修改过的行</li>
<li>-h：帮助</li>
<li>-V：版本信息</li>
<li>action:<ul>
<li><code>a</code>：后插，<code>sed &#39;/text/a\newline&#39;</code> OR <code>sed &#39;/text/a newline&#39;</code></li>
<li><code>c</code>：取代，<code>sed &#39;3,4c\replaceline&#39;</code></li>
<li><code>d</code>：删除，<code>sed &#39;2,4d&#39;</code> OR <code>sed &#39;/text/&#39;d</code></li>
<li><code>g</code>：获取缓冲区内容替换文本</li>
<li><code>G</code>：获取缓冲区内容追加文本</li>
<li><code>h</code>：拷贝文本到缓冲区</li>
<li><code>H</code>：追加文本到缓冲区，保持和获取：<code>sed -e &#39;/text/h&#39; -e &#39;$G&#39; file</code></li>
<li><code>i</code>：前插，<code>sed &#39;/text/i\newthing&#39;</code></li>
<li><code>l</code>：列表不打印字符清单</li>
<li><code>n</code>：读取下一个命令，用新命令处理新行</li>
<li><code>p</code>：打印，<code>sed &#39;1p;3,4p&#39;</code></li>
<li><code>q</code>：打印到第n行退出</li>
<li><code>r</code>：从file读内容，<code>sed &#39;/text/r file&#39;</code></li>
<li><code>s</code>：取代（正则）<ul>
<li><code>^</code>、<code>$</code>、<code>.</code>、<code>*</code>、<code>[]</code>、<code>[^]</code></li>
<li><code>(..)</code>：匹配子串，保存字符如<code>s/(love)able/\1rs</code>：<code>loveable</code> -&gt; <code>lovers</code></li>
<li><code>&amp;</code>：拼接，<code>s/love/sb&amp;</code> -&gt; <code>love</code> -&gt; <code>sblove</code></li>
<li><code>&lt;</code>：匹配单词开始，如<code>/&lt;love/</code>表示love开头的单词的行</li>
<li><code>&gt;</code>：匹配单词结束，如<code>/love&gt;/</code></li>
<li><code>x{m}</code>：重复x有m次，<code>/0{5}</code></li>
<li><code>x{m,}</code>：重复x至少m次</li>
<li><code>x{m, n}</code>：重复x至少m到n次</li>
</ul>
</li>
<li><code>t</code>：if分支</li>
<li><code>g</code>：全面替换</li>
<li><code>w</code>：把行写入file，<code>sed &#39;/text/w file&#39;</code></li>
<li><code>x</code>：</li>
<li><code>y</code>：把字符翻译为另外的字符，不做正则</li>
<li><code>!</code>：后续命令作用于没有选中的行</li>
<li><code>#</code>：注释扩展到下个换行符前</li>
</ul>
</li>
</ul>
<hr>
<h4 id="awk"><a href="#awk" class="headerlink" title="awk"></a><span id="awk">awk</span></h4><p><a href="https://www.jianshu.com/p/8c6a0d0d4f0d" target="_blank" rel="noopener">awk简明教程</a></p>
<h5 id="用法："><a href="#用法：" class="headerlink" title="用法："></a>用法：</h5><h5 id="awk-F-SEPSTRING-v-ASSIGNMENT-program-ARGUMENT"><a href="#awk-F-SEPSTRING-v-ASSIGNMENT-program-ARGUMENT" class="headerlink" title="awk [-F SEPSTRING] [-v ASSIGNMENT]... program [ARGUMENT...]"></a><code>awk [-F SEPSTRING] [-v ASSIGNMENT]... program [ARGUMENT...]</code></h5><h5 id="awk-F-SEPSTRING-f-PROFILE-f-v-ASSIGNMENT-ARGUMENT"><a href="#awk-F-SEPSTRING-f-PROFILE-f-v-ASSIGNMENT-ARGUMENT" class="headerlink" title="awk [-F SEPSTRING] -f PROFILE [-f]... [-v ASSIGNMENT]... [ARGUMENT...]"></a><code>awk [-F SEPSTRING] -f PROFILE [-f]... [-v ASSIGNMENT]... [ARGUMENT...]</code></h5><h5 id="选项：-2"><a href="#选项：-2" class="headerlink" title="选项："></a>选项：</h5><ul>
<li>-F fs：指定文件分隔符，fs是字符串或正则，如<code>-F:</code></li>
<li>-v var=value：定义变量</li>
<li>-f scripfile：从脚本中读取awk</li>
<li>-W：<ul>
<li><code>compact</code> or <code>traditional</code>：兼容模式运行awk，gawk将同awk，忽略扩展</li>
<li><code>copyleft</code> or <code>copyright</code>：版权信息</li>
<li><code>help</code> or <code>usage</code>：选项简短说明</li>
<li><code>lint</code> or <code>lint-old</code>：向传统unix移植结构警告</li>
<li><code>posix</code>：兼容模式不识别：\x、函数关键字、func、换码序列以及当fs是一个空格时，将新行作为一个域分隔符；操作符<strong>和</strong>=不能代替^和^=；fflush无效</li>
<li><code>re-interval</code>：允许间隔正则使用，POSIX字符类，括号表达式[[:alpha:]]</li>
<li><code>source PROG</code>：使用PROG作为源码，可搭配<code>-f</code><h5 id="环境变量"><a href="#环境变量" class="headerlink" title="环境变量"></a>环境变量</h5><table>
<thead>
<tr>
<th align="left">变量</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">$n</td>
<td align="left">记录的第n个字段，由FS分隔</td>
</tr>
<tr>
<td align="left">$0</td>
<td align="left">完整的输入记录</td>
</tr>
<tr>
<td align="left">ARGC</td>
<td align="left">命令行参数数量</td>
</tr>
<tr>
<td align="left">ARGIND</td>
<td align="left">命令行中当前文件位置</td>
</tr>
<tr>
<td align="left">ARGV</td>
<td align="left">命令行参数数组</td>
</tr>
<tr>
<td align="left">CONVFMT</td>
<td align="left">数字转换格式（%.6g）</td>
</tr>
<tr>
<td align="left">ENVIRON</td>
<td align="left">环境变量关联数组</td>
</tr>
<tr>
<td align="left">ERRNO</td>
<td align="left">系统错误</td>
</tr>
<tr>
<td align="left">FIELDWIDTHS</td>
<td align="left">字段宽度列表</td>
</tr>
<tr>
<td align="left">FILENAME</td>
<td align="left">当前文件名</td>
</tr>
<tr>
<td align="left">ENR</td>
<td align="left">同NR</td>
</tr>
<tr>
<td align="left">FS</td>
<td align="left">字段分隔符</td>
</tr>
<tr>
<td align="left">IGNORECASE</td>
<td align="left">为真则忽略大小写匹配</td>
</tr>
<tr>
<td align="left">NF</td>
<td align="left">记录中的字段数</td>
</tr>
<tr>
<td align="left">OFMT</td>
<td align="left">数字输出格式</td>
</tr>
<tr>
<td align="left">OFS</td>
<td align="left">输出字段分隔符（默认空格）</td>
</tr>
<tr>
<td align="left">ORS</td>
<td align="left">输出记录分隔符（默认换行）</td>
</tr>
<tr>
<td align="left">RLENGTH</td>
<td align="left">match函数匹配的字符串长度</td>
</tr>
<tr>
<td align="left">RS</td>
<td align="left">记录分隔符（默认换行）</td>
</tr>
<tr>
<td align="left">RSTART</td>
<td align="left">match函数匹配的第一个位置</td>
</tr>
<tr>
<td align="left">SUBSEP</td>
<td align="left">数组下标分隔符（默认\034）</td>
</tr>
<tr>
<td align="left">##### 通用正则表达式元字符</td>
<td align="left"></td>
</tr>
<tr>
<td align="left">符号</td>
<td align="left">描述</td>
</tr>
<tr>
<td align="left">:–</td>
<td align="left">:–</td>
</tr>
<tr>
<td align="left">\Y</td>
<td align="left">匹配单词开头或末尾的空字符串</td>
</tr>
<tr>
<td align="left">\B</td>
<td align="left">匹配单词内空字符串</td>
</tr>
<tr>
<td align="left">&lt;</td>
<td align="left">匹配单词开头</td>
</tr>
<tr>
<td align="left">&gt;</td>
<td align="left">匹配单词末尾</td>
</tr>
<tr>
<td align="left">\w</td>
<td align="left">匹配字母数字组成的单词</td>
</tr>
<tr>
<td align="left">\W</td>
<td align="left">匹配非字母数字组成的单词</td>
</tr>
<tr>
<td align="left">&#39;</td>
<td align="left">字符串开头空字符串</td>
</tr>
<tr>
<td align="left">‘</td>
<td align="left">字符换末尾空字符串</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
</ul>
<h4 id="tcpdump"><a href="#tcpdump" class="headerlink" title="tcpdump"></a><span id="tcpdump">tcpdump</span></h4><h3 id="其他一些工具"><a href="#其他一些工具" class="headerlink" title="其他一些工具"></a>其他一些工具</h3><h4 id="strace"><a href="#strace" class="headerlink" title="strace"></a><span id="strace">strace</span></h4><h3 id="perf性能分析工具"><a href="#perf性能分析工具" class="headerlink" title="perf性能分析工具"></a><span id="perf">perf</span>性能分析工具</h3><h4 id="调试关注点"><a href="#调试关注点" class="headerlink" title="调试关注点"></a>调试关注点</h4><ul>
<li><p>基于性能分析：可以<strong>算法优化</strong>、<strong>代码优化</strong></p>
</li>
<li><p>硬件资源使用：如cache访问次数、cache丢失次数、流水线停顿周期、前端总线访问次数等</p>
</li>
<li><p>操作系统资源：如系统调用次数、上下文切换次数、任务迁移次数</p>
</li>
<li><p>事件以下几种：</p>
<ul>
<li>Hardware Event：由PMU部件产生，特定条件下探测性能事件是否发生以及发生次数，如cache命中</li>
<li>Software Event：由内核产生的事件，分布各个功能模块，统计与操作系统相关的事件，如进程切换，tick数等</li>
<li>Tracepoint Event：由内核中静态tracepoint所触发的事件，tracepoint用来判断程序运行期间内核的行为细节，如slab分配器分配次数等</li>
<li>Tool Event：duration_time<h4 id="用法：perf-version-help-OPTIONS-COMMAND-ARGS"><a href="#用法：perf-version-help-OPTIONS-COMMAND-ARGS" class="headerlink" title="用法：perf [--version] [--help] [OPTIONS] COMMAND [ARGS]"></a>用法：<code>perf [--version] [--help] [OPTIONS] COMMAND [ARGS]</code></h4></li>
</ul>
</li>
<li><p>The most commonly used perf commands are:</p>
<ul>
<li><code>annotate</code>        Read perf.data (created by perf record) and display annotated code</li>
<li><code>archive</code>         Create archive with object files with build-ids found in perf.data file</li>
<li><code>bench</code>           General framework for benchmark suites</li>
<li><code>buildid-cache</code>   Manage build-id cache.</li>
<li><code>buildid-list</code>    List the buildids in a perf.data file</li>
<li><code>c2c</code>             Shared Data C2C/HITM Analyzer.</li>
<li><code>config</code>          Get and set variables in a configuration file.</li>
<li><code>data</code>            Data file related processing</li>
<li><code>diff</code>            Read perf.data files and display the differential profile</li>
<li><code>evlist</code>          List the event names in a perf.data file</li>
<li><code>ftrace</code>          simple wrapper for kernel’s ftrace functionality</li>
<li><code>inject</code>          Filter to augment the events stream with additional information</li>
<li><code>kallsyms</code>        Searches running kernel for symbols</li>
<li><code>kmem</code>            Tool to trace/measure kernel memory properties</li>
<li><code>kvm</code>             Tool to trace/measure kvm guest os</li>
<li><code>list</code>            List all symbolic event types</li>
<li><code>lock</code>            Analyze lock events</li>
<li><code>mem</code>             Profile memory accesses</li>
<li><code>record</code>          Run a command and record its profile into perf.data</li>
<li><code>report</code>          Read perf.data (created by perf record) and display the profile</li>
<li><code>sched</code>           Tool to trace/measure scheduler properties (latencies)</li>
<li><code>script</code>          Read perf.data (created by perf record) and display trace output</li>
<li><code>stat</code>            Run a command and gather performance counter statistics</li>
<li><code>test</code>            Runs sanity tests.</li>
<li><code>timechart</code>       Tool to visualize total system behavior during a workload</li>
<li><code>top</code>             System profiling tool.</li>
<li><code>version</code>         display the version of perf binary</li>
<li><code>probe</code>           Define new dynamic tracepoints</li>
<li><code>trace</code>           strace inspired tool<h4 id="功能分类"><a href="#功能分类" class="headerlink" title="功能分类"></a>功能分类</h4><h5 id="全局性概况"><a href="#全局性概况" class="headerlink" title="全局性概况"></a>全局性概况</h5></li>
</ul>
</li>
<li><p><code>list</code>：查看系统支持的性能事件</p>
</li>
<li><p><code>bench</code>：对系统性能摸底</p>
</li>
<li><p><code>test</code>：对系统健全性测试</p>
</li>
<li><p><code>stat</code>：对全局性能进行统计</p>
<h5 id="全局细节"><a href="#全局细节" class="headerlink" title="全局细节"></a>全局细节</h5></li>
<li><p><code>top</code>：实时查看系统进程函数占用率情况</p>
</li>
<li><p><code>probe</code>：自定义动态事件</p>
<h5 id="特定功能分析"><a href="#特定功能分析" class="headerlink" title="特定功能分析"></a>特定功能分析</h5></li>
<li><p><code>kmem</code>：支队slab子系统性能分析</p>
</li>
<li><p><code>kvm</code>：针对kvm虚拟化分析</p>
</li>
<li><p><code>lock</code>：分析锁性能</p>
</li>
<li><p><code>mem</code>：分析内核调度器性能</p>
</li>
<li><p><code>trace</code>：记录系统调用轨迹</p>
<h5 id="记录与分析"><a href="#记录与分析" class="headerlink" title="记录与分析"></a>记录与分析</h5></li>
<li><p><code>record</code>：记录信息到perf.data<br>(FrameGraph)[<a href="https://gitee.com/mirrors/FlameGraph.git]" target="_blank" rel="noopener">https://gitee.com/mirrors/FlameGraph.git]</a></p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用perf录制数据并使用FrameGraph查看火焰图</span></span><br><span class="line"><span class="comment"># 录制程序十秒</span></span><br><span class="line">$ perf record -F 99 -a -g -- sleep 10</span><br><span class="line"><span class="comment"># 数据展开</span></span><br><span class="line">$ perf script | ../stackcollapse-perf.pl &gt; out.perf-folded</span><br><span class="line"><span class="comment"># 生成svg矢量图</span></span><br><span class="line">$ ../flamegraph.pl out.perf-folded &gt; perf-kernel.svg</span><br></pre></td></tr></table></figure>
</li>
<li><p><code>report</code>：生成报告</p>
</li>
<li><p><code>diff</code>：对两个记录进行diff</p>
</li>
<li><p><code>evlist</code>：列出记录的性能事件</p>
</li>
<li><p><code>annotate</code>：显式perf.data函数代码</p>
</li>
<li><p><code>archive</code>：将相关符号打包，方便其他机器进行分析</p>
</li>
<li><p><code>script</code>：将perf.data输出可读性文本</p>
<h5 id="可视化工具"><a href="#可视化工具" class="headerlink" title="可视化工具"></a>可视化工具</h5></li>
<li><p><code>timechart record</code>：记录事件</p>
</li>
<li><p><code>timechart</code>：生成output.svg文档</p>
</li>
</ul>
<h4 id="一些技巧"><a href="#一些技巧" class="headerlink" title="一些技巧"></a>一些技巧</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看程序的pid、ppid、pgid、sid、comm信息</span></span><br><span class="line">$ ps -C program o pid,ppid,pgid,sid,comm</span><br><span class="line"><span class="comment"># 查看进程层次关系</span></span><br><span class="line">$ ps -ejH</span><br><span class="line">$ ps axjf</span><br></pre></td></tr></table></figure>

<h3 id="valgrind"><a href="#valgrind" class="headerlink" title="valgrind"></a><span id="grep">valgrind</span></h3><blockquote>
<p><a href="https://www.valgrind.org/docs/manual/manual-core.html#manual-core.report" target="_blank" rel="noopener">使用手册</a></p>
</blockquote>
<h4 id="用法：valgrind-VALGRIND-OPTIONS-programing-PROG-OPTIONS"><a href="#用法：valgrind-VALGRIND-OPTIONS-programing-PROG-OPTIONS" class="headerlink" title="用法：valgrind [VALGRIND_OPTIONS] programing [PROG_OPTIONS]"></a>用法：valgrind [VALGRIND_OPTIONS] programing [PROG_OPTIONS]</h4><h5 id="基本选项"><a href="#基本选项" class="headerlink" title="基本选项"></a>基本选项</h5><ul>
<li><p><code>-h</code>：帮助</p>
</li>
<li><p><code>-q</code>：静默模式</p>
</li>
<li><p><code>-v</code>：输出额外的信息，如加载的共享对象，检测和执行引擎进度，和异常行为警告，重复选项会增加详细程序</p>
<h4 id="要点"><a href="#要点" class="headerlink" title="要点"></a>要点</h4><h5 id="开始编译"><a href="#开始编译" class="headerlink" title="开始编译"></a>开始编译</h5></li>
<li><p>使用<code>-g</code>有助于获得直接指向源代码行的消息</p>
</li>
<li><p>如果使用C++，使用<code>-fno-inline</code>可以轻松看到函数调用链，或者使用valgrind选项<code>--read-inline-info=yes</code>让valgrind读取内联信息</p>
</li>
<li><p>开启<code>-O1</code>以上程序优化会导致memcheck误报未定义等错误，尽量不优化，且开启<code>-Wall</code></p>
<h5 id="注释输出"><a href="#注释输出" class="headerlink" title="注释输出"></a>注释输出</h5></li>
<li><p>默认valgrind将消息输出文件描述符2（stderr），如果要写入其他文件描述符，可以使用<code>--log-fd=&lt;n&gt;</code>来指定fd减少干扰项</p>
</li>
<li><p>为了减少干扰项，最好通过<code>-log-file=&lt;filename&gt;</code>将调试日志写到指定文件</p>
</li>
<li><p>最少干扰是通过<code>--log-socket=&lt;ip:port&gt;</code>指定网络套接字进行输出，可以省略端口号，将默认使用端口1500，默认值由<code>VG_CLO_DEFAULT_LOGPORT</code>定义。而valgrind监听端则使用<code>valgrind-listener</code>来进行监听，连接的切断listener并不会杀死valgrind，而是会自动切换回写入stderr</p>
<ul>
<li><code>valgrind-listener</code>接受多大50个valgrinded进程同时连接，它接收三个选项：<ul>
<li><code>-e --exit-at-zero</code>：当连接进程数为0时退出，否则将永远运行</li>
<li><code>--max-connect=&lt;n&gt;</code>：进程上限，默认50，可以自动设限</li>
<li><code>portnumber</code>：更改默认端口（1500）</li>
</ul>
</li>
</ul>
</li>
<li><p>检测重复错误会有大量开销，valgrind会在发现1000个不同的错误或总共发现10000000个错误后停止错误收集，为了避免中断，可以使用<code>--error-limit=no</code>选项</p>
</li>
<li><p>错误检查工具会检测系统中预装系统库的很多问题，如果不想看到这些错误，可以使用<code>--gen-suppressions=yes</code>选项进行消除。如果使用了<code>-v</code>选项，则在程序结束时打印已使用抑制行</p>
</li>
<li><p><code>--tool=&lt;toolname&gt;</code>：可以决定运行哪个valgrind工具。默认为<code>memcheck</code>，除此之外还有<code>cachegrind</code>、<code>callgrind</code>、<code>helgrind</code>、<code>drd</code>、<code>massif</code>、<code>dhat</code>、<code>bouchey</code>、<code>none</code>、<code>exp-sgcheck</code>、<code>exp-bbv</code>等</p>
</li>
</ul>
<h4 id="一些技巧记录"><a href="#一些技巧记录" class="headerlink" title="一些技巧记录"></a>一些技巧记录</h4><ul>
<li>显式本地IP：<code>curl myip.ipip.net</code></li>
<li>查找结构体：<code>grep -Rn --include=&quot;*.h&quot; --include=&quot;*.c&quot; &#39;struct ether_header&#39; /usr</code></li>
<li>查找宏定义：<code>grep -Rn --include=&quot;*.h&quot; --include=&quot;*.c&quot; &#39;#define PCAP_ERRBUF_SIZE&#39; /usr</code></li>
</ul>
]]></content>
      <categories>
        <category>2020.04</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>Debug</tag>
        <tag>Tool</tag>
      </tags>
  </entry>
  <entry>
    <title>第一章 关于对象</title>
    <url>/2020/06/12/%E7%AC%AC%E4%B8%80%E7%AB%A0-%E5%85%B3%E4%BA%8E%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<ul>
<li>C语言中，数据和函数是分开来声明的，也就是说，语言本身没有支持”数据和函数“之间的关联性，这种程序方法称为<font color="red">程序性的</font>（<font color="green">procedural</font>）。一般从C<br>struct转到C++的封装类后，唯一在布局以及存取时间上的负担是virtual引起的。</li>
</ul>
<h4 id="1-1-C-对象模式（The-C-Object-Model）"><a href="#1-1-C-对象模式（The-C-Object-Model）" class="headerlink" title="1.1 C++对象模式（The C++ Object Model）"></a>1.1 C++对象模式（The C++ Object Model）</h4><ul>
<li>C++中有<font color="red">两种class data members</font>：static和nonstatic，以及<font color="red">三种class member functions</font>：static、nonstatic和virtual。  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> xval);</span><br><span class="line">    <span class="keyword">virtual</span> ~Point();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">PointCount</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> ostream&amp; <span class="title">print</span><span class="params">(ostream &amp;os)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="keyword">float</span> _x;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> _point_count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h5 id="简单对象模型（A-Simple-Object-Model）"><a href="#简单对象模型（A-Simple-Object-Model）" class="headerlink" title="简单对象模型（A Simple Object Model）"></a>简单对象模型（A Simple Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">slot[Point pt] ==&gt; m[all members]</span><br></pre></td></tr></table></figure>

<ul>
<li>这个模型简单，一个object是一系列的slots，members按照声明次序各被指定一个slot。一个class object的大小是”指针大小*members个数“。<ul>
<li>这个模型<font color="red">应用</font>到C++的”<font color="red">指向成员的指针</font>“（<font color="green">pointer-to-member</font>）观念中。</li>
</ul>
</li>
</ul>
<h5 id="表格驱动对象模型（A-Table-driven-Object-Model）"><a href="#表格驱动对象模型（A-Table-driven-Object-Model）" class="headerlink" title="表格驱动对象模型（A Table-driven Object Model）"></a>表格驱动对象模型（A Table-driven Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">S[Point pt] == data member table ==&gt; e[内含实际数据]</span><br><span class="line">S == member function table ==&gt; m[内含函数地址]</span><br><span class="line">m ==&gt; a[...]</span><br><span class="line">m ==&gt; b[...]</span><br></pre></td></tr></table></figure>

<ul>
<li>这种模型把所有与members相关的信息抽出来，放在一个<strong>data member table</strong>和一个<strong>member function table</strong>之中，class object则包含指向这两个表格的指针。member function table是一系列slots，一个slot一个member function；data member table则直接含有data本身。</li>
<li>这个模型member function table这个观念称为支持<font color="red">virtual functions</font>的一个有效方案</li>
</ul>
<h5 id="C-对象模型（The-C-Object-Model）"><a href="#C-对象模型（The-C-Object-Model）" class="headerlink" title="C++对象模型（The C++ Object Model）"></a>C++对象模型（The C++ Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">e1(static data members)</span><br><span class="line">e2(static function members)</span><br><span class="line">e3(nonstatic function members)</span><br><span class="line">Point[nonstatic data members] == Virtual table for Point ==&gt; vptr[vtbl]</span><br><span class="line">vptr == RTTI ==&gt; ti[type_info for Point]</span><br><span class="line">vptr ==&gt; a[...]</span><br><span class="line">vptr ==&gt; b[...]</span><br></pre></td></tr></table></figure>

<ul>
<li>nonstatic data members被配置于每个class object之内，static data members则被存放在class object之外；</li>
<li>static和nonstatic function members也放在所有class object之外；</li>
<li>virtual functions以两个步骤支持它：<ul>
<li>每个class产生一堆指向virtual functions的指针，放在<font color="green">virtual table</font>（<font color="red">vtbl</font>）中;</li>
<li>每个class object被添加一个指针，指向vtbl，通常这个指针被称为<font color="red">vptr</font>。vptr的设定（setting）和重置（resetting）由每个class的<strong>constructor、destructor和copy assignment运算符</strong>自动完成。每个class所关联的<font color="red">type_info object</font>（用以支持<font color="green">runtime type identification</font>，<font color="red">RTTI</font>）也经由vtbl指出，通常放在表格第一个slot处。</li>
</ul>
</li>
<li>这个模型优点在于空间和存取时间的效率；缺点则是如果所用到的class objects的nonstatic data members有所改动，那么那些应用程序得重新编译。</li>
</ul>
<h5 id="加上继承（Adding-Inheritance）"><a href="#加上继承（Adding-Inheritance）" class="headerlink" title="加上继承（Adding Inheritance）"></a>加上继承（Adding Inheritance）</h5><ul>
<li>C++支持<font color="red">单一继承</font>、<font color="red">多继承</font>和<font color="red">虚拟继承</font>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">ostream --- ios</span><br><span class="line">istream --- ios</span><br><span class="line">iostream --- ostream</span><br><span class="line">iostream --- istream</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="对象模型如何影响程序"><a href="#对象模型如何影响程序" class="headerlink" title="对象模型如何影响程序"></a>对象模型如何影响程序</h5><ul>
<li>下面这个函数，其中class X定义一个coyp constructor，一个virtual destructor和一个virtual function foo：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">foobar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    X *px = <span class="keyword">new</span> X;</span><br><span class="line">    <span class="comment">// foo() is a virtual function</span></span><br><span class="line">    xx.foo();</span><br><span class="line">    px-&gt;foo();</span><br><span class="line">    <span class="keyword">delete</span> px;</span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这个函数可能在内部转化为：</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[0] --&gt; type_info object</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[1] --&gt; X::~X()</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[2] --&gt; X::foo()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foobar</span><span class="params">(X &amp;_result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    _result.X::X();</span><br><span class="line">    <span class="comment">// X *px = new X;</span></span><br><span class="line">    px = _new(<span class="keyword">sizeof</span>(x));</span><br><span class="line">    <span class="keyword">if</span> (px != <span class="number">0</span>)</span><br><span class="line">        px-&gt;X::X();</span><br><span class="line">    <span class="comment">// xx.foo()，不适用virtual机制</span></span><br><span class="line">    foo(&amp;_result);</span><br><span class="line">    <span class="comment">// virtaul机制扩展px-&gt;foo()</span></span><br><span class="line">    (*px-&gt;vtbl[<span class="number">2</span>])(px)</span><br><span class="line">    <span class="comment">// delete px</span></span><br><span class="line">    <span class="keyword">if</span> (px != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        (*px-&gt;vtbl[<span class="number">1</span>])(px); <span class="comment">// destructor</span></span><br><span class="line">        _delete(px);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 无须使用named return</span></span><br><span class="line">    statement</span><br><span class="line">    <span class="comment">// 无须摧毁local object xx</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="1-2-关键词所带来的差异（A-Keyword-Distinction）"><a href="#1-2-关键词所带来的差异（A-Keyword-Distinction）" class="headerlink" title="1.2 关键词所带来的差异（A Keyword Distinction）"></a>1.2 关键词所带来的差异（A Keyword Distinction）</h4><h5 id="关键词的困扰"><a href="#关键词的困扰" class="headerlink" title="关键词的困扰"></a>关键词的困扰</h5><ul>
<li><p>关键词struct的意思是一个数据集合体，没有private data，也没有data的相应操作。它和C++的<font color="red">使用者自定义类型</font>（<font color="green">user-defined type</font>）区分开来。struct关键词的使用实现了C的<font colo="red">数据抽象观念</font>，而class关键词实现的是C++的<font color="red">ADT</font>（<font color="grren">Abstract Data Type</font>）观念。</p>
<h5 id="策略性正确的struct（The-Politically-Correct-Struct）"><a href="#策略性正确的struct（The-Politically-Correct-Struct）" class="headerlink" title="策略性正确的struct（The Politically Correct Struct）"></a>策略性正确的struct（The Politically Correct Struct）</h5></li>
<li><p>C的巧计有时候是C++的陷阱。例如一个在struct尾端的单一数组，也是每个struct objects有可变长数据：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mutable</span> &#123;</span></span><br><span class="line">    <span class="comment">/* stuff */</span></span><br><span class="line">    <span class="keyword">char</span> pc[<span class="number">1</span>];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 为struct本身和该字符串配置足够的内存</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mumble</span> *<span class="title">pmumb1</span> = (<span class="title">struct</span> <span class="title">memble</span>*)<span class="title">malloc</span>(<span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">mumble</span>) + <span class="title">strlen</span>(<span class="title">string</span>) + 1);</span></span><br><span class="line"><span class="built_in">strcpy</span>(&amp;mumble.pc, <span class="built_in">string</span>);</span><br></pre></td></tr></table></figure>

<p>  让C++ class的部分数据拥有C声明那模样，是将那部分抽取出来成为独立的struct声明，然后<font color="red">组合</font>在一起，它能保证拥有与C兼容的空间布局：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">C_Point</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">C_point</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _c_point; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    C_point _c_point;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="1-3-对象的差异（An-Object-Distinction）"><a href="#1-3-对象的差异（An-Object-Distinction）" class="headerlink" title="1.3 对象的差异（An Object Distinction）"></a>1.3 对象的差异（An Object Distinction）</h4><ul>
<li><p>C++有以下方法支持<font color="red"><strong>多态</strong></font>：</p>
<ul>
<li><font color="red">隐式转化操作</font>。（<code>Shape *ps = new Circle()</code>）</li>
<li><font color="red">经由virtual function机制</font>。（<code>ps-&gt;rotate();</code>）</li>
<li><font color="red">经由dynamic_cast和typeid操作符</font>。（<code>if（Circle \*pc = dynamic\_cast&lt;Circle\*&gt;(ps))</code>）</li>
</ul>
</li>
<li><p>需要多少内存才能够表现一个<font color="red"><strong>class object大小</strong></font>？</p>
<ul>
<li><strong>nonstatic data members的总和大小</strong>。</li>
<li><strong>根据alignment需求padding的空间</strong>。（alignment是将数值调整到某数的倍数。32位机器上位4 bytes，64位机器上位8bytes）。</li>
<li><strong>支持virtual而内部产生的额外负担（overhead）</strong>。<h5 id="指针的类型（The-Type-of-a-Pointer）"><a href="#指针的类型（The-Type-of-a-Pointer）" class="headerlink" title="指针的类型（The Type of a Pointer）"></a>指针的类型（The Type of a Pointer）</h5></li>
</ul>
</li>
<li><p>一个指向ZooAnimal的指针与一个指向整数的指针或template Array的指针在内存上没什么区别。真正的不同是其所寻址出来的<font color="red"><strong>object类型</strong></font>不同，因为<font color="red">指针类型会教导编译器如何解释某个地址中的内存内容及其大小</font>。所以，这就是为什么void<em>能持有一个地址，但不同操作该地址的原因了。而<font color="red">转换</font>（<font color="green">cast</font>）也是一种编译器指令，*</em>它不改变地址，只影响内存大小和内容的解释方式**。</p>
<h5 id="加上多态之后（Adding-Polymorphism）"><a href="#加上多态之后（Adding-Polymorphism）" class="headerlink" title="加上多态之后（Adding Polymorphism）"></a>加上多态之后（Adding Polymorphism）</h5></li>
<li><p>定义一个Bear，作为一种ZooAnimal：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">ZooAnimal --- Bear</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bear</span> :</span> <span class="keyword">public</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Bear();</span><br><span class="line">    ~Bear();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Rotate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">dance</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">enum</span> Dances &#123; ... &#125;;</span><br><span class="line">    Dances dances_known;</span><br><span class="line">    <span class="keyword">int</span> cell_block;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Bear b;</span><br><span class="line">ZooAnimal *pz = &amp;b;</span><br><span class="line">Bear *pb = &amp;b;</span><br></pre></td></tr></table></figure>

<p>  两个对象都同指Bear object的第一个byte。区别是pb的地址包含Bear object，而pz的地址包含Bear Object中的ZooAnimal subobject。除非ZooAnimal subobject出现的members，否则不能使用pz直接处理Bear的members，唯一例外是virtual机制：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// error. cell_block不是ZooAnimal的member</span></span><br><span class="line">pz-&gt;cell_block;</span><br><span class="line"><span class="comment">// ok</span></span><br><span class="line">(<span class="keyword">static_cast</span>&lt;Bear*&gt;(pz))-&gt;cell_block;</span><br><span class="line"><span class="comment">// better</span></span><br><span class="line"><span class="keyword">if</span> (Bear *pb2 = <span class="keyword">dynamic_cast</span>&lt;Bear*&gt;(pz))</span><br><span class="line">    pb2-&gt;cell_block;</span><br><span class="line"><span class="comment">// ok</span></span><br><span class="line">pb-&gt;cell_block</span><br></pre></td></tr></table></figure>

<p>  当写下<code>pz-&gt;rotate();</code>时，pz的类型在编译期决定了两点：</p>
<ul>
<li><strong>固定的可用接口</strong>。就是说，pz只能调用ZooAnimal的public接口</li>
<li><strong>该接口的access level</strong>（例如ratate()时ZooAnimal的public member）。<br>每一个执行点，pz指的object类型决定rotate()调用的实例。<font color="red"><strong>类型信息的封装维护于object vptr和vtbl之间的link之中。</strong></font></li>
</ul>
</li>
<li><p><font color="red"><strong>一个pointer或一个reference之所以支持多态，是因为它们不引发内存中任何于类型有关的内存委托操作（type-dependent commitment），会受到改变的只有指向内存的大小和内容解释方式而已。</strong></font>当一个base class object被直接初始化（或被指定为）一个derived class object时，derived object就会被<font color="red">切割</font>（<font color="green">sliced</font>）以塞入较小的base type内存中，derived type将没有留下任何蛛丝马迹，因为配置所得的内存有限。</p>
</li>
<li><p>C++支持具体的ADT程序风格，被称为<font color="red">object-based</font>（<font color="red">OB</font>）。一个OB设计可能比一个对等oo设计速度更快而且空间更紧凑，因为所有函数调用操作在编译时期解析完成，对象构建起来不再设置virtual机制，而且空间紧凑，缺点是OB设计比较没有弹性。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>pthread_N_IPC </title>
    <url>/2019/06/30/pthread-N-IPC/</url>
    <content><![CDATA[<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// IEEE POSIX头文件</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;    // POSIX线程</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;  // 信号量</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/sem.h&gt;    // 有名信号量(system v)</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;   // 进程控制</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;     // 符号常量</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;  // 基本系统数据类型</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;      // 文件控制</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/stat.h&gt;   // 文件状态</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/ipc.h&gt;    // IPC</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/shm.h&gt;    // XSI共享存储</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;    // XSI 消息队列</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ISO C</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;     // 验证程序断言</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;      // 出错码</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;      // 标准I/O库</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;     // 实用函数</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;     // 信号</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// I/O</span></span><br><span class="line"></span><br><span class="line">STDIN_FILENO <span class="comment">// fd 0</span></span><br><span class="line">STDOUT_FILENO<span class="comment">// fd 1</span></span><br><span class="line">STDERR_FILENO<span class="comment">// fd 2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// read more to "Chapter_three.md"</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建线程标识</span></span><br><span class="line"><span class="keyword">pthread_t</span> thread_id;</span><br><span class="line"><span class="comment">// 创建互斥量</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> mutex = PTHREAD_MUTEX_INITIALIZER; <span class="comment">// static macro init</span></span><br><span class="line"><span class="comment">// 创建信号量</span></span><br><span class="line"><span class="keyword">sem_t</span> sem;</span><br><span class="line"><span class="comment">// 创建条件变量</span></span><br><span class="line"><span class="keyword">pthread_cond_t</span> cond = PTHREAD_COND_INITIALIZER <span class="comment">// static macro init</span></span><br><span class="line"><span class="comment">// 创建消息队列</span></span><br><span class="line"><span class="keyword">mqd_t</span> mqd;</span><br><span class="line"><span class="comment">// 创建读写锁</span></span><br><span class="line"><span class="keyword">pthread_rwlock_t</span> rwlock;</span><br><span class="line"><span class="comment">// 创建自旋锁</span></span><br><span class="line"><span class="keyword">pthread_spinlock_t</span> spinlock;</span><br><span class="line"><span class="comment">// 创建屏障</span></span><br><span class="line"><span class="keyword">pthread_barrier_t</span> barrier;</span><br><span class="line"><span class="comment">// 创建文件锁对象</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">flck</span>;</span></span><br><span class="line"><span class="comment">// 创建共享内存指针</span></span><br><span class="line"><span class="keyword">caddr_t</span> memptr;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性</span></span><br><span class="line"><span class="keyword">pthread_attr_t</span> pat;</span><br><span class="line"><span class="comment">// 互斥量属性</span></span><br><span class="line"><span class="keyword">pthread_mutexattr_t</span> pmt;</span><br><span class="line"><span class="comment">// 读写锁属性(唯一属性是进程共享属性)</span></span><br><span class="line"><span class="keyword">pthread_rwlockattr_t</span> prt;</span><br><span class="line"><span class="comment">// 条件变量属性</span></span><br><span class="line"><span class="keyword">pthread_condattr_t</span> pct;</span><br><span class="line"><span class="comment">// 栅栏属性(目前只有进程共享属性)</span></span><br><span class="line"><span class="keyword">pthread_barrierattr_t</span> pbt;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 线程</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_create</span><span class="params">(<span class="keyword">pthread_t</span> *thread,      </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> <span class="keyword">pthread_att_t</span> *attr,    </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> *(*start_routine)(<span class="keyword">void</span> *),</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> *arg)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_create(thread_id, NULL, (void *)func, (void *)&amp;arg);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 加入线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_join</span><span class="params">(<span class="keyword">pthread_t</span> thread, </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> **retval)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. error = pthread_join(pid, (void *)&amp;err_func);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程ID的比较</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_equal</span><span class="params">(<span class="keyword">pthread_t</span> t1, <span class="keyword">pthread_t</span> t2)</span></span>;</span><br><span class="line"><span class="comment">// success return non-zero value; otherwise, return 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程的取消</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cancel</span><span class="params">(<span class="keyword">pthread_t</span> thread)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; othwrwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 分离线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_detach</span><span class="params">(<span class="keyword">pthread_t</span> thread)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, reutrn error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 退出线程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pthread_exit</span><span class="params">(<span class="keyword">void</span> *retval)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号给线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_kill</span><span class="params">(<span class="keyword">pthread_t</span> thread, <span class="keyword">int</span> sig)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number and no signal to send</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得目前线程标识</span></span><br><span class="line"><span class="keyword">pthread_t</span> pthread_self(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// always succedds, return the calling thread's ID</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程让步</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_yield</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, reutrn error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// pthread接口允许通过设置每个对象关联的不同属性来细调线程和同步对象的行为</span></span><br><span class="line"><span class="comment">// e.g. 线程栈:虚地址空间大小是固定的,因为进程中只有一个栈.而它被所有线程栈共享.</span></span><br><span class="line"><span class="comment">// e.g. 如果许多线程的线程栈累计超过可用的虚地址空间,就要减少栈大小.如果调用函数涉及很深的栈帧就要调大</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_init</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性销毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_destroy</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 注册fork句柄??</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_atfork</span><span class="params">(<span class="keyword">void</span> (*prepare)(<span class="keyword">void</span>), </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> (*parent)(<span class="keyword">void</span>),</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> (*child)(<span class="keyword">void</span>))</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"><span class="comment">// e.g. 可以用来解决线程与fork的问题,比如用来清除锁状态,通过调用此函数建立fork处理程序</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈最小长度(字节数)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setstacksize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> stacksize)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getstacksize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> *stacksize)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈的分离状态属性</span></span><br><span class="line"><span class="comment">// PTHREAD_CREATE_JOINABLE(非分离状态) | PTHREAD_CREATE_JOINABLE(分离状态)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setdetachstate</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">int</span> detachstate)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getdetachstate</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">int</span> *detachstate)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 非分离线程在终止后,必须有一个线程用join来等待他,否则会资源泄露</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈末尾的境界缓冲区大小(字节数)</span></span><br><span class="line"><span class="comment">// e.g. guardsize的值向上舍入为PAGESIZE的倍数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setguardsize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> guardsize)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getguardsize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> *guardsize)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 溢出保护会浪费系统资源,如果创建大量线程,并已知线程不会溢出,那么关闭溢出保护区可以节省系统资源</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈的最低地址</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setstackaddr</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">void</span> *stackaddr)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getstackaddr</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">void</span> **stackaddr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥引用对象的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_init</span><span class="params">(<span class="keyword">pthraed_mutex_t</span> *<span class="keyword">restrict</span> mutex,</span></span></span><br><span class="line"><span class="function"><span class="params">                    <span class="keyword">const</span> <span class="keyword">pthread_mutexattr_t</span> *<span class="keyword">restrict</span> attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthraed_mutex_init(&amp;mutex, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥引用对象的销毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_destroy</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量上锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_lock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_trylock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_unlock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量属性初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutexattr_init</span><span class="params">(<span class="keyword">pthread_mutexattr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 如果进程共享互斥量属性设置位PTHREAD_PROCESS_SHARED,多个进程之间的内存数据块互斥量就可以用于进程同步</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁也叫共享互斥锁.当读写锁是写加锁状态时,解锁前,所有试图对这个锁加锁的线程都会被阻塞.当读写锁是读加锁状态,所有试图读模式对它加锁的线程都可以得到访问权.</span></span><br><span class="line"><span class="comment">// e.g. 读写锁使用前必须初始化,释放内存前必须销毁</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_init</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> <span class="keyword">pthread_rwlockattr_t</span> *<span class="keyword">restrict</span> attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_rwlock_init(&amp;rwlock, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_destroy</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 锁上写锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_trywrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_wrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_timedwrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, </span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 带超时的锁获取锁失败将返回TIMEOUT</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 锁上读锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_rdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_tryrdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_timerdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock,</span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 带超时的锁获取锁失败将返回TIMEOUT</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_unlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程等待条件</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *<span class="keyword">restrict</span> cond,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_timedwait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *<span class="keyword">restrict</span> cond,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abstime)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_cond_wait(&amp;cond, &amp;mutex);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 单独发送condition</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 广播发送condition</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_broadcast</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁跟互斥锁很相似,都是为了解决资源互斥使用.区别是互斥锁相当于只有0/1状态的信号量,当资源被占用,资源申请者将被阻塞,而自旋锁不会引起睡眠,而是不断循环空转</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_init</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock, <span class="keyword">int</span> pshared)</span></span>;    </span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 单进程可以设置pshared参数为PTHREAD_PROCESS_SHARED</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁加锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_lock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_trylock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_unlock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_destroy</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障(barrier)是用户协调多个线程并行工作的同步机制.屏障允许每个线程等待,直到所有合作线程都达到某一点,然后从该点继续执行. (fence | 栅栏?)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障对象初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_init</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *<span class="keyword">restrict</span> barrier,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> <span class="keyword">pthread_barrierattr_t</span>   *<span class="keyword">restrict</span> attr, </span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">unsigned</span> count)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_barrier_init(&amp;barrier, NULL, thread_num);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障对象摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_destroy</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 在栅栏中同步</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_wait</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br><span class="line"><span class="comment">// success return PTHREAD_BARRIER_SERIAL_THREAD or 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 当调用pthraed_barrier_wait的线程在屏障技术count未满足条件时,进入休眠,直到满足所有吸线程被唤醒</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 基本类型</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 进程或进程组ID</span></span><br><span class="line"><span class="keyword">pid_t</span> pid;</span><br><span class="line"><span class="comment">// 对象(如字符串)长度(不带符号的)</span></span><br><span class="line"><span class="keyword">size_t</span> st;</span><br><span class="line"><span class="comment">// 返回字节计数的函数(带符号的)</span></span><br><span class="line"><span class="keyword">ssize_t</span> sst;</span><br><span class="line"><span class="comment">//</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">ELF文件信息</th>
</tr>
</thead>
<tbody><tr>
<td align="center">ELF头</td>
</tr>
<tr>
<td align="center">程序头表</td>
</tr>
<tr>
<td align="center">.init 节</td>
</tr>
<tr>
<td align="center">.text 节</td>
</tr>
<tr>
<td align="center">.rodata 节</td>
</tr>
<tr>
<td align="center">.data 节</td>
</tr>
<tr>
<td align="center">.bss 节</td>
</tr>
<tr>
<td align="center">.symtab 节</td>
</tr>
<tr>
<td align="center">.debug 节</td>
</tr>
<tr>
<td align="center">.line 节</td>
</tr>
<tr>
<td align="center">.strtab 节</td>
</tr>
<tr>
<td align="center">节头表</td>
</tr>
<tr>
<td align="center"><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 进程</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 概念</span></span><br><span class="line"><span class="comment">// 孤儿进程:fork编程时,对于父进程已经终止的所有进程,它们的父进程都改变为init进程</span></span><br><span class="line"><span class="comment">// 僵尸进程:fork编程时,如果子进程先于父进程退出,就会处于终止状态,kernel为每个子进程保留了一定的信息(进程ID\终止状态\使用CPU事件总量等).</span></span><br><span class="line"><span class="comment">//      当父进程调用wait或waitip时得到这些信息,并将终止子进程从系统进程表删除,释放终止状态.如果没有获取子进程终止状态,进程表信息不会被移除,那么这些进程就会变成进程表中的僵尸进程.</span></span><br><span class="line"><span class="comment">// 预防措施有两大类型:</span></span><br><span class="line"><span class="comment">// 第一种是让父进程去通知系统,告诉系统它对子进程的终止没有任何兴趣:</span></span><br><span class="line">signal(SIGCHLD, SIG_IGN);   <span class="comment">// in parent: ignore notification</span></span><br><span class="line"><span class="comment">// 第二种是在子进程终止时,让父进程执行一个wait.等待子进程终止</span></span><br><span class="line">wait(<span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure></td>
</tr>
</tbody></table>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得目前进程标识</span></span><br><span class="line"><span class="keyword">pid_t</span> getpid(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// 取得目前进程父进程标识</span></span><br><span class="line"><span class="keyword">pid_t</span> getppid(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// 从已存在的进程中创建新进程</span></span><br><span class="line"><span class="keyword">pid_t</span> fort(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">    On success, the PID of the child process is returned  in  the  parent, </span></span><br><span class="line"><span class="comment">    and  0  is returned  in the child.  </span></span><br><span class="line"><span class="comment">    On failure, -1 is returned in the parent,</span></span><br><span class="line"><span class="comment">    no child process is created, and errno is set appropriately.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行程序</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[], <span class="keyword">char</span> *envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execl</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ...)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execve</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">char</span> <span class="keyword">const</span> *arg[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execle</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ..., <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execv</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">char</span> *<span class="keyword">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execlp</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ...)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execvp</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">char</span> *<span class="keyword">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execvpe</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">char</span> *<span class="keyword">const</span> argv[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="comment">// error return -1</span></span><br><span class="line"><span class="comment">// e.g. execve函数的功能是在当前进程上下文中加载并运行一个新程序</span></span><br><span class="line"><span class="comment">// p=filename | l=list | v=vector | e=environment</span></span><br><span class="line"><span class="comment">// e.g. execlp("ps", "ps", "-ef", NULL); // artv[0] = ps</span></span><br><span class="line"><span class="comment">/* e.g. </span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">    char *argv[] = &#123;"ps", "-ef", NULL);</span></span><br><span class="line"><span class="comment">    execvp("ps", argv);</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 终止进程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">exit</span><span class="params">(<span class="keyword">int</span> status)</span></span>;</span><br><span class="line"><span class="keyword">void</span> _exit(<span class="keyword">int</span> status);</span><br><span class="line"><span class="comment">// e.g. exit()与_exit()的区别在于,前者在调用exit系统调用前,还会调用退出处理函数和清除I/O缓冲</span></span><br><span class="line"><span class="comment">// e.g. 可以在父进程中利用wait(&amp;status)来提取执行完的终止状态status并分析</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待子进程停止或中断</span></span><br><span class="line"><span class="keyword">pid_t</span> wait(<span class="keyword">int</span> *stat_loc);</span><br><span class="line"><span class="keyword">pid_t</span> waitpid(<span class="keyword">pid_t</span> pid, </span><br><span class="line">            <span class="keyword">int</span> *stat_loc, </span><br><span class="line">            <span class="keyword">int</span> options);   </span><br><span class="line"><span class="comment">// using WNOHANG and no sub process exit return 0, otherwise, success return &gt;0 and error return -1</span></span><br><span class="line"><span class="comment">// e.g. pr = waitpid(status, NULL, WNOHANG);</span></span><br><span class="line"><span class="comment">// WNOHANG: 如果没有结束的子进程马上返回,不等待;</span></span><br><span class="line"><span class="comment">// WUNTRACED: 如果子进程暂行则返回,结束状态不理会</span></span><br><span class="line"><span class="comment">// 0: 同wait(),阻塞父进程,直到退出</span></span><br><span class="line"><span class="comment">// wait macros evaluate</span></span><br><span class="line">WIFEXITED(stat_val);    <span class="comment">// wait if exited </span></span><br><span class="line"><span class="comment">// evaluate to a non-zero for a child process</span></span><br><span class="line">WEXITSTATUS(stat_val);  <span class="comment">// wait exit status</span></span><br><span class="line"><span class="comment">// if WIFEXITED is non-zero, evaluate to the status argument that the child process passed to _exit() or exit() or return from main().</span></span><br><span class="line"><span class="comment">// e.g. if (WIFEXITED(status)) status = WEXITSTATUS(status);</span></span><br><span class="line">WIFSIGNALED(stat_val);  <span class="comment">// wait if signaled</span></span><br><span class="line">WTERMSIG(stat_val);     <span class="comment">// evaluate to the wait WIFISNALED number of the signal</span></span><br><span class="line">WIFSTOPPED(stat_val);   <span class="comment">// wait if stopped</span></span><br><span class="line">WSTOPSIG(stat_val);     <span class="comment">// evaluate to the WIFSTOPPED nmber of the signal</span></span><br><span class="line">WIFCONTINUED(stat_val); <span class="comment">// wait if continued, evaluate to a non-zero value if status has continued from a job control stop.</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 进程间通道</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建管道</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pipe</span><span class="params">(<span class="keyword">int</span> fildes[<span class="number">2</span>])</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. 管道主要是用于不同进程间的通信,通常是先创建管道再fork一个子进程,因为子进程会继承父进程创建的管道</span></span><br><span class="line"><span class="comment">// e.g. 管道是单工工作方式,要么读要么写.虽然父子进程都有读写端,但只能使用一个,因此需要把不适用的读或写端文件描述符关闭.</span></span><br><span class="line"><span class="comment">// e.g. pipe读端存在时,写入数据才有意义,否则向pipe写入数据的进程会受到kernel传来的SIGPIPE signal(Broken Pipea error norrmally)</span></span><br><span class="line"><span class="comment">// e.g. 写数据时,不保证写入原子性,只要缓冲区有空间,就会尝试写,如果缓冲区满,写操作就会阻塞</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建FIFO有名管道</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mkfifo</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *pathname, <span class="keyword">mode_t</span> mode)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. mkfifo("fifofile", 0644);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kill</span><span class="params">(<span class="keyword">pid_t</span> pid, <span class="keyword">int</span> sig)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">raise</span><span class="params">(<span class="keyword">int</span> sig)</span></span>; <span class="comment">// 向自身进程发起 相当于kill(getpid(), sig);</span></span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line">pid = <span class="number">0</span>+;   <span class="comment">// 发送信号给pid</span></span><br><span class="line">pid = <span class="number">0</span>;    <span class="comment">// 发送信号给当前进程同进程组的进程</span></span><br><span class="line">pid = <span class="number">-1</span>;   <span class="comment">// 发送信号给所有进程表中的进程</span></span><br><span class="line">pid = <span class="number">-1</span>-   <span class="comment">// 发送信号给进程组号为-pid的每个进程</span></span><br><span class="line">SIGINT      <span class="comment">// Ctrl + c</span></span><br><span class="line">SIGQUIT     <span class="comment">// Ctrl + \</span></span><br><span class="line">SIGKILL     <span class="comment">// 结束程序不能阻塞、处理和忽略</span></span><br><span class="line">SIGALARM    <span class="comment">// 当定时器到时发出</span></span><br><span class="line">SIGSTOP     <span class="comment">// 暂停进程,同SIGKILL不能忽视</span></span><br><span class="line">SIGTSTP     <span class="comment">// Ctrl + z</span></span><br><span class="line"><span class="comment">// man 7 signal</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置一个定时器</span></span><br><span class="line"><span class="keyword">unsigned</span> alarm(<span class="keyword">unsigned</span> seconds);</span><br><span class="line"><span class="comment">// success return SIGALRM signal. otherwise, return 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 挂起进程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pause</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号至处理函数</span></span><br><span class="line"><span class="keyword">void</span> (*signal(<span class="keyword">int</span> sig, <span class="keyword">void</span> (*func)(<span class="keyword">int</span>)))(<span class="keyword">int</span>);</span><br><span class="line"><span class="comment">// e.g. void exec_handler(int signo);</span></span><br><span class="line"><span class="comment">// e.g. signal(SIGINT, exec_handler); // 通过signo来判断状态</span></span><br><span class="line"><span class="comment">// e.g. signal(SIGTSTP, SIG_IGN); // 忽略改信号</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查或修改信号的处理动作(action)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sigaction</span><span class="params">(<span class="keyword">int</span> sig, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">const</span> struct sigaction *<span class="keyword">restrict</span> act,</span></span></span><br><span class="line"><span class="function"><span class="params">              struct sigaction *<span class="keyword">restrict</span> oact)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise return -1 and set errno</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// POSIX IPC</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mq_attr</span> &#123;</span></span><br><span class="line">	<span class="keyword">long</span> mq_flags;       <span class="comment">/* Flags: 0 or O_NONBLOCK */</span></span><br><span class="line">	<span class="keyword">long</span> mq_maxmsg;      <span class="comment">/* Max. # of messages on queue */</span></span><br><span class="line">	<span class="keyword">long</span> mq_msgsize;     <span class="comment">/* Max. message size (bytes) */</span></span><br><span class="line">	<span class="keyword">long</span> mq_curmsgs;     <span class="comment">/* # of messages currently in queue */</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 共享文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// flock结构体的初始化 e.g. </span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">lock</span>;</span>          </span><br><span class="line">lock.l_type = F_WRLCK;      <span class="comment">// read/write lock</span></span><br><span class="line">lock.l_whence = SEEK_SET;   <span class="comment">// base for seek offsets</span></span><br><span class="line">lock.l_start = <span class="number">0</span>;           </span><br><span class="line">lock.l_len = <span class="number">0</span>;             <span class="comment">// 0 here means until EOF</span></span><br><span class="line">lock.l_pid;                 <span class="comment">// proces id </span></span><br><span class="line"><span class="comment">// 文件控制</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fcntl</span><span class="params">(<span class="keyword">int</span> fildes, <span class="keyword">int</span> cmd, ...)</span></span>;</span><br><span class="line"><span class="comment">// Upon successful completion, the value returned shall depend on cmd as follows</span></span><br><span class="line"><span class="comment">// e.g. struct flock lock; fcntl(fd, F_SETLK, &amp;lock);</span></span><br><span class="line">F_DUPFD     <span class="comment">// return a new file descriptor</span></span><br><span class="line">F_GETFD     <span class="comment">// get the file descriptor flags</span></span><br><span class="line">F_SETFD     <span class="comment">// set the file descriptor flags</span></span><br><span class="line">F_GETFL     <span class="comment">// get the file status flags and file access modes</span></span><br><span class="line">F_SETFL     <span class="comment">// set the file status flags</span></span><br><span class="line">F_GETOWN    <span class="comment">// if fildes refers to a socket, get the process or process group ID specified to receive SIGURG signals when out-of-band data is available  </span></span><br><span class="line">F_SETOWN    <span class="comment">// set</span></span><br><span class="line">F_GETLK     <span class="comment">// get the lock bolcks description point to by the third argument</span></span><br><span class="line">F_SETLK     <span class="comment">// set or clear file segment </span></span><br><span class="line">F_SETLKW    <span class="comment">// wait setlk</span></span><br><span class="line">F_UNLCK     </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// message queue</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mqueue.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开消息队列</span></span><br><span class="line"><span class="keyword">mqd_t</span> mq_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag);</span><br><span class="line"><span class="keyword">mqd_t</span> mq_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, </span><br><span class="line">            <span class="keyword">mode_t</span> mode, struct mq_attr *attr);</span><br><span class="line"><span class="comment">// success return message queue descriptor. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mqd_t mqd = mq_open("/temp.mq", O_RDWR);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 关闭消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_close</span><span class="params">(<span class="keyword">mqd_t</span> mqdes)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置消息队列属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_setattr</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, struct mq_attr *newattr, struct mq_attr *oldattr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_getattr("/temp.mq", &amp;attr);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取消息队列属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_getattr</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, struct mq_attr *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送消息到消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_send</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> <span class="keyword">char</span> *msg_ptr,</span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> msg_prio)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_timedsend</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> <span class="keyword">char</span> *msg_ptr,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> msg_prio,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> struct timespec *abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_send的prio优先级必须小于MQ_PRIO_MAX</span></span><br><span class="line"><span class="comment">// e.g. mq_send("/temp.mq", "hello", 5, 1);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收消息从消息队列</span></span><br><span class="line"><span class="keyword">ssize_t</span> mq_receive(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">char</span> *msg_ptr,</span><br><span class="line">                <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> *msg_prio);</span><br><span class="line"><span class="keyword">ssize_t</span> mq_timedreceive(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">char</span> *msg_ptr,</span><br><span class="line">                    <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> *msg_prio,</span><br><span class="line">                    <span class="keyword">const</span> struct timespec *abs_timeout);</span><br><span class="line"><span class="comment">// success retunr number of bytes. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_receive("/temp.mq", buf, buf_len, &amp;msg_prio);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 当消息有效时通知（异步事件）</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_notify</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> struct sigevent *sevp)</span></span>;</span><br><span class="line"><span class="comment">// semaphore</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化和打开有名信号量</span></span><br><span class="line"><span class="keyword">sem_t</span> *sem_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag);</span><br><span class="line"><span class="keyword">sem_t</span> *sem_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, <span class="keyword">mode_t</span> mode, <span class="keyword">unsigned</span> <span class="keyword">int</span> value);</span><br><span class="line"><span class="comment">// success return address of the new semaphore; otherwise, return SEM_FAILED, and set errno</span></span><br><span class="line"><span class="comment">// e.g. sem_t *semptr = sem_open(SemaphoreName, O_CREAT, 0644, 0);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 有名信号量退出</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_close</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 有名信号量移除</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 无名信号量初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_init</span><span class="params">(<span class="keyword">sem_t</span> *sem, <span class="keyword">int</span> pshared, <span class="keyword">unsigned</span> <span class="keyword">int</span> value)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. sem_init(&amp;sem, 0, 1); init 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 销毁一个无名信号量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_destroy</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_wait</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量尝试P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_trywait</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量带超时的P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_timedwait</span><span class="params">(<span class="keyword">sem_t</span> *sem,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> struct timespec *abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量V操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_post</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量值的获取</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_getvalue</span><span class="params">(<span class="keyword">sem_t</span> *sem, <span class="keyword">int</span> *sval)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise , return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// shared memory segment</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开POSIX共享内存对象</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shm_open</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, <span class="keyword">mode_t</span> mode)</span></span>;</span><br><span class="line"><span class="comment">// success return a nonnegative file descriptor. otherwise, return -1.</span></span><br><span class="line"><span class="comment">// e.g. int fd = shm_open(filename, O_RDWR, 0644);</span></span><br><span class="line"><span class="comment">// e.g. 调用shm_open函数,此时,没有内存对象被分配.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 分离共享内存</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shm_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise , return -1</span></span><br><span class="line"><span class="comment">// e.g. shm_unlink(filename);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件截断到指定长度</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">truncate</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">off_t</span> length)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">ftruncate</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">off_t</span> length)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. ftruncate(fd, 512);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// </span></span><br><span class="line">fstat</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件或设备映射到内存</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mmap</span><span class="params">(<span class="keyword">void</span> *addr,      <span class="comment">// if addr is NULL, then the kernel chooses the address at which to create the mapping</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> length,  <span class="comment">// how many bytes</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> prot,       <span class="comment">// access protections</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> flags,      <span class="comment">// mapping visible to other processes</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> fd,         <span class="comment">// file descriptor</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">off_t</span> offset)</span></span>;  <span class="comment">// offset</span></span><br><span class="line"><span class="comment">// success return a pointer to the mapped area. otherwise, return MAP_FAILED and set errno</span></span><br><span class="line"><span class="comment">// e.g. caddr_t memptr = mmap(NULL, ByteSize, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);</span></span><br><span class="line"><span class="comment">// e.g. 将指定文件fd中偏移量offset开始的长度为length个字节的一块信息映射到虚拟空间中起始地址为addr，长度为length个字节的一块区域</span></span><br><span class="line"><span class="comment">//  prot(制定区域内页面的访问权限位,vm_area_struct中vm_flag字段)</span></span><br><span class="line">PROT_EXE    <span class="comment">// pages页由指令组成</span></span><br><span class="line">PROT_EXEC   <span class="comment">// pages may be executed(可执行)</span></span><br><span class="line">PROT_READ   <span class="comment">// pages may be read(可读)</span></span><br><span class="line">PROT_WRITE  <span class="comment">// pages may be written(可写)</span></span><br><span class="line">PROT_NONE   <span class="comment">// pages may not be accessed(区域内页面不能被访问)</span></span><br><span class="line"><span class="comment">// flags(制定所映射的对象的类型,vm_area_struct结构中vm_flag字段)</span></span><br><span class="line">MAP_SHARED  <span class="comment">// shared this mapping(共享对象，对应共享库文件中的信息)</span></span><br><span class="line">MAP_PRIVATE <span class="comment">// create a private copy-on-write mapping(私有对象，采用写时拷贝技术，对应可执行文件中只读代码区域（.init、.text、.rodata）和已初始化数据区域（.data）</span></span><br><span class="line">MAP_ANON    <span class="comment">// 请求0的页，对应内核创建的匿名文件，相应页框用0初始化并驻留内存</span></span><br><span class="line">MAP_PRIVATE|MAP_ANON<span class="comment">// 未初始化数据（.bss），堆和用户栈等对应区域</span></span><br><span class="line">            </span><br><span class="line">            </span><br><span class="line"><span class="comment">// 取消映射内存</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">munmap</span><span class="params">(<span class="keyword">void</span> *addr, <span class="keyword">size_t</span> length)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno(probably to EINVCAL).</span></span><br><span class="line"><span class="comment">// e.g. munmap(memptr, ByteSize);</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// system V IPC</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件名或对象标识符转换为System V IPC键</span></span><br><span class="line"><span class="keyword">key_t</span> ftok(<span class="keyword">const</span> <span class="keyword">char</span> *pathname, <span class="keyword">int</span> proj_id);</span><br><span class="line"><span class="comment">// success return the generated key_t value. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. key_t key = ftok(PathName, ProjectId); // pathname=queue.h, projectid=123</span></span><br><span class="line"><span class="comment">// e.g. IPC对象标识符与文件描述符不同.文件描述符为当前进程最小可用文件描述符数组下标,IPC对象是系统全局的流水号,归零循环分配使用</span></span><br><span class="line"><span class="comment">// e.g. 让多个进程访问某个特定IPC对象需要一个外部键,一个IPC对象与键管关联.</span></span><br><span class="line"><span class="comment">// e.g. path是个文件名,函数的操作是,取文件的stat结构的st_dev和st_ino部分值,然后与参数ID第八位结合生成键值.他有一个ipc_perm结构体</span></span><br><span class="line"><span class="comment">// e.g. key为IPC_PRIVATE</span></span><br><span class="line"><span class="comment">// e.g. IPC接口过于繁杂,且不适用通用文件系统,不能使用IO监控函数select及poll函数操作IPC,且缺少资源回收机制</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// message queue</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取XSI消息队列标识符</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgget</span><span class="params">(key-t key, <span class="keyword">int</span> msgflg)</span></span>;</span><br><span class="line"><span class="comment">// success return message queue identifier. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. int qid = mesgget(key, 0666 | IPC_CREAT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// XSI消息控制操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgctl</span><span class="params">(<span class="keyword">int</span> msqid, <span class="keyword">int</span> cmd, struct msqid_ds *buf)</span></span>;</span><br><span class="line"><span class="comment">// success IPC_STAT, IPC_SET, and IPC_RMID return 0. IPC_INFO or MSG_INFO operation returns the index of the highest used entry array recording. MSG_STAT operation return the identifier of the queue those index was given in msqid. otherwise return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgctl(qid, IPC_RMID, NULL);  // remove queue</span></span><br><span class="line"><span class="comment">// cmd</span></span><br><span class="line">IPC_STAT    <span class="comment">// copy information msqid into the msqid_ds</span></span><br><span class="line">IPC_SET     <span class="comment">// write the values of some members of the msqid_ds structure</span></span><br><span class="line">IPC_RMID    <span class="comment">// remove the message queue</span></span><br><span class="line">IPC_INFO    <span class="comment">// returns infomation about system-wide mq limits and parameters in the structure pointed to by buf.</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送消息</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgsnd</span><span class="params">(<span class="keyword">int</span> msgid, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">const</span> <span class="keyword">void</span> *msqp, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> msgsz, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> msgflg)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgsnd(qid, &amp;msg, sizeof(msg), IPC_NOWAIT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收消息</span></span><br><span class="line"><span class="keyword">ssize_t</span> msgrcv(<span class="keyword">int</span> msgid, </span><br><span class="line">                <span class="keyword">void</span> *msgp, </span><br><span class="line">                <span class="keyword">size_t</span> msgsz, </span><br><span class="line">                <span class="keyword">long</span> msgyp, </span><br><span class="line">                <span class="keyword">int</span> msgflg);</span><br><span class="line"><span class="comment">// success return the number of bytes actually copied. otherwise return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgrcv(qid, &amp;msg, sizeof(msg), types[i], MSG_NOERROR | IPC_NOWAIT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// semaphore</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/sem.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取一个System V信号量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semget</span><span class="params">(<span class="keyword">key_t</span> key, <span class="keyword">int</span> nsems, <span class="keyword">int</span> semflg)</span></span>;</span><br><span class="line"><span class="comment">// success return value the semaphore set identifier. otherwise return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量的控制操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semctl</span><span class="params">(<span class="keyword">int</span> semid, <span class="keyword">int</span> semnum, <span class="keyword">int</span> cmd, ...)</span></span>;</span><br><span class="line"><span class="comment">// failure return -1 and set errno. otherwise return the system call returns a nonnegative value depending on cmd as follows.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semop</span><span class="params">(<span class="keyword">int</span> semid, struct sembuf *sops, <span class="keyword">unsigned</span> nsops)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// shared memory segment</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开共享内存</span></span><br><span class="line"><span class="comment">// IPC_PRIVATE或ftok的返回 | 共享内存区大小 | 同open函数权限位,可8进制表示</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmget</span><span class="params">(<span class="keyword">key_t</span> key, <span class="keyword">size_t</span> size, <span class="keyword">int</span> shmflg)</span></span>;</span><br><span class="line"><span class="comment">// success return a non-negative shared memory identifier. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmid = shmget(IPC_PRIVATE, MSIZE, 0666);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存的控制操作</span></span><br><span class="line"><span class="comment">// 共享内存标识符 | IPC_STAT(获取对象属性)/IPCSET(设置对象属性)/IPC_RMID(删除对象) | 用以执行保存设置属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmctl</span><span class="params">(<span class="keyword">int</span> shmid, <span class="keyword">int</span> cmd, struct shmid_ds *buf)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmctl(shmid, IPC_RMID, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存附加操作</span></span><br><span class="line"><span class="comment">// 映射的共享内存标识符 | 将共享内存映射到地址(NULL系统自动完成) | 默认0,共享内存只读</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">shmat</span><span class="params">(<span class="keyword">int</span> shmid, <span class="keyword">const</span> <span class="keyword">void</span> *shmaddr, <span class="keyword">int</span> shmflg)</span></span>;</span><br><span class="line"><span class="comment">// success return data structure associated with the shmID and the segment's start address. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. char *shm_addr = shmat(shmid, NULL, 0); // error return (void *)-1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存分离操作</span></span><br><span class="line"><span class="comment">// 共享内存映射后地址</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmdt</span><span class="params">(<span class="keyword">const</span> <span class="keyword">void</span> *shmaddr)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 此函数仅用于共享内存区域和进程的地址空间分离, 并不删除共享内存本身.参数是调用shmat函数时的返回值.</span></span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmdt(shm_addr);</span></span><br></pre></td></tr></table></figure>

<p>异步IO、共享内存、内存映射文件、内存锁定、实时性时钟和定时器、备选调度策略、实时性信号、消息队列、信号量</p>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>unix</tag>
        <tag>APUE</tag>
        <tag>TLPI</tag>
      </tags>
  </entry>
  <entry>
    <title>第七章 站在对象模型的尖端</title>
    <url>/2020/06/12/%E7%AC%AC%E4%B8%83%E7%AB%A0-%E7%AB%99%E5%9C%A8%E5%AF%B9%E8%B1%A1%E6%A8%A1%E5%9E%8B%E7%9A%84%E5%B0%96%E7%AB%AF/</url>
    <content><![CDATA[<ul>
<li>有三个著名的C++语言扩充性质，它们都会影响C++对象。分别是<font color="red">template</font>、<font color="red">exception handing（<strong>EH</strong>）</font>和<font color="red">runtime type identification（<strong>RTTI</strong>）</font>（RTTI可以想象成EH的一个副作用）。</li>
</ul>
<h4 id="7-1-Template"><a href="#7-1-Template" class="headerlink" title="7.1 Template"></a>7.1 Template</h4><ul>
<li>template原本被视为是对container classes的支持，但现在成为STL的基础。它也被用于<strong>属性混合</strong>（如内存分配机制）或<strong>互斥（mutual exclusion）机制</strong>（使用于线程同步化控制）。它设置被用于<strong>template metaprograms</strong>技术：class expression templates将在编译时期而非执行期被评估（evaluated），因而带来效率提升。</li>
</ul>
<h5 id="Template的“实例化”行为（Template-Instantiation）"><a href="#Template的“实例化”行为（Template-Instantiation）" class="headerlink" title="Template的“实例化”行为（Template Instantiation）"></a>Template的“实例化”行为（Template Instantiation）</h5><ul>
<li><p>下面的template Point class：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">Type</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">enum</span> Status &#123; unallocated, normalized &#125;;</span><br><span class="line">    Point(Type x = <span class="number">0.0</span>, Type y = <span class="number">0.0</span>, Type z = <span class="number">0.0</span>);</span><br><span class="line">    ~Point();</span><br><span class="line">    <span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span>*, <span class="keyword">size_t</span>)</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> Piont&lt;Type&gt; *freeList;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> chunkSize;</span><br><span class="line">    Type _x, _y, _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  实际程序中，static data members并不可用，nested enum或enumerators也一样。它们每个<font color="red">只能通过template Point class的某个实例来存取或操作</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ok</span></span><br><span class="line">Point&lt;<span class="keyword">float</span>&gt;::Status s;</span><br><span class="line"><span class="comment">// error</span></span><br><span class="line">Point::Status s;</span><br></pre></td></tr></table></figure>

<p>  如果定义一个指针，指向特定实例：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point&lt;<span class="keyword">float</span>&gt; *ptr = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>  什么也没发生。<strong>因为指向class object的指针，本身并不是class object，编译器不需要直到与该class有关的任何members数据或object布局数据</strong>。如果不是pointer而是reference，又如何：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Point&lt;<span class="keyword">float</span>&gt; &amp;ref = <span class="number">0</span>;</span><br><span class="line"><span class="comment">// 内部扩展</span></span><br><span class="line">Point&lt;<span class="keyword">float</span>&gt; temporary(<span class="keyword">float</span>(<span class="number">0</span>));</span><br><span class="line"><span class="keyword">const</span> Point&lt;<span class="keyword">float</span>&gt; &amp;ref = temporary;</span><br></pre></td></tr></table></figure>

<p>  所以一个class object的定义，不论是由编译器暗中地做或是程序员显式地做，都会导致template class的实例化。也就是Point中的三个nonstatic members都会被绑定。然而member functions（未被使用过的）不应该被实例化。只有在需要时才实例化。但编译器不遵循这项要求，所以由使用者来主导<font color="red">实例化</font>（<font color="green">instantiation</font>），主要有两个原因：</p>
<ol>
<li><strong>空间和时间效率的考虑</strong>。</li>
<li><strong>尚未实现的机能</strong>。</li>
</ol>
</li>
</ul>
<h5 id="Template的错误报告（Error-Reporting-within-a-Template）"><a href="#Template的错误报告（Error-Reporting-within-a-Template）" class="headerlink" title="Template的错误报告（Error Reporting within a Template）"></a>Template的错误报告（Error Reporting within a Template）</h5><h5 id="Template中的名称决议法（Name-Resolution-within-a-Template）"><a href="#Template中的名称决议法（Name-Resolution-within-a-Template）" class="headerlink" title="Template中的名称决议法（Name Resolution within a Template）"></a>Template中的名称决议法（Name Resolution within a Template）</h5><ul>
<li><p>template的两种意义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// scope of the template definition</span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">double</span> <span class="title">foo</span><span class="params">(<span class="keyword">double</span>)</span></span>;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">type</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">ScopeRules</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">invariant</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        _member = foo(_val);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">type <span class="title">type_dependent</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> foo(_member);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> _val;</span><br><span class="line">    type _member;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// scope of the template declareation</span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">double</span> <span class="title">foo</span><span class="params">(<span class="keyword">double</span>)</span></span>;</span><br><span class="line"><span class="comment">// scope of the template instantiation</span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// ....</span></span><br><span class="line">ScopeFules&lt;<span class="keyword">int</span>&gt; sr0;</span><br></pre></td></tr></table></figure>

<p>  <strong>Template中，一个nonmember name的决议结果，是根据name的使用是否与实例化该template的参数类型有关而决定的。如果无关，就以scope of the template declareation决定name，否则以scope of the template instantiation决定name。</strong><br>  ScopeFules template中有两个foo()调用操作。它会调用foo(double)的版本，<strong>此外，_val的类型是int。因为函数的决议结果只和函数的原型（signature）有关，和函数的返回值没有关系</strong>。因此_member的类型不会影响哪一个foo()实例被选中。在scope中，只有一个foo()候选者。调用操作由scope of the template declareation决议。如果按以下方式：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">sr0.type_dependent();</span><br></pre></td></tr></table></figure>

<p>  它由scope of the template instantiation决议。它会调用int版本，被type把持。<br>  这意味着编译器必须保持两个scope contexts：</p>
<ol>
<li><strong>“scope of the template “，用于专注一般的template class</strong>。</li>
<li><strong>“scope of the template instantiation”，用于专注特定的实例</strong>。</li>
</ol>
</li>
</ul>
<h5 id="Member-Function的实例化行为（Member-Function-Instantiation）"><a href="#Member-Function的实例化行为（Member-Function-Instantiation）" class="headerlink" title="Member Function的实例化行为（Member Function Instantiation）"></a>Member Function的实例化行为（Member Function Instantiation）</h5><ul>
<li>对于template function的实例化（instantiation）。编译器提供了两个策略：一个是<font color="red">编译时期策略</font>，程序代码在program text file中备妥可用；另一个是<font color="red">链接时期策略</font>。</li>
<li>如果virtual function被实例化（instantiated），其实例化点紧跟在class的实例化点后。</li>
<li>template instantiation似乎拒绝全面自动化，虽然工作做对了，但产生出来的object files重新编译成本可能太高，以手动方式在个别object module中完成预先实例化操作（pre-instantiation）是唯一有效率的方法。</li>
</ul>
<h4 id="7-2-异常处理（Exception-Handing）"><a href="#7-2-异常处理（Exception-Handing）" class="headerlink" title="7.2 异常处理（Exception Handing）"></a>7.2 异常处理（Exception Handing）</h4><ul>
<li>想要支持exception handing，编译器的主要工作就是找出catch子句，以处理被抛出的exception。这需要<strong>追踪</strong>程序堆栈中的每个函数的目前作用区域，同时编译器需要提供某种<strong>查询</strong>exception objects的方法，以直到实际类型（RTTI）。还需要某种机制<strong>管理</strong>被抛出的object，包括产生、存储、析构、清理和一般存取。</li>
</ul>
<h5 id="Exception-Handing快速检阅"><a href="#Exception-Handing快速检阅" class="headerlink" title="Exception Handing快速检阅"></a>Exception Handing快速检阅</h5><ul>
<li>exception handing由三个主要组件构成：throw子句、catch子句和try区段。当一个exception被抛出去，控制权会从函数调用中释放出来，并寻找吻合的catch子句。如果没有，则调用terminate()中断例程。当控制权被放弃后，堆栈中每个函数调用也被推理（popped up）。</li>
</ul>
<h5 id="对Exception-Handing的支持"><a href="#对Exception-Handing的支持" class="headerlink" title="对Exception Handing的支持"></a>对Exception Handing的支持</h5><ul>
<li>当exception发生时，编译系统完成以下事情：<ol>
<li>检验发生throw操作的函数。</li>
<li>决定throw操作是否在try区段中。</li>
<li>把exception type拿来和每个catch子句比较。</li>
<li>流程控制交到吻合的匹配的catch子句手中。</li>
<li>如果throw不发生在try区段中，或没有catch子句吻合。那么：<ol>
<li>摧毁active local objects。</li>
<li>堆栈中将函数unwind掉</li>
<li>进行堆栈的下一个函数，然后重复2·5。</li>
</ol>
</li>
</ol>
</li>
</ul>
<h5 id="决定throw是否发生在一个try区段中"><a href="#决定throw是否发生在一个try区段中" class="headerlink" title="决定throw是否发生在一个try区段中"></a>决定throw是否发生在一个try区段中</h5><ul>
<li>一个函数可以想象为好几个区域：<ul>
<li>try区段以外的区域，没有active local objects。</li>
<li>try区段以外的区域，但有一个（或以上）的active local objects需要析构。</li>
<li>try区段以内的区域。</li>
</ul>
</li>
</ul>
<h5 id="将exception的类型和每一个catch子句的类型做比较"><a href="#将exception的类型和每一个catch子句的类型做比较" class="headerlink" title="将exception的类型和每一个catch子句的类型做比较"></a>将exception的类型和每一个catch子句的类型做比较</h5><ul>
<li>对于每个被抛出来的exception，编译器产生一个类型描述器，对exception的类型进行编码。如果那是derived type，编码内容包含所有base class的类型信息。</li>
<li>类型描述器（type derscriptor）是必要的。因为exception是在执行期被处理的，object必须有自己的类型信息。RTTI正是因为支持EH而获得的副产品。</li>
</ul>
<h5 id="当一个实际对象在程序执行时被抛出，会发生什么事？"><a href="#当一个实际对象在程序执行时被抛出，会发生什么事？" class="headerlink" title="当一个实际对象在程序执行时被抛出，会发生什么事？"></a>当一个实际对象在程序执行时被抛出，会发生什么事？</h5><h4 id="7-3-执行期类型识别（Runtime-Type-Identification，RTTI）"><a href="#7-3-执行期类型识别（Runtime-Type-Identification，RTTI）" class="headerlink" title="7.3 执行期类型识别（Runtime Type Identification，RTTI）"></a>7.3 执行期类型识别（Runtime Type Identification，RTTI）</h4><h5 id="Type-Safe-Downcast（保证安全的向下转换操作）"><a href="#Type-Safe-Downcast（保证安全的向下转换操作）" class="headerlink" title="Type-Safe Downcast（保证安全的向下转换操作）"></a>Type-Safe Downcast（保证安全的向下转换操作）</h5><h5 id="Type-Safe-Dynamic-Cast（保证安全的动态转换）"><a href="#Type-Safe-Dynamic-Cast（保证安全的动态转换）" class="headerlink" title="Type-Safe Dynamic Cast（保证安全的动态转换）"></a>Type-Safe Dynamic Cast（保证安全的动态转换）</h5><h5 id="Reference并不是Pointers"><a href="#Reference并不是Pointers" class="headerlink" title="Reference并不是Pointers"></a>Reference并不是Pointers</h5><h5 id="Typeid运算符"><a href="#Typeid运算符" class="headerlink" title="Typeid运算符"></a>Typeid运算符</h5><h4 id="7-4-效率有了，弹性呢？"><a href="#7-4-效率有了，弹性呢？" class="headerlink" title="7.4 效率有了，弹性呢？"></a>7.4 效率有了，弹性呢？</h4><h5 id="动态共享函数库（Dynamic-Shared-Libraries）"><a href="#动态共享函数库（Dynamic-Shared-Libraries）" class="headerlink" title="动态共享函数库（Dynamic Shared Libraries）"></a>动态共享函数库（Dynamic Shared Libraries）</h5><h5 id="共享内存（Shared-Memory）"><a href="#共享内存（Shared-Memory）" class="headerlink" title="共享内存（Shared Memory）"></a>共享内存（Shared Memory）</h5>]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第二章 构造函数语义学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%BA%8C%E7%AB%A0-%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E8%AF%AD%E4%B9%89%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>关键词explicit之所以被导入这个语言，就是为了给程序员一个方法，使他们能够制止“单一参数的constructor”被当作conversion运算符。</p>
<h4 id="2-1-Default-Constructor的构造操作"><a href="#2-1-Default-Constructor的构造操作" class="headerlink" title="2.1 Default Constructor的构造操作"></a>2.1 Default Constructor的构造操作</h4><h5 id="“带有Default-Constructor”的Member-Class-Object"><a href="#“带有Default-Constructor”的Member-Class-Object" class="headerlink" title="“带有Default Constructor”的Member Class Object"></a>“带有Default Constructor”的Member Class Object</h5></li>
<li><p>如果class没有constructor，但有一个member object，而且这个object有default constructor，那么这个class的implicit default constructor是<font color="red">nontrivial</font>的。举个例子，下面程序片段中，编译器为class Bar合成一个default constructor：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> &#123;</span> <span class="keyword">public</span>: Foo(), Foo(<span class="keyword">int</span>) ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bar</span> &#123;</span> <span class="keyword">public</span>: Foo foo; <span class="keyword">char</span> *str; &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Bar bar;    <span class="comment">// Bar::foo 必须在此处初始化。</span></span><br><span class="line">    <span class="keyword">if</span> (str) &#123; &#125; ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  被合成的Bar default constructor内含代码，能够调用class Foo的default constructor处理member object Bar::foo，因为Bar::foo初始化是编译期的责任：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 为member foo调用class Foo的default construcotr</span></span><br><span class="line"><span class="keyword">inline</span> Bar::Bar()</span><br><span class="line">&#123;</span><br><span class="line">    foo.Foo::Foo();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  而且，被合成default constructor只满足编译期需求，并不是程序员需求。如果default constructor由程序员显式定义出来了，那么编译器的行动是：</p>
</li>
<li><p>如果class A内含一个或一个以上的member class objects，那么class A的每一个constructor必须调用每一个member classes的default constructor。编译器会扩张已存在的constructors，使得user code被执行之前，先调用的default constructor。</p>
<h5 id="“带有Default-Constructor”的Base-Class"><a href="#“带有Default-Constructor”的Base-Class" class="headerlink" title="“带有Default Constructor”的Base Class"></a>“带有Default Constructor”的Base Class</h5></li>
<li><p>如果class没有constructors，却派生自有default constructor的base class，那么这个derived class的default constructor会被视为nontrivial。</p>
</li>
<li><p>如果提供多个constructor，但都没有default constructor，编译器会扩张现有的每一个constructors，将用以调用必要default constructors的程序代码加进去。它不会合成一个新的default constructor。</p>
<h5 id="“带有一个Virtual-Function”的Class"><a href="#“带有一个Virtual-Function”的Class" class="headerlink" title="“带有一个Virtual Function”的Class"></a>“带有一个Virtual Function”的Class</h5></li>
<li><p>另外有<font color="red">两种情况</font>，也要合成default constructor：</p>
<ol>
<li><p><strong>class声明（或继承）一个virtual function</strong></p>
</li>
<li><p><strong>class派生自一个继承串链，其中有一个或更多的virtual base classes。</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Bell --- Widget</span><br><span class="line">Whistle --- Widget</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">flip</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">flip</span><span class="params">(<span class="keyword">const</span> Widget &amp;widget)</span> </span>&#123; Widget.flip(); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Bell b;</span><br><span class="line">    Whistle w;</span><br><span class="line">    flip(b);</span><br><span class="line">    flip(w);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个扩张在编译期间发生了：</p>
</li>
<li><p>一个virtual function table（<font color="red">vtbl</font>）被产生出来，内含class的virtual functions地址。</p>
</li>
<li><p>每个class object中，额外的pointer member（<font color="red">vptr</font>）会被合成出来，内含相关的class vtbl地址。</p>
<p>widget.flip()虚拟调用操作会被重新改写，以使用widget之中的vtpr和vtbl：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&#123; *widget.vptr[<span class="number">1</span>])(&amp;widget)</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
<h5 id="“带有一个Virtual-Base-Class”的Class"><a href="#“带有一个Virtual-Base-Class”的Class" class="headerlink" title="“带有一个Virtual Base Class”的Class"></a>“带有一个Virtual Base Class”的Class</h5><ul>
<li><p>看以下代码：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">A --- X</span><br><span class="line">B --- X</span><br><span class="line">C --- A</span><br><span class="line">C --- B</span><br></pre></td></tr></table></figure>

 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span> <span class="keyword">public</span>: <span class="keyword">int</span> i; &#125; ;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123; <span class="keyword">public</span>: <span class="keyword">int</span> j; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123; <span class="keyword">public</span>: <span class="keyword">double</span> d; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> :</span> <span class="keyword">public</span> A, <span class="keyword">public</span> B &#123; <span class="keyword">public</span>: <span class="keyword">int</span> k; &#125;;</span><br><span class="line"><span class="comment">// 无法在编译时期决定（resolve）pa-&gt;X::i的位置</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">const</span> A *pa)</span> </span>&#123; pa-&gt;i = <span class="number">1024</span>; &#125;</span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">    foo(<span class="keyword">new</span> A);</span><br><span class="line">    foo(<span class="keyword">new</span> C);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 因为pa的类型可以改变，编译器无法固定住foo()中经由pa而存取的X::i的实际偏移位置。编译器必须改变“执行存取操作”的那些代码，使X::i可以延迟至执行期才决定下来。cfont做法是在virtual base classes中安插指针完成。经由reference或pointer存取virtual base class的操作都可以通过指针完成。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">const</span> A *pa)</span> </span>&#123;pa-&gt;__vbcX-&gt;i = <span class="number">1024</span>; &#125;</span><br></pre></td></tr></table></figure>

<p> __vbcX实在class object构造期间被完成的。编译器会安插允许每个virtual base class在执行器存取操作的代码，如果base没有声明任何constructor，编译器必须为它合成一个default的。</p>
<h4 id="2-2-Copy-Constructor的构造操作"><a href="#2-2-Copy-Constructor的构造操作" class="headerlink" title="2.2 Copy Constructor的构造操作"></a>2.2 Copy Constructor的构造操作</h4></li>
<li><p>有三种情况，会以一个obect的内容作为另一个class object的初值。</p>
<ul>
<li><strong>对object做显式初始化操作</strong></li>
<li><strong>当object作为参数交给某个函数时</strong></li>
<li><strong>函数回传一个class object时</strong></li>
</ul>
</li>
</ul>
<h5 id="Default-Memberwise-Initialization"><a href="#Default-Memberwise-Initialization" class="headerlink" title="Default Memberwise Initialization"></a>Default Memberwise Initialization</h5><ul>
<li><p>如果class没有提供explicit copy constructor，其内部是以<font color="red">default memberwise initialization</font>手法完成的，就是把每个内建的或派生的data member（如指针或数组）的值，从object拷贝到另一个object上，不过它并不会拷贝其中的member class object。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... 没有explicit copy constructor</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">char</span> *str;</span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// String object的default memberwise initialization发生在这种情况下：</span></span><br><span class="line"><span class="function">String <span class="title">noun</span><span class="params">(<span class="string">"book"</span>)</span></span>;</span><br><span class="line">String verb = noun;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 完成方式好像个别设定每个members一样</span></span><br><span class="line">verb.str = noun.str;</span><br><span class="line">verb.len = noun.len;</span><br></pre></td></tr></table></figure>

<p>  如果String object被声明为一个class的member:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... 没有explicit copy constructor</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> _occurs;</span><br><span class="line">    String _word;</span><br><span class="line">&#125;;s</span><br></pre></td></tr></table></figure>

<p>  那么Word object的default memberwise initialization会拷贝_occurs，然后再于_word身上递归实施memberwise initialization。</p>
</li>
<li><p>一个class可用两种方式复制得到：</p>
<ul>
<li><strong>一是被初始化</strong>。以copy constructor完成。</li>
<li><strong>二是被指定（asignment）</strong>。以copy assignment operator完成。</li>
</ul>
</li>
</ul>
<h5 id="Bitwise-Copy-Semantics-位逐次拷贝）"><a href="#Bitwise-Copy-Semantics-位逐次拷贝）" class="headerlink" title="Bitwise Copy Semantics(位逐次拷贝）"></a>Bitwise Copy Semantics(位逐次拷贝）</h5><ul>
<li>一个class没有定义explicit copy constructor，是否有编译器合成实例，取决于class是否展现<font color="red">bitwise copy semantics</font>而定。有两个例子：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 声明展现了bitwise copy semantics</span></span><br><span class="line"><span class="comment">// 这种情况下不需要合成default copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word(<span class="keyword">const</span> <span class="keyword">char</span>*);</span><br><span class="line">    ~Word() &#123; <span class="keyword">delete</span> []str; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> cnt;</span><br><span class="line">    <span class="keyword">char</span> *str;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 以下声明未展现出bitwise copy semantics</span></span><br><span class="line"><span class="comment">// 这种情况下需要合成出一个copy constructor，以便调用member class String object的copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word(<span class="keyword">const</span> String&amp;);</span><br><span class="line">    ~Word();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> cnt;</span><br><span class="line">    String str;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 其中String声明了explicit copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    String(<span class="keyword">const</span> <span class="keyword">char</span>*);</span><br><span class="line">    String(<span class="keyword">const</span> String&amp;);</span><br><span class="line">    ~String();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;s</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h5 id="不要Bitwise-Copy-Semantics"><a href="#不要Bitwise-Copy-Semantics" class="headerlink" title="不要Bitwise Copy Semantics!"></a>不要Bitwise Copy Semantics!</h5><ul>
<li><font color="red">有4中情况下，class不展现出“bitwise copy semantics”</font>： <ul>
<li><strong>当class内有个member object，而该object声明有copy constructor时</strong>；</li>
<li><strong>当class继承自一个base class，而该class存在copy constructor时</strong>，不论是显式声明的还是被合成的；</li>
<li><strong>当class声明了一个或多个virtual functional时</strong>。</li>
<li><strong>当class派生自一个继承串联，其中有一个或多个virutal base clases时</strong>。</li>
</ul>
</li>
</ul>
<h5 id="重新设定Virtual-Table的指针"><a href="#重新设定Virtual-Table的指针" class="headerlink" title="重新设定Virtual Table的指针"></a>重新设定Virtual Table的指针</h5><ul>
<li><p>只要有一个class声明了一个或多个virtual functional，就会：</p>
<ul>
<li><p><strong>增加vtbl，内含virtual function的地址</strong></p>
</li>
<li><p><strong>一个指向vtbl的vptr，插在class object内</strong>。</p>
<p>当vptr导入到class之中，class就不展示biwise semantics了。一个新产生的class object的vptr不能成功而正确地设好初值会导致可怕的后果，编译器需要合成出一个copy constructor以求将vptr初始化。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">ZooAnimal --- Bear</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ZooAnimal</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ZooAnimal();</span><br><span class="line">    <span class="keyword">virtual</span> ~ZooAnimal();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">animate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bear</span> :</span> <span class="keyword">public</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Bear();</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">animate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">dance</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ZooAnimal class object以另一个ZooAnimal class object作为初值，或Bear class object以另一个Bear class object作为初值，都可以直接靠bitwise copy semantics完成（除了member pointer）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Bear yogi;</span><br><span class="line">Bear winnie = yogi;</span><br></pre></td></tr></table></figure>

<p>这个例子里，yogi会被default Bear constructor初始化。yogi的vptr被指向Bear的vtbl（靠安插）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ZooAnimal franny = yogi;    <span class="comment">// sliced</span></span><br></pre></td></tr></table></figure>

<p>frany的vptr不可以被指向Bear的vtbl（如果yogi的vptr被bitwise copy，会导致此结果），否则当draw()被调用而franny被传进去时，会blow up：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">(<span class="keyword">const</span> ZooAnimal &amp;zoey)</span> </span>&#123; zoey.draw(); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// franny的vptr指向ZooAnimal的vtbl</span></span><br><span class="line">    ZooAnimal franny = yogi;</span><br><span class="line">    draw(yogi); <span class="comment">// call Bear::draw()</span></span><br><span class="line">    draw(franny);   <span class="comment">// call ZooAnimal::draw()</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过franny调用virtual function draw()，调用的时ZooAnimal而非Bear实例。事实上，yogi中的Bear部分在franny初始化时被sliced掉了，只有franny声明为reference或pointer时才会是Bear的函数实例。</p>
</li>
</ul>
</li>
</ul>
<h5 id="处理Virtual-Base-Class-Subobject"><a href="#处理Virtual-Base-Class-Subobject" class="headerlink" title="处理Virtual Base Class Subobject"></a>处理Virtual Base Class Subobject</h5><ul>
<li><p>编译器必须让derived class object中的virtual base class subobject位置在执行期准备妥当。Bitwise copy semantics可能会破坏位置的完整性，所以编译器必须必须在它自己合成出来的copy constructor中做出仲裁：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Bear -- public --- ZooAnimal</span><br><span class="line">Raccoon -- public virtual --- ZooAnimal</span><br><span class="line">RedPanda -- public --- Raccoon</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Raccoon</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Raccoon() &#123; <span class="comment">/*设定private data初值*/</span> &#125;</span><br><span class="line">    Raccoon(<span class="keyword">int</span> val) &#123; <span class="comment">/*设定private data初值*/</span> &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  一个virtual base class的存在会使bitwise copy semantics无效。问题不在于一个class object以另一个同类的object作为初值之时，而是发生于一个class object以其derived classes的某个object作为初值之时。<br>  一个Raccoon object作为另一个Raccoon object的初值，bitwise copy绰绰有余，而如果企图以RedPanda object作为little_critter的初值，编译器必须判断当后续企图存取ZooAnimal subobject时是否能正确执行：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 简单的bitwise copy还不够，必须显式将little_critter的virtual base class pointer/offset初始化</span></span><br><span class="line">RedPanda little_red;</span><br><span class="line">Raccoon little_critter = little_red;</span><br></pre></td></tr></table></figure>

<p>  在这种情况下，为了完成正确的little_critter初值设定，编译器合成一个copy constructor，安插代码设定virtual base class pointer/offset的初值（或是简单的确定它没被抹消），对每个members执行memberwise初始化操作，一起执行其他内存相关工作。</p>
</li>
</ul>
<h4 id="2-3-程序转化语义学（Program-Transformation-Semantics）"><a href="#2-3-程序转化语义学（Program-Transformation-Semantics）" class="headerlink" title="2.3 程序转化语义学（Program Transformation Semantics）"></a>2.3 程序转化语义学（Program Transformation Semantics）</h4><h5 id="显式的初始化操作（Explicit-Initialization）"><a href="#显式的初始化操作（Explicit-Initialization）" class="headerlink" title="显式的初始化操作（Explicit Initialization）"></a>显式的初始化操作（Explicit Initialization）</h5><ul>
<li><p>已知有定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X x0;</span><br></pre></td></tr></table></figure>

<p>  下面三个定义，每个都以x0来初始化class object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">X <span class="title">x1</span><span class="params">(x0)</span></span>;</span><br><span class="line">    X x2 = x0;</span><br><span class="line">    X x3 = X(x0);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  以上必要的程序转化有<font color="red">两个阶段</font>：</p>
<ul>
<li><p><strong>重写每个定义，其中初始化操作被剥除</strong>。</p>
</li>
<li><p><strong>class的copy constructor调用操作被安插进去</strong>。</p>
<p>foo_bar()可能看起来这样：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">C++ 伪码</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 定义重写，初始化操作被剥除</span></span><br><span class="line">    X x1;</span><br><span class="line">    X x2;</span><br><span class="line">    X x3;</span><br><span class="line">    <span class="comment">// 编译器安插copy construction的调用操作</span></span><br><span class="line">    <span class="comment">// 表现出X::X(const X &amp;xx);</span></span><br><span class="line">    x1.X::X(x0);</span><br><span class="line">    x2.X::X(x0);</span><br><span class="line">    x3.X::X(x0);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="参数的初始化（Argument-Initialization）"><a href="#参数的初始化（Argument-Initialization）" class="headerlink" title="参数的初始化（Argument Initialization）"></a>参数的初始化（Argument Initialization）</h5><ul>
<li><p>C++ Standard说，把class object当作参数传给函数，或者作为函数的返回值，相当于初始化操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X xx = arg;</span><br></pre></td></tr></table></figure>

<p>  这里xx是形式参数（或返回值），arg是真实参数。因此，函数：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(X x0)</span></span>;</span><br><span class="line">X xx;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">foo(xx);</span><br></pre></td></tr></table></figure>

<p>  会要求<font color="red">局部实例</font>（<font color="green">local instance</font>）x0以memberwise方式将xx当初值。代码转换为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="comment">// 编译器产生的临时对象</span></span><br><span class="line">X __temp0;</span><br><span class="line"><span class="comment">// 调用copy constructor</span></span><br><span class="line">__temp0.X::X(xx);</span><br><span class="line">foo(__temp0);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="返回值的初始化（Return-Value-Initialization）"><a href="#返回值的初始化（Return-Value-Initialization）" class="headerlink" title="返回值的初始化（Return Value Initialization）"></a>返回值的初始化（Return Value Initialization）</h5><ul>
<li><p>已知下面函数的定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  bar()的返回值如何从局部对象xx中拷贝过来？在cfont中的做法是一个双阶段转化：</p>
<ul>
<li><p><strong>加上额外参数，类型是class object的reference</strong>。这个参数用来放置被<font color="red">拷贝构建</font>（<font color="green">copy constructed</font>）而来的返回值。</p>
</li>
<li><p><strong>return之前安插一个copy constructor调用</strong>。</p>
<p>bar()转换如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// default constructor</span></span><br><span class="line">    xx.X::X();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// copy constructor</span></span><br><span class="line">    __result.X:XX(xx);</span><br><span class="line">    <span class="keyword">return</span>; <span class="comment">// 即使不传回任何值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X xx = bar();</span><br><span class="line"><span class="comment">// 不用default constructor,NRV</span></span><br><span class="line">X xx;</span><br><span class="line">bar(xx);</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">bar().memfunc();</span><br><span class="line"><span class="comment">// 执行bar()所传回的X class object的memfunc()可能转换</span></span><br><span class="line">X __temp0;</span><br><span class="line">(bar(__temp0), __temp0).memfunc();</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 同理，声明一个函数指针</span></span><br><span class="line">X (*pf)();</span><br><span class="line">pf = bar;</span><br><span class="line"><span class="comment">// 转为</span></span><br><span class="line"><span class="keyword">void</span> (*pf)(X&amp;);</span><br><span class="line">pf = bar;</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="在使用者层面做优化（Optimization-at-the-User-Level）"><a href="#在使用者层面做优化（Optimization-at-the-User-Level）" class="headerlink" title="在使用者层面做优化（Optimization at the User Level）"></a>在使用者层面做优化（Optimization at the User Level）</h5><ul>
<li><p>以下代码：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">(<span class="keyword">const</span> T &amp;y, <span class="keyword">const</span> T &amp;z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...以y</span></span><br><span class="line">    来处理xx</span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  可以这么写：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">(<span class="keyword">const</span> T &amp;y, <span class="keyword">const</span> T &amp;z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> X(y, z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  当bar()被转换后，效率会比较高：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    __result.X::X(y, z);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="在编译器层面做优化（Optimization-at-the-Compiler-Level）"><a href="#在编译器层面做优化（Optimization-at-the-Compiler-Level）" class="headerlink" title="在编译器层面做优化（Optimization at the Compiler Level）"></a>在编译器层面做优化（Optimization at the Compiler Level）</h5><ul>
<li><p>在一个像bar()这样的函数中，所有return指令传回相同具名数值（named value），因此<font color="red">编译器可能自己做优化</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  xx以_result取代：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// default constructor被调用</span></span><br><span class="line">    __result.X::X();</span><br><span class="line">    <span class="comment">// ... 直接处理__result</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  这样的优化操作，称为<font color="green">Named Return Value</font>（<font color="red">NRV</font>）。虽然NRV优化提供了重要效率改善，但它还是饱受批评。</p>
<ul>
<li><p><strong>优化是默默的，不透明</strong></p>
</li>
<li><p><strong>一旦函数复杂，很难优化</strong></p>
</li>
<li><p><strong>某些人不喜欢</strong></p>
<p>举个例子，以下三个初始化语义上相等：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">xx0</span><span class="params">(<span class="number">1024</span>)</span></span>;    <span class="comment">// xx0.X::X(1024);</span></span><br><span class="line">X xx1 = X(<span class="number">1024</span>);</span><br><span class="line">X xx2 = (X)<span class="number">1024</span>;</span><br></pre></td></tr></table></figure>

<p>xx0是被单一的constructor操作设定初值：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xx0.X::X(<span class="number">1024</span>);</span><br></pre></td></tr></table></figure>

<p>而xx1或xx2却调用两个constructor，产生临时性object，并针对临时object调用classX的destructor：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X __temp0;</span><br><span class="line">__temp0.X::X(<span class="number">1024</span>);</span><br><span class="line">xx1.X::X(__temp0);</span><br><span class="line">__temp0.X::~X();</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="Copy-Constructor：要还是不要？"><a href="#Copy-Constructor：要还是不要？" class="headerlink" title="Copy Constructor：要还是不要？"></a>Copy Constructor：要还是不要？</h5><ul>
<li><p>一个3D坐标点类：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y, _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  class的default copy constructor被视为trivial。默认情况下，一个Point3d class object的“memberwise”初始化操作会导致“bitwise copy”。这样效率高，也安全。<br>  实现copy constructor的最简单方法像这样：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    _x = rhs._x;</span><br><span class="line">    _y = rhs._y;</span><br><span class="line">    _z = rhs._z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  但使用C++library的memcpy()会更有效率：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">memcpy</span>(<span class="keyword">this</span>, &amp;rhs, <span class="keyword">sizeof</span>(Point3d));</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="2-4-成员们的初始化队伍（Member-Initialization-List）"><a href="#2-4-成员们的初始化队伍（Member-Initialization-List）" class="headerlink" title="2.4 成员们的初始化队伍（Member Initialization List）"></a>2.4 成员们的初始化队伍（Member Initialization List）</h4><ul>
<li><p>当你写下一个constructor时，就有机会设定class members的初值，不是在member initialization list，就是在constructor函数本体。</p>
</li>
<li><p>下列情况下，为了让你程序能够顺利执行，必须使用member initialization list：</p>
<ol>
<li><p><font color="red"><strong>当初始化reference member时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当初始化const member时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当调用一个base class的constructor，且拥有一组参数时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当调用一个member class的constructor，而 它拥有一组参数时</strong></font>。<br>这四种情况都能正确编译并执行，但效率不高。例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line">    String _name;</span><br><span class="line">    <span class="keyword">int</span> _cnt;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word() &#123;</span><br><span class="line">        _name = <span class="number">0</span>;</span><br><span class="line">        _cnt = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>constructor可能的内部扩张结果：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word( <span class="comment">/* this pointer goes here */</span> )</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// String default constructor</span></span><br><span class="line">    _name.String::String();</span><br><span class="line">    <span class="comment">// tempory</span></span><br><span class="line">    String temp = String(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">// memberwise copy _name</span></span><br><span class="line">    _name.String::<span class="keyword">operator</span>=(temp);</span><br><span class="line">    <span class="comment">// destroy tempory</span></span><br><span class="line">    temp.String::~String();</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个有效率的实现方法是：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word : _name(<span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>它会扩张：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word( <span class="comment">/* this pointer goes here */</span> )</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// String(int) constructor</span></span><br><span class="line">    _name.String::String(<span class="number">0</span>);</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第六章 执行期语意学</title>
    <url>/2020/06/12/%E7%AC%AC%E5%85%AD%E7%AB%A0-%E6%89%A7%E8%A1%8C%E6%9C%9F%E8%AF%AD%E6%84%8F%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>有个简单的式子：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (yy == xx.getValue())) ...</span><br></pre></td></tr></table></figure>

<p>  其中xx和yy的定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Y</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Y();</span><br><span class="line">    ~Y();</span><br><span class="line">    <span class="keyword">bool</span> oeprator==(<span class="keyword">const</span> Y&amp;) <span class="keyword">const</span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    X();</span><br><span class="line">    ~X();</span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">Y</span><span class="params">()</span> <span class="keyword">const</span></span>; <span class="comment">// conversion运算符</span></span><br><span class="line">    <span class="function">X <span class="title">getValue</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  当使用equality（等号）运算符时，xx被resolves为“被overloaded的Y成员实例”。下面是该式子的转换：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// resolution of intended operator</span></span><br><span class="line"><span class="keyword">if</span> (yy.<span class="keyword">operator</span>==(xx.getValue()))</span><br></pre></td></tr></table></figure>

<p>  Y的等号运算符需要一个类型为Y的参数，而getValue()传回的是一个类型为X的object。这时提供<font color="red">conversion运算符</font>，把一个X object转换为一个Y object，它<strong>施行于getValue()的返回值身上</strong>。下面是第二次转换：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// conversion of getValue()'s return value</span></span><br><span class="line"><span class="keyword">if</span> (yy.<span class="keyword">operator</span>==(xx.getalue().<span class="keyword">operator</span> Y()))</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h4 id="6-1-对象的构造和析构（Object-Construction-and-Destruction）"><a href="#6-1-对象的构造和析构（Object-Construction-and-Destruction）" class="headerlink" title="6.1 对象的构造和析构（Object Construction and Destruction）"></a>6.1 对象的构造和析构（Object Construction and Destruction）</h4><ul>
<li><p>一般constructor和destructor的安插都如下：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    Point point;</span><br><span class="line">    <span class="comment">// point.point::Point();</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// point.point::~Point();</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  如果一个区段或函数有一个以上的离开点，情况就会混乱一些，Destructor必须被放在每一个离开点（object还存活）之前。</p>
</li>
</ul>
<h5 id="全局对象（Global-Objects）"><a href="#全局对象（Global-Objects）" class="headerlink" title="全局对象（Global Objects）"></a>全局对象（Global Objects）</h5><ul>
<li><p>C++保证在第一次使用global数据前之前，将它构造出来，而在结束前把identity摧毁掉。<strong>如果global object如果有constructor和destructor的话，它需要静态的初始化操作和内存释放操作</strong>。所有的global obejct都被放置在程序的data segment中。如果不显式指定值，则内存内容为0（C略有不同，C并不自动设定初值）。C语言中一个global object只能被一个常量表达式（可在编译时期求值）设定初值。虽然class object在编译时期可以被放置data segment中并且内容为0，<strong>但constructor一直要到程序启动（startup）时才会实施。而必须对放置program data segment中的object的初始化表达式做评估（evaluate），这就是一个object需要静态初始化的原因</strong>。</p>
</li>
<li><p><font color="red"><strong>munch</strong></font><strong>是一个可移植但成本颇高的静态初始化（以及内存释放）方法</strong>。munch策略：</p>
<ol>
<li><p><strong>为每个需要静态初始化的文件产生<font color="red">__sti()函数</font>(sti是static initialization)，内含constructor调用操作或inline expansions</strong>。一个global对象identity会在matrix.c中产生：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">__sti_matrix_c__identity() &#123;</span><br><span class="line">    identity.Matrix::Matrix();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> matrix_c是文件名编码，_identity表示文件中所定义的static object。</p>
</li>
<li><p><strong>在每个需要静态的内存释放操作（static deallocation）的文件中，产生一个<font color="red">__std()函数</font>(std是static deallocation)，内含必要的destructor调用操作，或是inline expansions</strong>。比如以上identity对象会调用Matrix destructor。</p>
</li>
<li><p><strong>提供一组<font color="red">rumtime library “munch”函数</font>：一个_mani()函数(call __sti()，一个exit()函数(call __std())</strong>。</p>
</li>
</ol>
</li>
<li><p><em>最后一个需要解决的问题是，如何收集程序中各个object files的__sti()函数和__std()函数。解决方法是使用<font color="red">nm命令</font>，nm会dump出object file的<font color="red">符号表格项目</font>（<font color="green">symbol table entries</font>）。*</em>nm<strong>施行于可执行文件上，输出</strong>piped into<strong>到munch程序中。munch程序则搜寻__sti或__std开头的名称，然后把函数名称加到</strong>jump table*<em>中，然后把表格写到<font colorr="ed">program text</font>文件中，CC命令被重新激活，将内含表格的文件编译，整个可执行文件被重新链接。_main()和exit()在各个表格上走访一遍，轮流调用每一个项目（代表一个函数地址）</em>。但这方法离正统计科太远了……</p>
</li>
<li><p>当特性平台的C++编译器出现，更有效率的方法也随之出现，因为各平台上扩充链接器和目的文件格式（object file format），以直接支持静态初始化和内存释放操作。例如System V的Executable and Linking Format(ELF)被扩充以增加支持.init和.fini两个sections，两个sections内含的对象所需的信息，分别对应静态初始化和释放操作。编译器特定的startup函数会完成平台特定的支持。</p>
</li>
<li><p>使用被静态初始化的objects，<font color="red"><strong>有缺点</strong></font>。如果exception handing被支持，那些objects将不能被放置于try区段之内。因为任何的throw操作必然将触发exception handing library的默认terminate()函数以致于结束程序，这对于被静态调用的constructors可能是特别无法接受的。<font color="red"><strong>另一个缺点</strong></font>是为了控制跨模块静态初始化的objects的依赖顺序，而形成的复杂度。建议根本不要用需要静态初始化的global objects（虽然这建议不普遍为C程序员接受）。</p>
</li>
</ul>
<h5 id="局部静态对象（Local-Static-Objects）"><a href="#局部静态对象（Local-Static-Objects）" class="headerlink" title="局部静态对象（Local Static Objects）"></a>局部静态对象（Local Static Objects）</h5><ul>
<li><p>假设有以下程序片段：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">const</span> Matrix&amp; <span class="title">identity</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Matrix mat_identity;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> mat_identity;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  <font color="red">Local static class object</font>保证了什么样的语意？</p>
<ul>
<li><strong>mat_identity的constructor只能执行一次，虽然函数可能被调用多次</strong>。</li>
<li><strong>mat_identity的destructor只能施行一次，虽然函数可能会被调用多次</strong>。</li>
</ul>
</li>
<li><p>编译器的无条件构造对象会导致即使函数不曾被调用过也被程序起始时被初始化。cfont的做法是，用一个临时性对象保护mat_identity的初始化操作。第一次调用时，临时对象被评估为false，constructor被调用，然后临时对象改为true。而destructor判断则反过来。困难的是，没办法在静态的内存释放函数中存取local对象。取出local object的地址就好了……</p>
</li>
</ul>
<h5 id="对象数组（Array-of-Objects）"><a href="#对象数组（Array-of-Objects）" class="headerlink" title="对象数组（Array of Objects）"></a>对象数组（Array of Objects）</h5><ul>
<li><p>假如有数组定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point knots[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>

<p>  如果Point没有定义constructor也没有定义destructor，那么工作不会比（<font color>build-in</font>）类型的数组更多。然而它的确定义了一个default destructor，所以这个操作轮流施行每个元素上。cfont中，使用一个叫vec_new()的函数，产生以class object构造成的数组。一些新编译器则提供了两个函数，一个分别处理有和没有内含virtual base class的class。vec_vnew()用于内含virtual base class函数的class。当然不同平台有差异：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">vec_new</span> <span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">void</span> *<span class="built_in">array</span>,        <span class="comment">// 数组起始地址</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">size_t</span> elem_size,   <span class="comment">// class object的大小</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">int</span> elem_count,     <span class="comment">// 元素个数</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">void</span> (*constructor)(<span class="keyword">void</span>*), <span class="comment">// 函数指针</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">void</span> (*destructor)(<span class="keyword">void</span>*, <span class="keyword">char</span>)</span></span></span><br><span class="line"><span class="function"><span class="params">)</span></span></span><br></pre></td></tr></table></figure>

<p>  <strong>参数array如果持有不具名数组地址（knots），就是0，意味着数组经由应用程序new运算符动态配置于heap中</strong>。<br>  下面是编译器可能针对10个Point元素所做的vec_new()调用操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point knots[<span class="number">10</span>];</span><br><span class="line">vec_new(&amp;knots, <span class="keyword">sizeof</span>(Point), <span class="number">10</span>, &amp;Point::Point, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>  如果Point定义了一个destructor，当knots结束时destructor也施行于10个Point元素身上。这时经由一个类似vec_delete()的<font color="red">runtime library</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">vec_delete</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">void</span> *<span class="built_in">array</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">size_t</span> elem_size,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">int</span> elem_count,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">void</span> (*destructor)(<span class="keyword">void</span>*, <span class="keyword">char</span>)</span></span></span><br><span class="line"><span class="function"><span class="params">)</span></span></span><br></pre></td></tr></table></figure>

<p>  对于以下的组成方式：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point knots[<span class="number">10</span>] = &#123;</span><br><span class="line">    Point(),</span><br><span class="line">    Point(<span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">0.5</span>),</span><br><span class="line">    <span class="number">-1.0</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  对于明显获得初值的元素，vec_new()不再有必要。对于尚未初始化的元素，施行方式则像没有explicit initialization list的class elemnts一样：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line">Point knots[<span class="number">10</span>];</span><br><span class="line">Point::Point(&amp;knots[<span class="number">0</span>]);</span><br><span class="line">Point::Point(&amp;knots[<span class="number">1</span>], <span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">0.5</span>);</span><br><span class="line">Point::Point(&amp;knots[<span class="number">2</span>], <span class="number">-1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>);</span><br><span class="line"><span class="comment">// 初始化剩余7个元素</span></span><br><span class="line">vec_new(&amp;knots+<span class="number">3</span>, <span class="keyword">sizeof</span>(Point), <span class="number">7</span>, &amp;Point:Piont, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="Default-Constructors和数组"><a href="#Default-Constructors和数组" class="headerlink" title="Default Constructors和数组"></a>Default Constructors和数组</h5><h4 id="6-2-new和delete运算符"><a href="#6-2-new和delete运算符" class="headerlink" title="6.2 new和delete运算符"></a>6.2 new和delete运算符</h4><ul>
<li><p>运算符new的使用，看起来似乎是单一运算，像这样：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *pi = <span class="keyword">new</span> <span class="keyword">int</span>(<span class="number">5</span>);</span><br></pre></td></tr></table></figure>

<p>  它由两个<font color="red">步骤完成</font>：</p>
<ol>
<li><p><strong>通过new运算符函数实例，分配内存</strong>。</p>
</li>
<li><p><strong>为配置的内存初始化</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *pi;</span><br><span class="line"><span class="keyword">if</span> (pi = __new(<span class="keyword">sizeof</span>(<span class="keyword">int</span>)))</span><br><span class="line">    *pi = <span class="number">5</span>;</span><br></pre></td></tr></table></figure>

<p>pi所指对象的生命因delete而结束。所有后继对pi的参考操作不再保证有良好的行为。虽然地址上的对象不再合法，地址本身仍然代表一个合法的程序空间。因此pi能继续被使用，但不是个合法的编程风格。</p>
</li>
</ol>
</li>
<li><p>以constructor分配一个class object，情况类似：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Ponit3d *origin = <span class="keyword">new</span> Point3d;</span><br><span class="line"><span class="comment">// 转换</span></span><br><span class="line">Point3d *origin;</span><br><span class="line"><span class="keyword">if</span> (origin = __new(<span class="keyword">sizeof</span>(Point3d)))</span><br><span class="line">    origin = Point3d::Point3d(origin);</span><br><span class="line"><span class="comment">// 如果有exception hading，情况会复杂</span></span><br><span class="line"><span class="keyword">if</span> (origin = __new(<span class="keyword">sizeof</span>(Point3d))) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        origin = Point3d::Point3d(origin);</span><br><span class="line">    &#125; <span class="keyword">catch</span> ( ... ) &#123;</span><br><span class="line">        __delete(origin);</span><br><span class="line">        <span class="keyword">throw</span>;  <span class="comment">// 继续抛出</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  Destructor情况类似：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> origin;</span><br><span class="line"><span class="comment">// 变成</span></span><br><span class="line"><span class="keyword">if</span> (origin != <span class="number">0</span>) &#123;</span><br><span class="line">    Point3d::~Point3d(origin);</span><br><span class="line">    __delete(origin);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 如果有exception handing</span></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>

<p>  new运算符实现：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (size == <span class="number">0</span>)</span><br><span class="line">        size = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">void</span> *last_alloc;</span><br><span class="line">    <span class="keyword">while</span> (!(last_alloc = <span class="built_in">malloc</span>(size)))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (_new_handler)</span><br><span class="line">            (*_new_handler)();</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> last_alloc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  虽然这么写是合法的：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> T[<span class="number">0</span>];</span><br></pre></td></tr></table></figure>

<p>  <strong>但语言要求每次new调用都返回独一无二的指针。解决问题的传统方法是传回一个指针，指向一个默认为1-byte的内存区块（这就是为什么程序代码中size被设为1的原因）</strong>。这个程序的有趣之处在于，它允许使用者提供一个属于自己的_new_handler()函数。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> vodi <span class="keyword">operator</span> <span class="title">edlete</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (ptr)</span><br><span class="line">        <span class="built_in">free</span>((<span class="keyword">char</span>*)ptr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="针对数组的new语意"><a href="#针对数组的new语意" class="headerlink" title="针对数组的new语意"></a>针对数组的new语意</h5><ul>
<li><p>new一个数组；</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> *p_array = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>];</span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line"><span class="keyword">int</span> *p_array = (<span class="keyword">int</span>*)__new(<span class="number">5</span> * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br></pre></td></tr></table></figure>

<p>  vec_new()不会被调用，因为它主要功能是把default constructor施行于class objects所组成的数组的每个元素身上。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d *p_array = <span class="keyword">new</span> Point3d[<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 编译</span></span><br><span class="line">Point3d *p_array;</span><br><span class="line">p_array = vec_new(<span class="number">0</span>, <span class="keyword">sizeof</span>(Point3d), <span class="number">10</span>, &amp;Point3d::Point3d, &amp;Point3d::~Point3d);</span><br></pre></td></tr></table></figure>
</li>
<li><p>应该如何记录元素个数？一个方法是为vec_new()传回的每个内存块配置一个额外的word，然后元素个数藏在word中，这叫<font color="red">cookie</font>。原始编译器有两个用来存取cookie：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">void</span> *pv;</span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> __insert_new_array(PV array_key, <span class="keyword">int</span> elem_count);</span><br><span class="line"><span class="comment">// 表格中取出并移除array_key，传回不是elem_count则传回-1</span></span><br><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> __remove_old_array(PV array_key);</span><br><span class="line"><span class="comment">// cfront vec_new()</span></span><br><span class="line">PV __vec_new(PV ptr_array, <span class="keyword">int</span> elem_count, <span class="keyword">int</span> size, PV construct)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 如果ptr_array是0，从heap中配置数组。</span></span><br><span class="line">    <span class="keyword">int</span> alloc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> array_sz = elem_count == <span class="number">0</span>)</span><br><span class="line">        ptr_array = PV(<span class="keyword">new</span> <span class="keyword">char</span>[array_sz]);</span><br><span class="line">    <span class="keyword">if</span> (ptr_array == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 把数组元素个数放到cache中</span></span><br><span class="line">    <span class="keyword">int</span> status = __insert_new_array(ptr_array, elem_count);</span><br><span class="line">    <span class="keyword">if</span> (status === <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (alloc)</span><br><span class="line">            <span class="keyword">delete</span> ptr_array;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (construct) &#123;</span><br><span class="line">        <span class="keyword">register</span> <span class="keyword">char</span>* elem = (<span class="keyword">char</span>*)ptr_array;</span><br><span class="line">        <span class="keyword">register</span> <span class="keyword">char</span>* lim = elem + array_sz;</span><br><span class="line">        <span class="comment">// PF是typedef，代表函数指针</span></span><br><span class="line">        <span class="keyword">register</span> PF fp = PF(constructor);</span><br><span class="line">        <span class="keyword">while</span> (elem &lt; lim) &#123;</span><br><span class="line">            (*fp)((<span class="keyword">void</span>*)elem);</span><br><span class="line">            <span class="comment">// 下一个元素</span></span><br><span class="line">            elem += size;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> PV(ptr_array);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="Placement-Operator-new的语意"><a href="#Placement-Operator-new的语意" class="headerlink" title="Placement Operator new的语意"></a>Placement Operator new的语意</h5><ul>
<li><p>有一个预先定义好的重载的（overloaded）new运算符，称为<font colorr="red"><strong>placement oeprator new</strong></font>。它需要第二个参数，类型为void*，调用方式如下:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point2w *ptw = <span class="keyword">new</span>(arena) Point2w;</span><br></pre></td></tr></table></figure>

<p>  arena指向内存中的区块，用来放置新的Point2w object。placement operator new的实现方法很平凡：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>, <span class="keyword">void</span> *p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  placement new operator的强大之处在于，编译系统保证object的constructor会施行于其上：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point2w *ptw = (Point2w*)arena;</span><br><span class="line"><span class="keyword">if</span> (ptw != <span class="number">0</span>)</span><br><span class="line">    ptw-&gt;Point2w::Point2w();</span><br></pre></td></tr></table></figure>

<p>  对于arena表现的真正指针的类型，derived class很明显不在支持之列。对于derived class，或是其他没有关联的类型，行为虽然并非不合法，却也未经定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 可以这么配置</span></span><br><span class="line"><span class="keyword">char</span> *arena = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="keyword">sizeof</span>(Point2w)];</span><br><span class="line"><span class="comment">// 相同object可以这么获得</span></span><br><span class="line">Point2sw *arena = <span class="keyword">new</span> Point2w;</span><br><span class="line"><span class="comment">// 一般placement new operator并不支持多态。</span></span><br><span class="line"><span class="comment">// 如果derived class比base class大，Point3w的constructor会导致严重破坏。</span></span><br><span class="line">Point2w *p2w = <span class="keyword">new</span> (arena) Point3w;</span><br></pre></td></tr></table></figure>

<p>  Placement new operator引入C++2.0时，最晦涩难懂的问题就是：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Base</span> &#123;</span> <span class="keyword">int</span> j; <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">f</span><span class="params">()</span></span>; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Derived</span> :</span> Base &#123; <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span></span>; &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fooBar</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Base b;</span><br><span class="line">    b.f();      <span class="comment">// Base::f()被调用</span></span><br><span class="line">    b.~Base();</span><br><span class="line">    <span class="keyword">new</span>(&amp;b) Derived;    </span><br><span class="line">    b.f();      <span class="comment">// 哪个f()被调用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="6-3-临时性对象（Temporary-Objects）"><a href="#6-3-临时性对象（Temporary-Objects）" class="headerlink" title="6.3 临时性对象（Temporary Objects）"></a>6.3 临时性对象（Temporary Objects）</h4><ul>
<li><p>如果有函数：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T <span class="keyword">operator</span>+(<span class="keyword">const</span> T&amp;, <span class="keyword">const</span> T&amp;);</span><br></pre></td></tr></table></figure>

<p>  那么两个此对象的a和b进行<code>a+b;</code>。会产生临时变量放置传回的对象。详细过程是产生临时性对象，放置结果，使用T的copy constructor，把临时性对象当作c的初始值。当然，视operator+()定义而定，NRV优化可能实施起来。这导致直接求表达式结果，避免执行copy constructor和具名对象的destructor。至于会用哪种方式初始化，C++ Standard允许编译器有临时性对象的完全自由度：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T c = a + b;</span><br><span class="line"><span class="comment">// 而其中的加法运算符被定义为</span></span><br><span class="line">T <span class="keyword">operator</span>+(<span class="keyword">const</span> T&amp;, <span class="keyword">const</span> T&amp;);</span><br><span class="line"><span class="comment">// 或</span></span><br><span class="line">T T::<span class="keyword">operator</span>+(<span class="keyword">const</span> T&amp;);</span><br></pre></td></tr></table></figure>

<p>  那么实现根本<font color="red">不产生临时性变量</font>。如果以：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">c = a + b;</span><br></pre></td></tr></table></figure>

<p>  不能够忽略，它会导致下面结果：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T temp;</span><br><span class="line">temp.<span class="keyword">operator</span>+(a, b);   （<span class="number">1</span>）</span><br><span class="line">c.<span class="keyword">operator</span>=(temp);      （<span class="number">2</span>）</span><br><span class="line">temp.T::~T();</span><br></pre></td></tr></table></figure>

<p>  （1）那行意味着为构造的临时对象赋值给operator+()。意思是要么<strong>表达式结果被copy constructed到临时对象中</strong>，要么<strong>以临时对象取代NRV</strong>。然而不管哪一种情况都有问题。运算符函数并不为外加参数调用destructor（它期望新内存），所以需要在调用前先调用destructor。然而，<strong>转换语意被用来把copy assignment运算符的隐式调用操作和destructor和copy constructor来代替assignment操作</strong>。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">c.T::~T();</span><br><span class="line">c.T::T(a + b);</span><br></pre></td></tr></table></figure>

<p>  而以上操作都可以由使用者供应，而且它还会产生临时变量。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T c = a + b;</span><br><span class="line"><span class="comment">// 上面的比下面的操作更有效率被编译器转化</span></span><br><span class="line">c = a + b;</span><br></pre></td></tr></table></figure>
</li>
<li><p>第三种形式是，没有目标对象：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">a + b</span><br><span class="line"><span class="comment">// 不论怎么种情况</span></span><br><span class="line">String v;</span><br><span class="line">v = s + t + u;</span><br><span class="line"><span class="comment">// 或</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"%s\n"</span>, s + t);</span><br></pre></td></tr></table></figure>

<p> 都会产生临时对象，与s+t相关联。上述的printf可能不保证安全，因为它的正确性和s+t何时被摧毁有关。（malloc(0)）。例如对于该算式的一个可能的pre-Standard转换，可能造成重大灾难：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="comment">// 临时性对象被摧毁得太快（太早）了。</span></span><br><span class="line">String temp1 = <span class="keyword">operator</span>+(s, t);</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *temp2 = temp1.<span class="keyword">operator</span> <span class="keyword">const</span> <span class="keyword">char</span>*();</span><br><span class="line">temp1.~String();</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"%s\n"</span>, temp2);  <span class="comment">// temp2来自何方？</span></span><br></pre></td></tr></table></figure>

<p> 一种转换方式是在调用printf()之后实施String destructor。<font color="red">标准规格上</font>说：<font color="red"><strong>临时性对象的被摧毁，应该是对完整表达式（full-expression）求值过程种的最后一个步骤。该完整表达式造成临时对象的产生</strong></font>。<strong>完整表达式是被包裹的表达式种最外围的那个表达式</strong>。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// tertiary full expression with 5 sub-expressions</span></span><br><span class="line">((objA &gt; <span class="number">1024</span>) &amp;&amp; (objB &gt; <span class="number">1024</span>)) ? objA + objB : foo(objA, objB);</span><br></pre></td></tr></table></figure>

<p> 五个子算式内含在“？：完整表达式”中。<strong>任何子表达式所产生的任何临时对象，都应该在完整表达式被求值完成后，才可以毁去</strong>。  p273</p>
</li>
<li><p>临时性对象的生命规则有两个例外：</p>
<ul>
<li><p><strong>第一个例外发生在表达式被用来初始化一个object时</strong>。如果某个对象progNameVersion的初始化需要调用copy constructor，那么临时性对象的析构就不是我们期望的。C++ Standard要求说：<font color="red"><strong>凡持有表达式执行结果的临时性对象，应该存留到object的初始化操作完成为止</strong></font>。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* progNameVersion = progName + progVersion;</span><br><span class="line"><span class="comment">// C++ pseudo Code</span></span><br><span class="line">String temp;</span><br><span class="line"><span class="keyword">operator</span>+(temp, progName, progVersion);</span><br><span class="line">progNmaeVersion = temp.String::<span class="keyword">operator</span> <span class="keyword">char</span>*();</span><br><span class="line">temp.String::~String();</span><br></pre></td></tr></table></figure>

<p>  progNameVersion指向未定义的heap内存。</p>
</li>
<li><p><strong>第二个例外是当一个临时性对象被一个reference绑定时</strong>，例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> String &amp;space = <span class="string">" "</span>;</span><br><span class="line"><span class="comment">// 产生代码</span></span><br><span class="line"><span class="comment">// C++ pseudo Code</span></span><br><span class="line">String temp;</span><br><span class="line">temp.String::String(<span class="string">" "</span>);</span><br><span class="line"><span class="keyword">const</span> String &amp;space = temp;</span><br></pre></td></tr></table></figure>

<p>  很明晰那，临时性对象被摧毁，reference就没什么用了。所以Standard说：<br>  <font color="red"><strong>如果临时性对象被绑定与reference，对象将残留，直到reference的生命结束，直到临时对象的生命范畴（scope）结束</strong></font>。</p>
</li>
</ul>
</li>
</ul>
<h5 id="临时性对象的迷思（神话、传说）"><a href="#临时性对象的迷思（神话、传说）" class="headerlink" title="临时性对象的迷思（神话、传说）"></a>临时性对象的迷思（神话、传说）</h5>]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第三章 Data语义学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%B8%89%E7%AB%A0-Data%E8%AF%AD%E4%B9%89%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>一个空的class如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sizeof X == 1</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span> &#125;;                    <span class="comment">// sizeof X == 1</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Y</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123;&#125;;  <span class="comment">// sizeof X == 8</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Z</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123;&#125;;  <span class="comment">// sizeof X == 8</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> :</span> <span class="keyword">public</span> Y, <span class="keyword">public</span> Z &#123;&#125;;<span class="comment">// sizeof X == 12</span></span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Y --- X</span><br><span class="line">Z --- X</span><br><span class="line">A --- Y</span><br><span class="line">A --- Z</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>事实上并不是空的，它有一个隐藏的1byte大小，那是编译器安插进去的一个char</strong></font>。有机器上Y、Z得出大小是8。这个值的大小和机器有关，也和编译器有关：</p>
<ol>
<li><strong>语言本身所造成的额外负担（overhead）</strong>。当语言支持virtual base classes时，会导致额外负担。在derived中，反映在某种形式的<font color="red">指针</font>身上，它或者指向virtual base class subobject，或指向一个相关表格；表格中是virtual base class subobject地址。</li>
<li><strong>编译器对于特殊情况所提供的优化处理</strong>。</li>
<li><strong>Allgnment的限制</strong>。大部分机器结构体大小会收到alignment的限制（内存对齐），使它们能够更有效率在内存中被存取。<font color="red">alignment就是将数值调整到某数的整数倍</font>。</li>
</ol>
</li>
<li><p><font color="red">Empty virtual base class</font>已经成为C++OO设计的一个特有术语了。他提供一个<font color="red">virtual interface</font>，没有定义任何数据。</p>
</li>
<li><p>对于class A竟然大小为12这个结果。记住，<font color="red"><strong>一个virual base class sbobject只会在derived class中存在一份实例</strong></font>，不管它在继承体系中出现多少次。它的大小由以下决定：</p>
<ol>
<li><strong>被大家共享的唯一一个Class X，大小为1byte</strong>。</li>
<li><strong>Base class Y，减去因virtual base class X二配置的大小，结果是4bytes</strong>。</li>
<li><strong>class A自己的大小：0byte</strong>。</li>
<li><strong>class A的alignment数量</strong>。</li>
</ol>
</li>
<li><p>C++对象模型尽量以空间优化和存取速度优化的考虑来表现nonstatic data members，并且保持和C语言struct数据配置的兼容性。至于static data members，则被放置在程序的一个global data segment中，不会影响个别的class object大小。在程序之中，不管class被产生出多少个objects（经由直接产生或间接产生），static data members永远只存在一份实例。但是一个template class的static data members的行为稍有不同。</p>
</li>
<li><p>每一个class object必须有足够的大小容纳所有的nonstatic data members，因为它可能比你想象的还大，原因是：</p>
<ul>
<li><strong>编译器自动加上额外的data members</strong>，用以支持某些特性（virtual）。</li>
<li><strong>因为alignment的需要</strong>。</li>
</ul>
</li>
</ul>
<h4 id="3-1-Data-Member的绑定（The-Binding-of-a-Data-Member）"><a href="#3-1-Data-Member的绑定（The-Binding-of-a-Data-Member）" class="headerlink" title="3.1 Data Member的绑定（The Binding of a Data Member）"></a>3.1 Data Member的绑定（The Binding of a Data Member）</h4><ul>
<li><p>早期C++有两种防御性程序设计风格：</p>
<ol>
<li><p>把所有data members放在class声明处，以确保正确的绑定：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line">    <span class="comment">// ...etc</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>把所有的inline functions，不管大小都放在class声明之外：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">X</span><span class="params">(<span class="keyword">float</span>)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="comment">// ...etc</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">float</span> Point3d::X() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 但这种<strong>设计风格在C++2.0之后就不存在了</strong>。这个古老的语言规则称为“<font color="red">member rwriting rule</font>”。大概意思是“一个inline函数实体，在整个class声明未被完全看见之前，不会被评估求值（evaluated）的”。也就是说：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> x;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 对于函数本体的分析延迟，直到class声明右大括号出现才开始</span></span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> cons </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 分析在这里进行</span></span><br></pre></td></tr></table></figure>

<p> 因此一个inline member function躯体之内的data member绑定操作，会在整个class声明完成之后才发生。但这对member function的argument list并不是真的。argument list中的名称会在第一次遇到的时候被适当resolved完成。因此extern和nested type names之间的非直觉绑定操作还是会发生。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> length;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// length is resolved for global</span></span><br><span class="line">    <span class="comment">// _val is resolved for Point3d::_val</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">mumble</span><span class="params">(length val)</span> </span>&#123; _val = val; &#125;</span><br><span class="line">    <span class="function">length <span class="title">mumble</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _val; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// length必须在class对他第一个参考操作前被看见</span></span><br><span class="line">    <span class="comment">// 这样的声明使之前操作不合法</span></span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">float</span> length;</span><br><span class="line">    length _val;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 上述的语言状况，仍然需要某种防御性程序风格：<font color="red"><strong>请总是把“nested type 声明”放在class的起始处</strong></font>。这样做才能保证非直觉绑定的正确性。</p>
</li>
</ol>
</li>
</ul>
<h4 id="3-2-Data-Member的布局（Data-Member-Layout）"><a href="#3-2-Data-Member的布局（Data-Member-Layout）" class="headerlink" title="3.2 Data Member的布局（Data Member Layout）"></a>3.2 Data Member的布局（Data Member Layout）</h4><ul>
<li><p>已知一组data members：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x;</span><br><span class="line">    staic List&lt;Point3d*&gt; *freeList;</span><br><span class="line">    <span class="keyword">float</span> y;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> chunkSize  <span class="number">250</span>;</span><br><span class="line">    <span class="keyword">float</span> z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>Nonsatic data members在class object中的排列顺序和其被声明的顺序一样</strong>。</font>任何中介的static data members都不会放进对象布局之中。上述例子中，每个Point3d对象是由三个float组成的。static data members存放在程序的<font color="red">data segment</font>中，和个别class objects无关。<br>  C++ Standard要求，在同一个access section（private、public、protected等区段）中，members的排列只需要符合“较晚出现的members在class object中有较高的地址”这一条件就可以了。下面这个template funciton，接受两个data members，然后<strong>判断谁先出现在class object中</strong>。如果两个members都是不同的access sections中的第一个被声明者，函数就会判断哪个section先出现：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">class_type</span>, <span class="title">class</span> <span class="title">data_type1</span>, <span class="title">class</span> <span class="title">data_type2</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">char</span>* <span class="title">access_order</span>(<span class="title">data_type1</span> <span class="title">class_type</span>:</span>:*mem1, data_type2 class_type::*mem2)</span><br><span class="line">&#123;</span><br><span class="line">    assert (mem1 != mem2);</span><br><span class="line">    <span class="keyword">return</span> mem1 &lt; mem2 ? <span class="string">"Member 1 occurs first"</span> : <span class="string">"member 2 occurs first"</span>; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// call</span></span><br><span class="line">access_order(&amp;Point3d::z, &amp;Point3d::y);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-3-Data-Member的存取"><a href="#3-3-Data-Member的存取" class="headerlink" title="3.3 Data Member的存取"></a>3.3 Data Member的存取</h4><ul>
<li><p>已知这段代码：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d origin, *pt = &amp;origin;</span><br><span class="line"><span class="comment">// 存取data members， like this：</span></span><br><span class="line">origin.x = <span class="number">0.0</span>;</span><br><span class="line">pt-&gt;x = <span class="number">0.0</span>;</span><br></pre></td></tr></table></figure>

<p>  通过origin存取和通过pt存取有什么重大差异吗？</p>
</li>
</ul>
<h5 id="Static-Data-Members"><a href="#Static-Data-Members" class="headerlink" title="Static Data Members"></a>Static Data Members</h5><ul>
<li><p><font color="red">Static data members</font>，按字面意义，被编译器提出class之外，并被视为global变量。每个member的存取许可，以及class的关联，都不会招致任何空间上或执行时间上的额外负担。每个static data member只有一个实例。</p>
</li>
<li><p>但如果static data members是一个从<font color="red">复杂继承关系中继承</font>而来的，它仍然只有唯一一个实例，其存取路径仍然那么直接。如果static data member经由函数调用，或其他某些语法存取呢？例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">foobar().chunkSize = <span class="number">250</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可能的转化</span></span><br><span class="line">(<span class="keyword">void</span>) foobar();</span><br><span class="line">Point3d.chunkSize = <span class="number">250</span>;</span><br></pre></td></tr></table></figure>

<p>  若取一个static data member的地址，会得到一个指向其数据类型的指针，而不是指向其class member的指针，因为static member并不内含在一个class object之中：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&amp;Point3d::chunkSize;</span><br><span class="line"><span class="comment">// 会得到如下类型的内存地址</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>*</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果有两个classes，每个都声明了一个static member freeList，那么当它们放在程序的data segment时，会导致<font color="red">名称冲突</font>。编译器的解决方式是暗中对每个<font color="green">static data member编码</font>（<font color="red"><strong>name-mangling</strong></font>），以获得一个独一无二的程序识别代码。任何name-mangling做法都有两个重点：</p>
<ol>
<li>一个算法，<strong>推导出独一无二的名称</strong>。</li>
<li>万一编译系统（或环境工具）必须和使用者交谈，那些独一无二的名称可以轻易<strong>被推导回到原来的名称</strong>。 </li>
</ol>
</li>
</ul>
<h5 id="Nonstatic-Data-Members"><a href="#Nonstatic-Data-Members" class="headerlink" title="Nonstatic Data Members"></a>Nonstatic Data Members</h5><ul>
<li><p><font color="red">Nonstatic data members</font>直接放在每个class object中。除非经由<font color="red">显式</font>（<font color="green">explcit</font>）或<font color="red">隐式</font>的（<font color="green">implicit</font>），否则没有办法直接存取它们。只要在member funcion中直接处理一个nonstatic data member，implicit class object就会发生。例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d Point3d::translate(<span class="keyword">const</span> Point3d &amp;pt) &#123;</span><br><span class="line">    x += pt.x;</span><br><span class="line">    y += pt.y;</span><br><span class="line">    z += pt.z;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  表面上x、y、z直接存取，事实上是经由implicit class object（由this指针表述）完成的。这个函数的参数是：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// member function的内部转化</span></span><br><span class="line">Point3d Point3d::translate(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, <span class="keyword">const</span> Point3d &amp;pt) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;x += pt.x;</span><br><span class="line">    <span class="keyword">this</span>-&gt;y += pt.y;</span><br><span class="line">    <span class="keyword">this</span>-&gt;z += pt.z;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>欲对一个nonstatic data member进行存取操作，编译器需要把<strong>class object的起始地址加上data member的偏移位置（offset）</strong>。例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">origin._y = <span class="number">0.0</span>;</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">&amp;origin + (&amp;Point3d::_y - <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>  每一个nonstatic data member的偏移位置（offset）在编译时期即可获知，甚至如果派生自单一或多重继承串链也是一样。</p>
</li>
<li><p>再来看看<font color="red">虚拟继承</font>。虚拟继承将为经由base class subobject存取class members导入一层间接性。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">origin.x = <span class="number">0.0</span>;</span><br><span class="line">pt-&gt;x = <span class="number">0.0</span>;</span><br></pre></td></tr></table></figure>

<p>  从origin存取和从pt存取有什么重大差异？答案是当Pointe3d是一个derived class，而其继承结构中有一个virtual base class，并且被存取member是一个从该virtual base class继承而来的member就会有重大差异。这时候，我们无法在编译时期直到member真正的offset位置。<font color="red">这个存取操作必须延迟到执行期</font>。如果使用origin就不会有这个问题。</p>
</li>
</ul>
<h4 id="3-4-“继承”于Data-Member"><a href="#3-4-“继承”于Data-Member" class="headerlink" title="3.4 “继承”于Data Member"></a>3.4 “继承”于Data Member</h4><ul>
<li><p>如果为2D和3D坐标点提供两个抽象数据类型：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point2d</span><br><span class="line">Point3d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// supporting abstract data types</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// constructor(s)</span></span><br><span class="line">    <span class="comment">// operations</span></span><br><span class="line">    <span class="comment">// access functions</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x, y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// constructor(s)</span></span><br><span class="line">    <span class="comment">// operations</span></span><br><span class="line">    <span class="comment">// access functions</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这和提供两层或三层继承结构，每一层（代表一个维度）是一个class，派生自较低维层次有什么不同？</p>
</li>
</ul>
<h5 id="只要继承不要多态（Inheritance-without-Polymorphism）"><a href="#只要继承不要多态（Inheritance-without-Polymorphism）" class="headerlink" title="只要继承不要多态（Inheritance without Polymorphism）"></a>只要继承不要多态（Inheritance without Polymorphism）</h5><ul>
<li><p>我们可能希望不论是2D或3D坐标点，能共享同一个实例，但又能继续使用于类型性质相关的实例。以上的设计策略。带来的影响则是可以共享数据本身以及数据处理方法，并将它局部化。一般而言，<font color="red">具体继承</font>（<font color="green">concrete inheritance</font>，相对于虚拟继承virtual inheritance）并不会增加空间或存取时间上的额外负担。</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d --- Point2d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">y</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _y; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">x</span><span class="params">(<span class="keyword">float</span> newX)</span> </span>&#123; _x = newX; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">y</span><span class="params">(<span class="keyword">float</span> newY)</span> </span>&#123; _y = newY; &#125;</span><br><span class="line">    <span class="keyword">void</span> <span class="keyword">operator</span>+=(<span class="keyword">const</span> Piont2d &amp;rhs) &#123;</span><br><span class="line">        _x += rhs.x();</span><br><span class="line">        _y += rhs.y();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// inheritance from concrete class</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Point2d(x, y), _z(z) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span> newZ)</span> </span>&#123; _z = newZ; &#125;</span><br><span class="line">    <span class="keyword">void</span> oeprator+=(<span class="keyword">const</span> Point3d &amp;rhs) &#123;</span><br><span class="line">        Point2d::<span class="keyword">operator</span>+=(rhs);</span><br><span class="line">        _z += rhs.z();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这样的设计的好处是可以把管理x和y坐标的程序代码局部化。也表现处两个类之间的紧密关系。但把原本两个独立不相干的classes凑成一堆”type/subtype“，并带有继承关系，会有什么易犯的错误呢？</p>
<ul>
<li><p>经验不足的人可能会重复设计一些相同操作的函数。以例子中的constructor和operator+=为例，它们并没有被做成inline函数。</p>
</li>
<li><p>第二是，把class分解成两层或更多层，可能会为了”表现class继承体系的抽象化“而膨胀所需的空间。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> c1;</span><br><span class="line">    <span class="keyword">char</span> c2;</span><br><span class="line">    <span class="keyword">char</span> c3;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在32位机器中，每个Concrete class object大小是<strong>8bytes</strong>，细分如下:</p>
</li>
</ul>
<ol>
<li><p>val占用4bytes；</p>
</li>
<li><p>c1、c2和c3各占用1bytes；</p>
</li>
<li><p>alignment（调整到word边界）需要1bytes。</p>
<p>现在把Concrete分裂为三层结构：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Concrete2 --- Concrete1</span><br><span class="line">Concrete3 --- Concrete2</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete1</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit1;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete2</span> :</span> <span class="keyword">public</span> Concrete1 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete3</span> :</span> <span class="keyword">public</span> Concrete2 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit3;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>现在Concrete3 object的大小是16bytes，比原先的设计多了100%。（p106）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Concrete2 *pc2;</span><br><span class="line">Concrete1 *pc1_1, *pc1_2;</span><br><span class="line"><span class="comment">// 如果C++把derived class members和Concrete1 subobject捆绑在一起，去除填补空间</span></span><br><span class="line">pc1_1 = pc2;    <span class="comment">// pc1_1指向Concrete2对象</span></span><br><span class="line"><span class="comment">// derived class subobject被覆盖掉</span></span><br><span class="line"><span class="comment">// 于是bit2 member现在有了一个并非预期的数值</span></span><br><span class="line">*pc1_2 = *pc1_1;</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
<h5 id="加上多态（Adding-Polymorphism）"><a href="#加上多态（Adding-Polymorphism）" class="headerlink" title="加上多态（Adding Polymorphism）"></a>加上多态（Adding Polymorphism）</h5><ul>
<li><p>如果在继承关系中提供要给virtual function接口：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;;</span><br><span class="line">    <span class="comment">// 之前的存取操作</span></span><br><span class="line">    <span class="comment">// 加上z的保留空间（目前什么也不做）</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">0.0</span>; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span>)</span> </span>&#123;&#125;</span><br><span class="line">    <span class="comment">// 谁当以下运算符为virtual</span></span><br><span class="line">    <span class="keyword">virtual</span> <span class="keyword">void</span> <span class="keyword">operator</span>+=(<span class="keyword">const</span> Point2d &amp;rhs) &#123;</span><br><span class="line">        _x += rhs.x();</span><br><span class="line">        _y += rhs.y();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line">pblic:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0l</span>0, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Ponit2d(x, y), _z(z) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span> newZ)</span> </span>&#123; _z = newZ; &#125;</span><br><span class="line">    vodi <span class="keyword">operator</span>+=(<span class="keyword">const</span> Piont2d &amp;rhs) &#123;</span><br><span class="line">        Point2d::oeprator+=(rhs);</span><br><span class="line">        _z += rhs.z();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这个设计有个好处是可以把operator+=运用在一个Pointe3d对象和一个Point2对象身上：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point2d <span class="title">p2d</span><span class="params">(<span class="number">2.1</span>, <span class="number">2.2</span>)</span></span>;</span><br><span class="line"><span class="function">Point3d <span class="title">p3d</span><span class="params">(<span class="number">3.1</span>,, <span class="number">3.2</span>, <span class="number">3.3</span>)</span></span>;</span><br><span class="line">p3d += p2d;</span><br></pre></td></tr></table></figure>

<p>  虽然class的声明语法没变，但事情不一样了：z() member function和operator+=()运算符都成了虚函数；<font color="red"><strong>每个Point3d class object内含一个额外的vptr member和一个Piont3d virtual table</strong></font>；此外每个virtual member function的调用也复杂了。</p>
</li>
<li><p>C++编辑器领域主要讨论的问题是把vptr放置在class object哪里最好？cfont编译器中，被放在class object的尾端：  </p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">no_virts</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> d1, d2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">has_virts</span> :</span> <span class="keyword">public</span> no_virts &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> d3;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">no_virts *p = <span class="keyword">new</span> has_virts;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">struct no_virts nv;</th>
<th align="center">class has_virts : <br>public no_virts hv;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">int d1</td>
<td align="center"><strong>int d1</strong></td>
</tr>
<tr>
<td align="center">int d2</td>
<td align="center"><strong>int d2</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">int d3</td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center"><strong>vptr</strong>has_virts</td>
</tr>
<tr>
<td align="center">把vptr放在object尾端，可以保留base class C struct的对象布局，因而允许C程序代码也能使用，这做法出现在C++问世时。</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">到了C++2.0，开始支持<font color="red">虚继承</font>以及<font color="red">抽象基类</font>，某些编译器开始把vptr放到class object前端：</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">struct no_virts nv;</td>
<td align="center">class has_virts : <br>public no_virts hv;</td>
</tr>
<tr>
<td align="center">:-:</td>
<td align="center">:-:</td>
</tr>
<tr>
<td align="center">int d1</td>
<td align="center"><strong>vptr</strong>has_virts</td>
</tr>
<tr>
<td align="center">int d2</td>
<td align="center"><strong>int d1</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center"><strong>int d2</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">int d3</td>
</tr>
<tr>
<td align="center">vptr放在class object前端，对于多继承下通过指向class members的指针调用virtual function会有一些帮助。否则，<strong>不仅从class object起始点开始量起的offset必须在执行器备妥，class vptr之间的offset也必须备妥</strong>。</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">以下是Point2d和Point3d加上了virtual function之后的继承布局（单一继承）：</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">class Point2d p2d;</td>
<td align="center">class Point3d :<br>public Point2d pt3d;</td>
</tr>
<tr>
<td align="center">:-:</td>
<td align="center">:-:</td>
</tr>
<tr>
<td align="center">float _x</td>
<td align="center"><strong>float _x</strong></td>
</tr>
<tr>
<td align="center">float _y</td>
<td align="center"><strong>float _y</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
<td align="center"><strong><strong>vptr</strong>Point2d</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">float _z</td>
</tr>
</tbody></table>
</li>
</ul>
<h5 id="多重继承（Multiple-Inheritance）"><a href="#多重继承（Multiple-Inheritance）" class="headerlink" title="多重继承（Multiple Inheritance）"></a>多重继承（Multiple Inheritance）</h5><ul>
<li><p>单一继承提供了一种<font color="red">自然多态</font>（<font color="green">natural polymorphism</font>）形式，是关于base type和derived type之间的转换。base 它们的object都是从相同的地址开始。差异只在<strong>derived object比较大，用以容纳它自己的nonstatic data members</strong>。一个derived class指定给base class的指针或reference。这个操作并不需要编译器调停或修改地址。可以很自然地发生，而且提供了最佳执行效率。</p>
</li>
<li><p>多重继承不像单一继承，也不容易模塑处模型。它的复杂度在于derived class和其base class之间的非自然关系：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d --- Point2d </span><br><span class="line">Vertex3d --- Point3d</span><br><span class="line">Vertex3d --- Vertex</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...有virtual接口，会有vptr</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... vptr</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Vertex *next</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex3d</span> :</span> <span class="keyword">public</span> Point3d, <span class="keyword">public</span> Vertex &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  多重继承的问题主要发生在derived class objects和后继base class objects之间的转换，不论是：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">(<span class="keyword">const</span> Vertex&amp;)</span></span>;</span><br><span class="line">Vertex3d v;</span><br><span class="line">...</span><br><span class="line">mumble(v); <span class="comment">// 不自然</span></span><br></pre></td></tr></table></figure>

<p>  或是经由所支持的virtual function机制做转换。多继承对象符出的成本在于地址的指定操作而已：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Vertex3d v3d;</span><br><span class="line">Vertex *pv;</span><br><span class="line">Point2d *p2d;</span><br><span class="line">Point3d *p3d;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指定操作</span></span><br><span class="line">pv = &amp;v3d;</span><br><span class="line"><span class="comment">// 需要内部转化</span></span><br><span class="line">pv = (Vertex*)(((<span class="keyword">char</span>*)&amp;v3d) + <span class="keyword">sizeof</span>(Point3d));</span><br><span class="line"><span class="comment">// 如果v3d为指针类型，即pv3d，则:</span></span><br><span class="line">pv = pv3d ? (Vertex*)((<span class="keyword">char</span>*)pv3d) + <span class="keyword">sizeof</span>(Point3d) : <span class="number">0</span>; <span class="comment">// 防止pv3d为0</span></span><br></pre></td></tr></table></figure>

<p>  以下为<font color="red">多重继承</font>（<font color="green">Multiple Inheritance</font>)：</p>
<table>
<thead>
<tr>
<th align="center">class Point2d pt2d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">class Point3d :<br>public Point2d pt3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">Vertex v;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">class Vertex3d :<br> public Point3d,<br>public Vertex<br>{}v3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
<tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
<tr>
<td align="center">float mumble</td>
</tr>
</tbody></table>
</li>
</ul>
<h5 id="虚拟继承（Virtual-Inheritance）"><a href="#虚拟继承（Virtual-Inheritance）" class="headerlink" title="虚拟继承（Virtual Inheritance）"></a>虚拟继承（Virtual Inheritance）</h5><ul>
<li><p>多重继承的一个语意上的副作用是，<font color="red"><strong>必须支持某种形式上的”shared subobject继承“</strong></font>。如早期的iostream library：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pre-standard iostream implementation</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ios</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">istream</span> :</span> <span class="keyword">public</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ostream</span> :</span> <span class="keyword">public</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">iostream</span> :</span> <span class="keyword">public</span> istream, <span class="keyword">public</span> ostream &#123; ... &#125;;</span><br></pre></td></tr></table></figure>

<p>  下图可表现iostream的继承体系图。第一个为多重继承，第二个为虚拟多重继承：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">istream --- ios1[ios]</span><br><span class="line">ostream --- ios2[ios]</span><br><span class="line">iostream --- istream</span><br><span class="line">iostream --- ostream</span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">istream --- ios</span><br><span class="line">ostream --- ios</span><br><span class="line">iostream --- istream</span><br><span class="line">iostream --- ostream</span><br></pre></td></tr></table></figure>

<p>  在iostream对象布局中，只需要一份就好：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ios</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">istream</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ostream</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">iostream</span> :</span> <span class="keyword">public</span> istream, <span class="keyword">public</span> ostream &#123; ... &#125;;</span><br></pre></td></tr></table></figure>

<p>  上述iostream的实现挑战在于：<strong>一个有效的方法，将istream和ostream各自维护ios subobjet，折叠成由iostream维护的单一ios subobject，并且保存base class和derived class的指针（reference）之间的<font color="red">多态指定操作</font>（<font color="green">polymorphism assignments</font>)</strong>。</p>
</li>
<li><p>一般的实现方法是。Class如果内含一个或多个virtual base class subobjects，像istream那样，将被分割两部分：<font color="red"><strong>一个不变区域</strong></font>和<font color="red"><strong>一个共享区域</strong></font>。不变区域中的数据，总是有<strong>固定的offset</strong>，所以这里可以直接存取。而共享区域，就是virtual base class subobject。它们只能被间接存取。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Vertex *next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex3d</span> :</span> <span class="keyword">public</span> Vertex, <span class="keyword">public</span> Point3d &#123;</span><br><span class="line"><span class="keyword">public</span> :</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Vertex -- _x,_y--- Point2d</span><br><span class="line">Point3d -- _z --- Point2d</span><br><span class="line">Vertex3d -- next --- Vertex</span><br><span class="line">Vertex3d -- _z --- Point3d</span><br><span class="line">none[ ] -- mumble --- Vertex3d</span><br></pre></td></tr></table></figure>

<p>  这中间存在一个问题：如何能够存取class共享部分呢？cfont编译器是在derived class object中插指针，每个指向virtual base class。需要完成操作都是通过指针间接完成的。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> Point3d::<span class="keyword">operator</span>+=(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    _X += rhs._x;</span><br><span class="line">    _y += rhs._y;</span><br><span class="line">    _z += rhs._z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转化为虚构代码vbc为virtual base class</span></span><br><span class="line">__vbcPoint2d-&gt;_x += rhs.__vbcPoint2d-&gt;_x;</span><br><span class="line">__vbcPoint2d-&gt;_y += rhs.__vbcPoint2d-&gt;_y;</span><br><span class="line">_z += rhs._z;</span><br></pre></td></tr></table></figure>

<p>  这样实现模型有两个主要缺点：</p>
<ol>
<li><strong>每个对象针对其每个virtual base class背负一个额外的指针</strong>。而我们希望class object负担是稳定的。</li>
<li><strong>虚继承链加长会导致存取层次增加</strong>。我们呢希望有着固定的存取时间。</li>
</ol>
</li>
<li><p>对于第二个问题。MetaWare和其他编译器到今天还是用cfont原始模型：它们经由拷贝操作取得所有nested virtual base class指针，放到derived class object中，虽然有空间代价。</p>
</li>
<li><p>至于第一个问题，有两个解决方法。Microsoft编译器引入virtual base class table。每个class object如果有一个以上virual base classes，编译器就安插指针，指向virtual base class table，真正的vptr则被放在该表格中。第二个解决方法，是在virtual function table中放置virtual base class的offset（而非地址）。</p>
<table>
<thead>
<tr>
<th align="center">class Vertex3d :<br>public Vertex,<br>public Point3d<br>{…} v3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>Point2d *pPoint2d</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
<tr>
<td align="center"><strong>Point2d *pPoint2d</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point3d</td>
</tr>
<tr>
<td align="center">float mumble</td>
</tr>
<tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">该方法把<font color="red">virtual base class offset</font>和<font color="red">virtual function entires</font>混杂在一起。Sum编译器中，virtual function table由正值或负值来索引。正值则索引到virtual functions；负值则索引到virtual base class offsets。</td>
</tr>
<tr>
<td align="center"><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">this</span> + __vptr__Point3d[<span class="number">-1</span>])-&gt;_x +=</span><br><span class="line">    (&amp;rhs + rhs.__vptr__Point3d[<span class="number">-1</span>])-&gt;_x;</span><br><span class="line">(<span class="keyword">this</span> + __vptr__Point3d[<span class="number">-1</span>])-&gt;_y += </span><br><span class="line">    (&amp;rhs + rhs.__vptr__Point3d[<span class="number">-1</span>])-&gt;_y;</span><br><span class="line">_ += rhs._z;</span><br></pre></td></tr></table></figure></td>
</tr>
</tbody></table>
<p>  因此Derived class实例和base class实例之间的转换操作为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point2d *p2d = pv3d;</span><br><span class="line"><span class="comment">// translation</span></span><br><span class="line">Point2d *pt2 = pv3d ? pv3d + pv3d-&gt;__vptr__Point3d[<span class="number">-1</span>] : <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-5-对象成员的效率（Object-Member-Efficiency）"><a href="#3-5-对象成员的效率（Object-Member-Efficiency）" class="headerlink" title="3.5 对象成员的效率（Object Member Efficiency）"></a>3.5 对象成员的效率（Object Member Efficiency）</h4><ul>
<li>下面测试旨在测试<font color="red">聚合</font>（<font color="green">aggregation</font>）、<font color="red">封装</font>（<font color="green">encapsulation</font>）以及<font color="red">继承</font>（<font color="green">inheritance</font>）所引发的额外负荷程序。跳过。</li>
</ul>
<h4 id="3-6-指向Data-Members的指针（Poniter-to-Data-Members）"><a href="#3-6-指向Data-Members的指针（Poniter-to-Data-Members）" class="headerlink" title="3.6 指向Data Members的指针（Poniter to Data Members）"></a>3.6 指向Data Members的指针（Poniter to Data Members）</h4><ul>
<li><p>考虑下面Point3d声明。有一个virtual function，一个static data member，以及三个坐标：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Point3d();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">static</span> Point3d origin;</span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  每个Point3d object含有三个坐标值，依序为x、y、z，以及vptr。static data member origin放在class object之外。vptr的位置根据编译器不同而不同。不是放头就是尾。<code>&amp;Point3d::z;</code>取得某个坐标成员的地址代表什么呢？<strong>代表着z坐标在class object中偏移位置（offset）</strong>。</p>
</li>
<li><p>一台32位机器上，每一个float是4bytes，所以期望获取地址偏移位置要么是8要么是12。然而获取的总是多1，也就是9和13（我的输出并没有+1）。</p>
</li>
<li><p>在多继承下，要将第二个或后继base class的指针，和一个与derived class object绑定的member结合起来，那么将会因为需要加入offset值而变得复杂。例如:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Base1</span> &#123;</span> <span class="keyword">int</span> val1; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Base2</span> &#123;</span> <span class="keyword">int</span> val2; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Derived</span> :</span> Base1, Base2 &#123; ... &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func1</span><span class="params">(<span class="keyword">int</span> Derived::*dmp, Derived *pd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 期望第一个参数是一个derived class的member指针，结果是base class的会怎样。</span></span><br><span class="line">    pd-&gt;*dmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func2</span><span class="params">(Derived *pd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// bmp将成为1</span></span><br><span class="line">    <span class="keyword">int</span> Base2::*bmp = &amp;Base2::val2;</span><br><span class="line">    <span class="comment">// bmp == 1但在Derived中val2 == 5</span></span><br><span class="line">    func1(bmp, pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  要解决这个问题，必须：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 经由编译器内部转换</span></span><br><span class="line">func1(bmp ? bmp + <span class="keyword">sizeof</span>(Base1) : <span class="number">0</span>, pd);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="“指向Members的指针”的效率问题"><a href="#“指向Members的指针”的效率问题" class="headerlink" title="“指向Members的指针”的效率问题"></a>“指向Members的指针”的效率问题</h5><ul>
<li>下面是测试数据。了解在3D坐标点的不同class表现方式下指向members的指针所带来的影响。</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第五章 构造、析构、拷贝语意学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%BA%94%E7%AB%A0-%E6%9E%84%E9%80%A0%E3%80%81%E6%9E%90%E6%9E%84%E3%80%81%E6%8B%B7%E8%B4%9D%E8%AF%AD%E6%84%8F%E5%AD%A6/</url>
    <content><![CDATA[<h5 id="纯虚函数的存在（Presence-of-a-Pure-Virtal-Function）"><a href="#纯虚函数的存在（Presence-of-a-Pure-Virtal-Function）" class="headerlink" title="纯虚函数的存在（Presence of a Pure Virtal Function）"></a>纯虚函数的存在（Presence of a Pure Virtal Function）</h5><ul>
<li><p>考虑abstract base class声明:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Abstract_base</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Abstract_base() = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">interface</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">mumble</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _mumble; &#125;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">char</span> *_mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这个class被设计为一个抽象的base class(其中有pure virtual function,使得Abstract_base不可能有实例),但需要一个显式的构造函数以初始化data member _mumble。如果没有初始化操作，derived class的局部性对象_mumble及那个无法决定初值。</p>
</li>
</ul>
<h5 id="纯虚函数的存在-Presence-of-a-Pure-Virtual-Function"><a href="#纯虚函数的存在-Presence-of-a-Pure-Virtual-Function" class="headerlink" title="纯虚函数的存在(Presence of a Pure Virtual Function)"></a>纯虚函数的存在(Presence of a Pure Virtual Function)</h5><ul>
<li>pure virtual function只能被<font color="red">静态地调用</font>（<font color="green">invoked statically</font>），不能经由虚拟机制调用。<strong>而pure virtual destructor，class设计者一定得定义它。因为每个derived class class以及base class destructor。只要缺乏定义，就会导致链接失败</strong>。</li>
</ul>
<h5 id="虚拟规格的存在（Presence-of-a-Virtual-Specification）"><a href="#虚拟规格的存在（Presence-of-a-Virtual-Specification）" class="headerlink" title="虚拟规格的存在（Presence of a Virtual Specification）"></a>虚拟规格的存在（Presence of a Virtual Specification）</h5><ul>
<li>如果决定把Abstract_base::mumble()设计为virtual function，那是糟糕的选择，因为其函数定义内容并不与类型有关，因而几乎不会被后继的derived class改写。</li>
</ul>
<h5 id="虚拟规格中const的存在"><a href="#虚拟规格中const的存在" class="headerlink" title="虚拟规格中const的存在"></a>虚拟规格中const的存在</h5><ul>
<li>决定要给virtual function是否需要const，是一件繁琐的事情。如果声明为const，意味着subclass实例可能被无穷次数地使用。不把函数声明为const，意味和函数不能获得一个const reference或const pointer。头大的是，声明函数为const，才发现derived instance必须修改某一data member。解决办法就是不再用const就是了。</li>
</ul>
<h5 id="重新考虑class的声明"><a href="#重新考虑class的声明" class="headerlink" title="重新考虑class的声明"></a>重新考虑class的声明</h5><ul>
<li>综上所述，这才是比较适当的一种设计：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Abstract_base</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Abstract_base();   <span class="comment">// nonpure virtual</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">interface</span><span class="params">()</span> </span>= <span class="number">0</span>;   <span class="comment">// nonconst</span></span><br><span class="line">    <span class="function"><span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">mumble</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _mumble; &#125; <span class="comment">// nonvirtual</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Abstract_base(<span class="keyword">char</span> *pc =ss <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">char</span> *_mumble;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="5-1-”无继承”情况下的对象构造"><a href="#5-1-”无继承”情况下的对象构造" class="headerlink" title="5.1 ”无继承”情况下的对象构造"></a>5.1 ”无继承”情况下的对象构造</h4><ul>
<li><p>考虑程序片段：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global;                   <span class="comment">//1</span></span><br><span class="line">                                <span class="comment">//2</span></span><br><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">()</span>                  <span class="comment">//3</span></span></span><br><span class="line"><span class="function"></span>&#123;                               <span class="comment">//4</span></span><br><span class="line">    Point local;                <span class="comment">//5</span></span><br><span class="line">    Point *heap = <span class="keyword">new</span> Point;    <span class="comment">//6</span></span><br><span class="line">    *heap = local;              <span class="comment">//7 </span></span><br><span class="line">    <span class="comment">// ... stuff ...            //8</span></span><br><span class="line">    <span class="keyword">delete</span> heap;                <span class="comment">//9</span></span><br><span class="line">    <span class="keyword">return</span> local;               <span class="comment">//10</span></span><br><span class="line">&#125;                               <span class="comment">//11</span></span><br></pre></td></tr></table></figure>

<p>  L1、L5、L6分别为：<strong>global内存配置</strong>、<strong>local内存配置</strong>和<strong>heap内存配置</strong>。一个object的声明，是该object的一个执行期属性。local object的生命从L5定义开始，L10为止。global object的生命和这个程序的生命相同。heap object的生命从被new运算符配置出来开始，到delete运算符摧毁结束。<br>  下面是Point的第一次声明，写成C程序。这是一种Plain OI’ Data声明形式：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">    Point;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  如果用C++来编译这代码，<strong>编译器会为Point声明一个trivial default constructor、一个trivial destructor、一个trivial copy constructor，以及一个trivial copy assignment operator</strong>。但实际上，编译器会分析这个声明，并<font color="red">贴上POD标签</font>。</p>
</li>
<li><p>当编译器遇到这样的定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global;                   <span class="comment">//1</span></span><br></pre></td></tr></table></figure>

<p>  观念上Point的trival constructor和destructor会被产生并调用，constructor在程序起始（<font color="red">startup</font>）被调用，而destructor在程序的<font color="red">exit()</font>处被调用。然而事实上trivial members要么没定义，要么没被调用。<br>  在C中，global被视为一个<font color="red">临时性的定义</font>，因为它没有显式的初始化操作。它可以在程序中发生多次。实例<strong>会被折叠起来，只留下一个，放在程序data segment中</strong>。这块空间称为<font color="red"><strong>BSS</strong></font>，这是Block Started by Symbol缩写。<br>  C++并不支持临时性定义。虽然它可以判断class object或是POD。global在C++中被视为完全定义（会阻止多个定义）。C和C++的差异是，BSS data segment在C++中相对不重要。<font color="red"><strong>所有的全局变量都被以“初始化过的数据”来对待</strong></font>。<br>  至于foobar()函数的L5，既没有构造也没有被析构，不过可能没有经过初始化就会成BUG（L7）。至于heap object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point *heap = <span class="keyword">new</span> Point;    <span class="comment">//6</span></span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line">Point *heap = __new(<span class="keyword">sizeof</span>(Point));</span><br><span class="line"><span class="comment">// 有初始化过就没问题</span></span><br><span class="line">*heap = local;              <span class="comment">//7</span></span><br></pre></td></tr></table></figure>

<p>  事实上L7会产生编译警告，观念上，这样的指定操作会触发trivial copy assignment operator做拷贝搬运操作。而实际上object是POD，所以assignment只是像C那样纯粹bitwise搬运。</p>
</li>
</ul>
<h5 id="抽象数据类型（Abstract-Data-Type）"><a href="#抽象数据类型（Abstract-Data-Type）" class="headerlink" title="抽象数据类型（Abstract Data Type）"></a>抽象数据类型（Abstract Data Type）</h5><ul>
<li><p>以下是Point声明，提供了完整封装性，但没有提供virtual function：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : _x(x), _y(y), _z(z) &#123;&#125;</span><br><span class="line">    <span class="comment">// no copy constructor, copy operator</span></span><br><span class="line">    <span class="comment">// or destructor defined ...</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y, _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这里还是三个连续的float，不论private或public存取层或是member function都不会占用额外的对象空间。没有定义copy constructor或copy operator，因为有<strong>default bitwise semantics</strong>足够了，也不需要destructor。对于实例：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global; <span class="comment">// Point::Point(0.0, o.0, 0.0);</span></span><br></pre></td></tr></table></figure>

<p>  有了default constructor，global被定义在全局范畴，初始化操作将延迟到程序启动（startup）。<br>  如果要将class中的所有成员设定常量初值，给予一个<font color="red">explicit initialization list</font>会比较有效率（相比constructor的inline expansion而言）。但它有三项缺点：</p>
<ol>
<li>只有当class members都是public，才会奏效。</li>
<li>只能指定常量，因为在编译器就能评估求值。</li>
<li>由于编译器并不自动施行，初始化行为可能性很高。</li>
</ol>
</li>
</ul>
<h5 id="为继承做准备"><a href="#为继承做准备" class="headerlink" title="为继承做准备"></a>为继承做准备</h5><ul>
<li><p>第三个Point声明，为继承性质和某些操作的<font color="red">动态决议</font>（<font color="green">dynamic resolution</font>）做准备，目前限制z成员做存取操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;</span><br><span class="line">    <span class="comment">// no destructor, copy constructor, or</span></span><br><span class="line">    <span class="comment">// copy operator defined ... </span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  virtual functions的导入在这里附带一个virtual destructor的声明在这个例子里，并无好处。除此之外，每个class object多负担一个vptr之外，virtual function的导入也引发编译器对Point class产生的膨胀作用：</p>
<ul>
<li><strong>定义的constructor附带了一些代码，以便vptr初始化</strong>：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line">Point* Point::Point(Point *<span class="keyword">this</span>, <span class="keyword">float</span> x, <span class="keyword">float</span> y) : _x(x), _y(y)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 设定object的vptr</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point = __vtbl__Point;</span><br><span class="line">    <span class="comment">// 扩展member initialization list</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;_x = x;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_y = y;</span><br><span class="line">    <span class="comment">// 传回this</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ul>
</li>
</ul>
<pre><code>+ **合成一个copy constructor和copy assignment operator，而且操作不是trivial（implicit destructor仍然是）**。如果Point object被初始化或以derived class object赋值，那么以为基础（bitwise）的操作可能对vptr带来非法设定：
    <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="comment">// copy constructor 内部合成</span></span><br><span class="line"><span class="keyword">inline</span> Point* Point::Point(Point*<span class="keyword">this</span>, <span class="keyword">const</span> Point &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// object的vptr</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point = __vtbl__Point;</span><br><span class="line">    <span class="comment">// copy</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

编译器在优化状态会把object内容连续拷贝到另一个object，而不是精准memberwise。**L7的memberwise赋值操作可能出发copy assignment operator的合成，及调用&lt;font color=red&gt;inline expansion&lt;/font&gt;（行内扩张）：以this取代heap，以rhs取代local**。  
戏剧性的冲击在L10。由于copy constructor的出现，foobar()可能转化为：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">(Point &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Point local;</span><br><span class="line">    local.Point::Point(<span class="number">0.0</span>, <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// heap</span></span><br><span class="line">    <span class="comment">// copy constructor</span></span><br><span class="line">    __result.Point::Point(local);</span><br><span class="line">    <span class="comment">// local destructor</span></span><br><span class="line">    <span class="comment">// Point::derstructor</span></span><br><span class="line">    <span class="comment">// local.Point::~Point();</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

如果NRV优化，转化为：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">(Point &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    __result.Point::Point(<span class="number">0.0</span>, <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// heap</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre><h4 id="5-2-继承体系下的对象构造"><a href="#5-2-继承体系下的对象构造" class="headerlink" title="5.2 继承体系下的对象构造"></a>5.2 继承体系下的对象构造</h4><ul>
<li><p>当定义一个object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T object;</span><br></pre></td></tr></table></figure>

<p>  如果T有constructor，它会被调用。除此之外，constructor调用伴随了什么？constructor可能内含大量隐藏码，因为<font color="red"><strong>编译器会扩充每个constructor</strong></font>：</p>
<ol>
<li>在member initialization list中的data members初始化操作会被放进constructor函数本体，以声明顺序为顺序。</li>
<li>如果member没有在member initialization list中，但这个member有default constructor，那么被调用。</li>
<li>在那之前，如果class object有vptr，必须被设定初值指向适当vtbls。</li>
<li>在那之前，base class constructors必须被调用，以base class声明顺序为顺序。<ul>
<li><strong>如果base class被列入member initialization list中，那么任何显式参数都传递进去</strong>。</li>
<li><strong>如果base class没有被列入member initialization list中，而它有default constructor（或default memberwie copy constructor），那么调用它</strong>。</li>
<li><strong>如果base class是多层继承下的第二或后继base class，那么调整this指针</strong>。</li>
</ul>
</li>
<li>在那之前，所有virtual base class constructors必须被调用，从左到右，从深到浅：<ul>
<li><strong>如果class被列入member initialization list中，那么任何显式指定的参数，都传递进去。如果没有列入list中，而class有default constructor，调用它</strong>。</li>
<li><strong>class中的每个virtual base class subobject的偏移地址（offset）必须在执行期可被存取</strong>。</li>
<li><strong>如果class object是最底层（most-derived）的class，其constructors可能被调用；用以支持者行为的机制必须放出来</strong>。</li>
</ul>
</li>
</ol>
</li>
<li><p>以Point为例，探讨constructors扩充的必要性：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>);</span><br><span class="line">    Point(<span class="keyword">const</span> Point&amp;);            <span class="comment">// copy constructor</span></span><br><span class="line">    Point&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Point&amp;); <span class="comment">// copy assignment operator</span></span><br><span class="line">    <span class="keyword">virtual</span> ~Point();               <span class="comment">// virtual derstructor</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">0.0</span>; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span> &#123;</span></span><br><span class="line">    Point _begin, _end;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Line(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    Line(<span class="keyword">const</span> Point&amp;, <span class="keyword">const</span> Point&amp;);</span><br><span class="line">    draw();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  。。。。。。。。</p>
</li>
</ul>
<h5 id="虚拟继承-Virtual-Inheritance"><a href="#虚拟继承-Virtual-Inheritance" class="headerlink" title="虚拟继承(Virtual Inheritance)"></a>虚拟继承(Virtual Inheritance)</h5><ul>
<li><p>考虑下面虚拟继承，继承自Point：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Point(x, y), _z(z) &#123;&#125;</span><br><span class="line">    Point3d(<span class="keyword">const</span> Point3d &amp;rhs) : Point(rhs), _z(rhs._z) &#123;&#125;</span><br><span class="line">    ~Point3();</span><br><span class="line">    Point3d&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Point3d&amp;);</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  传统的constructor扩充并没有用，因为virtual base class的共享性之故：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不合法</span></span><br><span class="line">Point3d* Point3d::Point3d(Point3d *<span class="keyword">this</span>, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Piont3d = __vtbl_Point3d;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d__Point = __vtbl_Point3d_Point;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_z = rhs._z;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  看出什么错误了吗？</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d -- virtual --- Point</span><br><span class="line">Vertex -- virtual --- Point</span><br><span class="line">Vertex3d -- public --- Point3d</span><br><span class="line">Vertex3d -- public --- Vertex</span><br><span class="line">pVertex -- public --- Vertex3d</span><br></pre></td></tr></table></figure>

<p>  Vertex的constructor必须调用Point constrcutor。然而当Point3d和Vertex为Vertex3d的subobjects时，则调用操作不一定发生。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Point3d的constructor扩充内容</span></span><br><span class="line">Point3d* Point3d::Point3d(Point3d *<span class="keyword">this</span>, <span class="keyword">bool</span> __most_derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (__most_derived != <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d = __vtbl_Point3d;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d__Point = __vtbl_Point3d__Point;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_Z = ths._z;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  Vertex3d调用Point3d和Vertex的constructor时，会把__most_derived参数设为false，于是就压制了对两个constructors中对Point constructor的调用操作。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Vertex3d* Vertex3d::Vertex3d(Vertex3d *<span class="keyword">this</span>, <span class="keyword">bool</span> __most_derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (__most_derived != <span class="literal">false</span>) </span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point3d::Point3d(<span class="literal">false</span>, x, y, z);</span><br><span class="line">        <span class="keyword">this</span>-&gt;Vertex::Vertex(<span class="literal">false</span>, x, y);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  这个策略得以保持语义正确。当定义一个<strong>Point3d</strong>时，Point3d constructor可以正确调用Point virtual base class subobject。当定义<strong>Vertex3d时</strong>时，Vertex3d constructor正确调用Point constructor。Point3d和Vertex的constructor不会对Point调用。这种把一个constructor分裂为二的做法，可以带来速度的提升。？？</p>
</li>
</ul>
<h5 id="vptr初始化语意学（The-Semantics-of-the-vptr-Initialization）"><a href="#vptr初始化语意学（The-Semantics-of-the-vptr-Initialization）" class="headerlink" title="vptr初始化语意学（The Semantics of the vptr Initialization）"></a>vptr初始化语意学（The Semantics of the vptr Initialization）</h5><ul>
<li>当我们定义PVertex object时，constructors的调用顺序是：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point(x, y);</span><br><span class="line">Point3d(x, y, z);</span><br><span class="line">Vertex(x, y, z);</span><br><span class="line">Vertex3d(x, y, z);</span><br><span class="line">PVertex(x, y, z);</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><p>假设继承体系中每个class都定义一个virtual function size()，返回class的大小。而这个继承体系中的每个constructors内含调用操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z) : _x(x), _y(y), _z(z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within Point3d::Piont3d()"</span> &lt;&lt; <span class="string">"size: "</span> &lt;&lt; size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  <strong>在一个class的constructor（和destructor）中，经由构造中的对象来调用virtual function，其函数实例是在此class中有作用的那个</strong>。如果调用操作限制必须在constructor（或destructor）中直接调用，那么答案十分明显；将每个调用以静态方式决议它，不要用到虚拟机制。如果是在Point3d constructor中，就显式调用Point3d::size()。  </p>
</li>
<li><p>如果size()之中又调用一个virtual function，这个调用也必须决议为Point3d的函数实例。因此在执行constructor时，必须限制一组virtual functions候选名单。为了控制class中有所作用的函数，编译系统需要控制住vptr的初始化和设定操作，vptr初始化操作怎么处理，得视vptr在constructor中应该在什么时候被初始化而定。 <strong>在base class constructors调用操作之后，在member iniaialization list中members初始化操作前</strong>。它解决了class中限制一组virtual functions名单的问题。如果每个constructor都一直等待其base class constructors执行完毕之后才设定对象的vptr，那么每次都能调用正确的virtual function实例。<br><font color="red">constructor的执行算法</font>通常如下：</p>
<ol>
<li><strong>在derived class constructor中，所有virtual base classes及上层base class的constructors会被调用</strong>。</li>
<li><strong>对象的vptr(s)会被初始化，指向相关virtual table(s)</strong>。</li>
<li><strong>如果有member initialization list，将在constructor内扩展开来</strong>。这在vptr被设定之后才做，以免virtual member function被调用。</li>
<li><strong>最后才执行程序员所提供的代码</strong>。<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">PVertex::PVertex(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z) : _next(<span class="number">0</span>), Vertex3d(x, y, z), Point(x, y)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within PVertex::PVertex()"</span> &lt;&lt; <span class="string">"size: "</span> &lt;&lt; size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line">PVertex* PVertex::PVertex(PVertex *<span class="keyword">this</span>, <span class="keyword">bool</span> __most__derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 条件式地调用virtual base constructor</span></span><br><span class="line">    <span class="keyword">if</span> (__most__derived != <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">        </span><br><span class="line">    <span class="comment">// 无条件调用上一层base</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex3d::Vertex3d(x, y, z);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 将相关的vptr初始化</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_PVertex = __vtbl_PVertex;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point__PVertex = __vtbl_Point__PVertex;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 程序员缩写的代码</span></span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within PVertex::PVertex()"</span> </span><br><span class="line">            &lt;&lt; <span class="string">"size: "</span></span><br><span class="line">            <span class="comment">// 经由虚拟机制调用</span></span><br><span class="line">            &lt;&lt; (*<span class="keyword">this</span>-&gt;__vptr__PVertex[<span class="number">3</span>].faddr)(<span class="keyword">this</span>)</span><br><span class="line">            &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="comment">// 传回被构造的对象</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ol>
</li>
</ul>
<ul>
<li>下面是vptr必须被设定的两种情况：<ol>
<li>当一个完整的对象被构造起来时。如果我们声明一个Point对象，则Point constructor必须设定其vptr。</li>
<li>当一个subobject constructor调用一个virtual function（不论是直接调用还是间接调用）时。<br>p218</li>
</ol>
</li>
</ul>
<h4 id="5-3-对象复制语意学-Object-Copy-Semantics"><a href="#5-3-对象复制语意学-Object-Copy-Semantics" class="headerlink" title="5.3 对象复制语意学(Object Copy Semantics)"></a>5.3 对象复制语意学(Object Copy Semantics)</h4><ul>
<li><p>当我们设计一个class，并以一个class object指定给另一个class object时，我们有三种<font color="red">选择</font>：</p>
<ol>
<li><p><strong>什么都不做，施行默认行为</strong>。</p>
</li>
<li><p><strong>提供一个explicit copy assignment operator</strong>。</p>
</li>
<li><p><strong>显式地拒绝把一个class object指定给另一个class object</strong>。</p>
<p>当选择不准将一个class object指定给另一个class object时，只要<strong>把copy assignment operator声明为priavte，并且不提供其定义</strong>就可以了。设为private，就不再允许于任何地点（除了在<font color="red">member functions</font>以及该<font color="red">class的friends之中</font>）做<font color="red">赋值</font>（<font color="green">assign</font>）操作。</p>
</li>
</ol>
</li>
<li><p>需要验证copy assignment operator的语意，利用Point class来帮助讨论：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Piont</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// ... 没有virtual function</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  对于默认行为是否足够，如果要支持的只是一个简单的拷贝操作，那么默认行为不但足够而且有效率，而且没有理由再自己提供一个copy assignment operator。<br>  如果不对Point供应一个copy assignment operator，光是以来memberwise copy，编译器不会产生实例。编译器不会产生出一个实例，因为<strong>class有了bitwise copy语义，所以implicit copy assignment operator被视为毫无用处，也不会被合成出来</strong>。copy assignment operators并不表示bitwisecopy semantics是nontrivial，只有nontrivial instances才会被合成出来。因此，对于Point class，这样的赋值操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point a, b;</span><br><span class="line">...</span><br><span class="line">a = b;</span><br></pre></td></tr></table></figure>

<p>  由bitwise copy完成，期间并没有copy assignment oeprator被调用。注意，我们还是可能提供一个copy constructor，为的是把<font color="green">name return value</font>（<font color="red">NRV</font>）优化打开。copy constructor的出现不意味着也也要提供一个copy assignment operator。</p>
</li>
<li><p>如果该operator在继承之下呢？</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">inline</span> Point&amp; Point::<span class="keyword">operator</span>=(<span class="keyword">const</span> Point &amp;p)</span><br><span class="line">&#123;</span><br><span class="line">    _x = p._x;</span><br><span class="line">    _y = p._y;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 虚拟继承</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">virtual</span> <span class="keyword">public</span> Point &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 如果没有定义copy assignmnet operator，根据合成规则的第二项和第四项。合成的东西看起来这样：</span></span><br><span class="line"><span class="keyword">inline</span> Point3d&amp; Point3d::<span class="keyword">operator</span>=(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, <span class="keyword">const</span> Piont3d &amp;p)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// call base class</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(p);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// memberwise copy the derived class members</span></span><br><span class="line">    _z = p._z;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  它缺乏一个member assignment list（平行于member initialization list的东西）。缺少copy assignment list，看起来是小事，但如果没有它，编译器一般就没有办法压抑上一层base class的copy operators被调用。例如Vertex copy operator，Vertex虚拟继承自Point：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class Vertex : virtual public Point</span></span><br><span class="line"><span class="keyword">inline</span> Vertex&amp; Vertex::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    _next = v._next;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 从Point3d和Vertex派生Vertex3d，下面是其copy assignment operator</span></span><br><span class="line"><span class="keyword">inline</span> Vertex3d&amp; Vertex3d::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex3d &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point3d::<span class="keyword">operator</span>=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex::oeprator=(v);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  编译器怎么能在Point3d和Vertex的copy assignment operators中压抑Point的copy assignment operators呢？有一种方法可以保证most-derived class会（完成）virtual base class subobject的copy行为，就是在derived class的copy assignment operator函数实例的最后，显式调用oeprator：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">inline</span> Vertex3d&amp; Vertex3d::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex3d &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point3d::oeprator=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex::oeprator=(v);</span><br><span class="line">    <span class="comment">// must place this last if your compiler does</span></span><br><span class="line">    <span class="comment">// not suppress intermediate class invocations</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    ... </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  它不能够省略subobjects的多重拷贝，但可以保证语意。建议尽可能不要允许一个virtual base class的拷贝操作，甚至一个比较奇怪的建议：不要在任何virtual base class中声明数据。</p>
</li>
</ul>
<h4 id="5-4-对象的效能-Object-Efficiency"><a href="#5-4-对象的效能-Object-Efficiency" class="headerlink" title="5.4 对象的效能(Object Efficiency)"></a>5.4 对象的效能(Object Efficiency)</h4><ul>
<li>测试。</li>
</ul>
<h4 id="5-5-析构语意学-Semantics-of-Destruction"><a href="#5-5-析构语意学-Semantics-of-Destruction" class="headerlink" title="5.5 析构语意学(Semantics of Destruction)"></a>5.5 析构语意学(Semantics of Destruction)</h4><ul>
<li><p>如果class没有定义destructor，那么只有在class内含的member object（抑或是class的base class）拥有destructor的情况下，编译器才会自动合成一个来。当从父类派生子类（即使是一种虚拟派生关系）时，如果没有声明一个estructor，编译器就没有必要合成一个destructor。没有任何理由说在delete一个对象之前得先讲内容清除干净。在一个对象的生命之前，没有任何class使用层面的程序操作是必要的，因此也就不需要要给destructor。  </p>
</li>
<li><p>一个由程序员定义的<font color="red"><strong>destructor被扩展的方式类似constructors被扩展的方式</strong></font>，但顺序相反：</p>
<ol>
<li><strong>destructor的函数本体现在被执行，也就是说vptr会在程序员的代码执行前被重设（reset）</strong>。</li>
<li><strong>如果class拥有member classs objects，而后者会拥有destructors，那么它们会以生命顺序的相反顺序被调用</strong>。</li>
<li><strong>如果object内含一个vptr，那么首先重设（reset）相关的vtbl。指向适当的base class的vtbl</strong>。</li>
<li><strong>如果由任何直接的（上一层）nonvirtual base classes拥有destrucotr，它们会以其声明顺序的相反顺序被调用</strong>。</li>
<li><strong>如果有任何virtual base classes拥有destructor，那么也会以原来的构造顺序的相反顺序被调用</strong>。</li>
</ol>
</li>
<li><p>跟constructor一样，对destructor的一种最佳实现策略就是维护两份destrucotr实例：</p>
<ol>
<li>一个complete object实例，先设定好vptr(s)，并调用virtual base classes destructors。</li>
<li>一个base class subobject实例：除非在destructor函数中调用一个virtual function，否则不会调用virtual base class destructors并设定vptr。</li>
</ol>
</li>
<li><p><font color="red">一个object的声明结束于其destructor开始执行之时</font>。    </p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第四章 Function语意学</title>
    <url>/2020/06/12/%E7%AC%AC%E5%9B%9B%E7%AB%A0-Function%E8%AF%AD%E6%84%8F%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>看以下代码,会发生什么:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d obj;</span><br><span class="line">POint3d *ptr = &amp;obj;</span><br><span class="line">obj.normalize();</span><br><span class="line">ptr-&gt;normalize();</span><br><span class="line"></span><br><span class="line"><span class="comment">// declaretion</span></span><br><span class="line">Point3d Point3d::normalie() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude();</span><br><span class="line">    Point3d normal;</span><br><span class="line">    normal._x = _x/mag;</span><br><span class="line">    normal._y = _y/mag;</span><br><span class="line">    normal._z = _z/mag;</span><br><span class="line">    <span class="keyword">return</span> normal;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">float</span> Point3d::magnitude() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(_x * _x + _y * _y + _z * _z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  答案是不知道。<strong>C++支持三种类型的member function：static、nonstatic和virtual</strong>，我们蹦确定normalize()和magnitude()两函数是否为virtual或nonvirtual，但可以确定它不是static。因为</p>
<ul>
<li>它直接存取nonstatic数据。</li>
<li>它被声明为const。是的，static member functions不可能做到这两点。</li>
</ul>
</li>
</ul>
<h4 id="4-1-Member的各种调用方式"><a href="#4-1-Member的各种调用方式" class="headerlink" title="4.1 Member的各种调用方式"></a>4.1 Member的各种调用方式</h4><ul>
<li><p><strong>原始的“C with Class”只支持nonstaic member functions</strong>。它收到很多质疑：</p>
<blockquote>
<p>有一种常见的观点，认为virtual functions只不过是一种蹩脚的函数指针，没有什么用……其意思主要就是，virtual functions是一种没有效能的形式。</p>
</blockquote>
</li>
<li><p>Static member functions是最后被引入的一种函数类型。</p>
</li>
</ul>
<h5 id="Nonstatic-Member-Functions（非静态成员函数）"><a href="#Nonstatic-Member-Functions（非静态成员函数）" class="headerlink" title="Nonstatic Member Functions（非静态成员函数）"></a>Nonstatic Member Functions（非静态成员函数）</h5><ul>
<li><p>C++的设计准则之一就是：static member function至少必须和一般的nonmember function有相同的效率。然而，举个例子：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// nonmember</span></span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">magnitude3d</span><span class="params">(<span class="keyword">const</span> Point3d *_this)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(_this-&gt;_x * _this-&gt;_x + </span><br><span class="line">                _this-&gt;_y * _this-&gt;_y +</span><br><span class="line">                _this-&gt;_z * _this-&gt;_z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  乍见似乎member function比较没有效率，它经由参数取坐标，而member function却直截了当用坐标成员。然而<font color="red"><strong>实际上member function被内化为nonmember的形式</strong></font>。下面是转化步骤：</p>
<ol>
<li><p><strong>改写函数的signature（函数原型）以安插要给额外的参数到member function中，用以提供一个存取管道，使class object将此函数调用。这个额外的参数使this指针</strong>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// non-const nonstatic member</span></span><br><span class="line">Point3d Point3d::magnitude(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span><br><span class="line"><span class="comment">// 如果function是const</span></span><br><span class="line"><span class="comment">// const nonstatic member</span></span><br><span class="line">Point3d Point3d::magnitude(<span class="keyword">const</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>将每一个对nonstatic data member的存取操作变为经由this指针来存取</strong>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(<span class="keyword">this</span>-&gt;_x * <span class="keyword">this</span>-&gt;_x +</span><br><span class="line">                <span class="keyword">this</span>-&gt;_y * <span class="keyword">this</span>-&gt;_y +</span><br><span class="line">                <span class="keyword">this</span>-&gt;_z * <span class="keyword">this</span>-&gt;_z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>将member function重新写成一个外部函数。函数名经过“mangling”处理，使它在程序中成为独一无二的词汇：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="title">magnitude__7Point3dFv</span><span class="params">(<span class="keyword">register</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span></span>;</span><br></pre></td></tr></table></figure>


</li>
</ol>
</li>
</ul>
<ul>
<li>开章的normalize()函数会转化成以下形式，假设声明了Pointe3d copy constructor，而named returned value（NRV）的优化也施行了：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">normalize__7Point3dFv</span><span class="params">(<span class="keyword">register</span> <span class="keyword">const</span> POint3d *<span class="keyword">const</span> <span class="keyword">this</span>, Point3d &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = <span class="keyword">this</span>-&gt;magniude();</span><br><span class="line">    <span class="comment">// default constructor</span></span><br><span class="line">    __result.Point3d::Point3d();</span><br><span class="line">    __result._x = <span class="keyword">this</span>-&gt;_x/mag;</span><br><span class="line">    __result._y = <span class="keyword">this</span>-&gt;_y/mag;</span><br><span class="line">    __result._z = <span class="keyword">this</span>-&gt;_z/mag;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有效率的方式</span></span><br><span class="line">Pointe3d Point3d::normalize() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude();</span><br><span class="line">    <span class="keyword">return</span> Point3d(_x/mag, _y/mag, _z/mag);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">normalize__7Point3dFv</span><span class="params">(<span class="keyword">register</span> <span class="keyword">const</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, Point3d &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = <span class="keyword">this</span>-&gt;magnitude();</span><br><span class="line">    <span class="comment">// __result return value</span></span><br><span class="line">    __result.Point3d::Point3d(<span class="keyword">this</span>-&gt;_x/mag, <span class="keyword">this</span>-&gt;_y/mag, <span class="keyword">this</span>-&gt;_z/mag);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h6 id="名称的特殊处理（Name-Mangling）"><a href="#名称的特殊处理（Name-Mangling）" class="headerlink" title="名称的特殊处理（Name Mangling）"></a>名称的特殊处理（Name Mangling）</h6><ul>
<li><p>一般而言，member的名称前面会被加上class名称，形成独一无二的命名：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bar</span> &#123;</span> <span class="keyword">public</span>: <span class="keyword">int</span> ival; ... &#125;;</span><br><span class="line"><span class="comment">// ival可能为</span></span><br><span class="line"><span class="comment">// ival__3Bar</span></span><br></pre></td></tr></table></figure>

<p>  为什么这么做呢？考虑这样的派生操作:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> :</span> <span class="keyword">public</span> Bar &#123; <span class="keyword">public</span>: <span class="keyword">int</span> ival; ... &#125;;</span><br><span class="line"><span class="comment">// Foo对象内部结合了base class和derived class两者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">int</span> ival__3Bar;</span><br><span class="line">    <span class="keyword">int</span> ival__3Foo;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  不管要处理哪个ival，通过<font color="red">name mangling</font>都能指出来。由于member functions可能被重载（overloaded），所以更需要广泛的mangling手法：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">x</span><span class="params">(<span class="keyword">float</span> newX)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 转化为：</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">X__5PointFf</span><span class="params">(<span class="keyword">float</span> newX)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X__5PointFv</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><strong>把参数和函数名称编码在一起，编译器就在不同的编译模块之间达成了一种有限形式的<font color="red">类型检验</font></strong>。如果两个实例拥有独一无二的name mangling，任何不正确的调用操作在链接时会因无法resolved而失败。这就是确保<font color="red">类型安全的链接行为</font>（<font color="green">type-safe linkage</font>）。它只能捕捉到函数的标记（<strong>signature，函数名称+参数个数+参数类型）错误</strong>，而返回类型错误检查不出来。而一种demangling工具，用来拦截名称并转换回去。它向使用者隐藏了内部名称，使得出错时能得到一个友好的信息返回。</li>
</ul>
<h5 id="Virtual-Member-Functions（虚拟成员函数）"><a href="#Virtual-Member-Functions（虚拟成员函数）" class="headerlink" title="Virtual Member Functions（虚拟成员函数）"></a>Virtual Member Functions（虚拟成员函数）</h5><ul>
<li><p><strong>如果normalize()是</strong>virtual member function，那么以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ptr-&gt;normalie();</span><br></pre></td></tr></table></figure>

<p>  会被转化为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*ptr-&gt;vptr[<span class="number">1</span>])(ptr);</span><br></pre></td></tr></table></figure>

<p>  其中：</p>
<ul>
<li><strong>vptr由编译器产生，一个指向vtbl的指针</strong>。</li>
<li><strong>1是索引值</strong>。</li>
<li><strong>ptr是this指针</strong>。</li>
</ul>
</li>
<li><p>类似道理，<strong>如果magnitude()也是</strong>virtual function，它在normalize()之中的调用操作被转换如下：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// register float mag = magnitude();</span></span><br><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = (*<span class="keyword">this</span>-&gt;vptr[<span class="number">2</span>])(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  此时，由于Point3d::magnitude()是在Point3d::normalize()中被调用的，而后者已经由虚拟机之而决议（reslove）妥当，<font color="red">显式的调用Point3d实例会比较有效率</font>，并因此压制由于虚拟机之而产生的不必要重复调用操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// explicitly invocation</span></span><br><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = Point3d::magnitude();</span><br></pre></td></tr></table></figure>

<p>  <strong>如果magnitude()声明为inline函数，会更有效率。因为使用class scope operator显式调用virtual function，resolve方式和nonstatic member function一样</strong>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude__7Point3dFv(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  这时对于以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Point3d obj;</span></span><br><span class="line">obj.normalize();</span><br></pre></td></tr></table></figure>

<p>  如果编译器转化为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*obj.vptr[<span class="number">1</span>])(&amp;obj);</span><br></pre></td></tr></table></figure>

<p>  <strong>虽然语义正确，却没有必要</strong>。所以上述经由obj调用的函数实例只可以是<font color="red">Point3d:normalize()</font>。”经由class object调用virtual function”，这种操作总是被编译器像对待一般nonstatic member function一样resolved：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">normalize__7Point3Fv(&amp;obj);</span><br></pre></td></tr></table></figure>

<p>  这项工程的另一个利益是，<font color="red"><strong>virtual function的inline函数实例可以被扩展（expanded）开来，因而提供极大的效率利益</strong></font>。</p>
</li>
</ul>
<h5 id="Static-Member-Functions（静态成员函数）"><a href="#Static-Member-Functions（静态成员函数）" class="headerlink" title="Static Member Functions（静态成员函数）"></a>Static Member Functions（静态成员函数）</h5><ul>
<li><p>如果Point3d::normalize()是一个static member function，下面两个调用的转化：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">obj.normalize();</span><br><span class="line">ptr-&gt;normalize();</span><br><span class="line"><span class="comment">// 将被转化为一般的nonmember函数调用</span></span><br><span class="line"><span class="comment">// obj.normalize();</span></span><br><span class="line">normalize__7Point3dSFv();</span><br><span class="line"><span class="comment">// ptr-&gt;nomalize();</span></span><br><span class="line">normalize__7Point3dSFv();</span><br></pre></td></tr></table></figure>

<p>  在引进static member functions之前，你很少看到这种怪异写法：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">((Point3d*)<span class="number">0</span>)-&gt;object_count();</span><br></pre></td></tr></table></figure>

<p>  在引进<font color="red">static member functions</font>之前，C++语言要求member functions必须<strong>经由class的object来调用</strong>。而实际上，<strong>只有当一个或多个nonstatic data members在member function中被直接存取时，才需要class object</strong>。如果没有任何一个members被直接存取，就不需要this指针，也就不需要通过一个class object来调用member function。<br>  这一来存取static data members时产生了一些不规则性。如果class的static data member声明为nonpublic，那么就必须提供member functions来存取member。这时很显然，虽然补考class object来存取static member，但<strong>存取函数却得绑定一个class object上</strong>。<br>  独立于class object之外的存取操作很重要，尤其在没有class object存在的情况。<font color="red"><strong>程序方法上的解决之道</strong></font>是把0强制转换为一个class指针：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">object_count((Point3d*)<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>至于语言层面上的解决之道</strong></font>，是引进的static member functions。<font color="red">它的主要特性是没有this指针</font>。以下是次要特性：</p>
<ul>
<li><strong>它不能直接存取class中的nonstatic members</strong>。</li>
<li><strong>它不能被声明为const、volatile或virtual</strong>。</li>
<li><strong>它不需要经由class object才被调用——虽然大部分时候它是这样被调用的</strong>。</li>
</ul>
</li>
<li><p>如果去一个static member funciton的地址，获得的将是在内存中的位置，也就是其地址。由于static member function没有this指针，所以地址类型不是一个指向class member funciton的指针，而是一个<font color="red">nonmember函数指针</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&amp;Point3d::object_count();</span><br><span class="line"><span class="comment">// 会得到数值，类型是</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">int</span> <span class="params">(*)</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 而不是</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">int</span> <span class="params">(Point3d::*)</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p>  因为static member function缺乏this指针，所以差不多等同于nonmember function。它有个意想不到的好处：<strong>成为callback函数</strong>。</p>
</li>
</ul>
<h4 id="4-2-Virtuar-Member-Functions（虚拟成员函数）"><a href="#4-2-Virtuar-Member-Functions（虚拟成员函数）" class="headerlink" title="4.2 Virtuar Member Functions（虚拟成员函数）"></a>4.2 Virtuar Member Functions（虚拟成员函数）</h4><ul>
<li><p>我们已经知道了virtual function的一般实现模型：每个class中有个vtbl，其中包含着virtual function的地址，而每个object有vptr，指向vtbl的存在。</p>
</li>
<li><p>为了支持virtual function机制，对于多态对象必须有某种形式上的<font color="red">执行期类型判断法</font>（<font color="green">runtime type resolution</font>）。也就是下面调用需要ptr在执行期有某些信息，才能找到并调用z()：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ptr-&gt;z();</span><br></pre></td></tr></table></figure>

<p>  这份信息不能加在ptr身上，它增加了空间负担，即使不使用多态；第二它打断了与C程序间的链接兼容性。我们需要一个更好的规范，一个以class的使用为基础，而不在乎关键词是class或struct的规范。并且必须在<font color="red">执行期多态</font>（<font color="green">runtime polymorphism</font>）时才需要这份信息</p>
</li>
<li><p>C++中多态表示以一个public base class的指针（reference），寻址出一个derived class object的意思。经由指针，可以在程序中任何地方采用public derived类型，这种多态形式是<font color="red">消极的</font>（<font color="green">passive</font>），可以在编译期完成，除了virtual base class的情况。当指出的对象被使用时，才变成<font color="red">积极的</font>（<font color="green">active</font>）。</p>
</li>
<li><p>在<font color="green">runtime type identification</font>（<font color="red">RTTI</font>）性质在1993年被引入前，C++对于积极多态（active polymorphsim）的唯一支持，<strong>就是对virtual function call的resolution操作。有了RTTI，能够在执行期查询一个多态的pointer或多态的reference了</strong>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 积极多态的例子</span></span><br><span class="line">ptr-&gt;z();</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">if</span> (Point3d *p3d = <span class="keyword">dynamic_cast</span>&lt;Point3d*&gt;(ptr))</span><br><span class="line">    <span class="keyword">return</span> p3d-&gt;_z;</span><br></pre></td></tr></table></figure>

<p>  z()是一个virtula function。是什么信息让我们在执行期调用正确呢？，我们需要知道：</p>
<ul>
<li><p><strong>ptr所指对象的真实类型</strong>。</p>
</li>
<li><p><strong>z()实例的位置，以便能够调用它</strong>。</p>
<p>实现上，可以在class object身上增加两个members：</p>
</li>
</ul>
<ol>
<li><p><strong>一个字符串或数字</strong>，表示class的类型；</p>
</li>
<li><p><strong>一个指针</strong>。指向表格，表格中有个virtual function的执行期地址。</p>
<p>表格中的virtual functions地址是怎么建构起来的呢？<strong>virtual function（可由object被调用）在编译期获知</strong>。这个地址是固定的，执行期不增添也不替换。完全由编译器掌控，不需要执行期介入。而<strong>执行期只是备妥了函数地址</strong>，并未被找到。找到那些地址。两个步骤完成任务：</p>
</li>
<li><p><font color="red"><strong>为了找到表格，class object被安插了编译器内部产生的指针，指向该表格</strong></font>。</p>
</li>
<li><p><font color="red"><strong>为了找到函数地址，每个virtual function被指派为一个表格索引值</strong></font>。</p>
<p>以上的工作都由编译器完成。<font color="red"><strong>执行期要做的，只是在特定的virtual table slot中激活virtual funciton</strong></font>。  </p>
<p>一个class只有一个vtbl。每个table内含对应object的active virtual functions函数实例的地址，这些active virtual functions包括：</p>
</li>
</ol>
<ul>
<li><strong>class所定义的函数实例</strong>。它会overriding存在的base class virtual function函数实例。</li>
<li><strong>继承自base class的函数实例</strong>。这是在derived class决定不改写virtual function时才出现的情况。</li>
<li><strong>一个pure_virtual_called()函数实例</strong>。</li>
</ul>
</li>
<li><p>每个virtula function都被指派一个固定的索引值：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Point();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Point&amp; <span class="title">mult</span><span class="params">(<span class="keyword">float</span>)</span> </span>= <span class="number">0</span>; <span class="comment">// puree virtual function</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">y</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>);</span><br><span class="line">    <span class="keyword">float</span> _x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  virtual destructor被指派slot 1，而mult()被指派slot2，y()被指派slot3，z()被指派slot4。在单一继承体系中，virtual<br>  fucntion机制行为十分良好，有效率且容易塑造出模型。而在多重继承或虚继承中，对virtual functinos的支持就没那么好了。</p>
</li>
</ul>
<h5 id="多重继承下的Virtual-Functions"><a href="#多重继承下的Virtual-Functions" class="headerlink" title="多重继承下的Virtual Functions"></a>多重继承下的Virtual Functions</h5><ul>
<li><p>多重继承中支持virtual functions，<font color="red">复杂度围绕在<strong>第二个及后继base class</strong>身上，以及<strong>”必须在执行期调整this指针“</strong>这一点</font>:</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Derived -- public --- Base1</span><br><span class="line">Derived -- public --- Base2</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base1</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Base1();</span><br><span class="line">    virtula ~Base1();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">speakClearly</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Base1* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Base1;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base2</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Base2();</span><br><span class="line">    <span class="keyword">virtual</span> ~Base2();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Base2* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Base2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> :</span> <span class="keyword">public</span> Base1, <span class="keyword">public</span> Base2 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Derived();</span><br><span class="line">    <span class="keyword">virtual</span> ~Derived();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Derived* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Derived;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><p>它的困难度主要体现在Base2 subobject身上，有三个问题需要解决：*<font color="red">（1）virtual destructor*</font>，<font color="red"><em>（2）被继承的Base2::mumble()</em></font>，<font color="red"><em>（3）clone()函数实例</em></font>。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base2 *pbase2 = nwe Derived;</span><br><span class="line"><span class="comment">// 编译时期代码</span></span><br><span class="line"><span class="comment">// 转移以支持第二个base class</span></span><br><span class="line">Derived *temp = <span class="keyword">new</span> Derived;</span><br><span class="line">Base2 *pbase2 = temp ? temp + <span class="keyword">sizeof</span>(Base1) : <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>  如果没有这样的调整，任何非多态的应用都将失败：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pbase2被指定一个Derived对象，这也应该没有问题</span></span><br><span class="line">pbase2-&gt;data_Base2;</span><br><span class="line"><span class="comment">// 当程序员要删除pbase2所指的对象时</span></span><br><span class="line"><span class="comment">// 需要调用正确的virtual destructor函数实例，pbase2可能需要调整</span></span><br><span class="line"><span class="keyword">delete</span> pbase2;</span><br></pre></td></tr></table></figure>

<p>  一般规则时，<strong>经由第二或后继base class的指针（reference）来调用derived class virtual funciton。其所连带的必要this指针调整操作，必须在执行期完成</strong>。也就是offset的大小，offset加到this指针上的程序代码，必须由编译器在某个地方插入。</p>
<blockquote>
<p>cfont编译器中的方法是将vtbl加大，容纳所需的this指针，每个virtual table slot不再是指针，而是一个集合体，内含<font color="red">offset</font>以及地址：</p>
</blockquote>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*pbase2-&gt;vptr[<span class="number">1</span>])(pbase2);</span><br><span class="line"><span class="comment">// 改变为</span></span><br><span class="line">(*pbase2-&gt;vptr[<span class="number">1</span>].faddr)(pbase2 + pbase2-&gt;vptr[<span class="number">1</span>].offset);</span><br></pre></td></tr></table></figure>

<p>  其中faddr内含virtual function地址，offset内含this指针调整值。这个做法的缺点是，不管什么virtual function都得进行offset调整。<br>  比较有效率的解决方法是利用所谓的thunk（是一段assembly代码）：<font color="red">（1）适当的offset调整this指针</font>，<font color="red">（2）跳到virtual function去</font>，如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">pbase2_dtor_thunk:</span><br><span class="line">    <span class="keyword">this</span> += <span class="keyword">sizeof</span>(base1);</span><br><span class="line">    Derived::~Derived(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  Thunk技术允许vtbl slot继续内含简单指针，因此空间上不需要任何负担。slots的地址直接指向virtual function，也可以指向相关的thunk。</p>
</li>
<li><p>调整this指针的的第二个额外负担就是，由于<font color="red">（1）经由derived class（或base class）调用</font>，<font color="red">（2）经由第二（或后继）base class调用</font>，同一函数在vtbl可能需要多次对应的slots：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base1 *pbase1 = <span class="keyword">new</span> Derived;</span><br><span class="line">Base2 *pbase2 = <span class="keyword">new</span> Derived;</span><br><span class="line"><span class="keyword">delete</span> pbase1;</span><br><span class="line"><span class="keyword">delete</span> pbase2;</span><br></pre></td></tr></table></figure>

<p>  两个相同的derived destructor，但需要不同的vtbl slots：</p>
<ol>
<li>pbase1不需要调整this指针。vtbl slot需放置真正destructor地址。</li>
<li>pbase2需要调整this指针。vtbl slot需相关的thunk地址。  </li>
</ol>
<p>  <strong>多重继承下，derived class内含n-1个额外的vtbl，n表示上一层base classes的个数</strong>。对于本例的Derived而言，会有两个vtbl被编译器产生出来：</p>
<ol>
<li><p><strong>一个主要实例，与Base1共享</strong>。</p>
</li>
<li><p><strong>一个次要实例，与Base2有关</strong>。</p>
<p>用以支持一个clas拥有多个vtbl的传统方法是，将每个tables以外部对象的形式产生出来，并给予独一无二的名称：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vtbl__Derived;          <span class="comment">// 主</span></span><br><span class="line">vtbl__Base2__Derived;   <span class="comment">// 次</span></span><br></pre></td></tr></table></figure>

<p>于是当你将Derived对象地址指定给<font color="red"><strong>Base1指针或Derived指针时</strong></font>，被处理的vtbl时主要表格vtbl__Derived。当讲Derived对下给你地址指定给<font color="red"><strong>Base2指针时</strong></font>，被处理的vtbl时次要表格vtbl__Base2_Derived。</p>
</li>
</ol>
</li>
<li><p>由于<font color="red">执行期链接器</font>（<font color="green">runime linkers</font>）的降临（可以支持动态共享函数库），符号名称的链接可能变得非常缓慢。为了调节执行期编译器的效率，Sun编译器将多个vtbl连锁为一个：<strong>指向次要表格的指针，可由主要表格表格名称加上一个offset获得，这样的策略下，每个class只有一个具名的vtbl</strong>。</p>
</li>
<li><p>有三种情况，第二或后继的base class会影响virtual functions的支持。</p>
<ol>
<li><font color="red"><strong>通过一个指向第二个base class的指针，调用derived class virtual function</strong></font>。</li>
<li><font color="red"><strong>通过一个指向derived class的指针，调用第二个base class中的一个继承而来的virtual funciton</strong></font>。</li>
<li><font color="red"><strong>允许一个virtual function的返回值类型有所变化，可能是base type，也可能是publicly derived type</strong></font>。这一点经由Derived::clone()函数实例来说明。clone函数的Derived版本传回一个Derived class指针，默默地改写了它们两个base class函数实例。当通过指向第二个base class的指针来调用clone()时，this指针的offset问题诞生了：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base2 *pb1 = <span class="keyword">new</span> Derived;</span><br><span class="line"><span class="comment">// 调用Derived* Derived::clone()</span></span><br><span class="line"><span class="comment">// 返回值必须被调整，以指向Base2 subobject</span></span><br><span class="line">Base2 *pb2 = pb1-&gt;clone();</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p>当函数被认为足够小的时候，Sun编译器会提供一个split functions技术，以相同的算法产生两个函数。这样不论通过Base1指针还是通过Derived指针调用函数，都不需要调整返回值，而通过Base指针所调用的时另一个函数，并在返回前，为指针加上必要的offset。如果函数不小，会给函数中其中一个进入点，进入点需要三个指令。</p>
</li>
<li><p>thunk则是函数一开始先（1）调整this指针，然后才（2）执行程序员所写的函数码；无需调整函数调用操作。</p>
</li>
<li><p>Microsoft用adderss points来取代thunk策略，即overriding function期待获得的是引入该virtual function的class的地址，这就是函数的address point。</p>
</li>
</ul>
<h5 id="虚拟继承下的Virtual-Functinos"><a href="#虚拟继承下的Virtual-Functinos" class="headerlink" title="虚拟继承下的Virtual Functinos"></a>虚拟继承下的Virtual Functinos</h5><ul>
<li><p>考虑下面的virtual base class派生体系：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point2d --- Point3d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    <span class="keyword">virtual</span> ~Point2d();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> virtula Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    ~Point3d();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  当一个virtual base class从另一个virtual base class派生而来，并且两者都支持virtual functions和nonstatic data members时，编译器对于virtual base class的支持简直就像进了了迷宫一样。所以建议是，<font color="red"><strong>不要再一个virtual base class中声明nonstatic data members</strong></font>。如果这么做，你会距离复杂的深渊愈来愈近。</p>
</li>
</ul>
<h4 id="4-3-函数的效能"><a href="#4-3-函数的效能" class="headerlink" title="4.3 函数的效能"></a>4.3 函数的效能</h4><ul>
<li>下面这组测试，在编译器上计算两个3D点，其中用到一个nonmember friend function，一个member function，以及一个virtual member function。p170</li>
</ul>
<h4 id="4-4-指向Member-Function的指针（Pointer-toMember-Functions）"><a href="#4-4-指向Member-Function的指针（Pointer-toMember-Functions）" class="headerlink" title="4.4 指向Member Function的指针（Pointer-toMember Functions）"></a>4.4 指向Member Function的指针（Pointer-toMember Functions）</h4><ul>
<li><p>去一个nonstatic data member 的地址，得到的结果是该<font color="red">member在class布局中的bytes位置（再+1）</font>。可以想象它是一个不完整的值，它需要被绑定于某个class object的地址上，才能够被存取。<br>  取一个nonstatic member function的地址，如果该函数是nonvirtual，得到的结果是它在<font color="red">内存中真正的地址</font>。然而也是不完全的，也需要绑定于某个class object的地址上，才能够通过它调用函数。回顾一下：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> (Point::*pmf)();</span><br><span class="line"><span class="comment">// 定义</span></span><br><span class="line"><span class="keyword">double</span> (Point::*coord)() = &amp;Point::x;</span><br><span class="line">coord = &amp;Point::y;</span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line">(origin.*coord)();</span><br><span class="line">(ptr-&gt;*coord)();</span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line">(coord)(&amp;origin);</span><br><span class="line">(coord(ptr);</span><br></pre></td></tr></table></figure>

<p>  <font color="red">指向member function的指针的声明语法，以及指向member selection运算符的指针，作用是作为this指针的空间保留着。这就是为什么static member function（没有this指针）的类型是”函数指针“，而不是指向member function的指针的原因。</font>  </p>
<p>  使用一个member function指针，如果并不用于virtual functions、virtual base class或multiple base classes等情况的画，并不会比使用一个nonmember function指针的成本更高。上述三种情况对于member function指针的类型及调用都太过复杂。对于没有以上情况的class而言，编译器可以为它们提供<strong>相同的效率</strong>。</p>
</li>
</ul>
<h5 id="支持“指向Virtual-Member-Functions”的指针"><a href="#支持“指向Virtual-Member-Functions”的指针" class="headerlink" title="支持“指向Virtual Member Functions”的指针"></a>支持“指向Virtual Member Functions”的指针</h5><ul>
<li><p>考虑下面程序片段：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">float</span> (Point::*pmf)() = &amp;Point::z;</span><br><span class="line">Point *ptr = <span class="keyword">new</span> Point3d;</span><br></pre></td></tr></table></figure>

<p>  pmf，一个指向member function的指针，被设值为Point::z()（一个virtual function）的地址。如果由ptr调用z()，被调用的是Point3d::z()，但如果从pmf间接调用z()，正常运行吗？yes。<br>  对于一个virtual function取其地址，所能获得的只是一个索引值。对于一个指向member function的指针<font color="red">评估求值</font>（<font color="green">evaluated</font>），会因为该值由两种意义而复杂化，调用也有别于常规，<code>float (Point::*pmf)();</code>必须允许此函数能够寻址出nonvirtual x()和virtual z()两个member functions：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 两者都可以指定给pmf</span></span><br><span class="line"><span class="keyword">float</span> Point::x() &#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line"><span class="keyword">float</span> Point::z() &#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>  只不过其中一个代表<font color="red">内存地址</font>，另一个代表<font color="red">vtbl中的索引值</font>。为了使pmf能够（1）持有两种数值，（2）能区别代表内存地址还是vtbl中的索引值：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// true is non-virtual invocation</span></span><br><span class="line"><span class="comment">// false is virtual invocation</span></span><br><span class="line">(((<span class="keyword">int</span>)pmf) &amp; ~<span class="number">127</span>) ? (*pmf)(ptr) : (*ptr-&gt;vptr[(<span class="keyword">int</span>)pmf](ptr));</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="在多重继承之下，指向Member-Functions的指针"><a href="#在多重继承之下，指向Member-Functions的指针" class="headerlink" title="在多重继承之下，指向Member Functions的指针"></a>在多重继承之下，指向Member Functions的指针</h5><ul>
<li><p>为了让member functions的指针能够支持多重继承和虚拟继承，设计了一个结构体：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">mptr</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> delta;</span><br><span class="line">    <span class="keyword">int</span> index;</span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        ptrtofunc   faddr;</span><br><span class="line">        <span class="keyword">int</span>         v_offset;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  <strong>index和faddr分别持有vtbl slot和nonvirtual member function address（当index不指向vtbl时，设为-1）</strong>，像以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(ptr-&gt;*pmf)();</span><br><span class="line"><span class="comment">// 变成</span></span><br><span class="line">(pmf.index &lt; <span class="number">0</span>)</span><br><span class="line">? <span class="comment">// non-virtual invocation</span></span><br><span class="line">(*pm.faddr)(ptr)</span><br><span class="line">: <span class="comment">// virtual invocation</span></span><br><span class="line">(*ptr-&gt;vptr[pmf.index](ptr));</span><br></pre></td></tr></table></figure>

<p>  这个方法受到的批评是，每个调用操作都得付出成本。Microsoft把检查拿掉，导入一个vcall thunk，它会选出并调用相关vtbl中的slot。  </p>
<p>  这个结构体的另一个副作用是，当传递一个不变值的指针给member function时，需要产生临时变量：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> Point3d <span class="title">foo</span><span class="params">(<span class="keyword">const</span> Point3d&amp;, Point3d (Point3d::*)())</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(<span class="keyword">const</span> Point3d &amp;p)</span> </span>&#123;</span><br><span class="line">    Point3d pt = foo(p, &amp;Point3d::normal);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &amp;Point3d::normal value</span></span><br><span class="line">&#123;<span class="number">0</span>, <span class="number">-1</span>, <span class="number">10727417</span>&#125;</span><br><span class="line"><span class="comment">// 将变成</span></span><br><span class="line">__mptr temp = &#123;<span class="number">0</span>, <span class="number">-1</span>, <span class="number">10727417</span>&#125;</span><br><span class="line">foo(p, temp);</span><br></pre></td></tr></table></figure>


</li>
</ul>
<pre><code>回到开头那个结构体。delta字段表示this指针的offset值，而v_offset字段放的是一个virtual base class的vptr位置。如果vptr被编译器放在class对象起头处，这个字段就没必要了。它只在多重继承或虚拟继承情况下才有必然性。有些编译器根据不同的class特征提供多种memer functions的指向方式。如Microsoft：
1. **单一继承实例**（有vcall thnuk地址或是函数地址）；
2. **多重继承实例**（有faddr和delta两个members）；
3. **虚拟继承实例**（4个members）。</code></pre><h5 id="”指向Member-Functions之指针的效率"><a href="#”指向Member-Functions之指针的效率" class="headerlink" title="”指向Member Functions之指针的效率"></a>”指向Member Functions之指针的效率</h5><ul>
<li>又是测试。p180</li>
</ul>
<h4 id="4-5-Inline-Functions"><a href="#4-5-Inline-Functions" class="headerlink" title="4.5 Inline Functions"></a>4.5 Inline Functions</h4><ul>
<li><p>一个Point class的加法运算符的可能实现内容：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line">    <span class="keyword">friend</span> oint <span class="keyword">operator</span>+(<span class="keyword">const</span> Point&amp;, <span class="keyword">const</span> Point&amp;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Point <span class="keyword">operator</span>+(<span class="keyword">const</span> Point &amp;lhs, <span class="keyword">const</span> Point &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    Point new_pt;</span><br><span class="line">    new_pt._x = lhs._x + rhs._x;</span><br><span class="line">    new_pt._y = lhs._y + rhs._y;</span><br><span class="line">    <span class="keyword">return</span> new_pt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void Point::x(float new_x) &#123; _x = new_x; &#125;</span></span><br><span class="line"><span class="comment">// float Point::x() &#123; return _x; &#125;</span></span><br><span class="line">new_pt.x(lhs.x() + rhs.x());</span><br></pre></td></tr></table></figure>

<p>  以上通过将存取函数声明为inline，不但可以保持直接存取的高效率，而且加法运算符不再需要被声明为Point的friend。<br>  然而不能够强迫任何函数都变成inline。<font color="red">关键词inline（或者class declaration中的member function或friend function的定义）只是一项请求</font>。如果请求被接受，编译器就认为它可以用一个表达式（expression）合理地将这个函数扩展开来。cfront有一套复杂的测试方法，通常是用来计算assignments、function calls、virtual function calls等操作的次数。<strong>每个expression种类都有一个权值</strong>，<font color="red">inline函数的复杂度就以这些操作的总和来决定</font>。</p>
</li>
<li><p>一般处理一个inline函数有两个阶段：</p>
<ol>
<li><strong>分析函数定义，决定函数的intrinsic inline ability</strong>。如果函数因<font color="red">复杂度</font>或<font color="red">构建问题</font>不可成为inline，它会被转为一个static函数，并在被编译模块内产生对应的函数定义。在一个支持模块个别编译的环境中，编译器几乎没有什么权宜之计。理想情况下，链接器会将被产生出来的重复东西清理掉，但调试信息不会。UNIX环境的strip命令可以。</li>
<li><strong>真正的inline函数扩展操作是在调用的那一点上，这会带来参数的求值操作（ealuation）以及临时性对象的管理</strong>。</li>
</ol>
</li>
<li><p>在将要扩展的点上，cfront编译器中，<strong>inline函数如果只有一个表达式，而又有后续操作，则不会扩展开来</strong>。</p>
</li>
</ul>
<h5 id="形式参数（Formal-Arguments）"><a href="#形式参数（Formal-Arguments）" class="headerlink" title="形式参数（Formal Arguments）"></a>形式参数（Formal Arguments）</h5><ul>
<li><strong>inline扩展期间每个形式参数都被对应的实际参数取代。如果实际参数是一个常量表达式（constant expression），可以在替换之前完成求值操作（evaluations）；如果是个有副作用的表达式，那么需要引入临时性对象；如果既不是常量表达式，也不是带有副作用的表达式，那么就直接代替它。</strong>  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">min</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> j)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> i &lt; j ? i : j;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> minval;</span><br><span class="line">    <span class="keyword">int</span> val1 = <span class="number">1024</span>;</span><br><span class="line">    <span class="keyword">int</span> val2 = <span class="number">2048</span>;</span><br><span class="line">    minval = min(val1, val2);       <span class="comment">// 1</span></span><br><span class="line">    minval = min(<span class="number">1024</span>, <span class="number">2048</span>);       <span class="comment">// 2</span></span><br><span class="line">    minval = min(foo(), bar() + <span class="number">1</span>); <span class="comment">// 3</span></span><br><span class="line">    <span class="keyword">return</span> minval;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line">minval = val1 &lt; val2 ? val1 : val2; <span class="comment">// 1</span></span><br><span class="line">minval = <span class="number">1024</span>;                      <span class="comment">// 2</span></span><br><span class="line"><span class="comment">// 3</span></span><br><span class="line"><span class="keyword">int</span> t1;</span><br><span class="line"><span class="keyword">int</span> t2;</span><br><span class="line">minval = (t1 = foo()), (t2 = bar() + <span class="number">1</span>), t1 &lt; t2 ? t1 : t1;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="局部变量（Local-Variables）"><a href="#局部变量（Local-Variables）" class="headerlink" title="局部变量（Local Variables）"></a>局部变量（Local Variables）</h5><ul>
<li><p>如果在inline定义中加入局部变量：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">min</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> j)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> minval = i &lt; j ? i : j;</span><br><span class="line">    <span class="keyword">return</span> minval;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> local_var;</span><br><span class="line">    itn minval;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    minval = min(val1, val2);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> local_var;</span><br><span class="line">    <span class="keyword">int</span> minval;</span><br><span class="line">    <span class="comment">// inlnie函数的局部变量处以mangling操作</span></span><br><span class="line">    <span class="keyword">int</span> __min_lv-minval;</span><br><span class="line">    minval = </span><br><span class="line">        (__min_lv-minval = </span><br><span class="line">            val1 &lt; val2 ? val1 : val2),</span><br><span class="line">            __min_lv_minval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  一般inline函数中的每个局部变量都必须放在<font color="red">封闭的区段</font>中，拥有独一无二的名称。因为如果inline函数以<font color="red">分离的多个式子</font>（<font color="green">discrete statements</font>）被扩展多次，那么只需要一组局部变量，就可以重复使用。<br>  inline函数中的局部变量，加上有副作用的参数，可能会<strong>导致大量临时性对象的产生</strong>。特别是如果以单一表达式（expression）被扩展多次的话：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">minval = min(avl1, val2) + min(foo(), foo()+<span class="number">1</span>);</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line"><span class="comment">// 为局部变量产生临时变量</span></span><br><span class="line"><span class="keyword">int</span> __min_lv_minval_00;</span><br><span class="line"><span class="keyword">int</span> __min_lv_minval_01;</span><br><span class="line"><span class="comment">// 为放置副作用产生临时变量</span></span><br><span class="line"><span class="keyword">int</span> t1;</span><br><span class="line"><span class="keyword">int</span> t2;</span><br><span class="line">minval = ((__min_lv_minval_00 = </span><br><span class="line">            val1 &lt; val2 ? val1 : val2),</span><br><span class="line">            __min_lv_minval_00)</span><br><span class="line">          +</span><br><span class="line">          ((__min_lv_minval_01 = (t1 = foo()),</span><br><span class="line">            (t1 = foo() + <span class="number">1</span>),</span><br><span class="line">            t1 &lt; t2 ? t1 : t2),</span><br><span class="line">            __min_lv_minval__01);</span><br></pre></td></tr></table></figure>

<p>  Inline函数对封装提供了必要的支持，可以有效存取class的nonpublic数据。它同时是C程序中大量使用#define的一个安全代替品——特别如果宏中的参数有副作用的话。然而被调用太多次的话，会产生大量的扩展码，使<strong>程序大小暴涨</strong>。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
</search>
