<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Docker数据卷</title>
    <url>/2020/06/12/Docker%E6%95%B0%E6%8D%AE%E5%8D%B7/</url>
    <content><![CDATA[<h2 id="数据卷"><a href="#数据卷" class="headerlink" title="数据卷"></a>数据卷</h2><blockquote>
<p>使用Docker过程中，需要对数据进行持久化，或在多个容器间共享数据，需要容器的数据管理操作</p>
</blockquote>
<ul>
<li>数据管理主要有两种方式：<ul>
<li>数据卷（Data Volumes）：容器数据直接映射到本地主机环境</li>
<li>数据卷容器（Data Volume Containers）：使用特定容器维护数据卷</li>
</ul>
</li>
</ul>
<h4 id="数据卷-1"><a href="#数据卷-1" class="headerlink" title="数据卷"></a>数据卷</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># e.g.: training/webapp镜像创建web容器，并创建数据卷挂载到容器的/webapp目录</span></span><br><span class="line"><span class="comment"># 创建数据卷，-P是映射本地临时端口</span></span><br><span class="line"><span class="comment"># 挂在主机目录为数据卷: from/path:to/path</span></span><br><span class="line"><span class="comment"># 指定数据卷读写权限: ro/rw</span></span><br><span class="line">$ docker run -d -P --name web -v /src/webapp:/opt/webapp:ro training/webapp python app.py</span><br></pre></td></tr></table></figure>

<h4 id="数据卷容器"><a href="#数据卷容器" class="headerlink" title="数据卷容器"></a>数据卷容器</h4><blockquote>
<p>创建数据卷容器dbdata，创建数据卷挂载到容器dbdata</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it -v /dbdata --name dbdata ubuntu</span><br></pre></td></tr></table></figure>

<blockquote>
<p>其他容器对其进行挂载，之后三方任何一方修改各方都可见</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it --volumes-from dbdata --name db1 ubuntu</span><br><span class="line">$ docker run -it --volumes-from dbdata --name db2 ubuntu</span><br></pre></td></tr></table></figure>

<blockquote>
<p>删除最后挂载容器需要使用<code>docker rm -v</code>同时删除关联容器</p>
</blockquote>
<hr>
<h3 id="使用数据卷容器迁移数据"><a href="#使用数据卷容器迁移数据" class="headerlink" title="使用数据卷容器迁移数据"></a>使用数据卷容器迁移数据</h3><h4 id="备份"><a href="#备份" class="headerlink" title="备份"></a>备份</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from dbdata -v $(<span class="built_in">pwd</span>):/backup --name worker ubuntu tar cvf /backup/backup.tar /dbdata</span><br></pre></td></tr></table></figure>

<h4 id="恢复"><a href="#恢复" class="headerlink" title="恢复"></a>恢复</h4><blockquote>
<p>首先创建容器dbdata2</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -v /dbupdate --name dbdata2 ubuntu ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<blockquote>
<p>新容器挂载dbdata2，然后untar解压备份文件到所挂载的容器卷中</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from dbdata2 -v $(<span class="built_in">pwd</span>):/backup busybox tar xvf /backup/backup.tar</span><br></pre></td></tr></table></figure>

<h2 id="端口映射和容器互联"><a href="#端口映射和容器互联" class="headerlink" title="端口映射和容器互联"></a>端口映射和容器互联</h2><h3 id="端口映射"><a href="#端口映射" class="headerlink" title="端口映射"></a>端口映射</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 可以-p映射多端口-p po -p po</span></span><br><span class="line"><span class="comment"># 可以指定ip:po:po</span></span><br><span class="line"><span class="comment"># 还可以本地随机端口ip::po</span></span><br><span class="line"><span class="comment"># 还可以指定udp端口ip:po:po/udp</span></span><br></pre></td></tr></table></figure>

<h3 id="容器互联"><a href="#容器互联" class="headerlink" title="容器互联"></a>容器互联</h3><blockquote>
<p>创建新容器</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -d --name db training/postgres</span><br></pre></td></tr></table></figure>

<blockquote>
<p>创建新容器连接到db容器</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --link　name:alias</span></span><br><span class="line">$ docker run -d -P --name web --link db:db traning/webapp</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker基本使用</title>
    <url>/2020/06/12/Docker%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h2 id="修改镜像源"><a href="#修改镜像源" class="headerlink" title="修改镜像源"></a>修改镜像源</h2><blockquote>
<p>（manjaro）只需要添加配置文件/etc/docker/daemon.json</p>
</blockquote>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"registry-mirrors"</span>: [<span class="string">"https://docker.mirrors.ustc.edu.cn"</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="或者使用–registry-mirror选项"><a href="#或者使用–registry-mirror选项" class="headerlink" title="或者使用–registry-mirror选项"></a>或者使用–registry-mirror选项</h3><ul>
<li>修改:/etc/default/docker</li>
<li>添加:DOCKER_OPTS=”–registry-mirror=<a href="https://docker.mirrors.ustc.edu.cn&quot;" target="_blank" rel="noopener">https://docker.mirrors.ustc.edu.cn&quot;</a></li>
<li><code>systemctl daemon-reload</code></li>
<li><code>systemctl restart docker</code></li>
</ul>
<hr>
<h2 id="一些Docker基础命令"><a href="#一些Docker基础命令" class="headerlink" title="一些Docker基础命令"></a>一些Docker基础命令</h2><h2 id="容器操作"><a href="#容器操作" class="headerlink" title="容器操作"></a><em>容器操作</em></h2><h3 id="run-通过运行镜像输出hello"><a href="#run-通过运行镜像输出hello" class="headerlink" title="run: 通过运行镜像输出hello"></a><font color="red">run</font>: 通过运行镜像输出hello</h3><blockquote>
<p><code>docker run CONTAINER [COMMAND] [ARGS]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 相当于先create再start</span></span><br><span class="line"><span class="comment"># e.g. docker run CONTAINER echo 'hello'</span></span><br></pre></td></tr></table></figure>

<h3 id="启动交互式容器"><a href="#启动交互式容器" class="headerlink" title="启动交互式容器"></a>启动交互式容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># e.g.:指定ip:host进行后台运行</span></span><br><span class="line">$ docker run -it -d -p 127.0.0.1:12306:12306 --name postgres ichheitimg:9.1-deb /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="带名字的运行镜像"><a href="#带名字的运行镜像" class="headerlink" title="带名字的运行镜像"></a>带名字的运行镜像</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --name=<span class="string">'name'</span> CONTAINER <span class="built_in">echo</span> <span class="string">'hello'</span></span><br></pre></td></tr></table></figure>

<h3 id="守护形式运行容器"><a href="#守护形式运行容器" class="headerlink" title="守护形式运行容器"></a>守护形式运行容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># C^P C^Q退出，可docker ps查看守护容器</span></span><br></pre></td></tr></table></figure>

<h4 id="可直接启动后台"><a href="#可直接启动后台" class="headerlink" title="可直接启动后台"></a>可直接启动后台</h4><p><a href="https://docs.docker.com/reference/commandline/cli" target="_blank" rel="noopener">配置选项</a></p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 运行相关选项</span></span><br><span class="line"> <span class="comment"># -D, --debug=false</span></span><br><span class="line"> <span class="comment"># -e, --exec-driver="native"</span></span><br><span class="line"> <span class="comment"># -g, --graph="/var/lib/docker"</span></span><br><span class="line"> <span class="comment"># --icc=true</span></span><br><span class="line"> <span class="comment"># -l, --log-level="info"</span></span><br><span class="line"> <span class="comment"># --label=[]</span></span><br><span class="line"> <span class="comment"># -p, --pidfile="/var/run/docker.pid"</span></span><br><span class="line"> <span class="comment"># </span></span><br><span class="line">$ docker run -d CONTAINER [COMMAND] [ARG...]</span><br><span class="line"><span class="comment"># 服务器相关选项</span></span><br><span class="line"> <span class="comment"># -G, --group="docker"</span></span><br><span class="line"> <span class="comment"># -H, --host=[]</span></span><br><span class="line"> <span class="comment"># --tls=false</span></span><br><span class="line"> <span class="comment"># --tlscacert="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlscert="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlskey="/home/sven/.docker/ca.pem"</span></span><br><span class="line"> <span class="comment"># --tlsverify=false</span></span><br><span class="line"><span class="comment"># Remote API存储相关</span></span><br><span class="line"> <span class="comment"># -s, --storage-driver=""</span></span><br><span class="line"> <span class="comment"># --selinux-enabled=false</span></span><br><span class="line"> <span class="comment"># --storage-opt=[]</span></span><br><span class="line"><span class="comment"># Registry相关</span></span><br><span class="line"> <span class="comment"># --insecure-registry=[]</span></span><br><span class="line"> <span class="comment"># --registry-mirror=p[]</span></span><br><span class="line"><span class="comment"># 网络设置相关</span></span><br><span class="line"> <span class="comment"># -b, --bridge=""</span></span><br><span class="line"> <span class="comment"># --bip=""</span></span><br><span class="line"> <span class="comment"># --fixed-cidr=""</span></span><br><span class="line"> <span class="comment"># --fixed-cidr-v6=""</span></span><br><span class="line"> <span class="comment"># --dns=[]</span></span><br><span class="line"> <span class="comment"># --dns-search=[]</span></span><br><span class="line"> <span class="comment"># --ip=0</span></span><br><span class="line"> <span class="comment"># --ip-forward=true</span></span><br><span class="line"> <span class="comment"># --ip-masq=true</span></span><br><span class="line"> <span class="comment"># --iptables=true</span></span><br><span class="line"> <span class="comment"># --ipv6=false</span></span><br><span class="line"> <span class="comment"># --mtu=0</span></span><br></pre></td></tr></table></figure>

<h3 id="设置容器的端口映射"><a href="#设置容器的端口映射" class="headerlink" title="设置容器的端口映射"></a>设置容器的端口映射</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -p, --publish</span></span><br><span class="line"><span class="comment"># containerPort</span></span><br><span class="line">$ docker run -p 80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># hostPort:containerPort</span></span><br><span class="line">$ docker run -p 8080:80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># ip::containerPort</span></span><br><span class="line">$ docker run -p 0.0.0.0:80 -it CONTAINER /bin/bash</span><br><span class="line"><span class="comment"># ip:hostPort:containerPort</span></span><br><span class="line">$ docker run -p 0.0.0.0:8080:80 -it CONTAINER /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="create-创建容器"><a href="#create-创建容器" class="headerlink" title="create: 创建容器"></a><font color="red">create</font>: 创建容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 新建新容器，如果没有指定镜像，会自动pull镜像</span></span><br><span class="line">$ docker create -it ubuntu:latest</span><br></pre></td></tr></table></figure>

<h3 id="start-启动容器"><a href="#start-启动容器" class="headerlink" title="start: 启动容器"></a><font color="red">start</font>: 启动容器</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 启动创建好的容器</span></span><br><span class="line">$ docker start XXXXXXX</span><br></pre></td></tr></table></figure>

<h3 id="ps-查看容器"><a href="#ps-查看容器" class="headerlink" title="ps: 查看容器"></a><font color="red">ps</font>: 查看容器</h3><blockquote>
<p><code>docker ps [OPT]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a 全部记录</span></span><br><span class="line"><span class="comment"># -l 最新记录</span></span><br></pre></td></tr></table></figure>

<h3 id="rm-删除容器"><a href="#rm-删除容器" class="headerlink" title="rm: 删除容器"></a><font color="red">rm</font>: 删除容器</h3><blockquote>
<p><code>docker rm &#39;{IMAGE}&#39;</code></p>
</blockquote>
<h3 id="attach-继续运行后台守护容器"><a href="#attach-继续运行后台守护容器" class="headerlink" title="attach: 继续运行后台守护容器"></a><font color="red">attach</font>: 继续运行后台守护容器</h3><blockquote>
<p><code>docker attach &#39;{IMAGE}&#39;</code><br><code>dokcer attach [--detach-keys[=[]]] [--no-stdin][--sig-proxy[=true]] 
CONTAINER</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">```</span><br><span class="line"></span><br><span class="line"><span class="comment">### &lt;font color="red"&gt;logs&lt;/font&gt;: 查看容器日志</span></span><br><span class="line">&gt; `docker logs [OPT] <span class="string">'&#123;IMAGE&#125;'</span>`</span><br><span class="line">```zsh</span><br><span class="line"> <span class="comment"># -f --follows=true|false 默认false</span></span><br><span class="line"> <span class="comment"># -t --timestamps=true|false 默认false</span></span><br><span class="line"> <span class="comment"># --tail="all"</span></span><br></pre></td></tr></table></figure>

<h3 id="exec-在已运行的容器中启动新进程"><a href="#exec-在已运行的容器中启动新进程" class="headerlink" title="exec: 在已运行的容器中启动新进程"></a><font color="red">exec</font>: 在已运行的容器中启动新进程</h3><blockquote>
<p><code>docker exec [OPT] CONTAINER [COMMAND] {ARGS}</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -d</span></span><br><span class="line"><span class="comment"># -i </span></span><br><span class="line"><span class="comment"># -t</span></span><br></pre></td></tr></table></figure>

<h3 id="stop-kill-停止容器"><a href="#stop-kill-停止容器" class="headerlink" title="stop/kill: 停止容器"></a><font color="red">stop/kill</font>: 停止容器</h3><blockquote>
<p><code>docker stop [-t | --time[=10]] CONTAINER</code><br><code>docker kill CONTAINER</code></p>
</blockquote>
<hr>
<h2 id="镜像操作"><a href="#镜像操作" class="headerlink" title="镜像操作"></a><em>镜像操作</em></h2><h3 id="images-inspect-查看镜像"><a href="#images-inspect-查看镜像" class="headerlink" title="images/inspect: 查看镜像"></a><font color="red">images/inspect</font>: 查看镜像</h3><blockquote>
<p><code>docker iamges [OPT] [REPOSITORY]</code><br><code>docker inspect [OPT] CONTAINER|IMAGE [...]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a, all=false</span></span><br><span class="line"><span class="comment"># -f, --filter=[]</span></span><br><span class="line"><span class="comment"># --no-trunc=false</span></span><br><span class="line"><span class="comment"># -q, --quiet=false</span></span><br></pre></td></tr></table></figure>

<h3 id="search-查找镜像"><a href="#search-查找镜像" class="headerlink" title="search: 查找镜像"></a><a href="https://registry.hub.docker.com" target="_blank" rel="noopener"><font color="red">search</font>: 查找镜像</a></h3><blockquote>
<p><code>docker search [OPT] TERM</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --automated=false</span></span><br><span class="line"><span class="comment"># --no-trunc=false</span></span><br><span class="line"><span class="comment"># -s, --stars=0</span></span><br><span class="line"><span class="comment"># --filter=stars=3</span></span><br></pre></td></tr></table></figure>

<h3 id="pull-拉取镜像"><a href="#pull-拉取镜像" class="headerlink" title="pull: 拉取镜像"></a><font color="red">pull</font>: 拉取镜像</h3><blockquote>
<p><code>docker pull [OPT] NAME [:TAG]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -a, --all-ags=false</span></span><br></pre></td></tr></table></figure>

<h3 id="构建镜像"><a href="#构建镜像" class="headerlink" title="构建镜像"></a>构建镜像</h3><h4 id="commit-通过容器"><a href="#commit-通过容器" class="headerlink" title="commit: 通过容器"></a><font color="red">commit</font>: 通过容器</h4><blockquote>
<p><code>docker commit [OPT] CONTAINER [REPOSITORY[:TAG]]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -a,--author=""</span></span><br><span class="line"> <span class="comment"># -m,--message=""</span></span><br><span class="line"> <span class="comment"># -p,--pause=true, 提交时暂停容器运行</span></span><br><span class="line"><span class="comment"># e.g.:提交本地deb容器</span></span><br><span class="line">$ docker commit deb ichheitimg:9.1-deb</span><br></pre></td></tr></table></figure>

<h4 id="import-通过本地模板导入"><a href="#import-通过本地模板导入" class="headerlink" title="import: 通过本地模板导入"></a><font color="red">import</font>: 通过本地模板导入</h4><blockquote>
<p><code>docker import [OPT] file |URL|[REPOSITORY[:TAG]]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.:导入</span></span><br><span class="line">$ cat ubuntu-14.04-x86_64-minimal.tar.gz | docker import - ubuntu:14.04</span><br></pre></td></tr></table></figure>

<h4 id="build-通过Dockerfile文件"><a href="#build-通过Dockerfile文件" class="headerlink" title="build: 通过Dockerfile文件"></a><font color="red">build</font>: 通过Dockerfile文件</h4><blockquote>
<p><code>docker build</code></p>
</blockquote>
<h4 id="通过Dockerfile"><a href="#通过Dockerfile" class="headerlink" title="通过Dockerfile"></a>通过Dockerfile</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --force-rm=false</span></span><br><span class="line"> <span class="comment"># --no-cache=false</span></span><br><span class="line"> <span class="comment"># --pull=false</span></span><br><span class="line"> <span class="comment"># -q, --quiet=false</span></span><br><span class="line"> <span class="comment"># --rm=true</span></span><br><span class="line"> <span class="comment"># -t, --tag=""</span></span><br><span class="line">$ docker build [OPT] PATH | URL | -</span><br><span class="line"><span class="comment"># e.g. 在创建的目录中编写Dockerfile文件，然后使用$ docker build -t='name' .</span></span><br></pre></td></tr></table></figure>

<h5 id="Dockerfile格式："><a href="#Dockerfile格式：" class="headerlink" title="Dockerfile格式："></a>Dockerfile格式：</h5><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># First Dockerfile</span></span><br><span class="line">   FROM [CONTAINER]</span><br><span class="line">   MAINTAINER [MESSAGE]</span><br><span class="line">   [COMMAND...]</span><br><span class="line">   EXPOSE 80</span><br><span class="line">   CMD [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>] (<span class="built_in">exec</span> mode)</span><br><span class="line">   CMD <span class="built_in">command</span> param1 param2 (shell mode)</span><br><span class="line">   CMD [<span class="string">"param1"</span>, <span class="string">"param2"</span>] (ENTRYPOINT指令搭配下的参数)</span><br><span class="line">   ENTRYPOINT [<span class="string">"executable"</span>, <span class="string">"param1"</span>, <span class="string">"param2"</span>] (<span class="built_in">exec</span> mode)</span><br><span class="line">   ENTRYPOINT <span class="built_in">command</span> param1 param2 (shell mode)</span><br><span class="line">   <span class="comment"># CMD与ENTRYPOINT的区别是后者命令不会被构建时命令所覆盖，也可以使用--entrypoint覆盖</span></span><br><span class="line">   ADD &lt;src&gt; .. &lt;dest&gt;</span><br><span class="line">   COPY &lt;src&gt; ... &lt;dest&gt;</span><br><span class="line">   VOLUMN [<span class="string">"/data"</span>]</span><br><span class="line">   (共享数据等作用，使用时在build构建前应使用--volumes-from选项挂在数据卷容器)</span><br><span class="line">   WORKDIR /path/to/workdir (指定工作目录)</span><br><span class="line">   ENV &lt;key&gt; &lt;value&gt;</span><br><span class="line">   ENV &lt;key&gt;=&lt;value&gt; ...</span><br><span class="line">   USER user (e.g. | USER user | USER user:group | USER user:gid |)</span><br><span class="line">   ONBUILD [INSTRUCTION] (添加触发器)</span><br></pre></td></tr></table></figure>

<h5 id="Dockerfile构建过程"><a href="#Dockerfile构建过程" class="headerlink" title="Dockerfile构建过程"></a>Dockerfile构建过程</h5><ul>
<li>从基础镜像运行容器</li>
<li>执行指令，对容器做修改</li>
<li>执行类docker commit操作，提交新镜像层</li>
<li>基于新镜像运行新容器</li>
<li>执行Dockerfile的下一指令直到所有指令执行完毕</li>
</ul>
<h3 id="save-存入镜像"><a href="#save-存入镜像" class="headerlink" title="save: 存入镜像"></a><font color="red">save</font>: 存入镜像</h3><blockquote>
<p><code>docker save</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 导出本地的ubuntu:14.04为文件ubuntu_14.014.tar</span></span><br><span class="line">$ docker save -o ubuntu_14.04.tar ubuntu:14:04</span><br></pre></td></tr></table></figure>

<h3 id="load-载入镜像"><a href="#load-载入镜像" class="headerlink" title="load: 载入镜像"></a><font color="red">load</font>: 载入镜像</h3><blockquote>
<p><code>docker load</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 导出tar文件到本地镜像库</span></span><br><span class="line">$ docker load --input ubuntu_14.04.tar</span><br><span class="line"><span class="comment"># 或</span></span><br><span class="line">$ docker load &lt; ubuntu_14.04.tar</span><br></pre></td></tr></table></figure>

<h3 id="push-上传镜像"><a href="#push-上传镜像" class="headerlink" title="push: 上传镜像"></a><font color="red">push</font>: 上传镜像</h3><blockquote>
<p><code>docker push NAME[: TAG] |REGISTRY_HOST[: REGISTRY_PORT]/NAME[: TAG]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.: 用户user上传test:latest镜像到user/test:latest</span></span><br><span class="line">$ docker tag <span class="built_in">test</span>:latest user/<span class="built_in">test</span>:latest</span><br><span class="line">$ docker push user/<span class="built_in">test</span>:latest</span><br></pre></td></tr></table></figure>

<h3 id="rmi-删除镜像"><a href="#rmi-删除镜像" class="headerlink" title="rmi: 删除镜像"></a><font color="red">rmi</font>: 删除镜像</h3><blockquote>
<p><code>docker rmi [OPT] IMAGE [...]</code></p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -f, --force=false</span></span><br><span class="line"><span class="comment"># --no-prune=false</span></span><br></pre></td></tr></table></figure>

<h3 id="Docker的C-S模式：Remote-API"><a href="#Docker的C-S模式：Remote-API" class="headerlink" title="Docker的C/S模式：Remote API"></a>Docker的C/S模式：<a href="https://docs.docker.com/reference/api/docker_remote_api/" target="_blank" rel="noopener">Remote API</a></h3><h4 id="连接方式"><a href="#连接方式" class="headerlink" title="连接方式"></a>连接方式</h4><ul>
<li><code>unix:///var/run/docker.dock</code></li>
<li><code>tcp://host:port</code></li>
<li><code>fd://socketfd</code><h4 id="Docker的远程访问"><a href="#Docker的远程访问" class="headerlink" title="Docker的远程访问"></a>Docker的远程访问</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 默认配置-H unix:///var/run/docker.sock</span></span><br><span class="line"><span class="comment"># e.g.: docker -H tcp://ip:port COMMAND</span></span><br><span class="line"><span class="comment"># 可选择的环境变量DOCKER_HOST</span></span><br><span class="line"><span class="comment"># e.g.: export DOCKER_HOST="tcp://ip:port"</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="Docker容器互联"><a href="#Docker容器互联" class="headerlink" title="Docker容器互联"></a>Docker容器互联</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># --icc=true，允许容器间互联</span></span><br><span class="line"><span class="comment"># 每次镜像重启都可能导致IP的变更，因此使用以下方式</span></span><br><span class="line"> <span class="comment"># --link</span></span><br><span class="line">$ docker run --link=[CONTAINER]:[ALIAS] [IMAGE][COMMAND]</span><br><span class="line"><span class="comment"># e.g. docker run -it --name test --link=other:alias img:test</span></span><br><span class="line"><span class="comment"># 只允许特定容器间连接</span></span><br><span class="line">$ --icc=<span class="literal">false</span> --iptables=<span class="literal">true</span></span><br><span class="line">$ --link</span><br></pre></td></tr></table></figure>

<h3 id="Docker容器的数据卷"><a href="#Docker容器的数据卷" class="headerlink" title="Docker容器的数据卷"></a>Docker容器的数据卷</h3><h4 id="为容器添加数据卷"><a href="#为容器添加数据卷" class="headerlink" title="为容器添加数据卷"></a>为容器添加数据卷</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"> <span class="comment"># -v local_catalogue:docker_catalogue:[ro]</span></span><br><span class="line"><span class="comment"># e.g.</span></span><br><span class="line">$ sudo dcoker run -v ~/container_data:/data -it ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<h4 id="在Dockerfile中使用COLUMN下指定数据卷容器"><a href="#在Dockerfile中使用COLUMN下指定数据卷容器" class="headerlink" title="在Dockerfile中使用COLUMN下指定数据卷容器"></a>在Dockerfile中使用COLUMN下指定数据卷容器</h4><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># e.g.:</span></span><br><span class="line"><span class="comment"># 编写Dockefile，指定VOLUME目录</span></span><br><span class="line">$ docker build -t dorman/dvt .</span><br><span class="line">$ docker run -it --name <span class="built_in">test</span> dorman/dvt</span><br><span class="line"><span class="comment"># 以下--volumes-from将共享Dockfile中的目录</span></span><br><span class="line">$ docker run -it --name test1 --volumes-from <span class="built_in">test</span> ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<h3 id="Docker数据卷的备份和还原"><a href="#Docker数据卷的备份和还原" class="headerlink" title="Docker数据卷的备份和还原"></a>Docker数据卷的备份和还原</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ docker run --volumes-from [container name] -v local_catalogue:docker_catalogue ubuntu</span><br><span class="line">$ tar cvf /backup/backup.tar [container data volume]</span><br></pre></td></tr></table></figure>

<h3 id="Docker容器的跨主机连接"><a href="#Docker容器的跨主机连接" class="headerlink" title="Docker容器的跨主机连接"></a>Docker容器的跨主机连接</h3><h4 id="网桥实现"><a href="#网桥实现" class="headerlink" title="网桥实现"></a>网桥实现</h4><ul>
<li>通过在主机和虚拟机之间使用同一个网段来跨主机连接</li>
<li>需要网络配置并且配置Docker网桥，并通过–fixed-cidr指定网络范围</li>
<li>(/etc/network/interfaces)</li>
</ul>
<h4 id="Open-vSwitch实现"><a href="#Open-vSwitch实现" class="headerlink" title="Open vSwitch实现"></a>Open vSwitch实现</h4><h5 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h5><ul>
<li>双网卡，Host-Only &amp; NAT</li>
<li>安装Open vSwitch和网桥管理工具bridge-utils</li>
<li>同一网段的IP地址<h5 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h5></li>
<li>建立ovs网桥</li>
<li>添加gre连接</li>
<li>配置docker容器虚拟网桥</li>
<li>为虚拟网桥添加ovs接口</li>
<li>添加不同DOcker容器网段路由</li>
</ul>
<h4 id="weave实现"><a href="#weave实现" class="headerlink" title="weave实现"></a>weave实现</h4>]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker的dockerfile编写</title>
    <url>/2020/06/12/Docker%E7%9A%84dockerfile%E7%BC%96%E5%86%99/</url>
    <content><![CDATA[<h2 id="使用Dockerfile创建镜像"><a href="#使用Dockerfile创建镜像" class="headerlink" title="使用Dockerfile创建镜像"></a>使用Dockerfile创建镜像</h2><ul>
<li>Dockerfile分为四部分<ul>
<li>基础镜像信息</li>
<li>维护者信息</li>
<li>镜像操作指令</li>
<li>容器启动时执行指令<h3 id="detail"><a href="#detail" class="headerlink" title="detail"></a>detail</h3><table>
<thead>
<tr>
<th align="left">指令</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>FROM</code></td>
<td align="left">指定基础镜像</td>
</tr>
<tr>
<td align="left"><code>MAINTAINER</code></td>
<td align="left">维护者信息</td>
</tr>
<tr>
<td align="left"><code>RUN</code></td>
<td align="left">运行命令</td>
</tr>
<tr>
<td align="left"><code>CMD</code></td>
<td align="left">启动容器时默认执行命令，最多只能执行一个命令</td>
</tr>
<tr>
<td align="left"><code>LABEL</code></td>
<td align="left">元数据标签信息</td>
</tr>
<tr>
<td align="left"><code>EXPOSE</code></td>
<td align="left">监听端口</td>
</tr>
<tr>
<td align="left"><code>ENV</code></td>
<td align="left">环境变量</td>
</tr>
<tr>
<td align="left"><code>ADD</code></td>
<td align="left">复制&lt;src&gt;到容器&lt;dest&gt;，如果是tar自动解压，src可以是URL</td>
</tr>
<tr>
<td align="left"><code>COPY</code></td>
<td align="left">复制本地&lt;src&gt;到容器&lt;dest&gt;</td>
</tr>
<tr>
<td align="left"><code>ENTRYPOINT</code></td>
<td align="left">指定镜像默认入口，最多只有一个，可以被<code>docker run --entrypoint</code>覆盖掉</td>
</tr>
<tr>
<td align="left"><code>VOLUME</code></td>
<td align="left">创建数据卷挂载点</td>
</tr>
<tr>
<td align="left"><code>USER</code></td>
<td align="left">指定运行容器时的USER或UID</td>
</tr>
<tr>
<td align="left"><code>WORKDIR</code></td>
<td align="left">为<code>RUN</code>、<code>CMD</code>和<code>ENTRYPOINT</code>指令配置工作目录</td>
</tr>
<tr>
<td align="left"><code>ARG</code></td>
<td align="left">指定一些镜像内使用的参数</td>
</tr>
<tr>
<td align="left"><code>ONBUILD</code></td>
<td align="left">当创建的镜像为其他镜像的基础镜像时执行的操作命令</td>
</tr>
<tr>
<td align="left"><code>STOPSIGNAL</code></td>
<td align="left">指定创建镜像启动的容器接收退出的信号值</td>
</tr>
<tr>
<td align="left"><code>HEALTHCHECK</code></td>
<td align="left">配置启动容器如何进行健康检查</td>
</tr>
</tbody></table>
</li>
</ul>
</li>
<li>使用<code>.dockerignore</code>文件可以让Docker忽略匹配路径下的目录和文件<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># comment</span><br><span class="line">*/temp*</span><br><span class="line">*/*/temp*</span><br><span class="line">tmp?</span><br><span class="line">~*</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="desc"><a href="#desc" class="headerlink" title="desc"></a>desc</h3><table>
<thead>
<tr>
<th align="left">指令</th>
<th align="left">格式</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>FROM</code></td>
<td align="left"><code>&lt;image&gt;:&lt;tag&gt;</code> || <code>&lt;image&gt;@&lt;digest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>MAINTAINER</code></td>
<td align="left"><code>&lt;name&gt;</code></td>
</tr>
<tr>
<td align="left"><code>RUN</code></td>
<td align="left">shell:<code>&lt;command&gt;</code> || exec:<code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>CMD</code></td>
<td align="left"><code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code> || <code>command param1 param2</code> || [“param1”, “param2”]</td>
</tr>
<tr>
<td align="left"><code>LABEL</code></td>
<td align="left"><code>&lt;key&gt;=&lt;value&gt;[&lt;key&gt;=&lt;value&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>EXPOSE</code></td>
<td align="left"><code>&lt;port&gt;[&lt;port&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>ENV</code></td>
<td align="left"><code>&lt;key&gt; &lt;value&gt;</code> || <code>&lt;key&gt;=&lt;value&gt;[&lt;key&gt;=&lt;value&gt;...]</code></td>
</tr>
<tr>
<td align="left"><code>ADD</code></td>
<td align="left"><code>&lt;src&gt; &lt;dest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>COPY</code></td>
<td align="left"><code>&lt;src&gt; &lt;dest&gt;</code></td>
</tr>
<tr>
<td align="left"><code>ENTRYPOINT</code></td>
<td align="left">shell:<code>command param1 param2</code> || exec: <code>[&quot;executable&quot;, &quot;param1&quot;, &quot;param2&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>VOLUME</code></td>
<td align="left"><code>[&quot;/data&quot;]</code></td>
</tr>
<tr>
<td align="left"><code>USER</code></td>
<td align="left"><code>daemon</code></td>
</tr>
<tr>
<td align="left"><code>WORKDIR</code></td>
<td align="left"><code>/path/to/workdir</code>，如果是相对路径，路径是基于前个<code>WORKDIR</code>的</td>
</tr>
<tr>
<td align="left">### exampe</td>
<td align="left"></td>
</tr>
<tr>
<td align="left"><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">FROM debian:jessie</span><br><span class="line">MAINTAINER NGINX Docker Maintainers &quot;docker-maint@nginx.com&quot;</span><br><span class="line">ENV NGINX_VERSION 1.10.1-1-jessie</span><br><span class="line">RUN apt-key adv --keyserver hkp://pgp.mit.edu:80 --recv-keys ....</span><br><span class="line">RUN ln -sf /dev/stdout /var/log/nginx/access.log \</span><br><span class="line">        $$ ln -sf /dev/stderr /var/log/nginx/error.log</span><br><span class="line">EXPOSE 80 443</span><br><span class="line">CMD [&quot;nginx&quot;, &quot;-g&quot;, &quot;daemon off;&quot;]</span><br></pre></td></tr></table></figure></td>
<td align="left"></td>
</tr>
</tbody></table>
<h3 id="sys"><a href="#sys" class="headerlink" title="sys"></a>sys</h3><h4 id="如果使用alpine镜像"><a href="#如果使用alpine镜像" class="headerlink" title="如果使用alpine镜像"></a>如果使用alpine镜像</h4><blockquote>
<p>安装软件包需要用apk包管理器替换apt工具</p>
</blockquote>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ apk add --no-cache &lt;package&gt;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>可以从<a href="https://pkgs.alpinelinux.org/packages" target="_blank" rel="noopener">alpine</a>搜索并确定安装包名称<br>如果需要指定社区的安装包，则：</p>
</blockquote>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">echo</span> <span class="string">"http://dl-4.alpinelinux.org/alpine/edge/teseting"</span> &gt;&gt; /etc/apk/repositories</span><br><span class="line">$ apk --update add --no-cache &lt;package&gt;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>VM</tag>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt元对象系统</title>
    <url>/2019/07/06/Qt%E5%85%83%E5%AF%B9%E8%B1%A1%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h3 id="元对象系统"><a href="#元对象系统" class="headerlink" title="元对象系统"></a>元对象系统</h3><p>Qt的<font color="green">元对象编译器</font>(<font color="red">Meta-Object Compiler, MOC</font>)是一个预处理器，它用于将Qt特性的程序转换为标准C++兼容的形式，再由标准C++编译器进行编译。例如，在使用信号与槽机制的类里，通过添加一个<code>Q_OBJECT</code>宏，MOC才对类中的信号与槽的代码进行预处理。<br>Qt的<font color="green">元对象系统</font>(<font color="red">Meta-Object System</font>)提供了对象之间通信的<strong>信号与槽机制</strong>、<strong>运行时类型信息</strong>和<strong>动态属性系统</strong>。<br>除了信号与槽机制，元对象还提供如下一些功能：</p>
<ul>
<li><p><code>QObject::metaObject()</code>：返回类关联的元对象，元对象类<code>QMetaObject</code>包含了访问元对象的接口函数</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g. 运行时返回类的名称字符串</span></span><br><span class="line">QObject *obj = <span class="keyword">new</span> QPushButton;</span><br><span class="line">obj-&gt;metaObject()-&gt;classNmae;   <span class="comment">// 返回"QPushButton"</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>QMetaObject::newInstance()</code>：创建类的一个新的实例</p>
</li>
<li><p><code>QObject::inherits(const char *className)</code>：判断对象是否是名为className的类，或者QObject的子类的实例</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g.</span></span><br><span class="line">QTimer *timer = <span class="keyword">new</span> QTimer;</span><br><span class="line">timer-&gt;inherits(<span class="string">"Qtimer"</span>);          <span class="comment">// return true</span></span><br><span class="line">timer-&gt;inherits(<span class="string">"QObject"</span>);         <span class="comment">// return true</span></span><br><span class="line">timer-&gt;inherits(<span class="string">"QABstraactButton"</span>);<span class="comment">// return false</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>qobject_cast()</code>：dynamic cast功能</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// e.g. 假设QMyWidget是QWidget的子类并且类中声明了Q_OBJECT宏</span></span><br><span class="line">QObject *obj = <span class="keyword">new</span> QMyWidget;</span><br><span class="line">QWidget *widget = qobject_cast&lt;QWidget *&gt;(obj);         <span class="comment">// 向上转型</span></span><br><span class="line">QMyWidget *myWidget = qobject_cast&lt;QMyWidget *&gt;(obj);   <span class="comment">// 向下转型</span></span><br><span class="line">QLabel *label = qobject_cast&lt;QLabel *&gt;(obj);            <span class="comment">// error</span></span><br></pre></td></tr></table></figure>
</li>
<li><p><code>QObject::tr()</code>和<code>QObject::trUtf8()</code>用于翻译字符串，用于多语言界面设计</p>
</li>
<li><p><code>QObject::setProperty()</code>和<code>QObject::property()</code>函数用于通过属性名称动态设置和获取属性值</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>PostgreSQL安装.md</title>
    <url>/2020/06/12/PostgreSQL%E5%AE%89%E8%A3%85/</url>
    <content><![CDATA[<p><a href="https://www.postgresql.org/" target="_blank" rel="noopener">PostgreSQL官网</a></p>
<h3 id="安装依赖："><a href="#安装依赖：" class="headerlink" title="安装依赖："></a>安装依赖：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># centos</span></span><br><span class="line">$ yum -y install readline-devel zlib-devel gcc gcc-c++ zlib readline</span><br><span class="line"><span class="comment"># debian/ubuntu</span></span><br><span class="line">$ apt-get install -y gcc make</span><br><span class="line">$ apt-get install -y libreadline-dev zlib1g zlib1g.dev</span><br></pre></td></tr></table></figure>

<h3 id="安装PostgreSQL："><a href="#安装PostgreSQL：" class="headerlink" title="安装PostgreSQL："></a>安装PostgreSQL：</h3><p><a href="https://sandbox-experiment-resource-north-4.obs.cn-north-4.myhuaweicloud.com/postgresql-arm/postgresql-10.3.tar.gz" target="_blank" rel="noopener">postgresql-10.3(maybe too<br>old)</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ wget https://sandbox-experiment-resource-north-4.obs.cn-north-4.myhuaweicloud.com/postgresql-arm/postgresql-10.3.tar.gz</span><br><span class="line">$ tar -zxvf postgresql-10.3.tar.gz -C /opt/</span><br><span class="line">$ <span class="built_in">cd</span> /opt/postgresql-*/</span><br><span class="line">$ ./configure --build=arm-linux --host=arm-linux --prefix=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line"><span class="comment"># canuse：./configure --prefix=/usr/local/pgsql --with-blocksize=8 \</span></span><br><span class="line"><span class="comment"># --enable-dtrace --enable-debug --enable-thread-safety</span></span><br><span class="line"><span class="comment"># --with-libxml --with-python --with-openssl</span></span><br><span class="line"><span class="comment"># $ make world</span></span><br><span class="line"><span class="comment"># $ make check-world (by normal user)</span></span><br><span class="line"><span class="comment"># $ make install-world</span></span><br><span class="line">$ make &amp;&amp; make install &amp;&amp; make clean</span><br></pre></td></tr></table></figure>

<h3 id="授权用户："><a href="#授权用户：" class="headerlink" title="授权用户："></a>授权用户：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ adduser postgres</span><br><span class="line">$ chown -R postgres:postgres /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ ls -ld /usr/<span class="built_in">local</span>/pgsql</span><br></pre></td></tr></table></figure>

<h3 id="配置环境变量："><a href="#配置环境变量：" class="headerlink" title="配置环境变量："></a>配置环境变量：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ su postgres</span><br><span class="line">$ vim ~/.bash_profile</span><br></pre></td></tr></table></figure>

<blockquote>
<p>配置文件中加入：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">export</span> PGHOME=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ <span class="built_in">export</span> PGDATA=/usr/<span class="built_in">local</span>/pgsql/data</span><br><span class="line">$ <span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:<span class="variable">$PGHOME</span>/bin</span><br></pre></td></tr></table></figure>

<blockquote>
<p>执行修改：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span>  ~/.bash_profile &amp;&amp; psql -V</span><br></pre></td></tr></table></figure>

<h3 id="初始化数据库："><a href="#初始化数据库：" class="headerlink" title="初始化数据库："></a>初始化数据库：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ initdb -D <span class="variable">$PGDATA</span> -E UTF8 --locale=C -U postgres -W</span><br></pre></td></tr></table></figure>

<h3 id="启动日志服务："><a href="#启动日志服务：" class="headerlink" title="启动日志服务："></a>启动日志服务：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mkdir -p /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br><span class="line">$ chmod 755 /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br></pre></td></tr></table></figure>

<h3 id="启动数据库："><a href="#启动数据库：" class="headerlink" title="启动数据库："></a>启动数据库：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span>  -l  <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br></pre></td></tr></table></figure>

<h3 id="检查结果："><a href="#检查结果：" class="headerlink" title="检查结果："></a>检查结果：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ps -ef | grep <span class="string">"postgres"</span></span><br><span class="line">$ netstat -nlp | grep <span class="string">"postgres"</span></span><br></pre></td></tr></table></figure>

<h3 id="修改数据库密码："><a href="#修改数据库密码：" class="headerlink" title="修改数据库密码："></a>修改数据库密码：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ psql中`\password postgres`可修改</span><br></pre></td></tr></table></figure>

<h3 id="配置为远程访问："><a href="#配置为远程访问：" class="headerlink" title="配置为远程访问："></a>配置为远程访问：</h3><blockquote>
<p>将”listen_addresses=’localhost’”取消注释并修改为任意地址’*’</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim /usr/<span class="built_in">local</span>/pgsql/data/postgresql.conf</span><br></pre></td></tr></table></figure>

<blockquote>
<p>修改访问地址控制设置：</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ vim /usr/<span class="built_in">local</span>/pgsql/data/pg_hba.conf</span><br></pre></td></tr></table></figure>

<blockquote>
<p>在”IPv4 local connections”下构建规则：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">host    all             all             0.0.0.0/0               md5</span><br></pre></td></tr></table></figure>

<h3 id="重启生效："><a href="#重启生效：" class="headerlink" title="重启生效："></a>重启生效：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl restart</span><br></pre></td></tr></table></figure>

<h2 id="在debian中"><a href="#在debian中" class="headerlink" title="在debian中"></a>在debian中</h2><h3 id="基本安装"><a href="#基本安装" class="headerlink" title="基本安装"></a>基本安装</h3><blockquote>
<p>创建文件<code>/etc/apt/sources.list.d/pgdg.list</code>，里面添加：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 其中stretch-pgdg具体可以在https://www.postgresql.org/download/linux/debian/中选择具体版本得到</span></span><br><span class="line">deb http://apt.postgresql.org/pub/repos/apt/ stretch-pgdg main</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Import the repository signing key, and update the package lists:</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ apt-get install -y gnupg gnupg1 gnupg2</span><br><span class="line">$ wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -</span><br><span class="line">$ apt-get update</span><br><span class="line">$ apt-get install -y postgresql-10</span><br><span class="line"></span><br><span class="line">$ useradd -m -s /bin/bash postgres</span><br><span class="line">$ mkdir /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ chown postgres:postgres /usr/<span class="built_in">local</span>/pgsql</span><br><span class="line">$ sudo -u postgres /usr/lib/postgressql</span><br></pre></td></tr></table></figure>

<blockquote>
<p>数据库初始化：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ /usr/lib/postgresql/11/bin/initdb -D <span class="variable">$PGDATA</span> -E UTF8 --locale=C -U postgres -W</span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>vim ~/.bash_profile</code>配置：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PGHOME=/usr/<span class="built_in">local</span>/pgsql</span><br><span class="line"><span class="built_in">export</span> PGDATA=/usr/<span class="built_in">local</span>/pgsql/data</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/usr/lib/postgresql/11/bin</span><br><span class="line"><span class="built_in">export</span> LANG=en_US.utf8</span><br></pre></td></tr></table></figure>

<blockquote>
<p>数据库日志：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 大可不必</span></span><br><span class="line">$ mkdir -p /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br><span class="line">$ chmod 755 /usr/<span class="built_in">local</span>/pgsql/<span class="built_in">log</span></span><br></pre></td></tr></table></figure>

<ul>
<li>can start data server <figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 一般启动</span></span><br><span class="line">$ /usr/lib/postgresql/11/bin/pg_ctl -D /usr/<span class="built_in">local</span>/pgsql/data -l logfile start</span><br><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span> -l <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="通过配置文件"><a href="#通过配置文件" class="headerlink" title="通过配置文件"></a>通过配置文件</h4><blockquote>
<p>启动，vim /etc/sysctl.conf中配置内核相关参数：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">kernel.shmmni = 4096</span><br><span class="line">kernel.sem = 50100 64128000 50100 1280</span><br><span class="line">fs.file-max = 7672460</span><br><span class="line">net.ipv4.ip_local_port_range = 9000 65000</span><br><span class="line">net.core.rmem_default = 1048576</span><br><span class="line">net.core.rmem_max = 4194304</span><br><span class="line">net.core.wmem_default = 262144</span><br><span class="line">net.core.wmen_max = 1028576</span><br></pre></td></tr></table></figure>

<blockquote>
<p><code>vim /etc/sysconfig/iptables</code>进行防火墙设置：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-A RH-Firewall-1-INPUT -i lo -j ACCEPT</span><br><span class="line"># 允许源IP</span><br><span class="line">-A RH-Firewall-1-INPUT -s 192.168.0.0/16 -j ACCEPT</span><br><span class="line"># 允许源IP访问目标端口</span><br><span class="line">-A RH-Firewall-1-INPUT -s 192.168.1.0/24 -m state --state </span><br><span class="line">NEW -m tcp -p tcp --dport [PORT] -j ACCEPT</span><br><span class="line"># 允许任意IP访问目标端口</span><br><span class="line">-A RH-Firewall-1-INPUT -p tcp -m state --state NEW -m</span><br><span class="line">tcp --dport [PORT] -j ACCEPT</span><br><span class="line"># 不是很懂</span><br><span class="line"># 使用iptables -L -v -n查看出入规则</span><br></pre></td></tr></table></figure>

<ul>
<li>在/etc/security/limits.conf配置。。。<blockquote>
<p><code>vim /usr/local/pgsql/data/pg_hba.conf</code>中的IPv4 local<br>connections修改访问地址控制规则设置：</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">host    all             all             0.0.0.0/0               md5</span><br></pre></td></tr></table></figure>

</li>
</ul>
<blockquote>
<p><code>vim /usr/local/pgsql/data/postgresql.conf</code>中</p>
</blockquote>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">将listen_addresses=<span class="string">'localhost'</span>取消注释并修改为任意地址<span class="string">'*0.0.0.0'</span></span><br><span class="line">superuser_reserved_connections超级用户最多连接改为13</span><br><span class="line">unix_socket_directories连接改为<span class="string">'.'</span>根目录下</span><br><span class="line">unix_socket_permissions权限为0700</span><br><span class="line"><span class="comment"># 心跳检测</span></span><br><span class="line">tcp_keepalives_idle = 60</span><br><span class="line">tcp_keepalives_interval = 10</span><br><span class="line">tcp_keepalives_count = 10</span><br><span class="line"><span class="comment"># 延迟</span></span><br><span class="line">vacuum_cost_delay = 10</span><br><span class="line">bgwritter_delay = 10ms</span><br><span class="line">wal_writer_delay = 200ms</span><br><span class="line"><span class="comment"># 归档</span></span><br><span class="line">hot_standby = on</span><br><span class="line"><span class="comment"># 日志</span></span><br><span class="line">log_destination = <span class="string">"csvlog"</span></span><br><span class="line">logging_collector = on</span><br><span class="line">log_directory = <span class="string">'pg_log'</span></span><br><span class="line">log_file_mode = 0600</span><br><span class="line">log_truncate_on_rotation = on等</span><br><span class="line"><span class="comment"># 还有checkpoint</span></span><br><span class="line">log_checkpoints = on</span><br><span class="line">log_connection = on</span><br><span class="line">log_disconnections = on</span><br><span class="line">log_error_verbosity = verbose</span><br></pre></td></tr></table></figure>

<blockquote>
<p>启动psql</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ pg_ctl -D <span class="variable">$PGDATA</span> -l <span class="variable">$PGHOME</span>/<span class="built_in">log</span>/pg_server.log start</span><br><span class="line">$ psql -h localhost -U postgres</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2020.06</category>
      </categories>
      <tags>
        <tag>PostgreSQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt信号和槽</title>
    <url>/2019/07/07/Qt%E4%BF%A1%E5%8F%B7%E5%92%8C%E6%A7%BD/</url>
    <content><![CDATA[<p>信号和槽是Qt的一个核心特点，也是区别于其他框架的重要特性。因为有了信号和槽的编程机制，Qt中处理界面各个组件的交互操作时变得更加直观和简单。信号与槽是<font color="blue">对象间通信</font>的机制，由Qt的元对象系统的支持才能实现。<br><font color="red">信号(Signal)</font>会在某种特定情况或动作下被触发。就是特殊情况下被发射的事件，而GUI程序设计的主要内容就是对界面上各组件信号的响应。<br><font color="red">槽(Slot)</font>则等同于接收并处理信号的函数。它跟一般的C++函数是一样的，可以直接被调用。但槽函数与普通函数不同的是，槽函数与一个信号关联，当信号被发射时，关联的槽函数会自动执行。<br>对于这种对象间通信，Qt隐藏了复杂的底层实现。Qt的信号与槽机制类似于“事件——响应”，但更灵活。  </p>
<hr>
<h2 id="1-connect-函数的不同参数形式"><a href="#1-connect-函数的不同参数形式" class="headerlink" title="1.connect()函数的不同参数形式"></a>1.connect()函数的不同参数形式</h2><ul>
<li><code>QObject::connect()</code>函数多重函数形式，一种参数原型是：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">/×</span><br><span class="line">sender是发送信号的对象名称</span><br><span class="line">signal()是信号名称，根据SIGNAL宏展开成相应字符串</span><br><span class="line">receiver是接受信号的对象名称</span><br><span class="line">slot()是槽函数名称，根据SLOT宏展开成字符串</span><br><span class="line">×/</span><br><span class="line">QMetaObject::Connection QObject::connect(<span class="keyword">const</span> QObject *sender, <span class="keyword">const</span> <span class="keyword">char</span> *signal, <span class="keyword">const</span> QObject *receiver, <span class="keyword">const</span> <span class="keyword">char</span> *method, Qt::ConnectionType type = Qt::AutoConnection)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>一般语法如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// connect()是QObject类中的一个静态函数，QObject又是所有Qt类的基类，实际调用可以忽略前面的限定符</span></span><br><span class="line"><span class="comment">// 宏SIGNAL()指信号，SLOT()指槽函数</span></span><br><span class="line">QObject::connection(sender, SIGNAL(signal()), receiver, SLOT(slot()));</span><br><span class="line"><span class="comment">// 如果带有参数，标明参数类型</span></span><br><span class="line">QObject::connection(spinNum, SIGNAL(valueChanged(<span class="keyword">int</span>)), <span class="keyword">this</span>, SLOT(updateStatus(<span class="keyword">int</span>));</span><br></pre></td></tr></table></figure>

<hr>
<ul>
<li>另一种参数形式的connect()函数原型是：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QMetaObject::Connection QObject::connect(<span class="keyword">const</span> QObject *sender, <span class="keyword">const</span> QMetaMethod &amp;signal, <span class="keyword">const</span> QObject *receiver, <span class="keyword">const</span> QMetaMethod, &amp;method, Qt::ConnectionType type = Qt::Auto Connection)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>这种具有默认参数，信号名称是唯一的，可以使用这种函数指针形式进行关联，语法如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QLineEdit只有一个textChanged(QString)信号，通过在窗体类widget里定义一个槽函数可以将它们关联，语法如下：</span></span><br><span class="line">QObject::connection(lineEdit, &amp;QLineEdit::textChanged, <span class="keyword">this</span>, &amp;widget::on_textChanged);</span><br></pre></td></tr></table></figure>

<p>但如果说类中定义有不同参数的同名信号就不能用函数指针的方式来关联信号和槽了：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QSpinBox有两个valueChanged()信号</span></span><br><span class="line"><span class="keyword">void</span> QSpinBox::valueChanged(<span class="keyword">int</span> i);</span><br><span class="line"><span class="keyword">void</span> QSpinBox::valueChanged(<span class="keyword">const</span> QString &amp;text);</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在窗体中定义一个槽函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">onValueChanged</span><span class="params">(<span class="keyword">int</span> i)</span></span>;</span><br></pre></td></tr></table></figure>

<p>Ctrl+B编译时将会出错：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QObject::connect(spinNum, &amp;QSpinBox::valueChanged, <span class="keyword">this</span>, &amp;widget::onValueChanged); <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<hr>
<p><code>connect()</code>函数最后都有一个<code>Qt::ConnectionType
type</code>，缺省参数是<code>Qt::AutoConnection</code>。<code>Qt::ConnectionType</code>是枚举类型，表示信号与槽的关联方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 缺省值，在信号触发时自动确定关联方式。如果发射者和接受者在同一线程，使用Qt::DirectConnection；否则使用Qt::QueuedConnection</span></span><br><span class="line">Qt::AutoConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号被发射时槽函数立刻执行</span></span><br><span class="line">Qt::DirectConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件循环回到接受者线程后执行槽函数</span></span><br><span class="line">Qt::QueuedConnection type;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 与Qt::QueuedConnection相似，但信号线程会阻塞到槽函数执行完毕。</span></span><br><span class="line"><span class="comment">// 注意在同一线程时不用这种方式，会死锁</span></span><br><span class="line">Qt::BlockingQueuedConnection type;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-使用sender-获得信号发送者"><a href="#2-使用sender-获得信号发送者" class="headerlink" title="2.使用sender()获得信号发送者"></a>2.使用sender()获得信号发送者</h2><p>在槽函数里，使用QObject::sender()可以获取信号发送者的指针，知道发射者的类型就可以将它类型转换成确定的类型。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// QSpinBox的valueChanged(int)信号里可以通过sender()和qobject_cast去的发射者指针然后对发射者进行操作：</span></span><br><span class="line">QSpinBox *spinBox = qobject_cast&lt;QSpinBox *&gt;(sender());</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过sender返回并打印Button的text值</span></span><br><span class="line">QObject::connect(button1, &amp;QPushButton::clicked, [<span class="keyword">this</span>]&#123;</span><br><span class="line">    QPushButton *button = qobject_cast&lt;QPushButton *&gt;(sender());</span><br><span class="line">    qDebug() &lt;&lt; button-&gt;text();</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-自定义信号及其使用"><a href="#3-自定义信号及其使用" class="headerlink" title="3.自定义信号及其使用"></a>3.自定义信号及其使用</h2><p>通过在自己设计的类中自定义信号，这个信号就是声明的函数，但这个函数不用实现，只需要发射（emit）：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在类中定义一个 ageChanged(int)</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">QPerson</span> :</span> <span class="keyword">public</span> QObject</span><br><span class="line">&#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> m_age = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">incAge</span><span class="params">()</span></span>;</span><br><span class="line">signals:</span><br><span class="line">    <span class="comment">// void返回类型，可以多参数，但函数无需实现</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">ageChanged</span><span class="params">(<span class="keyword">int</span> value)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在incAge()函数中发射信号,当m_age改变时，就emit信号。至于有无信号关联的槽函数，发射者不管</span></span><br><span class="line"><span class="keyword">void</span> QPerson::incAge()</span><br><span class="line">&#123;</span><br><span class="line">    m_age++;</span><br><span class="line">    <span class="function">emit <span class="title">ageChanged</span><span class="params">(m_age)</span></span>; <span class="comment">// emit signal</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-关于信号和槽的使用上的规则"><a href="#4-关于信号和槽的使用上的规则" class="headerlink" title="4.关于信号和槽的使用上的规则"></a>4.关于信号和槽的使用上的规则</h2><ol>
<li>一个信号可以连接多个槽</li>
<li>多个信号可以连接同一个槽</li>
<li>一个信号可以连接另一个信号</li>
<li>信号与槽的个数和类型需要一致，至少信号的参数不能少于槽的参数</li>
<li>使用信号和槽的类中，需要在类定义中加入宏Q_OBJECT</li>
<li>当信号被发射时，关联的槽函数会被立即执行，直到执行完毕才会执行余下的程序流程。</li>
</ol>
]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt属性系统</title>
    <url>/2019/07/10/Qt%E5%B1%9E%E6%80%A7%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="1-属性定义"><a href="#1-属性定义" class="headerlink" title="1.属性定义"></a>1.属性定义</h2><p>Qt提供了<code>Q_PROPERTY()</code>宏来定义属性，它是基于<a href="https://zetzhang.github.io/2019/07/06/Qt%E5%85%83%E5%AF%B9%E8%B1%A1%E7%B3%BB%E7%BB%9F/" target="_blank" rel="noopener">元对象系统</a>来实现的。作为平台与编译器无关的库，Qt C++程序可以被任何标注你的C++编译器编译。<br>在QObject的子类中，用宏<code>Q_PROPERTY()</code>定义属性，格式是：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Q_PROPERTY(type name</span><br><span class="line">           (READ getFunction [WRITE setFunction] |</span><br><span class="line">            MEMBER memberName [(READ getFunction | WRITE setFunction)])</span><br><span class="line">           [RESET resetFunction]</span><br><span class="line">           [NOTIFY notifySignal]</span><br><span class="line">           [REVISION <span class="keyword">int</span>]</span><br><span class="line">           [DESIGNABLE <span class="keyword">bool</span>]</span><br><span class="line">           [SCRIPTABLE <span class="keyword">bool</span>]</span><br><span class="line">           [STORED <span class="keyword">bool</span>]</span><br><span class="line">           [USER <span class="keyword">bool</span>]</span><br><span class="line">           [CONSTANT]</span><br><span class="line">           [FINAL])</span><br></pre></td></tr></table></figure>

<p><code>Q_PROPERTY</code>宏定义一个返回值类型为<code>type</code>，名称为<code>name</code>的属性，用<code>READ</code>、<code>WRITE</code>来作为属性的读/写操作，还有其他的一些关键字定义属性的一些操作特性。属性的类型可以是<code>QVariant</code>支持的任何类型，也可以用户自定义类型。<br><code>Q_PROPERTY</code>宏定义属性的一些主要关键字的意义如下：</p>
<ul>
<li><code>READ</code>指定一个读取属性值的函数，没有<code>MEMBER</code>关键字时必须设置<code>READ</code>。</li>
<li><code>WRITE</code>指定一个设定属性值的函数，只读属性没有<code>WRITE</code>设置。</li>
<li><code>MEMBER</code>指定一个成员变量与属性关联，成为可读可写的属性，无需再设置<code>READ</code>和<code>WRITE</code>。</li>
<li><code>RESET</code>是可选的，用于指定一个设置属性缺省值的函数。</li>
<li><code>NOTIFY</code>是可选的，用于设置一个信号，当属性值变化时发射此信号。</li>
<li><code>DESIGNABLE</code>表示属性是否在Qt Designer里可见，缺省为<code>true</code>。</li>
<li><code>CONSTANT</code>表示属性值是一个常数，对于一个对象实例，<code>READ</code>指定的函数返回值是常数，但是每个实例的返回值可以不一样。具有<code>CONSTANT</code>关键字的属性不能有<code>WRITE</code>和<code>NOTIFY</code>关键字。</li>
<li><code>FINAL</code>表示所定义的属性不能被子类重载。</li>
</ul>
<p>QWidget 类定义属性的一些例子如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Q_PROPERTY(<span class="keyword">bool</span> focus READ hasFocus)</span><br><span class="line">Q_PROPERTY(<span class="keyword">bool</span> enabled READ isEnabled WRITE setEnabled)</span><br><span class="line">Q_PROPERTY(QCursor cursor READ cursor WRITE setCursor RESET unsetCursor)</span><br><span class="line">Q_PROPERTY(QString windowTitle READ windowTitle WRITE setWindowTitle NOTIFY windowTitleChanged DESIGNABLE isWindow)</span><br></pre></td></tr></table></figure>

<p>上面最后这个声明包含了<code>NOTIFY windowTitleChanged</code>，表明当<code>windowTitle</code>属性发生变化时，会触发<code>windowTitleChanged</code>信号，相应的信号声明在QWidget中。(<code>this-&gt;setProperty(&quot;windowTitle&quot;, &quot;test&quot;)</code>)  </p>
<hr>
<p>关于<a href="http://blog.sina.com.cn/s/blog_c3c7614d0102wdm2.html" target="_blank" rel="noopener">如何使用NTOIFY和RESET——Qt爱好者</a></p>
<hr>
<h2 id="2-动态属性"><a href="#2-动态属性" class="headerlink" title="2.动态属性"></a>2.动态属性</h2><p>其中，不管有没有用<code>READ</code>和<code>WRITE</code>定义接口函数，只要知道属性名称，就可以通过<code>QObject::property()</code>和<code>QObject::setProperty()</code>读取和设置属性值。e.x.：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QPushButton *button = <span class="keyword">new</span> QPushButton;</span><br><span class="line">QObject *object = button;</span><br><span class="line">object-&gt;setProperty(<span class="string">"flat"</span>, <span class="literal">true</span>);</span><br><span class="line"><span class="keyword">bool</span> isFlat = object-&gt;property(<span class="string">"flat"</span>);</span><br></pre></td></tr></table></figure>

<p>这就是<font color="red">动态属性</font>，运行时通过<code>setProperty()</code>定义新属性，并用<code>property()</code>查询，它是针对类实例定义的。<br>在数据表编辑界面上，为了高亮显示一些必填字段，可以在初始化界面为这些字段关联组件定义新的required属性：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">editName-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line">comboSex-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line">checkAgree-&gt;setProperty(<span class="string">"required"</span>, <span class="string">"true"</span>);</span><br><span class="line"><span class="comment">// 应用样式定义将必填字段背景颜色设置为亮绿色</span></span><br><span class="line">*[required=<span class="string">"true"</span>] &#123;background-color: lime&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-类的附加属性"><a href="#3-类的附加属性" class="headerlink" title="3.类的附加属性"></a>3.类的附加属性</h2><p>属性系统还有一个宏<code>Q_CLASSINFO()</code>，可以为类的元对象定义“名称——值”信息，如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">QMyClass</span>:</span><span class="keyword">public</span> QObject </span><br><span class="line">&#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line">    Q_CLASSINFO(<span class="string">"author"</span>, <span class="string">"Wang"</span>)</span><br><span class="line">    Q_CLASSINFO (<span class="string">"company"</span>, <span class="string">"UPC"</span>)</span><br><span class="line">    Q_CLASSINFO(<span class="string">"version "</span>, <span class="string">"3.0.1"</span>)</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>用<code>Q_CLASSINFO()</code>宏定义附加类信息后，可以通过元对象的一些函数获取类的附加信息，如<code>classlnfo(int)</code>获取某个加信息，函数原型定义如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QMetaClassInfo QMetaObject::classInfo(<span class="keyword">int</span> index) <span class="keyword">const</span></span><br></pre></td></tr></table></figure>

<p>返回值是<code>QMetaClassInfo</code>类型，有<code>name()</code>和<code>value()</code>两个函数，可获得类附加信息的名称和值。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置附加值</span></span><br><span class="line">Q_CLASSINFO(<span class="string">"First"</span>, <span class="string">"Second"</span>)</span><br><span class="line"><span class="comment">// 输出键值</span></span><br><span class="line">qDebug() &lt;&lt; <span class="keyword">this</span>-&gt;metaObject()-&gt;classInfo(<span class="number">0</span>).value() &lt;&lt; <span class="string">": "</span> &lt;&lt; <span class="keyword">this</span>-&gt;metaObject()-&gt;classInfo(<span class="number">0</span>).name();</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2019.07</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title>个人Blog</title>
    <url>/2019/06/30/%E5%85%B3%E4%BA%8E%E4%B8%AA%E4%BA%BABlog%E7%9A%84%E5%9F%BA%E7%A1%80%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h2 id="nvm：node-js版本管理工具安装"><a href="#nvm：node-js版本管理工具安装" class="headerlink" title="nvm：node.js版本管理工具安装"></a><font color="red">nvm</font>：node.js版本管理工具安装</h2><p>nvm是可以管理不同版本的node和npm，所以我们通过nvm来安装Node.js<br>nvm的Github地址[nvm][<a href="https://github.com/nvm-sh/nvm]" target="_blank" rel="noopener">https://github.com/nvm-sh/nvm]</a></p>
<h3 id="安装nvm"><a href="#安装nvm" class="headerlink" title="安装nvm"></a>安装nvm</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># cURL</span></span><br><span class="line">$ curl https://raw.github.com/creationix/nvm/v0.33.11/install.sh | sh</span><br><span class="line"><span class="comment"># Wget</span></span><br><span class="line">$ wget -qO- https://raw.github.com/creationix/nvm/v0.33.11/install.sh | sh</span><br></pre></td></tr></table></figure>

<h3 id="进入～-nvm目录下配置环境"><a href="#进入～-nvm目录下配置环境" class="headerlink" title="进入～/.nvm目录下配置环境"></a>进入～/.nvm目录下配置环境</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">cd</span> ~/.nvm/ &amp;&amp; touch .bash_profile</span><br></pre></td></tr></table></figure>

<h3 id="向-bash-profile中写入如下内容"><a href="#向-bash-profile中写入如下内容" class="headerlink" title="向.bash_profile中写入如下内容"></a>向.bash_profile中写入如下内容</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> NVM_DIR=<span class="string">"<span class="variable">$HOME</span>/.nvm"</span></span><br><span class="line">[ -s <span class="string">"<span class="variable">$NVM_DIR</span>/nvm.sh"</span> ] &amp;&amp; \. <span class="string">"<span class="variable">$NVM_DIR</span>/nvm.sh"</span> <span class="comment"># This loads nvm</span></span><br></pre></td></tr></table></figure>

<h3 id="执行文件配置"><a href="#执行文件配置" class="headerlink" title="执行文件配置"></a>执行文件配置</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">source</span> .bash_profile</span><br></pre></td></tr></table></figure>

<h3 id="这时候就安装成功了"><a href="#这时候就安装成功了" class="headerlink" title="这时候就安装成功了"></a>这时候就安装成功了</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">nvm --version</span><br></pre></td></tr></table></figure>

<h3 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ nvm install stable</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h2><p><font color="red">npm</font>是世界上最大的软件注册表，每个星期有30亿次下载量</p>
<h3 id="npm设置为淘宝的镜像源"><a href="#npm设置为淘宝的镜像源" class="headerlink" title="npm设置为淘宝的镜像源"></a>npm设置为淘宝的镜像源</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ npm install -g cnpm --registry=https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure>

<h3 id="安装hexo-1"><a href="#安装hexo-1" class="headerlink" title="安装hexo"></a>安装hexo</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cnpm install -g hexo-cli</span><br></pre></td></tr></table></figure>

<h3 id="在想要创建的文件夹下初始化hexo目录"><a href="#在想要创建的文件夹下初始化hexo目录" class="headerlink" title="在想要创建的文件夹下初始化hexo目录"></a>在想要创建的文件夹下初始化hexo目录</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo init</span><br></pre></td></tr></table></figure>

<h3 id="使用hexo"><a href="#使用hexo" class="headerlink" title="使用hexo"></a>使用hexo</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo s        <span class="comment"># 启动hexo</span></span><br><span class="line">$ hexo clean    <span class="comment"># 删除数据库</span></span><br><span class="line">$ hexo g        <span class="comment"># update generate</span></span><br></pre></td></tr></table></figure>

<p>然后就可以在浏览器地址栏中输入localhost:4000访问到博客了</p>
<hr>
<h2 id="部署到Github"><a href="#部署到Github" class="headerlink" title="部署到Github"></a>部署到Github</h2><p>通过部署到远端服务器来公开使用</p>
<h3 id="在github上创建名为-你的ID-github-io-的项目"><a href="#在github上创建名为-你的ID-github-io-的项目" class="headerlink" title="在github上创建名为[你的ID.github.io]的项目"></a>在github上创建名为[你的ID.github.io]的项目</h3><h3 id="安装部署插件"><a href="#安装部署插件" class="headerlink" title="安装部署插件"></a>安装部署插件</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ cnpm install --save hexo-deployer-git</span><br></pre></td></tr></table></figure>

<h3 id="设置-config-yml"><a href="#设置-config-yml" class="headerlink" title="设置_config.yml"></a>设置_config.yml</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Deployment下配置</span></span><br><span class="line">    <span class="built_in">type</span>: git</span><br><span class="line">    repo: [your github repository url]</span><br><span class="line">    branch: master</span><br></pre></td></tr></table></figure>

<h3 id="部署到远端"><a href="#部署到远端" class="headerlink" title="部署到远端"></a>部署到远端</h3><figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ hexo d</span><br></pre></td></tr></table></figure>

<p><a href="https://hexo.io/zh-cn/docs/" target="_blank" rel="noopener">Hoex</a>文档说明</p>
<p>Reference from <a href="https://www.codesheep.cn/" target="_blank" rel="noopener">codesheep</a></p>
<h3 id="hexo-console-rename插件安装"><a href="#hexo-console-rename插件安装" class="headerlink" title="hexo-console-rename插件安装"></a>hexo-console-rename插件安装</h3><p><a href="https://github.com/timnew/hexo-console-rename" target="_blank" rel="noopener">hexo-console-rename</a> install using npm</p>
<figure class="highlight zsh"><table><tr><td class="code"><pre><span class="line">$ npm install hexo-console-rename@3 --save</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>2019.09</category>
      </categories>
      <tags>
        <tag>Github</tag>
        <tag>hexo</tag>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>第一章 关于对象</title>
    <url>/2020/06/12/%E7%AC%AC%E4%B8%80%E7%AB%A0-%E5%85%B3%E4%BA%8E%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<ul>
<li>C语言中，数据和函数是分开来声明的，也就是说，语言本身没有支持”数据和函数“之间的关联性，这种程序方法称为<font color="red">程序性的</font>（<font color="green">procedural</font>）。一般从C<br>struct转到C++的封装类后，唯一在布局以及存取时间上的负担是virtual引起的。</li>
</ul>
<h4 id="1-1-C-对象模式（The-C-Object-Model）"><a href="#1-1-C-对象模式（The-C-Object-Model）" class="headerlink" title="1.1 C++对象模式（The C++ Object Model）"></a>1.1 C++对象模式（The C++ Object Model）</h4><ul>
<li>C++中有<font color="red">两种class data members</font>：static和nonstatic，以及<font color="red">三种class member functions</font>：static、nonstatic和virtual。  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> xval);</span><br><span class="line">    <span class="keyword">virtual</span> ~Point();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">PointCount</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> ostream&amp; <span class="title">print</span><span class="params">(ostream &amp;os)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="keyword">float</span> _x;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> _point_count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h5 id="简单对象模型（A-Simple-Object-Model）"><a href="#简单对象模型（A-Simple-Object-Model）" class="headerlink" title="简单对象模型（A Simple Object Model）"></a>简单对象模型（A Simple Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">slot[Point pt] ==&gt; m[all members]</span><br></pre></td></tr></table></figure>

<ul>
<li>这个模型简单，一个object是一系列的slots，members按照声明次序各被指定一个slot。一个class object的大小是”指针大小*members个数“。<ul>
<li>这个模型<font color="red">应用</font>到C++的”<font color="red">指向成员的指针</font>“（<font color="green">pointer-to-member</font>）观念中。</li>
</ul>
</li>
</ul>
<h5 id="表格驱动对象模型（A-Table-driven-Object-Model）"><a href="#表格驱动对象模型（A-Table-driven-Object-Model）" class="headerlink" title="表格驱动对象模型（A Table-driven Object Model）"></a>表格驱动对象模型（A Table-driven Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">S[Point pt] == data member table ==&gt; e[内含实际数据]</span><br><span class="line">S == member function table ==&gt; m[内含函数地址]</span><br><span class="line">m ==&gt; a[...]</span><br><span class="line">m ==&gt; b[...]</span><br></pre></td></tr></table></figure>

<ul>
<li>这种模型把所有与members相关的信息抽出来，放在一个<strong>data member table</strong>和一个<strong>member function table</strong>之中，class object则包含指向这两个表格的指针。member function table是一系列slots，一个slot一个member function；data member table则直接含有data本身。</li>
<li>这个模型member function table这个观念称为支持<font color="red">virtual functions</font>的一个有效方案</li>
</ul>
<h5 id="C-对象模型（The-C-Object-Model）"><a href="#C-对象模型（The-C-Object-Model）" class="headerlink" title="C++对象模型（The C++ Object Model）"></a>C++对象模型（The C++ Object Model）</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph LR</span><br><span class="line">e1(static data members)</span><br><span class="line">e2(static function members)</span><br><span class="line">e3(nonstatic function members)</span><br><span class="line">Point[nonstatic data members] == Virtual table for Point ==&gt; vptr[vtbl]</span><br><span class="line">vptr == RTTI ==&gt; ti[type_info for Point]</span><br><span class="line">vptr ==&gt; a[...]</span><br><span class="line">vptr ==&gt; b[...]</span><br></pre></td></tr></table></figure>

<ul>
<li>nonstatic data members被配置于每个class object之内，static data members则被存放在class object之外；</li>
<li>static和nonstatic function members也放在所有class object之外；</li>
<li>virtual functions以两个步骤支持它：<ul>
<li>每个class产生一堆指向virtual functions的指针，放在<font color="green">virtual table</font>（<font color="red">vtbl</font>）中;</li>
<li>每个class object被添加一个指针，指向vtbl，通常这个指针被称为<font color="red">vptr</font>。vptr的设定（setting）和重置（resetting）由每个class的<strong>constructor、destructor和copy assignment运算符</strong>自动完成。每个class所关联的<font color="red">type_info object</font>（用以支持<font color="green">runtime type identification</font>，<font color="red">RTTI</font>）也经由vtbl指出，通常放在表格第一个slot处。</li>
</ul>
</li>
<li>这个模型优点在于空间和存取时间的效率；缺点则是如果所用到的class objects的nonstatic data members有所改动，那么那些应用程序得重新编译。</li>
</ul>
<h5 id="加上继承（Adding-Inheritance）"><a href="#加上继承（Adding-Inheritance）" class="headerlink" title="加上继承（Adding Inheritance）"></a>加上继承（Adding Inheritance）</h5><ul>
<li>C++支持<font color="red">单一继承</font>、<font color="red">多继承</font>和<font color="red">虚拟继承</font>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">ostream --- ios</span><br><span class="line">istream --- ios</span><br><span class="line">iostream --- ostream</span><br><span class="line">iostream --- istream</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="对象模型如何影响程序"><a href="#对象模型如何影响程序" class="headerlink" title="对象模型如何影响程序"></a>对象模型如何影响程序</h5><ul>
<li>下面这个函数，其中class X定义一个coyp constructor，一个virtual destructor和一个virtual function foo：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">foobar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    X *px = <span class="keyword">new</span> X;</span><br><span class="line">    <span class="comment">// foo() is a virtual function</span></span><br><span class="line">    xx.foo();</span><br><span class="line">    px-&gt;foo();</span><br><span class="line">    <span class="keyword">delete</span> px;</span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这个函数可能在内部转化为：</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[0] --&gt; type_info object</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[1] --&gt; X::~X()</span></span><br><span class="line"><span class="comment">// px-&gt;_vtbl[2] --&gt; X::foo()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foobar</span><span class="params">(X &amp;_result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    _result.X::X();</span><br><span class="line">    <span class="comment">// X *px = new X;</span></span><br><span class="line">    px = _new(<span class="keyword">sizeof</span>(x));</span><br><span class="line">    <span class="keyword">if</span> (px != <span class="number">0</span>)</span><br><span class="line">        px-&gt;X::X();</span><br><span class="line">    <span class="comment">// xx.foo()，不适用virtual机制</span></span><br><span class="line">    foo(&amp;_result);</span><br><span class="line">    <span class="comment">// virtaul机制扩展px-&gt;foo()</span></span><br><span class="line">    (*px-&gt;vtbl[<span class="number">2</span>])(px)</span><br><span class="line">    <span class="comment">// delete px</span></span><br><span class="line">    <span class="keyword">if</span> (px != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        (*px-&gt;vtbl[<span class="number">1</span>])(px); <span class="comment">// destructor</span></span><br><span class="line">        _delete(px);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 无须使用named return</span></span><br><span class="line">    statement</span><br><span class="line">    <span class="comment">// 无须摧毁local object xx</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="1-2-关键词所带来的差异（A-Keyword-Distinction）"><a href="#1-2-关键词所带来的差异（A-Keyword-Distinction）" class="headerlink" title="1.2 关键词所带来的差异（A Keyword Distinction）"></a>1.2 关键词所带来的差异（A Keyword Distinction）</h4><h5 id="关键词的困扰"><a href="#关键词的困扰" class="headerlink" title="关键词的困扰"></a>关键词的困扰</h5><ul>
<li><p>关键词struct的意思是一个数据集合体，没有private data，也没有data的相应操作。它和C++的<font color="red">使用者自定义类型</font>（<font color="green">user-defined type</font>）区分开来。struct关键词的使用实现了C的<font colo="red">数据抽象观念</font>，而class关键词实现的是C++的<font color="red">ADT</font>（<font color="grren">Abstract Data Type</font>）观念。</p>
<h5 id="策略性正确的struct（The-Politically-Correct-Struct）"><a href="#策略性正确的struct（The-Politically-Correct-Struct）" class="headerlink" title="策略性正确的struct（The Politically Correct Struct）"></a>策略性正确的struct（The Politically Correct Struct）</h5></li>
<li><p>C的巧计有时候是C++的陷阱。例如一个在struct尾端的单一数组，也是每个struct objects有可变长数据：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mutable</span> &#123;</span></span><br><span class="line">    <span class="comment">/* stuff */</span></span><br><span class="line">    <span class="keyword">char</span> pc[<span class="number">1</span>];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 为struct本身和该字符串配置足够的内存</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mumble</span> *<span class="title">pmumb1</span> = (<span class="title">struct</span> <span class="title">memble</span>*)<span class="title">malloc</span>(<span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">mumble</span>) + <span class="title">strlen</span>(<span class="title">string</span>) + 1);</span></span><br><span class="line"><span class="built_in">strcpy</span>(&amp;mumble.pc, <span class="built_in">string</span>);</span><br></pre></td></tr></table></figure>

<p>  让C++ class的部分数据拥有C声明那模样，是将那部分抽取出来成为独立的struct声明，然后<font color="red">组合</font>在一起，它能保证拥有与C兼容的空间布局：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">C_Point</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">C_point</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _c_point; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    C_point _c_point;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="1-3-对象的差异（An-Object-Distinction）"><a href="#1-3-对象的差异（An-Object-Distinction）" class="headerlink" title="1.3 对象的差异（An Object Distinction）"></a>1.3 对象的差异（An Object Distinction）</h4><ul>
<li><p>C++有以下方法支持<font color="red"><strong>多态</strong></font>：</p>
<ul>
<li><font color="red">隐式转化操作</font>。（<code>Shape *ps = new Circle()</code>）</li>
<li><font color="red">经由virtual function机制</font>。（<code>ps-&gt;rotate();</code>）</li>
<li><font color="red">经由dynamic_cast和typeid操作符</font>。（<code>if（Circle \*pc = dynamic\_cast&lt;Circle\*&gt;(ps))</code>）</li>
</ul>
</li>
<li><p>需要多少内存才能够表现一个<font color="red"><strong>class object大小</strong></font>？</p>
<ul>
<li><strong>nonstatic data members的总和大小</strong>。</li>
<li><strong>根据alignment需求padding的空间</strong>。（alignment是将数值调整到某数的倍数。32位机器上位4 bytes，64位机器上位8bytes）。</li>
<li><strong>支持virtual而内部产生的额外负担（overhead）</strong>。<h5 id="指针的类型（The-Type-of-a-Pointer）"><a href="#指针的类型（The-Type-of-a-Pointer）" class="headerlink" title="指针的类型（The Type of a Pointer）"></a>指针的类型（The Type of a Pointer）</h5></li>
</ul>
</li>
<li><p>一个指向ZooAnimal的指针与一个指向整数的指针或template Array的指针在内存上没什么区别。真正的不同是其所寻址出来的<font color="red"><strong>object类型</strong></font>不同，因为<font color="red">指针类型会教导编译器如何解释某个地址中的内存内容及其大小</font>。所以，这就是为什么void<em>能持有一个地址，但不同操作该地址的原因了。而<font color="red">转换</font>（<font color="green">cast</font>）也是一种编译器指令，*</em>它不改变地址，只影响内存大小和内容的解释方式**。</p>
<h5 id="加上多态之后（Adding-Polymorphism）"><a href="#加上多态之后（Adding-Polymorphism）" class="headerlink" title="加上多态之后（Adding Polymorphism）"></a>加上多态之后（Adding Polymorphism）</h5></li>
<li><p>定义一个Bear，作为一种ZooAnimal：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">ZooAnimal --- Bear</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bear</span> :</span> <span class="keyword">public</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Bear();</span><br><span class="line">    ~Bear();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Rotate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">dance</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">enum</span> Dances &#123; ... &#125;;</span><br><span class="line">    Dances dances_known;</span><br><span class="line">    <span class="keyword">int</span> cell_block;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Bear b;</span><br><span class="line">ZooAnimal *pz = &amp;b;</span><br><span class="line">Bear *pb = &amp;b;</span><br></pre></td></tr></table></figure>

<p>  两个对象都同指Bear object的第一个byte。区别是pb的地址包含Bear object，而pz的地址包含Bear Object中的ZooAnimal subobject。除非ZooAnimal subobject出现的members，否则不能使用pz直接处理Bear的members，唯一例外是virtual机制：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// error. cell_block不是ZooAnimal的member</span></span><br><span class="line">pz-&gt;cell_block;</span><br><span class="line"><span class="comment">// ok</span></span><br><span class="line">(<span class="keyword">static_cast</span>&lt;Bear*&gt;(pz))-&gt;cell_block;</span><br><span class="line"><span class="comment">// better</span></span><br><span class="line"><span class="keyword">if</span> (Bear *pb2 = <span class="keyword">dynamic_cast</span>&lt;Bear*&gt;(pz))</span><br><span class="line">    pb2-&gt;cell_block;</span><br><span class="line"><span class="comment">// ok</span></span><br><span class="line">pb-&gt;cell_block</span><br></pre></td></tr></table></figure>

<p>  当写下<code>pz-&gt;rotate();</code>时，pz的类型在编译期决定了两点：</p>
<ul>
<li><strong>固定的可用接口</strong>。就是说，pz只能调用ZooAnimal的public接口</li>
<li><strong>该接口的access level</strong>（例如ratate()时ZooAnimal的public member）。<br>每一个执行点，pz指的object类型决定rotate()调用的实例。<font color="red"><strong>类型信息的封装维护于object vptr和vtbl之间的link之中。</strong></font></li>
</ul>
</li>
<li><p><font color="red"><strong>一个pointer或一个reference之所以支持多态，是因为它们不引发内存中任何于类型有关的内存委托操作（type-dependent commitment），会受到改变的只有指向内存的大小和内容解释方式而已。</strong></font>当一个base class object被直接初始化（或被指定为）一个derived class object时，derived object就会被<font color="red">切割</font>（<font color="green">sliced</font>）以塞入较小的base type内存中，derived type将没有留下任何蛛丝马迹，因为配置所得的内存有限。</p>
</li>
<li><p>C++支持具体的ADT程序风格，被称为<font color="red">object-based</font>（<font color="red">OB</font>）。一个OB设计可能比一个对等oo设计速度更快而且空间更紧凑，因为所有函数调用操作在编译时期解析完成，对象构建起来不再设置virtual机制，而且空间紧凑，缺点是OB设计比较没有弹性。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第三章 Data语义学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%B8%89%E7%AB%A0-Data%E8%AF%AD%E4%B9%89%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>一个空的class如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sizeof X == 1</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span> &#125;;                    <span class="comment">// sizeof X == 1</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Y</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123;&#125;;  <span class="comment">// sizeof X == 8</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Z</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123;&#125;;  <span class="comment">// sizeof X == 8</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> :</span> <span class="keyword">public</span> Y, <span class="keyword">public</span> Z &#123;&#125;;<span class="comment">// sizeof X == 12</span></span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Y --- X</span><br><span class="line">Z --- X</span><br><span class="line">A --- Y</span><br><span class="line">A --- Z</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>事实上并不是空的，它有一个隐藏的1byte大小，那是编译器安插进去的一个char</strong></font>。有机器上Y、Z得出大小是8。这个值的大小和机器有关，也和编译器有关：</p>
<ol>
<li><strong>语言本身所造成的额外负担（overhead）</strong>。当语言支持virtual base classes时，会导致额外负担。在derived中，反映在某种形式的<font color="red">指针</font>身上，它或者指向virtual base class subobject，或指向一个相关表格；表格中是virtual base class subobject地址。</li>
<li><strong>编译器对于特殊情况所提供的优化处理</strong>。</li>
<li><strong>Allgnment的限制</strong>。大部分机器结构体大小会收到alignment的限制（内存对齐），使它们能够更有效率在内存中被存取。<font color="red">alignment就是将数值调整到某数的整数倍</font>。</li>
</ol>
</li>
<li><p><font color="red">Empty virtual base class</font>已经成为C++OO设计的一个特有术语了。他提供一个<font color="red">virtual interface</font>，没有定义任何数据。</p>
</li>
<li><p>对于class A竟然大小为12这个结果。记住，<font color="red"><strong>一个virual base class sbobject只会在derived class中存在一份实例</strong></font>，不管它在继承体系中出现多少次。它的大小由以下决定：</p>
<ol>
<li><strong>被大家共享的唯一一个Class X，大小为1byte</strong>。</li>
<li><strong>Base class Y，减去因virtual base class X二配置的大小，结果是4bytes</strong>。</li>
<li><strong>class A自己的大小：0byte</strong>。</li>
<li><strong>class A的alignment数量</strong>。</li>
</ol>
</li>
<li><p>C++对象模型尽量以空间优化和存取速度优化的考虑来表现nonstatic data members，并且保持和C语言struct数据配置的兼容性。至于static data members，则被放置在程序的一个global data segment中，不会影响个别的class object大小。在程序之中，不管class被产生出多少个objects（经由直接产生或间接产生），static data members永远只存在一份实例。但是一个template class的static data members的行为稍有不同。</p>
</li>
<li><p>每一个class object必须有足够的大小容纳所有的nonstatic data members，因为它可能比你想象的还大，原因是：</p>
<ul>
<li><strong>编译器自动加上额外的data members</strong>，用以支持某些特性（virtual）。</li>
<li><strong>因为alignment的需要</strong>。</li>
</ul>
</li>
</ul>
<h4 id="3-1-Data-Member的绑定（The-Binding-of-a-Data-Member）"><a href="#3-1-Data-Member的绑定（The-Binding-of-a-Data-Member）" class="headerlink" title="3.1 Data Member的绑定（The Binding of a Data Member）"></a>3.1 Data Member的绑定（The Binding of a Data Member）</h4><ul>
<li><p>早期C++有两种防御性程序设计风格：</p>
<ol>
<li><p>把所有data members放在class声明处，以确保正确的绑定：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line">    <span class="comment">// ...etc</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>把所有的inline functions，不管大小都放在class声明之外：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">X</span><span class="params">(<span class="keyword">float</span>)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="comment">// ...etc</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">inline</span> <span class="keyword">float</span> Point3d::X() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 但这种<strong>设计风格在C++2.0之后就不存在了</strong>。这个古老的语言规则称为“<font color="red">member rwriting rule</font>”。大概意思是“一个inline函数实体，在整个class声明未被完全看见之前，不会被评估求值（evaluated）的”。也就是说：</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> x;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 对于函数本体的分析延迟，直到class声明右大括号出现才开始</span></span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">()</span> cons </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 分析在这里进行</span></span><br></pre></td></tr></table></figure>

<p> 因此一个inline member function躯体之内的data member绑定操作，会在整个class声明完成之后才发生。但这对member function的argument list并不是真的。argument list中的名称会在第一次遇到的时候被适当resolved完成。因此extern和nested type names之间的非直觉绑定操作还是会发生。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> length;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// length is resolved for global</span></span><br><span class="line">    <span class="comment">// _val is resolved for Point3d::_val</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">mumble</span><span class="params">(length val)</span> </span>&#123; _val = val; &#125;</span><br><span class="line">    <span class="function">length <span class="title">mumble</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _val; &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// length必须在class对他第一个参考操作前被看见</span></span><br><span class="line">    <span class="comment">// 这样的声明使之前操作不合法</span></span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">float</span> length;</span><br><span class="line">    length _val;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p> 上述的语言状况，仍然需要某种防御性程序风格：<font color="red"><strong>请总是把“nested type 声明”放在class的起始处</strong></font>。这样做才能保证非直觉绑定的正确性。</p>
</li>
</ol>
</li>
</ul>
<h4 id="3-2-Data-Member的布局（Data-Member-Layout）"><a href="#3-2-Data-Member的布局（Data-Member-Layout）" class="headerlink" title="3.2 Data Member的布局（Data Member Layout）"></a>3.2 Data Member的布局（Data Member Layout）</h4><ul>
<li><p>已知一组data members：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x;</span><br><span class="line">    staic List&lt;Point3d*&gt; *freeList;</span><br><span class="line">    <span class="keyword">float</span> y;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> chunkSize  <span class="number">250</span>;</span><br><span class="line">    <span class="keyword">float</span> z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>Nonsatic data members在class object中的排列顺序和其被声明的顺序一样</strong>。</font>任何中介的static data members都不会放进对象布局之中。上述例子中，每个Point3d对象是由三个float组成的。static data members存放在程序的<font color="red">data segment</font>中，和个别class objects无关。<br>  C++ Standard要求，在同一个access section（private、public、protected等区段）中，members的排列只需要符合“较晚出现的members在class object中有较高的地址”这一条件就可以了。下面这个template funciton，接受两个data members，然后<strong>判断谁先出现在class object中</strong>。如果两个members都是不同的access sections中的第一个被声明者，函数就会判断哪个section先出现：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">class_type</span>, <span class="title">class</span> <span class="title">data_type1</span>, <span class="title">class</span> <span class="title">data_type2</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">char</span>* <span class="title">access_order</span>(<span class="title">data_type1</span> <span class="title">class_type</span>:</span>:*mem1, data_type2 class_type::*mem2)</span><br><span class="line">&#123;</span><br><span class="line">    assert (mem1 != mem2);</span><br><span class="line">    <span class="keyword">return</span> mem1 &lt; mem2 ? <span class="string">"Member 1 occurs first"</span> : <span class="string">"member 2 occurs first"</span>; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// call</span></span><br><span class="line">access_order(&amp;Point3d::z, &amp;Point3d::y);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-3-Data-Member的存取"><a href="#3-3-Data-Member的存取" class="headerlink" title="3.3 Data Member的存取"></a>3.3 Data Member的存取</h4><ul>
<li><p>已知这段代码：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d origin, *pt = &amp;origin;</span><br><span class="line"><span class="comment">// 存取data members， like this：</span></span><br><span class="line">origin.x = <span class="number">0.0</span>;</span><br><span class="line">pt-&gt;x = <span class="number">0.0</span>;</span><br></pre></td></tr></table></figure>

<p>  通过origin存取和通过pt存取有什么重大差异吗？</p>
</li>
</ul>
<h5 id="Static-Data-Members"><a href="#Static-Data-Members" class="headerlink" title="Static Data Members"></a>Static Data Members</h5><ul>
<li><p><font color="red">Static data members</font>，按字面意义，被编译器提出class之外，并被视为global变量。每个member的存取许可，以及class的关联，都不会招致任何空间上或执行时间上的额外负担。每个static data member只有一个实例。</p>
</li>
<li><p>但如果static data members是一个从<font color="red">复杂继承关系中继承</font>而来的，它仍然只有唯一一个实例，其存取路径仍然那么直接。如果static data member经由函数调用，或其他某些语法存取呢？例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">foobar().chunkSize = <span class="number">250</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可能的转化</span></span><br><span class="line">(<span class="keyword">void</span>) foobar();</span><br><span class="line">Point3d.chunkSize = <span class="number">250</span>;</span><br></pre></td></tr></table></figure>

<p>  若取一个static data member的地址，会得到一个指向其数据类型的指针，而不是指向其class member的指针，因为static member并不内含在一个class object之中：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&amp;Point3d::chunkSize;</span><br><span class="line"><span class="comment">// 会得到如下类型的内存地址</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span>*</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果有两个classes，每个都声明了一个static member freeList，那么当它们放在程序的data segment时，会导致<font color="red">名称冲突</font>。编译器的解决方式是暗中对每个<font color="green">static data member编码</font>（<font color="red"><strong>name-mangling</strong></font>），以获得一个独一无二的程序识别代码。任何name-mangling做法都有两个重点：</p>
<ol>
<li>一个算法，<strong>推导出独一无二的名称</strong>。</li>
<li>万一编译系统（或环境工具）必须和使用者交谈，那些独一无二的名称可以轻易<strong>被推导回到原来的名称</strong>。 </li>
</ol>
</li>
</ul>
<h5 id="Nonstatic-Data-Members"><a href="#Nonstatic-Data-Members" class="headerlink" title="Nonstatic Data Members"></a>Nonstatic Data Members</h5><ul>
<li><p><font color="red">Nonstatic data members</font>直接放在每个class object中。除非经由<font color="red">显式</font>（<font color="green">explcit</font>）或<font color="red">隐式</font>的（<font color="green">implicit</font>），否则没有办法直接存取它们。只要在member funcion中直接处理一个nonstatic data member，implicit class object就会发生。例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d Point3d::translate(<span class="keyword">const</span> Point3d &amp;pt) &#123;</span><br><span class="line">    x += pt.x;</span><br><span class="line">    y += pt.y;</span><br><span class="line">    z += pt.z;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  表面上x、y、z直接存取，事实上是经由implicit class object（由this指针表述）完成的。这个函数的参数是：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// member function的内部转化</span></span><br><span class="line">Point3d Point3d::translate(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, <span class="keyword">const</span> Point3d &amp;pt) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;x += pt.x;</span><br><span class="line">    <span class="keyword">this</span>-&gt;y += pt.y;</span><br><span class="line">    <span class="keyword">this</span>-&gt;z += pt.z;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>欲对一个nonstatic data member进行存取操作，编译器需要把<strong>class object的起始地址加上data member的偏移位置（offset）</strong>。例如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">origin._y = <span class="number">0.0</span>;</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">&amp;origin + (&amp;Point3d::_y - <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>  每一个nonstatic data member的偏移位置（offset）在编译时期即可获知，甚至如果派生自单一或多重继承串链也是一样。</p>
</li>
<li><p>再来看看<font color="red">虚拟继承</font>。虚拟继承将为经由base class subobject存取class members导入一层间接性。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">origin.x = <span class="number">0.0</span>;</span><br><span class="line">pt-&gt;x = <span class="number">0.0</span>;</span><br></pre></td></tr></table></figure>

<p>  从origin存取和从pt存取有什么重大差异？答案是当Pointe3d是一个derived class，而其继承结构中有一个virtual base class，并且被存取member是一个从该virtual base class继承而来的member就会有重大差异。这时候，我们无法在编译时期直到member真正的offset位置。<font color="red">这个存取操作必须延迟到执行期</font>。如果使用origin就不会有这个问题。</p>
</li>
</ul>
<h4 id="3-4-“继承”于Data-Member"><a href="#3-4-“继承”于Data-Member" class="headerlink" title="3.4 “继承”于Data Member"></a>3.4 “继承”于Data Member</h4><ul>
<li><p>如果为2D和3D坐标点提供两个抽象数据类型：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point2d</span><br><span class="line">Point3d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// supporting abstract data types</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// constructor(s)</span></span><br><span class="line">    <span class="comment">// operations</span></span><br><span class="line">    <span class="comment">// access functions</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x, y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// constructor(s)</span></span><br><span class="line">    <span class="comment">// operations</span></span><br><span class="line">    <span class="comment">// access functions</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这和提供两层或三层继承结构，每一层（代表一个维度）是一个class，派生自较低维层次有什么不同？</p>
</li>
</ul>
<h5 id="只要继承不要多态（Inheritance-without-Polymorphism）"><a href="#只要继承不要多态（Inheritance-without-Polymorphism）" class="headerlink" title="只要继承不要多态（Inheritance without Polymorphism）"></a>只要继承不要多态（Inheritance without Polymorphism）</h5><ul>
<li><p>我们可能希望不论是2D或3D坐标点，能共享同一个实例，但又能继续使用于类型性质相关的实例。以上的设计策略。带来的影响则是可以共享数据本身以及数据处理方法，并将它局部化。一般而言，<font color="red">具体继承</font>（<font color="green">concrete inheritance</font>，相对于虚拟继承virtual inheritance）并不会增加空间或存取时间上的额外负担。</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d --- Point2d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">y</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _y; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">x</span><span class="params">(<span class="keyword">float</span> newX)</span> </span>&#123; _x = newX; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">y</span><span class="params">(<span class="keyword">float</span> newY)</span> </span>&#123; _y = newY; &#125;</span><br><span class="line">    <span class="keyword">void</span> <span class="keyword">operator</span>+=(<span class="keyword">const</span> Piont2d &amp;rhs) &#123;</span><br><span class="line">        _x += rhs.x();</span><br><span class="line">        _y += rhs.y();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// inheritance from concrete class</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Point2d(x, y), _z(z) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span> newZ)</span> </span>&#123; _z = newZ; &#125;</span><br><span class="line">    <span class="keyword">void</span> oeprator+=(<span class="keyword">const</span> Point3d &amp;rhs) &#123;</span><br><span class="line">        Point2d::<span class="keyword">operator</span>+=(rhs);</span><br><span class="line">        _z += rhs.z();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这样的设计的好处是可以把管理x和y坐标的程序代码局部化。也表现处两个类之间的紧密关系。但把原本两个独立不相干的classes凑成一堆”type/subtype“，并带有继承关系，会有什么易犯的错误呢？</p>
<ul>
<li><p>经验不足的人可能会重复设计一些相同操作的函数。以例子中的constructor和operator+=为例，它们并没有被做成inline函数。</p>
</li>
<li><p>第二是，把class分解成两层或更多层，可能会为了”表现class继承体系的抽象化“而膨胀所需的空间。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> c1;</span><br><span class="line">    <span class="keyword">char</span> c2;</span><br><span class="line">    <span class="keyword">char</span> c3;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在32位机器中，每个Concrete class object大小是<strong>8bytes</strong>，细分如下:</p>
</li>
</ul>
<ol>
<li><p>val占用4bytes；</p>
</li>
<li><p>c1、c2和c3各占用1bytes；</p>
</li>
<li><p>alignment（调整到word边界）需要1bytes。</p>
<p>现在把Concrete分裂为三层结构：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Concrete2 --- Concrete1</span><br><span class="line">Concrete3 --- Concrete2</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete1</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit1;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete2</span> :</span> <span class="keyword">public</span> Concrete1 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Concrete3</span> :</span> <span class="keyword">public</span> Concrete2 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">char</span> bit3;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>现在Concrete3 object的大小是16bytes，比原先的设计多了100%。（p106）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Concrete2 *pc2;</span><br><span class="line">Concrete1 *pc1_1, *pc1_2;</span><br><span class="line"><span class="comment">// 如果C++把derived class members和Concrete1 subobject捆绑在一起，去除填补空间</span></span><br><span class="line">pc1_1 = pc2;    <span class="comment">// pc1_1指向Concrete2对象</span></span><br><span class="line"><span class="comment">// derived class subobject被覆盖掉</span></span><br><span class="line"><span class="comment">// 于是bit2 member现在有了一个并非预期的数值</span></span><br><span class="line">*pc1_2 = *pc1_1;</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
<h5 id="加上多态（Adding-Polymorphism）"><a href="#加上多态（Adding-Polymorphism）" class="headerlink" title="加上多态（Adding Polymorphism）"></a>加上多态（Adding Polymorphism）</h5><ul>
<li><p>如果在继承关系中提供要给virtual function接口：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;;</span><br><span class="line">    <span class="comment">// 之前的存取操作</span></span><br><span class="line">    <span class="comment">// 加上z的保留空间（目前什么也不做）</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">0.0</span>; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span>)</span> </span>&#123;&#125;</span><br><span class="line">    <span class="comment">// 谁当以下运算符为virtual</span></span><br><span class="line">    <span class="keyword">virtual</span> <span class="keyword">void</span> <span class="keyword">operator</span>+=(<span class="keyword">const</span> Point2d &amp;rhs) &#123;</span><br><span class="line">        _x += rhs.x();</span><br><span class="line">        _y += rhs.y();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line">pblic:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0l</span>0, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Ponit2d(x, y), _z(z) &#123;&#125;;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">z</span><span class="params">(<span class="keyword">float</span> newZ)</span> </span>&#123; _z = newZ; &#125;</span><br><span class="line">    vodi <span class="keyword">operator</span>+=(<span class="keyword">const</span> Piont2d &amp;rhs) &#123;</span><br><span class="line">        Point2d::oeprator+=(rhs);</span><br><span class="line">        _z += rhs.z();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... more members</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这个设计有个好处是可以把operator+=运用在一个Pointe3d对象和一个Point2对象身上：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point2d <span class="title">p2d</span><span class="params">(<span class="number">2.1</span>, <span class="number">2.2</span>)</span></span>;</span><br><span class="line"><span class="function">Point3d <span class="title">p3d</span><span class="params">(<span class="number">3.1</span>,, <span class="number">3.2</span>, <span class="number">3.3</span>)</span></span>;</span><br><span class="line">p3d += p2d;</span><br></pre></td></tr></table></figure>

<p>  虽然class的声明语法没变，但事情不一样了：z() member function和operator+=()运算符都成了虚函数；<font color="red"><strong>每个Point3d class object内含一个额外的vptr member和一个Piont3d virtual table</strong></font>；此外每个virtual member function的调用也复杂了。</p>
</li>
<li><p>C++编辑器领域主要讨论的问题是把vptr放置在class object哪里最好？cfont编译器中，被放在class object的尾端：  </p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">no_virts</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> d1, d2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">has_virts</span> :</span> <span class="keyword">public</span> no_virts &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> d3;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">no_virts *p = <span class="keyword">new</span> has_virts;</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">struct no_virts nv;</th>
<th align="center">class has_virts : <br>public no_virts hv;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">int d1</td>
<td align="center"><strong>int d1</strong></td>
</tr>
<tr>
<td align="center">int d2</td>
<td align="center"><strong>int d2</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">int d3</td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center"><strong>vptr</strong>has_virts</td>
</tr>
<tr>
<td align="center">把vptr放在object尾端，可以保留base class C struct的对象布局，因而允许C程序代码也能使用，这做法出现在C++问世时。</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">到了C++2.0，开始支持<font color="red">虚继承</font>以及<font color="red">抽象基类</font>，某些编译器开始把vptr放到class object前端：</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">struct no_virts nv;</td>
<td align="center">class has_virts : <br>public no_virts hv;</td>
</tr>
<tr>
<td align="center">:-:</td>
<td align="center">:-:</td>
</tr>
<tr>
<td align="center">int d1</td>
<td align="center"><strong>vptr</strong>has_virts</td>
</tr>
<tr>
<td align="center">int d2</td>
<td align="center"><strong>int d1</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center"><strong>int d2</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">int d3</td>
</tr>
<tr>
<td align="center">vptr放在class object前端，对于多继承下通过指向class members的指针调用virtual function会有一些帮助。否则，<strong>不仅从class object起始点开始量起的offset必须在执行器备妥，class vptr之间的offset也必须备妥</strong>。</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">以下是Point2d和Point3d加上了virtual function之后的继承布局（单一继承）：</td>
<td align="center"></td>
</tr>
<tr>
<td align="center">class Point2d p2d;</td>
<td align="center">class Point3d :<br>public Point2d pt3d;</td>
</tr>
<tr>
<td align="center">:-:</td>
<td align="center">:-:</td>
</tr>
<tr>
<td align="center">float _x</td>
<td align="center"><strong>float _x</strong></td>
</tr>
<tr>
<td align="center">float _y</td>
<td align="center"><strong>float _y</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
<td align="center"><strong><strong>vptr</strong>Point2d</strong></td>
</tr>
<tr>
<td align="center">&nbsp;</td>
<td align="center">float _z</td>
</tr>
</tbody></table>
</li>
</ul>
<h5 id="多重继承（Multiple-Inheritance）"><a href="#多重继承（Multiple-Inheritance）" class="headerlink" title="多重继承（Multiple Inheritance）"></a>多重继承（Multiple Inheritance）</h5><ul>
<li><p>单一继承提供了一种<font color="red">自然多态</font>（<font color="green">natural polymorphism</font>）形式，是关于base type和derived type之间的转换。base 它们的object都是从相同的地址开始。差异只在<strong>derived object比较大，用以容纳它自己的nonstatic data members</strong>。一个derived class指定给base class的指针或reference。这个操作并不需要编译器调停或修改地址。可以很自然地发生，而且提供了最佳执行效率。</p>
</li>
<li><p>多重继承不像单一继承，也不容易模塑处模型。它的复杂度在于derived class和其base class之间的非自然关系：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d --- Point2d </span><br><span class="line">Vertex3d --- Point3d</span><br><span class="line">Vertex3d --- Vertex</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...有virtual接口，会有vptr</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... vptr</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Vertex *next</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex3d</span> :</span> <span class="keyword">public</span> Point3d, <span class="keyword">public</span> Vertex &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  多重继承的问题主要发生在derived class objects和后继base class objects之间的转换，不论是：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">(<span class="keyword">const</span> Vertex&amp;)</span></span>;</span><br><span class="line">Vertex3d v;</span><br><span class="line">...</span><br><span class="line">mumble(v); <span class="comment">// 不自然</span></span><br></pre></td></tr></table></figure>

<p>  或是经由所支持的virtual function机制做转换。多继承对象符出的成本在于地址的指定操作而已：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Vertex3d v3d;</span><br><span class="line">Vertex *pv;</span><br><span class="line">Point2d *p2d;</span><br><span class="line">Point3d *p3d;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指定操作</span></span><br><span class="line">pv = &amp;v3d;</span><br><span class="line"><span class="comment">// 需要内部转化</span></span><br><span class="line">pv = (Vertex*)(((<span class="keyword">char</span>*)&amp;v3d) + <span class="keyword">sizeof</span>(Point3d));</span><br><span class="line"><span class="comment">// 如果v3d为指针类型，即pv3d，则:</span></span><br><span class="line">pv = pv3d ? (Vertex*)((<span class="keyword">char</span>*)pv3d) + <span class="keyword">sizeof</span>(Point3d) : <span class="number">0</span>; <span class="comment">// 防止pv3d为0</span></span><br></pre></td></tr></table></figure>

<p>  以下为<font color="red">多重继承</font>（<font color="green">Multiple Inheritance</font>)：</p>
<table>
<thead>
<tr>
<th align="center">class Point2d pt2d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">class Point3d :<br>public Point2d pt3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">Vertex v;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="center">class Vertex3d :<br> public Point3d,<br>public Vertex<br>{}v3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
<tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
<tr>
<td align="center">float mumble</td>
</tr>
</tbody></table>
</li>
</ul>
<h5 id="虚拟继承（Virtual-Inheritance）"><a href="#虚拟继承（Virtual-Inheritance）" class="headerlink" title="虚拟继承（Virtual Inheritance）"></a>虚拟继承（Virtual Inheritance）</h5><ul>
<li><p>多重继承的一个语意上的副作用是，<font color="red"><strong>必须支持某种形式上的”shared subobject继承“</strong></font>。如早期的iostream library：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pre-standard iostream implementation</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ios</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">istream</span> :</span> <span class="keyword">public</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ostream</span> :</span> <span class="keyword">public</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">iostream</span> :</span> <span class="keyword">public</span> istream, <span class="keyword">public</span> ostream &#123; ... &#125;;</span><br></pre></td></tr></table></figure>

<p>  下图可表现iostream的继承体系图。第一个为多重继承，第二个为虚拟多重继承：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">istream --- ios1[ios]</span><br><span class="line">ostream --- ios2[ios]</span><br><span class="line">iostream --- istream</span><br><span class="line">iostream --- ostream</span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">istream --- ios</span><br><span class="line">ostream --- ios</span><br><span class="line">iostream --- istream</span><br><span class="line">iostream --- ostream</span><br></pre></td></tr></table></figure>

<p>  在iostream对象布局中，只需要一份就好：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ios</span> &#123;</span> ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">istream</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ostream</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ios &#123; ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">iostream</span> :</span> <span class="keyword">public</span> istream, <span class="keyword">public</span> ostream &#123; ... &#125;;</span><br></pre></td></tr></table></figure>

<p>  上述iostream的实现挑战在于：<strong>一个有效的方法，将istream和ostream各自维护ios subobjet，折叠成由iostream维护的单一ios subobject，并且保存base class和derived class的指针（reference）之间的<font color="red">多态指定操作</font>（<font color="green">polymorphism assignments</font>)</strong>。</p>
</li>
<li><p>一般的实现方法是。Class如果内含一个或多个virtual base class subobjects，像istream那样，将被分割两部分：<font color="red"><strong>一个不变区域</strong></font>和<font color="red"><strong>一个共享区域</strong></font>。不变区域中的数据，总是有<strong>固定的offset</strong>，所以这里可以直接存取。而共享区域，就是virtual base class subobject。它们只能被间接存取。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Vertex *next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vertex3d</span> :</span> <span class="keyword">public</span> Vertex, <span class="keyword">public</span> Point3d &#123;</span><br><span class="line"><span class="keyword">public</span> :</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Vertex -- _x,_y--- Point2d</span><br><span class="line">Point3d -- _z --- Point2d</span><br><span class="line">Vertex3d -- next --- Vertex</span><br><span class="line">Vertex3d -- _z --- Point3d</span><br><span class="line">none[ ] -- mumble --- Vertex3d</span><br></pre></td></tr></table></figure>

<p>  这中间存在一个问题：如何能够存取class共享部分呢？cfont编译器是在derived class object中插指针，每个指向virtual base class。需要完成操作都是通过指针间接完成的。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> Point3d::<span class="keyword">operator</span>+=(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    _X += rhs._x;</span><br><span class="line">    _y += rhs._y;</span><br><span class="line">    _z += rhs._z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转化为虚构代码vbc为virtual base class</span></span><br><span class="line">__vbcPoint2d-&gt;_x += rhs.__vbcPoint2d-&gt;_x;</span><br><span class="line">__vbcPoint2d-&gt;_y += rhs.__vbcPoint2d-&gt;_y;</span><br><span class="line">_z += rhs._z;</span><br></pre></td></tr></table></figure>

<p>  这样实现模型有两个主要缺点：</p>
<ol>
<li><strong>每个对象针对其每个virtual base class背负一个额外的指针</strong>。而我们希望class object负担是稳定的。</li>
<li><strong>虚继承链加长会导致存取层次增加</strong>。我们呢希望有着固定的存取时间。</li>
</ol>
</li>
<li><p>对于第二个问题。MetaWare和其他编译器到今天还是用cfont原始模型：它们经由拷贝操作取得所有nested virtual base class指针，放到derived class object中，虽然有空间代价。</p>
</li>
<li><p>至于第一个问题，有两个解决方法。Microsoft编译器引入virtual base class table。每个class object如果有一个以上virual base classes，编译器就安插指针，指向virtual base class table，真正的vptr则被放在该表格中。第二个解决方法，是在virtual function table中放置virtual base class的offset（而非地址）。</p>
<table>
<thead>
<tr>
<th align="center">class Vertex3d :<br>public Vertex,<br>public Point3d<br>{…} v3d;</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Vertex *next</td>
</tr>
<tr>
<td align="center"><strong>Point2d *pPoint2d</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Vertex</td>
</tr>
<tr>
<td align="center">float _z</td>
</tr>
<tr>
<td align="center"><strong>Point2d *pPoint2d</strong></td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point3d</td>
</tr>
<tr>
<td align="center">float mumble</td>
</tr>
<tr>
<td align="center">float _x</td>
</tr>
<tr>
<td align="center">float _y</td>
</tr>
<tr>
<td align="center"><strong>vptr</strong>Point2d</td>
</tr>
<tr>
<td align="center">该方法把<font color="red">virtual base class offset</font>和<font color="red">virtual function entires</font>混杂在一起。Sum编译器中，virtual function table由正值或负值来索引。正值则索引到virtual functions；负值则索引到virtual base class offsets。</td>
</tr>
<tr>
<td align="center"><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">this</span> + __vptr__Point3d[<span class="number">-1</span>])-&gt;_x +=</span><br><span class="line">    (&amp;rhs + rhs.__vptr__Point3d[<span class="number">-1</span>])-&gt;_x;</span><br><span class="line">(<span class="keyword">this</span> + __vptr__Point3d[<span class="number">-1</span>])-&gt;_y += </span><br><span class="line">    (&amp;rhs + rhs.__vptr__Point3d[<span class="number">-1</span>])-&gt;_y;</span><br><span class="line">_ += rhs._z;</span><br></pre></td></tr></table></figure></td>
</tr>
</tbody></table>
<p>  因此Derived class实例和base class实例之间的转换操作为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point2d *p2d = pv3d;</span><br><span class="line"><span class="comment">// translation</span></span><br><span class="line">Point2d *pt2 = pv3d ? pv3d + pv3d-&gt;__vptr__Point3d[<span class="number">-1</span>] : <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-5-对象成员的效率（Object-Member-Efficiency）"><a href="#3-5-对象成员的效率（Object-Member-Efficiency）" class="headerlink" title="3.5 对象成员的效率（Object Member Efficiency）"></a>3.5 对象成员的效率（Object Member Efficiency）</h4><ul>
<li>下面测试旨在测试<font color="red">聚合</font>（<font color="green">aggregation</font>）、<font color="red">封装</font>（<font color="green">encapsulation</font>）以及<font color="red">继承</font>（<font color="green">inheritance</font>）所引发的额外负荷程序。跳过。</li>
</ul>
<h4 id="3-6-指向Data-Members的指针（Poniter-to-Data-Members）"><a href="#3-6-指向Data-Members的指针（Poniter-to-Data-Members）" class="headerlink" title="3.6 指向Data Members的指针（Poniter to Data Members）"></a>3.6 指向Data Members的指针（Poniter to Data Members）</h4><ul>
<li><p>考虑下面Point3d声明。有一个virtual function，一个static data member，以及三个坐标：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Point3d();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">static</span> Point3d origin;</span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  每个Point3d object含有三个坐标值，依序为x、y、z，以及vptr。static data member origin放在class object之外。vptr的位置根据编译器不同而不同。不是放头就是尾。<code>&amp;Point3d::z;</code>取得某个坐标成员的地址代表什么呢？<strong>代表着z坐标在class object中偏移位置（offset）</strong>。</p>
</li>
<li><p>一台32位机器上，每一个float是4bytes，所以期望获取地址偏移位置要么是8要么是12。然而获取的总是多1，也就是9和13（我的输出并没有+1）。</p>
</li>
<li><p>在多继承下，要将第二个或后继base class的指针，和一个与derived class object绑定的member结合起来，那么将会因为需要加入offset值而变得复杂。例如:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Base1</span> &#123;</span> <span class="keyword">int</span> val1; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Base2</span> &#123;</span> <span class="keyword">int</span> val2; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Derived</span> :</span> Base1, Base2 &#123; ... &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func1</span><span class="params">(<span class="keyword">int</span> Derived::*dmp, Derived *pd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 期望第一个参数是一个derived class的member指针，结果是base class的会怎样。</span></span><br><span class="line">    pd-&gt;*dmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func2</span><span class="params">(Derived *pd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// bmp将成为1</span></span><br><span class="line">    <span class="keyword">int</span> Base2::*bmp = &amp;Base2::val2;</span><br><span class="line">    <span class="comment">// bmp == 1但在Derived中val2 == 5</span></span><br><span class="line">    func1(bmp, pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  要解决这个问题，必须：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 经由编译器内部转换</span></span><br><span class="line">func1(bmp ? bmp + <span class="keyword">sizeof</span>(Base1) : <span class="number">0</span>, pd);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="“指向Members的指针”的效率问题"><a href="#“指向Members的指针”的效率问题" class="headerlink" title="“指向Members的指针”的效率问题"></a>“指向Members的指针”的效率问题</h5><ul>
<li>下面是测试数据。了解在3D坐标点的不同class表现方式下指向members的指针所带来的影响。</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>pthread_N_IPC </title>
    <url>/2019/06/30/pthread-N-IPC/</url>
    <content><![CDATA[<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// IEEE POSIX头文件</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;    // POSIX线程</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;  // 信号量</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/sem.h&gt;    // 有名信号量(system v)</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;   // 进程控制</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;     // 符号常量</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;  // 基本系统数据类型</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;      // 文件控制</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/stat.h&gt;   // 文件状态</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/ipc.h&gt;    // IPC</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/shm.h&gt;    // XSI共享存储</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;    // XSI 消息队列</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ISO C</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;     // 验证程序断言</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;      // 出错码</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;      // 标准I/O库</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;     // 实用函数</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;     // 信号</span></span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// I/O</span></span><br><span class="line"></span><br><span class="line">STDIN_FILENO <span class="comment">// fd 0</span></span><br><span class="line">STDOUT_FILENO<span class="comment">// fd 1</span></span><br><span class="line">STDERR_FILENO<span class="comment">// fd 2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// read more to "Chapter_three.md"</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建线程标识</span></span><br><span class="line"><span class="keyword">pthread_t</span> thread_id;</span><br><span class="line"><span class="comment">// 创建互斥量</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> mutex = PTHREAD_MUTEX_INITIALIZER; <span class="comment">// static macro init</span></span><br><span class="line"><span class="comment">// 创建信号量</span></span><br><span class="line"><span class="keyword">sem_t</span> sem;</span><br><span class="line"><span class="comment">// 创建条件变量</span></span><br><span class="line"><span class="keyword">pthread_cond_t</span> cond = PTHREAD_COND_INITIALIZER <span class="comment">// static macro init</span></span><br><span class="line"><span class="comment">// 创建消息队列</span></span><br><span class="line"><span class="keyword">mqd_t</span> mqd;</span><br><span class="line"><span class="comment">// 创建读写锁</span></span><br><span class="line"><span class="keyword">pthread_rwlock_t</span> rwlock;</span><br><span class="line"><span class="comment">// 创建自旋锁</span></span><br><span class="line"><span class="keyword">pthread_spinlock_t</span> spinlock;</span><br><span class="line"><span class="comment">// 创建屏障</span></span><br><span class="line"><span class="keyword">pthread_barrier_t</span> barrier;</span><br><span class="line"><span class="comment">// 创建文件锁对象</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">flck</span>;</span></span><br><span class="line"><span class="comment">// 创建共享内存指针</span></span><br><span class="line"><span class="keyword">caddr_t</span> memptr;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性</span></span><br><span class="line"><span class="keyword">pthread_attr_t</span> pat;</span><br><span class="line"><span class="comment">// 互斥量属性</span></span><br><span class="line"><span class="keyword">pthread_mutexattr_t</span> pmt;</span><br><span class="line"><span class="comment">// 读写锁属性(唯一属性是进程共享属性)</span></span><br><span class="line"><span class="keyword">pthread_rwlockattr_t</span> prt;</span><br><span class="line"><span class="comment">// 条件变量属性</span></span><br><span class="line"><span class="keyword">pthread_condattr_t</span> pct;</span><br><span class="line"><span class="comment">// 栅栏属性(目前只有进程共享属性)</span></span><br><span class="line"><span class="keyword">pthread_barrierattr_t</span> pbt;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 线程</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_create</span><span class="params">(<span class="keyword">pthread_t</span> *thread,      </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> <span class="keyword">pthread_att_t</span> *attr,    </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> *(*start_routine)(<span class="keyword">void</span> *),</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> *arg)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_create(thread_id, NULL, (void *)func, (void *)&amp;arg);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 加入线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_join</span><span class="params">(<span class="keyword">pthread_t</span> thread, </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> **retval)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. error = pthread_join(pid, (void *)&amp;err_func);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程ID的比较</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_equal</span><span class="params">(<span class="keyword">pthread_t</span> t1, <span class="keyword">pthread_t</span> t2)</span></span>;</span><br><span class="line"><span class="comment">// success return non-zero value; otherwise, return 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程的取消</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cancel</span><span class="params">(<span class="keyword">pthread_t</span> thread)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; othwrwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 分离线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_detach</span><span class="params">(<span class="keyword">pthread_t</span> thread)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, reutrn error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 退出线程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pthread_exit</span><span class="params">(<span class="keyword">void</span> *retval)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号给线程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_kill</span><span class="params">(<span class="keyword">pthread_t</span> thread, <span class="keyword">int</span> sig)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number and no signal to send</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取得目前线程标识</span></span><br><span class="line"><span class="keyword">pthread_t</span> pthread_self(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// always succedds, return the calling thread's ID</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程让步</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_yield</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, reutrn error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// pthread接口允许通过设置每个对象关联的不同属性来细调线程和同步对象的行为</span></span><br><span class="line"><span class="comment">// e.g. 线程栈:虚地址空间大小是固定的,因为进程中只有一个栈.而它被所有线程栈共享.</span></span><br><span class="line"><span class="comment">// e.g. 如果许多线程的线程栈累计超过可用的虚地址空间,就要减少栈大小.如果调用函数涉及很深的栈帧就要调大</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_init</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程属性销毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_destroy</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 注册fork句柄??</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_atfork</span><span class="params">(<span class="keyword">void</span> (*prepare)(<span class="keyword">void</span>), </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> (*parent)(<span class="keyword">void</span>),</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">void</span> (*child)(<span class="keyword">void</span>))</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"><span class="comment">// e.g. 可以用来解决线程与fork的问题,比如用来清除锁状态,通过调用此函数建立fork处理程序</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈最小长度(字节数)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setstacksize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> stacksize)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getstacksize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> *stacksize)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈的分离状态属性</span></span><br><span class="line"><span class="comment">// PTHREAD_CREATE_JOINABLE(非分离状态) | PTHREAD_CREATE_JOINABLE(分离状态)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setdetachstate</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">int</span> detachstate)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getdetachstate</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">int</span> *detachstate)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 非分离线程在终止后,必须有一个线程用join来等待他,否则会资源泄露</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈末尾的境界缓冲区大小(字节数)</span></span><br><span class="line"><span class="comment">// e.g. guardsize的值向上舍入为PAGESIZE的倍数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setguardsize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> guardsize)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getguardsize</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">size_t</span> *guardsize)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 溢出保护会浪费系统资源,如果创建大量线程,并已知线程不会溢出,那么关闭溢出保护区可以节省系统资源</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程栈的最低地址</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_setstackaddr</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">void</span> *stackaddr)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_attr_getstackaddr</span><span class="params">(<span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">void</span> **stackaddr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error num</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥引用对象的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_init</span><span class="params">(<span class="keyword">pthraed_mutex_t</span> *<span class="keyword">restrict</span> mutex,</span></span></span><br><span class="line"><span class="function"><span class="params">                    <span class="keyword">const</span> <span class="keyword">pthread_mutexattr_t</span> *<span class="keyword">restrict</span> attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthraed_mutex_init(&amp;mutex, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥引用对象的销毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_destroy</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量上锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_lock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_trylock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_unlock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 互斥量属性初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutexattr_init</span><span class="params">(<span class="keyword">pthread_mutexattr_t</span> *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 如果进程共享互斥量属性设置位PTHREAD_PROCESS_SHARED,多个进程之间的内存数据块互斥量就可以用于进程同步</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁也叫共享互斥锁.当读写锁是写加锁状态时,解锁前,所有试图对这个锁加锁的线程都会被阻塞.当读写锁是读加锁状态,所有试图读模式对它加锁的线程都可以得到访问权.</span></span><br><span class="line"><span class="comment">// e.g. 读写锁使用前必须初始化,释放内存前必须销毁</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_init</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> <span class="keyword">pthread_rwlockattr_t</span> *<span class="keyword">restrict</span> attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_rwlock_init(&amp;rwlock, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_destroy</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 锁上写锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_trywrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_wrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_timedwrlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, </span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 带超时的锁获取锁失败将返回TIMEOUT</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 锁上读锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_rdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_tryrdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_timerdlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock,</span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 带超时的锁获取锁失败将返回TIMEOUT</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读写锁解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_unlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程等待条件</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *<span class="keyword">restrict</span> cond,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_timedwait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *<span class="keyword">restrict</span> cond,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">pthread_mutex_t</span> *<span class="keyword">restrict</span> mutex,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> struct timespec *<span class="keyword">restrict</span> abstime)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_cond_wait(&amp;cond, &amp;mutex);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 单独发送condition</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 广播发送condition</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_broadcast</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁跟互斥锁很相似,都是为了解决资源互斥使用.区别是互斥锁相当于只有0/1状态的信号量,当资源被占用,资源申请者将被阻塞,而自旋锁不会引起睡眠,而是不断循环空转</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁的初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_init</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock, <span class="keyword">int</span> pshared)</span></span>;    </span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 单进程可以设置pshared参数为PTHREAD_PROCESS_SHARED</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁加锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_lock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_trylock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁解锁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_unlock</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 自旋锁摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_spin_destroy</span><span class="params">(<span class="keyword">pthread_spinlock_t</span> *lock)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障(barrier)是用户协调多个线程并行工作的同步机制.屏障允许每个线程等待,直到所有合作线程都达到某一点,然后从该点继续执行. (fence | 栅栏?)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障对象初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_init</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *<span class="keyword">restrict</span> barrier,</span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">const</span> <span class="keyword">pthread_barrierattr_t</span>   *<span class="keyword">restrict</span> attr, </span></span></span><br><span class="line"><span class="function"><span class="params">                        <span class="keyword">unsigned</span> count)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. pthread_barrier_init(&amp;barrier, NULL, thread_num);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屏障对象摧毁</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_destroy</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return error number</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 在栅栏中同步</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_wait</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br><span class="line"><span class="comment">// success return PTHREAD_BARRIER_SERIAL_THREAD or 0; otherwise, return error number</span></span><br><span class="line"><span class="comment">// e.g. 当调用pthraed_barrier_wait的线程在屏障技术count未满足条件时,进入休眠,直到满足所有吸线程被唤醒</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 基本类型</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 进程或进程组ID</span></span><br><span class="line"><span class="keyword">pid_t</span> pid;</span><br><span class="line"><span class="comment">// 对象(如字符串)长度(不带符号的)</span></span><br><span class="line"><span class="keyword">size_t</span> st;</span><br><span class="line"><span class="comment">// 返回字节计数的函数(带符号的)</span></span><br><span class="line"><span class="keyword">ssize_t</span> sst;</span><br><span class="line"><span class="comment">//</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="center">ELF文件信息</th>
</tr>
</thead>
<tbody><tr>
<td align="center">ELF头</td>
</tr>
<tr>
<td align="center">程序头表</td>
</tr>
<tr>
<td align="center">.init 节</td>
</tr>
<tr>
<td align="center">.text 节</td>
</tr>
<tr>
<td align="center">.rodata 节</td>
</tr>
<tr>
<td align="center">.data 节</td>
</tr>
<tr>
<td align="center">.bss 节</td>
</tr>
<tr>
<td align="center">.symtab 节</td>
</tr>
<tr>
<td align="center">.debug 节</td>
</tr>
<tr>
<td align="center">.line 节</td>
</tr>
<tr>
<td align="center">.strtab 节</td>
</tr>
<tr>
<td align="center">节头表</td>
</tr>
<tr>
<td align="center"><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 进程</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 概念</span></span><br><span class="line"><span class="comment">// 孤儿进程:fork编程时,对于父进程已经终止的所有进程,它们的父进程都改变为init进程</span></span><br><span class="line"><span class="comment">// 僵尸进程:fork编程时,如果子进程先于父进程退出,就会处于终止状态,kernel为每个子进程保留了一定的信息(进程ID\终止状态\使用CPU事件总量等).</span></span><br><span class="line"><span class="comment">//      当父进程调用wait或waitip时得到这些信息,并将终止子进程从系统进程表删除,释放终止状态.如果没有获取子进程终止状态,进程表信息不会被移除,那么这些进程就会变成进程表中的僵尸进程.</span></span><br><span class="line"><span class="comment">// 预防措施有两大类型:</span></span><br><span class="line"><span class="comment">// 第一种是让父进程去通知系统,告诉系统它对子进程的终止没有任何兴趣:</span></span><br><span class="line">signal(SIGCHLD, SIG_IGN);   <span class="comment">// in parent: ignore notification</span></span><br><span class="line"><span class="comment">// 第二种是在子进程终止时,让父进程执行一个wait.等待子进程终止</span></span><br><span class="line">wait(<span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure></td>
</tr>
</tbody></table>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取得目前进程标识</span></span><br><span class="line"><span class="keyword">pid_t</span> getpid(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// 取得目前进程父进程标识</span></span><br><span class="line"><span class="keyword">pid_t</span> getppid(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">// 从已存在的进程中创建新进程</span></span><br><span class="line"><span class="keyword">pid_t</span> fort(<span class="keyword">void</span>);</span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">    On success, the PID of the child process is returned  in  the  parent, </span></span><br><span class="line"><span class="comment">    and  0  is returned  in the child.  </span></span><br><span class="line"><span class="comment">    On failure, -1 is returned in the parent,</span></span><br><span class="line"><span class="comment">    no child process is created, and errno is set appropriately.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行程序</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[], <span class="keyword">char</span> *envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execl</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ...)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execve</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">char</span> <span class="keyword">const</span> *arg[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execle</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ..., <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execv</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">char</span> *<span class="keyword">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execlp</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">const</span> <span class="keyword">char</span> *arg, ...)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execvp</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">char</span> *<span class="keyword">const</span> argv[])</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execvpe</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *file, <span class="keyword">char</span> *<span class="keyword">const</span> argv[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br><span class="line"><span class="comment">// error return -1</span></span><br><span class="line"><span class="comment">// e.g. execve函数的功能是在当前进程上下文中加载并运行一个新程序</span></span><br><span class="line"><span class="comment">// p=filename | l=list | v=vector | e=environment</span></span><br><span class="line"><span class="comment">// e.g. execlp("ps", "ps", "-ef", NULL); // artv[0] = ps</span></span><br><span class="line"><span class="comment">/* e.g. </span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">    char *argv[] = &#123;"ps", "-ef", NULL);</span></span><br><span class="line"><span class="comment">    execvp("ps", argv);</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 终止进程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">exit</span><span class="params">(<span class="keyword">int</span> status)</span></span>;</span><br><span class="line"><span class="keyword">void</span> _exit(<span class="keyword">int</span> status);</span><br><span class="line"><span class="comment">// e.g. exit()与_exit()的区别在于,前者在调用exit系统调用前,还会调用退出处理函数和清除I/O缓冲</span></span><br><span class="line"><span class="comment">// e.g. 可以在父进程中利用wait(&amp;status)来提取执行完的终止状态status并分析</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待子进程停止或中断</span></span><br><span class="line"><span class="keyword">pid_t</span> wait(<span class="keyword">int</span> *stat_loc);</span><br><span class="line"><span class="keyword">pid_t</span> waitpid(<span class="keyword">pid_t</span> pid, </span><br><span class="line">            <span class="keyword">int</span> *stat_loc, </span><br><span class="line">            <span class="keyword">int</span> options);   </span><br><span class="line"><span class="comment">// using WNOHANG and no sub process exit return 0, otherwise, success return &gt;0 and error return -1</span></span><br><span class="line"><span class="comment">// e.g. pr = waitpid(status, NULL, WNOHANG);</span></span><br><span class="line"><span class="comment">// WNOHANG: 如果没有结束的子进程马上返回,不等待;</span></span><br><span class="line"><span class="comment">// WUNTRACED: 如果子进程暂行则返回,结束状态不理会</span></span><br><span class="line"><span class="comment">// 0: 同wait(),阻塞父进程,直到退出</span></span><br><span class="line"><span class="comment">// wait macros evaluate</span></span><br><span class="line">WIFEXITED(stat_val);    <span class="comment">// wait if exited </span></span><br><span class="line"><span class="comment">// evaluate to a non-zero for a child process</span></span><br><span class="line">WEXITSTATUS(stat_val);  <span class="comment">// wait exit status</span></span><br><span class="line"><span class="comment">// if WIFEXITED is non-zero, evaluate to the status argument that the child process passed to _exit() or exit() or return from main().</span></span><br><span class="line"><span class="comment">// e.g. if (WIFEXITED(status)) status = WEXITSTATUS(status);</span></span><br><span class="line">WIFSIGNALED(stat_val);  <span class="comment">// wait if signaled</span></span><br><span class="line">WTERMSIG(stat_val);     <span class="comment">// evaluate to the wait WIFISNALED number of the signal</span></span><br><span class="line">WIFSTOPPED(stat_val);   <span class="comment">// wait if stopped</span></span><br><span class="line">WSTOPSIG(stat_val);     <span class="comment">// evaluate to the WIFSTOPPED nmber of the signal</span></span><br><span class="line">WIFCONTINUED(stat_val); <span class="comment">// wait if continued, evaluate to a non-zero value if status has continued from a job control stop.</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 进程间通道</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建管道</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pipe</span><span class="params">(<span class="keyword">int</span> fildes[<span class="number">2</span>])</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. 管道主要是用于不同进程间的通信,通常是先创建管道再fork一个子进程,因为子进程会继承父进程创建的管道</span></span><br><span class="line"><span class="comment">// e.g. 管道是单工工作方式,要么读要么写.虽然父子进程都有读写端,但只能使用一个,因此需要把不适用的读或写端文件描述符关闭.</span></span><br><span class="line"><span class="comment">// e.g. pipe读端存在时,写入数据才有意义,否则向pipe写入数据的进程会受到kernel传来的SIGPIPE signal(Broken Pipea error norrmally)</span></span><br><span class="line"><span class="comment">// e.g. 写数据时,不保证写入原子性,只要缓冲区有空间,就会尝试写,如果缓冲区满,写操作就会阻塞</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建FIFO有名管道</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mkfifo</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *pathname, <span class="keyword">mode_t</span> mode)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. mkfifo("fifofile", 0644);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kill</span><span class="params">(<span class="keyword">pid_t</span> pid, <span class="keyword">int</span> sig)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">raise</span><span class="params">(<span class="keyword">int</span> sig)</span></span>; <span class="comment">// 向自身进程发起 相当于kill(getpid(), sig);</span></span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line">pid = <span class="number">0</span>+;   <span class="comment">// 发送信号给pid</span></span><br><span class="line">pid = <span class="number">0</span>;    <span class="comment">// 发送信号给当前进程同进程组的进程</span></span><br><span class="line">pid = <span class="number">-1</span>;   <span class="comment">// 发送信号给所有进程表中的进程</span></span><br><span class="line">pid = <span class="number">-1</span>-   <span class="comment">// 发送信号给进程组号为-pid的每个进程</span></span><br><span class="line">SIGINT      <span class="comment">// Ctrl + c</span></span><br><span class="line">SIGQUIT     <span class="comment">// Ctrl + \</span></span><br><span class="line">SIGKILL     <span class="comment">// 结束程序不能阻塞、处理和忽略</span></span><br><span class="line">SIGALARM    <span class="comment">// 当定时器到时发出</span></span><br><span class="line">SIGSTOP     <span class="comment">// 暂停进程,同SIGKILL不能忽视</span></span><br><span class="line">SIGTSTP     <span class="comment">// Ctrl + z</span></span><br><span class="line"><span class="comment">// man 7 signal</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置一个定时器</span></span><br><span class="line"><span class="keyword">unsigned</span> alarm(<span class="keyword">unsigned</span> seconds);</span><br><span class="line"><span class="comment">// success return SIGALRM signal. otherwise, return 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 挂起进程</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pause</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送信号至处理函数</span></span><br><span class="line"><span class="keyword">void</span> (*signal(<span class="keyword">int</span> sig, <span class="keyword">void</span> (*func)(<span class="keyword">int</span>)))(<span class="keyword">int</span>);</span><br><span class="line"><span class="comment">// e.g. void exec_handler(int signo);</span></span><br><span class="line"><span class="comment">// e.g. signal(SIGINT, exec_handler); // 通过signo来判断状态</span></span><br><span class="line"><span class="comment">// e.g. signal(SIGTSTP, SIG_IGN); // 忽略改信号</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查或修改信号的处理动作(action)</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sigaction</span><span class="params">(<span class="keyword">int</span> sig, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">const</span> struct sigaction *<span class="keyword">restrict</span> act,</span></span></span><br><span class="line"><span class="function"><span class="params">              struct sigaction *<span class="keyword">restrict</span> oact)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise return -1 and set errno</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// POSIX IPC</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mq_attr</span> &#123;</span></span><br><span class="line">	<span class="keyword">long</span> mq_flags;       <span class="comment">/* Flags: 0 or O_NONBLOCK */</span></span><br><span class="line">	<span class="keyword">long</span> mq_maxmsg;      <span class="comment">/* Max. # of messages on queue */</span></span><br><span class="line">	<span class="keyword">long</span> mq_msgsize;     <span class="comment">/* Max. message size (bytes) */</span></span><br><span class="line">	<span class="keyword">long</span> mq_curmsgs;     <span class="comment">/* # of messages currently in queue */</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 共享文件</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// flock结构体的初始化 e.g. </span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">flock</span> <span class="title">lock</span>;</span>          </span><br><span class="line">lock.l_type = F_WRLCK;      <span class="comment">// read/write lock</span></span><br><span class="line">lock.l_whence = SEEK_SET;   <span class="comment">// base for seek offsets</span></span><br><span class="line">lock.l_start = <span class="number">0</span>;           </span><br><span class="line">lock.l_len = <span class="number">0</span>;             <span class="comment">// 0 here means until EOF</span></span><br><span class="line">lock.l_pid;                 <span class="comment">// proces id </span></span><br><span class="line"><span class="comment">// 文件控制</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fcntl</span><span class="params">(<span class="keyword">int</span> fildes, <span class="keyword">int</span> cmd, ...)</span></span>;</span><br><span class="line"><span class="comment">// Upon successful completion, the value returned shall depend on cmd as follows</span></span><br><span class="line"><span class="comment">// e.g. struct flock lock; fcntl(fd, F_SETLK, &amp;lock);</span></span><br><span class="line">F_DUPFD     <span class="comment">// return a new file descriptor</span></span><br><span class="line">F_GETFD     <span class="comment">// get the file descriptor flags</span></span><br><span class="line">F_SETFD     <span class="comment">// set the file descriptor flags</span></span><br><span class="line">F_GETFL     <span class="comment">// get the file status flags and file access modes</span></span><br><span class="line">F_SETFL     <span class="comment">// set the file status flags</span></span><br><span class="line">F_GETOWN    <span class="comment">// if fildes refers to a socket, get the process or process group ID specified to receive SIGURG signals when out-of-band data is available  </span></span><br><span class="line">F_SETOWN    <span class="comment">// set</span></span><br><span class="line">F_GETLK     <span class="comment">// get the lock bolcks description point to by the third argument</span></span><br><span class="line">F_SETLK     <span class="comment">// set or clear file segment </span></span><br><span class="line">F_SETLKW    <span class="comment">// wait setlk</span></span><br><span class="line">F_UNLCK     </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// message queue</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mqueue.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开消息队列</span></span><br><span class="line"><span class="keyword">mqd_t</span> mq_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag);</span><br><span class="line"><span class="keyword">mqd_t</span> mq_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, </span><br><span class="line">            <span class="keyword">mode_t</span> mode, struct mq_attr *attr);</span><br><span class="line"><span class="comment">// success return message queue descriptor. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mqd_t mqd = mq_open("/temp.mq", O_RDWR);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 关闭消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_close</span><span class="params">(<span class="keyword">mqd_t</span> mqdes)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置消息队列属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_setattr</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, struct mq_attr *newattr, struct mq_attr *oldattr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_getattr("/temp.mq", &amp;attr);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取消息队列属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_getattr</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, struct mq_attr *attr)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送消息到消息队列</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_send</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> <span class="keyword">char</span> *msg_ptr,</span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> msg_prio)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_timedsend</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> <span class="keyword">char</span> *msg_ptr,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> msg_prio,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> struct timespec *abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_send的prio优先级必须小于MQ_PRIO_MAX</span></span><br><span class="line"><span class="comment">// e.g. mq_send("/temp.mq", "hello", 5, 1);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收消息从消息队列</span></span><br><span class="line"><span class="keyword">ssize_t</span> mq_receive(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">char</span> *msg_ptr,</span><br><span class="line">                <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> *msg_prio);</span><br><span class="line"><span class="keyword">ssize_t</span> mq_timedreceive(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">char</span> *msg_ptr,</span><br><span class="line">                    <span class="keyword">size_t</span> msg_len, <span class="keyword">unsigned</span> *msg_prio,</span><br><span class="line">                    <span class="keyword">const</span> struct timespec *abs_timeout);</span><br><span class="line"><span class="comment">// success retunr number of bytes. otherwise, return -1</span></span><br><span class="line"><span class="comment">// e.g. mq_receive("/temp.mq", buf, buf_len, &amp;msg_prio);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 当消息有效时通知（异步事件）</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">mq_notify</span><span class="params">(<span class="keyword">mqd_t</span> mqdes, <span class="keyword">const</span> struct sigevent *sevp)</span></span>;</span><br><span class="line"><span class="comment">// semaphore</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化和打开有名信号量</span></span><br><span class="line"><span class="keyword">sem_t</span> *sem_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag);</span><br><span class="line"><span class="keyword">sem_t</span> *sem_open(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, <span class="keyword">mode_t</span> mode, <span class="keyword">unsigned</span> <span class="keyword">int</span> value);</span><br><span class="line"><span class="comment">// success return address of the new semaphore; otherwise, return SEM_FAILED, and set errno</span></span><br><span class="line"><span class="comment">// e.g. sem_t *semptr = sem_open(SemaphoreName, O_CREAT, 0644, 0);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 有名信号量退出</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_close</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 有名信号量移除</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 无名信号量初始化</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_init</span><span class="params">(<span class="keyword">sem_t</span> *sem, <span class="keyword">int</span> pshared, <span class="keyword">unsigned</span> <span class="keyword">int</span> value)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. sem_init(&amp;sem, 0, 1); init 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 销毁一个无名信号量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_destroy</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_wait</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量尝试P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_trywait</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量带超时的P操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_timedwait</span><span class="params">(<span class="keyword">sem_t</span> *sem,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">const</span> struct timespec *abs_timeout)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量V操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_post</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise, semaphore value is left unchanged, return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量值的获取</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_getvalue</span><span class="params">(<span class="keyword">sem_t</span> *sem, <span class="keyword">int</span> *sval)</span></span>;</span><br><span class="line"><span class="comment">// success return 0; otherwise , return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// shared memory segment</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开POSIX共享内存对象</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shm_open</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">int</span> oflag, <span class="keyword">mode_t</span> mode)</span></span>;</span><br><span class="line"><span class="comment">// success return a nonnegative file descriptor. otherwise, return -1.</span></span><br><span class="line"><span class="comment">// e.g. int fd = shm_open(filename, O_RDWR, 0644);</span></span><br><span class="line"><span class="comment">// e.g. 调用shm_open函数,此时,没有内存对象被分配.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 分离共享内存</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shm_unlink</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise , return -1</span></span><br><span class="line"><span class="comment">// e.g. shm_unlink(filename);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件截断到指定长度</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">truncate</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">off_t</span> length)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">ftruncate</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">off_t</span> length)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. ftruncate(fd, 512);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// </span></span><br><span class="line">fstat</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件或设备映射到内存</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mmap</span><span class="params">(<span class="keyword">void</span> *addr,      <span class="comment">// if addr is NULL, then the kernel chooses the address at which to create the mapping</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> length,  <span class="comment">// how many bytes</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> prot,       <span class="comment">// access protections</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> flags,      <span class="comment">// mapping visible to other processes</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> fd,         <span class="comment">// file descriptor</span></span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">off_t</span> offset)</span></span>;  <span class="comment">// offset</span></span><br><span class="line"><span class="comment">// success return a pointer to the mapped area. otherwise, return MAP_FAILED and set errno</span></span><br><span class="line"><span class="comment">// e.g. caddr_t memptr = mmap(NULL, ByteSize, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);</span></span><br><span class="line"><span class="comment">// e.g. 将指定文件fd中偏移量offset开始的长度为length个字节的一块信息映射到虚拟空间中起始地址为addr，长度为length个字节的一块区域</span></span><br><span class="line"><span class="comment">//  prot(制定区域内页面的访问权限位,vm_area_struct中vm_flag字段)</span></span><br><span class="line">PROT_EXE    <span class="comment">// pages页由指令组成</span></span><br><span class="line">PROT_EXEC   <span class="comment">// pages may be executed(可执行)</span></span><br><span class="line">PROT_READ   <span class="comment">// pages may be read(可读)</span></span><br><span class="line">PROT_WRITE  <span class="comment">// pages may be written(可写)</span></span><br><span class="line">PROT_NONE   <span class="comment">// pages may not be accessed(区域内页面不能被访问)</span></span><br><span class="line"><span class="comment">// flags(制定所映射的对象的类型,vm_area_struct结构中vm_flag字段)</span></span><br><span class="line">MAP_SHARED  <span class="comment">// shared this mapping(共享对象，对应共享库文件中的信息)</span></span><br><span class="line">MAP_PRIVATE <span class="comment">// create a private copy-on-write mapping(私有对象，采用写时拷贝技术，对应可执行文件中只读代码区域（.init、.text、.rodata）和已初始化数据区域（.data）</span></span><br><span class="line">MAP_ANON    <span class="comment">// 请求0的页，对应内核创建的匿名文件，相应页框用0初始化并驻留内存</span></span><br><span class="line">MAP_PRIVATE|MAP_ANON<span class="comment">// 未初始化数据（.bss），堆和用户栈等对应区域</span></span><br><span class="line">            </span><br><span class="line">            </span><br><span class="line"><span class="comment">// 取消映射内存</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">munmap</span><span class="params">(<span class="keyword">void</span> *addr, <span class="keyword">size_t</span> length)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno(probably to EINVCAL).</span></span><br><span class="line"><span class="comment">// e.g. munmap(memptr, ByteSize);</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// system V IPC</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件名或对象标识符转换为System V IPC键</span></span><br><span class="line"><span class="keyword">key_t</span> ftok(<span class="keyword">const</span> <span class="keyword">char</span> *pathname, <span class="keyword">int</span> proj_id);</span><br><span class="line"><span class="comment">// success return the generated key_t value. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. key_t key = ftok(PathName, ProjectId); // pathname=queue.h, projectid=123</span></span><br><span class="line"><span class="comment">// e.g. IPC对象标识符与文件描述符不同.文件描述符为当前进程最小可用文件描述符数组下标,IPC对象是系统全局的流水号,归零循环分配使用</span></span><br><span class="line"><span class="comment">// e.g. 让多个进程访问某个特定IPC对象需要一个外部键,一个IPC对象与键管关联.</span></span><br><span class="line"><span class="comment">// e.g. path是个文件名,函数的操作是,取文件的stat结构的st_dev和st_ino部分值,然后与参数ID第八位结合生成键值.他有一个ipc_perm结构体</span></span><br><span class="line"><span class="comment">// e.g. key为IPC_PRIVATE</span></span><br><span class="line"><span class="comment">// e.g. IPC接口过于繁杂,且不适用通用文件系统,不能使用IO监控函数select及poll函数操作IPC,且缺少资源回收机制</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// message queue</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取XSI消息队列标识符</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgget</span><span class="params">(key-t key, <span class="keyword">int</span> msgflg)</span></span>;</span><br><span class="line"><span class="comment">// success return message queue identifier. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. int qid = mesgget(key, 0666 | IPC_CREAT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// XSI消息控制操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgctl</span><span class="params">(<span class="keyword">int</span> msqid, <span class="keyword">int</span> cmd, struct msqid_ds *buf)</span></span>;</span><br><span class="line"><span class="comment">// success IPC_STAT, IPC_SET, and IPC_RMID return 0. IPC_INFO or MSG_INFO operation returns the index of the highest used entry array recording. MSG_STAT operation return the identifier of the queue those index was given in msqid. otherwise return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgctl(qid, IPC_RMID, NULL);  // remove queue</span></span><br><span class="line"><span class="comment">// cmd</span></span><br><span class="line">IPC_STAT    <span class="comment">// copy information msqid into the msqid_ds</span></span><br><span class="line">IPC_SET     <span class="comment">// write the values of some members of the msqid_ds structure</span></span><br><span class="line">IPC_RMID    <span class="comment">// remove the message queue</span></span><br><span class="line">IPC_INFO    <span class="comment">// returns infomation about system-wide mq limits and parameters in the structure pointed to by buf.</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 发送消息</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">msgsnd</span><span class="params">(<span class="keyword">int</span> msgid, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">const</span> <span class="keyword">void</span> *msqp, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">size_t</span> msgsz, </span></span></span><br><span class="line"><span class="function"><span class="params">            <span class="keyword">int</span> msgflg)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgsnd(qid, &amp;msg, sizeof(msg), IPC_NOWAIT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收消息</span></span><br><span class="line"><span class="keyword">ssize_t</span> msgrcv(<span class="keyword">int</span> msgid, </span><br><span class="line">                <span class="keyword">void</span> *msgp, </span><br><span class="line">                <span class="keyword">size_t</span> msgsz, </span><br><span class="line">                <span class="keyword">long</span> msgyp, </span><br><span class="line">                <span class="keyword">int</span> msgflg);</span><br><span class="line"><span class="comment">// success return the number of bytes actually copied. otherwise return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. msgrcv(qid, &amp;msg, sizeof(msg), types[i], MSG_NOERROR | IPC_NOWAIT);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// semaphore</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/sem.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取一个System V信号量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semget</span><span class="params">(<span class="keyword">key_t</span> key, <span class="keyword">int</span> nsems, <span class="keyword">int</span> semflg)</span></span>;</span><br><span class="line"><span class="comment">// success return value the semaphore set identifier. otherwise return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量的控制操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semctl</span><span class="params">(<span class="keyword">int</span> semid, <span class="keyword">int</span> semnum, <span class="keyword">int</span> cmd, ...)</span></span>;</span><br><span class="line"><span class="comment">// failure return -1 and set errno. otherwise return the system call returns a nonnegative value depending on cmd as follows.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号量操作</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">semop</span><span class="params">(<span class="keyword">int</span> semid, struct sembuf *sops, <span class="keyword">unsigned</span> nsops)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise return -1 and set errno</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// shared memory segment</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/shm.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建或打开共享内存</span></span><br><span class="line"><span class="comment">// IPC_PRIVATE或ftok的返回 | 共享内存区大小 | 同open函数权限位,可8进制表示</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmget</span><span class="params">(<span class="keyword">key_t</span> key, <span class="keyword">size_t</span> size, <span class="keyword">int</span> shmflg)</span></span>;</span><br><span class="line"><span class="comment">// success return a non-negative shared memory identifier. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmid = shmget(IPC_PRIVATE, MSIZE, 0666);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存的控制操作</span></span><br><span class="line"><span class="comment">// 共享内存标识符 | IPC_STAT(获取对象属性)/IPCSET(设置对象属性)/IPC_RMID(删除对象) | 用以执行保存设置属性</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmctl</span><span class="params">(<span class="keyword">int</span> shmid, <span class="keyword">int</span> cmd, struct shmid_ds *buf)</span></span>;</span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmctl(shmid, IPC_RMID, NULL);</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存附加操作</span></span><br><span class="line"><span class="comment">// 映射的共享内存标识符 | 将共享内存映射到地址(NULL系统自动完成) | 默认0,共享内存只读</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">shmat</span><span class="params">(<span class="keyword">int</span> shmid, <span class="keyword">const</span> <span class="keyword">void</span> *shmaddr, <span class="keyword">int</span> shmflg)</span></span>;</span><br><span class="line"><span class="comment">// success return data structure associated with the shmID and the segment's start address. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. char *shm_addr = shmat(shmid, NULL, 0); // error return (void *)-1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 共享内存分离操作</span></span><br><span class="line"><span class="comment">// 共享内存映射后地址</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmdt</span><span class="params">(<span class="keyword">const</span> <span class="keyword">void</span> *shmaddr)</span></span>;</span><br><span class="line"><span class="comment">// e.g. 此函数仅用于共享内存区域和进程的地址空间分离, 并不删除共享内存本身.参数是调用shmat函数时的返回值.</span></span><br><span class="line"><span class="comment">// success return 0. otherwise, return -1 and set errno</span></span><br><span class="line"><span class="comment">// e.g. shmdt(shm_addr);</span></span><br></pre></td></tr></table></figure>

<p>异步IO、共享内存、内存映射文件、内存锁定、实时性时钟和定时器、备选调度策略、实时性信号、消息队列、信号量</p>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>unix</tag>
        <tag>APUE</tag>
        <tag>TLPI</tag>
      </tags>
  </entry>
  <entry>
    <title>第二章 构造函数语义学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%BA%8C%E7%AB%A0-%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E8%AF%AD%E4%B9%89%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>关键词explicit之所以被导入这个语言，就是为了给程序员一个方法，使他们能够制止“单一参数的constructor”被当作conversion运算符。</p>
<h4 id="2-1-Default-Constructor的构造操作"><a href="#2-1-Default-Constructor的构造操作" class="headerlink" title="2.1 Default Constructor的构造操作"></a>2.1 Default Constructor的构造操作</h4><h5 id="“带有Default-Constructor”的Member-Class-Object"><a href="#“带有Default-Constructor”的Member-Class-Object" class="headerlink" title="“带有Default Constructor”的Member Class Object"></a>“带有Default Constructor”的Member Class Object</h5></li>
<li><p>如果class没有constructor，但有一个member object，而且这个object有default constructor，那么这个class的implicit default constructor是<font color="red">nontrivial</font>的。举个例子，下面程序片段中，编译器为class Bar合成一个default constructor：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> &#123;</span> <span class="keyword">public</span>: Foo(), Foo(<span class="keyword">int</span>) ... &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bar</span> &#123;</span> <span class="keyword">public</span>: Foo foo; <span class="keyword">char</span> *str; &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Bar bar;    <span class="comment">// Bar::foo 必须在此处初始化。</span></span><br><span class="line">    <span class="keyword">if</span> (str) &#123; &#125; ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  被合成的Bar default constructor内含代码，能够调用class Foo的default constructor处理member object Bar::foo，因为Bar::foo初始化是编译期的责任：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 为member foo调用class Foo的default construcotr</span></span><br><span class="line"><span class="keyword">inline</span> Bar::Bar()</span><br><span class="line">&#123;</span><br><span class="line">    foo.Foo::Foo();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  而且，被合成default constructor只满足编译期需求，并不是程序员需求。如果default constructor由程序员显式定义出来了，那么编译器的行动是：</p>
</li>
<li><p>如果class A内含一个或一个以上的member class objects，那么class A的每一个constructor必须调用每一个member classes的default constructor。编译器会扩张已存在的constructors，使得user code被执行之前，先调用的default constructor。</p>
<h5 id="“带有Default-Constructor”的Base-Class"><a href="#“带有Default-Constructor”的Base-Class" class="headerlink" title="“带有Default Constructor”的Base Class"></a>“带有Default Constructor”的Base Class</h5></li>
<li><p>如果class没有constructors，却派生自有default constructor的base class，那么这个derived class的default constructor会被视为nontrivial。</p>
</li>
<li><p>如果提供多个constructor，但都没有default constructor，编译器会扩张现有的每一个constructors，将用以调用必要default constructors的程序代码加进去。它不会合成一个新的default constructor。</p>
<h5 id="“带有一个Virtual-Function”的Class"><a href="#“带有一个Virtual-Function”的Class" class="headerlink" title="“带有一个Virtual Function”的Class"></a>“带有一个Virtual Function”的Class</h5></li>
<li><p>另外有<font color="red">两种情况</font>，也要合成default constructor：</p>
<ol>
<li><p><strong>class声明（或继承）一个virtual function</strong></p>
</li>
<li><p><strong>class派生自一个继承串链，其中有一个或更多的virtual base classes。</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Bell --- Widget</span><br><span class="line">Whistle --- Widget</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">flip</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">flip</span><span class="params">(<span class="keyword">const</span> Widget &amp;widget)</span> </span>&#123; Widget.flip(); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Bell b;</span><br><span class="line">    Whistle w;</span><br><span class="line">    flip(b);</span><br><span class="line">    flip(w);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个扩张在编译期间发生了：</p>
</li>
<li><p>一个virtual function table（<font color="red">vtbl</font>）被产生出来，内含class的virtual functions地址。</p>
</li>
<li><p>每个class object中，额外的pointer member（<font color="red">vptr</font>）会被合成出来，内含相关的class vtbl地址。</p>
<p>widget.flip()虚拟调用操作会被重新改写，以使用widget之中的vtpr和vtbl：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&#123; *widget.vptr[<span class="number">1</span>])(&amp;widget)</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
<h5 id="“带有一个Virtual-Base-Class”的Class"><a href="#“带有一个Virtual-Base-Class”的Class" class="headerlink" title="“带有一个Virtual Base Class”的Class"></a>“带有一个Virtual Base Class”的Class</h5><ul>
<li><p>看以下代码：</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">A --- X</span><br><span class="line">B --- X</span><br><span class="line">C --- A</span><br><span class="line">C --- B</span><br></pre></td></tr></table></figure>

 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">X</span> &#123;</span> <span class="keyword">public</span>: <span class="keyword">int</span> i; &#125; ;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123; <span class="keyword">public</span>: <span class="keyword">int</span> j; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> X &#123; <span class="keyword">public</span>: <span class="keyword">double</span> d; &#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> :</span> <span class="keyword">public</span> A, <span class="keyword">public</span> B &#123; <span class="keyword">public</span>: <span class="keyword">int</span> k; &#125;;</span><br><span class="line"><span class="comment">// 无法在编译时期决定（resolve）pa-&gt;X::i的位置</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">const</span> A *pa)</span> </span>&#123; pa-&gt;i = <span class="number">1024</span>; &#125;</span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">    foo(<span class="keyword">new</span> A);</span><br><span class="line">    foo(<span class="keyword">new</span> C);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 因为pa的类型可以改变，编译器无法固定住foo()中经由pa而存取的X::i的实际偏移位置。编译器必须改变“执行存取操作”的那些代码，使X::i可以延迟至执行期才决定下来。cfont做法是在virtual base classes中安插指针完成。经由reference或pointer存取virtual base class的操作都可以通过指针完成。</p>
 <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">const</span> A *pa)</span> </span>&#123;pa-&gt;__vbcX-&gt;i = <span class="number">1024</span>; &#125;</span><br></pre></td></tr></table></figure>

<p> __vbcX实在class object构造期间被完成的。编译器会安插允许每个virtual base class在执行器存取操作的代码，如果base没有声明任何constructor，编译器必须为它合成一个default的。</p>
<h4 id="2-2-Copy-Constructor的构造操作"><a href="#2-2-Copy-Constructor的构造操作" class="headerlink" title="2.2 Copy Constructor的构造操作"></a>2.2 Copy Constructor的构造操作</h4></li>
<li><p>有三种情况，会以一个obect的内容作为另一个class object的初值。</p>
<ul>
<li><strong>对object做显式初始化操作</strong></li>
<li><strong>当object作为参数交给某个函数时</strong></li>
<li><strong>函数回传一个class object时</strong></li>
</ul>
</li>
</ul>
<h5 id="Default-Memberwise-Initialization"><a href="#Default-Memberwise-Initialization" class="headerlink" title="Default Memberwise Initialization"></a>Default Memberwise Initialization</h5><ul>
<li><p>如果class没有提供explicit copy constructor，其内部是以<font color="red">default memberwise initialization</font>手法完成的，就是把每个内建的或派生的data member（如指针或数组）的值，从object拷贝到另一个object上，不过它并不会拷贝其中的member class object。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... 没有explicit copy constructor</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">char</span> *str;</span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// String object的default memberwise initialization发生在这种情况下：</span></span><br><span class="line"><span class="function">String <span class="title">noun</span><span class="params">(<span class="string">"book"</span>)</span></span>;</span><br><span class="line">String verb = noun;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 完成方式好像个别设定每个members一样</span></span><br><span class="line">verb.str = noun.str;</span><br><span class="line">verb.len = noun.len;</span><br></pre></td></tr></table></figure>

<p>  如果String object被声明为一个class的member:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// ... 没有explicit copy constructor</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> _occurs;</span><br><span class="line">    String _word;</span><br><span class="line">&#125;;s</span><br></pre></td></tr></table></figure>

<p>  那么Word object的default memberwise initialization会拷贝_occurs，然后再于_word身上递归实施memberwise initialization。</p>
</li>
<li><p>一个class可用两种方式复制得到：</p>
<ul>
<li><strong>一是被初始化</strong>。以copy constructor完成。</li>
<li><strong>二是被指定（asignment）</strong>。以copy assignment operator完成。</li>
</ul>
</li>
</ul>
<h5 id="Bitwise-Copy-Semantics-位逐次拷贝）"><a href="#Bitwise-Copy-Semantics-位逐次拷贝）" class="headerlink" title="Bitwise Copy Semantics(位逐次拷贝）"></a>Bitwise Copy Semantics(位逐次拷贝）</h5><ul>
<li>一个class没有定义explicit copy constructor，是否有编译器合成实例，取决于class是否展现<font color="red">bitwise copy semantics</font>而定。有两个例子：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 声明展现了bitwise copy semantics</span></span><br><span class="line"><span class="comment">// 这种情况下不需要合成default copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word(<span class="keyword">const</span> <span class="keyword">char</span>*);</span><br><span class="line">    ~Word() &#123; <span class="keyword">delete</span> []str; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> cnt;</span><br><span class="line">    <span class="keyword">char</span> *str;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 以下声明未展现出bitwise copy semantics</span></span><br><span class="line"><span class="comment">// 这种情况下需要合成出一个copy constructor，以便调用member class String object的copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word(<span class="keyword">const</span> String&amp;);</span><br><span class="line">    ~Word();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> cnt;</span><br><span class="line">    String str;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 其中String声明了explicit copy constructor</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">String</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    String(<span class="keyword">const</span> <span class="keyword">char</span>*);</span><br><span class="line">    String(<span class="keyword">const</span> String&amp;);</span><br><span class="line">    ~String();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;s</span><br></pre></td></tr></table></figure>


</li>
</ul>
<h5 id="不要Bitwise-Copy-Semantics"><a href="#不要Bitwise-Copy-Semantics" class="headerlink" title="不要Bitwise Copy Semantics!"></a>不要Bitwise Copy Semantics!</h5><ul>
<li><font color="red">有4中情况下，class不展现出“bitwise copy semantics”</font>： <ul>
<li><strong>当class内有个member object，而该object声明有copy constructor时</strong>；</li>
<li><strong>当class继承自一个base class，而该class存在copy constructor时</strong>，不论是显式声明的还是被合成的；</li>
<li><strong>当class声明了一个或多个virtual functional时</strong>。</li>
<li><strong>当class派生自一个继承串联，其中有一个或多个virutal base clases时</strong>。</li>
</ul>
</li>
</ul>
<h5 id="重新设定Virtual-Table的指针"><a href="#重新设定Virtual-Table的指针" class="headerlink" title="重新设定Virtual Table的指针"></a>重新设定Virtual Table的指针</h5><ul>
<li><p>只要有一个class声明了一个或多个virtual functional，就会：</p>
<ul>
<li><p><strong>增加vtbl，内含virtual function的地址</strong></p>
</li>
<li><p><strong>一个指向vtbl的vptr，插在class object内</strong>。</p>
<p>当vptr导入到class之中，class就不展示biwise semantics了。一个新产生的class object的vptr不能成功而正确地设好初值会导致可怕的后果，编译器需要合成出一个copy constructor以求将vptr初始化。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph TD</span><br><span class="line">ZooAnimal --- Bear</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ZooAnimal</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ZooAnimal();</span><br><span class="line">    <span class="keyword">virtual</span> ~ZooAnimal();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">animate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bear</span> :</span> <span class="keyword">public</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Bear();</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">animate</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">dance</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ZooAnimal class object以另一个ZooAnimal class object作为初值，或Bear class object以另一个Bear class object作为初值，都可以直接靠bitwise copy semantics完成（除了member pointer）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Bear yogi;</span><br><span class="line">Bear winnie = yogi;</span><br></pre></td></tr></table></figure>

<p>这个例子里，yogi会被default Bear constructor初始化。yogi的vptr被指向Bear的vtbl（靠安插）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ZooAnimal franny = yogi;    <span class="comment">// sliced</span></span><br></pre></td></tr></table></figure>

<p>frany的vptr不可以被指向Bear的vtbl（如果yogi的vptr被bitwise copy，会导致此结果），否则当draw()被调用而franny被传进去时，会blow up：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">draw</span><span class="params">(<span class="keyword">const</span> ZooAnimal &amp;zoey)</span> </span>&#123; zoey.draw(); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// franny的vptr指向ZooAnimal的vtbl</span></span><br><span class="line">    ZooAnimal franny = yogi;</span><br><span class="line">    draw(yogi); <span class="comment">// call Bear::draw()</span></span><br><span class="line">    draw(franny);   <span class="comment">// call ZooAnimal::draw()</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过franny调用virtual function draw()，调用的时ZooAnimal而非Bear实例。事实上，yogi中的Bear部分在franny初始化时被sliced掉了，只有franny声明为reference或pointer时才会是Bear的函数实例。</p>
</li>
</ul>
</li>
</ul>
<h5 id="处理Virtual-Base-Class-Subobject"><a href="#处理Virtual-Base-Class-Subobject" class="headerlink" title="处理Virtual Base Class Subobject"></a>处理Virtual Base Class Subobject</h5><ul>
<li><p>编译器必须让derived class object中的virtual base class subobject位置在执行期准备妥当。Bitwise copy semantics可能会破坏位置的完整性，所以编译器必须必须在它自己合成出来的copy constructor中做出仲裁：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Bear -- public --- ZooAnimal</span><br><span class="line">Raccoon -- public virtual --- ZooAnimal</span><br><span class="line">RedPanda -- public --- Raccoon</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Raccoon</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> ZooAnimal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Raccoon() &#123; <span class="comment">/*设定private data初值*/</span> &#125;</span><br><span class="line">    Raccoon(<span class="keyword">int</span> val) &#123; <span class="comment">/*设定private data初值*/</span> &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  一个virtual base class的存在会使bitwise copy semantics无效。问题不在于一个class object以另一个同类的object作为初值之时，而是发生于一个class object以其derived classes的某个object作为初值之时。<br>  一个Raccoon object作为另一个Raccoon object的初值，bitwise copy绰绰有余，而如果企图以RedPanda object作为little_critter的初值，编译器必须判断当后续企图存取ZooAnimal subobject时是否能正确执行：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 简单的bitwise copy还不够，必须显式将little_critter的virtual base class pointer/offset初始化</span></span><br><span class="line">RedPanda little_red;</span><br><span class="line">Raccoon little_critter = little_red;</span><br></pre></td></tr></table></figure>

<p>  在这种情况下，为了完成正确的little_critter初值设定，编译器合成一个copy constructor，安插代码设定virtual base class pointer/offset的初值（或是简单的确定它没被抹消），对每个members执行memberwise初始化操作，一起执行其他内存相关工作。</p>
</li>
</ul>
<h4 id="2-3-程序转化语义学（Program-Transformation-Semantics）"><a href="#2-3-程序转化语义学（Program-Transformation-Semantics）" class="headerlink" title="2.3 程序转化语义学（Program Transformation Semantics）"></a>2.3 程序转化语义学（Program Transformation Semantics）</h4><h5 id="显式的初始化操作（Explicit-Initialization）"><a href="#显式的初始化操作（Explicit-Initialization）" class="headerlink" title="显式的初始化操作（Explicit Initialization）"></a>显式的初始化操作（Explicit Initialization）</h5><ul>
<li><p>已知有定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X x0;</span><br></pre></td></tr></table></figure>

<p>  下面三个定义，每个都以x0来初始化class object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">X <span class="title">x1</span><span class="params">(x0)</span></span>;</span><br><span class="line">    X x2 = x0;</span><br><span class="line">    X x3 = X(x0);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  以上必要的程序转化有<font color="red">两个阶段</font>：</p>
<ul>
<li><p><strong>重写每个定义，其中初始化操作被剥除</strong>。</p>
</li>
<li><p><strong>class的copy constructor调用操作被安插进去</strong>。</p>
<p>foo_bar()可能看起来这样：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">C++ 伪码</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 定义重写，初始化操作被剥除</span></span><br><span class="line">    X x1;</span><br><span class="line">    X x2;</span><br><span class="line">    X x3;</span><br><span class="line">    <span class="comment">// 编译器安插copy construction的调用操作</span></span><br><span class="line">    <span class="comment">// 表现出X::X(const X &amp;xx);</span></span><br><span class="line">    x1.X::X(x0);</span><br><span class="line">    x2.X::X(x0);</span><br><span class="line">    x3.X::X(x0);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="参数的初始化（Argument-Initialization）"><a href="#参数的初始化（Argument-Initialization）" class="headerlink" title="参数的初始化（Argument Initialization）"></a>参数的初始化（Argument Initialization）</h5><ul>
<li><p>C++ Standard说，把class object当作参数传给函数，或者作为函数的返回值，相当于初始化操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X xx = arg;</span><br></pre></td></tr></table></figure>

<p>  这里xx是形式参数（或返回值），arg是真实参数。因此，函数：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(X x0)</span></span>;</span><br><span class="line">X xx;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">foo(xx);</span><br></pre></td></tr></table></figure>

<p>  会要求<font color="red">局部实例</font>（<font color="green">local instance</font>）x0以memberwise方式将xx当初值。代码转换为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="comment">// 编译器产生的临时对象</span></span><br><span class="line">X __temp0;</span><br><span class="line"><span class="comment">// 调用copy constructor</span></span><br><span class="line">__temp0.X::X(xx);</span><br><span class="line">foo(__temp0);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="返回值的初始化（Return-Value-Initialization）"><a href="#返回值的初始化（Return-Value-Initialization）" class="headerlink" title="返回值的初始化（Return Value Initialization）"></a>返回值的初始化（Return Value Initialization）</h5><ul>
<li><p>已知下面函数的定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  bar()的返回值如何从局部对象xx中拷贝过来？在cfont中的做法是一个双阶段转化：</p>
<ul>
<li><p><strong>加上额外参数，类型是class object的reference</strong>。这个参数用来放置被<font color="red">拷贝构建</font>（<font color="green">copy constructed</font>）而来的返回值。</p>
</li>
<li><p><strong>return之前安插一个copy constructor调用</strong>。</p>
<p>bar()转换如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// default constructor</span></span><br><span class="line">    xx.X::X();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// copy constructor</span></span><br><span class="line">    __result.X:XX(xx);</span><br><span class="line">    <span class="keyword">return</span>; <span class="comment">// 即使不传回任何值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X xx = bar();</span><br><span class="line"><span class="comment">// 不用default constructor,NRV</span></span><br><span class="line">X xx;</span><br><span class="line">bar(xx);</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">bar().memfunc();</span><br><span class="line"><span class="comment">// 执行bar()所传回的X class object的memfunc()可能转换</span></span><br><span class="line">X __temp0;</span><br><span class="line">(bar(__temp0), __temp0).memfunc();</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 同理，声明一个函数指针</span></span><br><span class="line">X (*pf)();</span><br><span class="line">pf = bar;</span><br><span class="line"><span class="comment">// 转为</span></span><br><span class="line"><span class="keyword">void</span> (*pf)(X&amp;);</span><br><span class="line">pf = bar;</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="在使用者层面做优化（Optimization-at-the-User-Level）"><a href="#在使用者层面做优化（Optimization-at-the-User-Level）" class="headerlink" title="在使用者层面做优化（Optimization at the User Level）"></a>在使用者层面做优化（Optimization at the User Level）</h5><ul>
<li><p>以下代码：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">(<span class="keyword">const</span> T &amp;y, <span class="keyword">const</span> T &amp;z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...以y</span></span><br><span class="line">    来处理xx</span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  可以这么写：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">(<span class="keyword">const</span> T &amp;y, <span class="keyword">const</span> T &amp;z)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> X(y, z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  当bar()被转换后，效率会比较高：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    __result.X::X(y, z);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="在编译器层面做优化（Optimization-at-the-Compiler-Level）"><a href="#在编译器层面做优化（Optimization-at-the-Compiler-Level）" class="headerlink" title="在编译器层面做优化（Optimization at the Compiler Level）"></a>在编译器层面做优化（Optimization at the Compiler Level）</h5><ul>
<li><p>在一个像bar()这样的函数中，所有return指令传回相同具名数值（named value），因此<font color="red">编译器可能自己做优化</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X xx;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> xx;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  xx以_result取代：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(X &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// default constructor被调用</span></span><br><span class="line">    __result.X::X();</span><br><span class="line">    <span class="comment">// ... 直接处理__result</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  这样的优化操作，称为<font color="green">Named Return Value</font>（<font color="red">NRV</font>）。虽然NRV优化提供了重要效率改善，但它还是饱受批评。</p>
<ul>
<li><p><strong>优化是默默的，不透明</strong></p>
</li>
<li><p><strong>一旦函数复杂，很难优化</strong></p>
</li>
<li><p><strong>某些人不喜欢</strong></p>
<p>举个例子，以下三个初始化语义上相等：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">X <span class="title">xx0</span><span class="params">(<span class="number">1024</span>)</span></span>;    <span class="comment">// xx0.X::X(1024);</span></span><br><span class="line">X xx1 = X(<span class="number">1024</span>);</span><br><span class="line">X xx2 = (X)<span class="number">1024</span>;</span><br></pre></td></tr></table></figure>

<p>xx0是被单一的constructor操作设定初值：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">xx0.X::X(<span class="number">1024</span>);</span><br></pre></td></tr></table></figure>

<p>而xx1或xx2却调用两个constructor，产生临时性object，并针对临时object调用classX的destructor：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X __temp0;</span><br><span class="line">__temp0.X::X(<span class="number">1024</span>);</span><br><span class="line">xx1.X::X(__temp0);</span><br><span class="line">__temp0.X::~X();</span><br></pre></td></tr></table></figure>

</li>
</ul>
</li>
</ul>
<h5 id="Copy-Constructor：要还是不要？"><a href="#Copy-Constructor：要还是不要？" class="headerlink" title="Copy Constructor：要还是不要？"></a>Copy Constructor：要还是不要？</h5><ul>
<li><p>一个3D坐标点类：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y, _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  class的default copy constructor被视为trivial。默认情况下，一个Point3d class object的“memberwise”初始化操作会导致“bitwise copy”。这样效率高，也安全。<br>  实现copy constructor的最简单方法像这样：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    _x = rhs._x;</span><br><span class="line">    _y = rhs._y;</span><br><span class="line">    _z = rhs._z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  但使用C++library的memcpy()会更有效率：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">const</span> Point3d &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">memcpy</span>(<span class="keyword">this</span>, &amp;rhs, <span class="keyword">sizeof</span>(Point3d));</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="2-4-成员们的初始化队伍（Member-Initialization-List）"><a href="#2-4-成员们的初始化队伍（Member-Initialization-List）" class="headerlink" title="2.4 成员们的初始化队伍（Member Initialization List）"></a>2.4 成员们的初始化队伍（Member Initialization List）</h4><ul>
<li><p>当你写下一个constructor时，就有机会设定class members的初值，不是在member initialization list，就是在constructor函数本体。</p>
</li>
<li><p>下列情况下，为了让你程序能够顺利执行，必须使用member initialization list：</p>
<ol>
<li><p><font color="red"><strong>当初始化reference member时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当初始化const member时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当调用一个base class的constructor，且拥有一组参数时</strong></font>；</p>
</li>
<li><p><font color="red"><strong>当调用一个member class的constructor，而 它拥有一组参数时</strong></font>。<br>这四种情况都能正确编译并执行，但效率不高。例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Word</span> &#123;</span></span><br><span class="line">    String _name;</span><br><span class="line">    <span class="keyword">int</span> _cnt;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Word() &#123;</span><br><span class="line">        _name = <span class="number">0</span>;</span><br><span class="line">        _cnt = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>constructor可能的内部扩张结果：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word( <span class="comment">/* this pointer goes here */</span> )</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// String default constructor</span></span><br><span class="line">    _name.String::String();</span><br><span class="line">    <span class="comment">// tempory</span></span><br><span class="line">    String temp = String(<span class="number">0</span>);</span><br><span class="line">    <span class="comment">// memberwise copy _name</span></span><br><span class="line">    _name.String::<span class="keyword">operator</span>=(temp);</span><br><span class="line">    <span class="comment">// destroy tempory</span></span><br><span class="line">    temp.String::~String();</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个有效率的实现方法是：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word : _name(<span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>它会扩张：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Word::Word( <span class="comment">/* this pointer goes here */</span> )</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// String(int) constructor</span></span><br><span class="line">    _name.String::String(<span class="number">0</span>);</span><br><span class="line">    _cnt = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第五章 构造、析构、拷贝语意学</title>
    <url>/2020/06/12/%E7%AC%AC%E4%BA%94%E7%AB%A0-%E6%9E%84%E9%80%A0%E3%80%81%E6%9E%90%E6%9E%84%E3%80%81%E6%8B%B7%E8%B4%9D%E8%AF%AD%E6%84%8F%E5%AD%A6/</url>
    <content><![CDATA[<h5 id="纯虚函数的存在（Presence-of-a-Pure-Virtal-Function）"><a href="#纯虚函数的存在（Presence-of-a-Pure-Virtal-Function）" class="headerlink" title="纯虚函数的存在（Presence of a Pure Virtal Function）"></a>纯虚函数的存在（Presence of a Pure Virtal Function）</h5><ul>
<li><p>考虑abstract base class声明:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Abstract_base</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Abstract_base() = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">interface</span><span class="params">()</span> <span class="keyword">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">mumble</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _mumble; &#125;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">char</span> *_mumble;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这个class被设计为一个抽象的base class(其中有pure virtual function,使得Abstract_base不可能有实例),但需要一个显式的构造函数以初始化data member _mumble。如果没有初始化操作，derived class的局部性对象_mumble及那个无法决定初值。</p>
</li>
</ul>
<h5 id="纯虚函数的存在-Presence-of-a-Pure-Virtual-Function"><a href="#纯虚函数的存在-Presence-of-a-Pure-Virtual-Function" class="headerlink" title="纯虚函数的存在(Presence of a Pure Virtual Function)"></a>纯虚函数的存在(Presence of a Pure Virtual Function)</h5><ul>
<li>pure virtual function只能被<font color="red">静态地调用</font>（<font color="green">invoked statically</font>），不能经由虚拟机制调用。<strong>而pure virtual destructor，class设计者一定得定义它。因为每个derived class class以及base class destructor。只要缺乏定义，就会导致链接失败</strong>。</li>
</ul>
<h5 id="虚拟规格的存在（Presence-of-a-Virtual-Specification）"><a href="#虚拟规格的存在（Presence-of-a-Virtual-Specification）" class="headerlink" title="虚拟规格的存在（Presence of a Virtual Specification）"></a>虚拟规格的存在（Presence of a Virtual Specification）</h5><ul>
<li>如果决定把Abstract_base::mumble()设计为virtual function，那是糟糕的选择，因为其函数定义内容并不与类型有关，因而几乎不会被后继的derived class改写。</li>
</ul>
<h5 id="虚拟规格中const的存在"><a href="#虚拟规格中const的存在" class="headerlink" title="虚拟规格中const的存在"></a>虚拟规格中const的存在</h5><ul>
<li>决定要给virtual function是否需要const，是一件繁琐的事情。如果声明为const，意味着subclass实例可能被无穷次数地使用。不把函数声明为const，意味和函数不能获得一个const reference或const pointer。头大的是，声明函数为const，才发现derived instance必须修改某一data member。解决办法就是不再用const就是了。</li>
</ul>
<h5 id="重新考虑class的声明"><a href="#重新考虑class的声明" class="headerlink" title="重新考虑class的声明"></a>重新考虑class的声明</h5><ul>
<li>综上所述，这才是比较适当的一种设计：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Abstract_base</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Abstract_base();   <span class="comment">// nonpure virtual</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">interface</span><span class="params">()</span> </span>= <span class="number">0</span>;   <span class="comment">// nonconst</span></span><br><span class="line">    <span class="function"><span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">mumble</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _mumble; &#125; <span class="comment">// nonvirtual</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Abstract_base(<span class="keyword">char</span> *pc =ss <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">char</span> *_mumble;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="5-1-”无继承”情况下的对象构造"><a href="#5-1-”无继承”情况下的对象构造" class="headerlink" title="5.1 ”无继承”情况下的对象构造"></a>5.1 ”无继承”情况下的对象构造</h4><ul>
<li><p>考虑程序片段：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global;                   <span class="comment">//1</span></span><br><span class="line">                                <span class="comment">//2</span></span><br><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">()</span>                  <span class="comment">//3</span></span></span><br><span class="line"><span class="function"></span>&#123;                               <span class="comment">//4</span></span><br><span class="line">    Point local;                <span class="comment">//5</span></span><br><span class="line">    Point *heap = <span class="keyword">new</span> Point;    <span class="comment">//6</span></span><br><span class="line">    *heap = local;              <span class="comment">//7 </span></span><br><span class="line">    <span class="comment">// ... stuff ...            //8</span></span><br><span class="line">    <span class="keyword">delete</span> heap;                <span class="comment">//9</span></span><br><span class="line">    <span class="keyword">return</span> local;               <span class="comment">//10</span></span><br><span class="line">&#125;                               <span class="comment">//11</span></span><br></pre></td></tr></table></figure>

<p>  L1、L5、L6分别为：<strong>global内存配置</strong>、<strong>local内存配置</strong>和<strong>heap内存配置</strong>。一个object的声明，是该object的一个执行期属性。local object的生命从L5定义开始，L10为止。global object的生命和这个程序的生命相同。heap object的生命从被new运算符配置出来开始，到delete运算符摧毁结束。<br>  下面是Point的第一次声明，写成C程序。这是一种Plain OI’ Data声明形式：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">float</span> x, y, z;</span><br><span class="line">    Point;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  如果用C++来编译这代码，<strong>编译器会为Point声明一个trivial default constructor、一个trivial destructor、一个trivial copy constructor，以及一个trivial copy assignment operator</strong>。但实际上，编译器会分析这个声明，并<font color="red">贴上POD标签</font>。</p>
</li>
<li><p>当编译器遇到这样的定义：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global;                   <span class="comment">//1</span></span><br></pre></td></tr></table></figure>

<p>  观念上Point的trival constructor和destructor会被产生并调用，constructor在程序起始（<font color="red">startup</font>）被调用，而destructor在程序的<font color="red">exit()</font>处被调用。然而事实上trivial members要么没定义，要么没被调用。<br>  在C中，global被视为一个<font color="red">临时性的定义</font>，因为它没有显式的初始化操作。它可以在程序中发生多次。实例<strong>会被折叠起来，只留下一个，放在程序data segment中</strong>。这块空间称为<font color="red"><strong>BSS</strong></font>，这是Block Started by Symbol缩写。<br>  C++并不支持临时性定义。虽然它可以判断class object或是POD。global在C++中被视为完全定义（会阻止多个定义）。C和C++的差异是，BSS data segment在C++中相对不重要。<font color="red"><strong>所有的全局变量都被以“初始化过的数据”来对待</strong></font>。<br>  至于foobar()函数的L5，既没有构造也没有被析构，不过可能没有经过初始化就会成BUG（L7）。至于heap object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point *heap = <span class="keyword">new</span> Point;    <span class="comment">//6</span></span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line">Point *heap = __new(<span class="keyword">sizeof</span>(Point));</span><br><span class="line"><span class="comment">// 有初始化过就没问题</span></span><br><span class="line">*heap = local;              <span class="comment">//7</span></span><br></pre></td></tr></table></figure>

<p>  事实上L7会产生编译警告，观念上，这样的指定操作会触发trivial copy assignment operator做拷贝搬运操作。而实际上object是POD，所以assignment只是像C那样纯粹bitwise搬运。</p>
</li>
</ul>
<h5 id="抽象数据类型（Abstract-Data-Type）"><a href="#抽象数据类型（Abstract-Data-Type）" class="headerlink" title="抽象数据类型（Abstract Data Type）"></a>抽象数据类型（Abstract Data Type）</h5><ul>
<li><p>以下是Point声明，提供了完整封装性，但没有提供virtual function：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : _x(x), _y(y), _z(z) &#123;&#125;</span><br><span class="line">    <span class="comment">// no copy constructor, copy operator</span></span><br><span class="line">    <span class="comment">// or destructor defined ...</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y, _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  这里还是三个连续的float，不论private或public存取层或是member function都不会占用额外的对象空间。没有定义copy constructor或copy operator，因为有<strong>default bitwise semantics</strong>足够了，也不需要destructor。对于实例：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point global; <span class="comment">// Point::Point(0.0, o.0, 0.0);</span></span><br></pre></td></tr></table></figure>

<p>  有了default constructor，global被定义在全局范畴，初始化操作将延迟到程序启动（startup）。<br>  如果要将class中的所有成员设定常量初值，给予一个<font color="red">explicit initialization list</font>会比较有效率（相比constructor的inline expansion而言）。但它有三项缺点：</p>
<ol>
<li>只有当class members都是public，才会奏效。</li>
<li>只能指定常量，因为在编译器就能评估求值。</li>
<li>由于编译器并不自动施行，初始化行为可能性很高。</li>
</ol>
</li>
</ul>
<h5 id="为继承做准备"><a href="#为继承做准备" class="headerlink" title="为继承做准备"></a>为继承做准备</h5><ul>
<li><p>第三个Point声明，为继承性质和某些操作的<font color="red">动态决议</font>（<font color="green">dynamic resolution</font>）做准备，目前限制z成员做存取操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>) : _x(x), _y(y) &#123;&#125;</span><br><span class="line">    <span class="comment">// no destructor, copy constructor, or</span></span><br><span class="line">    <span class="comment">// copy operator defined ... </span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  virtual functions的导入在这里附带一个virtual destructor的声明在这个例子里，并无好处。除此之外，每个class object多负担一个vptr之外，virtual function的导入也引发编译器对Point class产生的膨胀作用：</p>
<ul>
<li><strong>定义的constructor附带了一些代码，以便vptr初始化</strong>：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line">Point* Point::Point(Point *<span class="keyword">this</span>, <span class="keyword">float</span> x, <span class="keyword">float</span> y) : _x(x), _y(y)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 设定object的vptr</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point = __vtbl__Point;</span><br><span class="line">    <span class="comment">// 扩展member initialization list</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;_x = x;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_y = y;</span><br><span class="line">    <span class="comment">// 传回this</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ul>
</li>
</ul>
<pre><code>+ **合成一个copy constructor和copy assignment operator，而且操作不是trivial（implicit destructor仍然是）**。如果Point object被初始化或以derived class object赋值，那么以为基础（bitwise）的操作可能对vptr带来非法设定：
    <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="comment">// copy constructor 内部合成</span></span><br><span class="line"><span class="keyword">inline</span> Point* Point::Point(Point*<span class="keyword">this</span>, <span class="keyword">const</span> Point &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// object的vptr</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point = __vtbl__Point;</span><br><span class="line">    <span class="comment">// copy</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

编译器在优化状态会把object内容连续拷贝到另一个object，而不是精准memberwise。**L7的memberwise赋值操作可能出发copy assignment operator的合成，及调用&lt;font color=red&gt;inline expansion&lt;/font&gt;（行内扩张）：以this取代heap，以rhs取代local**。  
戏剧性的冲击在L10。由于copy constructor的出现，foobar()可能转化为：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">(Point &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Point local;</span><br><span class="line">    local.Point::Point(<span class="number">0.0</span>, <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// heap</span></span><br><span class="line">    <span class="comment">// copy constructor</span></span><br><span class="line">    __result.Point::Point(local);</span><br><span class="line">    <span class="comment">// local destructor</span></span><br><span class="line">    <span class="comment">// Point::derstructor</span></span><br><span class="line">    <span class="comment">// local.Point::~Point();</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

如果NRV优化，转化为：
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Point <span class="title">foobar</span><span class="params">(Point &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    __result.Point::Point(<span class="number">0.0</span>, <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// heap</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre><h4 id="5-2-继承体系下的对象构造"><a href="#5-2-继承体系下的对象构造" class="headerlink" title="5.2 继承体系下的对象构造"></a>5.2 继承体系下的对象构造</h4><ul>
<li><p>当定义一个object：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">T object;</span><br></pre></td></tr></table></figure>

<p>  如果T有constructor，它会被调用。除此之外，constructor调用伴随了什么？constructor可能内含大量隐藏码，因为<font color="red"><strong>编译器会扩充每个constructor</strong></font>：</p>
<ol>
<li>在member initialization list中的data members初始化操作会被放进constructor函数本体，以声明顺序为顺序。</li>
<li>如果member没有在member initialization list中，但这个member有default constructor，那么被调用。</li>
<li>在那之前，如果class object有vptr，必须被设定初值指向适当vtbls。</li>
<li>在那之前，base class constructors必须被调用，以base class声明顺序为顺序。<ul>
<li><strong>如果base class被列入member initialization list中，那么任何显式参数都传递进去</strong>。</li>
<li><strong>如果base class没有被列入member initialization list中，而它有default constructor（或default memberwie copy constructor），那么调用它</strong>。</li>
<li><strong>如果base class是多层继承下的第二或后继base class，那么调整this指针</strong>。</li>
</ul>
</li>
<li>在那之前，所有virtual base class constructors必须被调用，从左到右，从深到浅：<ul>
<li><strong>如果class被列入member initialization list中，那么任何显式指定的参数，都传递进去。如果没有列入list中，而class有default constructor，调用它</strong>。</li>
<li><strong>class中的每个virtual base class subobject的偏移地址（offset）必须在执行期可被存取</strong>。</li>
<li><strong>如果class object是最底层（most-derived）的class，其constructors可能被调用；用以支持者行为的机制必须放出来</strong>。</li>
</ul>
</li>
</ol>
</li>
<li><p>以Point为例，探讨constructors扩充的必要性：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>);</span><br><span class="line">    Point(<span class="keyword">const</span> Point&amp;);            <span class="comment">// copy constructor</span></span><br><span class="line">    Point&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Point&amp;); <span class="comment">// copy assignment operator</span></span><br><span class="line">    <span class="keyword">virtual</span> ~Point();               <span class="comment">// virtual derstructor</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="number">0.0</span>; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Line</span> &#123;</span></span><br><span class="line">    Point _begin, _end;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Line(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    Line(<span class="keyword">const</span> Point&amp;, <span class="keyword">const</span> Point&amp;);</span><br><span class="line">    draw();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  。。。。。。。。</p>
</li>
</ul>
<h5 id="虚拟继承-Virtual-Inheritance"><a href="#虚拟继承-Virtual-Inheritance" class="headerlink" title="虚拟继承(Virtual Inheritance)"></a>虚拟继承(Virtual Inheritance)</h5><ul>
<li><p>考虑下面虚拟继承，继承自Point：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> <span class="keyword">virtual</span> Point &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>) : Point(x, y), _z(z) &#123;&#125;</span><br><span class="line">    Point3d(<span class="keyword">const</span> Point3d &amp;rhs) : Point(rhs), _z(rhs._z) &#123;&#125;</span><br><span class="line">    ~Point3();</span><br><span class="line">    Point3d&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> Point3d&amp;);</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> _z; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  传统的constructor扩充并没有用，因为virtual base class的共享性之故：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不合法</span></span><br><span class="line">Point3d* Point3d::Point3d(Point3d *<span class="keyword">this</span>, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Piont3d = __vtbl_Point3d;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d__Point = __vtbl_Point3d_Point;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_z = rhs._z;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  看出什么错误了吗？</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point3d -- virtual --- Point</span><br><span class="line">Vertex -- virtual --- Point</span><br><span class="line">Vertex3d -- public --- Point3d</span><br><span class="line">Vertex3d -- public --- Vertex</span><br><span class="line">pVertex -- public --- Vertex3d</span><br></pre></td></tr></table></figure>

<p>  Vertex的constructor必须调用Point constrcutor。然而当Point3d和Vertex为Vertex3d的subobjects时，则调用操作不一定发生。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Point3d的constructor扩充内容</span></span><br><span class="line">Point3d* Point3d::Point3d(Point3d *<span class="keyword">this</span>, <span class="keyword">bool</span> __most_derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (__most_derived != <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d = __vtbl_Point3d;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point3d__Point = __vtbl_Point3d__Point;</span><br><span class="line">    <span class="keyword">this</span>-&gt;_Z = ths._z;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  Vertex3d调用Point3d和Vertex的constructor时，会把__most_derived参数设为false，于是就压制了对两个constructors中对Point constructor的调用操作。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Vertex3d* Vertex3d::Vertex3d(Vertex3d *<span class="keyword">this</span>, <span class="keyword">bool</span> __most_derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (__most_derived != <span class="literal">false</span>) </span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point3d::Point3d(<span class="literal">false</span>, x, y, z);</span><br><span class="line">        <span class="keyword">this</span>-&gt;Vertex::Vertex(<span class="literal">false</span>, x, y);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  这个策略得以保持语义正确。当定义一个<strong>Point3d</strong>时，Point3d constructor可以正确调用Point virtual base class subobject。当定义<strong>Vertex3d时</strong>时，Vertex3d constructor正确调用Point constructor。Point3d和Vertex的constructor不会对Point调用。这种把一个constructor分裂为二的做法，可以带来速度的提升。？？</p>
</li>
</ul>
<h5 id="vptr初始化语意学（The-Semantics-of-the-vptr-Initialization）"><a href="#vptr初始化语意学（The-Semantics-of-the-vptr-Initialization）" class="headerlink" title="vptr初始化语意学（The Semantics of the vptr Initialization）"></a>vptr初始化语意学（The Semantics of the vptr Initialization）</h5><ul>
<li>当我们定义PVertex object时，constructors的调用顺序是：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point(x, y);</span><br><span class="line">Point3d(x, y, z);</span><br><span class="line">Vertex(x, y, z);</span><br><span class="line">Vertex3d(x, y, z);</span><br><span class="line">PVertex(x, y, z);</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><p>假设继承体系中每个class都定义一个virtual function size()，返回class的大小。而这个继承体系中的每个constructors内含调用操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d::Point3d(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z) : _x(x), _y(y), _z(z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within Point3d::Piont3d()"</span> &lt;&lt; <span class="string">"size: "</span> &lt;&lt; size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  <strong>在一个class的constructor（和destructor）中，经由构造中的对象来调用virtual function，其函数实例是在此class中有作用的那个</strong>。如果调用操作限制必须在constructor（或destructor）中直接调用，那么答案十分明显；将每个调用以静态方式决议它，不要用到虚拟机制。如果是在Point3d constructor中，就显式调用Point3d::size()。  </p>
</li>
<li><p>如果size()之中又调用一个virtual function，这个调用也必须决议为Point3d的函数实例。因此在执行constructor时，必须限制一组virtual functions候选名单。为了控制class中有所作用的函数，编译系统需要控制住vptr的初始化和设定操作，vptr初始化操作怎么处理，得视vptr在constructor中应该在什么时候被初始化而定。 <strong>在base class constructors调用操作之后，在member iniaialization list中members初始化操作前</strong>。它解决了class中限制一组virtual functions名单的问题。如果每个constructor都一直等待其base class constructors执行完毕之后才设定对象的vptr，那么每次都能调用正确的virtual function实例。<br><font color="red">constructor的执行算法</font>通常如下：</p>
<ol>
<li><strong>在derived class constructor中，所有virtual base classes及上层base class的constructors会被调用</strong>。</li>
<li><strong>对象的vptr(s)会被初始化，指向相关virtual table(s)</strong>。</li>
<li><strong>如果有member initialization list，将在constructor内扩展开来</strong>。这在vptr被设定之后才做，以免virtual member function被调用。</li>
<li><strong>最后才执行程序员所提供的代码</strong>。<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">PVertex::PVertex(<span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z) : _next(<span class="number">0</span>), Vertex3d(x, y, z), Point(x, y)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within PVertex::PVertex()"</span> &lt;&lt; <span class="string">"size: "</span> &lt;&lt; size() &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line">PVertex* PVertex::PVertex(PVertex *<span class="keyword">this</span>, <span class="keyword">bool</span> __most__derived, <span class="keyword">float</span> x, <span class="keyword">float</span> y, <span class="keyword">float</span> z)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 条件式地调用virtual base constructor</span></span><br><span class="line">    <span class="keyword">if</span> (__most__derived != <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">this</span>-&gt;Point::Point(x, y);</span><br><span class="line">        </span><br><span class="line">    <span class="comment">// 无条件调用上一层base</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex3d::Vertex3d(x, y, z);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 将相关的vptr初始化</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_PVertex = __vtbl_PVertex;</span><br><span class="line">    <span class="keyword">this</span>-&gt;__vptr_Point__PVertex = __vtbl_Point__PVertex;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 程序员缩写的代码</span></span><br><span class="line">    <span class="keyword">if</span> (spyOn)</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"Within PVertex::PVertex()"</span> </span><br><span class="line">            &lt;&lt; <span class="string">"size: "</span></span><br><span class="line">            <span class="comment">// 经由虚拟机制调用</span></span><br><span class="line">            &lt;&lt; (*<span class="keyword">this</span>-&gt;__vptr__PVertex[<span class="number">3</span>].faddr)(<span class="keyword">this</span>)</span><br><span class="line">            &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="comment">// 传回被构造的对象</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ol>
</li>
</ul>
<ul>
<li>下面是vptr必须被设定的两种情况：<ol>
<li>当一个完整的对象被构造起来时。如果我们声明一个Point对象，则Point constructor必须设定其vptr。</li>
<li>当一个subobject constructor调用一个virtual function（不论是直接调用还是间接调用）时。<br>p218</li>
</ol>
</li>
</ul>
<h4 id="5-3-对象复制语意学-Object-Copy-Semantics"><a href="#5-3-对象复制语意学-Object-Copy-Semantics" class="headerlink" title="5.3 对象复制语意学(Object Copy Semantics)"></a>5.3 对象复制语意学(Object Copy Semantics)</h4><ul>
<li><p>当我们设计一个class，并以一个class object指定给另一个class object时，我们有三种<font color="red">选择</font>：</p>
<ol>
<li><p><strong>什么都不做，施行默认行为</strong>。</p>
</li>
<li><p><strong>提供一个explicit copy assignment operator</strong>。</p>
</li>
<li><p><strong>显式地拒绝把一个class object指定给另一个class object</strong>。</p>
<p>当选择不准将一个class object指定给另一个class object时，只要<strong>把copy assignment operator声明为priavte，并且不提供其定义</strong>就可以了。设为private，就不再允许于任何地点（除了在<font color="red">member functions</font>以及该<font color="red">class的friends之中</font>）做<font color="red">赋值</font>（<font color="green">assign</font>）操作。</p>
</li>
</ol>
</li>
<li><p>需要验证copy assignment operator的语意，利用Point class来帮助讨论：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Piont</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// ... 没有virtual function</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  对于默认行为是否足够，如果要支持的只是一个简单的拷贝操作，那么默认行为不但足够而且有效率，而且没有理由再自己提供一个copy assignment operator。<br>  如果不对Point供应一个copy assignment operator，光是以来memberwise copy，编译器不会产生实例。编译器不会产生出一个实例，因为<strong>class有了bitwise copy语义，所以implicit copy assignment operator被视为毫无用处，也不会被合成出来</strong>。copy assignment operators并不表示bitwisecopy semantics是nontrivial，只有nontrivial instances才会被合成出来。因此，对于Point class，这样的赋值操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point a, b;</span><br><span class="line">...</span><br><span class="line">a = b;</span><br></pre></td></tr></table></figure>

<p>  由bitwise copy完成，期间并没有copy assignment oeprator被调用。注意，我们还是可能提供一个copy constructor，为的是把<font color="green">name return value</font>（<font color="red">NRV</font>）优化打开。copy constructor的出现不意味着也也要提供一个copy assignment operator。</p>
</li>
<li><p>如果该operator在继承之下呢？</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">inline</span> Point&amp; Point::<span class="keyword">operator</span>=(<span class="keyword">const</span> Point &amp;p)</span><br><span class="line">&#123;</span><br><span class="line">    _x = p._x;</span><br><span class="line">    _y = p._y;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 虚拟继承</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">virtual</span> <span class="keyword">public</span> Point &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> x = <span class="number">0.0</span>, <span class="keyword">float</span> y = <span class="number">0.0</span>, <span class="keyword">float</span> z = <span class="number">0.0</span>);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 如果没有定义copy assignmnet operator，根据合成规则的第二项和第四项。合成的东西看起来这样：</span></span><br><span class="line"><span class="keyword">inline</span> Point3d&amp; Point3d::<span class="keyword">operator</span>=(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, <span class="keyword">const</span> Piont3d &amp;p)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// call base class</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(p);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// memberwise copy the derived class members</span></span><br><span class="line">    _z = p._z;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  它缺乏一个member assignment list（平行于member initialization list的东西）。缺少copy assignment list，看起来是小事，但如果没有它，编译器一般就没有办法压抑上一层base class的copy operators被调用。例如Vertex copy operator，Vertex虚拟继承自Point：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class Vertex : virtual public Point</span></span><br><span class="line"><span class="keyword">inline</span> Vertex&amp; Vertex::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    _next = v._next;</span><br><span class="line">    <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 从Point3d和Vertex派生Vertex3d，下面是其copy assignment operator</span></span><br><span class="line"><span class="keyword">inline</span> Vertex3d&amp; Vertex3d::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex3d &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point3d::<span class="keyword">operator</span>=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex::oeprator=(v);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  编译器怎么能在Point3d和Vertex的copy assignment operators中压抑Point的copy assignment operators呢？有一种方法可以保证most-derived class会（完成）virtual base class subobject的copy行为，就是在derived class的copy assignment operator函数实例的最后，显式调用oeprator：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">inline</span> Vertex3d&amp; Vertex3d::<span class="keyword">operator</span>=(<span class="keyword">const</span> Vertex3d &amp;v)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Point3d::oeprator=(v);</span><br><span class="line">    <span class="keyword">this</span>-&gt;Vertex::oeprator=(v);</span><br><span class="line">    <span class="comment">// must place this last if your compiler does</span></span><br><span class="line">    <span class="comment">// not suppress intermediate class invocations</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;Point::<span class="keyword">operator</span>=(v);</span><br><span class="line">    ... </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  它不能够省略subobjects的多重拷贝，但可以保证语意。建议尽可能不要允许一个virtual base class的拷贝操作，甚至一个比较奇怪的建议：不要在任何virtual base class中声明数据。</p>
</li>
</ul>
<h4 id="5-4-对象的效能-Object-Efficiency"><a href="#5-4-对象的效能-Object-Efficiency" class="headerlink" title="5.4 对象的效能(Object Efficiency)"></a>5.4 对象的效能(Object Efficiency)</h4><ul>
<li>测试。</li>
</ul>
<h4 id="5-5-析构语意学-Semantics-of-Destruction"><a href="#5-5-析构语意学-Semantics-of-Destruction" class="headerlink" title="5.5 析构语意学(Semantics of Destruction)"></a>5.5 析构语意学(Semantics of Destruction)</h4><ul>
<li><p>如果class没有定义destructor，那么只有在class内含的member object（抑或是class的base class）拥有destructor的情况下，编译器才会自动合成一个来。当从父类派生子类（即使是一种虚拟派生关系）时，如果没有声明一个estructor，编译器就没有必要合成一个destructor。没有任何理由说在delete一个对象之前得先讲内容清除干净。在一个对象的生命之前，没有任何class使用层面的程序操作是必要的，因此也就不需要要给destructor。  </p>
</li>
<li><p>一个由程序员定义的<font color="red"><strong>destructor被扩展的方式类似constructors被扩展的方式</strong></font>，但顺序相反：</p>
<ol>
<li><strong>destructor的函数本体现在被执行，也就是说vptr会在程序员的代码执行前被重设（reset）</strong>。</li>
<li><strong>如果class拥有member classs objects，而后者会拥有destructors，那么它们会以生命顺序的相反顺序被调用</strong>。</li>
<li><strong>如果object内含一个vptr，那么首先重设（reset）相关的vtbl。指向适当的base class的vtbl</strong>。</li>
<li><strong>如果由任何直接的（上一层）nonvirtual base classes拥有destrucotr，它们会以其声明顺序的相反顺序被调用</strong>。</li>
<li><strong>如果有任何virtual base classes拥有destructor，那么也会以原来的构造顺序的相反顺序被调用</strong>。</li>
</ol>
</li>
<li><p>跟constructor一样，对destructor的一种最佳实现策略就是维护两份destrucotr实例：</p>
<ol>
<li>一个complete object实例，先设定好vptr(s)，并调用virtual base classes destructors。</li>
<li>一个base class subobject实例：除非在destructor函数中调用一个virtual function，否则不会调用virtual base class destructors并设定vptr。</li>
</ol>
</li>
<li><p><font color="red">一个object的声明结束于其destructor开始执行之时</font>。    </p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
  <entry>
    <title>第四章 Function语意学</title>
    <url>/2020/06/12/%E7%AC%AC%E5%9B%9B%E7%AB%A0-Function%E8%AF%AD%E6%84%8F%E5%AD%A6/</url>
    <content><![CDATA[<ul>
<li><p>看以下代码,会发生什么:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Point3d obj;</span><br><span class="line">POint3d *ptr = &amp;obj;</span><br><span class="line">obj.normalize();</span><br><span class="line">ptr-&gt;normalize();</span><br><span class="line"></span><br><span class="line"><span class="comment">// declaretion</span></span><br><span class="line">Point3d Point3d::normalie() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude();</span><br><span class="line">    Point3d normal;</span><br><span class="line">    normal._x = _x/mag;</span><br><span class="line">    normal._y = _y/mag;</span><br><span class="line">    normal._z = _z/mag;</span><br><span class="line">    <span class="keyword">return</span> normal;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">float</span> Point3d::magnitude() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(_x * _x + _y * _y + _z * _z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  答案是不知道。<strong>C++支持三种类型的member function：static、nonstatic和virtual</strong>，我们蹦确定normalize()和magnitude()两函数是否为virtual或nonvirtual，但可以确定它不是static。因为</p>
<ul>
<li>它直接存取nonstatic数据。</li>
<li>它被声明为const。是的，static member functions不可能做到这两点。</li>
</ul>
</li>
</ul>
<h4 id="4-1-Member的各种调用方式"><a href="#4-1-Member的各种调用方式" class="headerlink" title="4.1 Member的各种调用方式"></a>4.1 Member的各种调用方式</h4><ul>
<li><p><strong>原始的“C with Class”只支持nonstaic member functions</strong>。它收到很多质疑：</p>
<blockquote>
<p>有一种常见的观点，认为virtual functions只不过是一种蹩脚的函数指针，没有什么用……其意思主要就是，virtual functions是一种没有效能的形式。</p>
</blockquote>
</li>
<li><p>Static member functions是最后被引入的一种函数类型。</p>
</li>
</ul>
<h5 id="Nonstatic-Member-Functions（非静态成员函数）"><a href="#Nonstatic-Member-Functions（非静态成员函数）" class="headerlink" title="Nonstatic Member Functions（非静态成员函数）"></a>Nonstatic Member Functions（非静态成员函数）</h5><ul>
<li><p>C++的设计准则之一就是：static member function至少必须和一般的nonmember function有相同的效率。然而，举个例子：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// nonmember</span></span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">magnitude3d</span><span class="params">(<span class="keyword">const</span> Point3d *_this)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(_this-&gt;_x * _this-&gt;_x + </span><br><span class="line">                _this-&gt;_y * _this-&gt;_y +</span><br><span class="line">                _this-&gt;_z * _this-&gt;_z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  乍见似乎member function比较没有效率，它经由参数取坐标，而member function却直截了当用坐标成员。然而<font color="red"><strong>实际上member function被内化为nonmember的形式</strong></font>。下面是转化步骤：</p>
<ol>
<li><p><strong>改写函数的signature（函数原型）以安插要给额外的参数到member function中，用以提供一个存取管道，使class object将此函数调用。这个额外的参数使this指针</strong>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// non-const nonstatic member</span></span><br><span class="line">Point3d Point3d::magnitude(Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span><br><span class="line"><span class="comment">// 如果function是const</span></span><br><span class="line"><span class="comment">// const nonstatic member</span></span><br><span class="line">Point3d Point3d::magnitude(<span class="keyword">const</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>将每一个对nonstatic data member的存取操作变为经由this指针来存取</strong>：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(<span class="keyword">this</span>-&gt;_x * <span class="keyword">this</span>-&gt;_x +</span><br><span class="line">                <span class="keyword">this</span>-&gt;_y * <span class="keyword">this</span>-&gt;_y +</span><br><span class="line">                <span class="keyword">this</span>-&gt;_z * <span class="keyword">this</span>-&gt;_z);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>将member function重新写成一个外部函数。函数名经过“mangling”处理，使它在程序中成为独一无二的词汇：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="title">magnitude__7Point3dFv</span><span class="params">(<span class="keyword">register</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>)</span></span>;</span><br></pre></td></tr></table></figure>


</li>
</ol>
</li>
</ul>
<ul>
<li>开章的normalize()函数会转化成以下形式，假设声明了Pointe3d copy constructor，而named returned value（NRV）的优化也施行了：  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++伪码</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">normalize__7Point3dFv</span><span class="params">(<span class="keyword">register</span> <span class="keyword">const</span> POint3d *<span class="keyword">const</span> <span class="keyword">this</span>, Point3d &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = <span class="keyword">this</span>-&gt;magniude();</span><br><span class="line">    <span class="comment">// default constructor</span></span><br><span class="line">    __result.Point3d::Point3d();</span><br><span class="line">    __result._x = <span class="keyword">this</span>-&gt;_x/mag;</span><br><span class="line">    __result._y = <span class="keyword">this</span>-&gt;_y/mag;</span><br><span class="line">    __result._z = <span class="keyword">this</span>-&gt;_z/mag;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有效率的方式</span></span><br><span class="line">Pointe3d Point3d::normalize() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude();</span><br><span class="line">    <span class="keyword">return</span> Point3d(_x/mag, _y/mag, _z/mag);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">normalize__7Point3dFv</span><span class="params">(<span class="keyword">register</span> <span class="keyword">const</span> Point3d *<span class="keyword">const</span> <span class="keyword">this</span>, Point3d &amp;__result)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">register</span> <span class="keyword">float</span> mag = <span class="keyword">this</span>-&gt;magnitude();</span><br><span class="line">    <span class="comment">// __result return value</span></span><br><span class="line">    __result.Point3d::Point3d(<span class="keyword">this</span>-&gt;_x/mag, <span class="keyword">this</span>-&gt;_y/mag, <span class="keyword">this</span>-&gt;_z/mag);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h6 id="名称的特殊处理（Name-Mangling）"><a href="#名称的特殊处理（Name-Mangling）" class="headerlink" title="名称的特殊处理（Name Mangling）"></a>名称的特殊处理（Name Mangling）</h6><ul>
<li><p>一般而言，member的名称前面会被加上class名称，形成独一无二的命名：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bar</span> &#123;</span> <span class="keyword">public</span>: <span class="keyword">int</span> ival; ... &#125;;</span><br><span class="line"><span class="comment">// ival可能为</span></span><br><span class="line"><span class="comment">// ival__3Bar</span></span><br></pre></td></tr></table></figure>

<p>  为什么这么做呢？考虑这样的派生操作:</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> :</span> <span class="keyword">public</span> Bar &#123; <span class="keyword">public</span>: <span class="keyword">int</span> ival; ... &#125;;</span><br><span class="line"><span class="comment">// Foo对象内部结合了base class和derived class两者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">int</span> ival__3Bar;</span><br><span class="line">    <span class="keyword">int</span> ival__3Foo;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  不管要处理哪个ival，通过<font color="red">name mangling</font>都能指出来。由于member functions可能被重载（overloaded），所以更需要广泛的mangling手法：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">x</span><span class="params">(<span class="keyword">float</span> newX)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span></span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 转化为：</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">X__5PointFf</span><span class="params">(<span class="keyword">float</span> newX)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">X__5PointFv</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><strong>把参数和函数名称编码在一起，编译器就在不同的编译模块之间达成了一种有限形式的<font color="red">类型检验</font></strong>。如果两个实例拥有独一无二的name mangling，任何不正确的调用操作在链接时会因无法resolved而失败。这就是确保<font color="red">类型安全的链接行为</font>（<font color="green">type-safe linkage</font>）。它只能捕捉到函数的标记（<strong>signature，函数名称+参数个数+参数类型）错误</strong>，而返回类型错误检查不出来。而一种demangling工具，用来拦截名称并转换回去。它向使用者隐藏了内部名称，使得出错时能得到一个友好的信息返回。</li>
</ul>
<h5 id="Virtual-Member-Functions（虚拟成员函数）"><a href="#Virtual-Member-Functions（虚拟成员函数）" class="headerlink" title="Virtual Member Functions（虚拟成员函数）"></a>Virtual Member Functions（虚拟成员函数）</h5><ul>
<li><p><strong>如果normalize()是</strong>virtual member function，那么以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ptr-&gt;normalie();</span><br></pre></td></tr></table></figure>

<p>  会被转化为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*ptr-&gt;vptr[<span class="number">1</span>])(ptr);</span><br></pre></td></tr></table></figure>

<p>  其中：</p>
<ul>
<li><strong>vptr由编译器产生，一个指向vtbl的指针</strong>。</li>
<li><strong>1是索引值</strong>。</li>
<li><strong>ptr是this指针</strong>。</li>
</ul>
</li>
<li><p>类似道理，<strong>如果magnitude()也是</strong>virtual function，它在normalize()之中的调用操作被转换如下：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// register float mag = magnitude();</span></span><br><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = (*<span class="keyword">this</span>-&gt;vptr[<span class="number">2</span>])(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  此时，由于Point3d::magnitude()是在Point3d::normalize()中被调用的，而后者已经由虚拟机之而决议（reslove）妥当，<font color="red">显式的调用Point3d实例会比较有效率</font>，并因此压制由于虚拟机之而产生的不必要重复调用操作：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// explicitly invocation</span></span><br><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = Point3d::magnitude();</span><br></pre></td></tr></table></figure>

<p>  <strong>如果magnitude()声明为inline函数，会更有效率。因为使用class scope operator显式调用virtual function，resolve方式和nonstatic member function一样</strong>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">register</span> <span class="keyword">float</span> mag = magnitude__7Point3dFv(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  这时对于以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Point3d obj;</span></span><br><span class="line">obj.normalize();</span><br></pre></td></tr></table></figure>

<p>  如果编译器转化为：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*obj.vptr[<span class="number">1</span>])(&amp;obj);</span><br></pre></td></tr></table></figure>

<p>  <strong>虽然语义正确，却没有必要</strong>。所以上述经由obj调用的函数实例只可以是<font color="red">Point3d:normalize()</font>。”经由class object调用virtual function”，这种操作总是被编译器像对待一般nonstatic member function一样resolved：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">normalize__7Point3Fv(&amp;obj);</span><br></pre></td></tr></table></figure>

<p>  这项工程的另一个利益是，<font color="red"><strong>virtual function的inline函数实例可以被扩展（expanded）开来，因而提供极大的效率利益</strong></font>。</p>
</li>
</ul>
<h5 id="Static-Member-Functions（静态成员函数）"><a href="#Static-Member-Functions（静态成员函数）" class="headerlink" title="Static Member Functions（静态成员函数）"></a>Static Member Functions（静态成员函数）</h5><ul>
<li><p>如果Point3d::normalize()是一个static member function，下面两个调用的转化：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">obj.normalize();</span><br><span class="line">ptr-&gt;normalize();</span><br><span class="line"><span class="comment">// 将被转化为一般的nonmember函数调用</span></span><br><span class="line"><span class="comment">// obj.normalize();</span></span><br><span class="line">normalize__7Point3dSFv();</span><br><span class="line"><span class="comment">// ptr-&gt;nomalize();</span></span><br><span class="line">normalize__7Point3dSFv();</span><br></pre></td></tr></table></figure>

<p>  在引进static member functions之前，你很少看到这种怪异写法：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">((Point3d*)<span class="number">0</span>)-&gt;object_count();</span><br></pre></td></tr></table></figure>

<p>  在引进<font color="red">static member functions</font>之前，C++语言要求member functions必须<strong>经由class的object来调用</strong>。而实际上，<strong>只有当一个或多个nonstatic data members在member function中被直接存取时，才需要class object</strong>。如果没有任何一个members被直接存取，就不需要this指针，也就不需要通过一个class object来调用member function。<br>  这一来存取static data members时产生了一些不规则性。如果class的static data member声明为nonpublic，那么就必须提供member functions来存取member。这时很显然，虽然补考class object来存取static member，但<strong>存取函数却得绑定一个class object上</strong>。<br>  独立于class object之外的存取操作很重要，尤其在没有class object存在的情况。<font color="red"><strong>程序方法上的解决之道</strong></font>是把0强制转换为一个class指针：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">object_count((Point3d*)<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>  <font color="red"><strong>至于语言层面上的解决之道</strong></font>，是引进的static member functions。<font color="red">它的主要特性是没有this指针</font>。以下是次要特性：</p>
<ul>
<li><strong>它不能直接存取class中的nonstatic members</strong>。</li>
<li><strong>它不能被声明为const、volatile或virtual</strong>。</li>
<li><strong>它不需要经由class object才被调用——虽然大部分时候它是这样被调用的</strong>。</li>
</ul>
</li>
<li><p>如果去一个static member funciton的地址，获得的将是在内存中的位置，也就是其地址。由于static member function没有this指针，所以地址类型不是一个指向class member funciton的指针，而是一个<font color="red">nonmember函数指针</font>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">&amp;Point3d::object_count();</span><br><span class="line"><span class="comment">// 会得到数值，类型是</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">int</span> <span class="params">(*)</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 而不是</span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="title">int</span> <span class="params">(Point3d::*)</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p>  因为static member function缺乏this指针，所以差不多等同于nonmember function。它有个意想不到的好处：<strong>成为callback函数</strong>。</p>
</li>
</ul>
<h4 id="4-2-Virtuar-Member-Functions（虚拟成员函数）"><a href="#4-2-Virtuar-Member-Functions（虚拟成员函数）" class="headerlink" title="4.2 Virtuar Member Functions（虚拟成员函数）"></a>4.2 Virtuar Member Functions（虚拟成员函数）</h4><ul>
<li><p>我们已经知道了virtual function的一般实现模型：每个class中有个vtbl，其中包含着virtual function的地址，而每个object有vptr，指向vtbl的存在。</p>
</li>
<li><p>为了支持virtual function机制，对于多态对象必须有某种形式上的<font color="red">执行期类型判断法</font>（<font color="green">runtime type resolution</font>）。也就是下面调用需要ptr在执行期有某些信息，才能找到并调用z()：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ptr-&gt;z();</span><br></pre></td></tr></table></figure>

<p>  这份信息不能加在ptr身上，它增加了空间负担，即使不使用多态；第二它打断了与C程序间的链接兼容性。我们需要一个更好的规范，一个以class的使用为基础，而不在乎关键词是class或struct的规范。并且必须在<font color="red">执行期多态</font>（<font color="green">runtime polymorphism</font>）时才需要这份信息</p>
</li>
<li><p>C++中多态表示以一个public base class的指针（reference），寻址出一个derived class object的意思。经由指针，可以在程序中任何地方采用public derived类型，这种多态形式是<font color="red">消极的</font>（<font color="green">passive</font>），可以在编译期完成，除了virtual base class的情况。当指出的对象被使用时，才变成<font color="red">积极的</font>（<font color="green">active</font>）。</p>
</li>
<li><p>在<font color="green">runtime type identification</font>（<font color="red">RTTI</font>）性质在1993年被引入前，C++对于积极多态（active polymorphsim）的唯一支持，<strong>就是对virtual function call的resolution操作。有了RTTI，能够在执行期查询一个多态的pointer或多态的reference了</strong>：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 积极多态的例子</span></span><br><span class="line">ptr-&gt;z();</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">if</span> (Point3d *p3d = <span class="keyword">dynamic_cast</span>&lt;Point3d*&gt;(ptr))</span><br><span class="line">    <span class="keyword">return</span> p3d-&gt;_z;</span><br></pre></td></tr></table></figure>

<p>  z()是一个virtula function。是什么信息让我们在执行期调用正确呢？，我们需要知道：</p>
<ul>
<li><p><strong>ptr所指对象的真实类型</strong>。</p>
</li>
<li><p><strong>z()实例的位置，以便能够调用它</strong>。</p>
<p>实现上，可以在class object身上增加两个members：</p>
</li>
</ul>
<ol>
<li><p><strong>一个字符串或数字</strong>，表示class的类型；</p>
</li>
<li><p><strong>一个指针</strong>。指向表格，表格中有个virtual function的执行期地址。</p>
<p>表格中的virtual functions地址是怎么建构起来的呢？<strong>virtual function（可由object被调用）在编译期获知</strong>。这个地址是固定的，执行期不增添也不替换。完全由编译器掌控，不需要执行期介入。而<strong>执行期只是备妥了函数地址</strong>，并未被找到。找到那些地址。两个步骤完成任务：</p>
</li>
<li><p><font color="red"><strong>为了找到表格，class object被安插了编译器内部产生的指针，指向该表格</strong></font>。</p>
</li>
<li><p><font color="red"><strong>为了找到函数地址，每个virtual function被指派为一个表格索引值</strong></font>。</p>
<p>以上的工作都由编译器完成。<font color="red"><strong>执行期要做的，只是在特定的virtual table slot中激活virtual funciton</strong></font>。  </p>
<p>一个class只有一个vtbl。每个table内含对应object的active virtual functions函数实例的地址，这些active virtual functions包括：</p>
</li>
</ol>
<ul>
<li><strong>class所定义的函数实例</strong>。它会overriding存在的base class virtual function函数实例。</li>
<li><strong>继承自base class的函数实例</strong>。这是在derived class决定不改写virtual function时才出现的情况。</li>
<li><strong>一个pure_virtual_called()函数实例</strong>。</li>
</ul>
</li>
<li><p>每个virtula function都被指派一个固定的索引值：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">virtual</span> ~Point();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Point&amp; <span class="title">mult</span><span class="params">(<span class="keyword">float</span>)</span> </span>= <span class="number">0</span>; <span class="comment">// puree virtual function</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">x</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">y</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    Point(<span class="keyword">float</span> x = <span class="number">0.0</span>);</span><br><span class="line">    <span class="keyword">float</span> _x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  virtual destructor被指派slot 1，而mult()被指派slot2，y()被指派slot3，z()被指派slot4。在单一继承体系中，virtual<br>  fucntion机制行为十分良好，有效率且容易塑造出模型。而在多重继承或虚继承中，对virtual functinos的支持就没那么好了。</p>
</li>
</ul>
<h5 id="多重继承下的Virtual-Functions"><a href="#多重继承下的Virtual-Functions" class="headerlink" title="多重继承下的Virtual Functions"></a>多重继承下的Virtual Functions</h5><ul>
<li><p>多重继承中支持virtual functions，<font color="red">复杂度围绕在<strong>第二个及后继base class</strong>身上，以及<strong>”必须在执行期调整this指针“</strong>这一点</font>:</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Derived -- public --- Base1</span><br><span class="line">Derived -- public --- Base2</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base1</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Base1();</span><br><span class="line">    virtula ~Base1();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">speakClearly</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Base1* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Base1;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base2</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Base2();</span><br><span class="line">    <span class="keyword">virtual</span> ~Base2();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Base2* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Base2;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span> :</span> <span class="keyword">public</span> Base1, <span class="keyword">public</span> Base2 &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Derived();</span><br><span class="line">    <span class="keyword">virtual</span> ~Derived();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Derived* <span class="title">clone</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> data_Derived;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>


</li>
</ul>
<ul>
<li><p>它的困难度主要体现在Base2 subobject身上，有三个问题需要解决：*<font color="red">（1）virtual destructor*</font>，<font color="red"><em>（2）被继承的Base2::mumble()</em></font>，<font color="red"><em>（3）clone()函数实例</em></font>。</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base2 *pbase2 = nwe Derived;</span><br><span class="line"><span class="comment">// 编译时期代码</span></span><br><span class="line"><span class="comment">// 转移以支持第二个base class</span></span><br><span class="line">Derived *temp = <span class="keyword">new</span> Derived;</span><br><span class="line">Base2 *pbase2 = temp ? temp + <span class="keyword">sizeof</span>(Base1) : <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>  如果没有这样的调整，任何非多态的应用都将失败：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pbase2被指定一个Derived对象，这也应该没有问题</span></span><br><span class="line">pbase2-&gt;data_Base2;</span><br><span class="line"><span class="comment">// 当程序员要删除pbase2所指的对象时</span></span><br><span class="line"><span class="comment">// 需要调用正确的virtual destructor函数实例，pbase2可能需要调整</span></span><br><span class="line"><span class="keyword">delete</span> pbase2;</span><br></pre></td></tr></table></figure>

<p>  一般规则时，<strong>经由第二或后继base class的指针（reference）来调用derived class virtual funciton。其所连带的必要this指针调整操作，必须在执行期完成</strong>。也就是offset的大小，offset加到this指针上的程序代码，必须由编译器在某个地方插入。</p>
<blockquote>
<p>cfont编译器中的方法是将vtbl加大，容纳所需的this指针，每个virtual table slot不再是指针，而是一个集合体，内含<font color="red">offset</font>以及地址：</p>
</blockquote>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(*pbase2-&gt;vptr[<span class="number">1</span>])(pbase2);</span><br><span class="line"><span class="comment">// 改变为</span></span><br><span class="line">(*pbase2-&gt;vptr[<span class="number">1</span>].faddr)(pbase2 + pbase2-&gt;vptr[<span class="number">1</span>].offset);</span><br></pre></td></tr></table></figure>

<p>  其中faddr内含virtual function地址，offset内含this指针调整值。这个做法的缺点是，不管什么virtual function都得进行offset调整。<br>  比较有效率的解决方法是利用所谓的thunk（是一段assembly代码）：<font color="red">（1）适当的offset调整this指针</font>，<font color="red">（2）跳到virtual function去</font>，如：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">pbase2_dtor_thunk:</span><br><span class="line">    <span class="keyword">this</span> += <span class="keyword">sizeof</span>(base1);</span><br><span class="line">    Derived::~Derived(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>  Thunk技术允许vtbl slot继续内含简单指针，因此空间上不需要任何负担。slots的地址直接指向virtual function，也可以指向相关的thunk。</p>
</li>
<li><p>调整this指针的的第二个额外负担就是，由于<font color="red">（1）经由derived class（或base class）调用</font>，<font color="red">（2）经由第二（或后继）base class调用</font>，同一函数在vtbl可能需要多次对应的slots：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base1 *pbase1 = <span class="keyword">new</span> Derived;</span><br><span class="line">Base2 *pbase2 = <span class="keyword">new</span> Derived;</span><br><span class="line"><span class="keyword">delete</span> pbase1;</span><br><span class="line"><span class="keyword">delete</span> pbase2;</span><br></pre></td></tr></table></figure>

<p>  两个相同的derived destructor，但需要不同的vtbl slots：</p>
<ol>
<li>pbase1不需要调整this指针。vtbl slot需放置真正destructor地址。</li>
<li>pbase2需要调整this指针。vtbl slot需相关的thunk地址。  </li>
</ol>
<p>  <strong>多重继承下，derived class内含n-1个额外的vtbl，n表示上一层base classes的个数</strong>。对于本例的Derived而言，会有两个vtbl被编译器产生出来：</p>
<ol>
<li><p><strong>一个主要实例，与Base1共享</strong>。</p>
</li>
<li><p><strong>一个次要实例，与Base2有关</strong>。</p>
<p>用以支持一个clas拥有多个vtbl的传统方法是，将每个tables以外部对象的形式产生出来，并给予独一无二的名称：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vtbl__Derived;          <span class="comment">// 主</span></span><br><span class="line">vtbl__Base2__Derived;   <span class="comment">// 次</span></span><br></pre></td></tr></table></figure>

<p>于是当你将Derived对象地址指定给<font color="red"><strong>Base1指针或Derived指针时</strong></font>，被处理的vtbl时主要表格vtbl__Derived。当讲Derived对下给你地址指定给<font color="red"><strong>Base2指针时</strong></font>，被处理的vtbl时次要表格vtbl__Base2_Derived。</p>
</li>
</ol>
</li>
<li><p>由于<font color="red">执行期链接器</font>（<font color="green">runime linkers</font>）的降临（可以支持动态共享函数库），符号名称的链接可能变得非常缓慢。为了调节执行期编译器的效率，Sun编译器将多个vtbl连锁为一个：<strong>指向次要表格的指针，可由主要表格表格名称加上一个offset获得，这样的策略下，每个class只有一个具名的vtbl</strong>。</p>
</li>
<li><p>有三种情况，第二或后继的base class会影响virtual functions的支持。</p>
<ol>
<li><font color="red"><strong>通过一个指向第二个base class的指针，调用derived class virtual function</strong></font>。</li>
<li><font color="red"><strong>通过一个指向derived class的指针，调用第二个base class中的一个继承而来的virtual funciton</strong></font>。</li>
<li><font color="red"><strong>允许一个virtual function的返回值类型有所变化，可能是base type，也可能是publicly derived type</strong></font>。这一点经由Derived::clone()函数实例来说明。clone函数的Derived版本传回一个Derived class指针，默默地改写了它们两个base class函数实例。当通过指向第二个base class的指针来调用clone()时，this指针的offset问题诞生了：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Base2 *pb1 = <span class="keyword">new</span> Derived;</span><br><span class="line"><span class="comment">// 调用Derived* Derived::clone()</span></span><br><span class="line"><span class="comment">// 返回值必须被调整，以指向Base2 subobject</span></span><br><span class="line">Base2 *pb2 = pb1-&gt;clone();</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p>当函数被认为足够小的时候，Sun编译器会提供一个split functions技术，以相同的算法产生两个函数。这样不论通过Base1指针还是通过Derived指针调用函数，都不需要调整返回值，而通过Base指针所调用的时另一个函数，并在返回前，为指针加上必要的offset。如果函数不小，会给函数中其中一个进入点，进入点需要三个指令。</p>
</li>
<li><p>thunk则是函数一开始先（1）调整this指针，然后才（2）执行程序员所写的函数码；无需调整函数调用操作。</p>
</li>
<li><p>Microsoft用adderss points来取代thunk策略，即overriding function期待获得的是引入该virtual function的class的地址，这就是函数的address point。</p>
</li>
</ul>
<h5 id="虚拟继承下的Virtual-Functinos"><a href="#虚拟继承下的Virtual-Functinos" class="headerlink" title="虚拟继承下的Virtual Functinos"></a>虚拟继承下的Virtual Functinos</h5><ul>
<li><p>考虑下面的virtual base class派生体系：</p>
  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">graph BT</span><br><span class="line">Point2d --- Point3d</span><br></pre></td></tr></table></figure>

  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point2d</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point2d(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    <span class="keyword">virtual</span> ~Point2d();</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mumble</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _x, _y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point3d</span> :</span> <span class="keyword">public</span> virtula Point2d &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Point3d(<span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>, <span class="keyword">float</span> = <span class="number">0.0</span>);</span><br><span class="line">    ~Point3d();</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">z</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">float</span> _z;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  当一个virtual base class从另一个virtual base class派生而来，并且两者都支持virtual functions和nonstatic data members时，编译器对于virtual base class的支持简直就像进了了迷宫一样。所以建议是，<font color="red"><strong>不要再一个virtual base class中声明nonstatic data members</strong></font>。如果这么做，你会距离复杂的深渊愈来愈近。</p>
</li>
</ul>
<h4 id="4-3-函数的效能"><a href="#4-3-函数的效能" class="headerlink" title="4.3 函数的效能"></a>4.3 函数的效能</h4><ul>
<li>下面这组测试，在编译器上计算两个3D点，其中用到一个nonmember friend function，一个member function，以及一个virtual member function。p170</li>
</ul>
<h4 id="4-4-指向Member-Function的指针（Pointer-toMember-Functions）"><a href="#4-4-指向Member-Function的指针（Pointer-toMember-Functions）" class="headerlink" title="4.4 指向Member Function的指针（Pointer-toMember Functions）"></a>4.4 指向Member Function的指针（Pointer-toMember Functions）</h4><ul>
<li><p>去一个nonstatic data member 的地址，得到的结果是该<font color="red">member在class布局中的bytes位置（再+1）</font>。可以想象它是一个不完整的值，它需要被绑定于某个class object的地址上，才能够被存取。<br>  取一个nonstatic member function的地址，如果该函数是nonvirtual，得到的结果是它在<font color="red">内存中真正的地址</font>。然而也是不完全的，也需要绑定于某个class object的地址上，才能够通过它调用函数。回顾一下：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> (Point::*pmf)();</span><br><span class="line"><span class="comment">// 定义</span></span><br><span class="line"><span class="keyword">double</span> (Point::*coord)() = &amp;Point::x;</span><br><span class="line">coord = &amp;Point::y;</span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line">(origin.*coord)();</span><br><span class="line">(ptr-&gt;*coord)();</span><br><span class="line"><span class="comment">// 转化</span></span><br><span class="line">(coord)(&amp;origin);</span><br><span class="line">(coord(ptr);</span><br></pre></td></tr></table></figure>

<p>  <font color="red">指向member function的指针的声明语法，以及指向member selection运算符的指针，作用是作为this指针的空间保留着。这就是为什么static member function（没有this指针）的类型是”函数指针“，而不是指向member function的指针的原因。</font>  </p>
<p>  使用一个member function指针，如果并不用于virtual functions、virtual base class或multiple base classes等情况的画，并不会比使用一个nonmember function指针的成本更高。上述三种情况对于member function指针的类型及调用都太过复杂。对于没有以上情况的class而言，编译器可以为它们提供<strong>相同的效率</strong>。</p>
</li>
</ul>
<h5 id="支持“指向Virtual-Member-Functions”的指针"><a href="#支持“指向Virtual-Member-Functions”的指针" class="headerlink" title="支持“指向Virtual Member Functions”的指针"></a>支持“指向Virtual Member Functions”的指针</h5><ul>
<li><p>考虑下面程序片段：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">float</span> (Point::*pmf)() = &amp;Point::z;</span><br><span class="line">Point *ptr = <span class="keyword">new</span> Point3d;</span><br></pre></td></tr></table></figure>

<p>  pmf，一个指向member function的指针，被设值为Point::z()（一个virtual function）的地址。如果由ptr调用z()，被调用的是Point3d::z()，但如果从pmf间接调用z()，正常运行吗？yes。<br>  对于一个virtual function取其地址，所能获得的只是一个索引值。对于一个指向member function的指针<font color="red">评估求值</font>（<font color="green">evaluated</font>），会因为该值由两种意义而复杂化，调用也有别于常规，<code>float (Point::*pmf)();</code>必须允许此函数能够寻址出nonvirtual x()和virtual z()两个member functions：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 两者都可以指定给pmf</span></span><br><span class="line"><span class="keyword">float</span> Point::x() &#123; <span class="keyword">return</span> _x; &#125;</span><br><span class="line"><span class="keyword">float</span> Point::z() &#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>  只不过其中一个代表<font color="red">内存地址</font>，另一个代表<font color="red">vtbl中的索引值</font>。为了使pmf能够（1）持有两种数值，（2）能区别代表内存地址还是vtbl中的索引值：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// true is non-virtual invocation</span></span><br><span class="line"><span class="comment">// false is virtual invocation</span></span><br><span class="line">(((<span class="keyword">int</span>)pmf) &amp; ~<span class="number">127</span>) ? (*pmf)(ptr) : (*ptr-&gt;vptr[(<span class="keyword">int</span>)pmf](ptr));</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="在多重继承之下，指向Member-Functions的指针"><a href="#在多重继承之下，指向Member-Functions的指针" class="headerlink" title="在多重继承之下，指向Member Functions的指针"></a>在多重继承之下，指向Member Functions的指针</h5><ul>
<li><p>为了让member functions的指针能够支持多重继承和虚拟继承，设计了一个结构体：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">mptr</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> delta;</span><br><span class="line">    <span class="keyword">int</span> index;</span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        ptrtofunc   faddr;</span><br><span class="line">        <span class="keyword">int</span>         v_offset;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>  <strong>index和faddr分别持有vtbl slot和nonvirtual member function address（当index不指向vtbl时，设为-1）</strong>，像以下调用：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">(ptr-&gt;*pmf)();</span><br><span class="line"><span class="comment">// 变成</span></span><br><span class="line">(pmf.index &lt; <span class="number">0</span>)</span><br><span class="line">? <span class="comment">// non-virtual invocation</span></span><br><span class="line">(*pm.faddr)(ptr)</span><br><span class="line">: <span class="comment">// virtual invocation</span></span><br><span class="line">(*ptr-&gt;vptr[pmf.index](ptr));</span><br></pre></td></tr></table></figure>

<p>  这个方法受到的批评是，每个调用操作都得付出成本。Microsoft把检查拿掉，导入一个vcall thunk，它会选出并调用相关vtbl中的slot。  </p>
<p>  这个结构体的另一个副作用是，当传递一个不变值的指针给member function时，需要产生临时变量：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> Point3d <span class="title">foo</span><span class="params">(<span class="keyword">const</span> Point3d&amp;, Point3d (Point3d::*)())</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">(<span class="keyword">const</span> Point3d &amp;p)</span> </span>&#123;</span><br><span class="line">    Point3d pt = foo(p, &amp;Point3d::normal);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// &amp;Point3d::normal value</span></span><br><span class="line">&#123;<span class="number">0</span>, <span class="number">-1</span>, <span class="number">10727417</span>&#125;</span><br><span class="line"><span class="comment">// 将变成</span></span><br><span class="line">__mptr temp = &#123;<span class="number">0</span>, <span class="number">-1</span>, <span class="number">10727417</span>&#125;</span><br><span class="line">foo(p, temp);</span><br></pre></td></tr></table></figure>


</li>
</ul>
<pre><code>回到开头那个结构体。delta字段表示this指针的offset值，而v_offset字段放的是一个virtual base class的vptr位置。如果vptr被编译器放在class对象起头处，这个字段就没必要了。它只在多重继承或虚拟继承情况下才有必然性。有些编译器根据不同的class特征提供多种memer functions的指向方式。如Microsoft：
1. **单一继承实例**（有vcall thnuk地址或是函数地址）；
2. **多重继承实例**（有faddr和delta两个members）；
3. **虚拟继承实例**（4个members）。</code></pre><h5 id="”指向Member-Functions之指针的效率"><a href="#”指向Member-Functions之指针的效率" class="headerlink" title="”指向Member Functions之指针的效率"></a>”指向Member Functions之指针的效率</h5><ul>
<li>又是测试。p180</li>
</ul>
<h4 id="4-5-Inline-Functions"><a href="#4-5-Inline-Functions" class="headerlink" title="4.5 Inline Functions"></a>4.5 Inline Functions</h4><ul>
<li><p>一个Point class的加法运算符的可能实现内容：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line">    <span class="keyword">friend</span> oint <span class="keyword">operator</span>+(<span class="keyword">const</span> Point&amp;, <span class="keyword">const</span> Point&amp;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Point <span class="keyword">operator</span>+(<span class="keyword">const</span> Point &amp;lhs, <span class="keyword">const</span> Point &amp;rhs)</span><br><span class="line">&#123;</span><br><span class="line">    Point new_pt;</span><br><span class="line">    new_pt._x = lhs._x + rhs._x;</span><br><span class="line">    new_pt._y = lhs._y + rhs._y;</span><br><span class="line">    <span class="keyword">return</span> new_pt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// void Point::x(float new_x) &#123; _x = new_x; &#125;</span></span><br><span class="line"><span class="comment">// float Point::x() &#123; return _x; &#125;</span></span><br><span class="line">new_pt.x(lhs.x() + rhs.x());</span><br></pre></td></tr></table></figure>

<p>  以上通过将存取函数声明为inline，不但可以保持直接存取的高效率，而且加法运算符不再需要被声明为Point的friend。<br>  然而不能够强迫任何函数都变成inline。<font color="red">关键词inline（或者class declaration中的member function或friend function的定义）只是一项请求</font>。如果请求被接受，编译器就认为它可以用一个表达式（expression）合理地将这个函数扩展开来。cfront有一套复杂的测试方法，通常是用来计算assignments、function calls、virtual function calls等操作的次数。<strong>每个expression种类都有一个权值</strong>，<font color="red">inline函数的复杂度就以这些操作的总和来决定</font>。</p>
</li>
<li><p>一般处理一个inline函数有两个阶段：</p>
<ol>
<li><strong>分析函数定义，决定函数的intrinsic inline ability</strong>。如果函数因<font color="red">复杂度</font>或<font color="red">构建问题</font>不可成为inline，它会被转为一个static函数，并在被编译模块内产生对应的函数定义。在一个支持模块个别编译的环境中，编译器几乎没有什么权宜之计。理想情况下，链接器会将被产生出来的重复东西清理掉，但调试信息不会。UNIX环境的strip命令可以。</li>
<li><strong>真正的inline函数扩展操作是在调用的那一点上，这会带来参数的求值操作（ealuation）以及临时性对象的管理</strong>。</li>
</ol>
</li>
<li><p>在将要扩展的点上，cfront编译器中，<strong>inline函数如果只有一个表达式，而又有后续操作，则不会扩展开来</strong>。</p>
</li>
</ul>
<h5 id="形式参数（Formal-Arguments）"><a href="#形式参数（Formal-Arguments）" class="headerlink" title="形式参数（Formal Arguments）"></a>形式参数（Formal Arguments）</h5><ul>
<li><strong>inline扩展期间每个形式参数都被对应的实际参数取代。如果实际参数是一个常量表达式（constant expression），可以在替换之前完成求值操作（evaluations）；如果是个有副作用的表达式，那么需要引入临时性对象；如果既不是常量表达式，也不是带有副作用的表达式，那么就直接代替它。</strong>  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">min</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> j)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> i &lt; j ? i : j;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">bar</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> minval;</span><br><span class="line">    <span class="keyword">int</span> val1 = <span class="number">1024</span>;</span><br><span class="line">    <span class="keyword">int</span> val2 = <span class="number">2048</span>;</span><br><span class="line">    minval = min(val1, val2);       <span class="comment">// 1</span></span><br><span class="line">    minval = min(<span class="number">1024</span>, <span class="number">2048</span>);       <span class="comment">// 2</span></span><br><span class="line">    minval = min(foo(), bar() + <span class="number">1</span>); <span class="comment">// 3</span></span><br><span class="line">    <span class="keyword">return</span> minval;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line">minval = val1 &lt; val2 ? val1 : val2; <span class="comment">// 1</span></span><br><span class="line">minval = <span class="number">1024</span>;                      <span class="comment">// 2</span></span><br><span class="line"><span class="comment">// 3</span></span><br><span class="line"><span class="keyword">int</span> t1;</span><br><span class="line"><span class="keyword">int</span> t2;</span><br><span class="line">minval = (t1 = foo()), (t2 = bar() + <span class="number">1</span>), t1 &lt; t2 ? t1 : t1;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h5 id="局部变量（Local-Variables）"><a href="#局部变量（Local-Variables）" class="headerlink" title="局部变量（Local Variables）"></a>局部变量（Local Variables）</h5><ul>
<li><p>如果在inline定义中加入局部变量：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">min</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> j)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> minval = i &lt; j ? i : j;</span><br><span class="line">    <span class="keyword">return</span> minval;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> local_var;</span><br><span class="line">    itn minval;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    minval = min(val1, val2);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> local_var;</span><br><span class="line">    <span class="keyword">int</span> minval;</span><br><span class="line">    <span class="comment">// inlnie函数的局部变量处以mangling操作</span></span><br><span class="line">    <span class="keyword">int</span> __min_lv-minval;</span><br><span class="line">    minval = </span><br><span class="line">        (__min_lv-minval = </span><br><span class="line">            val1 &lt; val2 ? val1 : val2),</span><br><span class="line">            __min_lv_minval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  一般inline函数中的每个局部变量都必须放在<font color="red">封闭的区段</font>中，拥有独一无二的名称。因为如果inline函数以<font color="red">分离的多个式子</font>（<font color="green">discrete statements</font>）被扩展多次，那么只需要一组局部变量，就可以重复使用。<br>  inline函数中的局部变量，加上有副作用的参数，可能会<strong>导致大量临时性对象的产生</strong>。特别是如果以单一表达式（expression）被扩展多次的话：</p>
  <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">minval = min(avl1, val2) + min(foo(), foo()+<span class="number">1</span>);</span><br><span class="line"><span class="comment">// 扩展</span></span><br><span class="line"><span class="comment">// 为局部变量产生临时变量</span></span><br><span class="line"><span class="keyword">int</span> __min_lv_minval_00;</span><br><span class="line"><span class="keyword">int</span> __min_lv_minval_01;</span><br><span class="line"><span class="comment">// 为放置副作用产生临时变量</span></span><br><span class="line"><span class="keyword">int</span> t1;</span><br><span class="line"><span class="keyword">int</span> t2;</span><br><span class="line">minval = ((__min_lv_minval_00 = </span><br><span class="line">            val1 &lt; val2 ? val1 : val2),</span><br><span class="line">            __min_lv_minval_00)</span><br><span class="line">          +</span><br><span class="line">          ((__min_lv_minval_01 = (t1 = foo()),</span><br><span class="line">            (t1 = foo() + <span class="number">1</span>),</span><br><span class="line">            t1 &lt; t2 ? t1 : t2),</span><br><span class="line">            __min_lv_minval__01);</span><br></pre></td></tr></table></figure>

<p>  Inline函数对封装提供了必要的支持，可以有效存取class的nonpublic数据。它同时是C程序中大量使用#define的一个安全代替品——特别如果宏中的参数有副作用的话。然而被调用太多次的话，会产生大量的扩展码，使<strong>程序大小暴涨</strong>。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>2019.06</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>C++对象模型</tag>
      </tags>
  </entry>
</search>
